// ============================================================
// 插件：INS
// 版本：1.2.5（User主页尺寸入口调整）
// 结构：Roche plugin.js + manifest.json（适合 GitHub Gist 部署）
// ============================================================
(function() {
  'use strict';

  // v1.07.8 intentionally uses fresh Roche identities. Some mobile builds keep
  // the already-registered app object when a TXT with the same ids is imported,
  // which leaves the old Home renderer alive even after the file is replaced.
  const PLUGIN_ID = 'nini-ins-roche-v1079';
  const APP_ID = 'nini-ins-home-v1079';
  const VERSION = '1.2.5';
  const ICON_URL = 'https://imgbed.heliar.top/i/x9grO6G8Z9llF1CC_free-instagram-icon-SnNvLphykLIU.webp';

  const DM_SHARE_V120_STYLE = `
/* ===== INS v1.2.5 DM Share ===== */
.dm-share-card-v120{width:min(262px,82vw);border-radius:18px;overflow:hidden;border:1px solid #e5e7ec;background:#fff;cursor:pointer;box-shadow:0 3px 14px rgba(0,0,0,.05);touch-action:manipulation}
.dm-share-card-v120.feed{color:#111;background:#fff}.dm-share-card-v120.reel{color:#fff;background:#0a0a0b;border-color:#222}
.dm-share-card-v120 .dm-share-media-v120{width:100%;aspect-ratio:1/1;display:flex;align-items:center;justify-content:center;overflow:hidden;text-align:center;font-size:11px;line-height:1.55;padding:16px;background:#f2f3f5;color:#676c74}
.dm-share-card-v120.reel .dm-share-media-v120{aspect-ratio:4/5;background:#050506;color:#f5f5f6;padding:20px;white-space:pre-wrap}
.dm-share-card-v120 .dm-share-media-v120 img{width:100%;height:100%;object-fit:cover;display:block}
.dm-share-card-v120 .dm-share-copy-v120{padding:10px 11px 12px}.dm-share-card-v120.reel .dm-share-copy-v120{background:#0a0a0b}
.dm-share-kicker-v120{font-size:9px;color:#9a9ea5;margin-bottom:5px}.dm-share-card-v120.reel .dm-share-kicker-v120{color:#8d8e93}
.dm-share-author-v120{font-size:12px;font-weight:850;line-height:1.25}.dm-share-caption-v120{margin-top:5px;font-size:10px;line-height:1.45;color:#4e535a;display:-webkit-box;-webkit-line-clamp:3;-webkit-box-orient:vertical;overflow:hidden}.dm-share-card-v120.reel .dm-share-caption-v120{color:#ececef}
.dm-share-card-translate-toggle-v124,.dm-share-detail-caption-translate-toggle-v124,.dm-share-comment-translate-toggle-v124{margin-top:6px;border:0;background:none;padding:0;color:#70757d;font-size:10px;font-weight:700;cursor:pointer;touch-action:manipulation}.dm-share-card-v120.reel .dm-share-card-translate-toggle-v124,.dm-share-detail-v120.reel .dm-share-detail-caption-translate-toggle-v124,.dm-share-detail-v120.reel .dm-share-comment-translate-toggle-v124{color:#a4a7ad}
.dm-share-card-translation-v124,.dm-share-detail-caption-translation-v124,.dm-share-comment-translation-v120{display:none;margin-top:6px;padding:8px 10px;border-radius:10px;background:#f5f6f8;color:#4f5660;font-size:10px;line-height:1.5;white-space:pre-wrap}.dm-share-card-v120.reel .dm-share-card-translation-v124,.dm-share-detail-v120.reel .dm-share-detail-caption-translation-v124,.dm-share-detail-v120.reel .dm-share-comment-translation-v120{background:#141518;color:#eef0f3}
.dm-share-card-translation-v124.show,.dm-share-detail-caption-translation-v124.show,.dm-share-comment-translation-v120.show{display:block}
.dm-share-open-v120{margin-top:8px;font-size:9px;color:#8a8f96}.dm-share-card-v120.reel .dm-share-open-v120{color:#9c9da2}
.dm-msg-row.them .dm-share-card-v120{max-width:min(262px,78vw)}
.dm-share-detail-v120{position:absolute;inset:0;z-index:190;display:none;align-items:center;justify-content:center;padding:16px;background:rgba(0,0,0,.28)}.dm-share-detail-v120.show{display:flex}.dm-share-detail-v120.reel{background:rgba(0,0,0,.55);color:#fff}
.dm-share-detail-panel-v124{width:min(92vw,380px);max-height:min(82vh,680px);display:flex;flex-direction:column;overflow:hidden;border-radius:24px;background:#fff;color:#111;box-shadow:0 18px 50px rgba(0,0,0,.22)}.dm-share-detail-v120.reel .dm-share-detail-panel-v124{background:#070708;color:#fff;border:1px solid #1f2126}
.dm-share-detail-head-v120{height:58px;flex:0 0 58px;display:flex;align-items:center;gap:8px;padding:0 12px;border-bottom:1px solid #eceef2;background:inherit}.dm-share-detail-v120.reel .dm-share-detail-head-v120{border-color:#202126}
.dm-share-detail-back-v120{width:38px;height:38px;border:0;background:transparent;color:inherit;font-size:30px;line-height:1;display:grid;place-items:center;cursor:pointer}.dm-share-detail-title-v120{min-width:0;flex:1}.dm-share-detail-title-v120 strong{display:block;font-size:14px}.dm-share-detail-title-v120 span{display:block;margin-top:2px;font-size:9px;color:#8d9198}.dm-share-detail-v120.reel .dm-share-detail-title-v120 span{color:#8f9198}
.dm-share-detail-scroll-v120{flex:1;min-height:0;overflow-y:auto;scrollbar-width:none}.dm-share-detail-scroll-v120::-webkit-scrollbar{display:none}
.dm-share-detail-media-v120{width:100%;aspect-ratio:1/1;background:#f1f2f4;display:flex;align-items:center;justify-content:center;overflow:hidden;text-align:center;padding:20px;color:#555b63;font-size:12px;line-height:1.6;white-space:pre-wrap}.dm-share-detail-v120.reel .dm-share-detail-media-v120{aspect-ratio:4/5;background:#050506;color:#fff;padding:28px}.dm-share-detail-media-v120 img{width:100%;height:100%;object-fit:cover;display:block}
.dm-share-detail-body-v120{padding:13px 14px 14px}.dm-share-detail-caption-v120{font-size:12px;line-height:1.62;white-space:pre-wrap}.dm-share-detail-meta-v120{margin-top:7px;font-size:9px;color:#92969d}.dm-share-detail-v120.reel .dm-share-detail-meta-v120{color:#8a8c92}
.dm-share-comments-head-v120{display:flex;align-items:center;justify-content:space-between;margin-top:16px;padding-top:12px;border-top:1px solid #eceef2}.dm-share-detail-v120.reel .dm-share-comments-head-v120{border-color:#22242a}.dm-share-comments-head-v120 strong{font-size:12px}.dm-share-comment-v120{padding:10px 0;border-bottom:1px solid #eef0f3}.dm-share-detail-v120.reel .dm-share-comment-v120{border-color:#1f2024}.dm-share-comment-v120 b{font-size:10px}.dm-share-comment-v120 p{margin:4px 0 0;font-size:11px;line-height:1.45}
.dm-share-load-v120{width:100%;height:40px;margin:12px 0 4px;border:0;border-radius:12px;background:#f0f1f4;color:#666b73;font-size:11px;font-weight:800;cursor:pointer}.dm-share-detail-v120.reel .dm-share-load-v120{background:#18191d;color:#f2f2f4}.dm-share-load-v120:disabled{opacity:.58}
#sheetShareDM{align-items:center;justify-content:center;padding:16px;z-index:191}#sheetShareDM .route-sheet{width:min(92vw,380px);max-height:min(72vh,520px);border-radius:24px;padding:12px 14px 18px;box-shadow:0 18px 50px rgba(0,0,0,.22)}#sheetShareDM .route-grab{display:none}
#reelsV15DMSheet{left:50%;right:auto;top:50%;bottom:auto;transform:translate(-50%,-50%);width:min(92vw,380px);max-height:min(72vh,520px);padding:12px 14px 18px;border-radius:24px;box-shadow:0 18px 50px rgba(0,0,0,.3)}#reelsV15DMSheet .reels-v15-sheet-head{padding-top:2px}#reelsV15DMSheet .reels-v15-block{padding-bottom:2px}
.dm-share-pending-v122{display:flex;align-items:flex-start;gap:8px;margin:6px 0 2px;padding-right:52px}
.dm-share-pending-v122 .dm-msg-avatar{flex:0 0 auto}
.dm-share-pending-bubble-v122{height:34px;min-width:48px;padding:0 12px;border-radius:8px 16px 16px 16px;background:#111;color:#fff;display:flex;align-items:center;justify-content:center;gap:4px}
.dm-share-pending-dot-v122{width:5px;height:5px;border-radius:50%;background:currentColor;opacity:.35;animation:dmSharePendingDotV122 1s infinite ease-in-out}
.dm-share-pending-dot-v122:nth-child(2){animation-delay:.14s}.dm-share-pending-dot-v122:nth-child(3){animation-delay:.28s}
@keyframes dmSharePendingDotV122{0%,60%,100%{transform:translateY(0);opacity:.3}30%{transform:translateY(-3px);opacity:1}}
`;
  let ACTIVE_DIRECT_HOST = null;
  let ACTIVE_DIRECT_STYLE = null;
  let ACTIVE_APP_WINDOW = null;
  let ACTIVE_DIRECT_RUNTIME = null;
  let ROCHE = null;
  let SKIP_NEXT_UNMOUNT_FLUSH = false;

  const DIRECT_STYLE_TEXT = ":host{position:absolute;inset:0;display:block;overflow:hidden;background:#fff;min-width:0;min-height:0;}\n.nini-ins-root{position:absolute!important;inset:0!important;width:100%!important;height:100%!important;min-width:0!important;min-height:0!important;overflow:hidden!important;background:#fff;color:#111;}\n\n*{box-sizing:border-box}\n.nini-ins-root,.nini-ins-root{width:100%;height:100%;min-width:0;min-height:0;overflow:hidden}\n.nini-ins-root{\n  margin:0;background:#fff;color:#111;\n  font-family:-apple-system,BlinkMacSystemFont,\"SF Pro Text\",\"PingFang SC\",\"Helvetica Neue\",Arial,sans-serif;\n}\n.phone{\n  width:100%;\n  height:100%;\n  max-width:none;\n  max-height:none;\n  margin:0;background:#fff;overflow:hidden;position:relative;\n  box-shadow:none;\n}\n\n/* ---------- Page shell ---------- */\n.screen{\n  position:absolute;\n  inset:0 0 68px 0;\n  overflow:hidden;\n  transition:background .25s ease,color .25s ease;\n}\n.screen.light{background:#fff;color:#111}\n.screen.dark{background:#000;color:#fff}\n.page{display:none;height:100%}\n.page.active{display:flex;flex-direction:column}\n\n/* ---------- HOME: agreed topbar + stories + feed ---------- */\n.topbar{\n  height:64px;flex:0 0 64px;\n  display:flex;align-items:center;justify-content:space-between;\n  padding:0 16px;border-bottom:1px solid #f0f0f2;background:#fff;z-index:20;\n}\n.logo{height:45px;width:auto;object-fit:contain;display:block;cursor:pointer}\n.top-actions{display:flex;align-items:center;gap:18px}\n.top-actions svg{\n  width:26px;height:26px;fill:none;stroke:#111;stroke-width:2;\n  stroke-linecap:round;stroke-linejoin:round;\n}\n.stories-wrap{flex:0 0 auto;border-bottom:1px solid #f4f4f6;background:#fff}\n.stories{\n  display:flex;gap:16px;padding:13px 14px 15px;\n  overflow-x:auto;scrollbar-width:none;\n}\n.stories::-webkit-scrollbar{display:none}\n.story{width:78px;flex:0 0 78px;text-align:center}\n.story-wrap{width:72px;height:72px;margin:auto;position:relative}\n.story-ring{\n  position:absolute;inset:0;border-radius:50%;padding:3px;\n  background:conic-gradient(from 0deg,#ffd600 0deg,#ff7a00 70deg,#ff0169 150deg,#d300c5 230deg,#7638fa 305deg,#ffd600 360deg);\n  animation:ringSpin 5.5s linear infinite;\n}\n.story.seen .story-ring{background:#cfd1d5;animation:none}\n.story.user .story-ring{background:#e0e2e6;animation:none}\n.story-inner{\n  width:100%;height:100%;border-radius:50%;border:2.5px solid #fff;\n  background:#eceef2;display:grid;place-items:center;overflow:hidden;\n  font-size:12px;font-weight:700;color:#444;\n}\n.story-ring .story-inner{animation:counterSpin 5.5s linear infinite}\n.story-inner img{width:100%;height:100%;object-fit:cover;display:block}\n.story.seen .story-inner,.story.user .story-inner{animation:none}\n.story-add{\n  position:absolute;right:-5px;bottom:-5px;width:31px;height:31px;\n  object-fit:contain;cursor:pointer;z-index:2;\n}\n.story-name{\n  font-size:11px;margin-top:6px;white-space:nowrap;\n  overflow:hidden;text-overflow:ellipsis;color:#111;\n}\n@keyframes ringSpin{to{transform:rotate(360deg)}}\n@keyframes counterSpin{to{transform:rotate(-360deg)}}\n\n.feed{\n  flex:1 1 auto;overflow-y:auto;background:#fff;\n  padding-bottom:18px;scrollbar-width:none;\n}\n.feed::-webkit-scrollbar{display:none}\n.post{background:#fff;border-bottom:1px solid #f2f2f4}\n.post-head{\n  height:62px;display:flex;align-items:center;gap:10px;padding:0 14px;\n}\n.avatar{\n  width:36px;height:36px;border-radius:50%;\n  background:linear-gradient(135deg,#dee3e9,#f1f3f6);\n  display:grid;place-items:center;font-size:12px;font-weight:700;color:#555;\n  overflow:hidden;flex:0 0 36px;\n}\n.head-main{min-width:0;flex:1}\n.post-id{\n  font-size:15px;font-weight:700;line-height:1.1;\n  white-space:nowrap;overflow:hidden;text-overflow:ellipsis;\n}\n.post-sub{font-size:12px;color:#777c83;line-height:1.1;margin-top:4px}\n.post-right{display:flex;align-items:center;gap:8px}\n.follow-btn{\n  min-width:86px;height:36px;border:0;border-radius:12px;background:#f1f1f4;\n  padding:0 15px;font-size:14px;font-weight:750;cursor:pointer;color:#111;\n}\n.follow-btn.feed-follow-locked:disabled{\n  opacity:1;cursor:default;color:#111;background:#f1f1f4;\n  -webkit-text-fill-color:#111;\n}\n.menu-btn{\n  width:30px;height:30px;border:0;background:none;\n  display:grid;place-items:center;padding:0;cursor:pointer;\n}\n.menu-btn svg{\n  width:24px;height:24px;stroke:#111;stroke-width:2.2;\n  fill:none;stroke-linecap:round;\n}\n.media{\n  width:100%;aspect-ratio:1/1;display:flex;align-items:center;justify-content:center;\n  text-align:center;padding:28px;color:#fff;font-size:15px;line-height:1.7;\n}\n.media.m1{background:linear-gradient(135deg,#111827,#263448)}\n.media.m2{background:linear-gradient(135deg,#614385,#516395)}\n.media.m3{background:linear-gradient(135deg,#334d50,#45b29d)}\n.media.m4{background:linear-gradient(135deg,#42275a,#734b6d)}\n\n.actions{\n  height:50px;display:flex;align-items:center;gap:15px;padding:8px 14px 4px;\n}\n.act{display:flex;align-items:center;gap:6px;cursor:pointer;user-select:none}\n.act .iconimg{\n  width:26px;height:26px;object-fit:contain;display:block;\n  transform-origin:center center;\n}\n.act .num{font-size:14px;font-weight:650;min-width:15px;color:#111}\n.icon-pop{animation:iconPop .24s ease-out}\n@keyframes iconPop{\n  0%{transform:scale(1)}\n  40%{transform:scale(1.18)}\n  100%{transform:scale(1)}\n}\n.caption{padding:1px 14px 4px;font-size:13px;line-height:1.58;color:#111}\n.caption strong{font-weight:800}\n.caption .tag{color:#1f5fae}\n.more{color:#6f737a;cursor:pointer}\n.translation-toggle{padding:2px 14px 8px;font-size:11px;color:#6f737a;cursor:pointer}\n.translation{\n  display:none;margin:0 14px 10px;padding:8px 10px;background:#f5f6f8;\n  border-radius:10px;font-size:12px;line-height:1.5;color:#222;\n}\n.translation.show{display:block}\n.comment-link{padding:0 14px 16px;font-size:11px;color:#7e8188;cursor:pointer}\n\n/* ---------- REELS placeholder shell ---------- */\n.reels-top{\n  height:62px;display:flex;align-items:center;justify-content:space-between;\n  padding:0 18px;border-bottom:1px solid rgba(255,255,255,.08);flex:0 0 62px;\n}\n.reels-top .title{font-size:20px;font-weight:800}\n.reels-top .mini{font-size:36px;font-weight:200;line-height:1}\n.reels-body{position:relative;flex:1;overflow:hidden;background:#000}\n.reels-center-text{\n  position:absolute;left:50%;top:50%;transform:translate(-50%,-40%);\n  width:72%;text-align:center;font-size:19px;line-height:1.65;font-weight:760;\n}\n.reels-bottom-copy{position:absolute;left:18px;right:18px;bottom:20px}\n.reels-userline{display:flex;align-items:center;gap:12px;margin-bottom:10px}\n.reels-avatar{width:42px;height:42px;border-radius:50%;background:#666}\n.reels-user{font-size:18px;font-weight:800}\n.follow-pill{\n  margin-left:6px;border:1.5px solid rgba(255,255,255,.85);color:#fff;\n  background:transparent;border-radius:11px;padding:6px 15px;\n  font-size:16px;font-weight:800;\n}\n.reels-caption{font-size:13px;line-height:1.6;color:#fff;opacity:.95}\n\n/* ---------- DM placeholder ---------- */\n.dm-head{padding:18px;display:flex;justify-content:space-between;align-items:center}\n.dm-head .title{font-size:22px;font-weight:800}\n.dm-head .sub{font-size:15px;color:#7c8188}\n.dm-search{\n  margin:0 18px;height:46px;background:#f1f2f6;border-radius:16px;\n  display:flex;align-items:center;padding:0 16px;color:#8a8f97;\n}\n.dm-list{padding:16px 18px 10px;display:grid;gap:16px}\n.dm-row{display:flex;align-items:center;gap:12px}\n.dm-avatar{\n  width:56px;height:56px;border-radius:50%;\n  background:linear-gradient(135deg,#dfe3ea,#f3f4f7);\n}\n.dm-meta strong{display:block;font-size:16px}\n.dm-meta span{font-size:13px;color:#7f838b}\n\n/* ---------- Profile placeholder ---------- */\n.profile-top{padding:18px;display:flex;align-items:center;justify-content:space-between}\n.profile-id{font-size:22px;font-weight:800}\n.profile-icons{font-size:28px}\n.profile-body{padding:0 18px 18px}\n.profile-row{display:flex;gap:18px;align-items:center}\n.profile-avatar{\n  width:88px;height:88px;border-radius:50%;\n  background:linear-gradient(135deg,#d9dce3,#f3f4f7);\n  flex:0 0 88px;\n}\n.profile-stats{display:flex;gap:24px;flex:1;justify-content:space-around}\n.stat{text-align:center}\n.stat strong{display:block;font-size:22px}\n.stat span{font-size:13px;color:#666}\n.profile-name{margin-top:18px;font-size:16px;font-weight:800}\n.profile-bio{margin-top:8px;font-size:14px;line-height:1.6;color:#444}\n.profile-grid{margin-top:20px;display:grid;grid-template-columns:repeat(3,1fr);gap:3px}\n.cell{aspect-ratio:1/1;background:linear-gradient(135deg,#edeef2,#e2e4ea)}\n\n/* ---------- Compose sheet ---------- */\n.sheet-backdrop{\n  position:absolute;inset:0;background:rgba(0,0,0,.18);\n  display:none;align-items:flex-end;justify-content:center;z-index:40;\n}\n.sheet-backdrop.show{display:flex}\n.sheet{\n  width:100%;padding:10px 14px 26px;\n  background:rgba(250,250,252,.9);\n  backdrop-filter:blur(22px);-webkit-backdrop-filter:blur(22px);\n  border-top-left-radius:26px;border-top-right-radius:26px;\n}\n.sheet-grab{width:40px;height:4px;border-radius:999px;background:#c8cad0;margin:0 auto 15px}\n.sheet-title{text-align:center;font-size:18px;font-weight:800;margin-bottom:16px}\n.sheet-btn{\n  height:50px;border-radius:17px;background:#fff;\n  display:grid;place-items:center;font-size:16px;font-weight:800;\n  margin-top:10px;\n}\n\n/* ---------- Flatter bottom nav ---------- */\n.bottom-nav{\n  position:absolute;left:0;right:0;bottom:0;\n  height:68px;\n  display:flex;align-items:center;justify-content:center;\n  transition:background .25s ease,border-color .25s ease;\n}\n.bottom-nav.light{background:#fff;border-top:1px solid #efeff4}\n.bottom-nav.dark{background:#000;border-top:1px solid rgba(255,255,255,.08)}\n.nav-inner{\n  width:100%;padding:0 8px 5px;\n  display:flex;align-items:flex-end;justify-content:space-around;\n}\n.tab{\n  position:relative;width:64px;height:46px;\n  display:flex;align-items:center;justify-content:center;cursor:pointer;\n}\n.tab-pill{\n  position:absolute;inset:5px 7px;border-radius:15px;\n  transform:scale(.9);opacity:0;transition:all .22s ease;\n}\n.bottom-nav.light .tab.active .tab-pill{\n  background:#eff0f4;opacity:1;transform:scale(1);\n}\n.bottom-nav.dark .tab.active .tab-pill{\n  background:#fff;opacity:1;transform:scale(1);\n}\n.tab img,.plus-glyph,.profile-chip{\n  position:relative;z-index:2;transition:transform .22s ease,opacity .22s ease;\n}\n.tab img{width:29px;height:29px;object-fit:contain}\n.plus-glyph{font-size:33px;line-height:1;font-weight:300;color:#111}\n.bottom-nav.dark .plus-glyph{color:#fff}\n.profile-chip{\n  width:30px;\n  height:30px;\n  aspect-ratio:1/1;\n  border-radius:999px!important;\n  background:linear-gradient(135deg,#d7dae2,#f2f4f7);\n  border:2px solid #969ba2;\n  box-sizing:border-box;\n  overflow:hidden;\n  display:grid;\n  place-items:center;\n  font-size:11px;\n  font-weight:800;\n  color:#555;\n  background-clip:padding-box;\n}\n.bottom-nav.light .tab:not(.active) .profile-chip{\n  border-color:#969ba2;\n}\n.bottom-nav.light .tab.active .profile-chip{\n  border-color:#6f747b;\n}\n.bottom-nav.dark .tab:not(.active) .profile-chip{\n  border-color:rgba(210,214,220,.82);\n}\n.bottom-nav.dark .tab.active .profile-chip{\n  border-color:#8a9097;\n  background:#eceef2;\n}\n\n/* ===== CREATE POST PAGE: isolated, does not modify frozen Home / bottom nav ===== */\n.create-post-overlay{\n  position:absolute;\n  inset:0;\n  z-index:80;\n  display:none;\n  background:#fff;\n  color:#111;\n  font-family:-apple-system,BlinkMacSystemFont,\"SF Pro Text\",\"PingFang SC\",\"Helvetica Neue\",Arial,sans-serif;\n}\n.create-post-overlay.show{display:flex;flex-direction:column}\n.cp-topbar{\n  height:62px;\n  flex:0 0 62px;\n  display:flex;\n  align-items:center;\n  gap:6px;\n  padding:0 14px;\n  border-bottom:1px solid #ededf1;\n  background:#fff;\n}\n.cp-back{\n  width:40px;height:40px;border:0;background:none;\n  display:grid;place-items:center;cursor:pointer;padding:0;\n}\n.cp-back span{\n  font-size:38px;line-height:1;font-weight:300;\n  transform:translateY(-1px);\n}\n.cp-title{font-size:20px;font-weight:800}\n.cp-content{\n  flex:1 1 auto;\n  overflow-y:auto;\n  padding:16px 16px 116px;\n  scrollbar-width:none;\n}\n.cp-content::-webkit-scrollbar{display:none}\n.cp-preview-track{\n  width:100%;\n  display:flex;\n  gap:10px;\n  overflow-x:auto;\n  scroll-snap-type:x mandatory;\n  scrollbar-width:none;\n  border-radius:18px;\n}\n.cp-preview-track::-webkit-scrollbar{display:none}\n.cp-slide{\n  min-width:100%;\n  scroll-snap-align:center;\n  aspect-ratio:1.18/1;\n  border:1.5px solid #d8d9de;\n  border-radius:18px;\n  background:#fafafa;\n  overflow:hidden;\n  position:relative;\n  display:flex;\n  align-items:center;\n  justify-content:center;\n}\n.cp-slide img{\n  width:100%;\n  height:100%;\n  object-fit:cover;\n  display:block;\n}\n.cp-slide-placeholder{\n  color:#9ea2a9;\n  text-align:center;\n  font-size:13px;\n  line-height:1.5;\n}\n.cp-slide-placeholder svg{\n  width:44px;height:44px;stroke:#a7abb1;fill:none;stroke-width:1.8;\n  margin-bottom:8px;\n}\n.cp-slide-desc{\n  padding:28px;\n  text-align:center;\n  color:#666;\n  font-size:13px;\n  line-height:1.6;\n}\n.cp-slide-desc strong{display:block;margin-bottom:8px;font-size:15px}\n.cp-remove{\n  position:absolute;\n  right:10px;top:10px;\n  width:28px;height:28px;\n  border:0;border-radius:50%;\n  background:rgba(17,17,17,.72);\n  color:#fff;font-size:17px;cursor:pointer;\n}\n.cp-counter{\n  margin:12px 2px;\n  color:#8b8e95;\n  font-size:15px;\n  font-weight:700;\n}\n.cp-media-actions{\n  display:grid;\n  grid-template-columns:1fr 1fr;\n  gap:12px;\n}\n.cp-action{\n  height:52px;\n  border:1px solid #d9dbe0;\n  border-radius:16px;\n  background:#fff;\n  display:flex;\n  align-items:center;\n  justify-content:center;\n  gap:9px;\n  font-size:15px;\n  font-weight:800;\n  cursor:pointer;\n}\n.cp-action svg{width:21px;height:21px;stroke:#222;fill:none;stroke-width:2}\n.cp-section{\n  border-top:1px solid #efeff2;\n  padding:18px 0 16px;\n  margin-top:18px;\n}\n.cp-label{\n  font-size:15px;\n  font-weight:800;\n  color:#666a71;\n  margin-bottom:10px;\n}\n.cp-caption{\n  width:100%;\n  min-height:108px;\n  border:0;\n  resize:none;\n  padding:0;\n  outline:none;\n  font:inherit;\n  font-size:16px;\n  line-height:1.65;\n  color:#111;\n}\n.cp-caption::placeholder{color:#a1a4aa}\n.cp-field{\n  min-height:56px;\n  display:flex;\n  align-items:center;\n  gap:12px;\n  border-bottom:1px solid #efeff2;\n  font-size:16px;\n}\n.cp-field svg{\n  width:22px;height:22px;stroke:#8e9197;fill:none;stroke-width:2;flex:0 0 22px;\n}\n.cp-input{\n  flex:1;\n  min-width:0;\n  height:52px;\n  border:0;\n  outline:none;\n  font:inherit;\n  font-size:16px;\n  color:#111;\n  background:transparent;\n}\n.cp-input::placeholder{color:#9a9da4}\n.cp-right{margin-left:auto;color:#aaa;font-size:20px}\n.cp-value{color:#111}\n.cp-placeholder{color:#9a9da4}\n.cp-switch-row{\n  display:flex;\n  align-items:center;\n  justify-content:space-between;\n  padding-top:18px;\n  gap:16px;\n}\n.cp-switch-copy strong{display:block;font-size:15px}\n.cp-switch-copy span{\n  display:block;\n  font-size:12px;\n  color:#8d9096;\n  margin-top:4px;\n  line-height:1.45;\n}\n.cp-switch{\n  width:48px;height:28px;border-radius:999px;background:#e4e5e9;\n  position:relative;cursor:pointer;flex:0 0 48px;\n}\n.cp-switch::after{\n  content:\"\";position:absolute;width:24px;height:24px;border-radius:50%;\n  top:2px;left:2px;background:#fff;box-shadow:0 1px 5px rgba(0,0,0,.18);\n  transition:.18s ease;\n}\n.cp-switch.on{background:#111}\n.cp-switch.on::after{left:22px}\n.cp-bottom{\n  position:absolute;\n  left:0;right:0;bottom:0;\n  padding:12px 16px 20px;\n  background:linear-gradient(to top,rgba(255,255,255,1) 74%,rgba(255,255,255,.88) 88%,rgba(255,255,255,0));\n  pointer-events:none;\n}\n.cp-share{\n  width:100%;\n  height:56px;\n  border:0;\n  border-radius:22px;\n  background:#111;\n  color:#fff;\n  font-size:18px;\n  font-weight:800;\n  cursor:pointer;\n  pointer-events:auto;\n  box-shadow:0 10px 26px rgba(0,0,0,.13);\n}\n.cp-modal{\n  position:absolute;\n  inset:0;\n  background:rgba(0,0,0,.24);\n  display:none;\n  align-items:flex-end;\n  justify-content:center;\n  z-index:90;\n}\n.cp-modal.show{display:flex}\n.cp-sheet{\n  width:100%;\n  background:rgba(250,250,252,.94);\n  backdrop-filter:blur(22px);\n  -webkit-backdrop-filter:blur(22px);\n  border-top-left-radius:26px;\n  border-top-right-radius:26px;\n  padding:10px 14px 24px;\n}\n.cp-grab{\n  width:42px;height:5px;border-radius:999px;background:#c7c9cf;margin:0 auto 16px;\n}\n.cp-sheet-title{text-align:center;font-size:18px;font-weight:800;margin-bottom:14px}\n.cp-sheet-textarea{\n  width:100%;\n  min-height:112px;\n  border:1px solid #e4e5ea;\n  background:#fff;\n  border-radius:16px;\n  padding:14px;\n  outline:none;\n  resize:none;\n  font:inherit;\n  font-size:15px;\n  line-height:1.55;\n}\n.cp-sheet-input{\n  width:100%;\n  height:48px;\n  border:1px solid #e4e5ea;\n  background:#fff;\n  border-radius:16px;\n  padding:0 14px;\n  outline:none;\n  font:inherit;\n  font-size:15px;\n}\n.cp-sheet-hint{\n  margin-top:8px;\n  color:#8f939a;\n  font-size:12px;\n  line-height:1.45;\n}\n.cp-sheet-actions{display:grid;gap:10px;margin-top:14px}\n#cpCaptureModal .cp-capture-actions{grid-template-columns:repeat(2,minmax(0,1fr))}\n.cp-sheet-btn{\n  height:50px;border:0;border-radius:16px;background:#fff;\n  display:grid;place-items:center;font-size:16px;font-weight:800;cursor:pointer;\n}\n.cp-sheet-btn.primary{background:#111;color:#fff}\n.cp-sheet-btn.gray{background:#f2f2f5;color:#111}\n.cp-sheet-btn.red{background:#fff;color:#ff3b50}\n.cp-pill{\n  display:inline-flex;\n  align-items:center;\n  min-height:32px;\n  padding:6px 10px;\n  border-radius:999px;\n  background:#f3f4f7;\n  color:#111;\n  font-size:13px;\n  font-weight:700;\n  margin:4px 8px 0 0;\n}\n.cp-hidden-file{display:none}\n.nini-ins-root .cp-placeholder-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:8px;margin:10px 0 4px}\n.nini-ins-root .cp-placeholder-choice{aspect-ratio:1/1;border:2px solid transparent;border-radius:12px;background:#f0f1f4;overflow:hidden;padding:0;cursor:pointer}\n.nini-ins-root .cp-placeholder-choice img{width:100%;height:100%;object-fit:cover;display:block}\n\n\n\n/* ===== NAVIGATION SKELETON: isolated overlays/sheets, frozen Home + bottom nav untouched ===== */\n.route-page{\n  position:absolute;\n  inset:0;\n  z-index:100;\n  display:none;\n  flex-direction:column;\n  background:#fff;\n  color:#111;\n}\n.route-page.show{display:flex}\n.route-page.dark{background:#000;color:#fff}\n.route-head{\n  height:62px;\n  flex:0 0 62px;\n  display:flex;\n  align-items:center;\n  gap:8px;\n  padding:0 14px;\n  border-bottom:1px solid #ededf1;\n  background:inherit;\n}\n.route-page.dark .route-head{border-bottom-color:rgba(255,255,255,.1)}\n.route-back{\n  width:38px;height:38px;border:0;background:none;padding:0;\n  display:grid;place-items:center;font-size:36px;line-height:1;cursor:pointer;color:inherit;\n}\n.route-title{font-size:19px;font-weight:800}\n.route-right{margin-left:auto;font-size:14px;color:#73777e}\n.route-page.dark .route-right{color:#b7bac1}\n.route-body{\n  flex:1;\n  overflow-y:auto;\n  padding:18px;\n}\n.route-empty{\n  min-height:240px;\n  display:flex;\n  flex-direction:column;\n  align-items:center;\n  justify-content:center;\n  text-align:center;\n  color:#8a8e95;\n  gap:8px;\n  line-height:1.55;\n}\n.route-page.dark .route-empty{color:#b7bac1}\n.route-card{\n  border:1px solid #eceef2;\n  border-radius:18px;\n  padding:15px;\n  margin-bottom:12px;\n  background:#fafbfc;\n}\n.route-page.dark .route-card{\n  border-color:rgba(255,255,255,.12);\n  background:rgba(255,255,255,.06);\n}\n.route-card strong{display:block;margin-bottom:6px}\n.route-btn{\n  height:46px;\n  border:0;\n  border-radius:14px;\n  background:#111;\n  color:#fff;\n  padding:0 16px;\n  font-size:15px;\n  font-weight:800;\n  cursor:pointer;\n}\n.route-btn.light{\n  background:#f1f2f5;\n  color:#111;\n}\n.story-stage{\n  flex:1;\n  display:flex;\n  align-items:center;\n  justify-content:center;\n  position:relative;\n  background:#000;\n  color:#fff;\n  text-align:center;\n  padding:28px;\n  overflow:hidden;\n}\n.story-progress{\n  position:absolute;\n  top:14px;\n  left:12px;\n  right:12px;\n  z-index:8;\n  display:flex;\n  gap:4px;\n  height:3px;\n}\n.story-progress-segment{\n  flex:1;\n  height:3px;\n  border-radius:999px;\n  background:rgba(255,255,255,.25);\n  overflow:hidden;\n}\n.story-progress-fill{\n  width:0%;\n  height:100%;\n  border-radius:inherit;\n  background:#fff;\n}\n.story-progress-segment.done .story-progress-fill,.story-progress-segment.current.image .story-progress-fill{\n  width:100%;\n}\n.story-progress-segment.current.video .story-progress-fill{\n  animation:storyVideoProgress var(--story-duration,6s) linear forwards;\n}\n@keyframes storyVideoProgress{\n  from{width:0%}\n  to{width:100%}\n}\n.story-meta{\n  position:absolute;\n  top:28px;\n  left:16px;\n  right:16px;\n  z-index:8;\n  display:flex;\n  align-items:center;\n  gap:9px;\n  text-align:left;\n}\n.story-avatar{\n  width:34px;\n  height:34px;\n  border-radius:50%;\n  background:#777;\n  flex:0 0 34px;\n  display:grid;\n  place-items:center;\n  font-size:11px;\n  font-weight:850;\n}\n.story-close{\n  margin-left:auto;\n  border:0;\n  background:none;\n  color:#fff;\n  font-size:26px;\n  cursor:pointer;\n}\n.story-content{\n  position:relative;\n  z-index:3;\n  width:min(78%,300px);\n  padding:22px 18px;\n  border:1px solid rgba(255,255,255,.13);\n  border-radius:23px;\n  background:rgba(255,255,255,.055);\n  backdrop-filter:blur(16px);\n  -webkit-backdrop-filter:blur(16px);\n  box-shadow:0 18px 42px rgba(0,0,0,.28);\n  font-size:12px;\n  line-height:1.62;\n  white-space:pre-wrap;\n}\n.story-content strong{\n  display:block;\n  margin-bottom:8px;\n  font-size:14px;\n}\n.story-content small{\n  display:block;\n  margin-top:10px;\n  color:rgba(255,255,255,.52);\n  font-size:9px;\n}\n.story-content img{\n  width:100%;\n  max-height:52vh;\n  object-fit:cover;\n  border-radius:16px;\n  display:block;\n  margin-bottom:10px;\n}\n/* v1.15.25: real Story image mode */\n.story-content.has-media{\n  position:absolute;\n  left:0;\n  right:0;\n  top:50%;\n  transform:translateY(-50%);\n  width:100%;\n  height:auto;\n  aspect-ratio:1 / 1;\n  padding:0;\n  margin:0;\n  border:0;\n  border-radius:0;\n  background:#111;\n  box-shadow:none;\n  backdrop-filter:none;\n  -webkit-backdrop-filter:none;\n  overflow:hidden;\n}\n.story-content.has-media img{\n  width:100%;\n  height:100%;\n  max-height:none;\n  min-width:100%;\n  min-height:100%;\n  margin:0;\n  padding:0;\n  border:0;\n  border-radius:0;\n  object-fit:cover;\n  object-position:center;\n  display:block;\n}\n.story-nav-zone{\n  position:absolute;\n  top:66px;\n  bottom:86px;\n  width:36%;\n  z-index:4;\n  border:0;\n  background:transparent;\n}\n.story-nav-zone.prev{left:0}\n.story-nav-zone.next{right:0}\n.story-bottom{\n  position:absolute;\n  left:14px;\n  right:14px;\n  bottom:18px;\n  z-index:10;\n  display:flex;\n  align-items:center;\n  justify-content:center;\n}\n.story-reactions{\n  display:flex;\n  align-items:center;\n  justify-content:center;\n  gap:6px;\n  padding:7px;\n  border:1px solid rgba(255,255,255,.13);\n  border-radius:999px;\n  background:rgba(18,18,21,.46);\n  backdrop-filter:blur(18px);\n  -webkit-backdrop-filter:blur(18px);\n}\n.story-reaction-btn{\n  width:39px;\n  height:39px;\n  border:0;\n  border-radius:50%;\n  background:transparent;\n  display:grid;\n  place-items:center;\n  font-size:22px;\n  cursor:pointer;\n  transition:transform .16s ease,background .16s ease;\n}\n.story-reaction-btn:active{\n  transform:scale(.88);\n  background:rgba(255,255,255,.10);\n}\n.story-bottom.own .story-reactions{\n  opacity:.38;\n  pointer-events:none;\n}\n.story-reaction-toast{\n  position:absolute;\n  left:50%;\n  bottom:78px;\n  z-index:12;\n  transform:translate(-50%,8px);\n  opacity:0;\n  pointer-events:none;\n  padding:8px 11px;\n  border-radius:999px;\n  background:rgba(24,24,27,.78);\n  border:1px solid rgba(255,255,255,.12);\n  font-size:10px;\n  color:#fff;\n  transition:.18s ease;\n}\n.story-reaction-toast.show{\n  opacity:1;\n  transform:translate(-50%,0);\n}\n.route-sheet-backdrop{\n  position:absolute;inset:0;z-index:110;\n  display:none;align-items:flex-end;\n  background:rgba(0,0,0,.25);\n}\n.route-sheet-backdrop.show{display:flex}\n.route-sheet{\n  width:100%;\n  max-height:72%;\n  overflow-y:auto;\n  background:rgba(250,250,252,.96);\n  backdrop-filter:blur(22px);\n  -webkit-backdrop-filter:blur(22px);\n  border-top-left-radius:26px;\n  border-top-right-radius:26px;\n  padding:10px 14px 24px;\n}\n.route-grab{\n  width:42px;height:5px;background:#c8cad0;border-radius:999px;margin:0 auto 14px;\n}\n.route-sheet-title{text-align:center;font-size:18px;font-weight:800;margin-bottom:14px}\n.comment-row{\n  display:flex;gap:10px;padding:11px 0;border-bottom:1px solid #eceef2;\n}\n.comment-avatar{width:34px;height:34px;border-radius:50%;background:#e4e6eb;flex:0 0 34px}\n.comment-copy{font-size:13px;line-height:1.5}\n.comment-copy strong{font-weight:800}\n.comment-actions{font-size:11px;color:#888;margin-top:4px}\n.dm-choice{\n  display:flex;align-items:center;gap:12px;padding:12px 2px;border-bottom:1px solid #eceef2;\n}\n.dm-choice-avatar{width:44px;height:44px;border-radius:50%;background:#e6e8ed}\n.dm-choice button{margin-left:auto}\n.refresh-grid{display:grid;gap:10px}\n.refresh-option{\n  border:1px solid #e4e5e9;border-radius:16px;padding:14px;background:#fff;\n}\n.refresh-option strong{display:block;margin-bottom:5px}\n.refresh-option span{font-size:12px;color:#858990;line-height:1.45}\n.char-checks{display:flex;flex-wrap:wrap;gap:8px;margin-top:10px}\n.char-chip{\n  border:1px solid #dfe1e6;background:#f5f6f8;border-radius:999px;padding:8px 12px;\n  font-size:13px;font-weight:700;cursor:pointer;\n}\n.char-chip.on{background:#111;color:#fff;border-color:#111}\n.notice-dot{\n  display:inline-block;width:8px;height:8px;border-radius:50%;background:#ff304f;margin-left:5px;\n}\n\n\n/* ===== INS v1.01: Feed topbar iOS glass cards ===== */\n.top-actions{position:relative}\n.feed-notice-dot{\n  position:absolute;\n  width:9px;\n  height:9px;\n  border-radius:50%;\n  background:#ff304f;\n  right:-2px;\n  top:-3px;\n  border:2px solid #fff;\n  z-index:3;\n  pointer-events:none;\n}\n.feed-glass-dim{\n  position:absolute;\n  inset:64px 0 68px 0;\n  background:rgba(17,17,17,.08);\n  opacity:0;\n  pointer-events:none;\n  transition:.18s ease;\n  z-index:61;\n}\n.feed-glass-dim.show{\n  opacity:1;\n  pointer-events:auto;\n}\n.feed-glass-panel{\n  position:absolute;\n  top:70px;\n  right:12px;\n  width:calc(100% - 24px);\n  max-width:390px;\n  max-height:calc(100% - 150px);\n  overflow-y:auto;\n  border:1px solid rgba(255,255,255,.72);\n  border-radius:26px;\n  background:rgba(247,248,250,.76);\n  backdrop-filter:blur(26px) saturate(1.45);\n  -webkit-backdrop-filter:blur(26px) saturate(1.45);\n  box-shadow:\n    0 18px 45px rgba(15,20,30,.18),\n    inset 0 1px 0 rgba(255,255,255,.9);\n  padding:14px;\n  z-index:62;\n  opacity:0;\n  transform:translateY(-8px) scale(.985);\n  pointer-events:none;\n  transition:.2s cubic-bezier(.2,.8,.2,1);\n  scrollbar-width:none;\n}\n.feed-glass-panel::-webkit-scrollbar{display:none}\n.feed-glass-panel.show{\n  opacity:1;\n  transform:translateY(0) scale(1);\n  pointer-events:auto;\n}\n.feed-glass-head{\n  display:flex;\n  align-items:center;\n  justify-content:space-between;\n  margin-bottom:12px;\n}\n.feed-glass-title{\n  font-size:18px;\n  font-weight:800;\n}\n.feed-glass-close{\n  width:30px;\n  height:30px;\n  border:0;\n  border-radius:50%;\n  background:rgba(255,255,255,.72);\n  color:#111;\n  font-size:18px;\n  cursor:pointer;\n}\n.feed-glass-block{\n  border-radius:18px;\n  background:rgba(255,255,255,.64);\n  border:1px solid rgba(255,255,255,.72);\n  padding:13px;\n  margin-top:10px;\n}\n.feed-glass-block strong{\n  display:block;\n  font-size:15px;\n  margin-bottom:5px;\n}\n.feed-glass-muted{\n  font-size:12px;\n  color:#777c84;\n  line-height:1.45;\n}\n.feed-char-chips{\n  display:flex;\n  flex-wrap:wrap;\n  gap:8px;\n  margin-top:11px;\n}\n.feed-char-chip{\n  border:0;\n  min-height:34px;\n  padding:0 12px;\n  border-radius:999px;\n  background:rgba(239,240,244,.95);\n  color:#111;\n  font-size:13px;\n  font-weight:750;\n  cursor:pointer;\n}\n.feed-char-chip.on{\n  background:#111;\n  color:#fff;\n}\n.feed-glass-action-row{\n  display:grid;\n  grid-template-columns:1fr 1fr;\n  gap:10px;\n  margin-top:12px;\n}\n.feed-glass-action{\n  min-height:46px;\n  border:0;\n  border-radius:16px;\n  font-size:14px;\n  font-weight:800;\n  cursor:pointer;\n}\n.feed-glass-action.dark{\n  background:#111;\n  color:#fff;\n}\n.feed-glass-action.light{\n  background:rgba(255,255,255,.78);\n  color:#111;\n}\n.feed-glass-action.selected{\n  background:#111!important;\n  color:#fff!important;\n}\n.feed-glass-action:disabled{opacity:1}\n.feed-glass-action.busy{\n  background:rgba(255,255,255,.78)!important;\n  color:#111!important;\n}\n.feed-notice-list{\n  border-radius:18px;\n  background:rgba(255,255,255,.64);\n  border:1px solid rgba(255,255,255,.72);\n  overflow:hidden;\n}\n.feed-notice-row{\n  width:100%;\n  min-height:66px;\n  display:flex;\n  align-items:center;\n  gap:10px;\n  padding:10px 10px;\n  border:0;\n  border-bottom:1px solid rgba(0,0,0,.06);\n  background:transparent;\n  color:#111;\n  text-align:left;\n  font:inherit;\n}\n.feed-notice-row:last-child{border-bottom:0}\n.feed-notice-row.clickable{cursor:pointer}\n.feed-notice-avatar{\n  width:38px;\n  height:38px;\n  border-radius:50%;\n  background:linear-gradient(135deg,#dde1e7,#f4f5f7);\n  flex:0 0 38px;\n}\n.feed-notice-copy{\n  min-width:0;\n  flex:1;\n  font-size:13px;\n  line-height:1.42;\n}\n.feed-notice-copy strong{\n  display:block;\n  font-size:14px;\n  font-weight:800;\n  margin:0 0 2px;\n}\n.feed-notice-time{\n  color:#999da4;\n  font-size:11px;\n  margin-top:2px;\n}\n.feed-notice-side{\n  flex:0 0 auto;\n  min-width:28px;\n  display:flex;\n  align-items:center;\n  justify-content:center;\n  font-size:22px;\n}\n.feed-notice-chevron{\n  width:30px;\n  height:30px;\n  border-radius:50%;\n  background:rgba(238,239,243,.92);\n  display:grid;\n  place-items:center;\n  color:#777b83;\n  font-size:18px;\n}\n.feed-glass-foot{\n  padding:10px 2px 2px;\n  font-size:11px;\n  color:#8b8f96;\n  text-align:center;\n}\n\n\n/* ===== INS v1.02: post menu / editor ===== */\n.post-glass-dim{\n  position:absolute;\n  inset:64px 0 68px 0;\n  z-index:63;\n  display:none;\n  background:rgba(17,17,17,.10);\n}\n.post-glass-dim.show{display:block}\n\n.post-action-glass,.post-edit-glass{\n  position:absolute;\n  left:12px;\n  right:12px;\n  z-index:64;\n  border:1px solid rgba(255,255,255,.72);\n  background:rgba(247,248,250,.80);\n  backdrop-filter:blur(28px) saturate(1.45);\n  -webkit-backdrop-filter:blur(28px) saturate(1.45);\n  box-shadow:\n    0 18px 45px rgba(15,20,30,.18),\n    inset 0 1px 0 rgba(255,255,255,.9);\n  opacity:0;\n  transform:translateY(8px) scale(.985);\n  pointer-events:none;\n  transition:.2s cubic-bezier(.2,.8,.2,1);\n}\n.post-action-glass.show,.post-edit-glass.show{\n  opacity:1;\n  transform:translateY(0) scale(1);\n  pointer-events:auto;\n}\n\n.post-action-glass{\n  bottom:84px;\n  border-radius:24px;\n  padding:10px;\n}\n.post-action-btn{\n  width:100%;\n  min-height:52px;\n  border:0;\n  border-radius:17px;\n  background:rgba(255,255,255,.72);\n  color:#111;\n  display:flex;\n  align-items:center;\n  gap:12px;\n  padding:0 15px;\n  font-size:15px;\n  font-weight:800;\n  cursor:pointer;\n}\n.post-action-btn + .post-action-btn{margin-top:8px}\n.post-action-btn.danger{color:#ff3b4f}\n.post-action-icon{\n  width:26px;\n  height:26px;\n  border-radius:9px;\n  background:rgba(238,239,243,.92);\n  display:grid;\n  place-items:center;\n  flex:0 0 26px;\n}\n.post-action-icon svg{\n  width:17px;height:17px;\n  fill:none;stroke:currentColor;stroke-width:1.9;\n  stroke-linecap:round;stroke-linejoin:round;\n}\n\n.post-edit-glass{\n  top:78px;\n  bottom:84px;\n  border-radius:26px;\n  padding:14px;\n  overflow-y:auto;\n  scrollbar-width:none;\n}\n.post-edit-glass::-webkit-scrollbar{display:none}\n.post-edit-head{\n  display:flex;\n  align-items:center;\n  justify-content:space-between;\n  margin-bottom:13px;\n}\n.post-edit-title{\n  font-size:18px;\n  font-weight:800;\n}\n.post-edit-close{\n  width:30px;height:30px;border:0;border-radius:50%;\n  background:rgba(255,255,255,.76);\n  color:#111;font-size:18px;cursor:pointer;\n}\n.post-edit-block{\n  border-radius:18px;\n  background:rgba(255,255,255,.66);\n  border:1px solid rgba(255,255,255,.72);\n  padding:13px;\n  margin-top:10px;\n}\n.post-edit-label{\n  display:block;\n  font-size:13px;\n  font-weight:800;\n  color:#5f636a;\n  margin-bottom:8px;\n}\n.post-edit-input{\n  width:100%;height:44px;border:1px solid rgba(0,0,0,.07);border-radius:14px;\n  background:rgba(255,255,255,.85);padding:0 12px;outline:none;font:inherit;font-size:14px;color:#111;\n}\n.post-edit-identity-grid{display:grid;grid-template-columns:1fr 1fr;gap:10px}\n.post-edit-identity-grid .wide{grid-column:1 / -1}\n.post-edit-stranger-identity{display:none}\n.post-edit-stranger-identity.show{display:block}\n\n.post-edit-textarea{\n  width:100%;\n  min-height:104px;\n  resize:none;\n  border:1px solid rgba(0,0,0,.07);\n  border-radius:14px;\n  background:rgba(255,255,255,.85);\n  padding:12px;\n  outline:none;\n  font:inherit;\n  font-size:14px;\n  line-height:1.55;\n  color:#111;\n}\n.post-edit-media{\n  width:100%;\n  aspect-ratio:1.35/1;\n  border-radius:15px;\n  background:linear-gradient(135deg,#e7e9ee,#f5f6f8);\n  display:flex;\n  align-items:center;\n  justify-content:center;\n  text-align:center;\n  padding:18px;\n  color:#777b83;\n  font-size:13px;\n  line-height:1.55;\n  overflow:hidden;\n}\n.post-edit-media img{\n  width:100%;\n  height:100%;\n  object-fit:cover;\n  display:block;\n}\n.post-edit-row{\n  display:grid;\n  grid-template-columns:1fr 1fr;\n  gap:9px;\n  margin-top:10px;\n}\n.post-edit-btn{\n  min-height:46px;\n  border:0;\n  border-radius:15px;\n  font-size:14px;\n  font-weight:800;\n  cursor:pointer;\n}\n.post-edit-btn.light{\n  background:rgba(255,255,255,.78);\n  color:#111;\n}\n.post-edit-btn.dark{\n  background:#111;\n  color:#fff;\n}\n.post-edit-save{\n  width:100%;\n  height:50px;\n  border:0;\n  border-radius:17px;\n  background:#111;\n  color:#fff;\n  font-size:15px;\n  font-weight:800;\n  cursor:pointer;\n  margin-top:12px;\n}\n\n/* ===== v1.15.22 Feed image flip / editor media picker ===== */\n.media.post-media-flippable{\n  position:relative;\n  width:100%;\n  aspect-ratio:1 / 1 !important;\n  height:auto !important;\n  min-height:0 !important;\n  padding:0 !important;\n  margin:0;\n  overflow:hidden;\n  cursor:pointer;\n  user-select:none;\n  -webkit-user-select:none;\n  touch-action:pan-y pinch-zoom;\n  overscroll-behavior-x:contain;\n}\n.media.post-media-flippable img{\n  width:100%;\n  height:100%;\n  min-width:100%;\n  min-height:100%;\n  object-fit:cover;\n  object-position:center;\n  display:block;\n  margin:0;\n  padding:0;\n  border:0;\n  pointer-events:none;\n  user-select:none;\n  -webkit-user-select:none;\n  -webkit-user-drag:none;\n}\n.post-media-counter{\n  position:absolute;\n  top:10px;\n  right:10px;\n  z-index:4;\n  min-width:38px;\n  height:25px;\n  padding:0 9px;\n  display:flex;\n  align-items:center;\n  justify-content:center;\n  border-radius:999px;\n  background:rgba(0,0,0,.52);\n  color:#fff;\n  font-size:10px;\n  font-weight:850;\n  letter-spacing:.01em;\n  pointer-events:none;\n  backdrop-filter:blur(8px);\n  -webkit-backdrop-filter:blur(8px);\n}\n.post-media-description-back{\n  width:100%;\n  height:100%;\n  min-width:100%;\n  min-height:100%;\n  display:flex;\n  align-items:center;\n  justify-content:center;\n  padding:28px;\n  background:linear-gradient(145deg,#f6f7f9,#eceff3);\n  color:#30353c;\n  text-align:left;\n  overflow:hidden;\n}\n.post-media-description-back span{\n  width:100%;\n  font-size:15px;\n  line-height:1.72;\n  font-weight:650;\n  white-space:pre-wrap;\n  word-break:break-word;\n}\n.post-edit-media-shell{\n  position:relative;\n}\n.post-edit-media{\n  position:relative;\n  width:100%;\n  aspect-ratio:1 / 1 !important;\n  height:auto !important;\n  padding:0 !important;\n  margin:0;\n  overflow:hidden;\n  background:#eef0f3;\n  cursor:pointer;\n  touch-action:pan-y pinch-zoom;\n}\n.post-edit-media > img{\n  width:100% !important;\n  height:100% !important;\n  min-width:100%;\n  min-height:100%;\n  object-fit:cover !important;\n  object-position:center;\n  display:block;\n}\n.post-edit-media .post-edit-media-back{\n  width:100%;\n  height:100%;\n  display:flex;\n  align-items:center;\n  justify-content:center;\n  padding:24px;\n  background:linear-gradient(145deg,#f7f8fa,#eceff3);\n  text-align:left;\n}\n.post-edit-media .post-edit-media-back span{\n  width:100%;\n  color:#333941;\n  font-size:14px;\n  line-height:1.68;\n  font-weight:700;\n  white-space:pre-wrap;\n  word-break:break-word;\n}\n.post-edit-media-nav{\n  position:absolute;\n  top:50%;\n  transform:translateY(-50%);\n  z-index:3;\n  width:34px;\n  height:34px;\n  border:0;\n  border-radius:50%;\n  background:rgba(255,255,255,.82);\n  color:#111;\n  font-size:18px;\n  font-weight:900;\n  box-shadow:0 5px 18px rgba(0,0,0,.12);\n  cursor:pointer;\n}\n.post-edit-media-nav.prev{left:9px}\n.post-edit-media-nav.next{right:9px}\n.post-edit-media-meta{\n  display:flex;\n  align-items:center;\n  justify-content:space-between;\n  gap:10px;\n  margin-top:8px;\n  color:#8a9098;\n  font-size:10px;\n}\n.post-edit-media-meta b{\n  color:#4b5159;\n  font-size:10px;\n}\n.post-edit-source-grid{\n  display:grid;\n  grid-template-columns:repeat(4,1fr);\n  gap:8px;\n  margin-top:8px;\n}\n.post-edit-source-btn{\n  min-height:50px;\n  border:1px solid rgba(0,0,0,.06);\n  border-radius:15px;\n  background:rgba(255,255,255,.78);\n  color:#555b63;\n  padding:8px 7px;\n  cursor:pointer;\n}\n.post-edit-source-btn strong{\n  display:block;\n  font-size:11px;\n  color:inherit;\n}\n.post-edit-source-btn span{\n  display:block;\n  margin-top:3px;\n  font-size:8px;\n  line-height:1.3;\n  color:#969ca5;\n}\n.post-edit-source-btn.active{\n  background:#111;\n  color:#fff;\n  border-color:#111;\n}\n.post-edit-source-btn.active span{color:#cdd1d6}\n.post-edit-source-btn:disabled{\n  cursor:not-allowed;\n  opacity:.38;\n}\n.post-edit-description-wrap{\n  margin-top:10px;\n}\n.post-edit-description-wrap .post-edit-textarea{\n  min-height:78px;\n}\n.post-edit-source-note{\n  margin-top:9px;\n  padding:9px 10px;\n  border-radius:13px;\n  background:#f4f5f7;\n  color:#747b84;\n  font-size:10px;\n  line-height:1.5;\n}\n.post-edit-generate-action{\n  display:none;\n  width:100%;\n  min-height:42px;\n  margin-top:9px;\n  border:0;\n  border-radius:13px;\n  background:#111;\n  color:#fff;\n  font-size:12px;\n  font-weight:850;\n  cursor:pointer;\n}\n.post-edit-generate-action.show{display:block}\n.post-edit-media-actions{display:grid;grid-template-columns:minmax(0,1fr) minmax(0,1fr);gap:9px}\n.post-edit-media-actions .post-edit-generate-action{margin-top:9px}\n.post-edit-upload-input{display:none}\n.user-cell.repost::after{display:none}\n.char-board-toggle{margin-top:5px;font-size:11px;color:#6f737a;cursor:pointer}\n.pcd-translation-toggle,.user-repost-preview-translation-toggle{margin-top:7px;font-size:11px;color:#6f737a;cursor:pointer}\n.pcd-translation,.user-repost-preview-translation{display:none;margin-top:7px;padding:8px 10px;border-radius:10px;background:#f5f6f8;color:#545b63;font-size:11px;line-height:1.5}\n.pcd-translation.show,.user-repost-preview-translation.show{display:block}\n.pcd-repost-note,.user-repost-preview-note{display:inline-flex;align-items:center;gap:4px;margin-top:3px;padding:3px 7px;border-radius:999px;background:#f2f3f6;color:#636973;font-size:10px;font-weight:800}\n\n\n/* ===== INS v1.03 fixes ===== */\n\n/* delete confirmation: glass instead of native confirm() */\n.post-delete-confirm{\n  position:absolute;\n  left:12px;\n  right:12px;\n  bottom:84px;\n  z-index:65;\n  border:1px solid rgba(255,255,255,.72);\n  border-radius:24px;\n  background:rgba(247,248,250,.82);\n  backdrop-filter:blur(28px) saturate(1.45);\n  -webkit-backdrop-filter:blur(28px) saturate(1.45);\n  box-shadow:\n    0 18px 45px rgba(15,20,30,.18),\n    inset 0 1px 0 rgba(255,255,255,.9);\n  padding:14px;\n  opacity:0;\n  transform:translateY(8px) scale(.985);\n  pointer-events:none;\n  transition:.2s cubic-bezier(.2,.8,.2,1);\n}\n.post-delete-confirm.show{\n  opacity:1;\n  transform:translateY(0) scale(1);\n  pointer-events:auto;\n}\n.post-delete-title{\n  font-size:17px;\n  font-weight:800;\n  text-align:center;\n}\n.post-delete-copy{\n  margin-top:7px;\n  color:#777b83;\n  font-size:12px;\n  line-height:1.5;\n  text-align:center;\n}\n.post-delete-actions{\n  display:grid;\n  grid-template-columns:1fr 1fr;\n  gap:9px;\n  margin-top:14px;\n}\n.post-delete-btn{\n  height:47px;\n  border:0;\n  border-radius:15px;\n  font-size:14px;\n  font-weight:800;\n  cursor:pointer;\n}\n.post-delete-btn.cancel{\n  background:rgba(255,255,255,.78);\n  color:#111;\n}\n.post-delete-btn.delete{\n  background:#ff3b4f;\n  color:#fff;\n}\n\n/* Comments: smoother iOS-glass slide-up animation */\n#sheetComments{\n  display:flex;\n  opacity:0;\n  visibility:hidden;\n  pointer-events:none;\n  background:rgba(0,0,0,0);\n  transition:\n    opacity .22s ease,\n    visibility 0s linear .22s,\n    background .22s ease;\n}\n#sheetComments.show{\n  opacity:1;\n  visibility:visible;\n  pointer-events:auto;\n  background:rgba(0,0,0,.22);\n  transition:\n    opacity .22s ease,\n    visibility 0s linear 0s,\n    background .22s ease;\n}\n#sheetComments .route-sheet{\n  transform:translateY(34px) scale(.99);\n  opacity:.96;\n  transition:transform .26s cubic-bezier(.2,.8,.2,1), opacity .22s ease;\n  background:rgba(250,250,252,.88);\n  backdrop-filter:blur(28px) saturate(1.35);\n  -webkit-backdrop-filter:blur(28px) saturate(1.35);\n}\n#sheetComments.show .route-sheet{\n  transform:translateY(0) scale(1);\n  opacity:1;\n}\n.comment-sheet-head{\n  display:grid;\n  grid-template-columns:40px 1fr 40px;\n  align-items:center;\n  margin-bottom:8px;\n}\n.comment-sheet-title{\n  text-align:center;\n  font-size:18px;\n  font-weight:800;\n}\n.comment-refresh{\n  grid-column:3;\n  width:34px;\n  height:34px;\n  border:0;\n  border-radius:50%;\n  background:rgba(255,255,255,.72);\n  display:grid;\n  place-items:center;\n  cursor:pointer;\n}\n.comment-refresh svg{\n  width:18px;\n  height:18px;\n  fill:none;\n  stroke:#111;\n  stroke-width:1.9;\n  stroke-linecap:round;\n  stroke-linejoin:round;\n}\n.comment-list{\n  min-height:110px;\n}\n.comment-row{\n  position:relative;\n  transition:background .18s ease;\n}\n.comment-row.focus{\n  background:rgba(235,237,242,.78);\n  border-radius:14px;\n  padding-left:8px;\n  padding-right:8px;\n}\n.comment-actions{\n  display:flex;\n  align-items:center;\n  gap:10px;\n}\n.comment-mini-btn{\n  border:0;\n  padding:0;\n  background:transparent;\n  color:#7e8289;\n  font:inherit;\n  font-size:11px;\n  cursor:pointer;\n}\n.comment-translation{\n  margin-top:6px;\n  padding:7px 9px;\n  border-radius:10px;\n  background:rgba(239,240,244,.82);\n  color:#44484f;\n  font-size:12px;\n  line-height:1.45;\n}\n.comment-composer{\n  position:sticky;\n  left:0;\n  right:0;\n  bottom:-24px;\n  display:flex;\n  gap:8px;\n  align-items:center;\n  margin:12px -14px -24px;\n  padding:10px 14px 18px;\n  border-top:1px solid rgba(0,0,0,.06);\n  background:rgba(250,250,252,.88);\n  backdrop-filter:blur(24px);\n  -webkit-backdrop-filter:blur(24px);\n}\n.comment-input{\n  flex:1;\n  min-width:0;\n  height:44px;\n  border:1px solid rgba(0,0,0,.09);\n  border-radius:999px;\n  background:rgba(255,255,255,.82);\n  padding:0 15px;\n  outline:none;\n  font:inherit;\n  font-size:14px;\n  color:#111;\n}\n.comment-send{\n  height:40px;\n  min-width:58px;\n  border:0;\n  border-radius:999px;\n  background:#111;\n  color:#fff;\n  font-size:13px;\n  font-weight:800;\n  cursor:pointer;\n}\n\n\n/* ===== INS v1.04: Feed post -> DM card linkage ===== */\n.dm-share-toast{\n  position:absolute;\n  left:50%;\n  bottom:82px;\n  transform:translateX(-50%) translateY(8px);\n  z-index:130;\n  min-width:118px;\n  max-width:80%;\n  padding:10px 14px;\n  border-radius:999px;\n  background:rgba(17,17,17,.88);\n  color:#fff;\n  font-size:12px;\n  font-weight:800;\n  text-align:center;\n  opacity:0;\n  pointer-events:none;\n  transition:.2s ease;\n}\n.dm-share-toast.show{\n  opacity:1;\n  transform:translateX(-50%) translateY(0);\n}\n\n.dm-chat-stream{\n  flex:1;\n  overflow-y:auto;\n  display:flex;\n  flex-direction:column;\n  gap:10px;\n  padding:10px 0 14px;\n  scrollbar-width:none;\n}\n.dm-chat-stream::-webkit-scrollbar{display:none}\n.dm-chat-empty{\n  margin:auto;\n  color:#92969d;\n  text-align:center;\n  font-size:13px;\n  line-height:1.55;\n}\n.dm-sent-row{\n  display:flex;\n  justify-content:flex-end;\n  padding-left:54px;\n}\n.dm-post-card{\n  width:min(248px,82%);\n  border-radius:18px;\n  overflow:hidden;\n  background:#f3f4f7;\n  border:1px solid #e5e7ec;\n}\n.dm-post-card-media{\n  aspect-ratio:1/1;\n  width:100%;\n  display:flex;\n  align-items:center;\n  justify-content:center;\n  text-align:center;\n  overflow:hidden;\n  padding:14px;\n  color:#fff;\n  font-size:12px;\n  line-height:1.45;\n}\n.dm-post-card-media img{\n  width:100%;\n  height:100%;\n  object-fit:cover;\n  display:block;\n}\n.dm-post-card-copy{\n  background:#fff;\n  padding:10px 11px 11px;\n}\n.dm-post-card-label{\n  font-size:10px;\n  color:#969aa1;\n  margin-bottom:4px;\n}\n.dm-post-card-author{\n  font-size:12px;\n  font-weight:800;\n  margin-bottom:4px;\n}\n.dm-post-card-caption{\n  font-size:11px;\n  line-height:1.45;\n  color:#4f535a;\n  display:-webkit-box;\n  -webkit-line-clamp:2;\n  -webkit-box-orient:vertical;\n  overflow:hidden;\n}\n\n\n/* ===== INS v1.05: approved DM Chat template ===== */\n.dm-chat-page{\n  background:#fff;\n  color:#111;\n}\n.dm-chat-topbar{\n  height:68px;\n  flex:0 0 68px;\n  display:flex;\n  align-items:center;\n  gap:11px;\n  padding:0 13px;\n  border-bottom:1px solid #efeff3;\n  background:#fff;\n  z-index:10;\n}\n.dm-chat-back{\n  width:34px;height:44px;border:0;background:none;padding:0;\n  display:grid;place-items:center;cursor:pointer;color:#111;\n}\n.dm-chat-back span{\n  font-size:38px;line-height:1;font-weight:300;transform:translateY(-1px);\n}\n.dm-chat-avatar{\n  width:42px;height:42px;border-radius:50%;\n  background:linear-gradient(135deg,#dfe3e9,#f4f5f7);\n  display:grid;place-items:center;font-size:12px;font-weight:800;color:#555;\n  flex:0 0 42px;\n}\n.dm-chat-id{min-width:0;flex:1}\n.dm-chat-name{\n  font-size:17px;font-weight:850;line-height:1.1;\n  white-space:nowrap;overflow:hidden;text-overflow:ellipsis;\n}\n.dm-chat-handle{\n  font-size:12px;color:#8b8f96;margin-top:3px;\n  white-space:nowrap;overflow:hidden;text-overflow:ellipsis;\n}\n.dm-chat-top-icon{\n  width:38px;height:38px;border:0;background:transparent;\n  display:grid;place-items:center;padding:0;cursor:pointer;flex:0 0 38px;\n}\n.dm-chat-top-icon svg{\n  width:25px;height:25px;fill:none;stroke:#111;stroke-width:2.15;\n  stroke-linecap:round;stroke-linejoin:round;\n}\n.dm-chat-scroll{\n  flex:1 1 auto;\n  overflow-y:auto;\n  padding:0 14px 96px;\n  scrollbar-width:none;\n  scroll-behavior:smooth;\n}\n.dm-chat-scroll::-webkit-scrollbar{display:none}\n.dm-chat-id-card{\n  padding:24px 0 28px;\n  text-align:center;\n}\n.dm-chat-big-avatar{\n  width:104px;height:104px;border-radius:50%;\n  background:linear-gradient(135deg,#dfe3e9,#f4f5f7);\n  display:grid;place-items:center;font-size:30px;font-weight:850;color:#555;\n  margin:0 auto 13px;\n}\n.dm-chat-card-name{\n  font-size:24px;font-weight:880;letter-spacing:-.3px;\n}\n.dm-chat-card-handle{\n  margin-top:5px;color:#858a92;font-size:15px;\n}\n.dm-chat-card-stats{\n  margin-top:7px;color:#858a92;font-size:14px;\n}\n.dm-chat-card-actions{\n  display:flex;justify-content:center;gap:34px;margin-top:26px;\n}\n.dm-chat-card-action{\n  min-width:58px;text-align:center;cursor:default;\n}\n.dm-chat-card-action.home-action{cursor:pointer}\n.dm-chat-card-action .circle{\n  width:46px;height:46px;border-radius:50%;display:grid;place-items:center;margin:0 auto 8px;color:#111;\n}\n.dm-chat-card-action svg{\n  width:31px;height:31px;stroke:#111;fill:none;stroke-width:2;stroke-linecap:round;stroke-linejoin:round;\n}\n.dm-chat-card-action span{\n  font-size:12px;font-weight:750;color:#444;\n}\n.dm-chat-time{\n  text-align:center;color:#9a9ea5;font-size:12px;margin:4px 0 14px;\n}\n.dm-chat-messages{\n  display:flex;flex-direction:column;gap:9px;\n}\n.dm-msg-row{\n  display:flex;align-items:flex-end;gap:8px;\n}\n.dm-msg-row.them{justify-content:flex-start}\n.dm-msg-row.me{justify-content:flex-end}\n.dm-msg-avatar{\n  width:28px;height:28px;border-radius:50%;\n  background:linear-gradient(135deg,#dfe3e9,#f4f5f7);\n  display:grid;place-items:center;font-size:9px;font-weight:800;color:#555;flex:0 0 28px;\n}\n.dm-bubble{\n  max-width:76%;\n  padding:10px 13px;\n  border-radius:20px;\n  font-size:15px;\n  line-height:1.45;\n  word-break:break-word;\n}\n.them .dm-bubble{\n  background:#f0f1f4;color:#111;border-bottom-left-radius:8px;\n}\n.me .dm-bubble{\n  background:#111;color:#fff;border-bottom-right-radius:8px;\n}\n.dm-char-photo{\n  max-width:68%;\n  border-radius:18px;\n  overflow:hidden;\n  background:#e7e9ef;\n  border:1px solid #e1e3e8;\n}\n.dm-char-photo .photo-box{\n  aspect-ratio:1/1;\n  display:grid;\n  place-items:center;\n  padding:20px;\n  text-align:center;\n  color:#6f747c;\n  font-size:13px;\n  line-height:1.55;\n  background:linear-gradient(135deg,#e5e8ee,#f8f8fa);\n}\n.dm-shared-cards{\n  display:flex;\n  flex-direction:column;\n  gap:10px;\n}\n.dm-typing{\n  display:none;align-items:flex-end;gap:8px;margin-top:3px;\n}\n.dm-typing.show{display:flex}\n.dm-typing-dots{\n  background:#f0f1f4;border-radius:20px;border-bottom-left-radius:8px;\n  padding:12px 14px;display:flex;gap:4px;\n}\n.dm-typing-dots span{\n  width:6px;height:6px;border-radius:50%;background:#9ca0a7;\n  animation:dmBounce 1s infinite ease-in-out;\n}\n.dm-typing-dots span:nth-child(2){animation-delay:.12s}\n.dm-typing-dots span:nth-child(3){animation-delay:.24s}\n@keyframes dmBounce{\n  0%,80%,100%{transform:translateY(0);opacity:.45}\n  40%{transform:translateY(-4px);opacity:1}\n}\n.dm-composer-wrap{\n  position:absolute;left:0;right:0;bottom:0;\n  padding:10px 12px 16px;\n  background:linear-gradient(to top,rgba(255,255,255,1) 72%,rgba(255,255,255,.88) 88%,rgba(255,255,255,0));\n  z-index:12;\n}\n.dm-composer{\n  min-height:54px;\n  border:1px solid rgba(255,255,255,.76);\n  border-radius:24px;\n  background:rgba(247,248,250,.76);\n  backdrop-filter:blur(24px) saturate(1.35);\n  -webkit-backdrop-filter:blur(24px) saturate(1.35);\n  box-shadow:0 8px 22px rgba(15,20,30,.11), inset 0 1px 0 rgba(255,255,255,.88);\n  display:flex;align-items:center;gap:8px;padding:7px 8px 7px 14px;\n}\n.dm-msg-input{\n  flex:1;min-width:0;border:0;outline:none;background:transparent;\n  height:38px;font:inherit;font-size:15px;color:#111;\n}\n.dm-msg-input::placeholder{color:#8f939a}\n.dm-composer-btn{\n  width:40px;height:40px;border:0;border-radius:50%;\n  background:rgba(255,255,255,.78);\n  color:#111;display:grid;place-items:center;cursor:pointer;flex:0 0 40px;\n}\n.dm-composer-btn.plane-btn{background:#111}\n.dm-composer-btn .plane-img{\n  width:21px;height:21px;object-fit:contain;display:block;filter:invert(1);\n}\n.dm-chat-glass-dim{\n  position:absolute;inset:0;background:rgba(17,17,17,.12);display:none;z-index:130;\n}\n.dm-chat-glass-dim.show{display:block}\n.dm-chat-glass-card{\n  position:absolute;left:12px;right:12px;bottom:84px;z-index:131;\n  border:1px solid rgba(255,255,255,.74);border-radius:26px;\n  background:rgba(247,248,250,.84);\n  backdrop-filter:blur(28px) saturate(1.45);\n  -webkit-backdrop-filter:blur(28px) saturate(1.45);\n  box-shadow:0 18px 45px rgba(15,20,30,.18), inset 0 1px 0 rgba(255,255,255,.9);\n  padding:14px;opacity:0;transform:translateY(10px) scale(.985);\n  pointer-events:none;transition:.2s cubic-bezier(.2,.8,.2,1);\n}\n.dm-chat-glass-card.top{top:74px;bottom:auto}\n.dm-chat-glass-card.show{\n  opacity:1;transform:translateY(0) scale(1);pointer-events:auto;\n}\n.dm-chat-glass-head{\n  display:flex;align-items:center;justify-content:space-between;margin-bottom:12px;\n}\n.dm-chat-glass-title{font-size:18px;font-weight:850}\n.dm-chat-glass-close{\n  width:30px;height:30px;border:0;border-radius:50%;background:rgba(255,255,255,.76);\n  color:#111;font-size:18px;cursor:pointer;\n}\n.dm-emoji-grid{\n  display:grid;grid-template-columns:repeat(6,1fr);gap:8px;\n}\n.dm-emoji-btn{\n  height:44px;border:0;border-radius:15px;background:rgba(255,255,255,.72);\n  font-size:22px;cursor:pointer;\n}\n.dm-menu-action{\n  width:100%;height:50px;border:0;border-radius:16px;background:rgba(255,255,255,.72);\n  color:#111;display:flex;align-items:center;justify-content:space-between;\n  padding:0 14px;font-size:15px;font-weight:800;cursor:pointer;\n}\n.dm-menu-action.danger{color:#ff3b4f}\n.dm-confirm-copy{\n  color:#767b83;font-size:13px;line-height:1.5;margin-bottom:12px;\n}\n.dm-confirm-row{display:grid;grid-template-columns:1fr 1fr;gap:9px}\n.dm-confirm-btn{\n  height:47px;border:0;border-radius:15px;font-size:14px;font-weight:800;cursor:pointer;\n}\n.dm-confirm-btn.cancel{background:rgba(255,255,255,.78);color:#111}\n.dm-confirm-btn.danger{background:#ff3b4f;color:#fff}\n\n\n/* ===== INS v1.06: User Profile tabs + drawer ===== */\n.user-profile-page{\n  height:100%;\n  display:flex;\n  flex-direction:column;\n  background:#fff;\n  color:#111;\n}\n.user-profile-top{\n  flex:0 0 62px;\n  height:62px;\n  display:flex;\n  align-items:center;\n  justify-content:space-between;\n  padding:0 18px;\n  border-bottom:1px solid #efeff3;\n  background:#fff;\n}\n.user-profile-id{\n  font-size:22px;\n  font-weight:850;\n  letter-spacing:-.2px;\n}\n.user-profile-menu{\n  width:42px;\n  height:42px;\n  border:0;\n  background:transparent;\n  display:grid;\n  place-items:center;\n  cursor:pointer;\n  padding:0;\n}\n.user-profile-menu svg{\n  width:28px;\n  height:28px;\n  stroke:#111;\n  fill:none;\n  stroke-width:2.2;\n  stroke-linecap:round;\n}\n.user-profile-scroll{\n  flex:1 1 auto;\n  overflow-y:auto;\n  scrollbar-width:none;\n  padding-bottom:8px;\n}\n.user-profile-scroll::-webkit-scrollbar{display:none}\n.user-profile-head{\n  padding:18px 18px 14px;\n}\n.user-profile-mainrow{\n  display:flex;\n  align-items:center;\n  gap:18px;\n}\n.user-avatar-wrap{\n  width:88px;\n  height:88px;\n  flex:0 0 88px;\n  border-radius:50%;\n  background:linear-gradient(135deg,#d9dce3,#f3f4f7);\n  position:relative;\n  display:grid;\n  place-items:center;\n  font-size:28px;\n  font-weight:850;\n  color:#555;\n}\n.user-avatar-add{\n  position:absolute;\n  right:-1px;\n  bottom:-1px;\n  width:28px;\n  height:28px;\n  border-radius:50%;\n  border:2px solid #fff;\n  background:#111;\n  color:#fff;\n  display:grid;\n  place-items:center;\n  font-size:19px;\n  font-weight:800;\n}\n.user-stats{\n  flex:1;\n  min-width:0;\n  display:flex;\n  justify-content:space-around;\n  gap:12px;\n}\n.user-stat{\n  text-align:center;\n  min-width:0;\n}\n.user-stat strong{\n  display:block;\n  font-size:20px;\n  font-weight:850;\n}\n.user-stat span{\n  display:block;\n  margin-top:3px;\n  font-size:12px;\n  color:#666b72;\n}\n.profile-name-location-line{\n  display:flex;align-items:center;gap:8px;flex-wrap:wrap;margin-top:18px;\n}\n.profile-name-location-line .user-profile-name,.profile-name-location-line .char-profile-name{margin-top:0}\n.profile-location-inline{\n  display:none;align-items:center;gap:3px;color:#8b9098;font-size:11px;font-weight:650;line-height:1.2;\n}\n.profile-location-inline.show{display:inline-flex}\n.profile-location-inline svg{width:12px;height:12px;fill:none;stroke:currentColor;stroke-width:1.8;stroke-linecap:round;stroke-linejoin:round}\n\n.user-profile-name{\n  margin-top:16px;\n  font-size:16px;\n  font-weight:850;\n}\n.user-profile-handle{\n  margin-top:3px;\n  font-size:13px;\n  color:#777c84;\n}\n.user-profile-bio{\n  margin-top:8px;\n  font-size:13px;\n  line-height:1.55;\n  color:#30343a;\n}\n.user-profile-buttons{\n  display:grid;\n  grid-template-columns:minmax(0,1fr) minmax(0,1fr) 34px;\n  gap:8px;\n  margin-top:14px;\n  align-items:center;\n}\n.user-profile-btn{\n  height:34px;\n  border:0;\n  border-radius:11px;\n  background:#f1f2f5;\n  color:#111;\n  display:grid;\n  place-items:center;\n  font-size:12px;\n  font-weight:800;\n  cursor:pointer;\n  padding:0 10px;\n}\n.user-profile-size-btn{\n  width:34px;\n  height:34px;\n  border:0;\n  border-radius:50%;\n  background:#f1f2f5;\n  color:#111;\n  display:grid;\n  place-items:center;\n  padding:0;\n  font-size:22px;\n  line-height:1;\n  font-weight:400;\n  cursor:pointer;\n  touch-action:manipulation;\n}\n.user-profile-size-btn:active{transform:scale(.94)}\n\n.user-profile-tabs{\n  position:sticky;\n  top:0;\n  z-index:5;\n  height:52px;\n  display:grid;\n  grid-template-columns:repeat(3,1fr);\n  background:#fff;\n  border-top:1px solid #f0f0f3;\n  border-bottom:1px solid #f0f0f3;\n}\n.user-tab{\n  position:relative;\n  border:0;\n  background:#fff;\n  display:grid;\n  place-items:center;\n  cursor:pointer;\n  padding:0;\n}\n.user-tab svg{\n  width:25px;\n  height:25px;\n  fill:none;\n  stroke:#7f838b;\n  stroke-width:2;\n  stroke-linecap:round;\n  stroke-linejoin:round;\n  transition:transform .22s ease, stroke .22s ease, opacity .22s ease;\n  opacity:.98;\n}\n.user-tab.active svg{\n  stroke:#111;\n  transform:scale(1.04);\n}\n.user-tab-indicator{\n  position:absolute;\n  left:0;\n  bottom:0;\n  width:33.3333%;\n  height:2.5px;\n  border-radius:999px;\n  background:#111;\n  transform:translateX(0%);\n  transition:transform .28s cubic-bezier(.2,.8,.2,1);\n  pointer-events:none;\n}\n.user-grid9-icon{\n  width:24px;\n  height:24px;\n  display:grid;\n  grid-template-columns:repeat(3,1fr);\n  grid-template-rows:repeat(3,1fr);\n  gap:2.2px;\n  transition:transform .22s ease, opacity .22s ease;\n}\n.user-grid9-icon span{\n  display:block;\n  width:100%;\n  height:100%;\n  border-radius:2px;\n  background:#7f838b;\n  transition:background .22s ease;\n}\n.user-tab.active .user-grid9-icon{\n  transform:scale(1.04);\n}\n.user-tab.active .user-grid9-icon span{\n  background:#111;\n}\n.user-grid{\n  display:grid;\n  grid-template-columns:repeat(3,1fr);\n  gap:2px;\n  background:#fff;\n  padding-top:2px;\n}\n.user-cell{\n  aspect-ratio:1/1;\n  background:linear-gradient(135deg,#eceef2,#dfe2e8);\n  display:grid;\n  place-items:center;\n  text-align:center;\n  padding:8px;\n  color:#70757d;\n  font-size:11px;\n  line-height:1.35;\n  position:relative;\n  overflow:hidden;\n}\n.user-cell.video::after{\n  content:\"▶\";\n  position:absolute;\n  right:7px;\n  top:5px;\n  color:#fff;\n  text-shadow:0 1px 4px rgba(0,0,0,.35);\n  font-size:13px;\n}\n.user-cell.repost::after{\n  content:\"↻\";\n  position:absolute;\n  right:7px;\n  top:5px;\n  color:#111;\n  font-size:16px;\n  font-weight:800;\n}\n.user-empty{\n  min-height:210px;\n  display:flex;\n  flex-direction:column;\n  align-items:center;\n  justify-content:center;\n  text-align:center;\n  color:#8d9198;\n  font-size:13px;\n  line-height:1.55;\n  padding:24px;\n}\n.user-empty strong{\n  color:#444950;\n  font-size:15px;\n  margin-bottom:6px;\n}\n\n/* profile edit modal */\n.profile-edit-card{\n  position:absolute;\n  left:12px;\n  right:12px;\n  bottom:84px;\n  z-index:152;\n  border:1px solid rgba(255,255,255,.74);\n  border-radius:26px;\n  background:rgba(247,248,250,.84);\n  backdrop-filter:blur(28px) saturate(1.45);\n  -webkit-backdrop-filter:blur(28px) saturate(1.45);\n  box-shadow:0 18px 45px rgba(15,20,30,.18), inset 0 1px 0 rgba(255,255,255,.9);\n  padding:14px;\n  opacity:0;\n  transform:translateY(10px) scale(.985);\n  pointer-events:none;\n  transition:.2s cubic-bezier(.2,.8,.2,1);\n}\n.profile-edit-card.show{\n  opacity:1;\n  transform:translateY(0) scale(1);\n  pointer-events:auto;\n}\n.profile-edit-title{\n  font-size:18px;\n  font-weight:850;\n  margin-bottom:12px;\n  display:flex;\n  align-items:center;\n  justify-content:space-between;\n}\n.profile-edit-close{\n  width:30px;\n  height:30px;\n  border:0;\n  border-radius:50%;\n  background:rgba(255,255,255,.76);\n  color:#111;\n  font-size:18px;\n  cursor:pointer;\n}\n.profile-edit-label{\n  display:block;\n  font-size:12px;\n  font-weight:800;\n  color:#666b72;\n  margin:10px 0 6px;\n}\n.profile-edit-input,.profile-edit-textarea{\n  width:100%;\n  border:1px solid rgba(0,0,0,.07);\n  border-radius:14px;\n  background:rgba(255,255,255,.86);\n  outline:none;\n  font:inherit;\n  font-size:14px;\n  color:#111;\n}\n.profile-edit-input{\n  height:44px;\n  padding:0 12px;\n}\n.profile-edit-textarea{\n  min-height:86px;\n  resize:none;\n  padding:11px 12px;\n  line-height:1.5;\n}\n.profile-edit-save{\n  width:100%;\n  height:48px;\n  border:0;\n  border-radius:16px;\n  background:#111;\n  color:#fff;\n  font-size:15px;\n  font-weight:850;\n  cursor:pointer;\n  margin-top:12px;\n}\n\n/* user profile drawer */\n.profile-drawer-dim{\n  position:absolute;\n  inset:0 0 68px 0;\n  z-index:150;\n  display:none;\n  background:rgba(17,17,17,.10);\n}\n.profile-drawer-dim.show{display:block}\n.profile-drawer{\n  position:absolute;\n  top:0;\n  right:0;\n  bottom:68px;\n  width:min(86%,360px);\n  z-index:151;\n  background:rgba(247,248,250,.88);\n  backdrop-filter:blur(30px) saturate(1.45);\n  -webkit-backdrop-filter:blur(30px) saturate(1.45);\n  border-left:1px solid rgba(255,255,255,.74);\n  box-shadow:-16px 0 42px rgba(15,20,30,.18);\n  transform:translateX(104%);\n  transition:.24s cubic-bezier(.2,.8,.2,1);\n  padding:16px 14px 38px;\n  overflow-y:auto;\n  overflow-x:hidden;\n  overscroll-behavior:contain;\n  -webkit-overflow-scrolling:touch;\n  scrollbar-width:none;\n  scroll-padding-top:70px;\n}\n.profile-drawer::-webkit-scrollbar{width:0;height:0;display:none}\n.profile-drawer.show{transform:translateX(0)}\n.drawer-head{\n  position:sticky;\n  top:-16px;\n  z-index:6;\n  height:60px;\n  display:flex;\n  align-items:center;\n  justify-content:space-between;\n  margin:-16px -2px 8px;\n  padding:16px 2px 8px;\n  background:linear-gradient(to bottom,rgba(247,248,250,.98),rgba(247,248,250,.86));\n  backdrop-filter:blur(18px);\n  -webkit-backdrop-filter:blur(18px);\n}\n.drawer-title{\n  font-size:20px;\n  font-weight:850;\n}\n.drawer-close{\n  width:32px;\n  height:32px;\n  border:0;\n  border-radius:50%;\n  background:rgba(255,255,255,.78);\n  font-size:18px;\n  cursor:pointer;\n}\n.drawer-section{\n  border-radius:19px;\n  background:rgba(255,255,255,.66);\n  border:1px solid rgba(255,255,255,.76);\n  overflow:hidden;\n  margin-top:11px;\n}\n.drawer-item{\n  width:100%;\n  min-height:54px;\n  border:0;\n  background:transparent;\n  display:flex;\n  align-items:center;\n  gap:12px;\n  padding:0 14px;\n  font:inherit;\n  color:#111;\n  cursor:pointer;\n  border-bottom:1px solid rgba(0,0,0,.055);\n}\n.drawer-item:last-child{border-bottom:0}\n.drawer-icon{\n  width:28px;\n  height:28px;\n  border-radius:10px;\n  background:#f0f1f5;\n  display:grid;\n  place-items:center;\n  font-size:15px;\n  flex:0 0 28px;\n}\n.drawer-copy{\n  flex:1;\n  min-width:0;\n  text-align:left;\n}\n.drawer-copy strong{\n  display:block;\n  font-size:14px;\n}\n.drawer-copy span{\n  display:block;\n  margin-top:2px;\n  color:#7e838b;\n  font-size:11px;\n}\n.drawer-arrow{\n  color:#a2a5ab;\n  font-size:19px;\n}\n.drawer-note{\n  padding:14px 6px 18px;\n  color:#8b8f96;\n  font-size:11px;\n  line-height:1.55;\n}\n.profile-drawer .drawer-section{box-shadow:0 8px 24px rgba(31,37,47,.035)}\n.profile-drawer .drawer-item:active{background:rgba(235,238,243,.62)}\n.drawer-item.drawer-item-danger .drawer-icon{background:#fff0f0;color:#c44242}\n.drawer-item.drawer-item-danger .drawer-copy strong{color:#bd3d3d}\n.drawer-item.drawer-item-danger .drawer-arrow{color:#d09a9a}\n.identity-bind-list{display:grid;gap:10px}\n.identity-bind-row{display:flex;align-items:center;gap:12px;padding:12px 2px;border-bottom:1px solid #eceef2}\n.identity-bind-row:last-child{border-bottom:0}\n.identity-bind-avatar{width:44px;height:44px;border-radius:50%;background:#e6e8ed;overflow:hidden;display:grid;place-items:center;font-size:14px;font-weight:800;color:#505762;background-size:cover;background-position:center}\n.identity-bind-copy{min-width:0;flex:1}\n.identity-bind-name{font-size:14px;font-weight:800;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}\n.identity-bind-handle{margin-top:4px;font-size:12px;color:#7a8088;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}\n.identity-bind-meta{margin-top:4px;font-size:11px;color:#8d939b;line-height:1.45}\n.identity-bind-apply{height:36px;border:0;border-radius:999px;background:#111;color:#fff;padding:0 14px;font-size:12px;font-weight:850;cursor:pointer;white-space:nowrap}\n.identity-bind-apply.light{background:#f1f2f5;color:#111}\n.identity-bind-empty{padding:14px 4px 2px;color:#8b9199;font-size:12px;line-height:1.6;text-align:center}\n\n\n/* ===== INS v1.07: User Profile plus URL + edit stats + highlights ===== */\n.user-avatar-add.url-plus{\n  background:transparent;\n  border:0;\n  width:31px;\n  height:31px;\n  right:-3px;\n  bottom:-3px;\n  padding:0;\n  overflow:hidden;\n}\n.user-avatar-add.url-plus img{\n  width:100%;\n  height:100%;\n  object-fit:contain;\n  display:block;\n}\n.user-profile-btn.disabled{\n  color:#9a9ea5;\n  cursor:default;\n  pointer-events:none;\n}\n.user-highlight-strip{\n  display:flex;\n  gap:14px;\n  overflow-x:auto;\n  scrollbar-width:none;\n  padding:4px 18px 15px;\n  border-top:1px solid #f5f5f7;\n}\n.user-highlight-strip::-webkit-scrollbar{display:none}\n.user-highlight{\n  width:68px;\n  flex:0 0 68px;\n  text-align:center;\n}\n.user-highlight-ring{\n  width:62px;\n  height:62px;\n  margin:0 auto;\n  border-radius:50%;\n  border:1.5px solid #d8dbe2;\n  background:linear-gradient(135deg,#f4f5f7,#ebeef3);\n  display:grid;\n  place-items:center;\n  color:#777c84;\n  font-size:20px;\n  font-weight:600;\n}\n.user-highlight.add .user-highlight-ring{\n  border-style:dashed;\n  background:#fff;\n  font-size:28px;\n  font-weight:300;\n}\n.user-highlight-name{\n  margin-top:6px;\n  font-size:11px;\n  color:#30343a;\n  white-space:nowrap;\n  overflow:hidden;\n  text-overflow:ellipsis;\n}\n.profile-edit-row{\n  display:grid;\n  grid-template-columns:1fr 1fr;\n  gap:10px;\n}\n\n\n/* ===== INS v1.09: user profile icon polish + highlight editor + avatar override ===== */\n.user-tab.video-tab svg{\n  width:30px;\n  height:30px;\n}\n.user-tab.repost-tab img{\n  width:29px;\n  height:29px;\n  object-fit:contain;\n  display:block;\n  opacity:.56;\n  transition:transform .22s ease, opacity .22s ease, filter .22s ease;\n  filter:grayscale(1) brightness(.55);\n}\n.user-tab.repost-tab.active img{\n  transform:scale(1.04);\n  opacity:1;\n  filter:grayscale(1) brightness(0);\n}\n.user-profile-btn.placeholder{\n  color:#111;\n  cursor:default;\n  pointer-events:none;\n}\n.user-avatar-wrap.has-image{\n  background-size:cover;\n  background-position:center;\n  color:transparent;\n}\n.profile-avatar-preview{\n  width:62px;\n  height:62px;\n  border-radius:50%;\n  background:linear-gradient(135deg,#d9dce3,#f3f4f7);\n  display:grid;\n  place-items:center;\n  color:#555;\n  font-size:20px;\n  font-weight:850;\n  margin:0 auto 10px;\n  overflow:hidden;\n}\n.profile-avatar-preview.has-image{\n  background-size:cover;\n  background-position:center;\n  color:transparent;\n}\n.profile-edit-mini{\n  display:grid;\n  grid-template-columns:1fr 1fr;\n  gap:8px;\n  margin-top:8px;\n}\n.profile-edit-mini button{\n  height:38px;\n  border:0;\n  border-radius:13px;\n  background:rgba(255,255,255,.78);\n  color:#111;\n  font-size:12px;\n  font-weight:800;\n  cursor:pointer;\n}\n.highlight-cover-img{\n  width:100%;\n  height:100%;\n  object-fit:cover;\n  display:block;\n  border-radius:50%;\n}\n.highlight-editor-card{\n  position:absolute;\n  left:12px;\n  right:12px;\n  bottom:84px;\n  z-index:153;\n  border:1px solid rgba(255,255,255,.74);\n  border-radius:26px;\n  background:rgba(247,248,250,.86);\n  backdrop-filter:blur(28px) saturate(1.45);\n  -webkit-backdrop-filter:blur(28px) saturate(1.45);\n  box-shadow:0 18px 45px rgba(15,20,30,.18), inset 0 1px 0 rgba(255,255,255,.9);\n  padding:14px;\n  opacity:0;\n  transform:translateY(10px) scale(.985);\n  pointer-events:none;\n  transition:.2s cubic-bezier(.2,.8,.2,1);\n}\n.highlight-editor-card.show{\n  opacity:1;\n  transform:translateY(0) scale(1);\n  pointer-events:auto;\n}\n.highlight-editor-title{\n  font-size:18px;\n  font-weight:850;\n  margin-bottom:12px;\n  display:flex;\n  align-items:center;\n  justify-content:space-between;\n}\n.highlight-close{\n  width:30px;\n  height:30px;\n  border:0;\n  border-radius:50%;\n  background:rgba(255,255,255,.76);\n  color:#111;\n  font-size:18px;\n  cursor:pointer;\n}\n.highlight-preview{\n  width:74px;\n  height:74px;\n  margin:0 auto 12px;\n  border-radius:50%;\n  border:1.5px solid #d8dbe2;\n  background:linear-gradient(135deg,#f4f5f7,#ebeef3);\n  display:grid;\n  place-items:center;\n  color:#777c84;\n  font-size:23px;\n  overflow:hidden;\n}\n.highlight-preview.has-image{\n  background-size:cover;\n  background-position:center;\n  color:transparent;\n}\n.highlight-field-label{\n  display:block;\n  font-size:12px;\n  font-weight:800;\n  color:#666b72;\n  margin:10px 0 6px;\n}\n.highlight-input{\n  width:100%;\n  height:44px;\n  border:1px solid rgba(0,0,0,.07);\n  border-radius:14px;\n  background:rgba(255,255,255,.86);\n  outline:none;\n  font:inherit;\n  font-size:14px;\n  color:#111;\n  padding:0 12px;\n}\n.highlight-content-box{\n  border-radius:17px;\n  background:rgba(255,255,255,.68);\n  border:1px solid rgba(255,255,255,.76);\n  padding:13px;\n  margin-top:10px;\n}\n.highlight-content-title{\n  font-size:13px;\n  font-weight:850;\n  margin-bottom:5px;\n}\n.highlight-content-empty{\n  color:#858a92;\n  font-size:12px;\n  line-height:1.5;\n}\n.highlight-actions{\n  display:grid;\n  grid-template-columns:1fr 1fr;\n  gap:9px;\n  margin-top:12px;\n}\n.highlight-btn{\n  height:46px;\n  border:0;\n  border-radius:15px;\n  font-size:14px;\n  font-weight:850;\n  cursor:pointer;\n}\n.highlight-btn.save{\n  background:#111;\n  color:#fff;\n}\n.highlight-btn.delete{\n  background:rgba(255,255,255,.78);\n  color:#ff3b4f;\n}\n\n\n/* ===== INS v1.12: clean DM list v2 restored ===== */\n.dm-v2-page{\n  height:100%;\n  display:flex;\n  flex-direction:column;\n  background:#fff;\n  color:#111;\n}\n.dm-v2-head{\n  flex:0 0 auto;\n  display:flex;\n  align-items:center;\n  justify-content:space-between;\n  padding:16px 18px 10px;\n}\n.dm-v2-id{\n  font-size:23px;\n  font-weight:850;\n  letter-spacing:-.2px;\n}\n.dm-v2-social{\n  width:38px;\n  height:38px;\n  border:0;\n  border-radius:50%;\n  background:#f1f2f5;\n  display:grid;\n  place-items:center;\n  cursor:pointer;\n}\n.dm-v2-social svg{\n  width:19px;\n  height:19px;\n  fill:none;\n  stroke:#111;\n  stroke-width:2;\n  stroke-linecap:round;\n  stroke-linejoin:round;\n}\n.dm-v2-search-wrap{\n  padding:0 18px 7px;\n}\n.dm-v2-search{\n  width:100%;\n  height:35px;\n  border:0;\n  border-radius:12px;\n  background:#f1f2f6;\n  padding:0 13px;\n  outline:none;\n  font:inherit;\n  font-size:13px;\n  color:#111;\n}\n.dm-v2-search::placeholder{color:#979ba2}\n.dm-avatar-strip{\n  flex:0 0 auto;\n  display:flex;\n  gap:14px;\n  padding:12px 14px 15px;\n  overflow-x:auto;\n  scrollbar-width:none;\n  border-bottom:1px solid #f1f1f4;\n}\n.dm-avatar-strip::-webkit-scrollbar{display:none}\n.dm-profile-item{\n  width:70px;\n  flex:0 0 70px;\n  text-align:center;\n  cursor:pointer;\n  min-width:0;\n}\n.dm-profile-ring{\n  width:60px;\n  height:60px;\n  margin:0 auto;\n  border-radius:50%;\n  background:linear-gradient(135deg,#dfe3e9,#f5f6f8);\n  position:relative;\n  display:grid;\n  place-items:center;\n  font-size:13px;\n  font-weight:800;\n  color:#555;\n}\n.dm-profile-ring.user-ring{border:1px solid #e5e7eb}\n.dm-note-bubble{\n  position:absolute;\n  top:-7px;\n  left:50%;\n  transform:translateX(-50%);\n  min-width:58px;\n  max-width:88px;\n  padding:5px 7px;\n  border-radius:11px;\n  background:#fff;\n  border:1px solid #e5e7ec;\n  box-shadow:0 4px 12px rgba(0,0,0,.06);\n  font-size:10px;\n  font-weight:700;\n  color:#555;\n  white-space:nowrap;\n  overflow:hidden;\n  text-overflow:ellipsis;\n}\n.dm-profile-name{\n  margin-top:7px;\n  font-size:11px;\n  white-space:nowrap;\n  overflow:hidden;\n  text-overflow:ellipsis;\n  color:#222;\n}\n.dm-profile-plus{\n  width:60px;\n  height:60px;\n  margin:0 auto;\n  border-radius:50%;\n  border:1.5px dashed #c9ccd2;\n  display:grid;\n  place-items:center;\n  font-size:30px;\n  font-weight:300;\n  color:#777b83;\n  background:#fafafa;\n}\n.dm-section-head{\n  display:flex;\n  align-items:center;\n  justify-content:space-between;\n  padding:14px 18px 7px;\n}\n.dm-section-title{font-size:15px;font-weight:800}\n.dm-section-sub{font-size:11px;color:#8b8f96}\n.dm-v2-list{\n  flex:1 1 auto;\n  overflow-y:auto;\n  padding:0 10px 16px;\n  scrollbar-width:none;\n}\n.dm-v2-list::-webkit-scrollbar{display:none}\n.dm-v2-row{\n  display:flex;\n  align-items:center;\n  gap:9px;\n  min-height:68px;\n  padding:6px 8px;\n  border-radius:17px;\n  cursor:pointer;\n}\n.dm-v2-row:active{background:#f6f6f8}\n.dm-v2-avatar{\n  width:54px;\n  height:54px;\n  flex:0 0 54px;\n  border-radius:50%;\n  background:linear-gradient(135deg,#dfe3e9,#f3f4f7);\n  display:grid;\n  place-items:center;\n  font-size:12px;\n  font-weight:800;\n  color:#555;\n}\n.dm-v2-main{min-width:0;flex:1}\n.dm-v2-topline{\n  display:flex;\n  align-items:center;\n  gap:7px;\n  min-width:0;\n}\n.dm-v2-name{\n  font-size:15px;\n  font-weight:800;\n  white-space:nowrap;\n  overflow:hidden;\n  text-overflow:ellipsis;\n}\n.dm-v2-badge{\n  flex:0 0 auto;\n  padding:3px 6px;\n  border-radius:999px;\n  background:#f1f2f5;\n  color:#747982;\n  font-size:9px;\n  font-weight:800;\n}\n.dm-v2-time{\n  margin-left:auto;\n  flex:0 0 auto;\n  font-size:10px;\n  color:#a0a4aa;\n}\n.dm-v2-preview{\n  margin-top:4px;\n  font-size:12px;\n  color:#7f838b;\n  white-space:nowrap;\n  overflow:hidden;\n  text-overflow:ellipsis;\n}\n.dm-v2-unread{\n  width:8px;\n  height:8px;\n  border-radius:50%;\n  background:#111;\n  flex:0 0 8px;\n}\n.dm-v2-arrow{\n  color:#aaa;\n  font-size:20px;\n  flex:0 0 auto;\n}\n\n/* DM list small glass cards */\n.dm-list-glass-dim{\n  position:absolute;\n  inset:0 0 68px 0;\n  z-index:120;\n  display:none;\n  background:rgba(17,17,17,.10);\n}\n.dm-list-glass-dim.show{display:block}\n.dm-list-glass-card{\n  position:absolute;\n  left:12px;\n  right:12px;\n  bottom:80px;\n  z-index:121;\n  border:1px solid rgba(255,255,255,.74);\n  border-radius:26px;\n  background:rgba(247,248,250,.84);\n  backdrop-filter:blur(28px) saturate(1.45);\n  -webkit-backdrop-filter:blur(28px) saturate(1.45);\n  box-shadow:0 18px 45px rgba(15,20,30,.18), inset 0 1px 0 rgba(255,255,255,.9);\n  padding:14px;\n  opacity:0;\n  transform:translateY(10px) scale(.985);\n  pointer-events:none;\n  transition:.2s cubic-bezier(.2,.8,.2,1);\n}\n.dm-list-glass-card.show{\n  opacity:1;\n  transform:translateY(0) scale(1);\n  pointer-events:auto;\n}\n.dm-list-glass-head{\n  display:flex;\n  align-items:center;\n  justify-content:space-between;\n  margin-bottom:12px;\n}\n.dm-list-glass-title{font-size:18px;font-weight:850}\n.dm-list-glass-close{\n  width:30px;\n  height:30px;\n  border:0;\n  border-radius:50%;\n  background:rgba(255,255,255,.76);\n  font-size:18px;\n  cursor:pointer;\n}\n.dm-list-block{\n  border-radius:18px;\n  background:rgba(255,255,255,.68);\n  border:1px solid rgba(255,255,255,.75);\n  padding:13px;\n  margin-top:10px;\n}\n.dm-list-label{\n  font-size:13px;\n  font-weight:800;\n  color:#646971;\n  margin-bottom:8px;\n}\n.dm-list-input{\n  width:100%;\n  height:46px;\n  border:1px solid rgba(0,0,0,.07);\n  border-radius:14px;\n  background:rgba(255,255,255,.86);\n  padding:0 13px;\n  outline:none;\n  font:inherit;\n  font-size:14px;\n}\n.dm-list-btn{\n  width:100%;\n  height:46px;\n  border:0;\n  border-radius:15px;\n  background:#111;\n  color:#fff;\n  font-size:14px;\n  font-weight:800;\n  cursor:pointer;\n  margin-top:10px;\n}\n.dm-list-btn.light{background:rgba(255,255,255,.82);color:#111}\n.dm-list-copy{\n  color:#7f838b;\n  font-size:12px;\n  line-height:1.55;\n}\n.dm-list-contact{\n  display:flex;\n  align-items:center;\n  gap:10px;\n  padding:10px 0;\n  border-bottom:1px solid rgba(0,0,0,.06);\n}\n.dm-list-contact:last-child{border-bottom:0}\n.dm-list-contact-avatar{\n  width:38px;\n  height:38px;\n  border-radius:50%;\n  background:#e4e6eb;\n  display:grid;\n  place-items:center;\n  font-size:11px;\n  font-weight:800;\n  color:#555;\n}\n\n/* ===== v1.15.26 unified identity visuals / translation controls ===== */\n[data-identity-avatar],[data-actor-avatar]{\n  background-size:cover!important;\n  background-position:center!important;\n  background-repeat:no-repeat!important;\n}\n.comment-avatar,.feed-notice-avatar,.reels-v15-avatar,.reels-v15-comment-avatar,.reels-v15-spinavatar,.dm-list-contact-avatar,.profile-chip{\n  overflow:hidden;\n  background-size:cover;\n  background-position:center;\n  background-repeat:no-repeat;\n}\n.reels-v15-translate-toggle{\n  border:0;\n  background:transparent;\n  color:rgba(255,255,255,.68);\n  padding:4px 0 0;\n  font:inherit;\n  font-size:10px;\n  font-weight:750;\n  cursor:pointer;\n}\n.reels-v15-comment-actions{\n  display:flex;\n  gap:9px;\n  margin-top:5px;\n}\n.reels-v15-comment-translate{\n  border:0;\n  background:transparent;\n  color:rgba(255,255,255,.52);\n  padding:0;\n  font:inherit;\n  font-size:9px;\n  cursor:pointer;\n}\n.reels-v15-comment-translation{\n  margin-top:6px;\n  padding:7px 8px;\n  border-radius:9px;\n  background:rgba(255,255,255,.08);\n  color:rgba(255,255,255,.76);\n  font-size:10px;\n  line-height:1.45;\n}\n.dm-msg-stack{\n  min-width:0;\n  max-width:min(78%,310px);\n}\n.dm-msg-stack .dm-bubble{\n  max-width:none;\n}\n.dm-translation-toggle{\n  border:0;\n  background:transparent;\n  padding:4px 3px 0;\n  color:#8a8f97;\n  font:inherit;\n  font-size:9px;\n  cursor:pointer;\n}\n.dm-translation-box{\n  margin-top:5px;\n  padding:7px 9px;\n  border-radius:10px;\n  background:#eceef2;\n  color:#555b63;\n  font-size:10px;\n  line-height:1.45;\n}\n.dm-msg-row.me .dm-translation-toggle{\n  display:none;\n}\n.user-cell img{\n  width:100%;\n  height:100%;\n  object-fit:cover;\n  display:block;\n}\n.user-cell.reel-live{\n  position:relative;\n  background:#111;\n  color:#fff;\n  display:flex;\n  align-items:flex-end;\n  padding:10px;\n  text-align:left;\n  font-size:10px;\n  line-height:1.4;\n}\n.user-cell.reel-live::after{\n  content:\"▶\";\n  position:absolute;\n  top:9px;\n  right:9px;\n  font-size:12px;\n  opacity:.9;\n}\n.dm-list-contact-copy{min-width:0;flex:1}\n.dm-list-contact-copy strong{display:block;font-size:14px}\n.dm-list-contact-copy span{font-size:11px;color:#898d94}\n.dm-list-contact-action{\n  height:32px;\n  border:0;\n  border-radius:11px;\n  background:#f0f1f4;\n  padding:0 10px;\n  font-size:11px;\n  font-weight:800;\n  cursor:pointer;\n}\n\n\n/* ===== INS v1.13: Char Profile ===== */\n.char-profile-page{\n  background:#fff;\n  color:#111;\n}\n.char-profile-top{\n  height:62px;\n  flex:0 0 62px;\n  display:flex;\n  align-items:center;\n  gap:8px;\n  padding:0 12px;\n  border-bottom:1px solid #efeff3;\n  background:#fff;\n}\n.char-profile-back{\n  width:38px;\n  height:38px;\n  border:0;\n  background:transparent;\n  display:grid;\n  place-items:center;\n  padding:0;\n  cursor:pointer;\n  color:#111;\n}\n.char-profile-back span{\n  font-size:36px;\n  line-height:1;\n  font-weight:300;\n  transform:translateY(-1px);\n}\n.char-profile-top-id{\n  flex:1;\n  min-width:0;\n  font-size:20px;\n  font-weight:850;\n  white-space:nowrap;\n  overflow:hidden;\n  text-overflow:ellipsis;\n}\n.char-profile-menu{\n  width:42px;\n  height:42px;\n  border:0;\n  background:transparent;\n  display:grid;\n  place-items:center;\n  padding:0;\n  cursor:pointer;\n}\n.char-profile-menu svg{\n  width:28px;\n  height:28px;\n  stroke:#111;\n  fill:none;\n  stroke-width:2.2;\n  stroke-linecap:round;\n}\n.char-profile-scroll{\n  flex:1 1 auto;\n  overflow-y:auto;\n  scrollbar-width:none;\n}\n.char-profile-scroll::-webkit-scrollbar{display:none}\n.char-profile-head{\n  padding:18px 18px 14px;\n}\n.char-profile-mainrow{\n  display:flex;\n  align-items:center;\n  gap:18px;\n}\n.char-profile-avatar{\n  width:88px;\n  height:88px;\n  flex:0 0 88px;\n  border-radius:50%;\n  background:linear-gradient(135deg,#d9dce3,#f3f4f7);\n  display:grid;\n  place-items:center;\n  font-size:28px;\n  font-weight:850;\n  color:#555;\n  overflow:hidden;\n}\n.char-profile-avatar.has-image{\n  background-size:cover;\n  background-position:center;\n  color:transparent;\n}\n.char-profile-stats{\n  flex:1;\n  min-width:0;\n  display:flex;\n  justify-content:space-around;\n  gap:12px;\n}\n.char-profile-stat{text-align:center;min-width:0}\n.char-profile-stat strong{\n  display:block;\n  font-size:20px;\n  font-weight:850;\n}\n.char-profile-stat span{\n  display:block;\n  margin-top:3px;\n  font-size:12px;\n  color:#666b72;\n}\n.char-profile-name{\n  margin-top:16px;\n  font-size:16px;\n  font-weight:850;\n}\n.char-profile-handle{\n  margin-top:3px;\n  font-size:13px;\n  color:#777c84;\n}\n.char-profile-bio{\n  margin-top:8px;\n  font-size:13px;\n  line-height:1.55;\n  color:#30343a;\n}\n.char-profile-buttons{\n  display:grid;\n  grid-template-columns:1fr 1fr;\n  gap:10px;\n  margin-top:14px;\n}\n.char-profile-btn{\n  height:38px;\n  border:0;\n  border-radius:12px;\n  background:#f1f2f5;\n  color:#111;\n  display:grid;\n  place-items:center;\n  font-size:14px;\n  font-weight:800;\n  cursor:pointer;\n}\n.char-profile-btn.following{\n  background:#f1f2f5;\n}\n.char-profile-btn.follow{\n  background:#111;\n  color:#fff;\n}\n.char-highlight-strip{\n  display:flex;\n  gap:14px;\n  overflow-x:auto;\n  scrollbar-width:none;\n  padding:4px 18px 15px;\n  border-top:1px solid #f5f5f7;\n}\n.char-highlight-strip::-webkit-scrollbar{display:none}\n.char-highlight{\n  width:68px;\n  flex:0 0 68px;\n  text-align:center;\n  cursor:pointer;\n}\n.char-highlight-ring{\n  width:62px;\n  height:62px;\n  margin:0 auto;\n  border-radius:50%;\n  border:1.5px solid #d8dbe2;\n  background:linear-gradient(135deg,#f4f5f7,#ebeef3);\n  display:grid;\n  place-items:center;\n  color:#777c84;\n  font-size:20px;\n  overflow:hidden;\n}\n.char-highlight.add .char-highlight-ring{\n  border-style:dashed;\n  background:#fff;\n  font-size:28px;\n  font-weight:300;\n}\n.char-highlight-cover{\n  width:100%;\n  height:100%;\n  object-fit:cover;\n  border-radius:50%;\n  display:block;\n}\n.char-highlight-name{\n  margin-top:6px;\n  font-size:11px;\n  color:#30343a;\n  white-space:nowrap;\n  overflow:hidden;\n  text-overflow:ellipsis;\n}\n.char-profile-tabs{\n  position:sticky;\n  top:0;\n  z-index:5;\n  height:52px;\n  display:grid;\n  grid-template-columns:repeat(3,1fr);\n  background:#fff;\n  border-top:1px solid #f0f0f3;\n  border-bottom:1px solid #f0f0f3;\n}\n.char-tab{\n  position:relative;\n  border:0;\n  background:#fff;\n  display:grid;\n  place-items:center;\n  cursor:pointer;\n  padding:0;\n}\n.char-tab svg{\n  width:27px;\n  height:27px;\n  fill:none;\n  stroke:#7f838b;\n  stroke-width:2;\n  stroke-linecap:round;\n  stroke-linejoin:round;\n  transition:transform .22s ease,stroke .22s ease;\n}\n.char-tab.active svg{\n  stroke:#111;\n  transform:scale(1.04);\n}\n.char-grid9-icon{\n  width:24px;\n  height:24px;\n  display:grid;\n  grid-template-columns:repeat(3,1fr);\n  grid-template-rows:repeat(3,1fr);\n  gap:2.2px;\n  transition:transform .22s ease;\n}\n.char-grid9-icon span{\n  display:block;\n  border-radius:2px;\n  background:#7f838b;\n  transition:background .22s ease;\n}\n.char-tab.active .char-grid9-icon{\n  transform:scale(1.04);\n}\n.char-tab.active .char-grid9-icon span{\n  background:#111;\n}\n.char-tab-indicator{\n  position:absolute;\n  left:0;\n  bottom:0;\n  width:33.3333%;\n  height:2.5px;\n  border-radius:999px;\n  background:#111;\n  transform:translateX(0%);\n  transition:transform .28s cubic-bezier(.2,.8,.2,1);\n  pointer-events:none;\n}\n.char-profile-grid{\n  display:grid;\n  grid-template-columns:repeat(3,1fr);\n  gap:2px;\n  padding-top:2px;\n  background:#fff;\n}\n.char-profile-cell{\n  aspect-ratio:1/1;\n  background:linear-gradient(135deg,#eceef2,#dfe2e8);\n  color:#70757d;\n  display:grid;\n  place-items:center;\n  text-align:center;\n  padding:8px;\n  font-size:11px;\n  line-height:1.35;\n  overflow:hidden;\n  position:relative;\n}\n.char-profile-cell img{\n  width:100%;\n  height:100%;\n  object-fit:cover;\n  display:block;\n}\n.user-cell.has-image,.char-profile-cell.has-image{\n  padding:0!important;\n}\n.char-profile-cell.reel::after{\n  content:\"▶\";\n  position:absolute;\n  right:7px;\n  top:5px;\n  color:#fff;\n  text-shadow:0 1px 4px rgba(0,0,0,.35);\n  font-size:13px;\n}\n.char-empty{\n  min-height:220px;\n  display:flex;\n  flex-direction:column;\n  align-items:center;\n  justify-content:center;\n  text-align:center;\n  color:#8d9198;\n  font-size:13px;\n  line-height:1.55;\n  padding:24px;\n}\n.char-empty strong{\n  color:#444950;\n  font-size:15px;\n  margin-bottom:6px;\n}\n\n/* guestbook */\n.char-board-wrap{\n  padding:12px 14px 24px;\n}\n.char-board-head{\n  display:flex;\n  align-items:center;\n  justify-content:space-between;\n  margin-bottom:8px;\n}\n.char-board-title{\n  font-size:15px;\n  font-weight:850;\n}\n.char-board-refresh{\n  width:34px;\n  height:34px;\n  border:0;\n  border-radius:50%;\n  background:#f1f2f5;\n  display:grid;\n  place-items:center;\n  cursor:pointer;\n}\n.char-board-refresh svg{\n  width:18px;\n  height:18px;\n  fill:none;\n  stroke:#111;\n  stroke-width:1.9;\n  stroke-linecap:round;\n  stroke-linejoin:round;\n}\n.char-board-row{\n  display:flex;\n  gap:10px;\n  padding:12px 2px;\n  border-bottom:1px solid #f0f1f4;\n}\n.char-board-main{flex:1;min-width:0}\n.char-board-actions{display:flex;gap:8px;align-items:flex-start;flex:0 0 auto}\n.char-board-mini{border:0;background:#f2f3f6;color:#555;padding:7px 10px;border-radius:999px;font-size:11px;font-weight:750;cursor:pointer}\n.char-board-mini.danger{background:#fff1f3;color:#ff4b68}\n.char-board-translation{margin-top:6px;padding:8px 10px;border-radius:10px;background:#f7f8fa;color:#8b8f96;font-size:12px;line-height:1.5}\n.char-board-avatar{\n  width:38px;\n  height:38px;\n  flex:0 0 38px;\n  border-radius:50%;\n  background:linear-gradient(135deg,#dfe3e9,#f3f4f7);\n  display:grid;\n  place-items:center;\n  font-size:11px;\n  font-weight:800;\n  color:#555;\n}\n.char-board-main{min-width:0;flex:1}\n.char-board-name{\n  font-size:13px;\n  font-weight:850;\n}\n.char-board-meta{\n  margin-left:5px;\n  color:#9a9ea5;\n  font-size:10px;\n  font-weight:500;\n}\n.char-board-copy{\n  margin-top:4px;\n  color:#3e4248;\n  font-size:13px;\n  line-height:1.5;\n}\n\n/* char drawer */\n.char-drawer-dim{\n  position:absolute;\n  inset:0;\n  z-index:160;\n  display:none;\n  background:rgba(17,17,17,.10);\n}\n.char-drawer-dim.show{display:block}\n.char-drawer{\n  position:absolute;\n  top:0;\n  right:0;\n  bottom:0;\n  width:min(86%,360px);\n  z-index:161;\n  background:rgba(247,248,250,.87);\n  backdrop-filter:blur(30px) saturate(1.45);\n  -webkit-backdrop-filter:blur(30px) saturate(1.45);\n  border-left:1px solid rgba(255,255,255,.74);\n  box-shadow:-16px 0 42px rgba(15,20,30,.18);\n  transform:translateX(104%);\n  transition:.24s cubic-bezier(.2,.8,.2,1);\n  padding:16px 14px 24px;\n}\n.char-drawer.show{transform:translateX(0)}\n.char-drawer-head{\n  height:44px;\n  display:flex;\n  align-items:center;\n  justify-content:space-between;\n  margin-bottom:12px;\n}\n.char-drawer-title{\n  font-size:20px;\n  font-weight:850;\n}\n.char-drawer-close{\n  width:32px;\n  height:32px;\n  border:0;\n  border-radius:50%;\n  background:rgba(255,255,255,.78);\n  font-size:18px;\n  cursor:pointer;\n}\n.char-drawer-section{\n  border-radius:19px;\n  background:rgba(255,255,255,.66);\n  border:1px solid rgba(255,255,255,.76);\n  overflow:hidden;\n  margin-top:11px;\n}\n.char-drawer-item{\n  width:100%;\n  min-height:58px;\n  border:0;\n  background:transparent;\n  display:flex;\n  align-items:center;\n  gap:12px;\n  padding:0 14px;\n  color:#111;\n  cursor:pointer;\n  border-bottom:1px solid rgba(0,0,0,.055);\n  font:inherit;\n}\n.char-drawer-item:last-child{border-bottom:0}\n.char-drawer-icon{\n  width:30px;\n  height:30px;\n  border-radius:10px;\n  background:#f0f1f5;\n  display:grid;\n  place-items:center;\n  font-size:15px;\n  flex:0 0 30px;\n}\n.char-drawer-copy{\n  min-width:0;\n  flex:1;\n  text-align:left;\n}\n.char-drawer-copy strong{\n  display:block;\n  font-size:14px;\n}\n.char-drawer-copy span{\n  display:block;\n  margin-top:2px;\n  color:#7e838b;\n  font-size:11px;\n}\n.char-drawer-arrow{\n  color:#a2a5ab;\n  font-size:19px;\n}\n.char-drawer-note{\n  padding:11px 4px 0;\n  color:#8b8f96;\n  font-size:11px;\n  line-height:1.5;\n}\n\n/* char quick settings cards */\n.char-quick-card{\n  position:absolute;\n  left:12px;\n  right:12px;\n  bottom:18px;\n  z-index:165;\n  max-height:calc(100% - 36px);\n  overflow-y:auto;\n  scrollbar-width:none;\n  border:1px solid rgba(255,255,255,.76);\n  border-radius:26px;\n  background:rgba(247,248,250,.92);\n  backdrop-filter:blur(30px) saturate(1.4);\n  -webkit-backdrop-filter:blur(30px) saturate(1.4);\n  box-shadow:0 18px 46px rgba(15,20,30,.18),inset 0 1px 0 rgba(255,255,255,.9);\n  padding:14px;\n  opacity:0;\n  transform:translateY(10px) scale(.985);\n  pointer-events:none;\n  transition:.2s cubic-bezier(.2,.8,.2,1);\n}\n.char-quick-card::-webkit-scrollbar{display:none}\n.char-quick-card.show{opacity:1;transform:translateY(0) scale(1);pointer-events:auto}\n.char-quick-head{display:flex;align-items:center;justify-content:space-between;gap:10px;margin-bottom:10px}\n.char-quick-head strong{font-size:18px;font-weight:850}\n.char-quick-close{width:30px;height:30px;border:0;border-radius:50%;background:#fff;color:#111;font-size:18px;cursor:pointer}\n.char-quick-note{padding:11px 12px;border-radius:15px;background:rgba(255,255,255,.72);color:#747a83;font-size:10px;line-height:1.55;margin-bottom:10px}\n.char-frequency-row{display:grid;grid-template-columns:1fr 92px;gap:10px;align-items:center;padding:11px;border-radius:16px;background:rgba(255,255,255,.74);margin-top:9px}\n.char-frequency-copy{display:flex;align-items:flex-start;gap:9px;min-width:0}\n.char-frequency-copy input{margin-top:2px}\n.char-frequency-copy strong{display:block;font-size:12px}\n.char-frequency-copy span{display:block;margin-top:3px;color:#8a8f97;font-size:9px;line-height:1.4}\n.char-frequency-minutes{width:100%;height:38px;border:1px solid rgba(0,0,0,.07);border-radius:12px;background:#f5f6f8;padding:0 9px;outline:none;font:inherit;font-size:11px;text-align:center}\n.char-frequency-next{margin-top:9px;color:#8b9098;font-size:9px;line-height:1.5;padding:0 2px}\n.char-quick-label{display:block;margin:11px 0 6px;color:#666b72;font-size:11px;font-weight:850}\n.char-quick-textarea{width:100%;min-height:78px;border:1px solid rgba(0,0,0,.06);border-radius:14px;background:rgba(255,255,255,.86);padding:10px 11px;outline:none;resize:none;font:inherit;font-size:11px;line-height:1.5;scrollbar-width:none}\n.char-quick-textarea::-webkit-scrollbar{display:none}\n.char-quick-save{width:100%;height:46px;margin-top:12px;border:0;border-radius:15px;background:#111;color:#fff;font-size:13px;font-weight:850;cursor:pointer}\n\n/* char highlight editor */\n.char-highlight-editor{\n  position:absolute;\n  left:12px;\n  right:12px;\n  bottom:18px;\n  z-index:162;\n  border:1px solid rgba(255,255,255,.74);\n  border-radius:26px;\n  background:rgba(247,248,250,.87);\n  backdrop-filter:blur(28px) saturate(1.45);\n  -webkit-backdrop-filter:blur(28px) saturate(1.45);\n  box-shadow:0 18px 45px rgba(15,20,30,.18), inset 0 1px 0 rgba(255,255,255,.9);\n  padding:14px;\n  opacity:0;\n  transform:translateY(10px) scale(.985);\n  pointer-events:none;\n  transition:.2s cubic-bezier(.2,.8,.2,1);\n}\n.char-highlight-editor.show{\n  opacity:1;\n  transform:translateY(0) scale(1);\n  pointer-events:auto;\n}\n.char-highlight-editor-title{\n  display:flex;\n  align-items:center;\n  justify-content:space-between;\n  font-size:18px;\n  font-weight:850;\n  margin-bottom:12px;\n}\n.char-highlight-editor-close{\n  width:30px;\n  height:30px;\n  border:0;\n  border-radius:50%;\n  background:rgba(255,255,255,.76);\n  font-size:18px;\n  cursor:pointer;\n}\n.char-highlight-preview{\n  width:74px;\n  height:74px;\n  margin:0 auto 12px;\n  border-radius:50%;\n  border:1.5px solid #d8dbe2;\n  background:linear-gradient(135deg,#f4f5f7,#ebeef3);\n  display:grid;\n  place-items:center;\n  color:#777c84;\n  font-size:23px;\n  overflow:hidden;\n}\n.char-highlight-preview.has-image{\n  background-size:cover;\n  background-position:center;\n  color:transparent;\n}\n.char-highlight-label{\n  display:block;\n  font-size:12px;\n  font-weight:800;\n  color:#666b72;\n  margin:10px 0 6px;\n}\n.char-highlight-input{\n  width:100%;\n  height:44px;\n  border:1px solid rgba(0,0,0,.07);\n  border-radius:14px;\n  background:rgba(255,255,255,.86);\n  outline:none;\n  font:inherit;\n  font-size:14px;\n  color:#111;\n  padding:0 12px;\n}\n.char-highlight-content{\n  border-radius:17px;\n  background:rgba(255,255,255,.68);\n  border:1px solid rgba(255,255,255,.76);\n  padding:13px;\n  margin-top:10px;\n  color:#858a92;\n  font-size:12px;\n  line-height:1.5;\n}\n.char-highlight-actions{\n  display:grid;\n  grid-template-columns:1fr 1fr;\n  gap:9px;\n  margin-top:12px;\n}\n.char-highlight-btn{\n  height:46px;\n  border:0;\n  border-radius:15px;\n  font-size:14px;\n  font-weight:850;\n  cursor:pointer;\n}\n.char-highlight-btn.delete{\n  background:rgba(255,255,255,.78);\n  color:#ff3b4f;\n}\n.char-highlight-btn.save{\n  background:#111;\n  color:#fff;\n}\n\n\n/* ===== INS v1.14.1: Char profile detail linkage ===== */\n\n/* Char profile editor */\n.char-profile-editor{\n  position:absolute;\n  left:12px;\n  right:12px;\n  bottom:18px;\n  z-index:164;\n  max-height:calc(100% - 36px);\n  overflow-y:auto;\n  border:1px solid rgba(255,255,255,.74);\n  border-radius:26px;\n  background:rgba(247,248,250,.88);\n  backdrop-filter:blur(30px) saturate(1.45);\n  -webkit-backdrop-filter:blur(30px) saturate(1.45);\n  box-shadow:0 18px 45px rgba(15,20,30,.18),inset 0 1px 0 rgba(255,255,255,.9);\n  padding:14px;\n  opacity:0;\n  transform:translateY(10px) scale(.985);\n  pointer-events:none;\n  transition:.2s cubic-bezier(.2,.8,.2,1);\n  scrollbar-width:none;\n}\n.char-profile-editor::-webkit-scrollbar{display:none}\n.char-profile-editor.show{\n  opacity:1;\n  transform:translateY(0) scale(1);\n  pointer-events:auto;\n}\n.char-profile-editor-title{\n  display:flex;\n  align-items:center;\n  justify-content:space-between;\n  font-size:18px;\n  font-weight:850;\n  margin-bottom:12px;\n}\n.char-profile-editor-close{\n  width:30px;\n  height:30px;\n  border:0;\n  border-radius:50%;\n  background:rgba(255,255,255,.76);\n  color:#111;\n  font-size:18px;\n  cursor:pointer;\n}\n.char-profile-edit-preview{\n  width:72px;\n  height:72px;\n  margin:0 auto 12px;\n  border-radius:50%;\n  background:linear-gradient(135deg,#d9dce3,#f3f4f7);\n  display:grid;\n  place-items:center;\n  font-size:22px;\n  font-weight:850;\n  color:#555;\n  overflow:hidden;\n}\n.char-profile-edit-preview.has-image{\n  background-size:cover;\n  background-position:center;\n  color:transparent;\n}\n.char-profile-edit-label{\n  display:block;\n  margin:10px 0 6px;\n  color:#666b72;\n  font-size:12px;\n  font-weight:800;\n}\n.char-profile-edit-input,.char-profile-edit-textarea{\n  width:100%;\n  border:1px solid rgba(0,0,0,.07);\n  border-radius:14px;\n  background:rgba(255,255,255,.86);\n  outline:none;\n  font:inherit;\n  font-size:14px;\n  color:#111;\n}\n.char-profile-edit-input{\n  height:44px;\n  padding:0 12px;\n}\n.char-profile-edit-textarea{\n  min-height:82px;\n  padding:11px 12px;\n  resize:none;\n  line-height:1.5;\n}\n.char-profile-edit-row{\n  display:grid;\n  grid-template-columns:1fr 1fr;\n  gap:10px;\n}\n.char-profile-edit-save{\n  width:100%;\n  height:48px;\n  border:0;\n  border-radius:16px;\n  background:#111;\n  color:#fff;\n  font-size:15px;\n  font-weight:850;\n  cursor:pointer;\n  margin-top:13px;\n}\n\n/* Char profile post glass card */\n.char-post-dim{\n  position:absolute;\n  inset:0;\n  z-index:170;\n  display:none;\n  background:rgba(17,17,17,.16);\n}\n.char-post-dim.show{display:block}\n.char-post-card{\n  position:absolute;\n  left:10px;\n  right:10px;\n  top:24px;\n  bottom:18px;\n  z-index:171;\n  display:flex;\n  flex-direction:column;\n  border:1px solid rgba(255,255,255,.76);\n  border-radius:28px;\n  background:rgba(249,249,251,.90);\n  backdrop-filter:blur(30px) saturate(1.45);\n  -webkit-backdrop-filter:blur(30px) saturate(1.45);\n  box-shadow:0 22px 52px rgba(15,20,30,.22),inset 0 1px 0 rgba(255,255,255,.92);\n  overflow:hidden;\n  opacity:0;\n  transform:translateY(12px) scale(.985);\n  pointer-events:none;\n  transition:.22s cubic-bezier(.2,.8,.2,1);\n}\n.char-post-card.show{\n  opacity:1;\n  transform:translateY(0) scale(1);\n  pointer-events:auto;\n}\n.char-post-card-head{\n  flex:0 0 auto;\n  height:58px;\n  display:flex;\n  align-items:center;\n  gap:10px;\n  padding:0 14px;\n  border-bottom:1px solid rgba(0,0,0,.055);\n}\n.char-post-card-avatar{\n  width:36px;\n  height:36px;\n  border-radius:50%;\n  background:linear-gradient(135deg,#dfe3e9,#f3f4f7);\n  display:grid;\n  place-items:center;\n  color:#555;\n  font-size:11px;\n  font-weight:850;\n}\n.char-post-card-author{\n  flex:1;\n  min-width:0;\n  font-size:14px;\n  font-weight:850;\n  white-space:nowrap;\n  overflow:hidden;\n  text-overflow:ellipsis;\n}\n.char-post-card-close{\n  width:32px;\n  height:32px;\n  border:0;\n  border-radius:50%;\n  background:rgba(255,255,255,.76);\n  color:#111;\n  font-size:18px;\n  cursor:pointer;\n}\n.char-post-card-scroll{\n  flex:1;\n  overflow-y:auto;\n  scrollbar-width:none;\n}\n.char-post-card-scroll::-webkit-scrollbar{display:none}\n.char-post-card-media{\n  width:100%;\n  aspect-ratio:1/1;\n  display:grid;\n  place-items:center;\n  text-align:center;\n  padding:20px;\n  color:#fff;\n  font-size:13px;\n  line-height:1.5;\n  overflow:hidden;\n}\n.char-post-card-media img{\n  width:100%;\n  height:100%;\n  object-fit:cover;\n  display:block;\n}\n.char-post-card-actions{\n  display:flex;\n  align-items:center;\n  gap:18px;\n  padding:12px 14px 5px;\n}\n.char-post-card-action{\n  display:flex;\n  align-items:center;\n  gap:5px;\n  font-size:12px;\n  font-weight:700;\n  color:#555a62;\n}\n.char-post-card-action img{\n  width:23px;\n  height:23px;\n  object-fit:contain;\n  display:block;\n}\n.char-post-card-caption{\n  padding:5px 14px 13px;\n  font-size:13px;\n  line-height:1.55;\n  color:#30343a;\n  border-bottom:1px solid rgba(0,0,0,.055);\n}\n.char-post-card-caption strong{font-weight:850}\n.char-post-comments{\n  padding:12px 14px 22px;\n}\n.char-post-comments-head{\n  display:flex;\n  align-items:center;\n  justify-content:space-between;\n  margin-bottom:7px;\n}\n.char-post-comments-title{\n  font-size:15px;\n  font-weight:850;\n}\n.char-post-comments-refresh{\n  width:34px;\n  height:34px;\n  border:0;\n  border-radius:50%;\n  background:rgba(255,255,255,.76);\n  display:grid;\n  place-items:center;\n  cursor:pointer;\n  transition:transform .22s ease;\n}\n.char-post-comments-refresh svg{\n  width:18px;\n  height:18px;\n  fill:none;\n  stroke:#111;\n  stroke-width:1.9;\n  stroke-linecap:round;\n  stroke-linejoin:round;\n}\n.char-post-comment-row{\n  display:flex;\n  gap:10px;\n  padding:11px 0;\n  border-bottom:1px solid rgba(0,0,0,.055);\n}\n.char-post-comment-row:last-child{border-bottom:0}\n.char-post-comment-avatar{\n  width:34px;\n  height:34px;\n  flex:0 0 34px;\n  border-radius:50%;\n  background:linear-gradient(135deg,#dfe3e9,#f3f4f7);\n  display:grid;\n  place-items:center;\n  font-size:10px;\n  font-weight:850;\n  color:#555;\n}\n.char-post-comment-main{\n  min-width:0;\n  flex:1;\n}\n.char-post-comment-name{\n  font-size:12px;\n  font-weight:850;\n}\n.char-post-comment-copy{\n  margin-top:3px;\n  color:#444950;\n  font-size:12px;\n  line-height:1.5;\n}\n.char-post-comment-translation{\n  margin-top:5px;\n  color:#858a92;\n  font-size:11px;\n  line-height:1.45;\n}\n.char-post-comment-empty{\n  padding:24px 0;\n  color:#8b8f96;\n  text-align:center;\n  font-size:12px;\n}\n\n/* Make char profile grid cells visibly tappable */\n.char-profile-cell[data-char-post-id]{\n  cursor:pointer;\n  transition:transform .14s ease,opacity .14s ease;\n}\n.char-profile-cell[data-char-post-id]:active{\n  transform:scale(.985);\n  opacity:.9;\n}\n\n/* DM profile/avatar can display edited char avatar */\n.dm-chat-avatar.has-image,.dm-chat-big-avatar.has-image,.dm-msg-avatar.has-image{\n  background-size:cover;\n  background-position:center;\n  color:transparent;\n}\n\n\n/* ===== v1.14.2 Char Album tab preview ===== */\n.char-profile-tabs{\n  grid-template-columns:repeat(4,1fr);\n}\n.char-tab-indicator{\n  width:25%;\n}\n.char-album-tab-icon{\n  width:27px;height:27px;position:relative;\n}\n.char-album-tab-icon:before{\n  content:\"\";\n  position:absolute;inset:3px;\n  border:2px solid #7f838b;\n  border-radius:4px;\n  transition:border-color .22s ease,transform .22s ease;\n}\n.char-album-tab-icon:after{\n  content:\"\";\n  position:absolute;\n  left:7px;right:7px;bottom:7px;height:7px;\n  border-left:2px solid #7f838b;\n  border-bottom:2px solid #7f838b;\n  transform:skew(-28deg) rotate(-3deg);\n  transition:border-color .22s ease,transform .22s ease;\n}\n.char-tab.active .char-album-tab-icon:before,.char-tab.active .char-album-tab-icon:after{\n  border-color:#111;\n}\n.char-tab.active .char-album-tab-icon:before{transform:scale(1.04)}\n\n.char-album-shell{\n  background:#fff;\n  min-height:300px;\n}\n.char-album-minihead{\n  display:flex;\n  align-items:center;\n  justify-content:space-between;\n  padding:12px 13px 10px;\n}\n.char-album-minihead strong{\n  font-size:13px;\n  font-weight:850;\n}\n.char-album-minihead span{\n  font-size:10px;\n  color:#989ca3;\n}\n.char-album-grid{\n  display:grid;\n  grid-template-columns:repeat(3,1fr);\n  gap:2px;\n  background:#fff;\n}\n.char-album-tile{\n  aspect-ratio:1/1;\n  position:relative;\n  overflow:hidden;\n  background:linear-gradient(135deg,#e6e8ed,#f5f6f8);\n  cursor:pointer;\n}\n.char-album-tile img{\n  width:100%;\n  height:100%;\n  object-fit:cover;\n  display:block;\n}\n.char-album-tile-copy{\n  position:absolute;\n  inset:0;\n  display:grid;\n  place-items:center;\n  text-align:center;\n  padding:10px;\n  font-size:10px;\n  line-height:1.35;\n  color:#686d75;\n}\n.char-album-add{\n  background:#fafafa;\n  border:1px solid #f0f1f4;\n  display:flex;\n  flex-direction:column;\n  align-items:center;\n  justify-content:center;\n  gap:6px;\n}\n.char-album-add strong{\n  display:grid;\n  place-items:center;\n  width:42px;\n  height:42px;\n  border-radius:50%;\n  border:1.5px solid #cfd2d8;\n  font-size:29px;\n  line-height:1;\n  font-weight:300;\n  color:#555a62;\n}\n.char-album-add span{\n  color:#777c84;\n  font-size:10px;\n  font-weight:750;\n}\n.char-album-tile-badge{\n  position:absolute;\n  left:5px;\n  top:5px;\n  padding:3px 5px;\n  border-radius:999px;\n  background:rgba(17,17,17,.72);\n  color:#fff;\n  font-size:8px;\n  font-weight:800;\n}\n.char-album-tile-badge.generated{\n  background:rgba(255,255,255,.88);\n  color:#444950;\n}\n.char-album-footnote{\n  padding:12px 14px 18px;\n  color:#8d9198;\n  font-size:10px;\n  line-height:1.5;\n  text-align:center;\n}\n\n/* manager */\n.char-album-dim{\n  position:absolute;\n  inset:0;\n  z-index:176;\n  display:none;\n  background:rgba(17,17,17,.13);\n}\n.char-album-dim.show{display:block}\n.char-album-manager{\n  position:absolute;\n  left:10px;right:10px;top:20px;bottom:16px;\n  z-index:177;\n  display:flex;\n  flex-direction:column;\n  overflow:hidden;\n  border:1px solid rgba(255,255,255,.76);\n  border-radius:28px;\n  background:rgba(248,249,251,.90);\n  backdrop-filter:blur(30px) saturate(1.45);\n  -webkit-backdrop-filter:blur(30px) saturate(1.45);\n  box-shadow:0 22px 50px rgba(15,20,30,.2),inset 0 1px 0 rgba(255,255,255,.9);\n  opacity:0;\n  transform:translateY(10px) scale(.985);\n  pointer-events:none;\n  transition:.22s cubic-bezier(.2,.8,.2,1);\n}\n.char-album-manager.show{\n  opacity:1;\n  transform:translateY(0) scale(1);\n  pointer-events:auto;\n}\n.char-album-manager-head{\n  flex:0 0 auto;\n  display:flex;\n  align-items:center;\n  gap:10px;\n  height:58px;\n  padding:0 14px;\n  border-bottom:1px solid rgba(0,0,0,.055);\n}\n.char-album-manager-title{\n  flex:1;\n  min-width:0;\n}\n.char-album-manager-title strong{\n  display:block;\n  font-size:17px;\n  font-weight:850;\n}\n.char-album-manager-title span{\n  display:block;\n  margin-top:2px;\n  color:#8b8f96;\n  font-size:10px;\n}\n.char-album-close{\n  width:32px;height:32px;border:0;border-radius:50%;\n  background:rgba(255,255,255,.8);\n  font-size:18px;cursor:pointer;\n}\n.char-album-manager-scroll{\n  flex:1;\n  overflow-y:auto;\n  padding:13px;\n  scrollbar-width:none;\n}\n.char-album-manager-scroll::-webkit-scrollbar{display:none}\n.char-album-section{\n  border-radius:19px;\n  background:rgba(255,255,255,.67);\n  border:1px solid rgba(255,255,255,.78);\n  padding:12px;\n  margin-bottom:10px;\n}\n.char-album-section-title{\n  display:flex;\n  align-items:center;\n  justify-content:space-between;\n  gap:8px;\n  margin-bottom:9px;\n}\n.char-album-section-title strong{\n  font-size:13px;\n  font-weight:850;\n}\n.char-album-section-title span{\n  color:#90949b;\n  font-size:9px;\n}\n.char-album-current{\n  font-size:11px;\n  line-height:1.5;\n  color:#50555d;\n  padding:9px;\n  background:#f5f6f8;\n  border-radius:13px;\n}\n.char-album-action-row{\n  display:grid;\n  grid-template-columns:repeat(3,1fr);\n  gap:7px;\n  margin-top:9px;\n}\n.char-album-smallbtn{\n  height:34px;\n  border:0;\n  border-radius:11px;\n  background:#eff0f3;\n  color:#555a62;\n  font-size:9px;\n  font-weight:800;\n  cursor:pointer;\n}\n.char-album-smallbtn.dark{background:#111;color:#fff}\n.char-album-era-row{\n  display:flex;\n  gap:7px;\n  overflow-x:auto;\n  scrollbar-width:none;\n}\n.char-album-era-row::-webkit-scrollbar{display:none}\n.char-album-era-chip{\n  flex:0 0 auto;\n  padding:8px 10px;\n  border-radius:13px;\n  background:#f1f2f5;\n  color:#5d626a;\n  font-size:9px;\n  font-weight:800;\n}\n.char-album-era-chip.current{\n  background:#111;\n  color:#fff;\n}\n\n.char-album-era-chip{\n  border:0;\n  cursor:pointer;\n}\n.char-album-era-empty{\n  width:100%;\n  padding:12px;\n  border-radius:14px;\n  background:#f4f5f7;\n  color:#8b9098;\n  font-size:9px;\n  line-height:1.45;\n}\n.char-album-era-add{\n  width:100%;\n  height:36px;\n  margin-top:8px;\n  border:1px dashed #d9dde3;\n  border-radius:12px;\n  background:#fafbfc;\n  color:#555b64;\n  font-size:9px;\n  font-weight:850;\n  cursor:pointer;\n}\n.char-album-era-editor{\n  margin-top:10px;\n  padding:11px;\n  border-radius:16px;\n  background:#f3f5f7;\n  border:1px solid #e8ebef;\n}\n.char-album-era-editor[hidden]{display:none}\n.char-album-era-editor-title{\n  display:flex;\n  align-items:center;\n  justify-content:space-between;\n  gap:8px;\n  margin-bottom:8px;\n}\n.char-album-era-editor-title strong{\n  font-size:11px;\n}\n.char-album-era-editor-title button{\n  width:28px;\n  height:28px;\n  border:0;\n  border-radius:50%;\n  background:#fff;\n  color:#555b63;\n  font-size:16px;\n  cursor:pointer;\n}\n.char-album-era-label{\n  display:block;\n  margin:8px 0 5px;\n  color:#686e76;\n  font-size:8px;\n  font-weight:850;\n}\n.char-album-era-input,.char-album-era-textarea{\n  width:100%;\n  border:1px solid #e3e6ea;\n  border-radius:11px;\n  background:#fff;\n  color:#171a1f;\n  outline:none;\n  font:inherit;\n  font-size:9px;\n}\n.char-album-era-input{\n  height:36px;\n  padding:0 9px;\n}\n.char-album-era-textarea{\n  min-height:72px;\n  padding:9px;\n  resize:none;\n  line-height:1.5;\n}\n.char-album-era-current-toggle{\n  display:flex;\n  align-items:flex-start;\n  gap:8px;\n  margin-top:9px;\n  padding:9px;\n  border-radius:12px;\n  background:#fff;\n  cursor:pointer;\n}\n.char-album-era-current-toggle input{\n  margin-top:2px;\n}\n.char-album-era-current-toggle span{\n  display:block;\n  flex:1;\n}\n.char-album-era-current-toggle strong{\n  display:block;\n  color:#343940;\n  font-size:9px;\n}\n.char-album-era-current-toggle small{\n  display:block;\n  margin-top:3px;\n  color:#8b9097;\n  font-size:7.5px;\n  line-height:1.35;\n}\n.char-album-era-editor-actions{\n  display:grid;\n  grid-template-columns:.8fr 1fr 1.2fr;\n  gap:7px;\n  margin-top:10px;\n}\n.char-album-era-editor-actions button{\n  height:34px;\n  border:0;\n  border-radius:10px;\n  background:#e5e8ec;\n  color:#5f656d;\n  font-size:8.5px;\n  font-weight:850;\n  cursor:pointer;\n}\n.char-album-era-editor-actions .primary{\n  background:#111;\n  color:#fff;\n}\n.char-album-era-editor-actions .danger{\n  background:#fff0f0;\n  color:#c84d4d;\n}\n.char-album-era-note{\n  margin-top:9px;\n  color:#858a92;\n  font-size:8px;\n  line-height:1.5;\n}\n.char-album-source-grid{\n  display:grid;\n  grid-template-columns:1fr 1fr;\n  gap:8px;\n}\n.char-album-source-card{\n  min-height:92px;\n  border-radius:16px;\n  background:#f4f5f7;\n  padding:11px;\n}\n.char-album-source-card strong{\n  display:block;\n  font-size:12px;\n}\n.char-album-source-card span{\n  display:block;\n  margin-top:5px;\n  color:#7f848c;\n  font-size:9px;\n  line-height:1.4;\n}\n.char-album-source-card button{\n  margin-top:9px;\n  height:30px;\n  border:0;\n  border-radius:10px;\n  background:#111;\n  color:#fff;\n  padding:0 10px;\n  font-size:9px;\n  font-weight:800;\n  cursor:pointer;\n}\n.char-album-source-input{\n  width:100%;height:34px;margin-top:8px;border:1px solid #e3e6ea;border-radius:10px;background:#fff;\n  padding:0 9px;outline:none;font:inherit;font-size:9px;color:#222;\n}\n.char-album-import-desc{margin-bottom:8px}\n.char-album-library-summary{margin-top:9px;padding:9px 10px;border-radius:12px;background:#f5f6f8;color:#6f757e;font-size:9px;line-height:1.45}\n.char-album-library-list{display:flex;gap:7px;overflow-x:auto;scrollbar-width:none;margin-top:8px}\n.char-album-library-list::-webkit-scrollbar{display:none}\n.char-album-library-item{position:relative;flex:0 0 72px;width:72px;height:72px;border-radius:13px;overflow:hidden;background:#eceef2;border:1px solid #e1e4e9}\n.char-album-library-item img{width:100%;height:100%;object-fit:cover;display:block}\n.char-album-library-item span{position:absolute;left:4px;right:4px;bottom:4px;padding:3px 4px;border-radius:8px;background:rgba(17,17,17,.62);color:#fff;font-size:7px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}\n.char-album-library-delete{position:absolute;right:4px;top:4px;width:21px;height:21px;border:0;border-radius:50%;background:rgba(17,17,17,.66);color:#fff;font-size:13px;line-height:1;cursor:pointer}\n.char-album-generated-empty{padding:12px;border-radius:14px;background:#f5f6f8;color:#858a92;font-size:9px;line-height:1.5}\n.char-album-generated-note{margin-top:8px;color:#8a8f96;font-size:8px;line-height:1.5}\n.char-album-generated-strip{\n  display:flex;\n  gap:7px;\n  overflow-x:auto;\n  scrollbar-width:none;\n}\n.char-album-generated-strip::-webkit-scrollbar{display:none}\n.char-album-generated{\n  width:76px;\n  flex:0 0 76px;\n}\n.char-album-generated-thumb{\n  width:76px;\n  height:76px;\n  border-radius:13px;\n  background:linear-gradient(135deg,#e4e7ec,#f7f7f9);\n  display:grid;\n  place-items:center;\n  text-align:center;\n  padding:7px;\n  overflow:hidden;\n  color:#656a72;\n  font-size:8px;\n  line-height:1.35;\n}\n.char-album-generated-thumb img{width:calc(100% + 14px);height:calc(100% + 14px);object-fit:cover;display:block}\n.char-album-generated span{\n  display:block;\n  margin-top:5px;\n  color:#81868e;\n  font-size:8px;\n  white-space:nowrap;\n  overflow:hidden;\n  text-overflow:ellipsis;\n}\n.char-album-rule{\n  font-size:10px;\n  line-height:1.55;\n  color:#6f747c;\n}\n.char-album-rule b{color:#33373d}\n\n\n/* ===== INS v1.15: integrated Reels ===== */\n#page-reels{\n  position:relative;\n  overflow:hidden;\n  background:#000;\n  color:#fff;\n}\n.reels-v15-top{\n  position:absolute;\n  top:0;left:0;right:0;\n  height:62px;\n  z-index:28;\n  display:flex;\n  align-items:center;\n  justify-content:space-between;\n  padding:0 14px;\n  pointer-events:none;\n  background:linear-gradient(to bottom,rgba(0,0,0,.56),rgba(0,0,0,0));\n}\n.reels-v15-top-left,.reels-v15-top-right{\n  display:flex;\n  align-items:center;\n  gap:10px;\n  pointer-events:auto;\n}\n.reels-v15-title{\n  font-size:20px;\n  font-weight:900;\n  letter-spacing:-.2px;\n}\n.reels-v15-topbtn{\n  width:38px;height:38px;\n  border:0;border-radius:50%;\n  background:rgba(28,28,31,.72);\n  color:#fff;\n  display:grid;\n  place-items:center;\n  cursor:pointer;\n  backdrop-filter:blur(18px);\n  -webkit-backdrop-filter:blur(18px);\n}\n.reels-v15-topbtn.plus{\n  font-size:27px;\n  font-weight:250;\n  padding-bottom:2px;\n}\n.reels-v15-topbtn svg{\n  width:21px;height:21px;\n  fill:none;stroke:#fff;stroke-width:2;\n  stroke-linecap:round;stroke-linejoin:round;\n}\n.reels-v15-scroller{\n  position:absolute;\n  inset:0;\n  overflow-y:auto;\n  scroll-snap-type:y mandatory;\n  overscroll-behavior-y:contain;\n  scrollbar-width:none;\n}\n.reels-v15-scroller::-webkit-scrollbar{display:none}\n.reels-v15-item{\n  position:relative;\n  height:100%;\n  min-height:100%;\n  scroll-snap-align:start;\n  overflow:hidden;\n  background:#0c0c0f;\n}\n.reels-v15-item:nth-child(2n){background:#111216}\n.reels-v15-item:nth-child(3n){background:#0a0b0d}\n.reels-v15-stage{\n  position:absolute;\n  inset:0;\n  display:grid;\n  place-items:center;\n  overflow:hidden;\n}\n.reels-v15-stage::before{\n  content:\"\";\n  position:absolute;\n  inset:-18%;\n  background:\n    radial-gradient(circle at 30% 24%,rgba(255,255,255,.10),transparent 30%),\n    radial-gradient(circle at 72% 65%,rgba(255,255,255,.06),transparent 28%),\n    linear-gradient(155deg,rgba(255,255,255,.04),transparent 42%,rgba(255,255,255,.03));\n  filter:blur(12px);\n}\n.reels-v15-video{\n  position:relative;\n  z-index:2;\n  width:72%;\n  max-width:300px;\n  min-height:178px;\n  border:1px solid rgba(255,255,255,.13);\n  border-radius:24px;\n  background:rgba(255,255,255,.06);\n  backdrop-filter:blur(18px);\n  -webkit-backdrop-filter:blur(18px);\n  box-shadow:0 18px 50px rgba(0,0,0,.24);\n  display:flex;\n  flex-direction:column;\n  align-items:center;\n  justify-content:center;\n  text-align:center;\n  padding:24px 18px;\n}\n.reels-v15-video strong{\n  font-size:15px;\n  font-weight:850;\n}\n.reels-v15-video span{\n  display:block;\n  margin-top:9px;\n  color:rgba(255,255,255,.66);\n  font-size:11px;\n  line-height:1.55;\n  white-space:pre-wrap;\n}\n.reels-v15-video small{\n  display:block;\n  margin-top:13px;\n  padding:5px 8px;\n  border-radius:999px;\n  background:rgba(255,255,255,.08);\n  color:rgba(255,255,255,.55);\n  font-size:9px;\n}\n.reels-v15-fade{\n  position:absolute;\n  left:0;right:0;bottom:0;\n  height:44%;\n  background:linear-gradient(to top,rgba(0,0,0,.82),rgba(0,0,0,.10),transparent);\n  pointer-events:none;\n}\n.reels-v15-info{\n  position:absolute;\n  left:14px;\n  right:78px;\n  bottom:20px;\n  z-index:8;\n}\n.reels-v15-authorline{\n  display:flex;\n  align-items:center;\n  gap:8px;\n}\n.reels-v15-avatar{\n  width:35px;height:35px;\n  border-radius:50%;\n  background:linear-gradient(135deg,#dfe3e9,#f3f4f7);\n  display:grid;\n  place-items:center;\n  color:#4f545c;\n  font-size:11px;\n  font-weight:900;\n  overflow:hidden;\n  cursor:default;\n}\n.reels-v15-author{\n  min-width:0;\n  font-size:13px;\n  font-weight:900;\n  white-space:nowrap;\n  overflow:hidden;\n  text-overflow:ellipsis;\n  cursor:default;\n}\n.reels-v15-avatar[data-reels-v15-profile],.reels-v15-author[data-reels-v15-profile]{cursor:pointer}\n.reels-v15-follow{\n  height:27px;\n  border:1px solid rgba(255,255,255,.45);\n  border-radius:8px;\n  background:rgba(0,0,0,.14);\n  color:#fff;\n  padding:0 9px;\n  font-size:10px;\n  font-weight:850;\n  cursor:pointer;\n}\n.reels-v15-follow.following{\n  background:rgba(255,255,255,.13);\n}\n.reels-v15-follow:disabled{\n  opacity:1;\n  cursor:default;\n  color:#fff;\n  -webkit-text-fill-color:#fff;\n}\n.reels-v15-caption{\n  margin-top:10px;\n  font-size:12px;\n  line-height:1.5;\n  color:#fff;\n  white-space:pre-wrap;\n  text-shadow:0 1px 8px rgba(0,0,0,.25);\n}\n.reels-v15-translation{\n  display:none;\n  margin-top:6px;\n  color:rgba(255,255,255,.72);\n  font-size:11px;\n  line-height:1.48;\n  white-space:pre-wrap;\n}\n.reels-v15-translation.show{display:block}\n.reels-v15-tags{\n  margin-top:5px;\n  color:#d8d9dd;\n  font-size:11px;\n  line-height:1.45;\n}\n.reels-v15-rail{\n  position:absolute;\n  right:8px;\n  bottom:16px;\n  z-index:9;\n  display:flex;\n  flex-direction:column;\n  align-items:center;\n  gap:13px;\n}\n.reels-v15-action{\n  width:54px;\n  display:flex;\n  flex-direction:column;\n  align-items:center;\n  gap:3px;\n}\n.reels-v15-railbtn{\n  width:45px;height:45px;\n  border:0;\n  background:transparent;\n  padding:6px;\n  display:grid;\n  place-items:center;\n  cursor:pointer;\n}\n.reels-v15-railbtn img{\n  width:30px;height:30px;\n  object-fit:contain;\n  display:block;\n  filter:brightness(0) invert(1)!important;\n}\n.reels-v15-railbtn.heart img{\n  filter:brightness(0) invert(1)!important;\n}\n.reels-v15-railbtn.heart.liked img{\n  filter:none!important;\n}\n.reels-v15-count{\n  font-size:9px;\n  font-weight:800;\n  color:#fff;\n  text-shadow:0 1px 6px rgba(0,0,0,.45);\n}\n.reels-v15-more-action{\n  height:28px;\n  gap:0;\n  margin-top:-2px;\n  margin-bottom:-1px;\n}\n.reels-v15-more-action .reels-v15-railbtn{\n  width:40px;\n  height:28px;\n  padding:0;\n}\n.reels-v15-moredots{\n  position:relative;\n  display:block;\n  width:22px;\n  height:8px;\n}\n.reels-v15-moredots::before{\n  content:\"\";\n  position:absolute;\n  left:2.75px;\n  top:50%;\n  width:3.5px;\n  height:3.5px;\n  border-radius:50%;\n  background:#fff;\n  transform:translateY(-50%);\n  box-shadow:6.5px 0 #fff,13px 0 #fff;\n}\n.reels-v15-spin{\n  width:43px;height:43px;\n  border-radius:50%;\n  border:6px solid #242428;\n  background:#111;\n  display:grid;\n  place-items:center;\n}\n.reels-v15-spinavatar{\n  width:29px;height:29px;\n  border-radius:50%;\n  background:linear-gradient(135deg,#dfe3e9,#f3f4f7);\n  color:#555;\n  display:grid;\n  place-items:center;\n  font-size:9px;\n  font-weight:900;\n  transform-origin:center;\n  animation:none;\n  will-change:auto;\n}\n#page-reels .reels-v15-spinavatar{\n  animation:none!important;\n  transform:none!important;\n  will-change:auto!important;\n}\n\n/* Reels glass sheets */\n.reels-v15-dim{\n  position:absolute;\n  inset:0 0 68px 0;\n  z-index:186;\n  background:rgba(0,0,0,.28);\n  display:none;\n}\n.reels-v15-dim.show{display:block}\n.reels-v15-sheet{\n  position:absolute;\n  left:10px;right:10px;bottom:78px;\n  z-index:187;\n  max-height:calc(100% - 96px);\n  overflow-y:auto;\n  scrollbar-width:none;\n  border:1px solid rgba(255,255,255,.18);\n  border-radius:26px;\n  background:rgba(28,28,32,.88);\n  backdrop-filter:blur(28px) saturate(1.35);\n  -webkit-backdrop-filter:blur(28px) saturate(1.35);\n  box-shadow:0 20px 50px rgba(0,0,0,.34);\n  color:#fff;\n  padding:14px;\n  opacity:0;\n  transform:translateY(12px) scale(.985);\n  pointer-events:none;\n  transition:.2s cubic-bezier(.2,.8,.2,1);\n}\n.reels-v15-sheet::-webkit-scrollbar{display:none}\n.reels-v15-sheet.show{\n  opacity:1;\n  transform:translateY(0) scale(1);\n  pointer-events:auto;\n}\n.reels-v15-sheet-head{\n  display:flex;\n  align-items:center;\n  justify-content:space-between;\n  margin-bottom:12px;\n}\n.reels-v15-sheet-title{\n  font-size:17px;\n  font-weight:850;\n}\n.reels-v15-sheet-close{\n  width:30px;height:30px;\n  border:0;border-radius:50%;\n  background:rgba(255,255,255,.10);\n  color:#fff;\n  font-size:18px;\n  cursor:pointer;\n}\n.reels-v15-block{\n  border-radius:17px;\n  background:rgba(255,255,255,.07);\n  padding:12px;\n  margin-top:9px;\n}\n.reels-v15-copy{\n  font-size:11px;\n  line-height:1.55;\n  color:rgba(255,255,255,.68);\n}\n.reels-v15-row{\n  display:flex;\n  align-items:center;\n  gap:10px;\n  min-height:48px;\n  border-bottom:1px solid rgba(255,255,255,.07);\n}\n.reels-v15-row:last-child{border-bottom:0}\n.reels-v15-row.clickable{cursor:pointer}\n.reels-v15-row-main{\n  flex:1;\n  min-width:0;\n}\n.reels-v15-row-main strong{\n  display:block;\n  font-size:12px;\n}\n.reels-v15-row-main span{\n  display:block;\n  margin-top:2px;\n  color:rgba(255,255,255,.5);\n  font-size:9px;\n}\n.reels-v15-sheet-avatar{\n  width:34px;height:34px;\n  border-radius:50%;\n  background:#dedfe3;\n  color:#555;\n  display:grid;\n  place-items:center;\n  font-size:10px;\n  font-weight:900;\n}\n.reels-v15-sheet-action{\n  height:30px;\n  border:0;\n  border-radius:10px;\n  background:#fff;\n  color:#111;\n  padding:0 10px;\n  font-size:9px;\n  font-weight:850;\n  cursor:pointer;\n}\n.reels-v15-comment-list{\n  max-height:280px;\n  overflow-y:auto;\n  scrollbar-width:none;\n}\n.reels-v15-comment-list::-webkit-scrollbar{display:none}\n.reels-v15-comment{\n  display:flex;\n  gap:9px;\n  padding:10px 0;\n  border-bottom:1px solid rgba(255,255,255,.06);\n}\n.reels-v15-comment:last-child{border-bottom:0}\n.reels-v15-comment-avatar{\n  width:32px;height:32px;\n  flex:0 0 32px;\n  border-radius:50%;\n  background:#ddd;\n  color:#555;\n  display:grid;\n  place-items:center;\n  font-size:9px;\n  font-weight:900;\n}\n.reels-v15-comment-main{flex:1;min-width:0}\n.reels-v15-comment-name{font-size:11px;font-weight:850}\n.reels-v15-comment-copy{\n  margin-top:3px;\n  font-size:11px;\n  line-height:1.45;\n  color:rgba(255,255,255,.75);\n  white-space:pre-wrap;\n}\n\n.nini-ins-root .reels-v15-comment-load-more{width:100%;height:42px;border:0;border-top:1px solid rgba(255,255,255,.06);background:transparent;color:#9da1a8;font-size:12px;font-weight:800;cursor:pointer}\n.nini-ins-root .reels-v15-comment-load-more:disabled{opacity:.55}\n.reels-v15-composer{\n  display:flex;\n  gap:8px;\n  margin-top:10px;\n}\n.reels-v15-composer input,.reels-v15-form input,.reels-v15-form textarea{\n  border:0;\n  border-radius:13px;\n  background:rgba(255,255,255,.08);\n  color:#fff;\n  outline:none;\n  font-size:11px;\n}\n.reels-v15-composer input{\n  flex:1;height:40px;padding:0 11px;\n}\n.reels-v15-composer button{\n  width:48px;\n  border:0;border-radius:13px;\n  background:#fff;color:#111;\n  font-size:10px;font-weight:850;\n  cursor:pointer;\n}\n.reels-v15-form textarea{\n  width:100%;\n  min-height:88px;\n  padding:10px;\n  resize:none;\n  line-height:1.5;\n}\n.reels-v15-form input{\n  width:100%;\n  height:40px;\n  padding:0 10px;\n  margin-top:8px;\n}\n.reels-v15-primary{\n  width:100%;\n  height:44px;\n  border:0;\n  border-radius:14px;\n  background:#fff;\n  color:#111;\n  margin-top:10px;\n  font-size:12px;\n  font-weight:850;\n  cursor:pointer;\n}\n.reels-v15-chips{\n  display:flex;\n  flex-wrap:wrap;\n  gap:7px;\n}\n.reels-v15-chip{\n  border:1px solid rgba(255,255,255,.12);\n  border-radius:999px;\n  background:rgba(255,255,255,.07);\n  color:#fff;\n  padding:8px 10px;\n  font-size:10px;\n  font-weight:800;\n  cursor:pointer;\n}\n.reels-v15-chip.active{\n  background:#fff;\n  color:#111;\n}\n.reels-v15-identity{\n  display:flex;\n  align-items:center;\n  gap:10px;\n  border-radius:16px;\n  background:rgba(255,255,255,.07);\n  padding:10px;\n  margin-bottom:9px;\n}\n.reels-v15-identity-avatar{\n  width:36px;height:36px;\n  border-radius:50%;\n  background:#ddd;color:#555;\n  display:grid;place-items:center;\n  font-size:10px;font-weight:900;\n}\n.reels-v15-identity-main{flex:1;min-width:0}\n.reels-v15-identity-main strong{display:block;font-size:12px}\n.reels-v15-identity-main span{\n  display:block;margin-top:2px;\n  color:rgba(255,255,255,.5);\n  font-size:9px;\n}\n.reels-v15-identity-switch{\n  height:30px;border:0;border-radius:10px;\n  background:#fff;color:#111;padding:0 10px;\n  font-size:9px;font-weight:850;cursor:pointer;\n}\n.reels-v15-identity-options{\n  display:none;\n  gap:7px;\n  flex-wrap:wrap;\n  margin-bottom:9px;\n}\n.reels-v15-identity-options.show{display:flex}\n.reels-v15-danger{color:#ff6b75!important}\n.reels-v15-toast{\n  position:absolute;\n  left:50%;\n  bottom:88px;\n  z-index:190;\n  transform:translate(-50%,12px);\n  background:rgba(24,24,27,.9);\n  color:#fff;\n  border:1px solid rgba(255,255,255,.12);\n  border-radius:999px;\n  padding:9px 13px;\n  font-size:10px;\n  font-weight:800;\n  opacity:0;\n  pointer-events:none;\n  transition:.2s ease;\n}\n.reels-v15-toast.show{\n  opacity:1;\n  transform:translate(-50%,0);\n}\n\n\n/* ===== v1.15.1 DM / batch prompt refinements ===== */\n.dm-stranger-request-list{\n  display:none;\n  margin-top:10px;\n  border-radius:17px;\n  overflow:hidden;\n  background:rgba(255,255,255,.58);\n}\n.dm-stranger-request-list.show{display:block}\n.dm-stranger-request{\n  display:flex;\n  align-items:center;\n  gap:9px;\n  min-height:58px;\n  padding:8px 9px;\n  border-bottom:1px solid rgba(0,0,0,.055);\n}\n.dm-stranger-request:last-child{border-bottom:0}\n.dm-stranger-request-avatar{\n  width:38px;\n  height:38px;\n  flex:0 0 38px;\n  border-radius:50%;\n  background:linear-gradient(135deg,#dde1e7,#f5f6f8);\n  display:grid;\n  place-items:center;\n  color:#555a62;\n  font-size:10px;\n  font-weight:850;\n}\n.dm-stranger-request-main{\n  min-width:0;\n  flex:1;\n}\n.dm-stranger-request-main strong{\n  display:block;\n  font-size:12px;\n}\n.dm-stranger-request-main span{\n  display:block;\n  margin-top:3px;\n  color:#7f848c;\n  font-size:10px;\n  line-height:1.4;\n  white-space:nowrap;\n  overflow:hidden;\n  text-overflow:ellipsis;\n}\n.dm-stranger-request-new{\n  width:7px;\n  height:7px;\n  flex:0 0 7px;\n  border-radius:50%;\n  background:#111;\n}\n.dm-stranger-request-delete{\n  width:24px;\n  height:24px;\n  flex:0 0 24px;\n  border:0;\n  border-radius:50%;\n  background:#f1f2f4;\n  color:#777d86;\n  display:grid;\n  place-items:center;\n  padding:0;\n  font-size:15px;\n  line-height:1;\n  cursor:pointer;\n}\n.batch-prompt-wrap{\n  margin-top:11px;\n}\n.batch-prompt-label{\n  display:block;\n  margin-bottom:6px;\n  color:#6f747c;\n  font-size:11px;\n  font-weight:800;\n}\n.batch-prompt-input{\n  width:100%;\n  min-height:68px;\n  border:1px solid rgba(0,0,0,.07);\n  border-radius:14px;\n  background:rgba(255,255,255,.82);\n  padding:10px 11px;\n  outline:none;\n  resize:none;\n  font:inherit;\n  font-size:12px;\n  line-height:1.45;\n  color:#111;\n}\n.batch-prompt-input::placeholder{color:#9b9fa6}\n.reels-v15-sheet .batch-prompt-label{\n  color:rgba(255,255,255,.62);\n}\n.reels-v15-sheet .batch-prompt-input{\n  border-color:rgba(255,255,255,.08);\n  background:rgba(255,255,255,.08);\n  color:#fff;\n}\n.reels-v15-sheet .batch-prompt-input::placeholder{\n  color:rgba(255,255,255,.38);\n}\n\n\n.dm-v2-arrow{display:none!important}\n.dm-section-edit{\n  border:0;\n  background:transparent;\n  color:#70757d;\n  font:inherit;\n  font-size:11px;\n  font-weight:800;\n  padding:4px 0 4px 8px;\n  cursor:pointer;\n}\n.dm-edit-check{\n  display:none;\n  width:22px;\n  height:22px;\n  flex:0 0 22px;\n  border:1.5px solid #c9ccd2;\n  border-radius:50%;\n  background:#fff;\n  color:#fff;\n  place-items:center;\n  padding:0;\n  font-size:12px;\n  font-weight:900;\n  cursor:pointer;\n}\n.dm-v2-page.editing .dm-edit-check{display:grid}\n.dm-v2-row.edit-selected .dm-edit-check{\n  background:#111;\n  border-color:#111;\n}\n.dm-v2-row.edit-selected .dm-edit-check::after{content:\"✓\"}\n.dm-edit-bar{\n  display:none;\n  flex:0 0 auto;\n  gap:8px;\n  padding:8px 12px 10px;\n  border-top:1px solid #f0f1f4;\n  background:rgba(255,255,255,.96);\n}\n.dm-v2-page.editing .dm-edit-bar{display:flex}\n.dm-edit-delete{\n  flex:1;\n  height:38px;\n  border:0;\n  border-radius:12px;\n  background:#111;\n  color:#fff;\n  font-size:11px;\n  font-weight:850;\n  cursor:pointer;\n}\n.dm-edit-delete:disabled{\n  opacity:.32;\n  cursor:default;\n}\n.dm-edit-count{\n  min-width:74px;\n  height:38px;\n  border-radius:12px;\n  background:#f1f2f5;\n  display:grid;\n  place-items:center;\n  color:#696e76;\n  font-size:10px;\n  font-weight:800;\n}\n\n\n/* ===== v1.15.3 Story editor / feed album editing ===== */\n.story-bottom{\n  position:absolute;\n  left:14px;\n  right:14px;\n  bottom:18px;\n  z-index:10;\n  display:flex;\n  align-items:center;\n  gap:10px;\n}\n.story-emoji-input{\n  flex:1;\n  height:44px;\n  border:1px solid rgba(255,255,255,.5);\n  border-radius:999px;\n  background:rgba(255,255,255,.06);\n  color:#fff;\n  font:inherit;\n  font-size:13px;\n  text-align:left;\n  padding:0 16px;\n  cursor:pointer;\n  backdrop-filter:blur(12px);\n  -webkit-backdrop-filter:blur(12px);\n}\n.story-small-icon{\n  width:44px;\n  height:44px;\n  flex:0 0 44px;\n  border:1px solid rgba(255,255,255,.32);\n  border-radius:50%;\n  background:rgba(255,255,255,.07);\n  color:#fff;\n  display:grid;\n  place-items:center;\n  font-size:21px;\n  cursor:pointer;\n  backdrop-filter:blur(12px);\n  -webkit-backdrop-filter:blur(12px);\n}\n.story-small-icon svg{\n  width:20px;height:20px;\n  stroke:#fff;fill:none;stroke-width:2;\n  stroke-linecap:round;stroke-linejoin:round;\n}\n.story-overlay-dim{\n  position:absolute;inset:0;z-index:13;background:rgba(0,0,0,.34);display:none;\n}\n.story-overlay-dim.show{display:block}\n.story-sheet{\n  position:absolute;left:12px;right:12px;bottom:14px;z-index:14;\n  border:1px solid rgba(255,255,255,.14);border-radius:24px;\n  background:rgba(22,22,26,.82);color:#fff;padding:14px;\n  backdrop-filter:blur(28px);-webkit-backdrop-filter:blur(28px);\n  box-shadow:0 18px 50px rgba(0,0,0,.36);\n  opacity:0;transform:translateY(10px) scale(.985);pointer-events:none;transition:.18s ease;\n}\n.story-sheet.show{opacity:1;transform:translateY(0) scale(1);pointer-events:auto}\n.story-sheet-head{display:flex;align-items:center;justify-content:space-between;margin-bottom:10px}\n.story-sheet-title{font-size:16px;font-weight:850}\n.story-sheet-close{\n  width:30px;height:30px;border:0;border-radius:50%;background:rgba(255,255,255,.09);color:#fff;font-size:18px;cursor:pointer;\n}\n.story-emoji-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:8px}\n.story-emoji-btn{\n  height:48px;border:0;border-radius:16px;background:rgba(255,255,255,.08);color:#fff;font-size:23px;cursor:pointer;\n}\n.story-emoji-copy{margin-top:10px;color:rgba(255,255,255,.62);font-size:10px;line-height:1.45}\n.story-edit-scroll{max-height:58vh;overflow-y:auto;scrollbar-width:none}\n.story-edit-scroll::-webkit-scrollbar{display:none}\n.story-edit-chiprow{display:flex;gap:7px;overflow-x:auto;padding-bottom:2px;scrollbar-width:none}\n.story-edit-chiprow::-webkit-scrollbar{display:none}\n.story-edit-chip{\n  border:1px solid rgba(255,255,255,.14);border-radius:999px;background:rgba(255,255,255,.07);color:#fff;\n  padding:7px 10px;font-size:10px;font-weight:800;white-space:nowrap;cursor:pointer;\n}\n.story-edit-chip.active{background:#fff;color:#111}\n.story-edit-block{margin-top:10px;border-radius:18px;background:rgba(255,255,255,.07);padding:12px}\n.story-edit-label{display:block;margin-bottom:6px;color:rgba(255,255,255,.72);font-size:10px;font-weight:800}\n.story-edit-copy{color:rgba(255,255,255,.64);font-size:10px;line-height:1.45}\n.story-edit-textarea{\n  width:100%;min-height:90px;border:0;border-radius:14px;background:rgba(255,255,255,.08);color:#fff;outline:none;resize:none;padding:10px;font:inherit;font-size:12px;line-height:1.5;\n}\n.story-edit-select{\n  width:100%;height:40px;border:0;border-radius:12px;background:rgba(255,255,255,.08);color:#fff;outline:none;padding:0 10px;font:inherit;font-size:11px;\n}\n.story-edit-row{display:flex;gap:8px;margin-top:10px}\n.story-edit-btn{\n  flex:1;height:38px;border:0;border-radius:12px;font-size:10px;font-weight:850;cursor:pointer;\n}\n.story-edit-btn.light{background:rgba(255,255,255,.12);color:#fff}\n.story-edit-btn.dark{background:#fff;color:#111}\n.story-edit-badge{margin-top:7px;border-radius:11px;background:rgba(255,255,255,.08);color:rgba(255,255,255,.78);padding:8px 10px;font-size:10px;line-height:1.45}\n.story-edit-footer{display:flex;gap:8px;margin-top:12px}\n.story-edit-save{flex:1;height:42px;border:0;border-radius:14px;background:#fff;color:#111;font-size:12px;font-weight:850;cursor:pointer}\n.post-edit-select{\n  width:100%;height:40px;border:0;border-radius:12px;background:#f4f5f7;color:#111;outline:none;padding:0 10px;font:inherit;font-size:12px;\n}\n.post-edit-note{margin-top:8px;color:#7c818a;font-size:11px;line-height:1.45}\n.post-edit-badge{margin-top:8px;border-radius:12px;background:#f4f5f7;color:#636872;padding:8px 10px;font-size:11px;line-height:1.45}\n\n\n/* ===== v1.15.4 Story variable set / context cards / stranger temp chat / atmosphere ===== */\n.story-heart-img{\n  width:24px;height:24px;object-fit:contain;display:block;\n  filter:brightness(0) invert(1);\n}\n.story-small-icon.liked .story-heart-img{filter:none}\n.story-edit-manage{\n  display:grid;\n  grid-template-columns:repeat(3,1fr);\n  gap:7px;\n  margin-top:10px;\n}\n.story-edit-btn.danger{\n  background:rgba(255,82,94,.16);\n  color:#ff8b94;\n}\n.story-edit-limit{\n  margin-top:7px;\n  color:rgba(255,255,255,.5);\n  font-size:9px;\n}\n.dm-context-card{\n  width:min(82%,310px);\n  border-radius:17px;\n  overflow:hidden;\n  background:#f6f7f9;\n  border:1px solid #eceef2;\n  box-shadow:0 7px 18px rgba(15,20,30,.06);\n}\n.dm-context-card.compact{\n  padding:10px 11px;\n}\n.dm-context-kicker{\n  font-size:9px;\n  color:#92969d;\n  margin-bottom:5px;\n}\n.dm-context-main{\n  font-size:11px;\n  line-height:1.48;\n  color:#2f3339;\n}\n.dm-context-main strong{font-weight:850}\n.dm-context-summary{\n  margin-top:5px;\n  font-size:10px;\n  line-height:1.45;\n  color:#737880;\n}\n.dm-context-emoji{\n  display:inline-grid;\n  place-items:center;\n  width:28px;height:28px;\n  margin-right:6px;\n  border-radius:10px;\n  background:#fff;\n  font-size:18px;\n  vertical-align:middle;\n}\n.dm-temp-chat-card{\n  max-height:76vh;\n}\n\n.nini-ins-root .dm-temp-avatar{width:46px!important;height:46px!important;min-width:46px!important;min-height:46px!important;flex:0 0 46px!important;aspect-ratio:1/1!important;border-radius:50%!important;overflow:hidden!important;background-size:cover!important;background-position:center!important}\n.nini-ins-root .dm-temp-bubble-translatable{cursor:pointer}\n.nini-ins-root .dm-temp-bubble-translation{margin-top:8px;padding-top:8px;border-top:1px solid rgba(127,127,127,.22);font-size:12px;line-height:1.55;opacity:.82}\n.nini-ins-root .dm-temp-tip{margin:0 0 10px;padding:9px 10px;border-radius:12px;background:#f1f2f5;color:#7b8088;font-size:10px;line-height:1.45}\n.dm-temp-meta{\n  display:flex;\n  align-items:center;\n  gap:10px;\n  padding:10px;\n  border-radius:16px;\n  background:rgba(255,255,255,.62);\n}\n.dm-temp-avatar{\n  width:42px;height:42px;\n  border-radius:50%;\n  background:linear-gradient(135deg,#dde1e7,#f6f7f9);\n  display:grid;place-items:center;\n  font-size:11px;font-weight:900;color:#555;\n}\n.dm-temp-meta-main{flex:1;min-width:0}\n.dm-temp-meta-main strong{display:block;font-size:13px}\n.dm-temp-meta-main span{display:block;margin-top:3px;color:#7d828a;font-size:10px}\n.dm-temp-badge{\n  border-radius:999px;\n  background:#111;color:#fff;\n  padding:5px 7px;\n  font-size:8px;font-weight:850;\n}\n.dm-temp-messages{\n  max-height:260px;\n  overflow-y:auto;\n  margin-top:10px;\n  padding:4px 2px;\n  scrollbar-width:none;\n}\n.dm-temp-messages::-webkit-scrollbar{display:none}\n.dm-temp-row{display:flex;margin:7px 0}\n.dm-temp-row.them{justify-content:flex-start}\n.dm-temp-row.me{justify-content:flex-end}\n.dm-temp-bubble{\n  max-width:78%;\n  padding:9px 11px;\n  border-radius:15px;\n  font-size:11px;\n  line-height:1.45;\n}\n.dm-temp-row.them .dm-temp-bubble{background:#eef0f3;color:#111}\n.dm-temp-row.me .dm-temp-bubble{background:#111;color:#fff}\n.dm-temp-stack{max-width:82%;min-width:0}\n.dm-temp-stack .dm-temp-bubble{max-width:none}\n.dm-temp-translation-toggle{\n  border:0;background:transparent;padding:4px 3px 0;color:#8a8f97;\n  font:inherit;font-size:9px;cursor:pointer;\n}\n.dm-temp-translation-box{\n  margin-top:5px;padding:7px 9px;border-radius:10px;background:#eceef2;\n  color:#555b63;font-size:10px;line-height:1.45;\n}\n.dm-temp-row.me .dm-temp-translation-toggle,.dm-temp-row.me .dm-temp-translation-box{display:none}\n.dm-temp-composer{\n  display:flex;gap:7px;margin-top:10px;\n}\n.dm-temp-composer input{\n  flex:1;height:38px;border:0;border-radius:12px;\n  background:#f1f2f5;padding:0 10px;outline:none;font:inherit;font-size:11px;\n}\n.dm-temp-composer button{\n  width:52px;border:0;border-radius:12px;background:#111;color:#fff;font-size:10px;font-weight:850;\n}\n.dm-temp-actions{\n  display:grid;\n  grid-template-columns:1fr 1fr;\n  gap:7px;\n  margin-top:10px;\n}\n.dm-temp-actions button{\n  height:37px;border:0;border-radius:12px;\n  background:#eceef2;color:#4f545c;font-size:9px;font-weight:850;\n}\n.dm-temp-actions button.primary{background:#111;color:#fff}\n.dm-temp-actions button.danger{color:#c3414b}\n.dm-temp-row-list{\n  display:flex;align-items:center;gap:9px;\n  min-height:64px;padding:7px 8px;border-radius:15px;\n}\n.dm-temp-row-list .dm-v2-avatar{width:46px;height:46px}\n.dm-temp-tag{\n  margin-left:5px;\n  padding:2px 5px;border-radius:999px;\n  background:#f0f1f4;color:#777c84;\n  font-size:8px;font-weight:800;\n}\n.social-stranger-settings{\n  display:none;\n  margin-top:10px;\n  padding:11px;\n  border-radius:15px;\n  background:#f5f6f8;\n}\n.social-stranger-settings.show{display:block}\n.social-setting-row{\n  display:flex;\n  align-items:center;\n  justify-content:space-between;\n  gap:10px;\n  padding:8px 0;\n  border-bottom:1px solid #e9ebef;\n}\n.social-setting-row:last-child{border-bottom:0}\n.social-setting-copy strong{display:block;font-size:11px}\n.social-setting-copy span{display:block;margin-top:2px;color:#81868e;font-size:9px;line-height:1.4}\n.social-setting-row select{\n  height:32px;border:0;border-radius:10px;background:#fff;padding:0 8px;font-size:10px;\n}\n.social-setting-textarea{\n  width:100%;min-height:72px;border:0;border-radius:12px;background:#fff;\n  padding:9px 10px;outline:none;resize:none;font:inherit;font-size:10px;line-height:1.45;\n}\n.social-setting-check{\n  display:flex;gap:7px;flex-wrap:wrap;margin-top:8px;\n}\n.social-setting-chip{\n  border:0;border-radius:999px;background:#fff;color:#666b73;padding:7px 9px;font-size:9px;font-weight:800;\n}\n.social-setting-chip.active{background:#111;color:#fff}\n.profile-setting-card{\n  position:absolute;\n  left:10px;right:10px;bottom:14px;\n  z-index:162;\n  max-height:calc(100% - 28px);\n  overflow-y:auto;\n  scrollbar-width:none;\n  border:1px solid rgba(255,255,255,.78);\n  border-radius:25px;\n  background:rgba(247,248,250,.9);\n  backdrop-filter:blur(28px);\n  -webkit-backdrop-filter:blur(28px);\n  box-shadow:0 18px 46px rgba(15,20,30,.18);\n  padding:14px;\n  opacity:0;\n  transform:translateY(10px) scale(.985);\n  pointer-events:none;\n  transition:.2s ease;\n}\n.profile-setting-card::-webkit-scrollbar{display:none}\n.profile-setting-card.show{\n  opacity:1;\n  transform:translateY(0) scale(1);\n  pointer-events:auto;\n}\n#profileAtmosphereCard .profile-setting-head{\n  position:sticky;\n  top:-14px;\n  z-index:8;\n  margin:-14px -4px 10px;\n  padding:14px 4px 8px;\n  background:linear-gradient(180deg,rgba(247,248,250,.98) 0%,rgba(247,248,250,.94) 78%,rgba(247,248,250,0) 100%);\n  backdrop-filter:blur(22px);\n  -webkit-backdrop-filter:blur(22px);\n}\n#profileAtmosphereCard .profile-setting-close{flex:0 0 30px}\n.profile-setting-head{\n  display:flex;align-items:center;justify-content:space-between;margin-bottom:10px;\n}\n.profile-setting-head strong{font-size:17px}\n.profile-setting-close{\n  width:30px;height:30px;border:0;border-radius:50%;background:#fff;font-size:18px;\n}\n.profile-setting-block{\n  margin-top:10px;padding:11px;border-radius:16px;background:rgba(255,255,255,.7);\n}\n.profile-setting-label{\n  display:block;margin-bottom:6px;color:#666b73;font-size:10px;font-weight:850;\n}\n.profile-setting-textarea{\n  width:100%;min-height:78px;border:0;border-radius:12px;background:#f3f4f6;\n  padding:9px 10px;outline:none;resize:none;font:inherit;font-size:11px;line-height:1.5;\n}\n.profile-setting-textarea.world-prompt{\n  min-height:310px;\n  resize:none;\n  scrollbar-width:none;\n}\n.profile-setting-textarea.world-prompt::-webkit-scrollbar{display:none}\n.profile-setting-actions{display:grid;grid-template-columns:1fr 1fr;gap:9px;margin-top:12px}\n.profile-setting-actions .profile-setting-save{margin-top:0}\n.profile-setting-reset{height:43px;border:1px solid rgba(0,0,0,.08);border-radius:14px;background:#fff;color:#555;font-size:12px;font-weight:800}\n.profile-setting-save{\n  width:100%;height:43px;border:0;border-radius:14px;background:#111;color:#fff;\n  margin-top:12px;font-size:12px;font-weight:850;\n}\n.profile-setting-notebox{\n  padding:13px 14px;\n  border-radius:16px;\n  background:rgba(255,255,255,.74);\n  border:1px solid rgba(0,0,0,.06);\n  color:#515762;\n  font-size:12px;\n  line-height:1.65;\n}\n.profile-setting-rulelist{\n  display:grid;\n  gap:8px;\n  padding:2px 0;\n  color:#555d68;\n  font-size:12px;\n  line-height:1.55;\n}\n.profile-setting-input,.profile-setting-select{\n  width:100%;\n  height:42px;\n  border:1px solid rgba(0,0,0,.08);\n  border-radius:14px;\n  background:rgba(255,255,255,.86);\n  padding:0 12px;\n  font-size:13px;\n  color:#1d2229;\n  outline:none;\n}\n.profile-setting-select{\n  appearance:none;\n}\n.profile-subapi-list{\n  display:grid;\n  gap:10px;\n}\n.profile-subapi-empty{\n  padding:13px 14px;\n  border-radius:14px;\n  background:rgba(255,255,255,.72);\n  border:1px dashed rgba(0,0,0,.09);\n  color:#808793;\n  font-size:12px;\n}\n.profile-subapi-item{\n  display:flex;\n  align-items:flex-start;\n  gap:10px;\n  padding:12px;\n  border-radius:16px;\n  background:rgba(255,255,255,.8);\n  border:1px solid rgba(0,0,0,.06);\n}\n.profile-subapi-item.active{\n  box-shadow:0 10px 26px rgba(0,0,0,.08);\n  border-color:rgba(17,17,17,.12);\n}\n.profile-subapi-copy{\n  flex:1;\n  min-width:0;\n}\n.profile-subapi-copy strong{\n  display:block;\n  font-size:13px;\n  color:#15191f;\n}\n.profile-subapi-copy span{\n  display:block;\n  margin-top:3px;\n  color:#77808b;\n  font-size:11px;\n  line-height:1.45;\n  word-break:break-all;\n}\n.profile-subapi-actions{\n  display:flex;\n  flex-direction:column;\n  gap:7px;\n}\n.profile-subapi-actions button,.profile-subapi-mini{\n  height:32px;\n  border:0;\n  border-radius:11px;\n  padding:0 10px;\n  font-size:12px;\n  font-weight:800;\n  cursor:pointer;\n}\n.profile-subapi-actions button{\n  min-width:58px;\n}\n.profile-subapi-actions .on{\n  background:#111;\n  color:#fff;\n}\n.profile-subapi-actions .off,.profile-subapi-mini{\n  background:#eef1f5;\n  color:#5d6671;\n}\n.profile-subapi-actions .danger{\n  background:#fff1f1;\n  color:#c65252;\n}\n.profile-subapi-form{\n  display:grid;\n  gap:10px;\n}\n.profile-subapi-formrow{\n  display:grid;\n  grid-template-columns:1fr auto;\n  gap:8px;\n}\n\n\n/* ===== v1.15.5 NPC profile mode / compact context strategy ===== */\n.char-profile-tabs.npc-profile{\n  grid-template-columns:repeat(3,1fr);\n}\n.char-profile-tabs.npc-profile .char-tab[data-char-tab=\"album\"]{\n  display:none;\n}\n.char-profile-tabs.npc-profile .char-tab-indicator{\n  width:33.3333%;\n}\n.char-album-svg{\n  width:28px!important;\n  height:28px!important;\n}\n.char-album-svg .back{\n  opacity:.45;\n}\n.char-profile-kind{\n  display:inline-flex;\n  align-items:center;\n  height:20px;\n  margin-left:6px;\n  padding:0 7px;\n  border-radius:999px;\n  background:#f1f2f5;\n  color:#777c84;\n  font-size:8px;\n  font-weight:850;\n  vertical-align:middle;\n}\n.npc-media-note{\n  margin-top:7px;\n  border-radius:11px;\n  background:#f5f6f8;\n  color:#747982;\n  padding:8px 10px;\n  font-size:10px;\n  line-height:1.45;\n}\n.story-batch-note{\n  margin-top:5px;\n  font-size:9px;\n  color:rgba(255,255,255,.48);\n}\n\n\n/* ===== v1.15.6 Story caption / user Story composer / own reactions ===== */\n.story-caption-shell{\n  position:absolute;\n  left:16px;\n  right:72px;\n  bottom:76px;\n  z-index:9;\n  height:50px;\n  overflow:hidden;\n  pointer-events:none;\n  -webkit-mask-image:linear-gradient(to bottom,transparent 0,#000 14%,#000 86%,transparent 100%);\n  mask-image:linear-gradient(to bottom,transparent 0,#000 14%,#000 86%,transparent 100%);\n}\n.story-caption-shell.own{\n  right:116px;\n}\n.story-caption-text{\n  font-size:12px;\n  line-height:1.55;\n  color:rgba(255,255,255,.93);\n  text-align:left;\n  text-shadow:0 1px 9px rgba(0,0,0,.48);\n  white-space:pre-wrap;\n  animation:storyCaptionDrift 8s ease-in-out infinite alternate;\n}\n@keyframes storyCaptionDrift{\n  0%{transform:translateY(4px);opacity:.72}\n  18%{opacity:1}\n  82%{opacity:1}\n  100%{transform:translateY(-5px);opacity:.76}\n}\n.story-bottom.own{\n  justify-content:flex-end;\n}\n.story-bottom.own .story-emoji-input{\n  display:none;\n}\n.story-own-reaction-sheet-list{\n  max-height:310px;\n  overflow-y:auto;\n  scrollbar-width:none;\n}\n.story-own-reaction-sheet-list::-webkit-scrollbar{display:none}\n.story-own-reaction-row{\n  display:flex;\n  align-items:center;\n  gap:9px;\n  min-height:54px;\n  padding:7px 2px;\n  border-bottom:1px solid rgba(255,255,255,.07);\n}\n.story-own-reaction-row:last-child{border-bottom:0}\n.story-own-reaction-avatar{\n  width:36px;height:36px;flex:0 0 36px;border-radius:50%;\n  background:rgba(255,255,255,.14);\n  display:grid;place-items:center;\n  font-size:10px;font-weight:900;\n}\n.story-own-reaction-main{flex:1;min-width:0}\n.story-own-reaction-main strong{display:block;font-size:11px}\n.story-own-reaction-main span{\n  display:block;margin-top:2px;\n  color:rgba(255,255,255,.54);\n  font-size:9px;\n  white-space:nowrap;overflow:hidden;text-overflow:ellipsis;\n}\n.story-own-reaction-emoji{\n  font-size:21px;\n}\n.story-own-reaction-plan-btn{\n  width:100%;height:40px;margin-top:12px;border:0;border-radius:13px;\n  background:#fff;color:#111;font-size:11px;font-weight:850;cursor:pointer;\n}\n.story-own-reaction-plan-btn:disabled{opacity:.52;cursor:default}\n.story-create-route{\n  background:#fff;\n}\n.story-create-body{\n  flex:1;\n  overflow-y:auto;\n  padding:14px 14px 92px;\n  scrollbar-width:none;\n}\n.story-create-body::-webkit-scrollbar{display:none}\n.story-create-hero{\n  display:flex;\n  align-items:center;\n  justify-content:space-between;\n  gap:12px;\n  margin-bottom:14px;\n}\n.story-create-hero strong{\n  font-size:18px;\n  font-weight:900;\n}\n.story-create-hero span{\n  color:#969aa2;\n  font-size:10px;\n}\n.story-create-add{\n  width:100%;\n  min-height:94px;\n  border:1px dashed #d8dbe1;\n  border-radius:20px;\n  background:#fafbfc;\n  display:flex;\n  flex-direction:column;\n  align-items:center;\n  justify-content:center;\n  gap:7px;\n  cursor:pointer;\n}\n.story-create-add-circle{\n  width:42px;height:42px;\n  border-radius:50%;\n  background:#111;\n  color:#fff;\n  display:grid;place-items:center;\n  font-size:28px;\n  font-weight:250;\n  line-height:1;\n}\n.story-create-add span{\n  font-size:11px;\n  color:#666b73;\n  font-weight:800;\n}\n.story-create-count{\n  margin-top:8px;\n  color:#9a9ea5;\n  font-size:9px;\n  text-align:center;\n}\n.story-create-items{\n  display:grid;\n  gap:9px;\n  margin-top:12px;\n}\n.story-create-item{\n  border-radius:18px;\n  background:#f5f6f8;\n  padding:11px;\n}\n.story-create-item-head{\n  display:flex;\n  align-items:center;\n  gap:8px;\n  margin-bottom:8px;\n}\n.story-create-type{\n  padding:5px 7px;\n  border-radius:999px;\n  background:#111;\n  color:#fff;\n  font-size:8px;\n  font-weight:850;\n}\n.story-create-item-head strong{\n  flex:1;\n  font-size:11px;\n}\n.story-create-remove{\n  width:27px;height:27px;\n  border:0;border-radius:50%;\n  background:#fff;color:#777c84;\n  font-size:17px;\n}\n.story-create-textarea{\n  width:100%;\n  min-height:76px;\n  border:0;\n  border-radius:13px;\n  background:#fff;\n  padding:9px 10px;\n  outline:none;\n  resize:none;\n  font:inherit;\n  font-size:11px;\n  line-height:1.5;\n}\n.story-create-section{\n  margin-top:14px;\n  border-radius:18px;\n  background:#f7f8fa;\n  padding:12px;\n}\n.story-create-label{\n  display:block;\n  margin-bottom:7px;\n  font-size:11px;\n  font-weight:850;\n}\n.story-create-caption{\n  width:100%;\n  min-height:88px;\n  border:0;\n  border-radius:14px;\n  background:#fff;\n  padding:10px 11px;\n  outline:none;\n  resize:none;\n  font:inherit;\n  font-size:12px;\n  line-height:1.55;\n}\n.story-create-hint{\n  margin-top:7px;\n  color:#9599a1;\n  font-size:9px;\n  line-height:1.45;\n}\n.story-create-bottom{\n  position:absolute;\n  left:0;right:0;bottom:0;\n  height:72px;\n  padding:10px 14px 14px;\n  background:linear-gradient(to top,#fff 74%,rgba(255,255,255,.88));\n}\n.story-create-publish{\n  width:100%;\n  height:46px;\n  border:0;\n  border-radius:15px;\n  background:#111;\n  color:#fff;\n  font-size:12px;\n  font-weight:900;\n}\n.story-create-choice{\n  display:grid;\n  grid-template-columns:1fr 1fr;\n  gap:9px;\n}\n.story-create-choice button{\n  height:72px;\n  border:0;\n  border-radius:16px;\n  background:rgba(255,255,255,.09);\n  color:#fff;\n  font-size:12px;\n  font-weight:850;\n}\n.story-create-choice button span{\n  display:block;\n  margin-top:4px;\n  color:rgba(255,255,255,.55);\n  font-size:9px;\n  font-weight:500;\n}\n\n/* v1.08.4 merged: Story add-content sheet only */\n.nini-ins-root #storyCreateChoiceSheet{background:rgba(248,248,250,.98)!important;color:#111!important;border-color:rgba(0,0,0,.06)!important;box-shadow:0 18px 50px rgba(15,20,30,.16)!important}\n.nini-ins-root #storyCreateChoiceSheet .story-sheet-close{background:#eceef2!important;color:#111!important}\n.nini-ins-root #storyCreateChoiceSheet .story-create-choice button{background:#fff!important;color:#111!important;border:1px solid rgba(0,0,0,.055)!important}\n.nini-ins-root #storyCreateChoiceSheet .story-create-choice button span{color:#8a8f97!important}\n.story-create-prompt-note{\n  margin-top:10px;\n  border-radius:14px;\n  background:#f2f3f6;\n  padding:10px;\n  color:#6f747c;\n  font-size:10px;\n  line-height:1.5;\n}\n\n\n/* ===== v1.15.7 Story fade/share + highlight binding + temp Enter ===== */\n.story-caption-shell{\n  height:46px;\n}\n.story-caption-text{\n  animation:storyCaptionFade 4.6s ease-in-out infinite;\n}\n@keyframes storyCaptionFade{\n  0%{opacity:0}\n  18%{opacity:1}\n  66%{opacity:1}\n  100%{opacity:0}\n}\n.story-bottom{\n  gap:7px;\n}\n.story-small-icon{\n  width:40px;\n  height:40px;\n  flex:0 0 40px;\n}\n.story-bottom.own{\n  justify-content:flex-start;\n  gap:6px;\n}\n.story-bottom.own .story-small-icon{\n  width:34px;\n  height:34px;\n  flex:0 0 34px;\n  border-width:1px;\n}\n.story-bottom.own .story-heart-img{\n  width:19px;\n  height:19px;\n}\n.story-bottom.own .story-small-icon svg{\n  width:17px;\n  height:17px;\n}\n.story-share-icon{\n  width:21px;\n  height:21px;\n  object-fit:contain;\n  filter:brightness(0) invert(1);\n}\n.story-bottom.own .story-share-icon{\n  width:17px;\n  height:17px;\n}\n.story-delete-icon{display:grid!important;color:#ff5a61}\n.story-delete-icon svg{stroke:#ff5a61}\n.story-share-list{\n  display:grid;\n  gap:6px;\n}\n.story-share-row{\n  display:flex;\n  align-items:center;\n  gap:9px;\n  min-height:50px;\n  padding:6px 4px;\n  border-bottom:1px solid rgba(255,255,255,.06);\n}\n.story-share-row:last-child{border-bottom:0}\n.story-share-avatar{\n  width:34px;height:34px;border-radius:50%;\n  background:rgba(255,255,255,.13);\n  display:grid;place-items:center;\n  font-size:10px;font-weight:900;\n}\n.story-share-main{flex:1;min-width:0}\n.story-share-main strong{display:block;font-size:11px}\n.story-share-main span{\n  display:block;margin-top:2px;color:rgba(255,255,255,.52);font-size:9px;\n}\n.story-share-send{\n  height:29px;border:0;border-radius:9px;\n  background:#fff;color:#111;padding:0 10px;\n  font-size:9px;font-weight:850;\n}\n.story-share-send:disabled{opacity:.32;cursor:default}\n.dm-story-share-card{\n  width:min(80%,292px);\n  border-radius:17px;\n  padding:11px 12px;\n  background:#f6f7f9;\n  border:1px solid #eceef2;\n}\n.dm-story-share-kicker{\n  color:#92969d;\n  font-size:9px;\n  margin-bottom:5px;\n}\n.dm-story-share-title{\n  font-size:11px;\n  font-weight:850;\n  color:#262a30;\n}\n.dm-story-share-copy{\n  margin-top:5px;\n  color:#6f747c;\n  font-size:10px;\n  line-height:1.45;\n}\n.highlight-story-bind-list,.char-highlight-story-bind-list{\n  margin-top:7px;\n  display:grid;\n  gap:7px;\n}\n.highlight-story-bind-row{\n  display:flex;\n  align-items:center;\n  gap:9px;\n  padding:9px;\n  border-radius:13px;\n  background:#f4f5f7;\n}\n.char-highlight-story-bind-list .highlight-story-bind-row{\n  background:#f4f5f7;\n}\n.highlight-story-bind-main{\n  flex:1;\n  min-width:0;\n}\n.highlight-story-bind-main strong{\n  display:block;\n  font-size:10px;\n  color:#33373d;\n}\n.highlight-story-bind-main span{\n  display:block;\n  margin-top:2px;\n  color:#8a8f97;\n  font-size:9px;\n  white-space:nowrap;\n  overflow:hidden;\n  text-overflow:ellipsis;\n}\n.highlight-story-bind-btn{\n  height:28px;\n  border:0;\n  border-radius:9px;\n  background:#111;\n  color:#fff;\n  padding:0 9px;\n  font-size:8px;\n  font-weight:850;\n}\n.highlight-story-bind-btn.bound{\n  background:#dfe2e7;\n  color:#555b64;\n}\n.highlight-story-bind-empty{\n  color:#8e939b;\n  font-size:10px;\n  line-height:1.45;\n}\n\n\n/* ===== v1.15.8 User Story media source + DM preview sync ===== */\n.story-create-media-tools{\n  display:grid;\n  gap:7px;\n  margin-bottom:8px;\n}\n.story-create-media-row{\n  display:grid;\n  grid-template-columns:auto 1fr auto;\n  gap:7px;\n  align-items:center;\n}\n.story-create-file-btn,.story-create-generate-btn{\n  height:36px;\n  border:0;\n  border-radius:11px;\n  background:#111;\n  color:#fff;\n  padding:0 10px;\n  display:flex;\n  align-items:center;\n  justify-content:center;\n  font-size:9px;\n  font-weight:850;\n  cursor:pointer;\n  white-space:nowrap;\n}\n.story-create-generate-btn{\n  background:#111;\n  color:#fff;\n}\n.story-create-url{\n  min-width:0;\n  height:36px;\n  border:0;\n  border-radius:11px;\n  background:#fff;\n  padding:0 10px;\n  outline:none;\n  font:inherit;\n  font-size:10px;\n  color:#111;\n}\n.story-create-preview{\n  min-height:48px;\n  border-radius:12px;\n  background:#fff;\n  overflow:hidden;\n  display:flex;\n  align-items:center;\n  justify-content:center;\n  color:#9398a0;\n  font-size:9px;\n  line-height:1.4;\n  text-align:center;\n  padding:6px;\n}\n.story-create-preview img{\n  width:100%;\n  max-height:150px;\n  display:block;\n  object-fit:cover;\n  border-radius:9px;\n}\n.story-create-source{\n  font-size:9px;\n  color:#8b9098;\n  line-height:1.4;\n}\n.story-create-video-note{\n  margin-bottom:8px;\n  border-radius:11px;\n  background:#fff;\n  padding:8px 9px;\n  color:#858a92;\n  font-size:9px;\n  line-height:1.45;\n}\n\n\n/* ===== v1.15.9 Vision fallback + per-item Highlight binding ===== */\n.story-create-desc-hint{\n  margin:6px 0 8px;\n  color:#8b9098;\n  font-size:9px;\n  line-height:1.45;\n}\n.story-create-desc-hint strong{color:#565b63}\n.highlight-description,.char-highlight-description{\n  width:100%;\n  min-height:66px;\n  border:0;\n  border-radius:12px;\n  background:#f4f5f7;\n  padding:9px 10px;\n  outline:none;\n  resize:none;\n  font:inherit;\n  font-size:11px;\n  line-height:1.45;\n  color:#111;\n}\n.highlight-story-item{\n  display:flex;\n  align-items:center;\n  gap:9px;\n  padding:9px;\n  border-radius:13px;\n  background:#f4f5f7;\n}\n.highlight-story-item + .highlight-story-item{margin-top:7px}\n.highlight-story-index{\n  width:31px;height:31px;flex:0 0 31px;\n  border-radius:10px;\n  background:#fff;\n  display:grid;place-items:center;\n  color:#656a72;\n  font-size:9px;\n  font-weight:900;\n}\n.highlight-story-select{\n  width:28px;height:28px;flex:0 0 28px;\n  border:0;border-radius:9px;\n  background:#e0e3e8;\n  color:#777c84;\n  display:grid;place-items:center;\n  font-size:12px;\n  font-weight:900;\n  cursor:pointer;\n}\n.highlight-story-select.selected{\n  background:#111;\n  color:#fff;\n}\n.highlight-story-type{\n  display:inline-block;\n  margin-right:5px;\n  padding:2px 5px;\n  border-radius:999px;\n  background:#e8eaee;\n  color:#71767e;\n  font-size:8px;\n  font-weight:850;\n}\n.highlight-story-summary{\n  min-width:0;flex:1;\n}\n.highlight-story-summary strong{\n  display:block;\n  font-size:10px;\n  color:#33373d;\n}\n.highlight-story-summary span{\n  display:block;\n  margin-top:3px;\n  color:#8b9098;\n  font-size:9px;\n  white-space:nowrap;\n  overflow:hidden;\n  text-overflow:ellipsis;\n}\n.highlight-bind-caption-note{\n  margin-bottom:7px;\n  color:#8e939b;\n  font-size:9px;\n  line-height:1.4;\n}\n\n\n/* ===== v1.15.11 Highlight purpose editor fix ===== */\n.highlight-editor-card{\n  top:14px;\n  bottom:82px;\n  max-height:none;\n  overflow-y:auto;\n  overscroll-behavior:contain;\n  -webkit-overflow-scrolling:touch;\n  scrollbar-width:none;\n}\n.highlight-editor-card::-webkit-scrollbar{display:none}\n\n.char-highlight-editor{\n  top:14px;\n  bottom:14px;\n  max-height:none;\n  overflow-y:auto;\n  overscroll-behavior:contain;\n  -webkit-overflow-scrolling:touch;\n  scrollbar-width:none;\n}\n.char-highlight-editor::-webkit-scrollbar{display:none}\n\n.highlight-purpose-card{\n  margin-top:11px;\n  padding:11px;\n  border-radius:16px;\n  background:rgba(255,255,255,.72);\n  border:1px solid rgba(255,255,255,.88);\n}\n.highlight-purpose-head{\n  display:flex;\n  align-items:flex-end;\n  justify-content:space-between;\n  gap:10px;\n  margin-bottom:7px;\n}\n.highlight-purpose-title{\n  font-size:12px;\n  font-weight:850;\n  color:#555a62;\n}\n.highlight-purpose-count{\n  flex:0 0 auto;\n  color:#a0a4ab;\n  font-size:9px;\n}\n.highlight-purpose-textarea{\n  display:block;\n  width:100%;\n  min-height:92px;\n  max-height:170px;\n  border:1px solid rgba(0,0,0,.055);\n  border-radius:13px;\n  background:#f4f5f7;\n  color:#111;\n  padding:10px 11px;\n  outline:none;\n  resize:vertical;\n  font:inherit;\n  font-size:12px;\n  line-height:1.55;\n  pointer-events:auto;\n  user-select:text;\n  -webkit-user-select:text;\n  touch-action:manipulation;\n}\n.highlight-purpose-textarea:focus{\n  background:#fff;\n  border-color:rgba(0,0,0,.14);\n  box-shadow:0 0 0 3px rgba(0,0,0,.035);\n}\n.highlight-purpose-hint{\n  margin-top:7px;\n  color:#92969d;\n  font-size:9px;\n  line-height:1.45;\n}\n\n\n/* ===== v1.15.12 Highlight full hit-area fix ===== */\n.user-highlight-strip,.char-highlight-strip{\n  position:relative;\n  z-index:7;\n  overflow-y:visible;\n}\n\n/* Every Highlight is a real button and the whole cell is clickable. */\n.user-highlight,.char-highlight{\n  appearance:none;\n  -webkit-appearance:none;\n  width:68px;\n  min-width:68px;\n  min-height:84px;\n  flex:0 0 68px;\n  margin:0;\n  padding:0 3px 4px;\n  border:0;\n  outline:0;\n  background:transparent;\n  color:inherit;\n  font:inherit;\n  text-align:center;\n  cursor:pointer;\n  position:relative;\n  z-index:2;\n  display:block;\n  touch-action:manipulation;\n  -webkit-tap-highlight-color:transparent;\n}\n\n/* Children never steal/cancel the click; the parent button owns the whole area. */\n.user-highlight-ring,.user-highlight-name,.user-highlight-ring *,.char-highlight-ring,.char-highlight-name,.char-highlight-ring *{\n  pointer-events:none;\n}\n\n.user-highlight:active,.char-highlight:active{\n  transform:scale(.97);\n}\n\n.user-highlight:focus-visible .user-highlight-ring,.char-highlight:focus-visible .char-highlight-ring{\n  outline:2px solid rgba(17,17,17,.2);\n  outline-offset:2px;\n}\n\n\n/* ===== v1.15.13 Highlight geometry hit-test fallback ===== */\n.user-highlight-strip,.char-highlight-strip{\n  z-index:20 !important;\n  isolation:isolate;\n}\n\n.user-profile-tabs,.char-profile-tabs{\n  z-index:2 !important;\n}\n\n/* Full-cell invisible hit surface owned by the Highlight button. */\n.user-highlight::after,.char-highlight::after{\n  content:\"\";\n  position:absolute;\n  inset:-2px -3px;\n  z-index:8;\n  border-radius:36px;\n  cursor:pointer;\n  pointer-events:auto;\n}\n\n.user-highlight-ring,.char-highlight-ring{\n  position:relative;\n  z-index:1;\n}\n\n\n/* ===== v1.15.14 Highlight final interaction polish ===== */\n.user-highlight-strip,.char-highlight-strip,.user-highlight-strip *,.char-highlight-strip *{\n  -webkit-user-select:none;\n  user-select:none;\n}\n\n.user-highlight,.char-highlight{\n  pointer-events:auto !important;\n  cursor:pointer !important;\n}\n\n.user-highlight::after,.char-highlight::after{\n  pointer-events:none !important;\n}\n\n/* Bottom action layout: Cancel + large Save, delete separated below. */\n.highlight-actions,.char-highlight-actions{\n  display:grid;\n  grid-template-columns:1fr 1.35fr;\n  gap:9px;\n  margin-top:14px;\n}\n\n.highlight-btn.cancel,.char-highlight-btn.cancel{\n  background:rgba(255,255,255,.82);\n  color:#565b63;\n}\n\n.highlight-btn.save,.char-highlight-btn.save{\n  min-width:0;\n  background:#111;\n  color:#fff;\n}\n\n.highlight-delete-row,.char-highlight-delete-row{\n  margin-top:8px;\n}\n\n.highlight-delete-row button,.char-highlight-delete-row button{\n  width:100%;\n  min-height:38px;\n  border:0;\n  border-radius:12px;\n  background:transparent;\n  color:#ff4d5f;\n  font-size:11px;\n  font-weight:800;\n  cursor:pointer;\n}\n\n/* Char Profile Reels reverse-read grid */\n.char-profile-cell.reel{\n  cursor:pointer;\n}\n.char-profile-cell.reel .char-reel-copy{\n  width:100%;\n  height:100%;\n  display:flex;\n  align-items:flex-end;\n  padding:8px;\n  background:linear-gradient(to top,rgba(0,0,0,.58),rgba(0,0,0,.03) 68%);\n  color:#fff;\n  font-size:9px;\n  line-height:1.35;\n  text-align:left;\n  overflow:hidden;\n}\n.char-profile-cell.reel:active{\n  transform:scale(.985);\n}\n\n\n/* ===== v1.15.15 People Registry + Card Relationship Network ===== */\n.pr-route-body{\n  position:absolute;\n  top:54px;\n  left:0;\n  right:0;\n  bottom:0;\n  overflow-y:auto;\n  background:#fff;\n  padding:12px 12px 28px;\n  scrollbar-width:none;\n}\n.pr-route-body::-webkit-scrollbar{display:none}\n.pr-tabs{\n  display:grid;\n  grid-template-columns:1fr 1fr;\n  gap:3px;\n  padding:3px;\n  border-radius:13px;\n  background:#f0f2f5;\n  margin-bottom:12px;\n}\n.pr-tab{\n  height:34px;\n  border:0;\n  border-radius:10px;\n  background:transparent;\n  color:#8b9098;\n  font-size:10px;\n  font-weight:900;\n}\n.pr-tab.active{\n  background:#fff;\n  color:#111;\n  box-shadow:0 2px 8px rgba(15,20,30,.06);\n}\n.pr-pane{display:none}\n.pr-pane.show{display:block}\n.pr-section-title{\n  margin:4px 2px 7px;\n  font-size:13px;\n  font-weight:900;\n  color:#16191e;\n}\n.pr-section-sub{\n  margin:0 2px 10px;\n  color:#8a9098;\n  font-size:9px;\n  line-height:1.5;\n}\n.pr-inline-system-rule{\n  display:block;\n  margin-top:5px;\n  color:#e43d4f;\n  font-weight:800;\n}\n.pr-actor-list{\n  display:grid;\n  gap:8px;\n}\n.pr-actor-card{\n  display:flex;\n  align-items:center;\n  gap:10px;\n  padding:11px;\n  border:1px solid #eceef2;\n  border-radius:17px;\n  background:#fff;\n}\n.pr-avatar{\n  width:43px;\n  height:43px;\n  border-radius:50%;\n  display:grid;\n  place-items:center;\n  flex:0 0 auto;\n  color:#fff;\n  font-size:13px;\n  font-weight:900;\n  background:#111;\n}\n.pr-avatar.roche{background:#657dff}\n.pr-avatar.npc{background:#2c3139}\n.pr-actor-main{\n  min-width:0;\n  flex:1;\n}\n.pr-actor-main strong{\n  display:block;\n  font-size:11px;\n  color:#15181d;\n}\n.pr-actor-main span{\n  display:block;\n  margin-top:3px;\n  color:#8a9098;\n  font-size:9px;\n  white-space:nowrap;\n  overflow:hidden;\n  text-overflow:ellipsis;\n}\n.pr-type{\n  padding:4px 7px;\n  border-radius:999px;\n  background:#f0f2f5;\n  color:#69707a;\n  font-size:8px;\n  font-weight:850;\n  white-space:nowrap;\n}\n.pr-type.roche{\n  background:#e9edff;\n  color:#536be1;\n}\n.pr-type.npc{\n  background:#edf0f2;\n  color:#4d535c;\n}\n.pr-view{\n  height:29px;\n  border:0;\n  border-radius:9px;\n  padding:0 9px;\n  background:#111;\n  color:#fff;\n  font-size:8px;\n  font-weight:850;\n}\n.pr-rule{\n  margin-top:10px;\n  padding:11px 12px;\n  border-radius:16px;\n  background:#111;\n  color:#fff;\n}\n.pr-rule strong{\n  display:block;\n  font-size:10px;\n}\n.pr-rule span{\n  display:block;\n  margin-top:4px;\n  color:#cbd0d6;\n  font-size:9px;\n  line-height:1.55;\n}\n.pr-focus-card{\n  padding:13px;\n  border:1px solid #edf0f3;\n  border-radius:20px;\n  background:linear-gradient(180deg,#f8f9fb,#f3f5f8);\n}\n.pr-focus-head{\n  display:flex;\n  align-items:center;\n  gap:11px;\n}\n.pr-focus-meta{\n  flex:1;\n  min-width:0;\n}\n.pr-focus-meta strong{\n  display:block;\n  font-size:16px;\n}\n.pr-focus-meta span{\n  display:block;\n  margin-top:3px;\n  color:#858b94;\n  font-size:9px;\n}\n.pr-focus-note{\n  margin-top:10px;\n  padding:9px 10px;\n  border-radius:13px;\n  background:#fff;\n  color:#6d737c;\n  font-size:9px;\n  line-height:1.5;\n}\n.pr-focus-switch{\n  display:flex;\n  gap:7px;\n  overflow-x:auto;\n  padding:10px 0 2px;\n  scrollbar-width:none;\n}\n.pr-focus-switch::-webkit-scrollbar{display:none}\n.pr-focus-chip{\n  height:30px;\n  border:0;\n  border-radius:999px;\n  padding:0 11px;\n  background:#f0f2f5;\n  color:#676d76;\n  font-size:9px;\n  font-weight:850;\n  white-space:nowrap;\n}\n.pr-focus-chip.active{\n  background:#111;\n  color:#fff;\n}\n.pr-actions{\n  display:grid;\n  grid-template-columns:1fr;\n  gap:7px;\n  margin:10px 0 12px;\n}\n.pr-action{\n  height:35px;\n  border:0;\n  border-radius:11px;\n  background:#111;\n  color:#fff;\n  font-size:9px;\n  font-weight:900;\n}\n.pr-rel-list{\n  display:grid;\n  gap:9px;\n}\n.pr-rel-card{\n  padding:11px;\n  border:1px solid #eceef2;\n  border-radius:17px;\n  background:#fff;\n}\n.pr-rel-head{\n  display:flex;\n  align-items:center;\n  gap:9px;\n}\n.pr-rel-head .pr-avatar{\n  width:39px;\n  height:39px;\n}\n.pr-rel-head-main{\n  min-width:0;\n  flex:1;\n}\n.pr-rel-head-main strong{\n  display:block;\n  font-size:11px;\n}\n.pr-rel-head-main span{\n  display:block;\n  margin-top:3px;\n  color:#8a9098;\n  font-size:9px;\n}\n.pr-lock{\n  padding:4px 6px;\n  border-radius:999px;\n  background:#eaf7ef;\n  color:#228251;\n  font-size:8px;\n  font-weight:850;\n}\n.pr-direction{\n  display:grid;\n  grid-template-columns:1fr 28px 1fr;\n  gap:7px;\n  align-items:center;\n  margin-top:10px;\n}\n.pr-direction-box{\n  min-height:53px;\n  padding:8px 9px;\n  border-radius:13px;\n  background:#f5f6f8;\n}\n.pr-direction-box b{\n  display:block;\n  color:#4f555e;\n  font-size:8px;\n}\n.pr-direction-box span{\n  display:block;\n  margin-top:4px;\n  color:#111;\n  font-size:9px;\n  line-height:1.4;\n}\n.pr-direction-mid{\n  width:28px;\n  height:28px;\n  border-radius:50%;\n  background:#111;\n  color:#fff;\n  display:grid;\n  place-items:center;\n  font-size:12px;\n}\n.pr-empty{\n  padding:18px 12px;\n  border-radius:16px;\n  background:#f5f6f8;\n  color:#858b94;\n  font-size:9px;\n  line-height:1.55;\n  text-align:center;\n}\n.pr-edit{\n  margin-top:9px;\n  width:100%;\n  height:31px;\n  border:0;\n  border-radius:10px;\n  background:#f0f2f5;\n  color:#555c65;\n  font-size:8px;\n  font-weight:850;\n}\n\n/* relation editor centered card */\n.pr-sheet-dim{\n  position:absolute;\n  inset:0;\n  background:rgba(15,18,22,.22);\n  opacity:0;\n  pointer-events:none;\n  transition:.18s;\n  z-index:318;\n}\n.pr-sheet-dim.show{\n  opacity:1;\n  pointer-events:auto;\n}\n.pr-sheet{\n  position:absolute;\n  left:50%;\n  right:auto;\n  top:50%;\n  bottom:auto;\n  width:min(390px,calc(100% - 28px));\n  max-height:min(620px,calc(100% - var(--ins-safe-top) - var(--ins-safe-bottom) - 48px));\n  overflow-y:auto;\n  padding:14px;\n  border-radius:24px;\n  background:rgba(250,251,252,.98);\n  backdrop-filter:blur(26px);\n  box-shadow:0 18px 48px rgba(20,24,30,.20);\n  transform:translate(-50%,-50%) scale(.985);\n  opacity:0;\n  pointer-events:none;\n  transition:.18s;\n  z-index:320;\n  scrollbar-width:none;\n  overscroll-behavior:contain;\n  -webkit-overflow-scrolling:touch;\n}\n.pr-sheet::-webkit-scrollbar{display:none}\n.pr-sheet.show{\n  transform:translate(-50%,-50%) scale(1);\n  opacity:1;\n  pointer-events:auto;\n}\n.pr-sheet-head{\n  display:flex;\n  align-items:center;\n  justify-content:space-between;\n  gap:10px;\n  margin-bottom:11px;\n}\n.pr-sheet-head strong{\n  font-size:14px;\n}\n.pr-sheet-close{\n  width:31px;\n  height:31px;\n  border:0;\n  border-radius:50%;\n  background:#fff;\n  font-size:18px;\n}\n.pr-field{\n  display:block;\n  margin-top:9px;\n}\n.pr-field span{\n  display:block;\n  margin-bottom:5px;\n  color:#666d76;\n  font-size:9px;\n  font-weight:850;\n}\n.pr-input,.pr-select{\n  width:100%;\n  min-height:38px;\n  border:1px solid #e8eaee;\n  border-radius:11px;\n  background:#fff;\n  color:#111;\n  padding:0 10px;\n  outline:none;\n  font-size:10px;\n}\n.pr-sheet-note{\n  margin-top:9px;\n  padding:9px 10px;\n  border-radius:12px;\n  background:#f0f2f5;\n  color:#7a8089;\n  font-size:8.5px;\n  line-height:1.5;\n}\n.pr-sheet-actions{\n  display:grid;\n  grid-template-columns:1fr 1.25fr;\n  gap:8px;\n  margin-top:11px;\n}\n.pr-sheet-actions button{\n  height:37px;\n  border:0;\n  border-radius:11px;\n  font-size:9px;\n  font-weight:900;\n}\n.pr-cancel{background:#eceef2;color:#555c65}\n.pr-save{background:#111;color:#fff}\n.pr-delete{\n  width:100%;\n  height:34px;\n  margin-top:7px;\n  border:0;\n  border-radius:11px;\n  background:transparent;\n  color:#ff4d5f;\n  font-size:9px;\n  font-weight:850;\n}\n\n/* DM source-of-truth notes */\n.dm-registry-note{\n  margin-top:9px;\n  padding:9px 10px;\n  border-radius:13px;\n  background:#f2f4f7;\n  color:#777d86;\n  font-size:9px;\n  line-height:1.5;\n}\n\n\n/* ===== v1.15.16 DM user-NPC / stranger quick hub ===== */\n.dm-user-hub-label{\n  margin:3px 2px 7px;\n  color:#1b1e23;\n  font-size:10px;\n  font-weight:900;\n}\n.dm-user-hub-sub{\n  margin:0 2px 8px;\n  color:#8a9098;\n  font-size:8.5px;\n  line-height:1.5;\n}\n.dm-user-inline-actions{\n  display:flex;\n  align-items:center;\n  gap:5px;\n}\n.dm-user-inline-actions .dm-list-contact-action{\n  min-width:42px;\n}\n.dm-user-hub-divider{\n  height:1px;\n  margin:11px 0;\n  background:#eceef2;\n}\n.dm-user-add-form{\n  display:none;\n  margin-top:9px;\n  padding:10px;\n  border-radius:15px;\n  background:#f4f6f8;\n}\n.dm-user-add-form.show{\n  display:block;\n}\n.dm-user-add-grid{\n  display:grid;\n  grid-template-columns:1fr 1fr;\n  gap:7px;\n}\n.dm-user-add-field{\n  display:block;\n  margin-top:7px;\n}\n.dm-user-add-field span{\n  display:block;\n  margin-bottom:4px;\n  color:#6d737c;\n  font-size:8px;\n  font-weight:850;\n}\n.dm-user-add-field input{\n  width:100%;\n  height:36px;\n  border:1px solid #e6e9ed;\n  border-radius:10px;\n  background:#fff;\n  padding:0 9px;\n  outline:none;\n  color:#111;\n  font-size:9px;\n}\n.dm-user-add-actions{\n  display:grid;\n  grid-template-columns:1fr 1.2fr;\n  gap:7px;\n  margin-top:9px;\n}\n.dm-user-add-actions button{\n  height:35px;\n  border:0;\n  border-radius:10px;\n  font-size:9px;\n  font-weight:900;\n}\n.dm-user-add-cancel{\n  background:#e9ebef;\n  color:#555c65;\n}\n.dm-user-add-save{\n  background:#111;\n  color:#fff;\n}\n.dm-user-quick-list{\n  display:grid;\n  gap:7px;\n  margin-top:7px;\n}\n.dm-user-temp-row{\n  display:flex;\n  align-items:center;\n  gap:9px;\n  padding:9px;\n  border-radius:13px;\n  background:#f5f6f8;\n}\n.dm-user-temp-avatar{\n  width:34px;\n  height:34px;\n  border-radius:50%;\n  display:grid;\n  place-items:center;\n  background:#c8ccd2;\n  color:#535961;\n  font-size:10px;\n  font-weight:900;\n  flex:0 0 auto;\n}\n.dm-user-temp-main{\n  min-width:0;\n  flex:1;\n}\n.dm-user-temp-main strong{\n  display:block;\n  color:#171a1f;\n  font-size:9px;\n}\n.dm-user-temp-main span{\n  display:block;\n  margin-top:3px;\n  color:#858b94;\n  font-size:8px;\n  white-space:nowrap;\n  overflow:hidden;\n  text-overflow:ellipsis;\n}\n.dm-user-temp-actions{\n  display:flex;\n  gap:5px;\n}\n.dm-user-temp-actions button{\n  height:28px;\n  border:0;\n  border-radius:8px;\n  padding:0 7px;\n  background:#e8eaee;\n  color:#555c65;\n  font-size:8px;\n  font-weight:850;\n}\n.dm-user-temp-actions button.primary{\n  background:#111;\n  color:#fff;\n}\n.dm-user-request-row{\n  display:flex;\n  align-items:center;\n  gap:9px;\n  padding:10px 9px;\n  border-radius:13px;\n  background:#f5f6f8;\n}\n.dm-user-request-badge{\n  min-width:26px;\n  height:26px;\n  padding:0 7px;\n  border-radius:999px;\n  display:grid;\n  place-items:center;\n  background:#111;\n  color:#fff;\n  font-size:8px;\n  font-weight:900;\n}\n.dm-user-request-main{\n  flex:1;\n  min-width:0;\n}\n.dm-user-request-main strong{\n  display:block;\n  font-size:9px;\n}\n.dm-user-request-main span{\n  display:block;\n  margin-top:3px;\n  color:#858b94;\n  font-size:8px;\n}\n.dm-user-request-open{\n  height:29px;\n  border:0;\n  border-radius:9px;\n  padding:0 9px;\n  background:#e8eaee;\n  color:#555c65;\n  font-size:8px;\n  font-weight:850;\n}\n\n\n/* ===== v1.15.17 DM NPC hub viewport fix ===== */\n#dmUserSocialCard{\n  top:68px;\n  bottom:78px;\n  max-height:none;\n  overflow-y:auto;\n  overscroll-behavior:contain;\n  -webkit-overflow-scrolling:touch;\n  padding-top:0;\n}\n#dmUserSocialCard .dm-list-glass-head{\n  position:sticky;\n  top:0;\n  z-index:5;\n  margin:0 -14px 10px;\n  padding:14px 14px 10px;\n  background:rgba(247,248,250,.94);\n  backdrop-filter:blur(28px) saturate(1.35);\n  -webkit-backdrop-filter:blur(28px) saturate(1.35);\n  border-bottom:1px solid rgba(225,228,233,.8);\n}\n#dmUserSocialCard .dm-list-glass-close{\n  flex:0 0 auto;\n  box-shadow:0 2px 10px rgba(15,20,30,.08);\n}\n\n/* ===== v1.15.17 INS social memory / knowledge ledger ===== */\n.ins-memory-body{\n  position:absolute;\n  top:54px;\n  left:0;\n  right:0;\n  bottom:0;\n  overflow-y:auto;\n  padding:12px 12px 28px;\n  background:#fff;\n  scrollbar-width:none;\n}\n.ins-memory-body::-webkit-scrollbar{display:none}\n.ins-memory-hero{\n  padding:13px;\n  border-radius:20px;\n  background:#111;\n  color:#fff;\n}\n.ins-memory-hero strong{\n  display:block;\n  font-size:13px;\n}\n.ins-memory-hero span{\n  display:block;\n  margin-top:6px;\n  color:#cbd0d6;\n  font-size:9px;\n  line-height:1.55;\n}\n.ins-memory-flow{\n  display:grid;\n  grid-template-columns:repeat(4,1fr);\n  gap:5px;\n  margin-top:10px;\n}\n.ins-memory-flow span{\n  margin:0;\n  padding:7px 5px;\n  border-radius:10px;\n  background:rgba(255,255,255,.1);\n  text-align:center;\n  color:#fff;\n  font-size:7.5px;\n  font-weight:850;\n  line-height:1.3;\n}\n.ins-memory-stat-grid{\n  display:grid;\n  grid-template-columns:repeat(3,1fr);\n  gap:7px;\n  margin-top:10px;\n}\n.ins-memory-stat{\n  padding:10px 7px;\n  border-radius:14px;\n  background:#f4f6f8;\n  text-align:center;\n}\n.ins-memory-stat strong{\n  display:block;\n  color:#111;\n  font-size:15px;\n}\n.ins-memory-stat span{\n  display:block;\n  margin-top:3px;\n  color:#888e97;\n  font-size:8px;\n}\n.ins-memory-title{\n  margin:15px 2px 7px;\n  color:#16191e;\n  font-size:12px;\n  font-weight:900;\n}\n.ins-memory-sub{\n  margin:0 2px 9px;\n  color:#858b94;\n  font-size:8.5px;\n  line-height:1.55;\n}\n.ins-memory-role-list,.ins-memory-event-list{\n  display:grid;\n  gap:8px;\n}\n.ins-memory-role{\n  display:flex;\n  align-items:center;\n  gap:9px;\n  padding:10px;\n  border:1px solid #eceef2;\n  border-radius:16px;\n  background:#fff;\n}\n.ins-memory-avatar{\n  width:39px;\n  height:39px;\n  border-radius:50%;\n  display:grid;\n  place-items:center;\n  flex:0 0 auto;\n  background:#657dff;\n  color:#fff;\n  font-size:12px;\n  font-weight:900;\n}\n.ins-memory-role-main{\n  flex:1;\n  min-width:0;\n}\n.ins-memory-role-main strong{\n  display:block;\n  font-size:10px;\n}\n.ins-memory-role-main span{\n  display:block;\n  margin-top:3px;\n  color:#888e97;\n  font-size:8px;\n  line-height:1.4;\n}\n.ins-memory-badge{\n  padding:5px 7px;\n  border-radius:999px;\n  background:#edf0f3;\n  color:#686f78;\n  font-size:8px;\n  font-weight:900;\n  white-space:nowrap;\n}\n.ins-memory-badge.pending{\n  background:#e9edff;\n  color:#526be0;\n}\n.ins-memory-preview-btn{\n  height:29px;\n  border:0;\n  border-radius:9px;\n  padding:0 9px;\n  background:#111;\n  color:#fff;\n  font-size:8px;\n  font-weight:900;\n}\n.ins-memory-event{\n  padding:10px;\n  border-radius:15px;\n  background:#f5f6f8;\n}\n.ins-memory-event-head{\n  display:flex;\n  align-items:center;\n  gap:7px;\n}\n.ins-memory-event-head strong{\n  flex:1;\n  min-width:0;\n  font-size:9px;\n}\n.ins-memory-event-head span{\n  padding:4px 6px;\n  border-radius:999px;\n  background:#fff;\n  color:#717780;\n  font-size:7.5px;\n  font-weight:850;\n}\n.ins-memory-event-copy{\n  margin-top:5px;\n  color:#555c65;\n  font-size:8.5px;\n  line-height:1.5;\n}\n.ins-memory-event-known{\n  margin-top:5px;\n  color:#90959d;\n  font-size:7.5px;\n  line-height:1.4;\n}\n.ins-memory-rule{\n  margin-top:10px;\n  padding:10px 11px;\n  border-radius:14px;\n  background:#eef1f5;\n  color:#737982;\n  font-size:8.5px;\n  line-height:1.55;\n}\n.ins-memory-rule strong{\n  color:#20242a;\n}\n.ins-memory-sheet-dim{\n  position:absolute;\n  inset:0;\n  z-index:152;\n  background:rgba(15,18,22,.22);\n  opacity:0;\n  pointer-events:none;\n  transition:.18s;\n}\n.ins-memory-sheet-dim.show{\n  opacity:1;\n  pointer-events:auto;\n}\n.ins-memory-sheet{\n  position:absolute;\n  left:9px;\n  right:9px;\n  bottom:9px;\n  z-index:155;\n  max-height:78%;\n  overflow-y:auto;\n  padding:14px;\n  border-radius:24px;\n  background:rgba(250,251,252,.98);\n  backdrop-filter:blur(28px);\n  -webkit-backdrop-filter:blur(28px);\n  box-shadow:0 -12px 40px rgba(20,24,30,.18);\n  transform:translateY(115%);\n  transition:.2s;\n  scrollbar-width:none;\n}\n.ins-memory-sheet::-webkit-scrollbar{display:none}\n.ins-memory-sheet.show{transform:translateY(0)}\n.ins-memory-sheet-head{\n  position:sticky;\n  top:-14px;\n  z-index:2;\n  display:flex;\n  align-items:center;\n  justify-content:space-between;\n  margin:-14px -14px 10px;\n  padding:14px 14px 10px;\n  background:rgba(250,251,252,.96);\n  border-bottom:1px solid #eceef2;\n}\n.ins-memory-sheet-head strong{\n  font-size:14px;\n}\n.ins-memory-sheet-close{\n  width:31px;\n  height:31px;\n  border:0;\n  border-radius:50%;\n  background:#fff;\n  font-size:18px;\n}\n.ins-memory-sync-meta{\n  padding:9px 10px;\n  border-radius:13px;\n  background:#f1f3f6;\n  color:#747b84;\n  font-size:8.5px;\n  line-height:1.5;\n}\n.ins-memory-preview{\n  margin-top:9px;\n  padding:11px;\n  border-radius:14px;\n  background:#111318;\n  color:#f4f5f6;\n  white-space:pre-wrap;\n  word-break:break-word;\n  font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;\n  font-size:8.5px;\n  line-height:1.6;\n}\n.ins-memory-disabled{\n  width:100%;\n  height:36px;\n  margin-top:9px;\n  border:0;\n  border-radius:11px;\n  background:#e8eaee;\n  color:#8b9098;\n  font-size:9px;\n  font-weight:900;\n}\n\n.ins-memory-role-actions{\n  display:flex;\n  gap:5px;\n  align-items:center;\n}\n.ins-memory-source-btn{\n  height:29px;\n  border:0;\n  border-radius:9px;\n  padding:0 9px;\n  background:#eef0f3;\n  color:#444b54;\n  font-size:8px;\n  font-weight:900;\n}\n.ins-memory-source-list{\n  display:grid;\n  gap:8px;\n  margin-top:9px;\n}\n.ins-memory-discovery-row{\n  margin-top:9px;\n  display:flex;\n  gap:10px;\n  align-items:center;\n  padding:10px 11px;\n  border:1px solid #e7e9ee;\n  border-radius:14px;\n  background:#fff;\n}\n.ins-memory-discovery-row>div{flex:1;min-width:0}\n.ins-memory-discovery-row strong{display:block;font-size:9px;color:#20242a}\n.ins-memory-discovery-row span{display:block;margin-top:3px;font-size:7.5px;line-height:1.45;color:#858b94}\n.ins-memory-discovery-row select{height:31px;border:1px solid #e2e5ea;border-radius:10px;background:#f7f8fa;color:#222;font-size:8.5px;padding:0 7px;outline:none}\n.ins-memory-source{\n  padding:10px;\n  border:1px solid #e7e9ee;\n  border-radius:15px;\n  background:#fff;\n}\n.ins-memory-source-head{\n  display:flex;\n  align-items:center;\n  gap:8px;\n}\n.ins-memory-source-main{\n  flex:1;\n  min-width:0;\n}\n.ins-memory-source-main strong{\n  display:block;\n  font-size:10px;\n}\n.ins-memory-source-main span{\n  display:block;\n  margin-top:3px;\n  color:#858b94;\n  font-size:8px;\n}\n.ins-memory-source-toggle{\n  width:34px;\n  height:20px;\n  border:0;\n  border-radius:999px;\n  padding:2px;\n  background:#d9dde3;\n}\n.ins-memory-source-toggle i{\n  display:block;\n  width:16px;\n  height:16px;\n  border-radius:50%;\n  background:#fff;\n  box-shadow:0 1px 3px rgba(0,0,0,.15);\n  transition:.18s;\n}\n.ins-memory-source-toggle.on{background:#111}\n.ins-memory-source-toggle.on i{transform:translateX(14px)}\n.ins-memory-source-opts{\n  display:grid;\n  grid-template-columns:1fr 1fr auto;\n  gap:7px;\n  align-items:end;\n  margin-top:9px;\n  padding-top:9px;\n  border-top:1px solid #f0f1f3;\n}\n.ins-memory-source-opts label{\n  display:grid;\n  gap:4px;\n  color:#818790;\n  font-size:7.5px;\n  font-weight:800;\n}\n.ins-memory-source-opts input[type=\"number\"]{\n  width:100%;\n  height:30px;\n  border:1px solid #e3e6eb;\n  border-radius:9px;\n  padding:0 8px;\n  outline:none;\n  font:inherit;\n  font-size:9px;\n  color:#20242a;\n  background:#f8f9fa;\n}\n.ins-memory-core-toggle{\n  display:flex!important;\n  align-items:center;\n  justify-content:center;\n  gap:5px!important;\n  min-width:54px;\n  height:30px;\n  padding:0 8px;\n  border-radius:9px;\n  background:#f1f3f6;\n  color:#454b53!important;\n}\n.ins-memory-core-toggle input{margin:0}\n.ins-memory-writeback{\n  margin-top:10px;\n  padding:10px 11px;\n  border-radius:14px;\n  background:#eef1f5;\n  color:#6f7680;\n  font-size:8.5px;\n  line-height:1.55;\n}\n.ins-memory-writeback strong{\n  display:block;\n  margin-bottom:4px;\n  color:#20242a;\n  font-size:9px;\n}\n.ins-memory-source-refresh{\n  width:100%;\n  height:36px;\n  margin-top:9px;\n  border:0;\n  border-radius:11px;\n  background:#111;\n  color:#fff;\n  font-size:9px;\n  font-weight:900;\n}\n.ins-memory-source-refresh:disabled{\n  background:#e8eaee;\n  color:#8b9098;\n}\n.ins-memory-empty-source{\n  padding:13px;\n  border-radius:14px;\n  background:#f4f6f8;\n  color:#7d838c;\n  font-size:8.5px;\n  line-height:1.55;\n}\n\n\n/* ===== v1.15.19 screenshot polish ===== */\n/* DM timeline: cards and bubbles share one vertical rhythm. */\n.dm-chat-messages{\n  gap:11px;\n}\n.dm-chat-messages > .dm-sent-row{\n  width:100%;\n  flex:0 0 auto;\n}\n#dmSharedCardsSlot{\n  display:none;\n}\n\n/* User profile editor must stay inside the phone viewport. */\n.profile-edit-card{\n  top:14px;\n  bottom:80px;\n  max-height:none;\n  overflow-y:auto;\n  overscroll-behavior:contain;\n  -webkit-overflow-scrolling:touch;\n  scrollbar-width:none;\n  padding-top:0;\n}\n.profile-edit-card::-webkit-scrollbar{display:none}\n.profile-edit-card .profile-edit-title{\n  position:sticky;\n  top:0;\n  z-index:5;\n  margin:0 -14px 12px;\n  padding:14px 14px 10px;\n  background:rgba(247,248,250,.95);\n  backdrop-filter:blur(28px) saturate(1.45);\n  -webkit-backdrop-filter:blur(28px) saturate(1.45);\n  border-bottom:1px solid rgba(225,228,233,.78);\n}\n\n/* The user NPC / stranger quick hub keeps touch scrolling without a heavy native rail. */\n#dmUserSocialCard{\n  scrollbar-width:none;\n}\n#dmUserSocialCard::-webkit-scrollbar{\n  width:0;\n  height:0;\n  display:none;\n}\n\n\n/* ===== v5.0.6 interaction polish ===== */\n.comment-author-line,.reels-v15-comment-name{\n  display:flex;align-items:baseline;gap:6px;flex-wrap:wrap;\n}\n.comment-handle,.reels-v15-comment-handle{\n  color:#8b8f96;font-size:11px;font-weight:500;\n}\n.comment-mini-btn.delete,.reels-v15-comment-delete{\n  color:#ff3b30;\n}\n.reels-v15-comment-refresh{\n  border:0;background:#f0f1f4;color:#111;border-radius:999px;\n  min-height:34px;padding:0 12px;font-size:12px;font-weight:800;cursor:pointer;\n}\n.reels-v15-comment-refresh:disabled{opacity:.5;cursor:default}\n.user-cell.repost-linked{cursor:pointer}\n.user-cell.repost-linked::before{\n  content:attr(data-link-label);\n  position:absolute;left:6px;bottom:6px;z-index:3;\n  padding:3px 6px;border-radius:999px;\n  background:rgba(0,0,0,.62);color:#fff;font-size:9px;font-weight:800;\n}\n.user-repost-preview-dim{\n  position:absolute;inset:0;z-index:1200;display:none;\n  align-items:center;justify-content:center;padding:20px;\n  background:rgba(0,0,0,.26);\n}\n.user-repost-preview-dim.show{display:flex}\n.user-repost-preview-card{\n  width:min(100%,390px);max-height:82%;\n  overflow:auto;background:rgba(255,255,255,.96);\n  border:1px solid rgba(0,0,0,.08);border-radius:24px;\n  box-shadow:0 18px 50px rgba(0,0,0,.18);\n  backdrop-filter:blur(22px);-webkit-backdrop-filter:blur(22px);\n}\n.user-repost-preview-head{\n  min-height:58px;display:flex;align-items:center;gap:10px;padding:10px 14px;\n  border-bottom:1px solid #eceef2;\n}\n.user-repost-preview-avatar{\n  width:36px;height:36px;border-radius:50%;background:#eceef2;\n  display:grid;place-items:center;overflow:hidden;font-weight:800;flex:0 0 36px;\n}\n.user-repost-preview-name{min-width:0;flex:1}\n.user-repost-preview-name strong{display:block;font-size:14px}\n.user-repost-preview-name span{display:block;font-size:11px;color:#8b8f96;margin-top:2px}\n.user-repost-preview-close{\n  width:34px;height:34px;border:0;background:#f1f2f5;border-radius:50%;\n  font-size:22px;line-height:1;cursor:pointer;\n}\n.user-repost-preview-media{\n  width:100%;aspect-ratio:1/1;background:#111;color:#fff;\n  display:flex;align-items:center;justify-content:center;text-align:center;\n  overflow:hidden;padding:24px;font-size:13px;line-height:1.6;\n}\n.user-repost-preview-media img{width:100%;height:100%;object-fit:cover;display:block}\n.user-repost-preview-body{padding:12px 14px 16px}\n.user-repost-preview-caption{font-size:13px;line-height:1.55;color:#111}\n.user-repost-preview-metrics{\n  display:flex;gap:14px;flex-wrap:wrap;margin-top:10px;color:#6f737a;font-size:11px;\n}\n\n\n/* ===== INS v5.0.7 interaction + profile detail ===== */\n/* Char Profile tabs: the full 1/4 cell is the hit target. */\n#routeCharProfile .char-highlight-strip{\n  position:relative;\n  z-index:4!important;\n}\n#routeCharProfile .char-profile-tabs{\n  position:sticky;\n  z-index:30!important;\n  isolation:isolate;\n  overflow:visible;\n}\n#routeCharProfile .char-tab{\n  width:100%!important;\n  height:52px!important;\n  min-height:52px!important;\n  align-self:stretch;\n  justify-self:stretch;\n  position:relative;\n  z-index:2;\n  cursor:pointer!important;\n  touch-action:manipulation;\n  -webkit-tap-highlight-color:transparent;\n}\n#routeCharProfile .char-tab > *{\n  pointer-events:none!important;\n}\n#routeCharProfile .char-tab::before{\n  content:\"\";\n  position:absolute;\n  inset:0;\n  z-index:0;\n}\n\n/* Reels comment reroll: same small circular language as Feed refresh. */\n.reels-v15-comment-refresh{\n  width:30px!important;\n  height:30px!important;\n  min-height:30px!important;\n  padding:0!important;\n  margin-left:auto;\n  border:0!important;\n  border-radius:50%!important;\n  display:grid!important;\n  place-items:center!important;\n  background:rgba(255,255,255,.10)!important;\n  color:#fff!important;\n}\n.reels-v15-comment-refresh svg{\n  width:17px;height:17px;fill:none;stroke:#fff;stroke-width:1.9;\n  stroke-linecap:round;stroke-linejoin:round;\n}\n.reels-v15-comment-refresh.loading svg{animation:none}\n@keyframes niniRerollSpin{to{transform:rotate(360deg)}}\n.nini-refresh-dots{\n  display:inline-flex;\n  align-items:center;\n  justify-content:center;\n  gap:3px;\n  width:20px;\n  height:18px;\n}\n.nini-refresh-dots i{\n  width:4px;\n  height:4px;\n  border-radius:50%;\n  background:currentColor;\n  display:block;\n  animation:niniDotBounce .72s ease-in-out infinite;\n}\n.nini-refresh-dots i:nth-child(2){animation-delay:.12s}\n.nini-refresh-dots i:nth-child(3){animation-delay:.24s}\n@keyframes niniDotBounce{\n  0%,60%,100%{transform:translateY(0);opacity:.45}\n  30%{transform:translateY(-3px);opacity:1}\n}\n.comment-refresh.nini-busy,.char-board-refresh.nini-busy{\n  color:#111;\n  transform:none!important;\n}\n.reels-v15-comment-refresh.nini-busy{\n  color:#fff!important;\n  transform:none!important;\n}\n\n/* Repost/Profile grids stay true 1:1. */\n.user-grid .user-cell,.char-profile-grid .char-profile-cell{\n  aspect-ratio:1/1!important;\n  min-width:0;\n  min-height:0;\n}\n.user-grid .user-cell img,.char-profile-grid .char-profile-cell img{\n  width:100%;height:100%;object-fit:cover;display:block;\n}\n.user-cell.repost-linked{\n  padding:0!important;\n  cursor:pointer;\n}\n.user-cell.repost-linked .repost-reel-card{\n  width:100%;height:100%;padding:10px;\n  display:flex;align-items:center;justify-content:center;\n  text-align:center;background:#111;color:#fff;\n  font-size:11px;line-height:1.4;\n}\n\n/* Full Profile Content Detail route. */\n.profile-content-detail-route{\n  position:absolute;inset:0;z-index:105;\n  display:none;flex-direction:column;\n  background:#fff;color:#111;\n}\n.profile-content-detail-route.show{display:flex}\n.profile-content-detail-route{overflow:hidden;contain:layout paint;overscroll-behavior:none}\n.pcd-head{\n  height:58px;flex:0 0 58px;\n  display:grid;grid-template-columns:42px 1fr 42px;\n  align-items:center;border-bottom:1px solid #eceef2;background:#fff;\n}\n.pcd-back{\n  width:42px;height:42px;border:0;background:none;font-size:32px;line-height:1;\n  display:grid;place-items:center;cursor:pointer;\n}\n.pcd-title{text-align:center;min-width:0}\n.pcd-title strong{display:block;font-size:14px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}\n.pcd-title span{display:block;margin-top:1px;font-size:10px;color:#8c9097}\n.pcd-scroll{flex:1;min-height:0;overflow-y:auto;background:#f6f7f9;scrollbar-width:none;overscroll-behavior:contain;-webkit-overflow-scrolling:touch}\n.pcd-scroll::-webkit-scrollbar{display:none}\n.pcd-card{background:#fff;margin-bottom:8px;scroll-margin-top:8px}\n.pcd-card-head{height:54px;display:flex;align-items:center;gap:10px;padding:8px 12px}\n.pcd-avatar{width:36px;height:36px;flex:0 0 36px;border-radius:50%;background:#eceef2;display:grid;place-items:center;overflow:hidden;font-weight:800}\n.pcd-author{min-width:0;flex:1}\n.pcd-author strong{display:block;font-size:13px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}\n.pcd-author span{display:block;font-size:10px;color:#8a8e95;margin-top:2px}\n.pcd-media{width:100%;background:#111;color:#fff;display:flex;align-items:center;justify-content:center;text-align:center;overflow:hidden}\n.pcd-media.feed{aspect-ratio:1/1;padding:0;font-size:12px;line-height:1.55}\n.pcd-media.feed.text-only{padding:20px}\n.pcd-media.reel{aspect-ratio:9/16;max-height:62vh;padding:24px;font-size:13px;line-height:1.65}\n.pcd-media img{width:100%;height:100%;object-fit:cover;display:block}\n.pcd-body{padding:10px 12px 15px}\n.pcd-actions{display:flex;align-items:center;gap:15px;margin-bottom:8px;color:#555b63;font-size:11px}\n.pcd-action{border:0;background:none;padding:2px 0;color:inherit;font:inherit;cursor:pointer;font-weight:700}\n.pcd-caption{font-size:12px;line-height:1.55;color:#222;word-break:break-word}\n.pcd-caption strong{margin-right:5px}\n.pcd-selected{box-shadow:inset 0 0 0 2px rgba(17,17,17,.10)}\n\n/* Highlight archive viewer: independent from live Story. */\n.highlight-viewer{\n  position:absolute;inset:0;z-index:205;display:none;flex-direction:column;\n  background:#000;color:#fff;\n}\n.highlight-viewer.show{display:flex}\n.highlight-viewer-top{\n  position:absolute;top:0;left:0;right:0;z-index:5;\n  display:flex;align-items:center;gap:10px;padding:12px 12px 8px;\n  background:linear-gradient(to bottom,rgba(0,0,0,.68),rgba(0,0,0,0));\n}\n.highlight-viewer-progress{position:absolute;top:7px;left:10px;right:10px;display:flex;gap:3px;z-index:7}\n.highlight-viewer-progress span{height:2px;flex:1;border-radius:99px;background:rgba(255,255,255,.28)}\n.highlight-viewer-progress span.on{background:#fff}\n.highlight-viewer-avatar{width:34px;height:34px;border-radius:50%;background:#e5e6ea;color:#444;display:grid;place-items:center;overflow:hidden;font-weight:800}\n.highlight-viewer-title{min-width:0;flex:1}\n.highlight-viewer-title strong{display:block;font-size:13px}\n.highlight-viewer-title span{display:block;font-size:10px;color:rgba(255,255,255,.72);margin-top:2px}\n.highlight-viewer-btn{width:34px;height:34px;border:0;border-radius:50%;background:rgba(255,255,255,.12);color:#fff;display:grid;place-items:center;cursor:pointer;font-size:18px}\n.highlight-viewer-stage{\n  flex:1;min-height:0;position:relative;overflow-y:auto;overflow-x:hidden;\n  padding:0;background:#000;text-align:center;\n  scroll-snap-type:y mandatory;overscroll-behavior-y:contain;\n  scrollbar-width:none;-webkit-overflow-scrolling:touch;touch-action:pan-y;\n}\n.highlight-viewer-stage::-webkit-scrollbar{display:none}\n.highlight-viewer-slide{\n  position:relative;width:100%;height:100%;min-height:100%;\n  display:flex;align-items:center;justify-content:center;\n  scroll-snap-align:start;scroll-snap-stop:always;\n  overflow:hidden;background:#000;color:#fff;\n}\n.highlight-viewer-slide img{\n  width:100%;height:100%;object-fit:contain;background:#000;display:block;\n}\n.highlight-viewer-slide .highlight-viewer-desc{\n  width:min(78%,320px);padding:20px;border-radius:22px;\n  background:rgba(255,255,255,.06);border:1px solid rgba(255,255,255,.12);\n  white-space:pre-wrap;line-height:1.6;\n}\n.highlight-viewer-desc{position:relative;z-index:2;max-width:90%;font-size:14px;line-height:1.7;white-space:pre-wrap}\n.highlight-viewer-caption{position:absolute;left:18px;right:18px;bottom:30px;z-index:4;text-align:center;font-size:13px;line-height:1.55;text-shadow:0 1px 5px rgba(0,0,0,.8)}\n.highlight-viewer-nav{display:none!important}\n.highlight-viewer-nav button{border:0;background:transparent;cursor:pointer;color:transparent}\n\n/* ===== INS v5.0.8 layout + interaction repair ===== */\n/* v5.0.10: user Story hard-delete, refresh action UI, stranger 5-comment/no-tag/locked-follow rules. */\n/* v5.0.9: guestbook empty-state, Highlight viewer semantics, and profile-detail viewport containment. */\n/* The iframe already owns the app viewport. Keep the phone shell exactly inside it,\n   so Roche host clipping cannot expose an oversized 100vh child shifted upward. */\n.nini-ins-root,.nini-ins-root{\n  width:100%;\n  height:100%;\n  min-height:0;\n  overflow:hidden;\n}\n.nini-ins-root{\n  overscroll-behavior:none;\n}\n.phone{\n  width:100%;\n  height:100%;\n  min-width:0;\n  min-height:0;\n  max-width:none;\n  max-height:none;\n  margin:0;\n  top:0;\n  transform:none;\n}\n.screen,#page-profile,.user-profile-page,.char-profile-page,.profile-content-detail-route,.route-page{\n  min-height:0;\n}\n#page-profile,.user-profile-page,.profile-content-detail-route,.route-page{\n  width:100%;\n  height:100%;\n}\n.user-profile-scroll,.char-profile-scroll,.pcd-scroll,.route-body{\n  min-height:0;\n}\n\n/* User profile detail really occupies the whole phone viewport. */\n.profile-content-detail-route{\n  inset:0!important;\n  width:100%!important;\n  height:100%!important;\n  max-height:none!important;\n}\n\n/* Char Profile: keep Highlights below the tabs and make every quarter-cell a real hit target. */\n#routeCharProfile .char-highlight-strip{\n  z-index:3!important;\n}\n#routeCharProfile .char-profile-tabs{\n  z-index:50!important;\n  pointer-events:auto!important;\n}\n#routeCharProfile .char-tab{\n  pointer-events:auto!important;\n  user-select:none;\n  -webkit-user-select:none;\n}\n#routeCharProfile .char-profile-tabs::before{\n  content:\"\";\n  position:absolute;\n  inset:0;\n  z-index:-1;\n  background:#fff;\n}\n\n/* Highlight text fields must stay above glass/dim layers and remain focusable in WebView. */\n#highlightDescriptionInput,#charHighlightDescriptionInput{\n  position:relative;\n  z-index:8;\n  pointer-events:none!important;\n  user-select:text!important;\n  -webkit-user-select:text!important;\n  touch-action:auto!important;\n  opacity:1!important;\n}\n#highlightEditorCard.show #highlightDescriptionInput,#charHighlightEditor.show #charHighlightDescriptionInput{\n  pointer-events:auto!important;\n}\n.highlight-purpose-card{\n  position:relative;\n  z-index:7;\n}\n\n/* Keep Highlight viewer controls visible after the global viewport repair. */\n.highlight-viewer-top{\n  top:0;\n  padding-top:max(12px,env(safe-area-inset-top));\n}\n\n\n/* ===== v5.1 Album Matcher V3 ===== */\n.char-album-switch-row{display:flex;align-items:center;justify-content:space-between;gap:12px;padding:11px 12px;border-radius:14px;background:#f5f6f8;margin-top:8px}\n.char-album-switch-copy{min-width:0}.char-album-switch-copy strong{display:block;font-size:10px;color:#25282d}.char-album-switch-copy span{display:block;margin-top:4px;font-size:8px;line-height:1.45;color:#878c94}\n.char-album-switch{position:relative;width:42px;height:24px;flex:0 0 42px}.char-album-switch input{position:absolute;opacity:0;pointer-events:none}.char-album-switch i{display:block;width:42px;height:24px;border-radius:999px;background:#d6d9de;transition:.2s;position:relative}.char-album-switch i:after{content:\"\";position:absolute;left:3px;top:3px;width:18px;height:18px;border-radius:50%;background:#fff;box-shadow:0 1px 4px rgba(0,0,0,.18);transition:.2s}.char-album-switch input:checked+i{background:#111}.char-album-switch input:checked+i:after{transform:translateX(18px)}\n.char-album-collapse[hidden]{display:none}.char-album-collapse{margin-top:10px}\n.char-album-rate-row{display:grid;grid-template-columns:repeat(4,1fr);gap:7px;margin-top:10px}.char-album-rate{border:1px solid #e1e4e9;background:#fff;border-radius:12px;padding:9px 4px;font-size:9px;font-weight:800;color:#5f646d;cursor:pointer}.char-album-rate.active{background:#111;color:#fff;border-color:#111}\n.char-album-category-title{font-size:9px;font-weight:800;color:#5d626a;margin:10px 0 7px}.char-album-category-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:7px}.char-album-category-chip{border:1px solid #dfe2e7;border-radius:11px;background:#fff;padding:8px 4px;font-size:8px;font-weight:800;color:#626770;cursor:pointer}.char-album-category-chip.active{background:#111;color:#fff;border-color:#111}\n.char-album-era-asset-wrap{margin-top:9px;padding:10px;border-radius:13px;background:#f5f6f8}.char-album-era-asset-wrap[hidden]{display:none}.char-album-era-asset-label{font-size:8px;line-height:1.45;color:#777d86;margin-bottom:7px}.char-album-era-select-row{display:flex;gap:6px;flex-wrap:wrap}.char-album-era-select-chip{border:1px solid #d9dde3;background:#fff;border-radius:999px;padding:6px 9px;font-size:8px;color:#666b73;cursor:pointer}.char-album-era-select-chip.active{background:#111;color:#fff;border-color:#111}\n.char-album-library-item .album-meta{left:4px;right:4px;bottom:4px;padding:3px 4px;border-radius:8px;background:rgba(17,17,17,.68);color:#fff;font-size:7px;line-height:1.2;white-space:normal;display:-webkit-box;-webkit-line-clamp:2;-webkit-box-orient:vertical;overflow:hidden}\n\n\n\n\n/* ===== v1.07 requested Story / Highlight UI only ===== */\n.story-create-bottom{z-index:12;pointer-events:auto;}\n.story-create-publish{height:50px;pointer-events:auto;touch-action:manipulation;}\n.highlight-editor-card{top:76px!important;bottom:76px!important;display:flex!important;flex-direction:column;overflow:hidden!important;padding:12px!important;}\n.highlight-editor-title{flex:0 0 auto;margin-bottom:8px;}\n.highlight-editor-scroll{flex:1 1 auto;min-height:0;overflow-y:auto;overscroll-behavior:contain;scrollbar-width:none;padding:0 2px 4px;}\n.highlight-editor-scroll::-webkit-scrollbar{display:none;}\n.highlight-actions,.highlight-delete-row{flex:0 0 auto;}\n.highlight-actions{margin-top:8px;}\n.highlight-delete-row{margin-top:5px;}\n.char-highlight-editor{top:76px!important;bottom:76px!important;display:flex!important;flex-direction:column;overflow:hidden!important;padding:12px!important;}\n.char-highlight-editor-title{flex:0 0 auto;margin-bottom:8px;}\n.char-highlight-editor-scroll{flex:1 1 auto;min-height:0;overflow-y:auto;overscroll-behavior:contain;scrollbar-width:none;padding:0 2px 4px;}\n.char-highlight-editor-scroll::-webkit-scrollbar{display:none;}\n.char-highlight-actions,.char-highlight-delete-row{flex:0 0 auto;}\n.char-highlight-actions{margin-top:8px;}\n.char-highlight-delete-row{margin-top:5px;}\n";
  const DIRECT_BODY_HTML = "\n<div class=\"phone\">\n<div class=\"screen light\" id=\"screen\">\n<section class=\"page active\" id=\"page-home\">\n<div class=\"topbar\">\n<img alt=\"Instagram\" class=\"logo\" id=\"instagramLogo\" src=\"https://imgbed.heliar.top/i/EQRjc2xSMxCNogzA_Instagram.webp\"/>\n<div class=\"top-actions\">\n<svg id=\"homeRefreshBtn\" style=\"cursor:pointer\" viewbox=\"0 0 24 24\"><path d=\"M20 6v5h-5\"></path><path d=\"M19 11a7 7 0 1 0 1 5\"></path></svg>\n<svg id=\"homeNotificationsBtn\" style=\"cursor:pointer\" viewbox=\"0 0 24 24\"><path d=\"M12 21s-8-4.5-8-11a4.5 4.5 0 0 1 8-2.9A4.5 4.5 0 0 1 20 10c0 6.5-8 11-8 11z\"></path></svg>\n<span class=\"feed-notice-dot\" id=\"feedNoticeDot\"></span>\n</div>\n</div>\n<div class=\"stories-wrap\">\n<div class=\"stories\">\n<div class=\"story user\">\n<div class=\"story-wrap\" data-story-owner=\"user\">\n<div class=\"story-ring\"><div class=\"story-inner\"><img alt=\"\" src=\"https://imgbed.heliar.top/i/RLwxO3YcIyV5qmm3\"/></div></div>\n<img alt=\"发限动\" class=\"story-add\" id=\"userStoryAddBtn\" src=\"https://imgbed.heliar.top/i/Xig3Vm87Atd7WyZL_copy_副本_pixian_ai (1)_副本.webp\"/>\n</div>\n<div class=\"story-name\">Story</div>\n</div>\n\n\n\n</div>\n</div>\n<div class=\"feed\" id=\"feed\"></div>\n</section>\n<section class=\"page\" id=\"page-reels\">\n<header class=\"reels-v15-top\">\n<div class=\"reels-v15-top-left\">\n<button aria-label=\"发布 Reel\" class=\"reels-v15-topbtn plus\" id=\"reelsV15UserPostBtn\" type=\"button\">＋</button>\n<div class=\"reels-v15-title\">Reels</div>\n</div>\n<div class=\"reels-v15-top-right\">\n<button aria-label=\"生成新的 Reels\" class=\"reels-v15-topbtn\" id=\"reelsV15GenerateBtn\" type=\"button\">\n<svg viewbox=\"0 0 24 24\">\n<path d=\"M20 6v5h-5\"></path>\n<path d=\"M19 11a7 7 0 1 0 1 5\"></path>\n</svg>\n</button>\n</div>\n</header>\n<div class=\"reels-v15-scroller\" id=\"reelsV15Scroller\"></div>\n</section>\n<section class=\"page\" id=\"page-dm\">\n<div class=\"dm-v2-page\">\n<div class=\"dm-v2-head\">\n<div class=\"dm-v2-id\">user.ins</div>\n<button aria-label=\"社交联系人\" class=\"dm-v2-social\" id=\"dmSocialSearchBtn\" type=\"button\">\n<svg viewbox=\"0 0 24 24\">\n<circle cx=\"9\" cy=\"8\" r=\"3\"></circle>\n<path d=\"M4.5 18c.7-3.2 2.6-5 4.5-5s3.8 1.8 4.5 5\"></path>\n<path d=\"M18 6v6M15 9h6\"></path>\n</svg>\n</button>\n</div>\n<div class=\"dm-v2-search-wrap\">\n<input aria-readonly=\"true\" class=\"dm-v2-search\" id=\"dmSearchInput\" inputmode=\"none\" placeholder=\"搜索消息\" readonly=\"\" tabindex=\"-1\"/>\n</div>\n<div class=\"dm-avatar-strip\">\n<div class=\"dm-profile-item\" id=\"dmUserNoteItem\">\n<div class=\"dm-profile-ring user-ring\" style='background-image:url(\"https://imgbed.heliar.top/i/RLwxO3YcIyV5qmm3\");background-size:cover;background-position:center'>\n<span class=\"dm-note-bubble\" id=\"dmUserNoteBubble\">在干嘛呢</span>\n</div>\n<div class=\"dm-profile-name\">你</div>\n</div>\n\n\n<div class=\"dm-profile-item\" id=\"dmImportManageBtn\">\n<div class=\"dm-profile-plus\">＋</div>\n<div class=\"dm-profile-name\">绑定</div>\n</div>\n</div>\n<div class=\"dm-section-head\">\n<div class=\"dm-section-title\">消息</div>\n<button class=\"dm-section-edit\" id=\"dmEditListBtn\" type=\"button\">编辑</button>\n</div>\n<div class=\"dm-v2-list\" id=\"dmV2List\">\n\n\n\n<div class=\"dm-v2-row\" data-kind=\"stranger\" data-search=\"陌生消息 请求\" id=\"dmStrangerRequests\">\n<div class=\"dm-v2-avatar\">?</div>\n<div class=\"dm-v2-main\">\n<div class=\"dm-v2-topline\">\n<div class=\"dm-v2-name\">陌生消息</div>\n<div class=\"dm-v2-badge\">0</div>\n</div>\n<div class=\"dm-v2-preview\">暂无请求 · 满足条件后可手动刷新生成</div>\n</div>\n</div>\n</div>\n<div class=\"dm-edit-bar\" id=\"dmEditBar\">\n<div class=\"dm-edit-count\" id=\"dmEditCount\">已选 0</div>\n<button class=\"dm-edit-delete\" disabled=\"\" id=\"dmDeleteSelectedBtn\" type=\"button\">删除选中的对话</button>\n</div>\n</div>\n</section>\n<section class=\"page\" id=\"page-profile\">\n<div class=\"user-profile-page\">\n<div class=\"user-profile-top\">\n<div class=\"user-profile-id\" id=\"userProfileTopId\">user.ins</div>\n<button aria-label=\"用户主页设置\" class=\"user-profile-menu\" id=\"userProfileMenuBtn\" type=\"button\">\n<svg viewbox=\"0 0 24 24\"><path d=\"M4 7h16M4 12h16M4 17h16\"></path></svg>\n</button>\n</div>\n<div class=\"user-profile-scroll\">\n<div class=\"user-profile-head\">\n<div class=\"user-profile-mainrow\">\n<div class=\"user-avatar-wrap\">\n                U\n                <div class=\"user-avatar-add url-plus\"><img alt=\"+\" src=\"https://imgbed.heliar.top/i/Xig3Vm87Atd7WyZL_copy_副本_pixian_ai (1)_副本.webp\"/></div>\n</div>\n<div class=\"user-stats\">\n<div class=\"user-stat\"><strong id=\"userPostCount\">12</strong><span>帖子</span></div>\n<div class=\"user-stat\"><strong id=\"userFollowers\">148</strong><span>粉丝</span></div>\n<div class=\"user-stat\"><strong id=\"userFollowing\">86</strong><span>关注</span></div>\n</div>\n</div>\n<div class=\"profile-name-location-line\">\n<div class=\"user-profile-name\" id=\"userDisplayName\">User</div>\n<div class=\"profile-location-inline\" id=\"userProfileLocation\">\n<svg aria-hidden=\"true\" viewbox=\"0 0 24 24\"><path d=\"M12 21s6-5.2 6-11a6 6 0 1 0-12 0c0 5.8 6 11 6 11Z\"></path><circle cx=\"12\" cy=\"10\" r=\"2\"></circle></svg>\n<span></span>\n</div>\n</div>\n<div class=\"user-profile-handle\" id=\"userHandle\">@user.ins</div>\n<div class=\"user-profile-bio\" id=\"userBio\">记录一点日常。<br/>study hard, live beautifully.</div>\n<div class=\"user-profile-buttons\">\n<button class=\"user-profile-btn\" id=\"editProfileBtn\" type=\"button\">编辑主页</button>\n<button aria-disabled=\"true\" class=\"user-profile-btn placeholder\" type=\"button\">分享主页</button>\n<button aria-label=\"界面尺寸调整\" class=\"user-profile-size-btn\" id=\"profileInterfaceSizeBtn\" title=\"界面尺寸调整\" type=\"button\">＋</button>\n</div>\n</div>\n<div class=\"user-highlight-strip\" id=\"userHighlightStrip\"></div>\n<div class=\"user-profile-tabs\">\n<button aria-label=\"图片动态\" class=\"user-tab active\" data-user-tab=\"posts\" type=\"button\">\n<div aria-hidden=\"true\" class=\"user-grid9-icon\">\n<span></span><span></span><span></span>\n<span></span><span></span><span></span>\n<span></span><span></span><span></span>\n</div>\n</button>\n<button aria-label=\"视频动态\" class=\"user-tab video-tab\" data-user-tab=\"videos\" type=\"button\">\n<svg viewbox=\"0 0 24 24\"><rect height=\"16\" rx=\"4\" width=\"16\" x=\"4\" y=\"4\"></rect><path d=\"m10 8 6 4-6 4z\"></path></svg>\n</button>\n<button aria-label=\"收藏\" class=\"user-tab repost-tab\" data-user-tab=\"reposts\" type=\"button\">\n<img alt=\"转发\" src=\"https://imgbed.heliar.top/i/0-BBc0_1k2Hl9jq7_转发_pixian_ai.webp\"/>\n</button>\n<div class=\"user-tab-indicator\" id=\"userTabIndicator\"></div>\n</div>\n<div id=\"userProfileGrid\"></div>\n</div>\n</div>\n</section>\n</div>\n<div class=\"sheet-backdrop\" id=\"sheetBackdrop\">\n<div class=\"sheet\">\n<div class=\"sheet-grab\"></div>\n<div class=\"sheet-title\">发动态</div>\n<div class=\"sheet-btn\">发普通动态</div>\n<div class=\"sheet-btn\">发限时动态</div>\n<div class=\"sheet-btn\" id=\"cancelCompose\">取消</div>\n</div>\n</div>\n<!-- Full-page user create post. Opened by frozen bottom '+'; bottom nav remains untouched behind it. -->\n<section class=\"create-post-overlay\" id=\"createPostOverlay\">\n<div class=\"cp-topbar\">\n<button class=\"cp-back\" id=\"createPostBack\" type=\"button\"><span>‹</span></button>\n<div class=\"cp-title\">新帖子</div>\n</div>\n<div class=\"cp-content\">\n<div class=\"cp-preview-track\" id=\"cpPreviewTrack\"></div>\n<div class=\"cp-counter\" id=\"cpCount\">0/4</div>\n<div class=\"cp-media-actions\">\n<button class=\"cp-action\" id=\"cpCaptureBtn\" type=\"button\">\n<svg viewbox=\"0 0 24 24\"><path d=\"M4 7h4l1.2-2h5.6L16 7h4v12H4z\"></path><circle cx=\"12\" cy=\"13\" r=\"4\"></circle></svg>\n          拍摄照片\n        </button>\n<button class=\"cp-action\" id=\"cpUploadBtn\" type=\"button\">\n<svg viewbox=\"0 0 24 24\"><rect height=\"16\" rx=\"2\" width=\"18\" x=\"3\" y=\"4\"></rect><circle cx=\"8.5\" cy=\"9\" r=\"1.5\"></circle><path d=\"M4.5 18 10 12.5l3.5 3 2.5-2 3.5 4.5\"></path></svg>\n          上传照片\n        </button>\n</div>\n<div class=\"cp-section\">\n<div class=\"cp-label\">添加配文</div>\n<textarea class=\"cp-caption\" id=\"cpCaption\" placeholder=\"写点什么……\"></textarea>\n</div>\n<div class=\"cp-section\">\n<div class=\"cp-label\">帖子设置</div>\n<div class=\"cp-field\">\n<span style=\"font-size:24px;color:#8e9197;flex:0 0 22px\">#</span>\n<input class=\"cp-input\" id=\"cpTag\" placeholder=\"输入 Tag\"/>\n</div>\n<div class=\"cp-field\">\n<svg viewbox=\"0 0 24 24\"><path d=\"M12 21s6-5.1 6-11a6 6 0 1 0-12 0c0 5.9 6 11 6 11z\"></path><circle cx=\"12\" cy=\"10\" r=\"2\"></circle></svg>\n<input class=\"cp-input\" id=\"cpLocation\" placeholder=\"输入地点\"/>\n</div>\n<div class=\"cp-field\" id=\"cpTagCharField\">\n<svg viewbox=\"0 0 24 24\"><circle cx=\"9\" cy=\"8\" r=\"3\"></circle><path d=\"M3.5 18c.8-3.2 2.7-5 5.5-5s4.7 1.8 5.5 5\"></path><path d=\"M17 8h4M19 6v4\"></path></svg>\n<div style=\"flex:1\">\n<div class=\"cp-placeholder\" id=\"cpTagCharLabel\">标记人物（选择已有 char）</div>\n<div id=\"cpTagCharPills\"></div>\n</div>\n<span class=\"cp-right\">›</span>\n</div>\n<div class=\"cp-switch-row\">\n<div class=\"cp-switch-copy\">\n<strong>允许陌生人评论</strong>\n<span>关闭后仅已关注角色和固定社交 NPC 可互动</span>\n</div>\n<div class=\"cp-switch on\" id=\"cpStrangerSwitch\"></div>\n</div>\n</div>\n</div>\n<div class=\"cp-bottom\">\n<button class=\"cp-share\" id=\"cpShareBtn\" type=\"button\">分享</button>\n</div>\n<div class=\"cp-modal\" id=\"cpCaptureModal\">\n<div class=\"cp-sheet\">\n<div class=\"cp-grab\"></div>\n<div class=\"cp-sheet-title\">拍摄照片</div>\n<textarea class=\"cp-sheet-textarea\" id=\"cpCaptureDesc\" placeholder=\"描述要发布的照片内容\"></textarea>\n<div class=\"cp-sheet-hint\">保存后作为照片描述，同样作为生图提示词。</div>\n<div class=\"cp-sheet-actions cp-capture-actions\">\n<button class=\"cp-sheet-btn primary\" id=\"cpSaveDescBtn\" type=\"button\">保存描述</button>\n<button class=\"cp-sheet-btn primary\" id=\"cpPlaceholderBtn\" type=\"button\">选择占位图</button>\n<button class=\"cp-sheet-btn primary\" id=\"cpGenerateBtn\" type=\"button\">生图</button>\n<button class=\"cp-sheet-btn primary cp-close-modal\" type=\"button\">取消</button>\n</div>\n</div>\n</div>\n<div class=\"cp-modal\" id=\"cpUploadModal\">\n<div class=\"cp-sheet\">\n<div class=\"cp-grab\"></div>\n<div class=\"cp-sheet-title\">上传照片</div>\n<input accept=\"image/*\" class=\"cp-hidden-file\" id=\"cpFileInput\" multiple=\"\" type=\"file\"/>\n<input class=\"cp-sheet-input\" id=\"cpUrlInput\" placeholder=\"粘贴图片 URL\"/>\n<div class=\"cp-sheet-hint\">URL 图片会直接显示在上方滑动预览区。</div>\n<div class=\"cp-sheet-actions\">\n<button class=\"cp-sheet-btn primary\" id=\"cpAddUrlBtn\" type=\"button\">添加 URL 图片</button>\n<button class=\"cp-sheet-btn primary\" id=\"cpDeviceBtn\" type=\"button\">从设备选择照片</button>\n<button class=\"cp-sheet-btn red cp-close-modal\" type=\"button\">取消</button>\n</div>\n</div>\n</div>\n<div class=\"cp-modal\" id=\"cpPlaceholderModal\">\n<div class=\"cp-sheet\">\n<div class=\"cp-grab\"></div>\n<div class=\"cp-sheet-title\">选择占位图</div>\n<div class=\"cp-placeholder-grid\" id=\"cpPlaceholderGrid\"></div>\n<div class=\"cp-sheet-actions\">\n<button class=\"cp-sheet-btn red cp-close-modal\" type=\"button\">取消</button>\n</div>\n</div>\n</div>\n<div class=\"cp-modal\" id=\"cpCharModal\">\n<div class=\"cp-sheet\">\n<div class=\"cp-grab\"></div>\n<div class=\"cp-sheet-title\">选择要标记的角色</div>\n<div class=\"cp-sheet-actions\" id=\"cpCharChoiceList\"></div>\n<div class=\"cp-sheet-actions\">\n<button class=\"cp-sheet-btn red cp-close-modal\" type=\"button\">完成</button>\n</div>\n</div>\n</div>\n</section>\n<!-- ===== v1.02 per-post glass menu/editor ===== -->\n<div class=\"post-glass-dim\" id=\"postGlassDim\"></div>\n<section class=\"post-action-glass\" id=\"postActionGlass\">\n<button class=\"post-action-btn\" id=\"postEditAction\" type=\"button\">\n<span class=\"post-action-icon\">\n<svg viewbox=\"0 0 24 24\"><path d=\"M4 20h4l11-11-4-4L4 16z\"></path><path d=\"m13.5 6.5 4 4\"></path></svg>\n</span>\n      编辑动态\n    </button>\n<button class=\"post-action-btn danger\" id=\"postDeleteAction\" type=\"button\">\n<span class=\"post-action-icon\">\n<svg viewbox=\"0 0 24 24\"><path d=\"M4 7h16\"></path><path d=\"M9 7V4h6v3\"></path><path d=\"M7 7l1 13h8l1-13\"></path><path d=\"M10 11v5M14 11v5\"></path></svg>\n</span>\n      删除动态\n    </button>\n</section>\n<section class=\"post-delete-confirm\" id=\"postDeleteConfirm\">\n<div class=\"post-delete-title\">删除这条动态？</div>\n<div class=\"post-delete-copy\" id=\"postDeleteConfirmCopy\">删除后这条动态会从当前 Feed 中移除。</div>\n<div class=\"post-delete-actions\">\n<button class=\"post-delete-btn cancel\" id=\"postDeleteCancel\" type=\"button\">取消</button>\n<button class=\"post-delete-btn delete\" id=\"postDeleteConfirmBtn\" type=\"button\">删除</button>\n</div>\n</section>\n<section class=\"post-edit-glass\" id=\"postEditGlass\">\n<div class=\"post-edit-head\">\n<div class=\"post-edit-title\">编辑动态</div>\n<button class=\"post-edit-close\" id=\"postEditClose\" type=\"button\">×</button>\n</div>\n<div class=\"post-edit-block\">\n<label class=\"post-edit-label\">图片</label>\n<div class=\"post-edit-media-shell\">\n<button class=\"post-edit-media-nav prev\" id=\"postEditMediaPrev\" type=\"button\">‹</button>\n<div aria-label=\"点击翻到图片描述\" class=\"post-edit-media\" id=\"postEditMediaPreview\" role=\"button\"></div>\n<button class=\"post-edit-media-nav next\" id=\"postEditMediaNext\" type=\"button\">›</button>\n</div>\n<div class=\"post-edit-media-meta\">\n<span>轻点翻面 · 左右滑动切换</span>\n<b id=\"postEditCarouselCounter\">1 / 1</b>\n</div>\n</div>\n<div class=\"post-edit-block post-edit-stranger-identity\" id=\"postEditStrangerIdentity\">\n<label class=\"post-edit-label\">生成陌生人身份</label>\n<div class=\"post-edit-identity-grid\">\n<div>\n<label class=\"post-edit-label\">昵称</label>\n<input class=\"post-edit-input\" id=\"postEditDisplayName\" placeholder=\"陌生人显示昵称\"/>\n</div>\n<div>\n<label class=\"post-edit-label\">IG ID</label>\n<input class=\"post-edit-input\" id=\"postEditHandle\" placeholder=\"IG ID\"/>\n</div>\n<div class=\"wide\">\n<label class=\"post-edit-label\">地点</label>\n<input class=\"post-edit-input\" id=\"postEditLocation\" placeholder=\"Seoul·Korea\"/>\n</div>\n</div>\n<div class=\"post-edit-note\">只对 AI 生成的陌生人开放；不会改动 Roche Character / 固定 NPC / user 身份。</div>\n</div>\n<div class=\"post-edit-block\">\n<label class=\"post-edit-label\">文案</label>\n<textarea class=\"post-edit-textarea\" id=\"postEditCaption\" placeholder=\"编辑这条动态的文案\"></textarea>\n</div>\n<div class=\"post-edit-block\">\n<label class=\"post-edit-label\">图片来源</label>\n<div class=\"post-edit-note\" id=\"postEditMediaTypeNote\">图片默认显示示例图；实际内容语义以图片描述为准。</div>\n<div class=\"post-edit-badge\" id=\"postEditSourceBadge\">当前来源：默认示例图</div>\n<div id=\"postEditAlbumTools\">\n<div class=\"post-edit-source-grid\">\n<button class=\"post-edit-source-btn active\" id=\"postEditUseFallback\" type=\"button\">\n<strong>默认图片</strong>\n<span>使用示例图池</span>\n</button>\n<button class=\"post-edit-source-btn\" id=\"postEditUseAlbum\" type=\"button\">\n<strong>角色相册</strong>\n<span>Roche Character</span>\n</button>\n<button class=\"post-edit-source-btn\" id=\"postEditUseGenerated\" type=\"button\">\n<strong>生图</strong>\n<span>按当前描述生成</span>\n</button>\n<button class=\"post-edit-source-btn\" id=\"postEditUseUpload\" type=\"button\">\n<strong>上传图片</strong>\n<span>char / user / 陌生人</span>\n</button>\n</div>\n<div class=\"post-edit-description-wrap\">\n<label class=\"post-edit-label\">图片描述</label>\n<textarea class=\"post-edit-textarea\" id=\"postEditMediaDescription\" placeholder=\"这段描述会作为图片语义，也会用于相册匹配 / 生图\"></textarea>\n</div>\n<div class=\"post-edit-source-note\" id=\"postEditSourceNote\">\n          默认图片只是视觉占位；AI / 记忆系统读取的是图片描述，不会把示例图本身当成真实剧情事实。\n        </div>\n<button class=\"post-edit-generate-action\" id=\"postEditGenerateImage\" type=\"button\">按当前描述生图</button>\n<button class=\"post-edit-generate-action\" id=\"postEditChooseUpload\" type=\"button\">从设备选择 / 添加图片</button>\n<div class=\"post-edit-media-actions\">\n<button class=\"post-edit-generate-action\" id=\"postEditAddMedia\" type=\"button\">新增图</button>\n<button class=\"post-edit-generate-action\" id=\"postEditDeleteMedia\" type=\"button\">删除这张图</button>\n</div>\n<input accept=\"image/*\" class=\"post-edit-upload-input\" id=\"postEditUploadInput\" multiple=\"\" type=\"file\"/>\n</div>\n</div>\n<button class=\"post-edit-save\" id=\"postEditSave\" type=\"button\">保存修改</button>\n</section>\n<!-- ===== v1.01 Feed topbar glass overlays ===== -->\n<div class=\"feed-glass-dim\" id=\"feedGlassDim\"></div>\n<section aria-label=\"刷新首页\" class=\"feed-glass-panel\" id=\"feedRefreshGlass\">\n<div class=\"feed-glass-head\">\n<div class=\"feed-glass-title\">刷新首页</div>\n<button class=\"feed-glass-close\" data-feed-glass-close=\"\" type=\"button\">×</button>\n</div>\n<div class=\"feed-glass-block\">\n<strong>本次偏好</strong>\n<div class=\"feed-glass-muted\">可选，作为本次陌生人 / 已有角色动态生成的额外倾向，不覆盖角色人设。</div>\n<div class=\"batch-prompt-wrap\">\n<textarea class=\"batch-prompt-input\" id=\"feedBatchPrompt\" placeholder=\"例如：这批想看偏生活流、夜晚、旅行或者更轻松一点的内容。\"></textarea>\n</div>\n</div>\n<div class=\"feed-glass-block\">\n<strong>刷新陌生人</strong>\n<div class=\"feed-glass-muted\">加载新一批陌生人，开始 INS 冲浪吧。</div>\n<button class=\"feed-glass-action light\" data-ins-feed-action=\"refresh_strangers\" id=\"feedRefreshStrangersBtn\" style=\"width:100%;margin-top:11px\" type=\"button\">刷新陌生人</button>\n</div>\n<div class=\"feed-glass-block\">\n<strong>选择已有角色</strong>\n<div class=\"feed-glass-muted\">叫 TA 过来发一条，可多选。</div>\n<div class=\"feed-char-chips\" id=\"feedCharChips\"></div>\n<div class=\"feed-glass-action-row\">\n<button class=\"feed-glass-action light\" data-ins-feed-action=\"char_post\" id=\"feedCharsPostBtn\" type=\"button\">发 IG 动态</button>\n<button class=\"feed-glass-action light\" data-ins-feed-action=\"char_story\" id=\"feedCharsStoryBtn\" type=\"button\">发 Story</button>\n</div>\n</div>\n</section>\n<section aria-label=\"消息提醒\" class=\"feed-glass-panel\" id=\"feedNoticeGlass\">\n<div class=\"feed-glass-head\">\n<div class=\"feed-glass-title\">消息提醒</div>\n<button class=\"feed-glass-close\" data-feed-glass-close=\"\" type=\"button\">×</button>\n</div>\n<div class=\"feed-notice-list\" id=\"feedNoticeList\"></div>\n<div class=\"feed-glass-foot\">陌生人普通评论不进入此处</div>\n</section>\n<!-- ===== Navigation destination placeholders ===== -->\n<section class=\"route-page\" id=\"routeNotifications\">\n<div class=\"route-head\">\n<button class=\"route-back\" data-route-back=\"\" type=\"button\">‹</button>\n<div class=\"route-title\">通知</div>\n</div>\n<div class=\"route-body\" id=\"routeNotificationList\">\n<div class=\"route-empty\">暂无新提醒</div>\n</div>\n</section>\n<section class=\"route-page\" id=\"routeRefresh\">\n<div class=\"route-head\">\n<button class=\"route-back\" data-route-back=\"\" type=\"button\">‹</button>\n<div class=\"route-title\">刷新首页</div>\n</div>\n<div class=\"route-body\">\n<div class=\"refresh-grid\">\n<div class=\"refresh-option\">\n<strong>刷新陌生人</strong>\n<span>加载新一批陌生人，开始 INS 冲浪吧。</span>\n<div style=\"margin-top:12px\"><button class=\"route-btn light\" type=\"button\">刷新陌生人</button></div>\n</div>\n<div class=\"refresh-option\">\n<strong>选择已有角色</strong>\n<span>支持单选 / 多选。选中后再决定发 IG 动态还是限时动态。</span>\n<div class=\"char-checks\">\n<button class=\"char-chip on\" type=\"button\">芙芙</button>\n<button class=\"char-chip\" type=\"button\">Yuan_</button>\n<button class=\"char-chip\" type=\"button\">Wujo_</button>\n</div>\n<div style=\"display:flex;gap:10px;margin-top:14px\">\n<button class=\"route-btn\" type=\"button\">发 IG 动态</button>\n<button class=\"route-btn light\" type=\"button\">发限时动态</button>\n</div>\n</div>\n</div>\n</div>\n</section>\n<section class=\"route-page dark\" id=\"routeStory\">\n<div class=\"story-stage\">\n<div class=\"story-progress\" id=\"storyProgress\"></div>\n<div class=\"story-meta\">\n<div class=\"story-avatar\" id=\"routeStoryAvatar\">A</div>\n<strong id=\"routeStoryOwner\">芙芙</strong>\n<span id=\"routeStoryTime\" style=\"font-size:12px;color:#b8bac0\">2小时前</span>\n<button class=\"story-close\" data-route-back=\"\" type=\"button\">×</button>\n</div>\n<button aria-label=\"上一条 Story\" class=\"story-nav-zone prev\" id=\"storyPrevBtn\" type=\"button\"></button>\n<button aria-label=\"下一条 Story\" class=\"story-nav-zone next\" id=\"storyNextBtn\" type=\"button\"></button>\n<div class=\"story-content\" id=\"routeStoryText\"></div>\n<div class=\"story-caption-shell\" id=\"storyCaptionShell\">\n<div class=\"story-caption-text\" id=\"storyCaptionText\"></div>\n</div>\n<div class=\"story-reaction-toast\" id=\"storyReactionToast\">已发送</div>\n<div class=\"story-bottom\" id=\"storyBottom\">\n<button class=\"story-emoji-input\" id=\"storyEmojiLauncher\" type=\"button\">发送消息…</button>\n<button aria-label=\"喜欢这条 Story\" class=\"story-small-icon\" id=\"storyHeartBtn\" type=\"button\">\n<img alt=\"\" class=\"story-heart-img\" id=\"storyHeartImg\" src=\"https://imgbed.heliar.top/i/pbz63LCde-0-ZoaE_空心_pixian_ai.webp\"/>\n</button>\n<button aria-label=\"分享这组 Story\" class=\"story-small-icon\" id=\"storyShareBtn\" type=\"button\">\n<img alt=\"\" class=\"story-share-icon\" src=\"https://imgbed.heliar.top/i/r4euFTG1Nx3YKVI9_dm_pixian_ai.webp\"/>\n</button>\n<button aria-label=\"删除这组 Story\" class=\"story-small-icon story-delete-icon\" id=\"storyDeleteBtn\" type=\"button\">\n<svg viewbox=\"0 0 24 24\"><path d=\"M4 7h16\"></path><path d=\"M9 7V4h6v3\"></path><path d=\"M7 7l1 13h8l1-13\"></path><path d=\"M10 11v5M14 11v5\"></path></svg>\n</button>\n<button aria-label=\"编辑这条 Story\" class=\"story-small-icon\" id=\"storyEditBtn\" type=\"button\">\n<svg viewbox=\"0 0 24 24\"><path d=\"M20 6v5h-5\"></path><path d=\"M19 11a7 7 0 1 0 1 5\"></path></svg>\n</button>\n</div>\n<div class=\"story-overlay-dim\" id=\"storyOverlayDim\"></div>\n<section class=\"story-sheet\" id=\"storyEmojiSheet\">\n<div class=\"story-sheet-head\">\n<div class=\"story-sheet-title\">发送一个反应</div>\n<button class=\"story-sheet-close\" data-story-sheet-close=\"\" type=\"button\">×</button>\n</div>\n<div class=\"story-emoji-grid\">\n<button class=\"story-emoji-btn\" data-story-reaction=\"🤣\" type=\"button\">🤣</button>\n<button class=\"story-emoji-btn\" data-story-reaction=\"🔥\" type=\"button\">🔥</button>\n<button class=\"story-emoji-btn\" data-story-reaction=\"🥰\" type=\"button\">🥰</button>\n<button class=\"story-emoji-btn\" data-story-reaction=\"😱\" type=\"button\">😱</button>\n<button class=\"story-emoji-btn\" data-story-reaction=\"👏\" type=\"button\">👏</button>\n<button class=\"story-emoji-btn\" data-story-reaction=\"🤯\" type=\"button\">🤯</button>\n<button class=\"story-emoji-btn\" data-story-reaction=\"😭\" type=\"button\">😭</button>\n<button class=\"story-emoji-btn\" data-story-reaction=\"🥵\" type=\"button\">🥵</button>\n</div>\n<div class=\"story-emoji-copy\">\n          这里只能发送 emoji。系统会把 emoji 对应的态度语义一起记录，后续总结记忆时可直接读出 user 对这条限时动态的反应。\n        </div>\n</section>\n<section class=\"story-sheet\" id=\"storyShareSheet\">\n<div class=\"story-sheet-head\">\n<div class=\"story-sheet-title\">分享这组 Story</div>\n<button class=\"story-sheet-close\" data-story-sheet-close=\"\" type=\"button\">×</button>\n</div>\n<div class=\"story-share-list\" id=\"storyShareList\"></div>\n<div class=\"story-emoji-copy\">角色 Story 只能分享给与原作者存在直接关系的联系人；User Story 仅限已绑定角色 / 社交联系人。</div>\n</section>\n<section class=\"story-sheet\" id=\"storyOwnReactionSheet\">\n<div class=\"story-sheet-head\">\n<div class=\"story-sheet-title\">我的快拍 · 浏览与表态</div>\n<button class=\"story-sheet-close\" data-story-sheet-close=\"\" type=\"button\">×</button>\n</div>\n<div class=\"story-own-reaction-sheet-list\" id=\"storyOwnReactionList\"></div>\n<div class=\"story-emoji-copy\" id=\"storyOwnReactionPlanStatus\"></div>\n<div class=\"story-emoji-copy\" id=\"storyOwnReactionEmpty\">还没有已发生的浏览或表态。Story 发布后会规划 24 小时内角色 / 社交联系人的浏览与表态。</div>\n<button class=\"story-own-reaction-plan-btn\" id=\"storyOwnReactionPlanBtn\" type=\"button\">规划 24 小时浏览与表态</button>\n</section>\n<section class=\"story-sheet\" id=\"storyDeleteSheet\">\n<div class=\"story-sheet-head\">\n<div class=\"story-sheet-title\">删除这组 Story？</div>\n<button class=\"story-sheet-close\" data-story-sheet-close=\"\" type=\"button\">×</button>\n</div>\n<div class=\"story-emoji-copy\" style=\"margin-bottom:12px\">手动删除会把这组 Story 从当前快拍、Story 记录和已绑定的 Highlight 中一起移除。24 小时自动过期只会从首页消失，不会删除 Story 记录或 Highlight。</div>\n<div class=\"story-edit-row\">\n<button class=\"story-edit-btn light\" id=\"storyDeleteCancelBtn\" type=\"button\">取消</button>\n<button class=\"story-edit-btn danger\" id=\"storyDeleteConfirmBtn\" type=\"button\">删除</button>\n</div>\n</section>\n<section class=\"story-sheet\" id=\"storyEditSheet\">\n<div class=\"story-sheet-head\">\n<div class=\"story-sheet-title\">编辑现实动态</div>\n<button class=\"story-sheet-close\" data-story-sheet-close=\"\" type=\"button\">×</button>\n</div>\n<div class=\"story-edit-scroll\">\n<div class=\"story-edit-block\">\n<label class=\"story-edit-label\">Story 文案</label>\n<textarea class=\"story-edit-textarea\" id=\"storyEditCaption\" placeholder=\"编辑这一组 Story 的正文\"></textarea>\n</div>\n<div class=\"story-edit-chiprow\" id=\"storyEditChipRow\"></div>\n<div class=\"story-edit-block\">\n<label class=\"story-edit-label\">当前条目类型</label>\n<div class=\"story-edit-copy\" id=\"storyEditTypeCopy\">图片 / 视频</div>\n<div class=\"story-edit-badge\" id=\"storyEditSourceBadge\">当前来源：文字描述</div>\n</div>\n<div class=\"story-edit-block\">\n<label class=\"story-edit-label\">描述内容</label>\n<textarea class=\"story-edit-textarea\" id=\"storyEditDescription\" placeholder=\"编辑这一条现实动态的内容描述\"></textarea>\n<div class=\"story-edit-row\">\n<button class=\"story-edit-btn light\" id=\"storyEditMoveUp\" type=\"button\">上移</button>\n<button class=\"story-edit-btn light\" id=\"storyEditMoveDown\" type=\"button\">下移</button>\n</div>\n<div class=\"story-edit-manage\">\n<button class=\"story-edit-btn light\" id=\"storyEditAddImage\" type=\"button\">＋ 图片</button>\n<button class=\"story-edit-btn light\" id=\"storyEditAddVideo\" type=\"button\">＋ 视频</button>\n<button class=\"story-edit-btn danger\" id=\"storyEditDeleteItem\" type=\"button\">删除当前</button>\n</div>\n<div class=\"story-edit-limit\" id=\"storyEditLimit\">一组现实动态最多 4 条；可以只发 1 条。</div>\n</div>\n<div class=\"story-edit-block\" id=\"storyEditImageTools\">\n<div id=\"storyEditAlbumControls\">\n<label class=\"story-edit-label\" id=\"storyEditAlbumLabel\">关联角色相册（仅 Roche Character）</label>\n<select class=\"story-edit-select\" id=\"storyEditAlbumSelect\"></select>\n<div class=\"story-edit-row\">\n<button class=\"story-edit-btn light\" id=\"storyEditLinkAlbum\" type=\"button\">关联相册图片</button>\n</div>\n</div>\n<div class=\"story-edit-row\">\n<button class=\"story-edit-btn dark\" id=\"storyEditGenerateImage\" type=\"button\">生图</button>\n</div>\n<div class=\"story-edit-copy\" id=\"storyEditImagePolicyNote\" style=\"margin-top:8px\">Roche Character 可优先关联私人相册；不关联则保持文字描述，之后由 user 决定是否生图。</div>\n</div>\n<div class=\"story-edit-block\" id=\"storyEditVideoNote\" style=\"display:none\">\n<label class=\"story-edit-label\">视频说明</label>\n<div class=\"story-edit-copy\">视频 Story 不走相册关联，也不生图，只保留文字描述。你可以编辑描述内容和排序。</div>\n</div>\n</div>\n<div class=\"story-edit-footer\">\n<button class=\"story-edit-save\" id=\"storyEditSaveBtn\" type=\"button\">保存修改</button>\n</div>\n</section>\n</div>\n</section>\n<section class=\"route-page story-create-route\" id=\"routeStoryCreate\">\n<div class=\"route-head\">\n<button class=\"route-back\" data-route-back=\"\" type=\"button\">‹</button>\n<div class=\"route-title\">发布 Story</div>\n<div class=\"route-right\"></div>\n</div>\n<div class=\"story-create-body\">\n<div class=\"story-create-hero\">\n<strong>新 Story</strong>\n<span>1–4 条 · 图片 / 视频可混合</span>\n</div>\n<button class=\"story-create-add\" id=\"storyCreateAddBtn\" type=\"button\">\n<div class=\"story-create-add-circle\">＋</div>\n<span>添加图片或视频</span>\n</button>\n<div class=\"story-create-count\" id=\"storyCreateCount\">0 / 4</div>\n<div class=\"story-create-items\" id=\"storyCreateItems\"></div>\n<div class=\"story-create-section\">\n<label class=\"story-create-label\" for=\"storyCreateCaption\">Story 文案</label>\n<textarea class=\"story-create-caption\" id=\"storyCreateCaption\" placeholder=\"写一句这组 Story 想表达的文案……\"></textarea>\n<div class=\"story-create-hint\">这一条文案属于整组 Story。切换第 1 / 2 / 3 / 4 个图片或视频时，文案不会跟着切换。</div>\n</div>\n</div>\n<div class=\"story-create-bottom\">\n<button class=\"story-create-publish\" id=\"storyCreatePublishBtn\" type=\"button\">分享</button>\n</div>\n<section class=\"story-sheet\" id=\"storyCreateChoiceSheet\">\n<div class=\"story-sheet-head\">\n<div class=\"story-sheet-title\">添加内容</div>\n<button class=\"story-sheet-close\" data-story-create-close=\"\" type=\"button\">×</button>\n</div>\n<div class=\"story-create-choice\">\n<button id=\"storyCreateAddImage\" type=\"button\">图片<span>图片描述 / 之后可接上传</span></button>\n<button id=\"storyCreateAddVideo\" type=\"button\">视频<span>视频内容用文字描述</span></button>\n</div>\n</section>\n<div class=\"story-overlay-dim\" id=\"storyCreateDim\"></div>\n</section>\n<section class=\"route-page char-profile-page\" id=\"routeCharProfile\">\n<div class=\"char-profile-top\">\n<button class=\"char-profile-back\" data-route-back=\"\" type=\"button\"><span>‹</span></button>\n<div class=\"char-profile-top-id\" id=\"charProfileTopId\">FufuA1</div>\n<button aria-label=\"角色主页设置\" class=\"char-profile-menu\" id=\"charProfileMenuBtn\" type=\"button\">\n<svg viewbox=\"0 0 24 24\"><path d=\"M4 7h16M4 12h16M4 17h16\"></path></svg>\n</button>\n</div>\n<div class=\"char-profile-scroll\">\n<div class=\"char-profile-head\">\n<div class=\"char-profile-mainrow\">\n<div class=\"char-profile-avatar\" id=\"charProfileAvatar\">A</div>\n<div class=\"char-profile-stats\">\n<div class=\"char-profile-stat\"><strong id=\"charProfilePostCount\">0</strong><span>帖子</span></div>\n<div class=\"char-profile-stat\"><strong id=\"charProfileFollowers\">0</strong><span>粉丝</span></div>\n<div class=\"char-profile-stat\"><strong id=\"charProfileFollowingCount\">0</strong><span>关注</span></div>\n</div>\n</div>\n<div class=\"profile-name-location-line\">\n<div class=\"char-profile-name\" id=\"charProfileDisplayName\">芙芙</div>\n<div class=\"profile-location-inline\" id=\"charProfileLocation\">\n<svg aria-hidden=\"true\" viewbox=\"0 0 24 24\"><path d=\"M12 21s6-5.2 6-11a6 6 0 1 0-12 0c0 5.8 6 11 6 11Z\"></path><circle cx=\"12\" cy=\"10\" r=\"2\"></circle></svg>\n<span></span>\n</div>\n</div>\n<div class=\"char-profile-handle\" id=\"charProfileHandle\">@FufuA1</div>\n<div class=\"char-profile-bio\" id=\"charProfileBio\">char bio</div>\n<div class=\"char-profile-buttons\">\n<button class=\"char-profile-btn following\" id=\"charProfileFollowBtn\" type=\"button\">已关注</button>\n<button class=\"char-profile-btn\" id=\"charProfileMessageBtn\" type=\"button\">发消息</button>\n</div>\n</div>\n<div class=\"char-highlight-strip\" id=\"charHighlightStrip\"></div>\n<div class=\"char-profile-tabs\">\n<button aria-label=\"IG 动态\" class=\"char-tab active\" data-char-tab=\"posts\" type=\"button\">\n<div aria-hidden=\"true\" class=\"char-grid9-icon\">\n<span></span><span></span><span></span>\n<span></span><span></span><span></span>\n<span></span><span></span><span></span>\n</div>\n</button>\n<button aria-label=\"Reels\" class=\"char-tab\" data-char-tab=\"reels\" type=\"button\">\n<svg viewbox=\"0 0 24 24\">\n<rect height=\"16\" rx=\"4\" width=\"16\" x=\"4\" y=\"4\"></rect>\n<path d=\"m10 8 6 4-6 4z\"></path>\n</svg>\n</button>\n<button aria-label=\"私人相册\" class=\"char-tab\" data-char-tab=\"album\" type=\"button\">\n<svg aria-hidden=\"true\" class=\"char-album-svg\" viewbox=\"0 0 24 24\">\n<rect class=\"back\" height=\"14\" rx=\"3\" width=\"14\" x=\"4\" y=\"5\"></rect>\n<rect height=\"12.5\" rx=\"3\" width=\"13.5\" x=\"6.5\" y=\"7.5\"></rect>\n<circle cx=\"15.5\" cy=\"11.5\" r=\"1.25\"></circle>\n<path d=\"m8.5 17 3.2-3.5 2.5 2.2 1.9-1.9 2.4 3.2\"></path>\n</svg>\n</button>\n<button aria-label=\"留言板\" class=\"char-tab\" data-char-tab=\"board\" type=\"button\">\n<svg viewbox=\"0 0 24 24\">\n<path d=\"M5 5h14v11H9l-4 4z\"></path>\n<path d=\"M8 9h8M8 12h5\"></path>\n</svg>\n</button>\n<div class=\"char-tab-indicator\" id=\"charTabIndicator\"></div>\n</div>\n<div id=\"charProfileContent\"></div>\n</div>\n<div class=\"char-drawer-dim\" id=\"charDrawerDim\"></div>\n<aside class=\"char-drawer\" id=\"charDrawer\">\n<div class=\"char-drawer-head\">\n<div class=\"char-drawer-title\">角色控制中心</div>\n<button class=\"char-drawer-close\" id=\"charDrawerClose\" type=\"button\">×</button>\n</div>\n<div class=\"char-drawer-section\">\n<button class=\"char-drawer-item\" id=\"charEditProfileOpenBtn\" type=\"button\">\n<span class=\"char-drawer-icon\">🖊️</span>\n<span class=\"char-drawer-copy\"><strong>编辑角色主页</strong><span>头像、昵称、IG ID、地点、粉丝、关注、简介</span></span>\n<span class=\"char-drawer-arrow\">›</span>\n</button>\n<button class=\"char-drawer-item\" id=\"charRelationsOpenBtn\" type=\"button\">\n<span class=\"char-drawer-icon\">👨‍👩‍👧‍👦</span>\n<span class=\"char-drawer-copy\"><strong>人际关系</strong><span>打开同一张总关系图，并以当前账号为中心</span></span>\n<span class=\"char-drawer-arrow\">›</span>\n</button>\n<button class=\"char-drawer-item\" id=\"charActiveFrequencyBtn\" type=\"button\">\n<span class=\"char-drawer-icon\">⏱️</span>\n<span class=\"char-drawer-copy\"><strong>主动发布频率</strong><span>Feed / Story / Reels 独立开关与间隔</span></span>\n<span class=\"char-drawer-arrow\">›</span>\n</button>\n</div>\n<div class=\"char-drawer-section\">\n<button class=\"char-drawer-item\" id=\"charGenerationPreferenceBtn\" type=\"button\">\n<span class=\"char-drawer-icon\">🎨</span>\n<span class=\"char-drawer-copy\"><strong>角色生成偏好</strong><span>长期软偏好，不覆盖人设与记忆事实</span></span>\n<span class=\"char-drawer-arrow\">›</span>\n</button>\n</div>\n<div class=\"char-drawer-note\">\n        此处人际关系为快捷窗口，指向唯一人物库。\n      </div>\n</aside>\n<section class=\"char-quick-card\" id=\"charActiveFrequencyCard\">\n<div class=\"char-quick-head\">\n<strong>主动发布频率</strong>\n<button class=\"char-quick-close\" id=\"charActiveFrequencyClose\" type=\"button\">×</button>\n</div>\n<div class=\"char-frequency-row\">\n<label class=\"char-frequency-copy\"><input id=\"charFreqFeedEnabled\" type=\"checkbox\"/><span><strong>Feed 主动发布</strong><span>自动生成 1 条 IG 动态并补齐评论。</span></span></label>\n<input aria-label=\"Feed 间隔分钟\" class=\"char-frequency-minutes\" id=\"charFreqFeedMinutes\" min=\"30\" step=\"10\" type=\"number\" value=\"180\"/>\n</div>\n<div class=\"char-frequency-row\">\n<label class=\"char-frequency-copy\"><input id=\"charFreqStoryEnabled\" type=\"checkbox\"/><span><strong>Story 主动发布</strong><span>自动生成一组 1–4 条 Story。</span></span></label>\n<input aria-label=\"Story 间隔分钟\" class=\"char-frequency-minutes\" id=\"charFreqStoryMinutes\" min=\"30\" step=\"10\" type=\"number\" value=\"180\"/>\n</div>\n<div class=\"char-frequency-row\">\n<label class=\"char-frequency-copy\"><input id=\"charFreqReelsEnabled\" type=\"checkbox\"/><span><strong>Reels 主动发布</strong><span>自动生成 1 条短视频动态并补齐评论。</span></span></label>\n<input aria-label=\"Reels 间隔分钟\" class=\"char-frequency-minutes\" id=\"charFreqReelsMinutes\" min=\"30\" step=\"10\" type=\"number\" value=\"240\"/>\n</div>\n<div class=\"char-frequency-next\" id=\"charFrequencyNextHint\"></div>\n<button class=\"char-quick-save\" id=\"charActiveFrequencySave\" type=\"button\">保存并重新排程</button>\n</section>\n<section class=\"char-quick-card\" id=\"charGenerationPreferenceCard\">\n<div class=\"char-quick-head\">\n<strong>角色生成偏好</strong>\n<button class=\"char-quick-close\" id=\"charGenerationPreferenceClose\" type=\"button\">×</button>\n</div>\n<div class=\"char-quick-note\">\n        这里是这个角色长期使用的“软偏好”。首页「这批想看什么」只负责当前这一批的临时主题；它可以临时把内容往旅行、夜景、聚会等方向带，但不会改写角色人设、记忆、关系、语言或地点事实。角色偏好继续决定这个人习惯怎么发、怎么拍。\n      </div>\n<label class=\"char-quick-label\">社交氛围</label>\n<textarea class=\"char-quick-textarea\" id=\"charPrefSocial\" placeholder=\"例如：公开互动偏克制，熟人才会开玩笑；不主动对陌生人过度热情。\"></textarea>\n<label class=\"char-quick-label\">发帖风格</label>\n<textarea class=\"char-quick-textarea\" id=\"charPrefPosting\" placeholder=\"例如：文案很短，生活流为主，偶尔发咖啡店和夜路，不爱写长篇。\"></textarea>\n<label class=\"char-quick-label\">图片 / 视频意图</label>\n<textarea class=\"char-quick-textarea\" id=\"charPrefVisual\" placeholder=\"例如：偏随手抓拍、胶片感、少正脸自拍；Reels 更喜欢走路/城市片段。\"></textarea>\n<button class=\"char-quick-save\" id=\"charGenerationPreferenceSave\" type=\"button\">保存偏好</button>\n</section>\n<section class=\"char-highlight-editor\" id=\"charHighlightEditor\">\n<div class=\"char-highlight-editor-title\">\n<span id=\"charHighlightEditorTitle\">编辑 Highlight</span>\n<button class=\"char-highlight-editor-close\" id=\"charHighlightEditorClose\" type=\"button\">×</button>\n</div>\n<div class=\"char-highlight-editor-scroll\">\n<div class=\"char-highlight-preview\" id=\"charHighlightPreview\">✦</div>\n<label class=\"char-highlight-label\">名称</label>\n<input class=\"char-highlight-input\" id=\"charHighlightNameInput\" placeholder=\"例如：日常\"/>\n<label class=\"char-highlight-label\">封面 URL</label>\n<input class=\"char-highlight-input\" id=\"charHighlightCoverInput\" placeholder=\"粘贴图片 URL 替换封面\"/>\n<div class=\"highlight-purpose-card\">\n<div class=\"highlight-purpose-head\">\n<div class=\"highlight-purpose-title\">分类用途说明</div>\n<div class=\"highlight-purpose-count\" id=\"charHighlightDescriptionCount\">0 / 160</div>\n</div>\n<textarea class=\"highlight-purpose-textarea\" id=\"charHighlightDescriptionInput\" maxlength=\"160\" placeholder=\"例如：这个账号旅行期间留下的街景、餐厅和朋友片段。\"></textarea>\n<div class=\"highlight-purpose-hint\">\n          用来说明这个 Highlight 的收纳范围；固定 NPC / Roche Character 都可以单独编辑。\n        </div>\n</div>\n<div class=\"char-highlight-content\">\n<strong style=\"display:block;font-size:11px;margin-bottom:6px\">绑定 Story 记录</strong>\n<div class=\"char-highlight-story-bind-list\" id=\"charHighlightStoryBindingList\"></div>\n</div>\n</div>\n<div class=\"char-highlight-actions\">\n<button class=\"char-highlight-btn cancel\" id=\"charHighlightCancelBtn\" type=\"button\">取消</button>\n<button class=\"char-highlight-btn save\" id=\"charHighlightSaveBtn\" type=\"button\">保存</button>\n</div>\n<div class=\"char-highlight-delete-row\">\n<button id=\"charHighlightDeleteBtn\" type=\"button\">删除这个 Highlight</button>\n</div>\n</section>\n<section class=\"char-profile-editor\" id=\"charProfileEditor\">\n<div class=\"char-profile-editor-title\">\n<span>编辑角色主页</span>\n<button class=\"char-profile-editor-close\" id=\"charProfileEditorClose\" type=\"button\">×</button>\n</div>\n<div class=\"char-profile-edit-preview\" id=\"charProfileEditPreview\">A</div>\n<label class=\"char-profile-edit-label\">头像 URL</label>\n<input class=\"char-profile-edit-input\" id=\"charProfileEditAvatar\" placeholder=\"粘贴头像图片 URL\"/>\n<label class=\"char-profile-edit-label\">昵称</label>\n<input class=\"char-profile-edit-input\" id=\"charProfileEditName\"/>\n<label class=\"char-profile-edit-label\">IG ID</label>\n<input class=\"char-profile-edit-input\" id=\"charProfileEditHandle\"/>\n<label class=\"char-profile-edit-label\">地理位置</label>\n<input class=\"char-profile-edit-input\" id=\"charProfileEditLocation\" placeholder=\"例如 Seoul·Korea\"/>\n<div class=\"char-profile-edit-row\">\n<div>\n<label class=\"char-profile-edit-label\">粉丝数</label>\n<input class=\"char-profile-edit-input\" id=\"charProfileEditFollowers\" inputmode=\"numeric\"/>\n</div>\n<div>\n<label class=\"char-profile-edit-label\">关注数</label>\n<input class=\"char-profile-edit-input\" id=\"charProfileEditFollowing\" inputmode=\"numeric\"/>\n</div>\n</div>\n<label class=\"char-profile-edit-label\">简介</label>\n<textarea class=\"char-profile-edit-textarea\" id=\"charProfileEditBio\"></textarea>\n<button class=\"char-profile-edit-save\" id=\"charProfileEditSave\" type=\"button\">保存修改</button>\n</section>\n<div class=\"char-album-dim\" id=\"charAlbumDim\"></div>\n<section class=\"char-album-manager\" id=\"charAlbumManager\">\n<div class=\"char-album-manager-head\">\n<div class=\"char-album-manager-title\">\n<strong id=\"charAlbumManagerTitle\">私人相册</strong>\n<span>角色媒体资产库 · 不等于公开主页</span>\n</div>\n<button class=\"char-album-close\" id=\"charAlbumManagerClose\" type=\"button\">×</button>\n</div>\n<div class=\"char-album-manager-scroll\">\n<div class=\"char-album-section\">\n<div class=\"char-album-section-title\"><strong>相册参与度</strong><span id=\"charAlbumParticipationBadge\">70%</span></div>\n<div class=\"char-album-current\">决定每个图片槽位有多大概率尝试从私人相册找素材。关闭时完全不看相册；100% 也只是“每张都先尝试”，单张没命中仍保留文字描述，不阻止发布。</div>\n<div class=\"char-album-rate-row\" id=\"charAlbumParticipationRow\">\n<button class=\"char-album-rate\" data-album-rate=\"0\" type=\"button\">关闭</button>\n<button class=\"char-album-rate\" data-album-rate=\"40\" type=\"button\">40%</button>\n<button class=\"char-album-rate\" data-album-rate=\"70\" type=\"button\">70%</button>\n<button class=\"char-album-rate\" data-album-rate=\"100\" type=\"button\">100%</button>\n</div>\n<label class=\"char-album-switch-row\">\n<span class=\"char-album-switch-copy\"><strong>未命中时标记为待生图</strong><span>关闭 = 只保留中文图片描述；开启 = 把未命中的槽位标记为待生图。是否真正调用生图仍由 INS / Roche 生图链路决定。</span></span>\n<span class=\"char-album-switch\"><input id=\"charAlbumAutoGenerateMissing\" type=\"checkbox\"/><i></i></span>\n</label>\n</div>\n<div class=\"char-album-section\">\n<div class=\"char-album-section-title\"><strong>时期匹配</strong><span id=\"charAlbumEraStatusBadge\">关闭</span></div>\n<label class=\"char-album-switch-row\">\n<span class=\"char-album-switch-copy\"><strong>开启人像时期匹配</strong><span>默认关闭。开启后只约束自拍 / 他拍 / 合影；风景、物品、食物、宠物永远不需要标“现在还是过去”。</span></span>\n<span class=\"char-album-switch\"><input id=\"charAlbumEraEnabled\" type=\"checkbox\"/><i></i></span>\n</label>\n<div class=\"char-album-collapse\" hidden=\"\" id=\"charAlbumEraCollapse\">\n<div class=\"char-album-current\" id=\"charAlbumEraStatus\"></div>\n<div class=\"char-album-era-row\" id=\"charAlbumEraRow\"></div>\n<button class=\"char-album-era-add\" id=\"charAlbumAddEraBtn\" type=\"button\">＋ 添加时期</button>\n<div class=\"char-album-era-editor\" hidden=\"\" id=\"charAlbumEraEditor\">\n<div class=\"char-album-era-editor-title\"><strong id=\"charAlbumEraEditorTitle\">添加时期</strong><button id=\"charAlbumEraEditorClose\" type=\"button\">×</button></div>\n<label class=\"char-album-era-label\">时期名称</label>\n<input class=\"char-album-era-input\" id=\"charAlbumEraName\" placeholder=\"例如：现在 / 去年眼镜时期\"/>\n<label class=\"char-album-era-label\">外貌特征 Tag</label>\n<textarea class=\"char-album-era-textarea\" id=\"charAlbumEraDesc\" placeholder=\"例如：戴眼镜、长发、棕发。只写需要区分角色外貌的特征 tag。\"></textarea>\n<label class=\"char-album-era-current-toggle\"><input id=\"charAlbumEraCurrent\" type=\"checkbox\"/><span><strong>设为当前时期</strong><small>开启时期匹配后，人像若没有明确指定旧时期，会优先使用当前时期。</small></span></label>\n<div class=\"char-album-era-editor-actions\"><button class=\"danger\" id=\"charAlbumEraDelete\" type=\"button\">删除</button><button id=\"charAlbumEraCancel\" type=\"button\">取消</button><button class=\"primary\" id=\"charAlbumEraSave\" type=\"button\">保存</button></div>\n</div>\n<div class=\"char-album-era-note\">要开启时期匹配，必须至少有 2 个时期：1 个当前时期 + 至少 1 个非当前时期，而且两边都要有外貌特征 Tag。时期信息只注入人像素材。</div>\n</div>\n</div>\n<div class=\"char-album-section\">\n<div class=\"char-album-section-title\"><strong>照片库来源</strong><span>先选固定分类，再写自由 Tag</span></div>\n<div class=\"char-album-category-title\">固定分类（必选一个）</div>\n<div class=\"char-album-category-grid\" id=\"charAlbumCategoryGrid\"></div>\n<input class=\"char-album-source-input char-album-import-desc\" id=\"charAlbumImportDesc\" placeholder=\"图片 Tag / 描述，例如：夏季 · 甜品店 · 白色上衣 · 下午\"/>\n<div class=\"char-album-era-asset-wrap\" hidden=\"\" id=\"charAlbumAssetEraWrap\"><div class=\"char-album-era-asset-label\">这张人像属于哪些时期？开启时期匹配时，人像必须至少选一个；可多选。</div><div class=\"char-album-era-select-row\" id=\"charAlbumAssetEraRow\"></div></div>\n<div class=\"char-album-source-grid\">\n<div class=\"char-album-source-card\"><strong>URL 加入</strong><span>粘贴图片直链，加入当前角色私人照片库。</span><input class=\"char-album-source-input\" id=\"charAlbumUrlInput\" placeholder=\"https://...\"/><button id=\"charAlbumAddUrlBtn\" type=\"button\">＋ 加入 URL</button></div>\n<div class=\"char-album-source-card\"><strong>手机导入</strong><span>从手机照片库选择；同一次导入沿用当前分类、Tag 和时期选择。</span><input accept=\"image/*\" hidden=\"\" id=\"charAlbumPhoneInput\" multiple=\"\" type=\"file\"/><button id=\"charAlbumPickPhoneBtn\" type=\"button\">从手机选择</button></div>\n</div>\n<div class=\"char-album-library-summary\" id=\"charAlbumLibrarySummary\">当前照片库 0 张。</div><div class=\"char-album-library-list\" id=\"charAlbumLibraryList\"></div>\n</div>\n<div class=\"char-album-section\"><div class=\"char-album-section-title\"><strong>生成收藏</strong><span>只收藏，不参与动态选图</span></div><div class=\"char-album-generated-strip\" id=\"charAlbumGeneratedCollection\"></div><div class=\"char-album-generated-note\">这里只保存该 Char 生成过并被你收藏的图片 + 当时描述；永远不进入 Feed / Story 的 Matcher。</div></div>\n<div class=\"char-album-section\"><div class=\"char-album-section-title\"><strong>Matcher 规则</strong><span>v5.1</span></div><div class=\"char-album-rule\"><b>分类决定“图是什么”，普通 Tag 决定“图里有什么”，时期只决定“图中的角色长什么样”。</b><br/><br/>AI 先自由决定动态内容和 1–4 个媒体槽位，再由本地 Matcher 按相册参与度逐张尝试。部分命中完全允许：命中的用相册图，没命中的保留中文描述或标记待生图；不会因为缺一张图就取消整条动态。</div></div>\n</div>\n</section>\n<div class=\"char-post-dim\" id=\"charPostDim\"></div>\n<section class=\"char-post-card\" id=\"charPostCard\">\n<div class=\"char-post-card-head\">\n<div class=\"char-post-card-avatar\" id=\"charPostCardAvatar\">A</div>\n<div class=\"char-post-card-author\" id=\"charPostCardAuthor\">FufuA1</div>\n<button class=\"char-post-card-close\" id=\"charPostCardClose\" type=\"button\">×</button>\n</div>\n<div class=\"char-post-card-scroll\">\n<div class=\"char-post-card-media\" id=\"charPostCardMedia\"></div>\n<div class=\"char-post-card-actions\" id=\"charPostCardActions\"></div>\n<div class=\"char-post-card-caption\" id=\"charPostCardCaption\"></div>\n<div class=\"char-post-comments\">\n<div class=\"char-post-comments-head\">\n<div class=\"char-post-comments-title\">留言</div>\n<button aria-label=\"刷新留言\" class=\"char-post-comments-refresh\" id=\"charPostCommentRefresh\" type=\"button\">\n<svg viewbox=\"0 0 24 24\">\n<path d=\"M20 6v5h-5\"></path>\n<path d=\"M19 11a7 7 0 1 0 1 5\"></path>\n</svg>\n</button>\n</div>\n<div id=\"charPostCommentList\"></div>\n</div>\n</div>\n</section>\n</section>\n<section class=\"route-page dm-chat-page\" id=\"routeDMChat\">\n<div class=\"dm-chat-topbar\">\n<button class=\"dm-chat-back\" data-route-back=\"\" type=\"button\"><span>‹</span></button>\n<div class=\"dm-chat-avatar\" id=\"dmChatTopAvatar\">A</div>\n<div class=\"dm-chat-id\">\n<div class=\"dm-chat-name\" id=\"dmChatTopName\">芙芙</div>\n<div class=\"dm-chat-handle\" id=\"dmChatTopHandle\">FufuA1</div>\n</div>\n<button aria-label=\"电话占位\" class=\"dm-chat-top-icon\" type=\"button\">\n<svg viewbox=\"0 0 24 24\">\n<path d=\"M22 16.9v3a2 2 0 0 1-2.2 2 19.8 19.8 0 0 1-8.6-3.1 19.4 19.4 0 0 1-6-6A19.8 19.8 0 0 1 2.1 4.2 2 2 0 0 1 4.1 2h3a2 2 0 0 1 2 1.7c.1.9.3 1.7.6 2.5a2 2 0 0 1-.4 2.1L8 9.6a16 16 0 0 0 6.4 6.4l1.3-1.3a2 2 0 0 1 2.1-.4c.8.3 1.6.5 2.5.6a2 2 0 0 1 1.7 2z\"></path>\n</svg>\n</button>\n<button aria-label=\"DM 设置\" class=\"dm-chat-top-icon\" id=\"dmChatMenuBtn\" type=\"button\">\n<svg viewbox=\"0 0 24 24\"><path d=\"M5 8h14M5 16h14\"></path></svg>\n</button>\n</div>\n<div class=\"dm-chat-scroll\" id=\"dmChatScroll\">\n<section class=\"dm-chat-id-card\">\n<div class=\"dm-chat-big-avatar\" id=\"dmChatBigAvatar\">A</div>\n<div class=\"dm-chat-card-name\" id=\"dmChatCardName\">芙芙</div>\n<div class=\"dm-chat-card-handle\" id=\"dmChatCardHandle\">@FufuA1</div>\n<div class=\"dm-chat-card-stats\" id=\"dmChatCardStats\">518 粉丝 · 15 帖子</div>\n<div class=\"dm-chat-card-actions\">\n<div class=\"dm-chat-card-action home-action\" id=\"dmChatHomeAction\">\n<div class=\"circle\">\n<svg viewbox=\"0 0 24 24\"><circle cx=\"12\" cy=\"8\" r=\"4\"></circle><path d=\"M4 21c1.4-4.4 4-6.5 8-6.5s6.6 2.1 8 6.5\"></path></svg>\n</div>\n<span>主页</span>\n</div>\n<div class=\"dm-chat-card-action\">\n<div class=\"circle\">\n<svg viewbox=\"0 0 24 24\"><path d=\"M12 3 20 6v6c0 5-3.2 8-8 9-4.8-1-8-4-8-9V6z\"></path><path d=\"m9 12 2 2 4-4\"></path></svg>\n</div>\n<span>安全提示</span>\n</div>\n<div class=\"dm-chat-card-action\">\n<div class=\"circle\">\n<svg viewbox=\"0 0 24 24\"><circle cx=\"12\" cy=\"12\" r=\"9\"></circle><path d=\"M5.8 18.2 18.2 5.8\"></path></svg>\n</div>\n<span>拉黑</span>\n</div>\n</div>\n</section>\n<div class=\"dm-chat-time\">今天 22:12</div>\n<div class=\"dm-chat-messages\" id=\"dmChatMessages\"></div>\n<div class=\"dm-shared-cards\" id=\"dmSharedCardsSlot\"></div>\n<div class=\"dm-typing\" id=\"dmTyping\">\n<div class=\"dm-msg-avatar\">A</div>\n<div class=\"dm-typing-dots\"><span></span><span></span><span></span></div>\n</div>\n</div>\n<div class=\"dm-composer-wrap\">\n<div class=\"dm-composer\">\n<input class=\"dm-msg-input\" id=\"dmMsgInput\" placeholder=\"发送消息……\"/>\n<button aria-label=\"召唤 AI 回复\" class=\"dm-composer-btn plane-btn dm-sent-btn\" id=\"dmAIPlaneBtn\" type=\"button\">sent</button>\n</div>\n</div>\n<div class=\"dm-chat-glass-dim\" id=\"dmChatDim\"></div>\n<section class=\"dm-chat-glass-card\" id=\"dmEmojiCard\">\n<div class=\"dm-chat-glass-head\">\n<div class=\"dm-chat-glass-title\">发送表情</div>\n<button class=\"dm-chat-glass-close\" data-dm-chat-close=\"\" type=\"button\">×</button>\n</div>\n<div class=\"dm-emoji-grid\">\n<button class=\"dm-emoji-btn\" type=\"button\">❤️</button>\n<button class=\"dm-emoji-btn\" type=\"button\">😭</button>\n<button class=\"dm-emoji-btn\" type=\"button\">🥺</button>\n<button class=\"dm-emoji-btn\" type=\"button\">😂</button>\n<button class=\"dm-emoji-btn\" type=\"button\">🔥</button>\n<button class=\"dm-emoji-btn\" type=\"button\">👍</button>\n<button class=\"dm-emoji-btn\" type=\"button\">😳</button>\n<button class=\"dm-emoji-btn\" type=\"button\">😡</button>\n<button class=\"dm-emoji-btn\" type=\"button\">💘</button>\n<button class=\"dm-emoji-btn\" type=\"button\">😴</button>\n<button class=\"dm-emoji-btn\" type=\"button\">✨</button>\n<button class=\"dm-emoji-btn\" type=\"button\">🙏</button>\n</div>\n</section>\n<section class=\"dm-chat-glass-card top\" id=\"dmChatMenuCard\">\n<div class=\"dm-chat-glass-head\">\n<div class=\"dm-chat-glass-title\">DM 设置</div>\n<button class=\"dm-chat-glass-close\" data-dm-chat-close=\"\" type=\"button\">×</button>\n</div>\n<button class=\"dm-menu-action danger\" id=\"dmClearOpenBtn\" type=\"button\">清除聊天记录</button>\n</section>\n<section class=\"dm-chat-glass-card top\" id=\"dmClearConfirmCard\">\n<div class=\"dm-chat-glass-head\">\n<div class=\"dm-chat-glass-title\">清除聊天记录？</div>\n<button class=\"dm-chat-glass-close\" data-dm-chat-close=\"\" type=\"button\">×</button>\n</div>\n<div class=\"dm-confirm-copy\">\n        清空当前 INS DM 会话。被清除的消息不会继续参与 DM 上下文，也不会进入之后的待写入记忆。\n      </div>\n<div class=\"dm-confirm-row\">\n<button class=\"dm-confirm-btn cancel\" data-dm-chat-close=\"\" type=\"button\">取消</button>\n<button class=\"dm-confirm-btn danger\" id=\"dmClearConfirmBtn\" type=\"button\">清除</button>\n</div>\n</section>\n</section>\n<section class=\"route-page\" id=\"routeSocialCircle\">\n<div class=\"route-head\">\n<button class=\"route-back\" data-route-back=\"\" type=\"button\">‹</button>\n<div class=\"route-title\">固定社交范围</div>\n<div class=\"route-right\">＋</div>\n</div>\n<div class=\"route-body\">\n<div class=\"route-card\">\n<strong>Wujo_</strong>\n        固定社交 NPC · 与 user 的关系：网友\n        <div style=\"margin-top:10px\"><button class=\"route-btn light\" type=\"button\">修改关系</button></div>\n</div>\n<div class=\"route-card\">\n<strong>陌生人 → 固定 NPC</strong>\n        陌生人被提升为固定联系人 后会出现在这里和 DM 列表，并可修改与 user 的关系。\n      </div>\n</div>\n</section>\n<section class=\"route-page\" id=\"routeMemoryBridge\">\n<div class=\"route-head\">\n<button class=\"route-back\" data-route-back=\"\" type=\"button\">‹</button>\n<div class=\"route-title\">INS 记忆同步</div>\n<div class=\"route-right\"></div>\n</div>\n<div class=\"ins-memory-body\">\n<div class=\"ins-memory-hero\">\n<strong>角色相关事件只存一份，角色只保存“我知道了什么”</strong>\n<span>Roche → INS 只读取你为该角色手动开启的会话；INS → Roche 关闭插件时只回传到你手动开启的单聊。群聊可挂载读取，但不会作为回传目标。</span>\n<div class=\"ins-memory-flow\">\n<span>Social<br/>Event</span>\n<span>Knowledge<br/>Ledger</span>\n<span>角色视角<br/>格式化</span>\n<span>Roche<br/>单聊</span>\n</div>\n</div>\n<div class=\"ins-memory-stat-grid\" id=\"insMemoryStats\"></div>\n<div class=\"ins-memory-title\">Roche 角色 · 记忆关联</div>\n<div class=\"ins-memory-sub\">每个角色只读取你手动挂载的 Roche 会话；每个会话独立设置短期条数 / 事实条数 / 核心记忆。只有开启的单聊可以作为关闭 INS 时的回传目标。</div>\n<div class=\"ins-memory-role-list\" id=\"insMemoryRoleList\"></div>\n<div class=\"ins-memory-rule\">\n<strong>公开不等于已知：</strong>\n        陌生人与陌生人的闲聊默认只是平台内容，不进入角色记忆。只有内容涉及正式人物后才升级为 Event；而 Event 也不会自动变成角色已知，仍需实际刷到、被 @、收到分享或明确通知。\n      </div>\n<div class=\"ins-memory-title\">最近角色相关事件</div>\n<div class=\"ins-memory-sub\">纯路人内容不会进入这里。角色发布、直接互动、公开提及等才会成为 Event；“已知”仍只是轻量 Knowledge 索引。</div>\n<div class=\"ins-memory-event-list\" id=\"insMemoryEventList\"></div>\n<div class=\"ins-memory-rule\">\n<strong>短期社交回传不调用 AI：</strong>世界事件先按角色 Knowledge 过滤，再用第一人称模板直接组成一批记忆。只有以后要压缩成长事实记忆时才需要单独的 AI 总结。\n      </div>\n</div>\n<div class=\"ins-memory-sheet-dim\" id=\"insMemorySheetDim\"></div>\n<section class=\"ins-memory-sheet\" id=\"insMemoryPreviewSheet\">\n<div class=\"ins-memory-sheet-head\">\n<strong id=\"insMemoryPreviewTitle\">回传预览</strong>\n<button class=\"ins-memory-sheet-close\" id=\"insMemoryPreviewClose\" type=\"button\">×</button>\n</div>\n<div class=\"ins-memory-sync-meta\" id=\"insMemoryPreviewMeta\"></div>\n<div class=\"ins-memory-preview\" id=\"insMemoryPreviewText\"></div>\n<button class=\"ins-memory-disabled\" disabled=\"\" type=\"button\">正式 Roche 写回由 INS 记忆桥接接入</button>\n</section>\n<section class=\"ins-memory-sheet\" id=\"insMemorySourceSheet\">\n<div class=\"ins-memory-sheet-head\">\n<strong id=\"insMemorySourceTitle\">记忆来源</strong>\n<button class=\"ins-memory-sheet-close\" id=\"insMemorySourceClose\" type=\"button\">×</button>\n</div>\n<div class=\"ins-memory-sync-meta\" id=\"insMemorySourceMeta\"></div>\n<div class=\"ins-memory-discovery-row\">\n<div><strong>公开内容发现倾向</strong><span>只影响“别人公开提到这个角色但没有 @”时，ta 自己刷到的概率；@ / 直接回复 / 分享仍按明确通知处理。</span></div>\n<select aria-label=\"公开内容发现倾向\" id=\"insMemoryDiscoverySelect\">\n<option value=\"low\">低</option>\n<option value=\"normal\">普通</option>\n<option value=\"high\">高</option>\n</select>\n</div>\n<div class=\"ins-memory-source-list\" id=\"insMemorySourceList\"></div>\n<div class=\"ins-memory-writeback\" id=\"insMemoryWritebackInfo\"></div>\n<button class=\"ins-memory-source-refresh\" id=\"insMemorySourceRefresh\" type=\"button\">刷新 Roche 会话</button>\n</section>\n</section>\n<section class=\"route-page\" id=\"routePeopleRelations\">\n<div class=\"route-head\">\n<button class=\"route-back\" data-route-back=\"\" type=\"button\">‹</button>\n<div class=\"route-title\">人物与关系</div>\n<div class=\"route-right\"></div>\n</div>\n<div class=\"pr-route-body\">\n<div class=\"pr-tabs\">\n<button class=\"pr-tab active\" data-pr-tab=\"actors\" type=\"button\">人物库</button>\n<button class=\"pr-tab\" data-pr-tab=\"relations\" type=\"button\">关系卡片</button>\n</div>\n<div class=\"pr-pane show\" id=\"prActorsPane\">\n<div class=\"pr-section-title\">人物库</div>\n<div class=\"pr-section-sub\">\n          人物库只回答“INS里有谁”。User、角色、社交联系人都只存在一个身份，临时陌生人不进入正式人物库。\n          <span class=\"pr-inline-system-rule\">系统规则：“人物库里存在” ≠ “彼此认识”。只有建立关系边以后，AI 才能把两个人当成认识。</span>\n        </div>\n<div class=\"pr-actor-list\" id=\"prActorList\"></div>\n</div>\n<div class=\"pr-pane\" id=\"prRelationsPane\">\n<div class=\"pr-focus-card\" id=\"prFocusCard\"></div>\n<div class=\"pr-focus-switch\" id=\"prFocusSwitch\"></div>\n<div class=\"pr-actions\">\n<button class=\"pr-action\" id=\"prAddRelationBtn\" type=\"button\">＋ 建立 / 修改关系</button>\n</div>\n<div class=\"pr-section-title\">直接关系</div>\n<div class=\"pr-section-sub\">\n          这里显示的是同一张全局关系图，以当前人物为中心的一度关系。手动保存的关系会标记为锁定，后续 AI 不应擅自覆盖。\n        </div>\n<div class=\"pr-rel-list\" id=\"prRelationList\"></div>\n</div>\n</div>\n<div class=\"pr-sheet-dim\" id=\"prSheetDim\"></div>\n<section class=\"pr-sheet\" id=\"prRelationSheet\">\n<div class=\"pr-sheet-head\">\n<strong id=\"prSheetTitle\">建立关系</strong>\n<button class=\"pr-sheet-close\" id=\"prSheetClose\" type=\"button\">×</button>\n</div>\n<label class=\"pr-field\">\n<span>关联人物</span>\n<select class=\"pr-select\" id=\"prOtherActor\"></select>\n</label>\n<label class=\"pr-field\">\n<span id=\"prForwardLabel\">A 看 B</span>\n<input class=\"pr-input\" id=\"prForwardInput\" placeholder=\"例如：哥哥 / 朋友 / 同事 / 认识\"/>\n</label>\n<label class=\"pr-field\">\n<span id=\"prBackwardLabel\">B 看 A</span>\n<input class=\"pr-input\" id=\"prBackwardInput\" placeholder=\"例如：妹妹 / 朋友 / 同事 / 认识\"/>\n</label>\n<div class=\"pr-sheet-note\">\n        这两条是有方向的关系。保存后写入同一张总关系图；没有填写的一边不会自动猜。\n      </div>\n<div class=\"pr-sheet-actions\">\n<button class=\"pr-cancel\" id=\"prRelationCancel\" type=\"button\">取消</button>\n<button class=\"pr-save\" id=\"prRelationSave\" type=\"button\">保存关系</button>\n</div>\n<button class=\"pr-delete\" id=\"prRelationDelete\" type=\"button\">删除这组关系</button>\n</section>\n</section>\n<div class=\"route-sheet-backdrop\" id=\"sheetComments\">\n<div class=\"route-sheet\">\n<div class=\"route-grab\"></div>\n<div class=\"comment-sheet-head\">\n<div></div>\n<div class=\"comment-sheet-title\">评论</div>\n<button aria-label=\"刷新评论\" class=\"comment-refresh\" id=\"commentRefreshBtn\" type=\"button\">\n<svg viewbox=\"0 0 24 24\">\n<path d=\"M20 6v5h-5\"></path>\n<path d=\"M19 11a7 7 0 1 0 1 5\"></path>\n</svg>\n</button>\n</div>\n<div class=\"comment-list\" id=\"commentList\"></div>\n<button class=\"feed-comment-load-more\" id=\"commentLoadMoreBtn\" type=\"button\">再加载 5 条评论</button>\n<div class=\"comment-composer\">\n<input class=\"comment-input\" id=\"commentInput\" placeholder=\"添加评论…\"/>\n<button class=\"comment-send\" id=\"commentSendBtn\" type=\"button\">发布</button>\n</div>\n</div>\n</div>\n<div class=\"dm-share-toast\" id=\"dmShareToast\">已发送</div>\n<div class=\"route-sheet-backdrop\" id=\"sheetShareDM\">\n<div class=\"route-sheet\">\n<div class=\"route-grab\"></div>\n<div class=\"route-sheet-title\">分享到 DM</div>\n<div id=\"feedShareDMList\"></div>\n</div>\n</div>\n<!-- v1.06 User Profile drawer / edit card -->\n<div class=\"profile-drawer-dim\" id=\"profileDrawerDim\"></div>\n<aside class=\"profile-drawer\" id=\"profileDrawer\">\n<div class=\"drawer-head\">\n<div class=\"drawer-title\">INS 控制中心</div>\n<button class=\"drawer-close\" id=\"profileDrawerClose\" type=\"button\">×</button>\n</div>\n<div class=\"drawer-section\">\n<button class=\"drawer-item\" id=\"drawerEditProfileBtn\" type=\"button\">\n<span class=\"drawer-icon\">🖊️</span>\n<span class=\"drawer-copy\"><strong>编辑User主页</strong><span>昵称、ID、简介、头像</span></span>\n<span class=\"drawer-arrow\">›</span>\n</button>\n</div>\n<div class=\"drawer-section\">\n<button class=\"drawer-item\" id=\"drawerMemorySystemBtn\" type=\"button\">\n<span class=\"drawer-icon\">🧠</span>\n<span class=\"drawer-copy\"><strong>记忆系统</strong><span>世界事件、角色已知记录、Roche 回传预览</span></span>\n<span class=\"drawer-arrow\">›</span>\n</button>\n<button class=\"drawer-item\" id=\"drawerImagePromptBtn\" type=\"button\">\n<span class=\"drawer-icon\">📷</span>\n<span class=\"drawer-copy\"><strong>生图提示词</strong><span>留空 = 继承 Roche 全局生图提示词；填写 = INS 内优先使用</span></span>\n<span class=\"drawer-arrow\">›</span>\n</button>\n<button class=\"drawer-item\" id=\"drawerAtmosphereBtn\" type=\"button\">\n<span class=\"drawer-icon\">📋</span>\n<span class=\"drawer-copy\"><strong>INS平台运行守则</strong><span>固定核心规则 + 可编辑世界观 / 网络文化层</span></span>\n<span class=\"drawer-arrow\">›</span>\n</button>\n<button class=\"drawer-item\" id=\"drawerPeopleRelationsBtn\" type=\"button\">\n<span class=\"drawer-icon\">👨‍👩‍👧‍👦</span>\n<span class=\"drawer-copy\"><strong>人物与关系</strong><span>人物库、卡片式关系网、统一人物引用</span></span>\n<span class=\"drawer-arrow\">›</span>\n</button>\n</div>\n<div class=\"drawer-section\">\n<button class=\"drawer-item\" id=\"drawerClearImageCacheBtn\" type=\"button\">\n<span class=\"drawer-icon\">🧹</span>\n<span class=\"drawer-copy\"><strong>清空图片缓存</strong><span>清除识图摘要与 INS 临时图片缓存；不会删除私人相册</span></span>\n<span class=\"drawer-arrow\">›</span>\n</button>\n<button class=\"drawer-item drawer-item-danger\" id=\"drawerClearDataBtn\" type=\"button\">\n<span class=\"drawer-icon\">🗑️</span>\n<span class=\"drawer-copy\"><strong>清空插件数据</strong><span>重新开始 INS；会清空相册、设置、DM 与社交事件</span></span>\n<span class=\"drawer-arrow\">›</span>\n</button>\n</div>\n<div class=\"drawer-note\">\n      Build By Raccoon001<br>\n      Bug反馈/功能许愿：#腾讯频道：pd38679095\n    </div>\n</aside>\n<section class=\"ins-interface-size-panel\" id=\"insInterfaceSizePanel\" aria-hidden=\"true\">\n<header class=\"ins-interface-size-head\">\n<strong>界面尺寸调整</strong>\n<button id=\"insInterfaceSizeClose\" type=\"button\" aria-label=\"关闭\">×</button>\n</header>\n<div class=\"ins-interface-size-body\">\n<p class=\"ins-interface-size-help\">拖动滑块实时预览，数值会自动保存，并同步应用到所有页面。</p>\n<div class=\"ins-interface-size-row ins-interface-size-row-top\">\n<div class=\"ins-interface-size-copy\"><strong>顶栏高度</strong><span id=\"insTopSafeValue\">44px</span></div>\n<p>apk用户调整此处避开状态栏。</p>\n<input id=\"insTopSafeRange\" type=\"range\" min=\"0\" max=\"80\" step=\"1\" value=\"44\" aria-label=\"顶栏高度\"/>\n</div>\n<div class=\"ins-interface-size-row ins-interface-size-row-bottom\">\n<div class=\"ins-interface-size-copy\"><strong>底栏高度</strong><span id=\"insBottomSafeValue\">26px</span></div>\n<p>调整底栏占用面积避免互动重叠。</p>\n<input id=\"insBottomSafeRange\" type=\"range\" min=\"0\" max=\"56\" step=\"1\" value=\"26\" aria-label=\"底栏高度\"/>\n</div>\n</div>\n<footer class=\"ins-interface-size-actions\">\n<button class=\"secondary\" id=\"insInterfaceSizeReset\" type=\"button\">恢复默认</button>\n<button class=\"primary\" id=\"insInterfaceSizeDone\" type=\"button\">完成</button>\n</footer>\n</section>\n<div class=\"route-sheet-backdrop\" id=\"identityBindSheet\">\n<div class=\"route-sheet\">\n<div class=\"route-grab\"></div>\n<div class=\"route-sheet-title\">切换绑定 user 身份</div>\n<div class=\"identity-bind-list\" id=\"identityBindList\"></div>\n</div>\n</div>\n<section class=\"profile-setting-card\" id=\"profileAtmosphereCard\">\n<div class=\"profile-setting-head\">\n<strong>INS平台运行守则</strong>\n<button class=\"profile-setting-close\" id=\"profileAtmosphereClose\" type=\"button\">×</button>\n</div>\n<div class=\"profile-setting-block\">\n<label class=\"profile-setting-label\">固定核心规则（不可覆盖）</label>\n<div class=\"profile-setting-rulelist\">\n<div>• 平台持续运行；人物不围绕 User 转，并保留各自独立生活。</div>\n<div>• 陌生人纯路人内容只是平台 Content；只有涉及正式人物才升级 Event。公开可见 ≠ 已经看见；Knowledge 仍按真实曝光 / 分享 / @ / 通知进入。</div>\n<div>• 人物库存在 ≠ 彼此认识；关系事实、记忆边界与输出结构优先于自定义世界观。</div>\n<div>• Feed / Story / Reels / 评论 / DM 的产品逻辑不会被世界观文本覆盖。</div>\n</div>\n</div>\n<div class=\"profile-setting-block\">\n<label class=\"profile-setting-label\">可编辑世界观 / 网络文化层</label>\n<textarea class=\"profile-setting-textarea world-prompt\" id=\"profileWorldCulturePrompt\"></textarea>\n<div class=\"profile-setting-notebox\" style=\"margin-top:9px\">\n        已内置当前使用的现实世界版本。你可以按自己喜欢的世界观直接修改，例如赛博、古风、魔法、架空地区或自定义网络文化。<br/><br/>\n        自定义内容只负责“这个世界是什么样、这里的人怎么起账号和使用网络”；如果与平台核心规则、人物事实、记忆或输出格式冲突，以固定规则为准。自定义生成效果不作保证。\n      </div>\n<div class=\"profile-setting-actions\">\n<button class=\"profile-setting-reset\" id=\"profileAtmosphereReset\" type=\"button\">恢复内置默认</button>\n<button class=\"profile-setting-save\" id=\"profileAtmosphereSave\" type=\"button\">保存提示词</button>\n</div>\n</div>\n</section>\n<section class=\"profile-setting-card\" id=\"profileImagePromptCard\">\n<div class=\"profile-setting-head\">\n<strong>INS 生图提示词</strong>\n<button class=\"profile-setting-close\" id=\"profileImagePromptClose\" type=\"button\">×</button>\n</div>\n<div class=\"profile-setting-block\">\n<label class=\"profile-setting-label\">优先级说明</label>\n<div class=\"profile-setting-notebox\">\n        这里的提示词是 <b>INS 专属覆盖层</b>。<br/>\n<b>留空</b>：默认继承 Roche 小手机的全局生图提示词。<br/>\n<b>填写</b>：生成 Feed / Story / Reels 图片时，优先把这里的提示词提交给 INS 的生图链路。<br/><br/>\n        实际提交时的思路：<br/>\n<b>INS 生图提示词（如有）</b> + <b>AI 输出的图片描述</b> + <b>可选负向提示词</b>。\n      </div>\n</div>\n<div class=\"profile-setting-block\">\n<label class=\"profile-setting-label\">图片描述语言</label>\n<select class=\"profile-setting-select\" id=\"insImagePromptLanguage\">\n<option value=\"zh\">中文（默认）</option>\n<option value=\"en\">英文（适合只懂英文的生图模型）</option>\n</select>\n</div>\n<div class=\"profile-setting-block\">\n<label class=\"profile-setting-label\">INS 专属正向提示词</label>\n<textarea class=\"profile-setting-textarea\" id=\"insImagePromptPositive\" placeholder=\"留空 = 继承 Roche 全局生图提示词；填写 = INS 内优先使用\"></textarea>\n</div>\n<div class=\"profile-setting-block\">\n<label class=\"profile-setting-label\">INS 专属负向提示词</label>\n<textarea class=\"profile-setting-textarea\" id=\"insImagePromptNegative\" placeholder=\"留空 = 不额外传负向提示词\"></textarea>\n</div>\n<button class=\"profile-setting-save\" id=\"profileImagePromptSave\" type=\"button\">保存生图设置</button>\n</section>\n<section class=\"profile-edit-card\" id=\"profileEditCard\">\n<div class=\"profile-edit-title\">\n      编辑User主页\n      <button class=\"profile-edit-close\" id=\"profileEditClose\" type=\"button\">×</button>\n</div>\n<div class=\"profile-avatar-preview\" id=\"profileAvatarPreview\">U</div>\n<label class=\"profile-edit-label\">头像 URL</label>\n<input class=\"profile-edit-input\" id=\"profileEditAvatar\" placeholder=\"留空则使用默认头像\"/>\n<div class=\"profile-edit-mini\">\n<button id=\"profileAvatarApplyBtn\" type=\"button\">预览头像</button>\n<button id=\"profileAvatarResetBtn\" type=\"button\">恢复默认</button>\n</div>\n<label class=\"profile-edit-label\">昵称</label>\n<input class=\"profile-edit-input\" id=\"profileEditName\"/>\n<label class=\"profile-edit-label\">IG ID</label>\n<input class=\"profile-edit-input\" id=\"profileEditId\"/>\n<label class=\"profile-edit-label\">地理位置</label>\n<input class=\"profile-edit-input\" id=\"profileEditLocation\" placeholder=\"例如 Seoul·Korea\"/>\n<div class=\"profile-edit-row\">\n<div>\n<label class=\"profile-edit-label\">粉丝数</label>\n<input class=\"profile-edit-input\" id=\"profileEditFollowers\" inputmode=\"numeric\"/>\n</div>\n<div>\n<label class=\"profile-edit-label\">关注数</label>\n<input class=\"profile-edit-input\" id=\"profileEditFollowing\" inputmode=\"numeric\"/>\n</div>\n</div>\n<label class=\"profile-edit-label\">简介</label>\n<textarea class=\"profile-edit-textarea\" id=\"profileEditBio\"></textarea>\n<button class=\"profile-edit-save\" id=\"profileEditSave\" type=\"button\">保存修改</button>\n</section>\n<section class=\"highlight-editor-card\" id=\"highlightEditorCard\">\n<div class=\"highlight-editor-title\">\n<span id=\"highlightEditorTitle\">编辑 Highlight</span>\n<button class=\"highlight-close\" id=\"highlightEditorClose\" type=\"button\">×</button>\n</div>\n<div class=\"highlight-editor-scroll\">\n<div class=\"highlight-preview\" id=\"highlightPreview\">✦</div>\n<label class=\"highlight-field-label\">名称</label>\n<input class=\"highlight-input\" id=\"highlightNameInput\" placeholder=\"例如：日常\"/>\n<label class=\"highlight-field-label\">封面 URL</label>\n<input class=\"highlight-input\" id=\"highlightCoverInput\" placeholder=\"粘贴图片 URL 替换封面\"/>\n<div class=\"highlight-purpose-card\">\n<div class=\"highlight-purpose-head\">\n<div class=\"highlight-purpose-title\">分类用途说明</div>\n<div class=\"highlight-purpose-count\" id=\"highlightDescriptionCount\">0 / 160</div>\n</div>\n<textarea class=\"highlight-purpose-textarea\" id=\"highlightDescriptionInput\" maxlength=\"160\" placeholder=\"例如：旅行期间想保留的街景、吃饭和朋友片段。以后这个 Highlight 就按这个用途收内容。\"></textarea>\n<div class=\"highlight-purpose-hint\">\n        这不是 AI 提示词，只是说明这个 Highlight 是拿来收什么内容的。可以随时重新编辑。\n      </div>\n</div>\n<div class=\"highlight-content-box\">\n<div class=\"highlight-content-title\">绑定 Story 记录</div>\n<div class=\"highlight-story-bind-list\" id=\"highlightStoryBindingList\"></div>\n</div>\n</div>\n<div class=\"highlight-actions\">\n<button class=\"highlight-btn cancel\" id=\"highlightCancelBtn\" type=\"button\">取消</button>\n<button class=\"highlight-btn save\" id=\"highlightSaveBtn\" type=\"button\">保存</button>\n</div>\n<div class=\"highlight-delete-row\">\n<button id=\"highlightDeleteBtn\" type=\"button\">删除这个 Highlight</button>\n</div>\n</section>\n<!-- v1.12 DM List v2 glass cards -->\n<div class=\"dm-list-glass-dim\" id=\"dmListDim\"></div>\n<section class=\"dm-list-glass-card\" id=\"dmNoteCard\">\n<div class=\"dm-list-glass-head\">\n<div class=\"dm-list-glass-title\">当前状态</div>\n<button class=\"dm-list-glass-close\" data-dm-list-close=\"\" type=\"button\">×</button>\n</div>\n<div class=\"dm-list-block\">\n<div class=\"dm-list-label\">你的 note</div>\n<input class=\"dm-list-input\" id=\"dmNoteInput\" maxlength=\"24\" value=\"在干嘛呢\"/>\n<button class=\"dm-list-btn\" id=\"dmSaveNoteBtn\" type=\"button\">保存状态</button>\n</div>\n</section>\n<section class=\"dm-list-glass-card\" id=\"dmImportCard\">\n<div class=\"dm-list-glass-head\">\n<div class=\"dm-list-glass-title\">绑定角色</div>\n<button class=\"dm-list-glass-close\" data-dm-list-close=\"\" type=\"button\">×</button>\n</div>\n<div class=\"dm-registry-note\">角色不会自动进入 INS，请在此处逐个绑定，此处为唯一绑定及解绑角色位置，关注和取消关注不影响绑定与解绑。双向记忆挂载请至 User 主页设置界面。此处仅显示角色，社交联系人与陌生人不在此处显示。</div>\n<div class=\"dm-list-block\" id=\"dmImportActorList\"></div>\n</section>\n<section class=\"dm-list-glass-card\" id=\"dmUserSocialCard\">\n<div class=\"dm-list-glass-head\">\n<div class=\"dm-list-glass-title\">社交联系人</div>\n<button class=\"dm-list-glass-close\" data-dm-list-close=\"\" type=\"button\">×</button>\n</div>\n<div class=\"dm-list-block\">\n<div class=\"dm-user-hub-label\">已保存联系人</div>\n<div class=\"dm-user-hub-sub\">即 User 保存在本插件内部的社交关系，不会导出到 Roche，可理解为 INS 网友，支持新建、编辑主页、编辑关系或删除。陌生人可通过加入陌生消息页面升级为社交联系人，见陌生消息页面。</div>\n<div id=\"dmUserSocialActorList\"></div>\n<button class=\"dm-list-btn\" id=\"dmQuickAddNpcBtn\" style=\"margin-top:9px\" type=\"button\">＋ 新建社交联系人</button>\n<div class=\"dm-user-add-form\" id=\"dmQuickAddNpcForm\">\n<div class=\"dm-user-add-grid\">\n<label class=\"dm-user-add-field\">\n<span>昵称</span>\n<input id=\"dmQuickNpcName\" placeholder=\"例如 小A\"/>\n</label>\n<label class=\"dm-user-add-field\">\n<span>IG ID</span>\n<input id=\"dmQuickNpcHandle\" placeholder=\"例如 A_0102\"/>\n</label>\n</div>\n<div class=\"dm-user-add-grid\">\n<label class=\"dm-user-add-field\">\n<span>User → 对方</span>\n<input id=\"dmQuickNpcForwardRel\" placeholder=\"例如 网友\"/>\n</label>\n<label class=\"dm-user-add-field\">\n<span>对方 → User</span>\n<input id=\"dmQuickNpcBackwardRel\" placeholder=\"例如 网友\"/>\n</label>\n</div>\n<div class=\"dm-user-add-actions\">\n<button class=\"dm-user-add-cancel\" id=\"dmQuickNpcCancel\" type=\"button\">取消</button>\n<button class=\"dm-user-add-save\" id=\"dmQuickNpcSave\" type=\"button\">保存社交联系人</button>\n</div>\n</div>\n<div class=\"dm-user-hub-divider\"></div>\n<div class=\"dm-list-contact\" style=\"margin-top:8px\">\n<div class=\"dm-list-contact-avatar\">?</div>\n<div class=\"dm-list-contact-copy\">\n<strong>陌生人生成范围</strong>\n<span>刷新数量、触发来源、陌生人氛围</span>\n</div>\n<button class=\"dm-list-contact-action\" id=\"dmStrangerScopeToggle\" type=\"button\">展开</button>\n</div>\n<div class=\"social-stranger-settings\" id=\"dmStrangerScopeSettings\">\n<div class=\"social-setting-row\">\n<div class=\"social-setting-copy\"><strong>每次刷新请求数</strong><span>手动刷新一次生成 3–5 个陌生请求</span></div>\n<select id=\"dmStrangerBatchSize\"><option value=\"3\">3</option><option selected=\"\" value=\"4\">4</option><option value=\"5\">5</option></select>\n</div>\n<div class=\"social-setting-row\">\n<div class=\"social-setting-copy\"><strong>触发来源</strong><span>哪些公开行为允许陌生人找到 User</span></div>\n</div>\n<div class=\"social-setting-check\">\n<button class=\"social-setting-chip active\" data-social-trigger=\"public_post\" type=\"button\">User 公开发帖</button>\n<button class=\"social-setting-chip active\" data-social-trigger=\"char_comment\" type=\"button\">评论角色动态</button>\n<button class=\"social-setting-chip\" data-social-trigger=\"reel\" type=\"button\">Reels 互动</button>\n</div>\n<div class=\"social-setting-row\" style=\"display:block\">\n<div class=\"social-setting-copy\"><strong>陌生人氛围提示</strong><span>控制大概出现什么圈层、语气和社交距离</span></div>\n<textarea class=\"social-setting-textarea\" id=\"dmStrangerScopePrompt\" placeholder=\"例如：以同龄兴趣圈为主，语气自然，不要太自来熟，不要每个人都夸 User。\"></textarea>\n</div>\n<button class=\"dm-list-btn\" id=\"dmStrangerScopeSave\" style=\"margin-top:9px\" type=\"button\">保存陌生人范围</button>\n</div>\n<div class=\"dm-registry-note\">完整关系见 User 主页设置页面。</div>\n</div>\n</section>\n<section class=\"dm-list-glass-card\" id=\"dmStrangerCard\">\n<div class=\"dm-list-glass-head\">\n<div class=\"dm-list-glass-title\">陌生消息</div>\n<button class=\"dm-list-glass-close\" data-dm-list-close=\"\" type=\"button\">×</button>\n</div>\n<div class=\"dm-list-block\">\n<div class=\"dm-list-copy\" id=\"dmStrangerRequestCopy\">当前没有陌生请求。正式版里，只有 User 公开发帖或评论后，才会允许手动刷新 3–5 条陌生消息请求。</div>\n<div class=\"dm-stranger-request-list\" id=\"dmStrangerRequestList\"></div>\n<button class=\"dm-list-btn\" id=\"dmRefreshStrangersBtn\" type=\"button\">刷新请求</button>\n</div>\n</section>\n<section class=\"dm-list-glass-card dm-temp-chat-card\" id=\"dmStrangerTempCard\">\n<div class=\"dm-list-glass-head\">\n<div class=\"dm-list-glass-title\">临时会话</div>\n</div>\n<div class=\"dm-list-block\">\n<div class=\"dm-temp-meta\">\n<div class=\"dm-temp-avatar\" id=\"dmTempAvatar\">?</div>\n<div class=\"dm-temp-meta-main\">\n<strong id=\"dmTempHandle\">@stranger</strong>\n<span>临时陌生会话</span>\n</div>\n</div>\n<div class=\"dm-temp-tip\">陌生人可维持临时会话窗口沟通，也可提升为社交联系人，不会导出到 Roche。</div>\n<div class=\"dm-temp-messages\" id=\"dmTempMessages\"></div>\n<div class=\"dm-temp-composer\">\n<input id=\"dmTempInput\" placeholder=\"回复这个陌生人…\"/>\n<button id=\"dmTempSendBtn\" type=\"button\">发送</button>\n</div>\n<div class=\"dm-temp-actions\">\n<button class=\"primary\" id=\"dmTempSaveToList\" type=\"button\">保留此临时会话窗口至消息列表</button>\n<button id=\"dmTempPromoteNpc\" type=\"button\">提升为社交联系人</button>\n<button class=\"danger\" id=\"dmTempDelete\" type=\"button\">删除临时会话</button>\n<button data-dm-list-close=\"\" type=\"button\">关闭</button>\n</div>\n</div>\n</section>\n<!-- v1.15 Reels glass sheets -->\n<div class=\"reels-v15-dim\" id=\"reelsV15Dim\"></div>\n<div class=\"reels-v15-toast\" id=\"reelsV15Toast\">已更新</div>\n<section class=\"reels-v15-sheet\" id=\"reelsV15CommentSheet\">\n<div class=\"reels-v15-sheet-head\">\n<div class=\"reels-v15-sheet-title\">评论</div>\n<button class=\"reels-v15-sheet-close\" data-reels-v15-close=\"\" type=\"button\">×</button>\n</div>\n<div class=\"reels-v15-comment-list\" id=\"reelsV15CommentList\"></div>\n<button class=\"reels-v15-comment-load-more\" id=\"reelsV15CommentRefresh\" type=\"button\">再加载 5 条评论</button>\n<div class=\"reels-v15-composer\">\n<input id=\"reelsV15CommentInput\" placeholder=\"添加评论…\"/>\n<button id=\"reelsV15CommentSend\" type=\"button\">发送</button>\n</div>\n</section>\n<section class=\"reels-v15-sheet\" id=\"reelsV15DMSheet\">\n<div class=\"reels-v15-sheet-head\">\n<div class=\"reels-v15-sheet-title\">分享到 DM</div>\n<button class=\"reels-v15-sheet-close\" data-reels-v15-close=\"\" type=\"button\">×</button>\n</div>\n<div class=\"reels-v15-block\" id=\"reelsV15DMList\"></div>\n</section>\n<section class=\"reels-v15-sheet\" id=\"reelsV15MoreSheet\">\n<div class=\"reels-v15-sheet-head\">\n<div class=\"reels-v15-sheet-title\">更多</div>\n<button class=\"reels-v15-sheet-close\" data-reels-v15-close=\"\" type=\"button\">×</button>\n</div>\n<div class=\"reels-v15-block\">\n<div class=\"reels-v15-row clickable\" id=\"reelsV15EditAction\">\n<div class=\"reels-v15-row-main\"><strong>编辑内容</strong><span>修改视频描述、文案和 tag</span></div>\n</div>\n<div class=\"reels-v15-row clickable\" id=\"reelsV15DeleteAction\">\n<div class=\"reels-v15-row-main\"><strong class=\"reels-v15-danger\">删除</strong><span>删除当前 Reel</span></div>\n</div>\n</div>\n</section>\n<section class=\"reels-v15-sheet reels-v15-form\" id=\"reelsV15UserPostSheet\">\n<div class=\"reels-v15-sheet-head\">\n<div class=\"reels-v15-sheet-title\">发布 Reel</div>\n<button class=\"reels-v15-sheet-close\" data-reels-v15-close=\"\" type=\"button\">×</button>\n</div>\n<div class=\"reels-v15-identity\">\n<div class=\"reels-v15-identity-avatar\" id=\"reelsV15IdentityAvatar\" data-identity-avatar=\"user\">U</div>\n<div class=\"reels-v15-identity-main\">\n<strong id=\"reelsV15IdentityName\">User</strong>\n<span id=\"reelsV15IdentityHandle\">@user.ins</span>\n</div>\n</div>\n<textarea id=\"reelsV15UserVideoDesc\" placeholder=\"描述要发布的视频内容\"></textarea>\n<input id=\"reelsV15UserCaption\" placeholder=\"文案\"/>\n<input id=\"reelsV15UserTags\" placeholder=\"#tag\"/>\n<button class=\"reels-v15-primary\" id=\"reelsV15UserPublish\" type=\"button\">发布</button>\n</section>\n<section class=\"reels-v15-sheet\" id=\"reelsV15GenerateSheet\">\n<div class=\"reels-v15-sheet-head\">\n<div class=\"reels-v15-sheet-title\">生成新的 Reels</div>\n<button class=\"reels-v15-sheet-close\" data-reels-v15-close=\"\" type=\"button\">×</button>\n</div>\n<div class=\"reels-v15-copy\">\n      选择陌生人或已绑定角色 / 社交联系人，按各自人设、记忆与关系生成独立 Reel。\n    </div>\n<div class=\"batch-prompt-wrap\">\n<label class=\"batch-prompt-label\" for=\"reelsV15BatchPrompt\">这一批想看什么</label>\n<textarea class=\"batch-prompt-input\" id=\"reelsV15BatchPrompt\" placeholder=\"例如：多来一点城市夜景、随手生活流、旅行感，不要太营业。\"></textarea>\n</div>\n<div class=\"reels-v15-block\">\n<div class=\"reels-v15-chips\" id=\"reelsV15SourceChips\">\n<button class=\"reels-v15-chip active\" data-reels-v15-source=\"stranger\" type=\"button\">陌生人</button>\n</div>\n<button class=\"reels-v15-primary\" id=\"reelsV15GenerateConfirm\" type=\"button\">生成 Reel</button>\n</div>\n</section>\n<section class=\"reels-v15-sheet reels-v15-form\" id=\"reelsV15EditSheet\">\n<div class=\"reels-v15-sheet-head\">\n<div class=\"reels-v15-sheet-title\">编辑 Reel</div>\n<button class=\"reels-v15-sheet-close\" data-reels-v15-close=\"\" type=\"button\">×</button>\n</div>\n<textarea id=\"reelsV15EditDesc\" placeholder=\"视频内容 / 视频描述\"></textarea>\n<input id=\"reelsV15EditCaption\" placeholder=\"文案\"/>\n<input id=\"reelsV15EditTranslation\" placeholder=\"中文翻译（外语文案可填写）\"/>\n<input id=\"reelsV15EditTags\" placeholder=\"#tag\"/>\n<button class=\"reels-v15-primary\" id=\"reelsV15EditSave\" type=\"button\">保存修改</button>\n</section>\n<section class=\"reels-v15-sheet\" id=\"reelsV15DeleteSheet\">\n<div class=\"reels-v15-sheet-head\">\n<div class=\"reels-v15-sheet-title\">删除 Reel</div>\n<button class=\"reels-v15-sheet-close\" data-reels-v15-close=\"\" type=\"button\">×</button>\n</div>\n<div class=\"reels-v15-copy\">确定删除当前这条 Reel 吗？</div>\n<button class=\"reels-v15-primary\" id=\"reelsV15DeleteConfirm\" type=\"button\">删除</button>\n</section>\n<section class=\"reels-v15-sheet\" id=\"reelsV15PublicProfileSheet\">\n<div class=\"reels-v15-sheet-head\">\n<div class=\"reels-v15-sheet-title\">公开账号</div>\n<button class=\"reels-v15-sheet-close\" data-reels-v15-close=\"\" type=\"button\">×</button>\n</div>\n<div class=\"reels-v15-block\">\n<div class=\"reels-v15-row\">\n<div class=\"reels-v15-sheet-avatar\" id=\"reelsV15PublicAvatar\">?</div>\n<div class=\"reels-v15-row-main\"><strong id=\"reelsV15PublicHandle\">@public</strong><span>陌生公开账号 · 以后可扩展 public profile</span></div>\n</div>\n</div>\n</section>\n<nav class=\"bottom-nav light\" id=\"bottomNav\">\n<div class=\"nav-inner\">\n<div class=\"tab active\" data-tab=\"home\">\n<span class=\"tab-pill\"></span>\n<img alt=\"home\" id=\"icon-home\" src=\"data:image/png;base64,\"/>\n</div>\n<div class=\"tab\" data-tab=\"reels\">\n<span class=\"tab-pill\"></span>\n<img alt=\"reels\" id=\"icon-reels\" src=\"data:image/png;base64,\"/>\n</div>\n<div class=\"tab\" data-tab=\"compose\">\n<span class=\"tab-pill\" style=\"opacity:0!important\"></span>\n<div class=\"plus-glyph\">＋</div>\n</div>\n<div class=\"tab\" data-tab=\"dm\">\n<span class=\"tab-pill\"></span>\n<img alt=\"dm\" id=\"icon-dm\" src=\"data:image/png;base64,\"/>\n</div>\n<div class=\"tab\" data-tab=\"profile\">\n<span class=\"tab-pill\"></span>\n<div class=\"profile-chip\" data-identity-avatar=\"user\"></div>\n</div>\n</div>\n</nav>\n</div>\n\n";

  function runDirectApp(document, window, setTimeout, clearTimeout, setInterval, clearInterval, requestAnimationFrame, cancelAnimationFrame) {


/* v5.1.2 mobile diagnostics: visible only when a real runtime exception occurs. */
(function installNiniRuntimeDiagnostics(){
  let timer=0;
  const show=(message)=>{
    try{
      let el=document.getElementById('niniRuntimeDiagnostic');
      if(!el){
        el=document.createElement('div');
        el.id='niniRuntimeDiagnostic';
        el.style.cssText='position:fixed;left:10px;right:10px;bottom:78px;z-index:99999;padding:9px 11px;border-radius:12px;background:rgba(185,28,28,.94);color:#fff;font:11px/1.45 -apple-system,BlinkMacSystemFont,Segoe UI,sans-serif;box-shadow:0 6px 24px rgba(0,0,0,.22);display:none;pointer-events:none;';
        document.body.appendChild(el);
      }
      el.textContent='INS runtime · '+String(message||'unknown error').slice(0,220);
      el.style.display='block';
      clearTimeout(timer);
      timer=setTimeout(()=>{el.style.display='none';},8000);
    }catch(_e){}
  };
  window.addEventListener('error',(event)=>{
    if(event.target!==window)return;
    show(event.message||event.error?.message||'runtime error');
  });
  window.addEventListener('unhandledrejection',(event)=>{
    const reason=event.reason;
    show(reason?.message||reason||'unhandled promise rejection');
  });
})();

const ICONS={
  heart0:"https://imgbed.heliar.top/i/pbz63LCde-0-ZoaE_空心_pixian_ai.webp",
  heart1:"https://imgbed.heliar.top/i/kNrpXmT8iNdggBzr_红心_191553_副本_pixian_ai.webp",
  reply:"https://imgbed.heliar.top/i/0d6p56GNpBEonfwz_回复_pixian_ai.webp",
  repost0:"https://imgbed.heliar.top/i/0-BBc0_1k2Hl9jq7_转发_pixian_ai.webp",
  repost1:"https://imgbed.heliar.top/i/ltMwMkfzZjCsK4x9_已转发_pixian_ai.webp",
  dm:"https://imgbed.heliar.top/i/r4euFTG1Nx3YKVI9_dm_pixian_ai.webp"
};
Object.values(ICONS).forEach(url=>{const i=new Image();i.src=url;});

/* ===== v1.15.22 Feed fallback visual pool =====
   When a Feed image has no real album/generated/uploaded media yet, use one of
   these user-provided images instead of a black/gray text placeholder.
   The real semantic source remains mediaDescription; tapping the card flips
   to the description. */
const INS_FEED_FALLBACK_MEDIA_POOL = [
  {id:'fallback_01',url:'https://imgbed.heliar.top/i/oW8EEfWBeh_Ylit8_朋友圈九宫格凑图专用੧ᐛ੭！_1_糕糕喵_来自小红书网页版.webp'},
  {id:'fallback_02',url:'https://imgbed.heliar.top/i/yHL5nRr3DAHKwMIH_朋友圈九宫格凑图专用੧ᐛ੭！_5_糕糕喵_来自小红书网页版.webp'},
  {id:'fallback_03',url:'https://imgbed.heliar.top/i/M_0EL4Z-ziUNjWEu_朋友圈九宫格凑图专用੧ᐛ੭！_4_糕糕喵_来自小红书网页版.webp'},
  {id:'fallback_04',url:'https://imgbed.heliar.top/i/Z9pVbi4MhCr9IsKK_朋友圈九宫格凑图专用੧ᐛ੭！_11_糕糕喵_来自小红书网页版.webp'},
  {id:'fallback_05',url:'https://imgbed.heliar.top/i/oMEiCCU8_Km7DTbh_朋友圈九宫格凑图专用੧ᐛ੭！_14_糕糕喵_来自小红书网页版.webp'},
  {id:'fallback_06',url:'https://imgbed.heliar.top/i/n5wOmCuFl8Ge5tA3_朋友圈九宫格凑图专用੧ᐛ੭！_16_糕糕喵_来自小红书网页版.webp'},
  {id:'fallback_07',url:'https://imgbed.heliar.top/i/oZ99BgWCWNizenj3_朋友圈九宫格凑图专用੧ᐛ੭！_17_糕糕喵_来自小红书网页版.webp'},
  {id:'fallback_08',url:'https://imgbed.heliar.top/i/xC73uTFZ_01_aNKO_朋友圈九宫格凑图专用੧ᐛ੭！_13_糕糕喵_来自小红书网页版.webp'}
];
INS_FEED_FALLBACK_MEDIA_POOL.forEach(item=>{const img=new Image();img.src=item.url;});

function randomFallbackMediaDeck(desiredCount,excludeIds=[]){
  const excluded=new Set((excludeIds||[]).map(String));
  const shuffled=[...INS_FEED_FALLBACK_MEDIA_POOL]
    .filter(item=>!excluded.has(String(item.id||'')))
    .map(item=>({item,sort:Math.random()}))
    .sort((a,b)=>a.sort-b.sort)
    .map(row=>row.item);

  const parsed=Number.isFinite(Number(desiredCount))?Number(desiredCount):null;
  const count=Math.max(1,Math.min(4,parsed||1+Math.floor(Math.random()*4)));
  return shuffled.slice(0,count).map(item=>({id:item.id,url:item.url}));
}

function ensurePostFallbackDeck(post,index=0){
  if(!post || (post.mediaType||'image')==='video')return [];

  const desiredCount=Array.isArray(post.mediaItems) && post.mediaItems.length
    ? Math.max(1,Math.min(4,post.mediaItems.filter(item=>item && (item.url || item.desc || item.draft)).length))
    : null;

  if(!Array.isArray(post.fallbackMediaItems) || !post.fallbackMediaItems.length){
    post.fallbackMediaItems=randomFallbackMediaDeck(desiredCount);
  }

  post.fallbackMediaItems=post.fallbackMediaItems.filter(item=>item && item.url).slice(0,4);

  if(desiredCount){
    if(post.fallbackMediaItems.length>desiredCount){
      post.fallbackMediaItems=post.fallbackMediaItems.slice(0,desiredCount);
    }else if(post.fallbackMediaItems.length<desiredCount){
      const need=desiredCount-post.fallbackMediaItems.length;
      const more=randomFallbackMediaDeck(need,post.fallbackMediaItems.map(item=>item.id));
      post.fallbackMediaItems=[...post.fallbackMediaItems,...more].slice(0,desiredCount);
    }
  }

  if(!post.fallbackMediaItems.length){
    const picked=INS_FEED_FALLBACK_MEDIA_POOL[Math.abs(Number(index)||0)%INS_FEED_FALLBACK_MEDIA_POOL.length] || INS_FEED_FALLBACK_MEDIA_POOL[0];
    post.fallbackMediaItems=[{id:picked?.id||'',url:picked?.url||''}];
  }

  if(!Number.isInteger(post.mediaCarouselIndex))post.mediaCarouselIndex=0;
  post.mediaCarouselIndex=(post.mediaCarouselIndex+post.fallbackMediaItems.length)%post.fallbackMediaItems.length;

  const current=post.fallbackMediaItems[post.mediaCarouselIndex] || post.fallbackMediaItems[0];
  post.fallbackMediaUrl=current?.url||'';
  post.fallbackMediaId=current?.id||'';
  return post.fallbackMediaItems;
}

function ensurePostFallbackMedia(post,index=0){
  const deck=ensurePostFallbackDeck(post,index);
  const current=deck[post.mediaCarouselIndex||0] || deck[0];
  return current?.url||'';
}

function getPostMediaItems(post,index=0){
  if(!post)return [];

  if(Array.isArray(post.mediaItems) && post.mediaItems.length){
    const baseItems=post.mediaItems.filter(item=>item && (item.url || item.desc)).slice(0,4);
    if(!baseItems.length)return [];
    const deck=ensurePostFallbackDeck(post,index);
    return baseItems.map((item,idx)=>({
      id:item.id||deck[idx]?.id||`media_${idx}`,
      url:item.url||deck[idx]?.url||'',
      desc:item.desc||post.mediaDescription||post.mediaText||'',
      source:item.source||post.mediaSource||''
    }));
  }

  if(post.mediaUrl){
    return [{
      id:post.linkedAlbumId||'media_current',
      url:post.mediaUrl,
      desc:post.mediaDescription||post.mediaText||'',
      source:post.mediaSource||''
    }];
  }

  return ensurePostFallbackDeck(post,index).map(item=>({
    id:item.id||'',
    url:item.url||'',
    desc:post.mediaDescription||post.mediaText||'',
    source:post.mediaSource||'description'
  }));
}

function getPostCurrentMediaItem(post,index=0){
  const items=getPostMediaItems(post,index);
  if(!items.length)return {id:'',url:'',desc:post?.mediaDescription||post?.mediaText||''};

  if(!Number.isInteger(post.mediaCarouselIndex))post.mediaCarouselIndex=0;
  post.mediaCarouselIndex=(post.mediaCarouselIndex+items.length)%items.length;

  const current=items[post.mediaCarouselIndex] || items[0];
  if(!post.mediaUrl){
    post.fallbackMediaUrl=current?.url||'';
    post.fallbackMediaId=current?.id||'';
  }
  return current;
}

function getPostDisplayMediaUrl(post,index=0){
  return getPostCurrentMediaItem(post,index).url||'';
}

const NAV_ASSET={
  homeBlackSelected:"data:image/png;base64,",
  homeBlackOutline:"data:image/png;base64,",
  reelsBlack:"data:image/png;base64,",
  dmBlack:"data:image/png;base64,",
  homeWhite:"data:image/png;base64,",
  reelsWhite:"data:image/png;base64,",
  dmWhite:"data:image/png;base64,"
};

const posts=[
{id:'p_rec_food',type:'stranger',followed:false,author:'heyy.he',avatar:'H',sub:'为你推荐',mediaClass:'m1',mediaText:'莓果奶油面包、白草莓和咖啡的日常餐桌照片。',mediaDescription:'木桌上的日常甜食随拍：白色椭圆盘里是一条表面烤成焦糖色的长面包，中间挤着白色奶油并整齐放着一排蓝莓；后方浅色碗里装着白草莓和黑莓，右侧是一杯浅棕色冰咖啡或奶咖。自然室内光线，普通用户随手记录的一顿甜食。',mediaUrl:'https://imgbed.heliar.top/i/Y7dNw_BRdx54VeOS_Screenshot_20260826_214751.webp',likes:1284,comments:45,reposts:31,shares:66,liked:false,reposted:false,expanded:false,translated:false,captionOriginal:'本来只想吃个面包，最后旁边那碗莓果也一起解决了。白草莓没想象中甜，蓝莓奶油这个倒是会再买。咖啡就……正常发挥。',captionTranslation:'',captionShort:'本来只想吃个面包，最后旁边那碗莓果也一起解决了。白草莓没想象中甜，蓝莓奶油这个倒是会再买。咖啡就……正常发挥。',captionLong:'',translation:'',sourceLanguage:'zh',tags:['#daily','#food','#dessert']},
{id:'p_rec_idol',type:'stranger',followed:false,author:'seoyun.after',avatar:'H',sub:'为你推荐',mediaClass:'m2',mediaText:'活动结束后的后台休息室，年轻艺人坐在化妆镜前，黑色舞台服还没有换下，桌上放着水、耳返和散开的化妆用品。镜子周围是暖白色灯光，头发和妆容带着一点演出结束后的凌乱感，像工作人员随手留下的一张自然幕后照片，不刻意看镜头。',mediaDescription:'活动结束后的后台休息室，年轻艺人坐在化妆镜前，黑色舞台服还没有换下，桌上放着水、耳返和散开的化妆用品。镜子周围是暖白色灯光，头发和妆容带着一点演出结束后的凌乱感，像工作人员随手留下的一张自然幕后照片，不刻意看镜头。',mediaUrl:'',likes:26400,comments:1287,reposts:807,shares:1930,liked:false,reposted:false,expanded:false,translated:false,captionOriginal:'终于收工了。耳返摘下来的那一刻才发现今天真的结束了，回去洗个澡就睡。谢谢来看舞台的人。',captionTranslation:'',captionShort:'终于收工了。耳返摘下来的那一刻才发现今天真的结束了，回去洗个澡就睡。谢谢来看舞台的人。',captionLong:'',translation:'',sourceLanguage:'ko',tags:['#backstage','#today','#seoul']},
{id:'p_rec_swiss',type:'stranger',followed:false,author:'mialater',avatar:'M',sub:'为你推荐',mediaClass:'m3',mediaText:'瑞士雪山旅行照片。',mediaDescription:'瑞士雪山旅行随拍，远处是高耸雪峰与开阔山谷，空气清透，光线干净，带有真实旅行记录感。',mediaUrl:'https://imgbed.heliar.top/i/Z_jJ7jv3jZq6bkaY_Screenshot_20260826_214453.webp',likes:6942,comments:236,reposts:142,shares:417,liked:false,reposted:false,expanded:false,translated:false,captionOriginal:'早上拉开窗帘就是这个景色，站着看了一会儿才想起来拿手机。今天没排什么行程，准备沿着湖边慢慢走。',captionTranslation:'',captionShort:'早上拉开窗帘就是这个景色，站着看了一会儿才想起来拿手机。今天没排什么行程，准备沿着湖边慢慢走。',captionLong:'',translation:'',sourceLanguage:'en',tags:['#switzerland','#mountains','#travel']}
];

posts.forEach((p,index)=>{
  if(!p.mediaType) p.mediaType='image';
  if(typeof p.linkedAlbumId === 'undefined') p.linkedAlbumId='';
  if(typeof p.mediaSource === 'undefined') p.mediaSource='description';
  if(typeof p.mediaFlipped === 'undefined') p.mediaFlipped=false;
  if(!Number.isInteger(p.mediaCarouselIndex))p.mediaCarouselIndex=0;
  ensurePostFallbackDeck(p,index);
});

function fmtSocial(n){
  n=Number(n||0);
  if(n>=10000){
    const v=n/10000;
    return (v>=10?Math.round(v):Math.round(v*10)/10).toString().replace(/\\.0$/,'')+'万';
  }
  return n.toLocaleString('en-US');
}

function isLikelyChineseText(text=''){
  const value=String(text||'').trim();
  if(!value)return false;
  const hasHan=/[\u4e00-\u9fff]/.test(value);
  const hasKana=/[\u3040-\u30ff]/.test(value);
  const hasHangul=/[\uac00-\ud7af]/.test(value);
  return hasHan && !hasKana && !hasHangul;
}
function shouldOfferTranslation(original='',translation=''){
  return !!String(translation||'').trim() && !isLikelyChineseText(original);
}

function normalizeFeedPostTags(raw){
  const source=Array.isArray(raw)?raw.join(' '):String(raw||'');
  return [...new Set(source.split(/[\s,，]+/).map(tag=>tag.trim()).filter(Boolean).map(tag=>tag.startsWith('#')?tag:'#'+tag).filter(tag=>/^#[^#\s]+$/.test(tag)))];
}
function renderFeedPostTags(post){
  const tags=normalizeFeedPostTags(post?.tags||[]);
  return tags.length?' '+tags.map(tag=>`<span class="tag">${escapeDMCardHTML(tag)}</span>`).join(' '):'';
}
function splitFeedEditorCaptionAndTags(raw=''){
  const text=String(raw||'').trim();
  const tags=normalizeFeedPostTags((text.match(/(^|\s)#[^\s#]+/g)||[]).map(x=>String(x).trim()));
  const clean=stripGeneratedCaptionHashtags(text);
  const parts=splitGeneratedCaptionText(clean);
  return {parts,tags};
}

function feedCaptionPlainText(value=''){
  const holder=document.createElement('div');
  holder.innerHTML=String(value||'');
  return String(holder.textContent||'').replace(/\s+/g,' ').trim();
}

/* v1.07.9 Home-only caption model.
   Home Feed now stores and renders a Chinese caption first. A separate,
   user-configured OpenAI-compatible API is called only when the user taps
   "显示原文" for the first time. The result is cached locally; Reels, Story,
   comments, DM and the main Roche AI route do not use this model. */
const HOME_FEED_TRANSLATION_API_STORAGE_KEY='nini-ins-feed-translation-api-v1';
const HOME_FEED_TRANSLATION_CACHE_STORAGE_KEY='nini-ins-feed-translation-cache-v1';
function readHomeFeedTranslationJSON(key,fallback){
  try{
    const value=JSON.parse(localStorage.getItem(key)||'null');
    return value&&typeof value==='object'?value:fallback;
  }catch(e){return fallback;}
}
let homeFeedTranslationApiSettings={
  url:'https://api.siliconflow.cn/v1',
  apiKey:'sk-eoifyctjxnsqcozvzyemvhwonsnfxxppzqhtactoipqbakjv',
  model:'deepseek-ai/DeepSeek-V4-Flash',
  ...readHomeFeedTranslationJSON(HOME_FEED_TRANSLATION_API_STORAGE_KEY,{})
};
let homeFeedTranslationCache=readHomeFeedTranslationJSON(HOME_FEED_TRANSLATION_CACHE_STORAGE_KEY,{});
const homeFeedTranslationRuntime=new Map();
function persistHomeFeedTranslationApiSettings(){
  try{localStorage.setItem(HOME_FEED_TRANSLATION_API_STORAGE_KEY,JSON.stringify(homeFeedTranslationApiSettings));}catch(e){}
}
function persistHomeFeedTranslationCache(){
  try{localStorage.setItem(HOME_FEED_TRANSLATION_CACHE_STORAGE_KEY,JSON.stringify(homeFeedTranslationCache));}catch(e){}
}
function isHomeFeedChineseLanguage(value=''){
  const key=String(value||'').trim().toLowerCase().replace('_','-');
  if(['yue','cantonese','粤语','粵語','zh-hk','zh-yue','yue-hk'].includes(key))return false;
  return /^(zh(?:[-_](?:cn|hans|tw|hant))?|chinese|中文|汉语|漢語)$/i.test(String(value||'').trim());
}
function homeFeedLanguageLabel(value=''){
  const key=String(value||'').trim().toLowerCase().replace('_','-');
  const labels={
    ko:'韩语',korean:'韩语','ko-kr':'韩语',ja:'日语',japanese:'日语','ja-jp':'日语',
    yue:'粤语',cantonese:'粤语','粤语':'粤语','粵語':'粤语','zh-hk':'粤语','zh-yue':'粤语','yue-hk':'粤语',
    en:'英语',english:'英语','en-us':'英语','en-gb':'英语',th:'泰语',thai:'泰语',
    fr:'法语',french:'法语',de:'德语',german:'德语',es:'西班牙语',spanish:'西班牙语',
    pt:'葡萄牙语',portuguese:'葡萄牙语',vi:'越南语',vietnamese:'越南语',
    id:'印尼语',indonesian:'印尼语',ru:'俄语',russian:'俄语',it:'意大利语',italian:'意大利语'
  };
  return labels[key]||String(value||'外语').trim()||'外语';
}
function firstHomeFeedCaptionText(...values){
  for(const value of values){
    if((typeof value==='string'||typeof value==='number') && String(value).trim()){
      return feedCaptionPlainText(String(value));
    }
  }
  return '';
}
function ensureHomeFeedCaptionModel(post){
  if(!post)return {chinese:'',originalText:'',sourceLanguage:'zh',showingOriginal:false,state:'idle'};
  const legacyOriginal=feedCaptionPlainText([post.captionShort||'',post.captionLong||''].filter(Boolean).join(' '));
  const sourceLanguage=firstHomeFeedCaptionText(post.feedSourceLanguage,post.sourceLanguage,post.language)||'zh';
  const legacyTranslation=firstHomeFeedCaptionText(
    post.captionTranslation,post.homeCaptionTranslation,post.translation,post.textTranslation,post.caption_translation
  );
  const legacyCaption=firstHomeFeedCaptionText(
    post.captionOriginal,post.homeCaptionOriginal,legacyOriginal,post.caption,post.textOriginal,post.text
  );
  const chinese=stripGeneratedCaptionHashtags(firstHomeFeedCaptionText(
    post.feedCaptionZh,
    isLikelyChineseText(legacyTranslation)?legacyTranslation:'',
    isHomeFeedChineseLanguage(sourceLanguage)?legacyCaption:'',
    legacyCaption,
    legacyTranslation
  )).trim();
  const cached=homeFeedTranslationCache[String(post.id||'')];
  const originalText=stripGeneratedCaptionHashtags(firstHomeFeedCaptionText(
    post.feedOriginalText,cached?.text
  )).trim();
  const inlineSource=firstHomeFeedCaptionText(post.feedCaptionZh,chinese,legacyCaption,legacyTranslation);
  const inlineTags=normalizeFeedPostTags((inlineSource.match(/(^|\s)#[^\s#]+/g)||[]).map(x=>String(x).trim()));
  const tags=normalizeFeedPostTags([...(post.tags||[]),...inlineTags]);
  if(!tags.length && post.type!=='user')tags.push('#daily');

  post.feedCaptionZh=chinese;
  post.feedOriginalText=originalText;
  post.feedSourceLanguage=sourceLanguage;
  post.feedShowingOriginal=!!originalText&&!!post.feedShowingOriginal;
  post.tags=tags;

  /* Keep legacy mirrors for profile cards, memory summaries and old saves. */
  post.captionOriginal=chinese;
  post.captionTranslation='';
  post.homeCaptionOriginal=chinese;
  post.homeCaptionTranslation='';
  post.homeTranslationOpen=false;
  const parts=splitGeneratedCaptionText(chinese);
  post.captionShort=parts.short?` ${escapeDMCardHTML(parts.short)}`:'';
  post.captionLong=parts.long?` ${escapeDMCardHTML(parts.long)}`:'';
  post.translation='';
  post.translated=false;
  const runtime=homeFeedTranslationRuntime.get(String(post.id||''))||{state:'idle',error:''};
  return {chinese,originalText,sourceLanguage,showingOriginal:post.feedShowingOriginal,state:runtime.state||'idle',error:runtime.error||''};
}
function shouldOfferHomeFeedOriginal(model){
  return !!model?.chinese && !isHomeFeedChineseLanguage(model?.sourceLanguage);
}
function homeFeedCaptionDisplayText(model){
  return model?.showingOriginal&&model?.originalText?model.originalText:model?.chinese||'';
}
function homeFeedOriginalButtonText(model){
  const language=homeFeedLanguageLabel(model?.sourceLanguage);
  if(model?.state==='loading')return `正在恢复${language}原文…`;
  if(model?.state==='error'&&!model?.originalText)return `原文加载失败 · 重试`;
  return model?.showingOriginal?`翻译自${language} · 显示中文`:`翻译自${language} · 显示原文`;
}

function renderFeed(){
  let html='';
  posts.forEach((p,i)=>{
    const captionModel=ensureHomeFeedCaptionModel(p);
    html+=`
      <article class="post" id="${p.id}">
        <div class="post-head">
          ${(()=>{
            const identity=getIdentityMeta(p.author);
            const isOwnPost=p.type==='user' || p.author==='user' || String(p.author||'')===String(userProfileState?.id||'') || String(p.author||'')===String(userProfileState?.rochePersonaId||'');
            const showFollow=!isOwnPost;
            return `<div class="avatar" data-post-profile="${escapeDMCardHTML(p.author)}" data-post-type="${escapeDMCardHTML(p.type)}" data-identity-avatar="${escapeDMCardHTML(p.author)}">${identity.avatarUrl?'':escapeDMCardHTML(identity.initial)}</div>
          <div class="head-main">
            <div class="post-id" data-post-profile="${escapeDMCardHTML(p.author)}" data-post-type="${escapeDMCardHTML(p.type)}">${escapeDMCardHTML(identity.name)}</div>
            ${p.sub?`<div class="post-sub">${escapeDMCardHTML(p.sub)}</div>`:''}
          </div>
          <div class="post-right">
            ${showFollow?`<button class="follow-btn feed-follow-locked" type="button" disabled aria-disabled="true">${p.type==='stranger'?'关注':'已关注'}</button>`:''}
            <button class="menu-btn" type="button" data-feed-menu-index="${i}"><svg viewBox="0 0 24 24"><path d="M5 8h14M5 16h14"/></svg></button>
          </div>`;
          })()}
        </div>

        ${(()=>{
          const items=getPostMediaItems(p,i);
          const current=getPostCurrentMediaItem(p,i);
          const total=Math.max(1,items.length);
          const currentNo=Math.min(total,(p.mediaCarouselIndex||0)+1);
          return `<div class="media ${p.mediaClass} post-media-flippable" data-post-media-index="${i}">
            ${p.mediaFlipped
              ? `<div class="post-media-description-back">
                  <span>${escapeDMCardHTML(current.desc || p.mediaDescription || p.mediaText || '暂无图片描述')}</span>
                </div>`
              : `<img draggable="false" src="${escapeDMCardHTML(current.url||ensurePostFallbackMedia(p,i))}" alt="${escapeDMCardHTML(p.author)} post">`}
            ${total>1?`<div class="post-media-counter">${currentNo}/${total}</div>`:''}
          </div>`;
        })()}

        <div class="actions">
          <div class="act" data-feed-like-index="${i}">
            <img class="iconimg" data-role="like" src="${p.liked?ICONS.heart1:ICONS.heart0}">
            <span class="num">${fmtSocial(p.likes+(p.liked?1:0))}</span>
          </div>
          <div class="act" data-comments-open="${p.id}">
            <img class="iconimg" src="${ICONS.reply}">
            <span class="num">${fmtSocial(p.comments)}</span>
          </div>
          <div class="act" data-feed-repost-index="${i}">
            <img class="iconimg" data-role="repost" src="${p.reposted?ICONS.repost1:ICONS.repost0}">
            <span class="num">${fmtSocial(p.reposts+(p.reposted?1:0))}</span>
          </div>
          <div class="act" data-share-dm="${p.id}">
            <img class="iconimg" src="${ICONS.dm}">
            <span class="num">${fmtSocial(p.shares)}</span>
          </div>
        </div>

        ${(()=>{
          const identity=getIdentityMeta(p.author);
          const canShowOriginal=shouldOfferHomeFeedOriginal(captionModel);
          const tagHtml=renderFeedPostTags(p);
          return `<div class="caption">
          <strong>${escapeDMCardHTML(identity.name)}</strong>
          ${captionModel.chinese?`<span data-feed-caption-text="${escapeDMCardHTML(p.id)}"> ${escapeDMCardHTML(homeFeedCaptionDisplayText(captionModel))}</span>`:''}
          ${tagHtml}
        </div>
        ${canShowOriginal?`<button class="feed-original-toggle" data-home-feed-original="${escapeDMCardHTML(p.id)}" type="button" ${captionModel.state==='loading'?'disabled':''}>${escapeDMCardHTML(homeFeedOriginalButtonText(captionModel))}</button>`:''}`;
        })()}
      </article>`;
  });
  const feedEl=document.getElementById('feed');
  feedEl.innerHTML=html;
  hydrateIdentityAvatars(feedEl);
}


/* v1.04.2: Feed primary controls delegate from document, not #feed.
   In Direct Mount the #feed node is not guaranteed to exist yet when this script section runs;
   the document is stable, exactly like the already-working comment/share delegation below. */
function handleFeedPrimaryControlTap(event){
  const target=event.target?.closest ? event.target : event.target?.parentElement;
  if(!target)return false;
  const menu=target.closest?.('[data-feed-menu-index]');
  if(menu){event.preventDefault();const i=Number(menu.dataset.feedMenuIndex);if(Number.isInteger(i))openPostMenu(i);return true;}
  const like=target.closest?.('[data-feed-like-index]');
  if(like){event.preventDefault();const i=Number(like.dataset.feedLikeIndex);if(Number.isInteger(i))toggleLike(i);return true;}
  const repost=target.closest?.('[data-feed-repost-index]');
  if(repost){event.preventDefault();const i=Number(repost.dataset.feedRepostIndex);if(Number.isInteger(i))toggleRepost(i);return true;}
  const translation=target.closest?.('[data-home-feed-original]');
  if(translation){event.preventDefault();void toggleHomeFeedOriginal(translation.dataset.homeFeedOriginal,translation.closest('.post'));return true;}
  return false;
}
document.addEventListener('click',event=>{
  handleFeedPrimaryControlTap(event);
});


let postMediaPointerState=null;

function togglePostMediaFlip(index){
  const post=posts[index];
  if(!post || (post.mediaType||'image')==='video')return;
  post.mediaFlipped=!post.mediaFlipped;
  rerenderKeepScroll();
}

document.getElementById('feed')?.addEventListener('pointerdown',event=>{
  const media=event.target.closest('[data-post-media-index]');
  if(!media)return;
  postMediaPointerState={
    index:Number(media.dataset.postMediaIndex),
    x:event.clientX,
    y:event.clientY,
    pointerId:event.pointerId
  };
},{passive:true});

function shiftPostMediaCarousel(index,delta){
  const post=posts[index];
  if(!post || (post.mediaType||'image')==='video')return;

  const items=getPostMediaItems(post,index);
  if(items.length<=1)return;

  post.mediaCarouselIndex=
    ((post.mediaCarouselIndex||0)+delta+items.length)%items.length;
  post.mediaFlipped=false;

  /* Keep compatibility fields aligned with the visible page. */
  const current=items[post.mediaCarouselIndex];
  if(!post.mediaUrl){
    post.fallbackMediaUrl=current?.url||'';
    post.fallbackMediaId=current?.id||'';
  }
  rerenderKeepScroll();
}

document.getElementById('feed')?.addEventListener('pointerup',event=>{
  const state=postMediaPointerState;
  if(!state || state.pointerId!==event.pointerId)return;
  postMediaPointerState=null;

  const dx=event.clientX-state.x;
  const dy=event.clientY-state.y;
  const absX=Math.abs(dx);
  const absY=Math.abs(dy);
  const distance=Math.hypot(dx,dy);

  /* Horizontal swipe: real page change. */
  if(absX>=34 && absX>absY*1.15){
    shiftPostMediaCarousel(state.index,dx<0?1:-1);
    return;
  }

  /* Real tap: flip current image to its description. */
  if(distance<10){
    togglePostMediaFlip(state.index);
  }
},{passive:true});

document.getElementById('feed')?.addEventListener('pointercancel',()=>{
  postMediaPointerState=null;
},{passive:true});

function rerenderKeepScroll(){
  const feed=document.getElementById('feed');
  const y=feed.scrollTop;
  renderFeed();
  feed.scrollTop=y;
}
function popAfter(selector){
  requestAnimationFrame(()=>{
    const node=document.querySelector(selector);
    if(!node)return;
    node.classList.remove('icon-pop');
    void node.offsetWidth;
    node.classList.add('icon-pop');
  });
}
function toggleFollow(i){return; /* v5.0.10 Feed follow labels are intentionally locked. */}
function toggleLike(i){
  posts[i].liked=!posts[i].liked;
  rerenderKeepScroll();
  popAfter(`#${posts[i].id} [data-role="like"]`);
}
function toggleRepost(i){
  posts[i].reposted=!posts[i].reposted;
  rerenderKeepScroll();
  if(typeof renderUserProfile==='function')renderUserProfile();
  popAfter(`#${posts[i].id} [data-role="repost"]`);
}
function normalizeHomeFeedTranslationEndpoint(url=''){
  const clean=String(url||'').trim().replace(/\/+$/,'');
  if(!clean)return '';
  return /\/chat\/completions$/i.test(clean)?clean:clean+'/chat/completions';
}
function getHomeFeedTranslationRequestConfig(){
  /* Dedicated Feed-translation route only.
     Intentionally isolated from window.NiniINSAIBridge / Roche main API settings. */
  return {
    url:String(homeFeedTranslationApiSettings.url||'').trim(),
    apiKey:String(homeFeedTranslationApiSettings.apiKey||'').trim(),
    model:String(homeFeedTranslationApiSettings.model||'').trim()
  };
}
function extractHomeFeedTranslationText(data){
  let content=data?.choices?.[0]?.message?.content??data?.choices?.[0]?.text??data?.output_text??data?.text??data?.content??'';
  if(Array.isArray(content))content=content.map(item=>typeof item==='string'?item:(item?.text||item?.content||'')).join('');
  if(content&&typeof content==='object')content=content.text||content.content||'';
  return String(content||'').trim().replace(/^```(?:text)?\s*/i,'').replace(/\s*```$/,'').replace(/^[“"]|[”"]$/g,'').trim();
}
async function callHomeFeedOriginalApi(chineseCaption,sourceLanguage){
  const requestConfig=getHomeFeedTranslationRequestConfig();
  const url=normalizeHomeFeedTranslationEndpoint(requestConfig.url);
  const apiKey=requestConfig.apiKey;
  const model=requestConfig.model;
  if(!url||!apiKey||!model)throw new Error('请先在侧边栏填写 Feed 正文翻译 API');
  const language=homeFeedLanguageLabel(sourceLanguage);
  const response=await fetch(url,{
    method:'POST',
    headers:{'Content-Type':'application/json','Authorization':'Bearer '+apiKey},
    body:JSON.stringify({
      model,
      temperature:0.2,
      stream:false,
      messages:[
        {role:'system',content:`你是社交媒体翻译器。把简体中文 Instagram 正文翻译成${language}。保持语气、换行和 emoji，自然像该语言用户本人发帖。只输出译文正文，不要解释、引号、标签或 Markdown。`},
        {role:'user',content:String(chineseCaption||'')}
      ]
    })
  });
  let data={};
  try{data=await response.json();}catch(e){}
  if(!response.ok)throw new Error(String(data?.error?.message||data?.message||`接口状态 ${response.status}`));
  const text=extractHomeFeedTranslationText(data);
  if(!text)throw new Error('接口没有返回原文');
  return text;
}
function refreshHomeFeedCaptionArticle(post,article){
  const model=ensureHomeFeedCaptionModel(post);
  const root=article||document.getElementById(String(post.id||''))||document;
  const caption=root.querySelector?.(`[data-feed-caption-text="${String(post.id||'').replace(/"/g,'\\"')}"]`);
  const button=root.querySelector?.(`[data-home-feed-original="${String(post.id||'').replace(/"/g,'\\"')}"]`);
  if(caption)caption.textContent=' '+homeFeedCaptionDisplayText(model);
  if(button){
    button.textContent=homeFeedOriginalButtonText(model);
    button.disabled=model.state==='loading';
  }
}
async function toggleHomeFeedOriginal(postId,article){
  const post=posts.find(row=>String(row?.id||'')===String(postId||''));
  if(!post)return;
  const model=ensureHomeFeedCaptionModel(post);
  if(!shouldOfferHomeFeedOriginal(model))return;
  if(model.originalText){
    post.feedShowingOriginal=!model.showingOriginal;
    refreshHomeFeedCaptionArticle(post,article);
    return;
  }
  if(!String(homeFeedTranslationApiSettings.url||'').trim()||!String(homeFeedTranslationApiSettings.apiKey||'').trim()||!String(homeFeedTranslationApiSettings.model||'').trim()){
    openHomeFeedTranslationApiSettings();
    alert('请先填写 Feed 正文翻译 API 的 URL、Key 和模型');
    return;
  }
  const key=String(post.id||'');
  if(homeFeedTranslationRuntime.get(key)?.state==='loading')return;
  homeFeedTranslationRuntime.set(key,{state:'loading',error:''});
  refreshHomeFeedCaptionArticle(post,article);
  try{
    const text=await callHomeFeedOriginalApi(model.chinese,model.sourceLanguage);
    post.feedOriginalText=text;
    post.feedShowingOriginal=true;
    homeFeedTranslationCache[key]={text,sourceLanguage:model.sourceLanguage,updatedAt:Date.now()};
    persistHomeFeedTranslationCache();
    homeFeedTranslationRuntime.set(key,{state:'idle',error:''});
  }catch(error){
    homeFeedTranslationRuntime.set(key,{state:'error',error:String(error?.message||'翻译失败')});
    alert('Feed 原文翻译失败：'+String(error?.message||'未知错误'));
  }
  refreshHomeFeedCaptionArticle(post,article);
}


/* ===== v1.15 Integrated Reels ===== */
const reelsV15State = {
activeId:'reel_kamakura_01',selectedIdentity:'current',globalTranslation:false,
items:[
{id:'reel_kamakura_01',author:'umi.after5',initial:'U',kind:'stranger',followed:false,liked:false,reposted:false,translated:false,likes:18600,comments:486,reposts:329,videoDescription:'傍晚贴着海岸慢慢沉下来，江之电晃进小小的车站，站台上的人影被夕阳拖得很长，远处的海亮成一片晃眼的银。车门开合，风顺着轨道钻进来，衣角和路边的旗子一起轻轻扬起来。\n\n电车沿着海边缓缓滑过去，半开的车窗把天空、房屋和海切成一格一格流动的画面。夕阳落在浪尖上，金橘色的光碎得很薄，冲浪板靠在路边的墙上，像还舍不得收起这一天。\n\n海边的人行道上有人推着单车，有人踩着拖鞋慢慢往回走，风声和轨道摩擦的声音偶尔撞在一起。路灯还没有完全亮起来，天空已经从暖黄色退成很浅的蓝。\n\n电车从海边公路的一端驶过去，车窗里的灯一格格亮起来，又很快消失在远处。海水仍旧一下一下推回岸边，夏天像是走到了门口，却忽然又回过头，看了这个傍晚一眼。',caption:'夏が終わる前に、もう一度ここへ。',captionTranslation:'夏天结束之前，还想再来一次这里。',sourceLanguage:'ja',tags:['#kamakura','#enoden','#summerdays']},
{id:'reel_amusement_01',author:'momo.zip',initial:'M',kind:'stranger',followed:false,liked:false,reposted:false,translated:false,likes:9730,comments:327,reposts:251,videoDescription:'阳光亮得让人有些睁不开眼。戴着毛绒发箍的女孩站在人群里，穿着粉色外套的朋友从远处叫她，她一回头就笑起来，过山车刚好从身后轰地掠过去，连空气都像被带得晃了一下。\n\n镜头跟着她们往前跑，彩色气球在头顶轻轻摇晃，糖果摊和游戏摊一格一格擦过去。背着蓝色包的短发男孩从旁边追上来，怀里抱着刚赢来的玩偶，笑得站都站不稳，戴着毛绒发箍的女孩伸手去抢，穿粉色外套的朋友就在旁边故意添乱，画面一下子被他们闹得乱七八糟。\n\n水花、尖叫、机器运转的声音和笑声混在一起，旋转设施把灯光和人影搅成一圈一圈。戴眼镜、手里拿着棉花糖的高个子男孩本来还站在旁边看热闹，下一秒就被朋友拖进镜头里，几个人挤在一起笑，连拍摄的人都被撞得往后退了两步。\n\n天色慢慢暗下来，游乐园的灯一盏一盏亮起来。戴着毛绒发箍的女孩抱着玩偶站在发光的旋转木马旁边，转头朝镜头这边挥了挥手，穿粉色外套的朋友又从后面扑上来抱住她，所有人的笑声还留在这一天的边缘，吵闹得刚刚好。',caption:'오늘 애들 웃는 거 보느라 나도 하루 종일 웃었다 ㅋㅋ 🎡',captionTranslation:'今天光是看他们笑，我自己也跟着笑了一整天ㅋㅋ 🎡',sourceLanguage:'ko',tags:['#dayoff','#amusementpark','#withfriends']}],
comments:{
reel_kamakura_01:[
{initial:'?',author:'natsu.zip',text:'この時間の鎌倉ほんとに好き',translation:'真的很喜欢镰仓这个时间',translated:false},
{initial:'?',author:'melo__21',text:'the train passing by the sea 😭',translation:'电车从海边经过的画面 😭',translated:false},
{initial:'?',author:'yuuki.log',text:'夏終わらないでほしい',translation:'希望夏天不要结束',translated:false},
{initial:'?',author:'cloudyday_8',text:'这里傍晚真的会让人舍不得走',translation:'',translated:false},
{initial:'?',author:'00sora',text:'この動画ずっと見てられる…',translation:'这个视频可以一直看下去…',translated:false}],
reel_amusement_01:[
{initial:'?',author:'yena.03',text:'놀이공원 에너지 미쳤다 ㅋㅋ',translation:'游乐园这个能量也太疯了ㅋㅋ',translated:false},
{initial:'?',author:'peachfilm',text:'the way everyone kept running into frame 😭',translation:'所有人不停冲进镜头的样子 😭',translated:false},
{initial:'?',author:'minji.zip',text:'저 인형 누가 결국 가져갔어',translation:'那个玩偶最后到底谁拿走了',translated:false},
{initial:'?',author:'cloudroom_',text:'看得我也想和朋友出去疯一天',translation:'',translated:false},
{initial:'?',author:'sora.wav',text:'마지막에 다같이 웃는 느낌 너무 좋다',translation:'最后大家一起笑的感觉太好了',translated:false}]
}};

/* Built-in Reels copy follows the same global, natural-account prompt contract as generated content. */
const BUILTIN_REELS_SOCIAL_COPY={
reel_kamakura_01:{
caption:'帰りの電車を一本遅らせた。海の色が変わるまで、もう少しだけ。',
captionTranslation:'回去的电车晚坐了一班。想再待一会儿，等海的颜色慢慢变掉。',
sourceLanguage:'ja',
comments:[
{initial:'?',author:'natsu.zip',displayName:'なつ',text:'一本遅らせるのわかる',translation:'能理解为什么要晚坐一班',sourceLanguage:'ja',translated:false},
{initial:'?',author:'melo__21',displayName:'Melo',text:'okay now I need to know what stop this was',translation:'好了，现在我必须知道这是哪一站',sourceLanguage:'en',translated:false},
{initial:'?',author:'haeun.frame',displayName:'해은',text:'이 시간대 바람 진짜 좋겠다',translation:'这个时间的海风肯定特别舒服',sourceLanguage:'ko',translated:false},
{initial:'?',author:'cloudyday_8',displayName:'阴天也出门',text:'我会在这坐到天黑再走',translation:'',sourceLanguage:'zh',translated:false},
{initial:'?',author:'00sora',displayName:'そら',text:'夏終わらないでほしい😭',translation:'希望夏天不要结束😭',sourceLanguage:'ja',translated:false}]},
reel_amusement_01:{
caption:'사진보다 영상에 웃음소리가 더 많이 남았다. 다들 집에 갈 때 목이 쉬어 있었음 ㅋㅋ',
captionTranslation:'比起照片，视频里留下了更多笑声。大家回家的时候嗓子都哑了ㅋㅋ',
sourceLanguage:'ko',
comments:[
{initial:'?',author:'yena.03',displayName:'예나',text:'목 쉬었다는 거 너무 현실적 ㅋㅋ',translation:'嗓子都哑了这点也太真实了ㅋㅋ',sourceLanguage:'ko',translated:false},
{initial:'?',author:'peachfilm',displayName:'Peach',text:'the camera person deserves a medal 😭',translation:'拍视频的人值得一枚奖牌😭',sourceLanguage:'en',translated:false},
{initial:'?',author:'yuuna_day',displayName:'ゆうな',text:'ぬいぐるみ結局だれが持って帰ったの笑',translation:'那个玩偶最后到底是谁带回家的笑',sourceLanguage:'ja',translated:false},
{initial:'?',author:'cloudroom_',displayName:'云房间',text:'后面抢玩偶那段我看了三遍',translation:'',sourceLanguage:'zh',translated:false},
{initial:'?',author:'sora.wav',displayName:'소라',text:'나는 첫번째 기구 타고 바로 집 갔을 듯',translation:'换我坐完第一个项目大概就直接回家了',sourceLanguage:'ko',translated:false}]}
};
Object.entries(BUILTIN_REELS_SOCIAL_COPY).forEach(([id,copy])=>{
  const item=reelsV15State.items.find(entry=>entry.id===id);
  if(item) Object.assign(item,{caption:copy.caption,captionTranslation:copy.captionTranslation,sourceLanguage:copy.sourceLanguage});
  reelsV15State.comments[id]=copy.comments.map(comment=>({...comment}));
});

function getReelsV15Item(id){
  return reelsV15State.items.find(item=>item.id===id) || null;
}
function getActiveReelsV15Item(){
  return getReelsV15Item(reelsV15State.activeId);
}
function renderReelsV15(){
  const box=document.getElementById('reelsV15Scroller');
  if(!box)return;

  box.innerHTML=reelsV15State.items.map(item=>{
    const identity=(item.kind==='stranger' && item.identitySnapshot)?{...getIdentityMeta(item.author),...item.identitySnapshot}:getIdentityMeta(item.author);
    const canTranslate=shouldOfferTranslation(item.caption,item.captionTranslation);
    const profileAttr=item.kind==='stranger'?'':` data-reels-v15-profile="${escapeDMCardHTML(item.author)}"`;
    return `
    <article class="reels-v15-item" data-reels-v15-id="${escapeDMCardHTML(item.id)}">
      <div class="reels-v15-stage">
        <div class="reels-v15-video">
          <strong>视频描述</strong>
          <span>${escapeDMCardHTML(item.videoDescription)}</span>
        </div>
      </div>
      <div class="reels-v15-fade"></div>

      <div class="reels-v15-info">
        <div class="reels-v15-authorline">
          <div class="reels-v15-avatar"${profileAttr} data-identity-avatar="${escapeDMCardHTML(item.author)}">${identity.avatarUrl?'':escapeDMCardHTML(identity.initial)}</div>
          <div class="reels-v15-author"${profileAttr}>${escapeDMCardHTML(identity.name)}</div>
          <button class="reels-v15-follow ${item.followed?'following':''}" data-reels-v15-follow type="button" ${item.kind==='stranger'?'disabled aria-disabled="true"':''}>${item.followed?'已关注':'关注'}</button>
        </div>

        <div class="reels-v15-caption">${escapeDMCardHTML(item.caption)}</div>
        ${item.location?`<div class="reels-v15-tags">${escapeDMCardHTML(item.location)}</div>`:''}
        ${canTranslate?`
          <button class="reels-v15-translate-toggle" data-reels-v15-translate type="button">${item.translated?'收起翻译':'查看翻译'}</button>
          <div class="reels-v15-translation ${item.translated?'show':''}">${escapeDMCardHTML(item.captionTranslation||'')}</div>
        `:''}
        <div class="reels-v15-tags">${item.tags.map(tag=>escapeDMCardHTML(tag)).join(' ')}</div>
      </div>

      <aside class="reels-v15-rail">
        <div class="reels-v15-action">
          <button class="reels-v15-railbtn heart ${item.liked?'liked':''}" data-reels-v15-like type="button">
            <img src="${item.liked?ICONS.heart1:ICONS.heart0}" alt="">
          </button>
          <span class="reels-v15-count">${fmtSocial(item.likes+(item.liked?1:0))}</span>
        </div>

        <div class="reels-v15-action">
          <button class="reels-v15-railbtn" data-reels-v15-comment type="button">
            <img src="${ICONS.reply}" alt="">
          </button>
          <span class="reels-v15-count">${fmtSocial(item.comments)}</span>
        </div>

        <div class="reels-v15-action">
          <button class="reels-v15-railbtn" data-reels-v15-repost type="button">
            <img src="${item.reposted?ICONS.repost1:ICONS.repost0}" alt="">
          </button>
          <span class="reels-v15-count">${fmtSocial(item.reposts+(item.reposted?1:0))}</span>
        </div>

        <div class="reels-v15-action">
          <button class="reels-v15-railbtn" data-reels-v15-dm type="button">
            <img src="${ICONS.dm}" alt="">
          </button>
          <span class="reels-v15-count">${fmtSocial(Number(item.shares)||0)}</span>
        </div>

        <div class="reels-v15-action reels-v15-more-action">
          <button class="reels-v15-railbtn" data-reels-v15-more type="button" aria-label="更多">
            <span class="reels-v15-moredots" aria-hidden="true"></span>
          </button>
        </div>

        <div class="reels-v15-spin">
          <div class="reels-v15-spinavatar" data-identity-avatar="${escapeDMCardHTML(item.author)}">${identity.avatarUrl?'':escapeDMCardHTML(identity.initial)}</div>
        </div>
      </aside>
    </article>`;
  }).join('');

  hydrateIdentityAvatars(box);
}

function closeReelsV15Sheets(){
  document.getElementById('reelsV15Dim')?.classList.remove('show');
  document.querySelectorAll('.reels-v15-sheet.show').forEach(el=>el.classList.remove('show'));
}
function openReelsV15Sheet(id){
  closeReelsV15Sheets();
  document.getElementById('reelsV15Dim')?.classList.add('show');
  document.getElementById(id)?.classList.add('show');
}
function showReelsV15Toast(text){
  const toast=document.getElementById('reelsV15Toast');
  if(!toast)return;
  toast.textContent=text;
  toast.classList.add('show');
  clearTimeout(window.__reelsV15ToastTimer);
  window.__reelsV15ToastTimer=setTimeout(()=>toast.classList.remove('show'),1200);
}
function renderReelsV15Comments(){
  const box=document.getElementById('reelsV15CommentList');
  const item=getActiveReelsV15Item();
  if(!box||!item)return;
  const rows=reelsV15State.comments[item.id]||[];
  box.innerHTML=rows.map((row,index)=>{
    const identity=getIdentityMeta(row.author);
    const handle=identity.handle||String(row.author||'public');
    const canTranslate=shouldOfferTranslation(row.text,row.translation);
    return `
    <div class="reels-v15-comment" data-reels-comment-index="${index}">
      <div class="reels-v15-comment-avatar" data-identity-avatar="${escapeDMCardHTML(row.author)}">${identity.avatarUrl?'':escapeDMCardHTML(identity.initial||row.initial||'?')}</div>
      <div class="reels-v15-comment-main">
        <div class="reels-v15-comment-name">
          <strong>${escapeDMCardHTML(identity.name||row.displayName||handle)}</strong>
          <span class="reels-v15-comment-handle">@${escapeDMCardHTML(handle)}</span>
        </div>
        <div class="reels-v15-comment-copy">${escapeDMCardHTML(row.text)}</div>
        <div class="reels-v15-comment-actions">
          ${canTranslate?`<button class="reels-v15-comment-translate" data-reels-comment-translate="${index}" type="button">${row.translated?'收起翻译':'查看翻译'}</button>`:''}
          <button class="reels-v15-comment-translate reels-v15-comment-delete" data-reels-comment-delete="${index}" type="button">删除</button>
        </div>
        ${canTranslate && row.translated?`<div class="reels-v15-comment-translation">${escapeDMCardHTML(row.translation)}</div>`:''}
      </div>
    </div>`;
  }).join('');
  if(!rows.length){
    box.innerHTML='<div style="padding:30px 0;text-align:center;color:#9a9da4;font-size:13px">还没有评论，点“再加载 5 条评论”生成 5 条。</div>';
  }
  hydrateIdentityAvatars(box);
}
document.getElementById('reelsV15CommentList')?.addEventListener('click',event=>{
  const item=getActiveReelsV15Item();
  if(!item)return;
  const rows=reelsV15State.comments[item.id]||[];

  const deleteBtn=event.target.closest('[data-reels-comment-delete]');
  if(deleteBtn){
    const index=Number(deleteBtn.dataset.reelsCommentDelete);
    if(Number.isInteger(index) && rows[index]){
      rows.splice(index,1);
      item.comments=Math.max(0,(Number(item.comments)||0)-1);
      renderReelsV15Comments();
      renderReelsV15();
      scheduleINSReelsPersistence();
    }
    return;
  }

  const btn=event.target.closest('[data-reels-comment-translate]');
  if(!btn)return;
  const row=rows[Number(btn.dataset.reelsCommentTranslate)];
  if(!row || !shouldOfferTranslation(row.text,row.translation))return;
  row.translated=!row.translated;
  renderReelsV15Comments();
});

function setReelsV15ActiveFromEvent(target){
  const itemNode=target.closest('[data-reels-v15-id]');
  if(itemNode)reelsV15State.activeId=itemNode.dataset.reelsV15Id;
  return getActiveReelsV15Item();
}
function getReelV15ShareSnapshot(id){
  const item=getReelsV15Item(id);
  if(!item)return null;
  return makeDMContextCard('reel',{
    kind:'reel',
    postId:item.id,
    author:item.author,
    caption:item.caption,
    mediaUrl:'',
    mediaDescription:'Reel 视频描述\n'+item.videoDescription,
    mediaClass:'m1',
    senderId:'user',source:'linked',contentType:'reel',sourceLanguage:item.sourceLanguage||'',captionTranslation:item.captionTranslation||'',location:item.location||'',tags:item.tags||[],metrics:{likes:item.likes||0,comments:item.comments||0,reposts:item.reposts||0,shares:item.shares||0}
  });
}
function sendReelV15ToDM(recipient){
  const item=getActiveReelsV15Item();
  if(!item)return;
  const snapshot=getReelV15ShareSnapshot(item.id);
  if(!snapshot)return;
  if(!dmSharedCards[recipient])dmSharedCards[recipient]=[];
  dmSharedCards[recipient].push(snapshot);
  scheduleINSDMHistoryPersistence();
  updateDMListPreview(recipient,'分享了一条 Reel');
  item.shares=(Number(item.shares)||0)+1;
  renderReelsV15();
  scheduleINSReelsPersistence();

  if(typeof recordSocialEvent==='function'){
    const recipientId=normalizeSocialActorId(recipient);
    const ownerId=normalizeSocialActorId(item.author);
    recordSocialEvent({
      type:'dm_share_content',
      actorId:'user',
      targetIds:[recipientId],
      contentId:item.id,
      contentType:'reel',
      contentOwnerId:ownerId,
      visibility:'private',
      payload:{
        sharedType:'Reel',
        sharedOwnerId:ownerId,
        sharedOwnerLabel:item.author,
        caption:item.caption||'',
        sharedSummary:item.caption||item.videoDescription||''
      },
      summary:`${socialActorName('user')}在 DM 给 ${socialActorName(recipientId)} 分享了 ${socialActorName(ownerId)} 的 Reel。`
    },{knownActorIds:['user',recipientId],learnedVia:'dm_share'});
    if(isFormalSocialActor(recipientId))exposePublicContentToActor(recipientId,item.id,'dm_share');
  }

  closeReelsV15Sheets();
  showReelsV15Toast('已发送到 DM');
}
function toggleReelsV15Repost(item){
  item.reposted=!item.reposted;
  renderReelsV15();
  renderUserProfile();
  scheduleINSReelsPersistence();
  showReelsV15Toast(item.reposted?'已保存到 User 转发':'已取消转发');
}

/* A single global switch can call this later.
   Original text, translation text and tags stay in separate DOM/data fields. */
function setGlobalTranslationEnabled(enabled){
  const on=!!enabled;
  reelsV15State.globalTranslation=on;
  posts.forEach(post=>{
    const model=ensureHomeFeedCaptionModel(post);
    post.feedShowingOriginal=on && !!model.originalText;
  });
  reelsV15State.items.forEach(item=>{
    item.translated=on && shouldOfferTranslation(item.caption,item.captionTranslation);
  });
  renderFeed();
  renderReelsV15();
}

document.getElementById('reelsV15Scroller')?.addEventListener('click',e=>{
  const item=setReelsV15ActiveFromEvent(e.target);
  if(!item)return;

  if(e.target.closest('[data-reels-v15-translate]')){
    if(shouldOfferTranslation(item.caption,item.captionTranslation)){
      item.translated=!item.translated;
      renderReelsV15();
    }
    return;
  }

  if(e.target.closest('[data-reels-v15-like]')){
    item.liked=!item.liked;
    renderReelsV15();
    scheduleINSReelsPersistence();
    return;
  }
  if(e.target.closest('[data-reels-v15-comment]')){
    renderReelsV15Comments();
    openReelsV15Sheet('reelsV15CommentSheet');
    return;
  }
  if(e.target.closest('[data-reels-v15-repost]')){
    toggleReelsV15Repost(item);
    return;
  }
  if(e.target.closest('[data-reels-v15-dm]')){
    renderReelsV15DMRecipients();
    openReelsV15Sheet('reelsV15DMSheet');
    return;
  }
  if(e.target.closest('[data-reels-v15-more]')){
    openReelsV15Sheet('reelsV15MoreSheet');
    return;
  }
  if(e.target.closest('[data-reels-v15-follow]')){
    if(item.kind==='stranger')return;
    item.followed=!item.followed;
    renderReelsV15();
    scheduleINSReelsPersistence();
    return;
  }
  if(e.target.closest('[data-reels-v15-profile]')){
    if(item.kind==='char' || item.kind==='npc'){
      openCharProfile(item.author);
    }else{
      const publicAvatar=document.getElementById('reelsV15PublicAvatar');if(publicAvatar){publicAvatar.textContent='';publicAvatar.style.backgroundImage=`url("${STRANGER_AVATAR_URL}")`;publicAvatar.style.backgroundSize='cover';publicAvatar.style.backgroundPosition='center';}
      document.getElementById('reelsV15PublicHandle').textContent='@'+item.author;
      openReelsV15Sheet('reelsV15PublicProfileSheet');
    }
  }
});

document.getElementById('reelsV15Dim')?.addEventListener('click',closeReelsV15Sheets);
document.querySelectorAll('[data-reels-v15-close]').forEach(btn=>btn.addEventListener('click',closeReelsV15Sheets));

document.getElementById('reelsV15UserPostBtn')?.addEventListener('click',()=>{
  const currentId=typeof userProfileState!=='undefined'?String(userProfileState.id||'user.ins'):'user.ins';
  const currentName=typeof userProfileState!=='undefined'?String(userProfileState.displayName||currentId):'User';
  document.getElementById('reelsV15IdentityName').textContent=currentName;
  document.getElementById('reelsV15IdentityHandle').textContent='@'+currentId;
  hydrateIdentityAvatars(document.getElementById('reelsV15UserPostSheet'));
  openReelsV15Sheet('reelsV15UserPostSheet');
});
function renderReelsV15GenerationSources(){
  const wrap=document.getElementById('reelsV15SourceChips');
  if(!wrap)return;
  const selected=new Set(
    [...wrap.querySelectorAll('[data-reels-v15-source].active')]
      .map(btn=>String(btn.dataset.reelsV15Source||''))
      .filter(Boolean)
  );
  if(!selected.size)selected.add('stranger');

  const actors=(typeof getRelationshipActors==='function'?getRelationshipActors():[])
    .filter(actor=>actor && (actor.kind==='roche'||actor.kind==='npc'));

  wrap.innerHTML=[
    `<button class="reels-v15-chip ${selected.has('stranger')?'active':''}" data-reels-v15-source="stranger" type="button">陌生人</button>`,
    ...actors.map(actor=>`
      <button class="reels-v15-chip ${selected.has(actor.id)?'active':''}" data-reels-v15-source="${escapeDMCardHTML(actor.id)}" type="button">
        ${escapeDMCardHTML(actor.name)}
      </button>`)
  ].join('');
}

function getSelectedReelsV15Sources(){
  return [...document.querySelectorAll('#reelsV15SourceChips [data-reels-v15-source].active')]
    .map(btn=>String(btn.dataset.reelsV15Source||''))
    .filter(Boolean);
}

async function runINSReelsGeneration(){
  const btn=document.getElementById('reelsV15GenerateConfirm');
  if(btn?.disabled)return;

  const selected=getSelectedReelsV15Sources();
  if(!selected.length){
    alert('至少选择一个已有角色 / NPC，或选择“陌生人”');
    return;
  }

  const prompt=document.getElementById('reelsV15BatchPrompt')?.value.trim()||'';
  const wantStranger=selected.includes('stranger');
  const formal=selected
    .filter(id=>id!=='stranger')
    .map(normalizeSocialActorId)
    .filter(id=>isFormalSocialActor(id)&&id!=='user');

  setINSGenerationButtonBusy(btn,true,'生成中…');

  try{
    const made=[];
    const commentJobs=[];

    if(formal.length){
      const ownerId=formal.length===1?formal[0]:'user';
      const result=await callNiniINSAI(INS_AI_TASKS.REELS_GENERATE,{
        actorIds:formal,
        ownerId,
        surface:'reels',
        prompt:composeCharGenerationPrompt({actorIds:formal,surface:'reels',batchPrompt:prompt,extraPrompt:buildINSActorLocationPrompt(formal)}),
        extra:{
          requestedCount:formal.length,
          onePerActor:true,
          includeInitialComments:true,
          initialCommentCount:5,
          diagnosticBuild:'5.0.5-reels'
        }
      });
      if(!result.ok)throw new Error(result.reason||'角色 Reels 生成失败');

      const rows=getGeneratedItems(result.data);
      rows.forEach((row,index)=>{
        const actorId=resolveGeneratedActorId(row,formal,index);
        if(!actorId||!formal.includes(actorId))return;
        const actor=getRelationshipActor(actorId);
        const kind=actor?.kind==='npc'?'npc':'char';
        const item=makeGeneratedReelItem(row,actorId,kind,index);
        made.push(item);
        recordGeneratedReelPublish(actorId,item);

        const initialComments=Array.isArray(row?.initialComments)?row.initialComments:[];
        if(initialComments.length){
          commentJobs.push({contentId:item.id,ownerId:actorId,comments:initialComments});
        }
      });
    }

    if(wantStranger){
      const result=await callNiniINSAI(INS_AI_TASKS.REELS_GENERATE,{
        actorIds:[],
        ownerId:'',
        surface:'reels',
        prompt,
        extra:{
          requestedCount:1,
          generateStrangers:true,
          strangerCount:1,
          includeInitialComments:true,
          initialCommentCount:5,
          diagnosticBuild:'5.0.5-reels-stranger'
        }
      });
      if(!result.ok)throw new Error(result.reason||'陌生人 Reels 生成失败');

      const rows=getGeneratedItems(result.data).slice(0,1);
      rows.forEach((row,index)=>{
        const handle=registerGeneratedPublicIdentity(row,index);
        const item=makeGeneratedReelItem(row,handle,'stranger',formal.length+index);
        made.push(item);
        recordGeneratedReelPublish(handle,item,{knownByUser:true});

        const initialComments=Array.isArray(row?.initialComments)?row.initialComments:[];
        if(initialComments.length){
          commentJobs.push({contentId:item.id,ownerId:handle,comments:initialComments});
        }
      });
    }

    if(!made.length)throw new Error('AI 没有返回可用的 Reel items');

    reelsV15State.items.unshift(...made);
    reelsV15State.activeId=made[0].id;

    commentJobs.forEach(job=>{
      applyGeneratedPublicCommentBatch({
        surface:'reels',
        contentId:job.contentId,
        ownerId:job.ownerId,
        comments:job.comments
      });
    });
    made.forEach(item=>{
      queueMicrotask(()=>void ensurePublicCommentFloor('reels',item.id,5).then(()=>scheduleINSReelsPersistence()).catch(err=>console.warn('[INS] Reel initial comment top-up failed',err)));
    });

    scheduleINSReelsPersistence();
    renderReelsV15();
    closeReelsV15Sheets();
    showReelsV15Toast(`已生成 ${made.length} 条 Reel`);
  }catch(error){
    alert('生成 Reel 失败（v5.0.5）\n\n'+(error?.message||error));
  }finally{
    setINSGenerationButtonBusy(btn,false,'','生成 Reel');
  }
}

document.getElementById('reelsV15GenerateBtn')?.addEventListener('click',()=>{
  renderReelsV15GenerationSources();
  openReelsV15Sheet('reelsV15GenerateSheet');
});

document.getElementById('reelsV15SourceChips')?.addEventListener('click',event=>{
  const btn=event.target.closest('[data-reels-v15-source]');
  if(!btn)return;
  btn.classList.toggle('active');
});
document.getElementById('reelsV15GenerateConfirm')?.addEventListener('click',runINSReelsGeneration);

document.getElementById('reelsV15CommentRefresh')?.addEventListener('click',async ()=>{
  const item=getActiveReelsV15Item();
  const btn=document.getElementById('reelsV15CommentRefresh');
  if(!item||btn?.disabled)return;
  setNiniRefreshDotsBusy(btn,true);
  try{
    const count=await generateNextPublicCommentBatch('reels',item.id,5);
    scheduleINSReelsPersistence();
    showReelsV15Toast(`新增 ${count} 条评论`);
  }catch(error){
    alert('生成下一批 Reel 评论失败：'+(error?.message||error));
  }finally{
    setNiniRefreshDotsBusy(btn,false);
  }
});

document.getElementById('reelsV15CommentSend')?.addEventListener('click',()=>{
  const item=getActiveReelsV15Item();
  const input=document.getElementById('reelsV15CommentInput');
  const text=input?.value.trim();
  if(!item||!text)return;
  if(!reelsV15State.comments[item.id])reelsV15State.comments[item.id]=[];
  reelsV15State.comments[item.id].push({initial:'U',author:'you',text});
  item.comments+=1;

  if(typeof recordSocialEvent==='function'){
    const ownerId=normalizeSocialActorId(item.author);
    const known=['user'];
    if(isFormalSocialActor(ownerId))known.push(ownerId);

    recordSocialEvent({
      type:'reel_comment',
      actorId:'user',
      targetIds:known.filter(id=>id!=='user'),
      contentId:item.id,
      contentType:'reel',
      contentOwnerId:ownerId,
      threadId:`reel_comments:${item.id}`,
      text,
      visibility:'public',
      summary:`${socialActorName('user')}评论了 ${socialActorName(ownerId)} 的 Reel：“${text}”`
    },{knownActorIds:known,learnedVia:'direct_comment'});
  }

  input.value='';
  renderReelsV15Comments();
  renderReelsV15();
  scheduleINSReelsPersistence();
});

document.getElementById('reelsV15DMList')?.addEventListener('click',e=>{
  const btn=e.target.closest('[data-reels-v15-recipient]');
  if(!btn)return;
  sendReelV15ToDM(btn.dataset.reelsV15Recipient);
});

document.getElementById('reelsV15EditAction')?.addEventListener('click',()=>{
  const item=getActiveReelsV15Item();
  if(!item)return;
  document.getElementById('reelsV15EditDesc').value=item.videoDescription;
  document.getElementById('reelsV15EditCaption').value=item.caption;
  document.getElementById('reelsV15EditTranslation').value=item.captionTranslation||'';
  document.getElementById('reelsV15EditTags').value=item.tags.join(' ');
  openReelsV15Sheet('reelsV15EditSheet');
});
document.getElementById('reelsV15EditSave')?.addEventListener('click',()=>{
  const item=getActiveReelsV15Item();
  if(!item)return;
  item.videoDescription=document.getElementById('reelsV15EditDesc').value.trim();
  item.caption=document.getElementById('reelsV15EditCaption').value.trim();
  item.captionTranslation=document.getElementById('reelsV15EditTranslation').value.trim();
  item.tags=document.getElementById('reelsV15EditTags').value.trim().split(/\s+/).filter(Boolean);
  scheduleINSReelsPersistence();
  renderReelsV15();
  closeReelsV15Sheets();
  showReelsV15Toast('已保存');
});

document.getElementById('reelsV15ProfileAction')?.addEventListener('click',()=>{
  const item=getActiveReelsV15Item();
  if(!item)return;
  closeReelsV15Sheets();
  if(item.kind==='char'){
    openCharProfile(item.author);
  }else{
    document.getElementById('reelsV15PublicAvatar').textContent=item.initial;
    document.getElementById('reelsV15PublicHandle').textContent='@'+item.author;
    openReelsV15Sheet('reelsV15PublicProfileSheet');
  }
});

document.getElementById('reelsV15DeleteAction')?.addEventListener('click',()=>openReelsV15Sheet('reelsV15DeleteSheet'));
document.getElementById('reelsV15DeleteConfirm')?.addEventListener('click',()=>{
  const item=getActiveReelsV15Item();
  if(!item)return;
  reelsV15State.items=reelsV15State.items.filter(x=>x.id!==item.id);
  if(item.reposted){
    const label=`转发 Reel · @${item.author} · ${item.caption}`;
    userProfileState.reposts=userProfileState.reposts.filter(x=>x!==label);
  }
  reelsV15State.activeId=null;
  scheduleINSReelsPersistence();
  renderReelsV15();
  renderUserProfile();
  closeReelsV15Sheets();
  showReelsV15Toast('已删除 Reel');
});

document.getElementById('reelsV15UserPublish')?.addEventListener('click',()=>{
  const desc=document.getElementById('reelsV15UserVideoDesc').value.trim();
  const caption=document.getElementById('reelsV15UserCaption').value.trim();
  const tags=document.getElementById('reelsV15UserTags').value.trim().split(/\s+/).filter(Boolean);
  if(!desc&&!caption)return;

  const identity={author:'user',initial:(userProfileState?.displayName||'N').slice(0,1)};

  reelsV15State.items.unshift({
    id:'reel_user_'+Date.now(),author:identity.author,initial:identity.initial,kind:'user',
    followed:true,liked:false,reposted:false,translated:false,
    likes:0,comments:0,reposts:0,shares:0,
    videoDescription:desc||'user Reel 视频描述',
    caption:caption||'',
    captionTranslation:'',
    tags
  });

  if(typeof renderUserProfile==='function')renderUserProfile();

  renderReelsV15();
  closeReelsV15Sheets();
  showReelsV15Toast('Reel 已发布');
});

/* Reels first render is deferred until all identity/profile registries are initialized. */


/* bottom nav */
const state={active:'home'};
const screen=document.getElementById('screen');
const bottomNav=document.getElementById('bottomNav');
const pages={
  home:document.getElementById('page-home'),
  reels:document.getElementById('page-reels'),
  dm:document.getElementById('page-dm'),
  profile:document.getElementById('page-profile')
};
const tabs=[...document.querySelectorAll('.tab')];
const navIcons={
  home:document.getElementById('icon-home'),
  reels:document.getElementById('icon-reels'),
  dm:document.getElementById('icon-dm')
};

let __niniPageStabilizeSeq=0;
let __niniReelScrollRAF=0;

function syncReelsV15ActiveFromScroll(){
  const box=document.getElementById('reelsV15Scroller');
  if(!box || !box.children.length)return;
  const h=box.clientHeight;
  if(!(h>0))return;
  const index=Math.max(0,Math.min(box.children.length-1,Math.round(box.scrollTop/h)));
  const node=box.children[index];
  const id=node?.dataset?.reelsV15Id;
  if(id && getReelsV15Item(id))reelsV15State.activeId=id;
}

function stabilizeActiveRootPage(name){
  // Mobile WebView already owns the plugin viewport. Repeated forced layout reads
  // during tab changes/resizes are a major source of full-app stalls and white frames.
  if(window.__NiniINSMobileLite){
    if(name==='home'){
      const feed=document.getElementById('feed');
      if(feed && Array.isArray(posts) && posts.length && feed.childElementCount===0 && typeof renderFeed==='function')renderFeed();
    }else if(name==='reels'){
      const box=document.getElementById('reelsV15Scroller');
      if(box && Array.isArray(reelsV15State.items) && reelsV15State.items.length && box.childElementCount===0)renderReelsV15();
    }
    return;
  }
  const seq=++__niniPageStabilizeSeq;
  requestAnimationFrame(()=>{
    requestAnimationFrame(()=>{
      if(seq!==__niniPageStabilizeSeq || state.active!==name)return;
      const page=pages[name];
      if(!page)return;

      // Layout reads are intentional here: commit the newly-active page geometry
      // before touching nested scrolling/compositor content.
      const pageRect=page.getBoundingClientRect();
      void page.offsetWidth;
      void page.offsetHeight;
      if(!(pageRect.width>0 && pageRect.height>0))return;

      if(name==='home'){
        const feed=document.getElementById('feed');
        if(feed && Array.isArray(posts) && posts.length && feed.childElementCount===0 && typeof renderFeed==='function'){
          renderFeed();
        }
        feed?.getBoundingClientRect();
      }

      if(name==='reels'){
        const box=document.getElementById('reelsV15Scroller');
        if(!box)return;
        if(Array.isArray(reelsV15State.items) && reelsV15State.items.length && box.childElementCount===0){
          renderReelsV15();
        }

        let id=reelsV15State.activeId;
        if(!id || !getReelsV15Item(id)){
          id=reelsV15State.items[0]?.id||null;
          reelsV15State.activeId=id;
        }
        const target=id ? [...box.children].find(node=>node.dataset.reelsV15Id===id) : null;
        const h=box.clientHeight;
        if(target && h>0){
          const desired=target.offsetTop;
          if(Math.abs(box.scrollTop-desired)>2)box.scrollTop=desired;
          target.getBoundingClientRect();
        }
        box.getBoundingClientRect();
      }
    });
  });
}

const __niniReelsScroller=document.getElementById('reelsV15Scroller');
__niniReelsScroller?.addEventListener('scroll',()=>{
  if(__niniReelScrollRAF)return;
  __niniReelScrollRAF=requestAnimationFrame(()=>{
    __niniReelScrollRAF=0;
    syncReelsV15ActiveFromScroll();
  });
},{passive:true});

function renderNav(){
  const dark=state.active==='reels';
  screen.className='screen '+(dark?'dark':'light');
  bottomNav.className='bottom-nav '+(dark?'dark':'light');

  Object.entries(pages).forEach(([k,el])=>el.classList.toggle('active',k===state.active));
  tabs.forEach(tab=>tab.classList.toggle('active',tab.dataset.tab===state.active));

  if(dark){
    navIcons.home.src=NAV_ASSET.homeWhite;
    navIcons.reels.src=NAV_ASSET.reelsBlack;
    navIcons.dm.src=NAV_ASSET.dmWhite;
  }else{
    navIcons.home.src=state.active==='home'?NAV_ASSET.homeBlackSelected:NAV_ASSET.homeBlackOutline;
    navIcons.reels.src=NAV_ASSET.reelsBlack;
    navIcons.dm.src=NAV_ASSET.dmBlack;
  }

  stabilizeActiveRootPage(state.active);
}

window.addEventListener('resize',()=>stabilizeActiveRootPage(state.active),{passive:true});
window.visualViewport?.addEventListener('resize',()=>stabilizeActiveRootPage(state.active),{passive:true});
window.addEventListener('pageshow',()=>stabilizeActiveRootPage(state.active),{passive:true});
document.addEventListener('visibilitychange',()=>{
  if(!document.hidden)stabilizeActiveRootPage(state.active);
});

const sheet=document.getElementById('sheetBackdrop');
function activateBottomTab(name){
  if(!name)return;
  if(name==='compose'){
    openCreatePost();
    return;
  }
  // A bottom-tab jump is a fresh root page. Never leave an old route/editor layer above it.
  document.querySelectorAll('.route-page.show').forEach(el=>el.classList.remove('show'));
  routeStack.length=0;
  clearTransientProfileRoutes();
  state.active=name;
  renderNav();
}
tabs.forEach(tab=>tab.addEventListener('click',()=>activateBottomTab(tab.dataset.tab)));

/* v5.1.11 mobile bottom-nav geometry router.
   The recording showed a stale invisible settings card receiving the Reels tap.
   Resolve the five equal nav zones before any covered child can claim pointerup. */
(function bindBottomNavGeometryRouter(){
  const names=['home','reels','compose','dm','profile'];
  let down=null;
  let suppressClickUntil=0;
  const tabAtPoint=(x,y)=>{
    /* v1.06.1: while the DM chat route is open, the bottom nav is visually covered.
       Never let its geometry router steal taps from the DM composer/AI plane. */
    if(
      document.getElementById('routeDMChat')?.classList.contains('show') ||
      document.getElementById('routeStoryCreate')?.classList.contains('show')
    )return '';
    const rect=bottomNav?.getBoundingClientRect();
    if(!rect || x<rect.left || x>rect.right || y<rect.top-6 || y>rect.bottom+4)return '';
    const unit=Math.max(1,rect.width/names.length);
    return names[Math.max(0,Math.min(names.length-1,Math.floor((x-rect.left)/unit)))]||'';
  };
  document.body.addEventListener('pointerdown',event=>{
    const tab=tabAtPoint(event.clientX,event.clientY);
    down=tab?{pointerId:event.pointerId,x:event.clientX,y:event.clientY,tab}:null;
  },true);
  document.body.addEventListener('pointerup',event=>{
    if(!down || down.pointerId!==event.pointerId){down=null;return;}
    const start=down;
    down=null;
    const moved=Math.hypot(event.clientX-start.x,event.clientY-start.y);
    const tab=tabAtPoint(event.clientX,event.clientY);
    if(moved>=12 || !tab || tab!==start.tab)return;
    suppressClickUntil=Date.now()+550;
    event.preventDefault();
    event.stopImmediatePropagation();
    activateBottomTab(tab);
  },true);
  document.body.addEventListener('pointercancel',()=>{down=null;},true);
  document.body.addEventListener('click',event=>{
    if(Date.now()>suppressClickUntil || !tabAtPoint(event.clientX,event.clientY))return;
    suppressClickUntil=0;
    event.preventDefault();
    event.stopImmediatePropagation();
  },true);
})();
document.getElementById('cancelCompose').addEventListener('click',()=>sheet.classList.remove('show'));
sheet.addEventListener('click',e=>{if(e.target===sheet)sheet.classList.remove('show')});


/* ===== Create post navigation + local UI state ===== */
const createPostState = {
  media: [],
  taggedChars: [],
  maxMedia: 4,
  returnTab: 'home'
};

const createPostOverlay = document.getElementById('createPostOverlay');
const cpPreviewTrack = document.getElementById('cpPreviewTrack');

function openCreatePost(){
  createPostState.returnTab = state.active;
  createPostOverlay.classList.add('show');
  renderCreatePostPreview();
}

function closeCreatePost(){
  createPostOverlay.classList.remove('show');
  // Return to the exact tab that opened it; frozen nav UI is rendered unchanged.
  state.active = createPostState.returnTab || 'home';
  renderNav();
}

function renderCreatePostPreview(){
  document.getElementById('cpCount').textContent = createPostState.media.length + '/4';

  if(createPostState.media.length === 0){
    cpPreviewTrack.innerHTML = `
      <div class="cp-slide">
        <div class="cp-slide-placeholder">
          <svg viewBox="0 0 24 24">
            <rect x="3" y="4" width="18" height="16" rx="2"/>
            <circle cx="8.5" cy="9" r="1.5"/>
            <path d="M4.5 18 10 12.5l3.5 3 2.5-2 3.5 4.5"/>
          </svg>
          <div>添加照片后可左右滑动查看</div>
        </div>
      </div>`;
    return;
  }

  cpPreviewTrack.innerHTML = createPostState.media.map((item,index)=>{
    if(item.type === 'image'){
      return `
        <div class="cp-slide">
          <img src="${item.src}" alt="photo ${index+1}">
          <button class="cp-remove" type="button" data-cp-remove-index="${index}">×</button>
        </div>`;
    }
    return `
      <div class="cp-slide">
        <div class="cp-slide-desc">
          <strong>照片描述草稿</strong>
          ${item.text}
        </div>
        <button class="cp-remove" type="button" data-cp-remove-index="${index}">×</button>
      </div>`;
  }).join('');
}

function createPostHasSpace(){
  if(createPostState.media.length >= createPostState.maxMedia){
    alert('已经有 4 张了');
    return false;
  }
  return true;
}

function openCpModal(id){
  document.getElementById(id).classList.add('show');
}
function closeCpModal(id){
  document.getElementById(id).classList.remove('show');
}

function removeCreatePostMedia(index){
  const safeIndex=Number(index);
  if(!Number.isInteger(safeIndex) || safeIndex<0 || safeIndex>=createPostState.media.length)return;
  createPostState.media.splice(safeIndex,1);
  renderCreatePostPreview();
}
cpPreviewTrack?.addEventListener('click',event=>{
  const btn=event.target.closest('[data-cp-remove-index]');
  if(!btn)return;
  event.preventDefault();
  event.stopPropagation();
  removeCreatePostMedia(Number(btn.dataset.cpRemoveIndex));
});

function renderCreatePostTaggedChars(){
  const label = document.getElementById('cpTagCharLabel');
  const pills = document.getElementById('cpTagCharPills');

  if(createPostState.taggedChars.length){
    label.className = 'cp-value';
    label.textContent = '已标记人物';
    pills.innerHTML = createPostState.taggedChars
      .map(actorId=>{
          const meta=getIdentityMeta(actorId);
          return `<span class="cp-pill">${escapeDMCardHTML(meta.name||meta.handle||actorId)}</span>`;
        })
      .join('');
  }else{
    label.className = 'cp-placeholder';
    label.textContent = '标记人物（选择已有 char）';
    pills.innerHTML = '';
  }
}

document.getElementById('createPostBack').addEventListener('click', closeCreatePost);

document.getElementById('cpCaptureBtn').addEventListener('click', ()=>{
  if(createPostHasSpace()) openCpModal('cpCaptureModal');
});

document.getElementById('cpUploadBtn').addEventListener('click', ()=>{
  if(createPostHasSpace()) openCpModal('cpUploadModal');
});

document.getElementById('cpTagCharField').addEventListener('click', ()=>{
  renderCreatePostCharChoices();
  openCpModal('cpCharModal');
});

document.getElementById('cpStrangerSwitch').addEventListener('click', function(){
  this.classList.toggle('on');
});

document.getElementById('cpSaveDescBtn').addEventListener('click', ()=>{
  if(!createPostHasSpace()) return;
  const text = document.getElementById('cpCaptureDesc').value.trim();
  if(!text){ alert('先写一点照片描述'); return; }
  createPostState.media.push({type:'desc', text});
  document.getElementById('cpCaptureDesc').value = '';
  closeCpModal('cpCaptureModal');
  renderCreatePostPreview();
});

document.getElementById('cpGenerateBtn').addEventListener('click', async ()=>{
  if(!createPostHasSpace()) return;
  const text = document.getElementById('cpCaptureDesc').value.trim();
  if(!text){ alert('先写一点照片描述'); return; }
  const btn=document.getElementById('cpGenerateBtn');
  try{
    const generated=await withINSImageButtonBusy(btn,'生图中…',()=>requestRocheGeneratedImage({description:text,entry:'feed'}));
    createPostState.media.push({type:'image',src:generated.url,text,generated:true,source:'generated'});
    document.getElementById('cpCaptureDesc').value = '';
    closeCpModal('cpCaptureModal');
    renderCreatePostPreview();
  }catch(error){
    const reason=String(error?.message||error||'未知错误');
    alert(reason==='bridge_not_connected'?'当前没有连接到 Roche 生图接口。请先在 Roche 生图设置里完成配置。':`生图失败：${reason}`);
  }
});

function renderCreatePostPlaceholderChoices(){
  const box=document.getElementById('cpPlaceholderGrid');
  if(!box)return;
  box.innerHTML=(INS_FEED_FALLBACK_MEDIA_POOL||[]).map((item,index)=>`
    <button class="cp-placeholder-choice" data-cp-placeholder-index="${index}" type="button" aria-label="占位图 ${index+1}">
      <img src="${escapeDMCardHTML(item.url||'')}" alt="占位图 ${index+1}">
    </button>
  `).join('');
}
document.getElementById('cpPlaceholderBtn')?.addEventListener('click',()=>{
  if(!createPostHasSpace())return;
  renderCreatePostPlaceholderChoices();
  openCpModal('cpPlaceholderModal');
});
document.getElementById('cpPlaceholderGrid')?.addEventListener('click',event=>{
  const btn=event.target.closest('[data-cp-placeholder-index]');
  if(!btn || !createPostHasSpace())return;
  const picked=INS_FEED_FALLBACK_MEDIA_POOL[Number(btn.dataset.cpPlaceholderIndex)];
  if(!picked?.url)return;
  const desc=document.getElementById('cpCaptureDesc')?.value.trim()||'';
  createPostState.media.push({type:'image',src:picked.url,text:desc,placeholder:true});
  const descInput=document.getElementById('cpCaptureDesc');
  if(descInput)descInput.value='';
  closeCpModal('cpPlaceholderModal');
  closeCpModal('cpCaptureModal');
  renderCreatePostPreview();
});

document.getElementById('cpDeviceBtn').addEventListener('click', ()=>{
  document.getElementById('cpFileInput').click();
});

document.getElementById('cpFileInput').addEventListener('change', e=>{
  const files = [...e.target.files];
  const remaining = createPostState.maxMedia - createPostState.media.length;
  files.slice(0,remaining).forEach(file=>{
    createPostState.media.push({
      type:'image',
      src:URL.createObjectURL(file),
      local:true
    });
  });
  e.target.value = '';
  closeCpModal('cpUploadModal');
  renderCreatePostPreview();
});

document.getElementById('cpAddUrlBtn').addEventListener('click', ()=>{
  if(!createPostHasSpace()) return;
  const url = document.getElementById('cpUrlInput').value.trim();
  if(!url){ alert('先粘贴图片 URL'); return; }
  createPostState.media.push({type:'image',src:url});
  document.getElementById('cpUrlInput').value = '';
  closeCpModal('cpUploadModal');
  renderCreatePostPreview();
});

document.querySelectorAll('.cp-close-modal').forEach(btn=>{
  btn.addEventListener('click', ()=>{
    const modal = btn.closest('.cp-modal');
    if(modal) modal.classList.remove('show');
  });
});

document.querySelectorAll('.cp-modal').forEach(modal=>{
  modal.addEventListener('click', e=>{
    if(e.target === modal) modal.classList.remove('show');
  });
});

function renderCreatePostCharChoices(){
  const box=document.getElementById('cpCharChoiceList');
  if(!box)return;
  const actors=getRelationshipActors().filter(actor=>actor.kind==='roche'||actor.kind==='npc');
  box.innerHTML=actors.length
    ? actors.map(actor=>{
        const selected=createPostState.taggedChars.includes(actor.id);
        return `<button class="cp-sheet-btn gray cp-char-choice" data-char="${escapeDMCardHTML(actor.id)}" type="button">${selected?'✓ ':''}${escapeDMCardHTML(actor.name)}</button>`;
      }).join('')
    : '<div class="cp-sheet-hint">还没有已绑定角色 / 社交联系人。</div>';
}

async function publishUserFeedPost(){
  if(createPostState.media.length===0){
    alert('请先添加照片或照片描述');
    return;
  }
  const caption=document.getElementById('cpCaption').value.trim();
  const tagsRaw=document.getElementById('cpTag').value.trim();
  const inlineTags=normalizeFeedPostTags((caption.match(/(^|\s)#[^\s#]+/g)||[]).map(x=>String(x).trim()));
  const tags=normalizeFeedPostTags([...normalizeFeedPostTags(tagsRaw),...inlineTags]);
  const captionParts=splitGeneratedCaptionText(stripGeneratedCaptionHashtags(caption));
  const location=document.getElementById('cpLocation').value.trim();
  const taggedIds=[...new Set(createPostState.taggedChars.map(normalizeSocialActorId).filter(isFormalSocialActor))];
  const mediaItems=createPostState.media.slice(0,4).map((item,index)=>{
    if(item.type==='image')return {id:`user_media_${Date.now()}_${index}`,url:item.src,desc:item.text||''};
    const fallback=INS_FEED_FALLBACK_MEDIA_POOL[(Date.now()+index)%INS_FEED_FALLBACK_MEDIA_POOL.length];
    return {id:`user_desc_${Date.now()}_${index}`,url:fallback?.url||'',desc:item.text||''};
  });
  const first=mediaItems[0]||{};
  const post={
    id:'user_post_'+Date.now(),type:'user',followed:true,author:'user',
    avatar:(userProfileState.displayName||userProfileState.id||'U').slice(0,1).toUpperCase(),
    sub:location||'',mediaClass:'m1',mediaText:first.desc||'user 发布图片',mediaDescription:first.desc||'',mediaUrl:'',
    mediaItems,mediaCarouselIndex:0,mediaFlipped:false,mediaType:'image',linkedAlbumId:'',mediaSource:'user_upload',
    likes:Math.max(0,Math.round(userProfileState.followers*(0.12+Math.random()*0.28))),
    comments:Math.max(0,Math.round(userProfileState.followers*(0.02+Math.random()*0.08))),
    reposts:Math.max(0,Math.round(userProfileState.followers*(0.005+Math.random()*0.025))),
    shares:Math.max(0,Math.round(userProfileState.followers*(0.008+Math.random()*0.03))),
    liked:false,reposted:false,expanded:false,translated:false,homeTranslationOpen:false,
    captionOriginal:stripGeneratedCaptionHashtags(caption).trim(),
    captionTranslation:'',
    homeCaptionOriginal:stripGeneratedCaptionHashtags(caption).trim(),
    homeCaptionTranslation:'',
    captionShort:captionParts.short?` ${escapeDMCardHTML(captionParts.short)}`:'',
    captionLong:captionParts.long?` ${escapeDMCardHTML(captionParts.long)}`:'',
    tags,
    translation:'',taggedChars:taggedIds,
    allowStrangerComments:document.getElementById('cpStrangerSwitch')?.classList.contains('on')!==false
  };
  posts.unshift(post);
  commentsByPost[post.id]=[];
  if(typeof recordSocialEvent==='function'){
    recordSocialEvent({
      type:'feed_publish',actorId:'user',targetIds:taggedIds,contentId:post.id,contentType:'feed',contentOwnerId:'user',
      text:caption,visibility:'public',
      payload:{tags,location,taggedActorIds:taggedIds,allowStrangerComments:post.allowStrangerComments},
      summary:`${socialActorName('user')}发布了一条 IG 动态${taggedIds.length?'并标记了 '+taggedIds.map(socialActorName).join('、'):''}。`
    },{knownActorIds:['user',...taggedIds],learnedVia:taggedIds.length?'direct_tag':'self_publish'});
  }
  createPostState.media=[];createPostState.taggedChars=[];
  document.getElementById('cpCaption').value='';document.getElementById('cpTag').value='';document.getElementById('cpLocation').value='';
  renderCreatePostPreview();renderCreatePostTaggedChars();
  closeCreatePost();state.active='home';renderFeed();renderUserProfile();renderNav();
}

document.getElementById('cpCharModal')?.addEventListener('click',event=>{
  const btn=event.target.closest?.('.cp-char-choice[data-char]');
  if(!btn)return;
  const actorId=normalizeSocialActorId(btn.dataset.char);
  if(!actorId)return;
  if(createPostState.taggedChars.includes(actorId))createPostState.taggedChars=createPostState.taggedChars.filter(id=>id!==actorId);
  else createPostState.taggedChars.push(actorId);
  renderCreatePostTaggedChars();renderCreatePostCharChoices();
});

document.addEventListener('click',event=>{
  const btn=event.target.closest?.('#cpShareBtn');
  if(!btn)return;
  event.preventDefault();event.stopPropagation();
  void publishUserFeedPost();
},{capture:true});


renderCreatePostPreview();
renderCreatePostTaggedChars();



/* ===== v1.04 Feed -> DM real share card ===== */
let activeSharePostId = null;


function makeDMContextCard(kind,data={}){
  const card={
    kind,
    createdAt:Date.now(),
    author:data.author||'',
    caption:data.caption||'',
    mediaUrl:data.mediaUrl||'',
    mediaDescription:data.mediaDescription||'',
    mediaClass:data.mediaClass||'m1',
    emoji:data.emoji||'',
    attitude:data.attitude||'',
    storyId:data.storyId||'',
    storyDescription:data.storyDescription||'',
    contextSummary:data.contextSummary||'',
    cardId:data.cardId||`dmcard_${Date.now()}_${Math.random().toString(36).slice(2,7)}`,
    postId:data.postId||data.contentId||'',
    senderId:data.senderId||'user',
    source:data.source||'linked',
    contentType:data.contentType||((kind==='reel')?'reel':(kind==='post'?'feed':'')),
    displayName:data.displayName||'',
    captionTranslation:data.captionTranslation||'',
    sourceLanguage:data.sourceLanguage||'',
    location:data.location||'',
    tags:Array.isArray(data.tags)?data.tags:[],
    metrics:data.metrics&&typeof data.metrics==='object'?{...data.metrics}:{},
    comments:Array.isArray(data.comments)?data.comments.map(row=>({...row})):[]
  };
  if(!card.contextSummary){
    if(kind==='story_reaction'){
      card.contextSummary=`user 对 @${card.author} 这一组 Story 使用 ${card.emoji}，表达：${card.attitude}。Story 组：${card.storyDescription}`;
    }else if(kind==='story_share'){
      card.contextSummary=`user 分享了 @${card.author} 的一组 Story。文案：${card.caption || '无'}。媒体：${card.mediaDescription || 'Story 内容'}`;
    }else if(kind==='reel'){
      card.contextSummary=`user 在 DM 中分享了 @${card.author} 的 Reel：${card.caption || card.mediaDescription}`;
    }else{
      card.contextSummary=`user 在 DM 中分享了 @${card.author} 的 IG 动态：${card.caption || card.mediaDescription}`;
    }
  }
  return card;
}
function compactDMContextText(text,maxChars=180){
  const clean=String(text||'').replace(/\s+/g,' ').trim();
  return clean.length>maxChars ? clean.slice(0,maxChars-1)+'…' : clean;
}
function getDMContextCards(recipient,limit=8){
  const cards=(dmSharedCards[recipient]||[]).slice(-Math.max(1,limit));
  const seen=new Set();
  const rows=[];
  for(const card of cards){
    const summary=compactDMContextText(card.contextSummary||'',180);
    if(!summary || seen.has(summary)) continue;
    seen.add(summary);
    rows.push(summary);
  }
  return rows;
}
function getDMModelContext(recipient,limit=8){
  const rows=[];
  const seen=new Set();

  const push=summary=>{
    const clean=compactDMContextText(summary||'',180);
    if(!clean || seen.has(clean))return;
    seen.add(clean);
    rows.push(clean);
  };

  if(typeof getActorKnowledgeContext==='function'){
    getActorKnowledgeContext(recipient,limit).forEach(push);
  }
  getDMContextCards(recipient,limit).forEach(push);

  return rows.slice(-Math.max(1,limit)).map((summary,index)=>({
    index:index+1,
    summary
  }));
}

const dmSharedCards = {};

function escapeDMCardHTML(value){
  return String(value ?? '')
    .replaceAll('&','&amp;')
    .replaceAll('<','&lt;')
    .replaceAll('>','&gt;')
    .replaceAll('"','&quot;')
    .replaceAll("'",'&#039;');
}

function getSharePostSnapshot(postId){
  const post = posts.find(p=>p.id === postId);
  if(!post) return null;

  return makeDMContextCard('post',{
    postId: post.id,
    author: post.author,
    caption: [
      (post.captionShort || '').replace(/<[^>]+>/g,''),
      (post.captionLong || '').replace(/<[^>]+>/g,'')
    ].join('').trim(),
    mediaUrl: post.mediaUrl || '',
    mediaDescription: post.mediaDescription || post.mediaText || '',
    mediaClass: post.mediaClass || 'm1',
    senderId:'user',source:'linked',contentType:'feed',sourceLanguage:post.sourceLanguage||post.feedSourceLanguage||'',captionTranslation:post.captionTranslation||'',location:post.location||post.sub||'',tags:post.tags||[],metrics:{likes:post.likes||0,comments:post.comments||0,reposts:post.reposts||0,shares:post.shares||0}
  });
}

const dmProfiles = {};


function getUnifiedDMShareRecipients(){
  const map=new Map();

  const add=(profile,key='')=>{
    if(!profile)return;
    const id=String(profile.rocheId||key||profile.handle||'').trim();
    if(!id || id==='user')return;
    if(profile.handle===userProfileState?.id)return;
    map.set(id,{
      id,
      handle:profile.handle||id,
      name:profile.name||profile.handle||id,
      initial:profile.initial||String(profile.name||profile.handle||id).slice(0,1).toUpperCase(),
      avatarUrl:profile.avatarUrl||'',
      profileType:profile.profileType||'npc'
    });
  };

  Object.entries(dmProfiles||{}).forEach(([key,profile])=>add(profile,key));

  if(typeof charProfileState!=='undefined'){
    Object.entries(charProfileState.profiles||{}).forEach(([key,profile])=>add(profile,key));
  }

  document.querySelectorAll('#dmV2List [data-dm-chat]').forEach(row=>{
    const id=row.dataset.dmChat;
    if(!id || map.has(id))return;
    const name=row.querySelector('.dm-v2-name')?.textContent?.trim() || id;
    add({
      rocheId:id,
      handle:id,
      name,
      initial:name.slice(0,1).toUpperCase(),
      profileType:row.dataset.kind==='npc'?'npc':'roche'
    },id);
  });

  return [...map.values()].sort((a,b)=>a.name.localeCompare(b.name));
}

function renderFeedShareDMRecipients(){
  const box=document.getElementById('feedShareDMList');
  if(!box)return;
  const rows=getUnifiedDMShareRecipients();
  box.innerHTML=rows.length ? rows.map(row=>`
    <div class="dm-choice">
      <div class="dm-choice-avatar" data-identity-avatar="${escapeDMCardHTML(row.id||row.handle)}">${row.avatarUrl?'':escapeDMCardHTML(row.initial)}</div>
      <strong>${escapeDMCardHTML(row.name)}</strong>
      <button class="route-btn light" data-share-recipient="${escapeDMCardHTML(row.id||row.handle)}" type="button">发送</button>
    </div>
  `).join('') : '<div class="route-empty"><span>当前没有可分享的 DM 联系人。</span></div>';
  hydrateIdentityAvatars(box);
}

function renderReelsV15DMRecipients(){
  const box=document.getElementById('reelsV15DMList');
  if(!box)return;
  const rows=getUnifiedDMShareRecipients();
  box.innerHTML=rows.length ? rows.map(row=>`
    <div class="reels-v15-row">
      <div class="reels-v15-sheet-avatar" data-identity-avatar="${escapeDMCardHTML(row.id||row.handle)}">${row.avatarUrl?'':escapeDMCardHTML(row.initial)}</div>
      <div class="reels-v15-row-main">
        <strong>${escapeDMCardHTML(row.name)}</strong>
        <span>@${escapeDMCardHTML(row.handle)} · ${row.profileType==='npc'?'固定 NPC':'Roche char'}</span>
      </div>
      <button class="reels-v15-sheet-action" data-reels-v15-recipient="${escapeDMCardHTML(row.id||row.handle)}" type="button">发送</button>
    </div>
  `).join('') : '<div class="reels-v15-note">当前没有可分享的 DM 联系人。</div>';
  hydrateIdentityAvatars(box);
}

const dmBaseMessages = {};

/* ===== INS v5.0 persistent DM history =====
   Roche storage is the source of truth across plugin opens.
   We keep the INS DM timeline separate from Roche chat history.
*/
let niniPersistedDMState={
  version:1,
  messages:{},
  sharedCards:{},
  updatedAt:0
};
let niniDMPersistTimer=0;

function cloneINSDMJSON(value,fallback){
  try{return JSON.parse(JSON.stringify(value));}
  catch(e){return fallback;}
}

function normalizeINSDMStoredState(raw){
  const state=raw&&typeof raw==='object'?raw:{};
  return {
    version:1,
    messages:state.messages&&typeof state.messages==='object'?cloneINSDMJSON(state.messages,{}):{},
    sharedCards:state.sharedCards&&typeof state.sharedCards==='object'?cloneINSDMJSON(state.sharedCards,{}):{},
    updatedAt:Number(state.updatedAt)||0
  };
}

function sanitizeINSDMMessage(row={}){
  return {
    from:String(row.from||'me'),
    text:String(row.text||''),
    translation:String(row.translation||''),
    translated:!!row.translated,
    createdAt:Number(row.createdAt)||0,
    _uiId:String(row._uiId||''),
    _memoryEventId:String(row._memoryEventId||'')
  };
}

function sanitizeINSDMCard(card={}){
  const copy=cloneINSDMJSON(card,{});
  // Browser blob URLs cannot survive a new plugin session.
  if(typeof copy.mediaUrl==='string' && copy.mediaUrl.startsWith('blob:'))copy.mediaUrl='';
  return copy;
}

function snapshotINSDMHistory(){
  const messages={};
  const sharedCards={};

  Object.entries(dmBaseMessages||{}).forEach(([actorId,rows])=>{
    if(!Array.isArray(rows) || !rows.length)return;
    messages[actorId]=rows.map(sanitizeINSDMMessage);
  });

  Object.entries(dmSharedCards||{}).forEach(([actorId,rows])=>{
    if(!Array.isArray(rows) || !rows.length)return;
    sharedCards[actorId]=rows.map(sanitizeINSDMCard);
  });

  niniPersistedDMState={
    version:1,
    messages,
    sharedCards,
    updatedAt:Date.now()
  };
  return cloneINSDMJSON(niniPersistedDMState,{version:1,messages:{},sharedCards:{},updatedAt:Date.now()});
}

function getStoredINSDMMessages(actorId){
  return cloneINSDMJSON(niniPersistedDMState.messages?.[String(actorId)]||[],[]);
}

function getStoredINSDMCards(actorId){
  return cloneINSDMJSON(niniPersistedDMState.sharedCards?.[String(actorId)]||[],[]);
}

function applyPersistedINSDMHistory(raw){
  niniPersistedDMState=normalizeINSDMStoredState(raw);
  return niniPersistedDMState;
}

async function flushINSDMHistoryPersistence(){
  clearTimeout(niniDMPersistTimer);
  niniDMPersistTimer=0;
  const snapshot=snapshotINSDMHistory();
  try{
    if(window.NiniINSHost && typeof window.NiniINSHost.setDMHistoryState==='function'){
      await window.NiniINSHost.setDMHistoryState(snapshot);
      return {ok:true,updatedAt:snapshot.updatedAt};
    }
    return {ok:false,reason:'host_storage_not_connected'};
  }catch(error){
    console.warn('[INS] 保存 DM 历史失败',error);
    return {ok:false,reason:error?.message||'save_failed'};
  }
}

function scheduleINSDMHistoryPersistence(){
  // Update the in-session snapshot immediately so unimport/reimport will not lose messages.
  snapshotINSDMHistory();
  clearTimeout(niniDMPersistTimer);
  niniDMPersistTimer=setTimeout(()=>{
    void flushINSDMHistoryPersistence();
  },160);
}

window.NiniINSDMHistory={
  apply:applyPersistedINSDMHistory,
  snapshot:snapshotINSDMHistory,
  schedule:scheduleINSDMHistoryPersistence,
  flush:flushINSDMHistoryPersistence,
  getState:()=>cloneINSDMJSON(niniPersistedDMState,{version:1,messages:{},sharedCards:{}})
};

function getDMProfile(recipient){
  const base = dmProfiles[recipient] || {initial:'?', name:recipient, handle:recipient, followers:0};
  const identityId=String(base.rocheId||recipient||base.handle||'');
  if(typeof charProfileState !== 'undefined'){
    const profile = charProfileState.profiles?.[recipient];
    if(profile){
      return {
        ...base,identityId:String(profile.rocheId||identityId),
        initial:profile.initial || base.initial,
        name:profile.name || base.name,
        handle:profile.handle || base.handle,
        avatarUrl:profile.avatarUrl || base.avatarUrl || '',
        followers:Number.isFinite(Number(profile.followers)) ? profile.followers : base.followers
      };
    }
  }
  return {...base,identityId};
}

const STRANGER_AVATAR_URL='https://imgbed.heliar.top/i/z_LF4zw588PIQySz_1787642229596.webp';
const publicIdentityRegistry={};

function getIdentityMeta(ref){
  const raw=String(ref??'').replace(/^@/,'');
  const userId=(typeof userProfileState!=='undefined' ? userProfileState.id : 'user');

  if(raw==='user' || raw===userId || raw===String(userProfileState?.rochePersonaId||'')){
    const name=(typeof userProfileState!=='undefined' ? userProfileState.displayName : 'User') || 'User';
    const avatarUrl=(typeof getUserAvatarUrl==='function' ? getUserAvatarUrl() : '');
    return {
      id:'user',
      handle:userId,
      name,
      initial:name.slice(0,1).toUpperCase(),
      avatarUrl:avatarUrl||''
    };
  }

  const findEntry=obj=>{
    if(!obj)return null;
    if(obj[raw])return [raw,obj[raw]];
    return Object.entries(obj).find(([key,p])=>
      key===raw ||
      String(p?.rocheId||'')===raw ||
      String(p?.handle||'')===raw ||
      String(p?.name||'')===raw
    ) || null;
  };

  const dmEntry=findEntry(typeof dmProfiles!=='undefined'?dmProfiles:null);
  const profileEntry=findEntry(typeof charProfileState!=='undefined'?charProfileState.profiles:null);
  const merged=dmEntry||profileEntry
    ? {...(dmEntry?.[1]||{}),...(profileEntry?.[1]||{})}
    : null;

  if(merged){
    const key=profileEntry?.[0]||dmEntry?.[0]||raw;
    const id=String(merged.rocheId||key||raw);
    const handle=String(merged.handle||id);
    const name=merged.name||merged.displayName||handle;
    return {
      id,
      handle,
      name,
      initial:merged.initial||String(name).slice(0,1).toUpperCase(),
      avatarUrl:merged.avatarUrl||''
    };
  }

  const pub=publicIdentityRegistry[raw];
  if(pub){
    const name=pub.name||pub.displayName||pub.handle||raw;
    return {id:raw,handle:pub.handle||raw,name,initial:pub.initial||String(name).slice(0,1).toUpperCase(),avatarUrl:STRANGER_AVATAR_URL};
  }

  return {
    id:raw,
    handle:raw,
    name:raw || 'unknown',
    initial:raw && !raw.startsWith('public_') && !raw.startsWith('viewer_') ? raw.slice(0,1).toUpperCase() : '?',
    avatarUrl:raw?STRANGER_AVATAR_URL:''
  };
}

function hydrateIdentityAvatars(root=document){
  root.querySelectorAll?.('[data-identity-avatar],[data-actor-avatar]').forEach(el=>{
    const ref=el.getAttribute('data-identity-avatar') || el.getAttribute('data-actor-avatar') || '';
    const meta=getIdentityMeta(ref);
    el.textContent=meta.avatarUrl?'':meta.initial;
    el.style.backgroundImage=meta.avatarUrl?`url("${meta.avatarUrl}")`:'';
    el.style.backgroundSize='cover';
    el.style.backgroundPosition='center';
    el.style.backgroundRepeat='no-repeat';
  });
}

/* ===== v1.05 Reels persistence: generated Reels only ===== */
let niniPersistedReelsState={version:1,items:[],comments:{},updatedAt:0};
let niniReelsPersistTimer=0;

function normalizeINSReelsStoredState(raw){
  const state=raw&&typeof raw==='object'?raw:{};
  const items=Array.isArray(state.items)?cloneINSDMJSON(state.items,[]):[];
  const comments=state.comments&&typeof state.comments==='object'?cloneINSDMJSON(state.comments,{}):{};
  return {version:1,items:items.filter(item=>item&&item.generated===true&&item.id),comments,updatedAt:Number(state.updatedAt)||0};
}

function snapshotINSReelsPersistence(){
  const items=(reelsV15State.items||[])
    .filter(item=>item&&item.generated===true&&item.id)
    .map(item=>cloneINSDMJSON(item,{}));
  const ids=new Set(items.map(item=>String(item.id)));
  const comments={};
  ids.forEach(id=>{
    if(Array.isArray(reelsV15State.comments?.[id]))comments[id]=cloneINSDMJSON(reelsV15State.comments[id],[]);
  });
  niniPersistedReelsState={version:1,items,comments,updatedAt:Date.now()};
  return cloneINSDMJSON(niniPersistedReelsState,{version:1,items:[],comments:{},updatedAt:Date.now()});
}

function applyPersistedINSReelsState(raw){
  niniPersistedReelsState=normalizeINSReelsStoredState(raw);
  const currentNonGenerated=(reelsV15State.items||[]).filter(item=>!item?.generated);
  const storedItems=niniPersistedReelsState.items.map(item=>cloneINSDMJSON(item,{}));
  reelsV15State.items=[...storedItems,...currentNonGenerated.filter(item=>!storedItems.some(saved=>saved.id===item.id))];
  Object.entries(niniPersistedReelsState.comments||{}).forEach(([id,rows])=>{
    if(storedItems.some(item=>item.id===id))reelsV15State.comments[id]=cloneINSDMJSON(rows,[]);
  });
  if(storedItems.length)reelsV15State.activeId=storedItems[0].id;
  else if(!getReelsV15Item(reelsV15State.activeId))reelsV15State.activeId=reelsV15State.items[0]?.id||null;
  return niniPersistedReelsState;
}

async function flushINSReelsPersistence(){
  clearTimeout(niniReelsPersistTimer);
  niniReelsPersistTimer=0;
  const snapshot=snapshotINSReelsPersistence();
  try{
    if(window.NiniINSHost && typeof window.NiniINSHost.setReelsState==='function'){
      await window.NiniINSHost.setReelsState(snapshot);
      return {ok:true,updatedAt:snapshot.updatedAt};
    }
    return {ok:false,reason:'host_storage_not_connected'};
  }catch(error){
    console.warn('[INS] 保存 Reels 失败',error);
    return {ok:false,reason:error?.message||'save_failed'};
  }
}

function scheduleINSReelsPersistence(){
  snapshotINSReelsPersistence();
  clearTimeout(niniReelsPersistTimer);
  niniReelsPersistTimer=setTimeout(()=>{void flushINSReelsPersistence();},160);
}

window.NiniINSReelsPersistence={
  apply:applyPersistedINSReelsState,
  snapshot:snapshotINSReelsPersistence,
  schedule:scheduleINSReelsPersistence,
  flush:flushINSReelsPersistence,
  getState:()=>cloneINSDMJSON(niniPersistedReelsState,{version:1,items:[],comments:{}})
};

function calcCharPostCount(handle){
  return posts.filter(p => p.author === handle && p.type !== 'stranger').length;
}

function renderDMBaseMessages(recipient){
  const box = document.getElementById('dmChatMessages');
  if(!box) return;

  const profile = getDMProfile(recipient);
  const rows = dmBaseMessages[recipient] || [];

  box.innerHTML = rows.map((row,index)=>{
    if(row.from === 'photo'){
      return `<div class="dm-char-photo" data-dm-msg-id="${escapeDMCardHTML(row._uiId||'')}"><div class="photo-box">${escapeDMCardHTML(row.text).replace(/\n/g,'<br>')}</div></div>`;
    }
    if(row.from === 'them'){
      return `<div class="dm-msg-row them" data-dm-msg-index="${index}"><div class="dm-msg-avatar">${profile.initial}</div><div class="dm-bubble" title="长按删除">${escapeDMCardHTML(row.text)}</div></div>`;
    }
    return `<div class="dm-msg-row me" data-dm-msg-index="${index}"><div class="dm-bubble" title="长按删除">${escapeDMCardHTML(row.text)}</div></div>`;
  }).join('');
}

function renderDMSharedCards(recipient){
  const stream = document.getElementById('dmSharedCardsSlot');
  if(!stream) return;

  const cards = dmSharedCards[recipient] || [];
  if(!cards.length){
    stream.innerHTML = '';
    return;
  }

  stream.innerHTML = cards.map(card=>{
    if(card.kind==='story_reaction'){
      return `
        <div class="dm-sent-row">
          <div class="dm-context-card compact">
            <div class="dm-context-kicker">Story 反应</div>
            <div class="dm-context-main"><span class="dm-context-emoji">${escapeDMCardHTML(card.emoji)}</span><strong>你对 @${escapeDMCardHTML(card.author)} 这一组 Story 表达了反应</strong></div>
            <div class="dm-context-summary">${escapeDMCardHTML(card.attitude)} · ${escapeDMCardHTML(card.storyDescription)}</div>
          </div>
        </div>`;
    }

    if(card.kind==='story_share'){
      return `
        <div class="dm-sent-row">
          <div class="dm-story-share-card">
            <div class="dm-story-share-kicker">你分享了一组 Story</div>
            <div class="dm-story-share-title">@${escapeDMCardHTML(card.author)}</div>
            <div class="dm-story-share-copy">${escapeDMCardHTML(card.caption || '无文案')} · ${escapeDMCardHTML(card.mediaDescription || 'Story 内容')}</div>
          </div>
        </div>`;
    }

    const media = card.mediaUrl
      ? `<img src="${escapeDMCardHTML(card.mediaUrl)}" alt="分享的 IG 动态">`
      : escapeDMCardHTML(card.mediaDescription).replace(/\n/g,'<br>');

    return `
      <div class="dm-sent-row">
        <div class="dm-post-card">
          <div class="dm-post-card-media ${escapeDMCardHTML(card.mediaClass)}">${media}</div>
          <div class="dm-post-card-copy">
            <div class="dm-post-card-label">${card.kind === 'reel' ? '你分享了一条 Reel' : '你分享了一条 IG 动态'}</div>
            <div class="dm-post-card-author">@${escapeDMCardHTML(card.author)}</div>
            <div class="dm-post-card-caption">${escapeDMCardHTML(card.caption || 'IG 动态')}</div>
          </div>
        </div>
      </div>`;
  }).join('');
}


/* ===== v1.15.19 chronological DM timeline =====
   Shared Story/Feed/Reel cards used to live in a separate slot after all text messages,
   so a newly sent text could appear visually above an older share card. Merge both
   sources by createdAt at render time; storage remains unchanged.
*/
function renderDMTimelineMessageHTML(row,profile){
  if(row.from==='photo'){
    return `<div class="dm-char-photo"><div class="photo-box">${escapeDMCardHTML(row.text).replace(/\n/g,'<br>')}</div></div>`;
  }

  const canTranslate=row.from==='them' && shouldOfferTranslation(row.text,row.translation);
  const bubble=`
    <div class="dm-msg-stack">
      <div class="dm-bubble ${canTranslate?'dm-bubble-translatable':''}" ${canTranslate?`data-dm-translate="${escapeDMCardHTML(row._uiId||'')}"`:''} title="长按删除">
        <div class="dm-bubble-original">${escapeDMCardHTML(row.text)}</div>
        ${canTranslate&&row.translated?`<div class="dm-bubble-translation">${escapeDMCardHTML(row.translation)}</div>`:''}
      </div>
    </div>`;

  if(row.from==='them'){
    return `<div class="dm-msg-row them" data-dm-msg-id="${escapeDMCardHTML(row._uiId||'')}"><div class="dm-msg-avatar" data-identity-avatar="${escapeDMCardHTML(profile.identityId||profile.handle)}">${profile.avatarUrl?'':escapeDMCardHTML(profile.initial)}</div>${bubble}</div>`;
  }
  return `<div class="dm-msg-row me" data-dm-msg-id="${escapeDMCardHTML(row._uiId||'')}">${bubble}</div>`;
}

function renderDMTimelineCardHTML(card){
  if(card.kind==='story_reaction'){
    return `
      <div class="dm-sent-row">
        <div class="dm-context-card compact">
          <div class="dm-context-kicker">Story 反应</div>
          <div class="dm-context-main"><span class="dm-context-emoji">${escapeDMCardHTML(card.emoji)}</span><strong>你对 @${escapeDMCardHTML(card.author)} 这一组 Story 表达了反应</strong></div>
          <div class="dm-context-summary">${escapeDMCardHTML(card.attitude)} · ${escapeDMCardHTML(card.storyDescription)}</div>
        </div>
      </div>`;
  }

  if(card.kind==='story_share'){
    return `
      <div class="dm-sent-row">
        <div class="dm-story-share-card">
          <div class="dm-story-share-kicker">你分享了一组 Story</div>
          <div class="dm-story-share-title">@${escapeDMCardHTML(card.author)}</div>
          <div class="dm-story-share-copy">${escapeDMCardHTML(card.caption || '无文案')} · ${escapeDMCardHTML(card.mediaDescription || 'Story 内容')}</div>
        </div>
      </div>`;
  }

  if(!card.cardId)card.cardId=`dmcard_${Date.now()}_${Math.random().toString(36).slice(2,7)}`;
  const type=(card.contentType||card.kind)==='reel'?'reel':'feed';
  const incoming=normalizeSocialActorId(card.senderId||'user')!=='user';
  const sender=incoming?getDMProfile(card.senderId):null;
  const label=incoming
    ? `@${escapeDMCardHTML(sender?.handle||card.senderId)} 分享了一条 ${type==='reel'?'Reel':'IG 动态'}`
    : `你分享了一条 ${type==='reel'?'Reel':'IG 动态'}`;
  const media=card.mediaUrl
    ? `<img src="${escapeDMCardHTML(card.mediaUrl)}" alt="分享内容">`
    : escapeDMCardHTML(String(card.mediaDescription||'').replace(/^Reel 视频描述\s*/,'')).replace(/\n/g,'<br>');
  const canTranslateCaption=shouldOfferTranslation(card.caption||'',card.captionTranslation||'');
  const cardHtml=`
    <div class="dm-share-card-v120 ${type}" data-dm-share-card="${escapeDMCardHTML(card.cardId)}" role="button" tabindex="0">
      <div class="dm-share-media-v120">${media|| (type==='reel'?'Reel':'IG 动态')}</div>
      <div class="dm-share-copy-v120">
        <div class="dm-share-kicker-v120">${label}</div>
        <div class="dm-share-author-v120">@${escapeDMCardHTML(card.author||'unknown')}</div>
        <div class="dm-share-caption-v120">${escapeDMCardHTML(card.caption||'')}</div>
        ${canTranslateCaption?`<button class="dm-share-card-translate-toggle-v124" data-dm-share-card-translate="${escapeDMCardHTML(card.cardId)}" type="button">${card.translationOpen?'收起翻译':'查看翻译'}</button><div class="dm-share-card-translation-v124 ${card.translationOpen?'show':''}">${escapeDMCardHTML(card.captionTranslation||'')}</div>`:''}
        <div class="dm-share-open-v120">点击查看 · 评论区</div>
      </div>
    </div>`;
  if(incoming){
    const identity=sender?.identityId||card.senderId||'';
    return `<div class="dm-msg-row them"><div class="dm-msg-avatar" data-identity-avatar="${escapeDMCardHTML(identity)}">${sender?.avatarUrl?'':escapeDMCardHTML(sender?.initial||'?')}</div>${cardHtml}</div>`;
  }
  return `<div class="dm-sent-row">${cardHtml}</div>`;
}

function renderDMConversationTimeline(recipient){
  const box=document.getElementById('dmChatMessages');
  const legacySlot=document.getElementById('dmSharedCardsSlot');
  if(!box)return;

  const profile=getDMProfile(recipient);
  const messages=(dmBaseMessages[recipient]||[]).map((row,index)=>{
    if(!row._uiId)row._uiId=`dm_${recipient}_${index}_${Number(row.createdAt)||0}`;
    return {
      kind:'message',
      row,
      /* Legacy seed messages predate timestamps; keep them before real runtime events. */
      ts:Number(row.createdAt)||index+1,
      tie:index
    };
  });

  const cards=(dmSharedCards[recipient]||[]).map((card,index)=>({
    kind:'card',
    card,
    ts:Number(card.createdAt)||Date.now()+index,
    tie:index
  }));

  const timeline=[...messages,...cards].sort((a,b)=>{
    if(a.ts!==b.ts)return a.ts-b.ts;
    /* When two runtime actions land in the same millisecond, show the share card first
       and the subsequently typed message after it. */
    if(a.kind!==b.kind)return a.kind==='card'?-1:1;
    return a.tie-b.tie;
  });

  box.innerHTML=timeline.map(item=>
    item.kind==='card'
      ? renderDMTimelineCardHTML(item.card)
      : renderDMTimelineMessageHTML(item.row,profile)
  ).join('');

  if(legacySlot)legacySlot.innerHTML='';
  hydrateIdentityAvatars(box);
}

function renderDMChatStream(recipient){
  const profile = getDMProfile(recipient);

  const topAvatar = document.getElementById('dmChatTopAvatar');
  const bigAvatar = document.getElementById('dmChatBigAvatar');

  topAvatar.textContent = profile.avatarUrl ? '' : profile.initial;
  bigAvatar.textContent = profile.avatarUrl ? '' : profile.initial;
  [topAvatar,bigAvatar].forEach(el=>{
    el.classList.toggle('has-image',!!profile.avatarUrl);
    el.style.backgroundImage = profile.avatarUrl ? `url("${profile.avatarUrl}")` : '';
  });

  document.getElementById('dmChatTopName').textContent = profile.name;
  document.getElementById('dmChatTopHandle').textContent = profile.handle;
  document.getElementById('dmChatCardName').textContent = profile.name;
  document.getElementById('dmChatCardHandle').textContent = '@' + profile.handle;
  document.getElementById('dmChatCardStats').textContent =
    profile.followers + ' 粉丝 · ' + calcCharPostCount(profile.handle) + ' 帖子';

  renderDMConversationTimeline(recipient);

  requestAnimationFrame(()=>{
    const scroller = document.getElementById('dmChatScroll');
    if(scroller) scroller.scrollTop = scroller.scrollHeight;
  });
}

document.getElementById('dmChatMessages')?.addEventListener('click',event=>{
  const bubble=event.target.closest('.dm-bubble[data-dm-translate]');
  if(!bubble || !currentDMChat)return;
  const row=(dmBaseMessages[currentDMChat]||[]).find(item=>item._uiId===bubble.dataset.dmTranslate);
  if(!row || !shouldOfferTranslation(row.text,row.translation))return;
  row.translated=!row.translated;
  scheduleINSDMHistoryPersistence();
  renderDMChatStream(currentDMChat);
});

function sendFeedPostToDM(recipient){
  const snapshot = getSharePostSnapshot(activeSharePostId);
  if(!snapshot) return;

  if(!dmSharedCards[recipient]){
    dmSharedCards[recipient] = [];
  }

  dmSharedCards[recipient].push(snapshot);
  scheduleINSDMHistoryPersistence();
  updateDMListPreview(recipient,'分享了一条 IG 动态');

  if(typeof recordSocialEvent==='function'){
    const recipientId=normalizeSocialActorId(recipient);
    const ownerId=normalizeSocialActorId(snapshot.author);
    recordSocialEvent({
      type:'dm_share_content',
      actorId:'user',
      targetIds:[recipientId],
      contentId:snapshot.postId||activeSharePostId||'',
      contentType:'feed',
      contentOwnerId:ownerId,
      visibility:'private',
      payload:{
        sharedType:'IG 动态',
        sharedOwnerId:ownerId,
        sharedOwnerLabel:snapshot.author,
        caption:snapshot.caption||'',
        sharedSummary:snapshot.caption||snapshot.mediaDescription||''
      },
      summary:`${socialActorName('user')}在 DM 给 ${socialActorName(recipientId)} 分享了 ${socialActorName(ownerId)} 的 IG 动态。`
    },{knownActorIds:['user',recipientId],learnedVia:'dm_share'});
    if(isFormalSocialActor(recipientId))exposePublicContentToActor(recipientId,snapshot.postId||activeSharePostId||'','dm_share');
  }

  // Roche write-back is handled by the unified Social Event / Knowledge bridge.
  closeRouteSheet('sheetShareDM');

  const toast = document.getElementById('dmShareToast');
  if(toast){
    const displayName = getUnifiedDMShareRecipients()
      .find(row=>(row.id||row.handle)===recipient)?.name || recipient;
    toast.textContent = '已发送给 ' + displayName;
    toast.classList.add('show');
    clearTimeout(window.__dmShareToastTimer);
    window.__dmShareToastTimer = setTimeout(()=>{
      toast.classList.remove('show');
    }, 1200);
  }
}

document.getElementById('feedShareDMList')?.addEventListener('click',e=>{
  const btn=e.target.closest('[data-share-recipient]');
  if(!btn)return;
  sendFeedPostToDM(btn.dataset.shareRecipient);
});



/* ===== v1.05 DM Chat composer/settings behavior ===== */
function dmCloseCards(){
  document.getElementById('dmChatDim')?.classList.remove('show');
  document.getElementById('dmEmojiCard')?.classList.remove('show');
  document.getElementById('dmChatMenuCard')?.classList.remove('show');
  document.getElementById('dmClearConfirmCard')?.classList.remove('show');
}
function dmOpenCard(id){
  dmCloseCards();
  document.getElementById('dmChatDim')?.classList.add('show');
  document.getElementById(id)?.classList.add('show');
}

function getDMListRow(recipient){
  return document.querySelector(`#dmV2List [data-dm-chat="${CSS.escape(recipient||'')}"]`);
}
function formatDMListPreviewMessage(row){
  if(!row)return '暂无消息';
  if(row.from==='photo') return '照片';
  return row.text || '暂无消息';
}
function updateDMListPreview(recipient, customText=''){
  const row=getDMListRow(recipient);
  if(!row)return;
  const preview=row.querySelector('.dm-v2-preview');
  const time=row.querySelector('.dm-v2-time');

  let text=customText;
  if(!text){
    const messages=dmBaseMessages[recipient]||[];
    text=formatDMListPreviewMessage(messages.at(-1));
  }

  if(preview)preview.textContent=text || '暂无消息';
  if(time)time.textContent='刚刚';
}
function syncAllDMListPreviews(){
  Object.keys(dmBaseMessages).forEach(recipient=>updateDMListPreview(recipient));
}

function addDMMessage(recipient, from, text, translation=''){
  if(!recipient)return;
  if(!dmBaseMessages[recipient])dmBaseMessages[recipient]=[];
  const createdAt=Date.now();
  const normalizedText=typeof text==='object'?String(text.text||text.textOriginal||''):String(text||'');
  const normalizedTranslation=typeof text==='object'?String(text.translation||text.textTranslation||''):String(translation||'');
  const messageRow={from,text:normalizedText,translation:normalizedTranslation,translated:false,createdAt,_uiId:`dm_${recipient}_${createdAt}_${Math.random().toString(36).slice(2,5)}`,_memoryEventId:''};
  dmBaseMessages[recipient].push(messageRow);

  if(typeof recordSocialEvent==='function' && normalizedText){
    const recipientId=normalizeSocialActorId(recipient);
    const actorId=from==='them'?recipientId:'user';
    const targetId=from==='them'?'user':recipientId;
    if(isFormalSocialActor(actorId)&&isFormalSocialActor(targetId)){
      const event=recordSocialEvent({type:'dm_message',actorId,targetIds:[targetId],contentId:`dm:${recipientId}`,contentType:'dm',text:normalizedText,visibility:'private',createdAt,dedupeKey:`dm_message:${messageRow._uiId}`,summary:`${socialActorName(actorId)}在 INS 私信里对 ${socialActorName(targetId)} 说：“${compactMemoryText(normalizedText,120)}”`},{knownActorIds:[actorId,targetId],learnedVia:'dm_message'});
      if(event?.eventId)messageRow._memoryEventId=event.eventId;
    }
  }

  scheduleINSDMHistoryPersistence();
  updateDMListPreview(recipient);
  renderDMChatStream(recipient);
  if(from==='them'&&normalizedText&&isFormalSocialActor(recipient))pushINSNotification({actorId:recipient,type:'dm',copy:'发来了一条新私信'});
}

function dmSendInput(){
  const input = document.getElementById('dmMsgInput');
  const text = input?.value.trim() || '';
  if(!text) return;
  addDMMessage(currentDMChat, 'me', text);
  input.value = '';
}
let dmAIReplyInFlight=false;

const DM_SHARE_INTENT_STORAGE_KEY='nini-ins-dm-share-intent-v120';
let dmShareIntentState=(()=>{try{return JSON.parse(localStorage.getItem(DM_SHARE_INTENT_STORAGE_KEY)||'{}')||{};}catch(e){return {};}})();
let dmShareDetailActive=null;
function persistDMShareIntentState(){try{localStorage.setItem(DM_SHARE_INTENT_STORAGE_KEY,JSON.stringify(dmShareIntentState));}catch(e){}}
function detectDMExplicitShareRequest(text=''){
  const t=String(text||'').toLowerCase();
  return /(发|分享|找|给我看|看看).{0,10}(ig|instagram|动态|帖子|feed|reel|视频)|(reel|feed|动态|帖子).{0,10}(发|分享|给我看|看看)/i.test(t);
}
function getDMShareCandidates(actorId,limit=12){
  const id=normalizeSocialActorId(actorId);
  const known=new Set((typeof getActorKnowledgeEntries==='function'?getActorKnowledgeEntries(id):[]).map(row=>String(row?.event?.contentId||'')).filter(Boolean));
  const rows=[];
  posts.forEach(post=>rows.push({contentId:post.id,contentType:'feed',author:post.author,caption:compactDMContextText(ensureHomeFeedCaptionModel(post).chinese||post.captionOriginal||'',120),mediaDescription:compactDMContextText(post.mediaDescription||post.mediaText||'',120),known:known.has(String(post.id)),own:normalizeSocialActorId(post.author)===id}));
  (reelsV15State.items||[]).forEach(item=>rows.push({contentId:item.id,contentType:'reel',author:item.author,caption:compactDMContextText(item.caption||'',120),mediaDescription:compactDMContextText(item.videoDescription||'',120),known:known.has(String(item.id)),own:normalizeSocialActorId(item.author)===id}));
  rows.sort((a,b)=>(Number(b.own)-Number(a.own))||(Number(b.known)-Number(a.known)));
  return rows.slice(0,Math.max(1,Number(limit)||12));
}
function normalizeDMShareCommentRow(row={}){
  const rawAuthor=row.author;
  const author=typeof rawAuthor==='string'?rawAuthor:String(rawAuthor?.handle||row.handle||'guest_'+Math.random().toString(36).slice(2,6));
  return {author,displayName:String(row.displayName||rawAuthor?.displayName||author),text:String(row.textOriginal??row.text??''),translation:String(row.textTranslation??row.translation??''),sourceLanguage:String(row.sourceLanguage||''),translated:false,generated:true};
}
function findDMShareCard(recipient,cardId){return (dmSharedCards[recipient]||[]).find(card=>String(card.cardId||'')===String(cardId||''))||null;}
function resolveDMShareCardContent(card){
  if(!card)return null;
  const type=(card.contentType||card.kind)==='reel'?'reel':'feed';
  if(card.source==='dm_only')return {type,source:'dm_only',id:card.postId||card.cardId,author:card.author,caption:card.caption||'',captionTranslation:card.captionTranslation||'',mediaUrl:card.mediaUrl||'',mediaDescription:card.mediaDescription||'',sourceLanguage:card.sourceLanguage||'',location:card.location||'',tags:card.tags||[],metrics:card.metrics||{}};
  if(type==='feed'){
    let post=posts.find(row=>String(row.id)===String(card.postId||''));
    if(!post)post=posts.find(row=>String(row.author)===String(card.author)&&String(ensureHomeFeedCaptionModel(row).chinese||row.captionOriginal||'')===String(card.caption||''));
    if(post)return {type,source:'linked',id:post.id,author:post.author,caption:ensureHomeFeedCaptionModel(post).chinese||post.captionOriginal||'',captionTranslation:post.captionTranslation||'',mediaUrl:post.mediaUrl||'',mediaDescription:post.mediaDescription||post.mediaText||'',sourceLanguage:post.sourceLanguage||post.feedSourceLanguage||'',location:post.location||post.sub||'',tags:post.tags||[],metrics:{likes:post.likes||0,comments:post.comments||0,reposts:post.reposts||0,shares:post.shares||0}};
  }else{
    let item=getReelsV15Item(card.postId||'');
    if(!item)item=(reelsV15State.items||[]).find(row=>String(row.author)===String(card.author)&&String(row.caption||'')===String(card.caption||''));
    if(item)return {type,source:'linked',id:item.id,author:item.author,caption:item.caption||'',captionTranslation:item.captionTranslation||'',mediaUrl:'',mediaDescription:item.videoDescription||'',sourceLanguage:item.sourceLanguage||'',location:item.location||'',tags:item.tags||[],metrics:{likes:item.likes||0,comments:item.comments||0,reposts:item.reposts||0,shares:item.shares||0}};
  }
  return {type,source:card.source||'snapshot',id:card.postId||card.cardId,author:card.author,caption:card.caption||'',captionTranslation:card.captionTranslation||'',mediaUrl:card.mediaUrl||'',mediaDescription:card.mediaDescription||'',sourceLanguage:card.sourceLanguage||'',location:card.location||'',tags:card.tags||[],metrics:card.metrics||{}};
}
function getDMShareDetailComments(card,content){
  if(!card||!content)return [];
  if(card.source==='dm_only')return card.comments||[];
  if(content.type==='feed')return commentsByPost[content.id]||[];
  return reelsV15State.comments[content.id]||[];
}
function ensureDMShareDetailV120(){
  let el=document.getElementById('dmShareDetailV120');if(el)return el;
  el=document.createElement('section');el.id='dmShareDetailV120';el.className='dm-share-detail-v120';
  el.innerHTML=`<div class="dm-share-detail-panel-v124"><div class="dm-share-detail-head-v120"><button class="dm-share-detail-back-v120" id="dmShareDetailBackV120" type="button">‹</button><div class="dm-share-detail-title-v120"><strong id="dmShareDetailTypeV120">IG 动态</strong><span id="dmShareDetailAuthorV120"></span></div></div><div class="dm-share-detail-scroll-v120"><div class="dm-share-detail-media-v120" id="dmShareDetailMediaV120"></div><div class="dm-share-detail-body-v120"><div class="dm-share-detail-caption-v120" id="dmShareDetailCaptionV120"></div><button class="dm-share-detail-caption-translate-toggle-v124" id="dmShareDetailCaptionToggleV124" type="button" style="display:none">查看翻译</button><div class="dm-share-detail-caption-translation-v124" id="dmShareDetailCaptionTranslationV124"></div><div class="dm-share-detail-meta-v120" id="dmShareDetailMetaV120"></div><div class="dm-share-comments-head-v120"><strong>评论</strong><span id="dmShareDetailCommentCountV120"></span></div><div id="dmShareDetailCommentsV120"></div><button class="dm-share-load-v120" id="dmShareDetailLoadV120" type="button">再加载 5 条评论</button></div></div></div>`;
  (document.querySelector('.phone')||document.body).appendChild(el);
  el.querySelector('#dmShareDetailBackV120')?.addEventListener('click',closeDMShareCardDetail);
  el.querySelector('#dmShareDetailLoadV120')?.addEventListener('click',()=>void loadMoreDMShareCardComments());
  el.addEventListener('click',event=>{if(event.target===el)closeDMShareCardDetail();});
  return el;
}
function closeDMShareCardDetail(){const el=document.getElementById('dmShareDetailV120');el?.classList.remove('show','reel');dmShareDetailActive=null;}
function toggleDMShareCardTranslation(cardId){
  if(!currentDMChat||!cardId)return;
  const card=findDMShareCard(currentDMChat,String(cardId));
  if(!card)return;
  card.translationOpen=!card.translationOpen;
  renderDMConversationTimeline(currentDMChat);
}
function toggleDMShareDetailCaptionTranslation(){
  if(!dmShareDetailActive)return;
  const card=findDMShareCard(dmShareDetailActive.recipient,dmShareDetailActive.cardId);
  if(!card)return;
  card.detailCaptionTranslationOpen=!card.detailCaptionTranslationOpen;
  renderDMShareCardDetail();
}
function toggleDMShareDetailCommentTranslation(index){
  if(!dmShareDetailActive)return;
  const card=findDMShareCard(dmShareDetailActive.recipient,dmShareDetailActive.cardId);
  const content=resolveDMShareCardContent(card);
  const comments=getDMShareDetailComments(card,content);
  const row=comments?.[Number(index)];
  if(!row)return;
  row.__dmShareTranslationOpen=!row.__dmShareTranslationOpen;
  renderDMShareCardDetail();
}
function renderDMShareCardDetail(){
  if(!dmShareDetailActive)return;
  const card=findDMShareCard(dmShareDetailActive.recipient,dmShareDetailActive.cardId);if(!card){closeDMShareCardDetail();return;}
  const content=resolveDMShareCardContent(card);if(!content)return;
  const el=ensureDMShareDetailV120();el.classList.toggle('reel',content.type==='reel');el.classList.add('show');
  document.getElementById('dmShareDetailTypeV120').textContent=content.type==='reel'?'Reels':'IG 动态';
  document.getElementById('dmShareDetailAuthorV120').textContent='@'+(content.author||'unknown');
  const media=document.getElementById('dmShareDetailMediaV120');if(media)media.innerHTML=content.mediaUrl?`<img src="${escapeDMCardHTML(content.mediaUrl)}" alt="分享内容">`:escapeDMCardHTML(String(content.mediaDescription||'')).replace(/\n/g,'<br>');
  const cap=document.getElementById('dmShareDetailCaptionV120');if(cap)cap.textContent=content.caption||'';
  const captionToggle=document.getElementById('dmShareDetailCaptionToggleV124');
  const captionTranslation=document.getElementById('dmShareDetailCaptionTranslationV124');
  const canTranslateCaption=shouldOfferTranslation(content.caption||'',content.captionTranslation||card.captionTranslation||'');
  if(captionToggle&&captionTranslation){
    if(canTranslateCaption){
      captionToggle.style.display='inline-flex';
      captionToggle.textContent=card.detailCaptionTranslationOpen?'收起翻译':'查看翻译';
      captionTranslation.textContent=content.captionTranslation||card.captionTranslation||'';
      captionTranslation.classList.toggle('show',!!card.detailCaptionTranslationOpen);
    }else{
      captionToggle.style.display='none';
      captionTranslation.textContent='';
      captionTranslation.classList.remove('show');
    }
  }
  const meta=document.getElementById('dmShareDetailMetaV120');if(meta)meta.textContent=[content.location,(content.tags||[]).join(' '),card.source==='dm_only'?'仅保留在本次 DM':'' ].filter(Boolean).join(' · ');
  const comments=getDMShareDetailComments(card,content);
  const count=document.getElementById('dmShareDetailCommentCountV120');if(count)count.textContent=comments.length+' 条';
  const box=document.getElementById('dmShareDetailCommentsV120');if(box)box.innerHTML=comments.map((row,index)=>{const original=row.text||row.textOriginal||'';const translated=row.translation||row.textTranslation||'';const canTranslate=shouldOfferTranslation(original,translated);return `<div class="dm-share-comment-v120"><b>@${escapeDMCardHTML(row.author||'guest')}</b><p>${escapeDMCardHTML(original)}</p>${canTranslate?`<button class="dm-share-comment-translate-toggle-v124" data-dm-share-comment-translate="${index}" type="button">${row.__dmShareTranslationOpen?'收起翻译':'查看翻译'}</button><div class="dm-share-comment-translation-v120 ${row.__dmShareTranslationOpen?'show':''}">${escapeDMCardHTML(translated)}</div>`:''}</div>`;}).join('')||'<div class="dm-share-comment-v120"><p>还没有评论。</p></div>';
}
function openDMShareCardDetail(cardId){if(!currentDMChat)return;dmShareDetailActive={recipient:currentDMChat,cardId:String(cardId||'')};renderDMShareCardDetail();}
async function loadMoreDMShareCardComments(){
  if(!dmShareDetailActive)return;
  const card=findDMShareCard(dmShareDetailActive.recipient,dmShareDetailActive.cardId);const content=resolveDMShareCardContent(card);if(!card||!content)return;
  const btn=document.getElementById('dmShareDetailLoadV120');if(btn?.disabled)return;if(btn){btn.disabled=true;btn.textContent='生成中…';}
  try{
    if(card.source!=='dm_only'&&content.id){
      await generateNextPublicCommentBatch(content.type==='feed'?'feed':'reels',content.id,5);
    }else{
      const existing=(card.comments||[]).map(row=>({author:row.author,text:row.text||''})).slice(-12);
      const result=await callNiniINSAI(INS_AI_TASKS.PUBLIC_COMMENT_BATCH,{actorIds:[],ownerId:card.author||'',surface:content.type==='feed'?'feed':'reels',content:{caption:content.caption,mediaDescription:content.type==='feed'?content.mediaDescription:'',videoDescription:content.type==='reel'?content.mediaDescription:'',existingComments:existing},prompt:'Generate exactly 5 new natural public comments for this DM-only shared content. Do not repeat existing comments.',extra:{actualCommentCount:5,appendBatch:true,allowKnownActors:true,allowStrangers:true,forbidCurrentUser:true}});
      if(!result.ok)throw new Error(result.reason||'评论生成失败');
      const rows=Array.isArray(result.data?.comments)?result.data.comments:[];if(!rows.length)throw new Error('AI 没有返回 comments');
      card.comments=Array.isArray(card.comments)?card.comments:[];card.comments.push(...rows.slice(0,5).map(normalizeDMShareCommentRow));scheduleINSDMHistoryPersistence();
    }
    renderDMShareCardDetail();
  }catch(error){alert('加载评论失败：'+(error?.message||error));}
  finally{if(btn){btn.disabled=false;btn.textContent='再加载 5 条评论';}}
}
function recordActorSawSharedLinkedContent(actorId,content){
  if(!content||!actorId||typeof recordSocialEvent!=='function')return;
  const existing=(insSocialMemoryState?.events||[]).find(event=>event.visibility==='public'&&String(event.contentId||'')===String(content.id||''));
  if(existing){grantActorKnowledge(actorId,existing.eventId,'dm_share_seen');return;}
  recordSocialEvent({type:content.type==='reel'?'reel_seen':'feed_seen',actorId,targetIds:[],contentId:content.id,contentType:content.type,contentOwnerId:normalizeSocialActorId(content.author),text:content.caption||'',visibility:'public',payload:{caption:content.caption||'',mediaDescription:content.type==='feed'?content.mediaDescription:'',videoDescription:content.type==='reel'?content.mediaDescription:''},summary:`${socialActorName(actorId)}刷到了 @${content.author} 的 ${content.type==='reel'?'Reel':'IG 动态'}。`},{knownActorIds:[actorId],learnedVia:'dm_share_seen',forceEvent:true});
}
function createActorPublishedDMShare(actorId,type,data={}){
  const kind=(typeof isFixedNPCHandle==='function'&&isFixedNPCHandle(actorId))?'npc':'char';
  if(type==='feed'){
    const row={captionZh:String(data.captionZh??data.caption??data.captionOriginal??''),sourceLanguage:data.sourceLanguage||'',mediaItems:Array.isArray(data.mediaItems)?data.mediaItems:[{mediaDescription:data.mediaDescription||'生活随手记录',mediaUrl:data.mediaUrl||''}],location:data.location||'',tags:data.tags||[],metrics:data.metrics||{},initialComments:Array.isArray(data.initialComments)?data.initialComments:[]};
    const post=makeGeneratedFeedPost(row,actorId,kind,0);post.followed=true;posts.unshift(post);commentsByPost[post.id]=[];applyGeneratedPublicCommentBatch({surface:'feed',contentId:post.id,ownerId:actorId,comments:row.initialComments.slice(0,5)});recordGeneratedFeedPublish(actorId,post);renderFeed();queueMicrotask(()=>void ensurePublicCommentFloor('feed',post.id,5).catch(()=>{}));
    return makeDMContextCard('post',{postId:post.id,author:post.author,caption:ensureHomeFeedCaptionModel(post).chinese,mediaUrl:post.mediaUrl||'',mediaDescription:post.mediaDescription||post.mediaText||'',mediaClass:post.mediaClass||'m1',senderId:actorId,source:'linked',contentType:'feed',sourceLanguage:post.sourceLanguage||'',location:post.location||'',tags:post.tags||[],metrics:{likes:post.likes||0,comments:post.comments||0,reposts:post.reposts||0,shares:post.shares||0}});
  }
  const row={captionOriginal:String(data.captionOriginal??data.caption??''),captionTranslation:String(data.captionTranslation||''),sourceLanguage:String(data.sourceLanguage||''),videoDescription:String(data.videoDescription??data.mediaDescription??'随手短视频'),location:data.location||'',tags:data.tags||[],metrics:data.metrics||{},initialComments:Array.isArray(data.initialComments)?data.initialComments:[]};
  const reel=makeGeneratedReelItem(row,actorId,kind,0);reelsV15State.items.unshift(reel);reelsV15State.comments[reel.id]=row.initialComments.slice(0,5).map(normalizeDMShareCommentRow);recordGeneratedReelPublish(actorId,reel);renderReelsV15();scheduleINSReelsPersistence();queueMicrotask(()=>void ensurePublicCommentFloor('reels',reel.id,5).catch(()=>{}));
  return makeDMContextCard('reel',{postId:reel.id,author:reel.author,caption:reel.caption,captionTranslation:reel.captionTranslation||'',mediaUrl:'',mediaDescription:reel.videoDescription,mediaClass:'m1',senderId:actorId,source:'linked',contentType:'reel',sourceLanguage:reel.sourceLanguage||'',location:reel.location||'',tags:reel.tags||[],metrics:{likes:reel.likes||0,comments:reel.comments||0,reposts:reel.reposts||0,shares:reel.shares||0}});
}
function createActorDMOnlyShare(actorId,type,data={}){
  const author=String(data.author||data.handle||data.actor?.handle||'shared.today').replace(/^@/,'');
  const caption=String(data.captionOriginal??data.captionZh??data.caption??'');
  const description=String(type==='reel'?(data.videoDescription??data.mediaDescription??'随手短视频'):(data.mediaDescription??data.mediaItems?.[0]?.mediaDescription??'生活随手记录'));
  return makeDMContextCard(type==='reel'?'reel':'post',{postId:`dm_only_${Date.now()}_${Math.random().toString(36).slice(2,6)}`,author,displayName:String(data.displayName||data.actor?.displayName||author),caption,captionTranslation:String(data.captionTranslation||''),mediaUrl:type==='feed'?String(data.mediaUrl||data.mediaItems?.[0]?.mediaUrl||''):'',mediaDescription:description,mediaClass:'m1',senderId:actorId,source:'dm_only',contentType:type,sourceLanguage:String(data.sourceLanguage||''),location:String(data.location||''),tags:Array.isArray(data.tags)?data.tags:[],metrics:data.metrics||{},comments:(Array.isArray(data.initialComments)?data.initialComments:[]).slice(0,5).map(normalizeDMShareCommentRow)});
}
function detectDMRequestedShareType(text=''){
  const t=String(text||'').toLowerCase();
  if(/reel|短视频|视频/.test(t))return 'reel';
  if(/feed|ig|instagram|动态|帖子/.test(t))return 'feed';
  return '';
}
function looksLikeDMAffirmativeShareReply(rows=[]){
  const text=(Array.isArray(rows)?rows:[]).map(row=>String(row?.textOriginal??row?.text??'')).join(' ').trim();
  if(!text)return false;
  return /(喏|给你|发你|分享给你|这个呢|你看这个|看这个|找到了|就这个|这个可以|这个给你|刷到一个|我发一个|我发给你)/i.test(text)
    && !/(不发|不给|不想发|懒得发|下次再说|以后再说|才不给|不行|拒绝)/i.test(text);
}
function extractDMShareDecision(data,rows=[]){
  if(!data||typeof data!=='object')return null;
  const candidates=[data.share,data.dmShare,data.shareDecision,data.share_action];
  for(const candidate of candidates){if(candidate&&typeof candidate==='object')return {...candidate};}
  for(const row of (Array.isArray(rows)?rows:[])){
    const candidate=row?.share||row?.dmShare||row?.shareDecision;
    if(candidate&&typeof candidate==='object')return {...candidate};
  }
  return null;
}
function repairDMShareDecisionForExplicitRequest(actorId,share,lastUserText,rows=[]){
  const id=normalizeSocialActorId(actorId);
  const requestedType=detectDMRequestedShareType(lastUserText);
  const all=getDMShareCandidates(id,30);
  const pick=(type='')=>all.find(row=>!type||row.contentType===type)||null;
  let next=share&&typeof share==='object'?{...share}:null;

  // v1.2.3: an explicit User request should no longer fall back to built-in/linked stock.
  // If the character says they are sharing now, mark it as a fresh dm_only share; the fresh card is generated below.
  if(looksLikeDMAffirmativeShareReply(rows)){
    const currentDecision=String(next?.decision||next?.action||'').toLowerCase();
    const currentSource=String(next?.source||'').toLowerCase();
    const hasRenderableOwn=next&&currentDecision==='share'&&currentSource==='own_publish'&&next.content&&typeof next.content==='object';
    if(!hasRenderableOwn){
      next={...(next||{}),decision:'share',source:'dm_only',contentType:requestedType||String(next?.contentType||next?.type||'').toLowerCase()||'feed',relevance:1};
      delete next.contentId;
    }
  }
  if(!next)return null;
  const decision=String(next.decision||next.action||'none').toLowerCase();
  if(decision!=='share')return next;
  let type=String(next.contentType||next.type||requestedType||'').toLowerCase();
  if(type!=='feed'&&type!=='reel')type=pick(requestedType)?.contentType||'';
  if(!type)return {...next,decision:'defer'};
  next.contentType=type;
  let source=String(next.source||'linked').toLowerCase();
  if(!['linked','dm_only','own_publish'].includes(source))source='linked';
  next.source=source;

  if(source==='linked'){
    const valid=all.find(row=>String(row.contentId)===String(next.contentId||'')&&row.contentType===type);
    if(!valid){
      const fallback=pick(type);
      if(fallback)next.contentId=fallback.contentId;
      else return {...next,decision:'defer'};
    }
  }else if(source==='dm_only'){
    const content=next.content&&typeof next.content==='object'?next.content:{};
    const hasUseful=String(content.captionOriginal??content.captionZh??content.caption??content.videoDescription??content.mediaDescription??'').trim();
    if(!hasUseful){
      const fallback=pick(type);
      if(fallback)next={...next,source:'linked',contentId:fallback.contentId};
    }
  }
  return next;
}

async function generateFreshDMOnlyShareV123(actorId,type,lastUserText=''){
  const shareType=type==='reel'?'reel':'feed';
  const topic=String(lastUserText||'').trim();
  const avoid=(dmSharedCards[currentDMChat]||[]).slice(-8).map(card=>({
    type:card.contentType||card.kind,
    author:card.author||'',
    caption:card.caption||'',
    description:card.mediaDescription||''
  }));
  const commonPrompt=`为当前 INS 私信临时生成一条全新的 ${shareType==='reel'?'Reel':'Feed'} 分享内容。它只存在于这段 DM，不发布到公开 Feed/Reels，也不进入任何角色主页。必须是本次新生成的内容，不要复用内置示例、已有候选、最近已经分享过的作者/文案/主题。根据 User 当前请求和聊天语境自然决定题材；如果请求没有指定主题，就生成角色此刻自然会挑给 User 看的内容。评论区同时生成 5 条自然评论，保持现有多语言与真实网感规则。User 原话：${topic||'未指定主题'}。最近已分享内容（仅用于避重）：${JSON.stringify(avoid)}`;
  if(shareType==='reel'){
    const result=await callNiniINSAI(INS_AI_TASKS.REELS_GENERATE,{
      actorIds:[],ownerId:'',surface:'reels',prompt:commonPrompt,
      extra:{requestedCount:1,generateStrangers:true,strangerCount:1,includeInitialComments:true,initialCommentCount:5,dmOnlyShare:true,doNotPublish:true,avoidRecentShares:true}
    });
    if(!result.ok)throw new Error(result.reason||'DM Reel 生成失败');
    const row=getGeneratedItems(result.data)[0];
    if(!row)throw new Error('DM Reel 没有返回内容');
    return {
      decision:'share',source:'dm_only',contentType:'reel',
      content:{
        author:String(row?.actor?.handle||row?.author||row?.handle||`reel_${Date.now().toString(36)}`).replace(/^@/,''),
        displayName:String(row?.actor?.displayName||row?.actor?.name||row?.displayName||row?.author||'Reel'),
        captionOriginal:String(row?.captionOriginal??row?.caption??row?.textOriginal??row?.text??''),
        captionTranslation:String(row?.captionTranslation??row?.translation??row?.textTranslation??''),
        sourceLanguage:String(row?.sourceLanguage||''),
        videoDescription:String(row?.videoDescription??row?.mediaDescription??'随手短视频'),
        location:String(row?.location||''),tags:Array.isArray(row?.tags)?row.tags:[],metrics:row?.metrics||{},
        initialComments:Array.isArray(row?.initialComments)?row.initialComments.slice(0,5):[]
      }
    };
  }
  const result=await callNiniINSAI(INS_AI_TASKS.HOME_STRANGERS,{
    actorIds:[],ownerId:'',surface:'feed',prompt:commonPrompt,
    extra:{requestedCount:1,requestedMediaCounts:[1],includeInitialComments:true,initialCommentCount:5,dmOnlyShare:true,doNotPublish:true,avoidRecentShares:true}
  });
  if(!result.ok)throw new Error(result.reason||'DM Feed 生成失败');
  const row=getGeneratedItems(result.data)[0];
  if(!row)throw new Error('DM Feed 没有返回内容');
  const mediaItems=Array.isArray(row?.mediaItems)?row.mediaItems:[];
  return {
    decision:'share',source:'dm_only',contentType:'feed',
    content:{
      author:String(row?.actor?.handle||row?.author||row?.handle||`feed_${Date.now().toString(36)}`).replace(/^@/,''),
      displayName:String(row?.actor?.displayName||row?.actor?.name||row?.displayName||row?.author||'Feed'),
      captionZh:String(row?.captionZh??row?.captionOriginal??row?.caption??row?.text??''),
      sourceLanguage:String(row?.sourceLanguage||''),
      mediaDescription:String(mediaItems[0]?.mediaDescription??row?.mediaDescription??row?.mediaText??'生活随手记录'),
      mediaUrl:String(mediaItems[0]?.mediaUrl??row?.mediaUrl??''),
      location:String(row?.location||''),tags:Array.isArray(row?.tags)?row.tags:[],metrics:row?.metrics||{},
      initialComments:Array.isArray(row?.initialComments)?row.initialComments.slice(0,5):[]
    }
  };
}

function applyActorDMShareDecision(actorId,share,userRequested){
  if(!share||typeof share!=='object')return false;
  const decision=String(share.decision||share.action||'none').toLowerCase();
  const id=normalizeSocialActorId(actorId);const state=dmShareIntentState[id]||{pending:0};
  if(decision==='defer'||decision==='refuse'){state.pending=Math.min(3,(Number(state.pending)||0)+1);dmShareIntentState[id]=state;persistDMShareIntentState();return false;}
  if(decision!=='share')return false;
  // v1.2.1: once AI has decided "share", do not run a second random veto here.
  // The unsolicited probability gate happens BEFORE the AI response so text and card cannot disagree.
  const rawType=String(share.contentType||share.type||'').toLowerCase();
  if(rawType!=='feed'&&rawType!=='reel')return false;
  const type=rawType;
  const source=String(share.source||'linked').toLowerCase();let card=null;
  if(source==='linked'){
    const contentId=String(share.contentId||'');const candidate=getDMShareCandidates(id,30).find(row=>String(row.contentId)===contentId&&row.contentType===type);if(!candidate)return false;
    const content=type==='feed'?resolveDMShareCardContent({kind:'post',contentType:'feed',postId:contentId,author:candidate.author,caption:candidate.caption,source:'linked'}):resolveDMShareCardContent({kind:'reel',contentType:'reel',postId:contentId,author:candidate.author,caption:candidate.caption,source:'linked'});if(!content)return false;recordActorSawSharedLinkedContent(id,content);
    card=makeDMContextCard(type==='reel'?'reel':'post',{postId:content.id,author:content.author,caption:content.caption,captionTranslation:content.captionTranslation||'',mediaUrl:content.mediaUrl||'',mediaDescription:content.mediaDescription||'',mediaClass:'m1',senderId:id,source:'linked',contentType:type,sourceLanguage:content.sourceLanguage||'',location:content.location||'',tags:content.tags||[],metrics:content.metrics||{}});
  }else if(source==='own_publish')card=createActorPublishedDMShare(id,type,share.content||share);
  else card=createActorDMOnlyShare(id,type,share.content||share);
  if(!card)return false;if(!dmSharedCards[currentDMChat])dmSharedCards[currentDMChat]=[];dmSharedCards[currentDMChat].push(card);state.pending=0;dmShareIntentState[id]=state;persistDMShareIntentState();scheduleINSDMHistoryPersistence();updateDMListPreview(currentDMChat,`分享了一条 ${type==='reel'?'Reel':'IG 动态'}`);renderDMChatStream(currentDMChat);return true;
}

document.getElementById('dmChatMessages')?.addEventListener('click',event=>{const toggle=event.target.closest?.('[data-dm-share-card-translate]');if(toggle){event.preventDefault();event.stopPropagation();toggleDMShareCardTranslation(toggle.dataset.dmShareCardTranslate);return;}const card=event.target.closest?.('[data-dm-share-card]');if(!card)return;openDMShareCardDetail(card.dataset.dmShareCard);});
document.getElementById('dmChatMessages')?.addEventListener('keydown',event=>{if(event.key!=='Enter'&&event.key!==' ')return;if(event.target.closest?.('[data-dm-share-card-translate]'))return;const card=event.target.closest?.('[data-dm-share-card]');if(!card)return;event.preventDefault();openDMShareCardDetail(card.dataset.dmShareCard);});
document.addEventListener('click',event=>{const captionBtn=event.target.closest?.('#dmShareDetailCaptionToggleV124');if(captionBtn){event.preventDefault();toggleDMShareDetailCaptionTranslation();return;}const commentBtn=event.target.closest?.('[data-dm-share-comment-translate]');if(commentBtn){event.preventDefault();toggleDMShareDetailCommentTranslation(commentBtn.dataset.dmShareCommentTranslate);return;}});

function showDMSharePendingBubbleV122(recipient){
  const box=document.getElementById('dmChatMessages');
  if(!box||!recipient)return null;
  removeDMSharePendingBubbleV122();
  const profile=getDMProfile(recipient);
  const row=document.createElement('div');
  row.id='dmSharePendingBubbleV122';
  row.className='dm-share-pending-v122';
  row.innerHTML=`<div class="dm-msg-avatar" data-identity-avatar="${escapeDMCardHTML(profile.identityId||profile.handle||recipient)}">${profile.avatarUrl?'':escapeDMCardHTML(profile.initial||'?')}</div><div class="dm-share-pending-bubble-v122" aria-label="正在准备分享"><span class="dm-share-pending-dot-v122"></span><span class="dm-share-pending-dot-v122"></span><span class="dm-share-pending-dot-v122"></span></div>`;
  box.appendChild(row);
  hydrateIdentityAvatars(row);
  requestAnimationFrame(()=>{const scroller=document.getElementById('dmChatScroll');if(scroller)scroller.scrollTop=scroller.scrollHeight;});
  return row;
}
function removeDMSharePendingBubbleV122(){document.getElementById('dmSharePendingBubbleV122')?.remove();}
function shouldAttemptDMShareCardV122(share,rows=[],userRequested=false){
  const decision=String(share?.decision||share?.action||'none').toLowerCase();
  return decision==='share'||(userRequested&&looksLikeDMAffirmativeShareReply(rows));
}
function waitDMSharePendingV122(ms=520){return new Promise(resolve=>setTimeout(resolve,ms));}

async function dmSummonAI(){
  if(!currentDMChat||dmAIReplyInFlight)return;
  const typing=document.getElementById('dmTyping');
  const plane=document.getElementById('dmAIPlaneBtn');
  const profile=getDMProfile(currentDMChat);
  const typingAvatar=typing?.querySelector('.dm-msg-avatar');
  dmAIReplyInFlight=true;
  if(plane){plane.disabled=true;plane.setAttribute('aria-busy','true');}
  if(typingAvatar){typingAvatar.textContent=profile.avatarUrl?'':profile.initial;typingAvatar.style.backgroundImage=profile.avatarUrl?`url("${profile.avatarUrl}")`:'';typingAvatar.style.backgroundSize=profile.avatarUrl?'cover':'';typingAvatar.style.backgroundPosition=profile.avatarUrl?'center':'';}
  typing?.classList.add('show');
  requestAnimationFrame(()=>{const scroller=document.getElementById('dmChatScroll');if(scroller)scroller.scrollTop=scroller.scrollHeight;});
  try{
    const actorId=normalizeSocialActorId(currentDMChat);
    const recent=(dmBaseMessages[currentDMChat]||[]).slice(-18).map(row=>({id:row._uiId||'',from:row.from,text:row.text,translation:row.translation||'',createdAt:Number(row.createdAt)||0}));
    const lastUserText=[...recent].reverse().find(row=>row.from==='me')?.text||'';
    const userRequestedShare=detectDMExplicitShareRequest(lastUserText);
    const shareState=dmShareIntentState[actorId]||{pending:0};
    const shareCandidates=getDMShareCandidates(actorId,12);
    const unsolicitedChance=Math.min(.72,.10+(Number(shareState.pending)||0)*.16);
    const allowShareNow=userRequestedShare||Math.random()<unsolicitedChance;
    const result=await callNiniINSAI(INS_AI_TASKS.DM_REPLY,{actorIds:actorId?[actorId]:[],ownerId:actorId,surface:'dm',content:{recentMessages:recent,sharedCards:(dmSharedCards[currentDMChat]||[]).slice(-4),socialContext:typeof getDMModelContext==='function'?getDMModelContext(actorId,8):[],dmShare:{userRequestedShare,pendingInterest:Number(shareState.pending)||0,allowShareNow,candidates:shareCandidates,policy:'You may occasionally share one Feed/Reel when it fits the conversation and your personality. Never share Story. IMPORTANT: if allowShareNow=false and User did not explicitly request a share, do NOT say or imply that you are sending/sharing a post now and return decision=none. If you decide to share now, you MUST return share.decision=share together with a valid card payload; never write a message like “here, this one / I sent it” without the share object. linked must use one candidate contentId. dm_only creates a card that exists only in this DM. own_publish means you personally publish it first, so it will also appear on your profile and the real Feed/Reels. If User explicitly asks, you still may comply, defer, tease, or refuse according to personality; use decision=defer when you do not share now but may be more likely later. IMPORTANT FOR EXPLICIT REQUESTS: if you decide to comply now, prefer a NEW dm_only Feed/Reel instead of linked stock; do not choose built-in/example/candidate content unless this is an unsolicited spontaneous share. own_publish is only for content you personally decide to publish publicly.'}}});
    if(!result.ok)throw new Error(result.reason||'AI 回复失败');
    const rows=Array.isArray(result.data?.messages)?result.data.messages:(result.data?.reply?[result.data.reply]:[]);
    let share=extractDMShareDecision(result.data,rows);
    if(userRequestedShare)share=repairDMShareDecisionForExplicitRequest(actorId,share,lastUserText,rows);
    if(userRequestedShare&&share&&String(share.decision||share.action||'none').toLowerCase()==='share'&&String(share.source||'').toLowerCase()!=='own_publish'){
      const freshType=(String(share.contentType||share.type||detectDMRequestedShareType(lastUserText)||'feed').toLowerCase()==='reel')?'reel':'feed';
      try{share=await generateFreshDMOnlyShareV123(actorId,freshType,lastUserText);}
      catch(error){console.warn('[INS] fresh DM share generation failed',error);share={decision:'none'};}
    }
    if(!userRequestedShare&&!allowShareNow&&share&&String(share.decision||share.action||'none').toLowerCase()==='share')share={decision:'none'};
    if(!rows.length&&!share)throw new Error('AI 没有返回 messages');
    const pendingBefore=Number((dmShareIntentState[actorId]||{}).pending)||0;
    const willAttemptShare=shouldAttemptDMShareCardV122(share,rows,userRequestedShare);
    if(willAttemptShare){
      showDMSharePendingBubbleV122(currentDMChat);
      await waitDMSharePendingV122();
    }
    const didShare=applyActorDMShareDecision(actorId,share,userRequestedShare);
    removeDMSharePendingBubbleV122();
    // Render text after the card decision. If the character claimed to send a card but card creation failed, drop that claim.
    rows.slice(0,4).forEach(row=>{const text=String(row.textOriginal??row.text??'').trim();if(!text)return;if(userRequestedShare&&looksLikeDMAffirmativeShareReply([row])&&!didShare)return;addDMMessage(currentDMChat,'them',text,String(row.textTranslation??row.translation??'').trim());});
    if(userRequestedShare&&!didShare&&(Number((dmShareIntentState[actorId]||{}).pending)||0)===pendingBefore){const state=dmShareIntentState[actorId]||{pending:0};state.pending=Math.min(3,(Number(state.pending)||0)+1);dmShareIntentState[actorId]=state;persistDMShareIntentState();}
  }catch(error){alert('INS 私信 AI 调用失败：'+(error?.message||error));}
  finally{removeDMSharePendingBubbleV122();typing?.classList.remove('show');if(plane){plane.disabled=false;plane.removeAttribute('aria-busy');}dmAIReplyInFlight=false;}
}

document.getElementById('dmMsgInput')?.addEventListener('keydown', e=>{
  if(e.key === 'Enter' && !e.shiftKey){
    e.preventDefault();
    dmSendInput();
  }
});
document.getElementById('dmAIPlaneBtn')?.addEventListener('click', dmSummonAI);
document.getElementById('dmChatMenuBtn')?.addEventListener('click', ()=>dmOpenCard('dmChatMenuCard'));
document.getElementById('dmClearOpenBtn')?.addEventListener('click', ()=>dmOpenCard('dmClearConfirmCard'));
document.getElementById('dmClearConfirmBtn')?.addEventListener('click', ()=>{
  if(currentDMChat){removeINSDMMemoryForConversation(currentDMChat);if(dmBaseMessages[currentDMChat])dmBaseMessages[currentDMChat]=[];if(dmSharedCards[currentDMChat])dmSharedCards[currentDMChat]=[];}
  scheduleINSDMHistoryPersistence();
  dmCloseCards();
  updateDMListPreview(currentDMChat,'暂无消息');
  renderDMChatStream(currentDMChat);
});
document.getElementById('dmChatDim')?.addEventListener('click', dmCloseCards);
document.querySelectorAll('[data-dm-chat-close]').forEach(btn=>btn.addEventListener('click', dmCloseCards));
document.getElementById('dmChatHomeAction')?.addEventListener('click', ()=>{
  if(currentDMChat) openCharProfile(currentDMChat);
});


/* ===== Navigation skeleton router ===== */
const routeStack = [];
let currentCharProfile = null;
let currentDMChat = null;

function getVisibleRoute(){
  return [...document.querySelectorAll('.route-page.show')].pop() || null;
}

function showRoute(id){
  const next = document.getElementById(id);
  if(!next) return;

  const current = getVisibleRoute();
  if(current && current.id === id) return;

  if(current){
    routeStack.push(current.id);
    current.classList.remove('show');
  }

  // Defensive cleanup: never allow two route overlays to cover each other.
  document.querySelectorAll('.route-page.show').forEach(el=>el.classList.remove('show'));
  next.classList.add('show');
}

function hideRoute(id){
  const el = document.getElementById(id);
  if(el) el.classList.remove('show');
}

function backRoute(){
  const current = getVisibleRoute();
  if(current) current.classList.remove('show');

  let prev = routeStack.pop();
  while(prev && !document.getElementById(prev)){
    prev = routeStack.pop();
  }

  if(prev){
    document.getElementById(prev)?.classList.add('show');
  }
}


/* ===== v5.0.8 overlay hygiene: stale editor/dim layers must never block taps ===== */
function clearProfileInteractionOverlays(){
  [
    'profileDrawerDim','profileDrawer','profileEditCard','highlightEditorCard',
    'profileAtmosphereCard','profileImagePromptCard','profileFeedTranslationApiCard','memorySystemCard',
    'charDrawerDim','charDrawer','charHighlightEditor','charProfileEditor'
  ].forEach(id=>document.getElementById(id)?.classList.remove('show'));
}

function clearTransientProfileRoutes(){
  clearProfileInteractionOverlays();
  document.getElementById('profileContentDetailRoute')?.classList.remove('show');
  document.getElementById('niniHighlightViewer')?.classList.remove('show');
}

const storySeenState = {};

function canCurrentUserViewStory(owner){
  const ownerId=normalizeSocialActorId(owner);
  if(!ownerId || ownerId==='user')return true;
  const actor=typeof getRelationshipActor==='function'?getRelationshipActor(ownerId):null;
  if(!actor || !['roche','npc'].includes(actor.kind))return false;
  const partners=typeof getDirectRelationshipPartners==='function'?getDirectRelationshipPartners(ownerId):[];
  return partners.map(normalizeSocialActorId).includes('user');
}
function canStoryRecipientReceiveFromOwner(owner,recipient){
  const ownerId=normalizeSocialActorId(owner);
  const recipientId=normalizeSocialActorId(recipient);
  if(!ownerId || !recipientId || ownerId===recipientId)return false;
  const recipientActor=typeof getRelationshipActor==='function'?getRelationshipActor(recipientId):null;
  if(ownerId==='user')return !!recipientActor && ['roche','npc'].includes(recipientActor.kind);
  if(!recipientActor || !['user','roche','npc'].includes(recipientActor.kind))return false;
  const partners=typeof getDirectRelationshipPartners==='function'?getDirectRelationshipPartners(ownerId):[];
  return partners.map(normalizeSocialActorId).includes(recipientId);
}

function syncHomeStoryStrip(options={}){
  if(!options.skipExpiry)expireLiveStoriesNow({render:false});
  const container=document.querySelector('.stories');
  if(!container)return;

  [...container.querySelectorAll('.story:not(.user)')].forEach(el=>el.remove());

  const actors=(typeof getRelationshipActors==='function'?getRelationshipActors():[])
    .filter(actor=>actor && (actor.kind==='roche'||actor.kind==='npc'));

  actors.forEach(actor=>{
    const items=storyViewerData[actor.id]||[];
    if(!items.length || !canCurrentUserViewStory(actor.id))return;

    const item=document.createElement('div');
    item.className='story'+(storySeenState[actor.id]?' seen':'');
    item.innerHTML=`
      <div class="story-wrap" data-story-owner="${escapeRocheHydrationHTML(actor.id)}">
        <div class="story-ring"><div class="story-inner">
          ${actor.avatarUrl
            ? `<img src="${escapeRocheHydrationHTML(actor.avatarUrl)}" alt="">`
            : `<div style="width:100%;height:100%;display:grid;place-items:center;font-weight:800">${escapeRocheHydrationHTML(actor.initial||'?')}</div>`}
        </div></div>
      </div>
      <div class="story-name">${escapeRocheHydrationHTML(actor.name||actor.handle||actor.id)}</div>`;
    item.querySelector('[data-story-owner]')?.addEventListener('click',()=>openStory(actor.id));
    container.appendChild(item);
  });

  sortStoryStrip();
}

function sortStoryStrip(){
  const container = document.querySelector('.stories');
  if(!container) return;

  const userStory = container.querySelector('.story.user');
  const charStories = [...container.querySelectorAll('.story:not(.user)')];

  // unread colored rings first; viewed gray rings after them.
  charStories.sort((a,b)=>{
    return Number(a.classList.contains('seen')) - Number(b.classList.contains('seen'));
  });

  const frag = document.createDocumentFragment();
  if(userStory) frag.appendChild(userStory);
  charStories.forEach(el=>frag.appendChild(el));
  container.appendChild(frag);
}

function markStorySeen(owner){
  if(owner === 'user') return;
  storySeenState[owner]=true;

  const wrap = document.querySelector(`[data-story-owner="${owner}"]`);
  const story = wrap?.closest('.story');
  if(story){
    story.classList.add('seen');
    sortStoryStrip();
  }

  if(typeof recordSocialEvent==='function'){
    const ownerId=normalizeSocialActorId(owner);
    if(isFormalSocialActor(ownerId)){
      const contentId=currentStoryMemoryContentId(owner);
      recordSocialEvent({
        type:'story_view',
        actorId:'user',
        targetIds:[ownerId],
        contentId,
        contentType:'story',
        contentOwnerId:ownerId,
        visibility:'private',
        dedupeKey:`story_view:user:${ownerId}:${contentId}`,
        summary:`${socialActorName('user')}看了 ${socialActorName(ownerId)} 的 Story。`
      },{knownActorIds:['user',ownerId],learnedVia:'story_viewer'});
    }
  }
}

/* ===== v1.15.18 reaction semantics =====
   Stable semantic code first; emoji is presentation only.
   Later emoji can be replaced without rewriting Social Events / memory.
*/
const INS_REACTION_SCHEMA = {
  like:{code:'like',emoji:'❤️',label:'看到了 / 喜欢',promptMeaning:'看到了这条 Story，顺手表达喜欢或轻度认可'},
  amused:{code:'amused',emoji:'🤣',label:'笑疯了',promptMeaning:'真的被逗笑，觉得非常好笑或很有梗'},
  excited:{code:'excited',emoji:'🔥',label:'太赞了',promptMeaning:'觉得很酷、很赞、有冲击力或明显兴奋'},
  affectionate:{code:'affectionate',emoji:'🥰',label:'好喜欢 / 好可爱',promptMeaning:'觉得可爱、喜欢、温柔或很有好感'},
  shocked:{code:'shocked',emoji:'😱',label:'吓到 / 震惊',promptMeaning:'强烈惊讶、被吓到或没想到会看到这样的内容'},
  supportive:{code:'supportive',emoji:'👏',label:'赞赏 / 支持',promptMeaning:'赞赏、支持、为对方开心或给予肯定'},
  mindblown:{code:'mindblown',emoji:'🤯',label:'太炸了',promptMeaning:'觉得非常震撼、不可思议或超出预期'},
  emotional:{code:'emotional',emoji:'😭',label:'被戳中了',promptMeaning:'情绪反应很强，可能是感动、共鸣或夸张式被戳中'},
  flustered:{code:'flustered',emoji:'🥵',label:'太绝了 / 顶不住',promptMeaning:'因为内容很有吸引力、很强烈或太绝而产生夸张式反应'}
};
const INS_REACTION_BY_EMOJI = Object.fromEntries(Object.values(INS_REACTION_SCHEMA).map(item=>[item.emoji,item]));
const STORY_REACTION_MEANINGS = Object.fromEntries(Object.values(INS_REACTION_SCHEMA).map(item=>[item.emoji,item.label+' · '+item.promptMeaning]));
function getINSReactionDescriptor(input){
  const raw=String(input||'').trim();
  if(INS_REACTION_SCHEMA[raw])return INS_REACTION_SCHEMA[raw];
  if(INS_REACTION_BY_EMOJI[raw])return INS_REACTION_BY_EMOJI[raw];
  return {code:'custom',emoji:raw||'🙂',label:'自定义表态',promptMeaning:'对这条内容做出了一个自定义情绪表态'};
}
function normalizeINSReactionDecision(data={}){
  const descriptor=getINSReactionDescriptor(data.reactionCode||data.code||data.emoji);
  return {
    react:data.react!==false,
    reactionCode:descriptor.code,
    emoji:data.emoji||descriptor.emoji,
    attitudeSummary:String(data.attitudeSummary||data.attitude||descriptor.promptMeaning).trim(),
    displayLabel:descriptor.label
  };
}

const storyViewerData = {user:[]};

Object.values(storyViewerData).forEach(items=>{
  items.forEach(item=>{
    if(typeof item.liked==='undefined') item.liked=false;
    if(typeof item.linkedAlbumId==='undefined') item.linkedAlbumId='';
    if(typeof item.generated==='undefined') item.generated=false;
  });
});

/* Story generation contract:
   one Story batch contains 1..4 items, not always 4.
   Each item independently chooses image or video. */
function normalizeGeneratedStoryItems(items){
  const safe=(Array.isArray(items)?items:[]).slice(0,4);
  return safe.length ? safe : [{id:'story_auto_'+Date.now(),type:'image',time:'刚刚',description:'Story 内容占位',liked:false}];
}

const INS_CONTENT_GENERATION_CONTRACT = {
  feed:{
    media:'image_or_video_description',
    caption:'required',
    translation:'on_demand_separate_user_api_not_in_generation',
    metrics:'likes_comments_reposts_shares_in_same_result',
    initialComments:'same_call_optional_batch_from_relationship_eligible_actors_and_strangers'
  },
  story:{
    items:'1_to_4_image_or_video',
    caption:'one_caption_for_whole_batch',
    translation:'same_call_separate_field_when_foreign',
    comments:'none',
    reactions:'semantic_reaction_code_then_ui_emoji'
  },
  reels:{
    media:'video_description',
    caption:'required',
    translation:'same_call_separate_field_when_foreign',
    metrics:'likes_comments_reposts_in_same_result',
    initialComments:'same_call_optional_batch_from_relationship_eligible_actors_and_strangers'
  }
};

/* Char / NPC generation should follow the same content contract.
   Roche chars may resolve image media through Album Matcher;
   NPC / stranger media stays as text description unless user chooses generation.

   IMPORTANT: this standalone prototype does NOT call an AI API.
   The functions below are the single clean adapter boundary for the future Roche wrapper.
   One batch may include multiple chars/NPCs; do not call once per actor.
*/
const INS_AI_TASKS = Object.freeze({
  HOME_STRANGERS:'home_strangers',
  FEED_GENERATE:'feed_generate',
  STORY_GENERATE:'story_generate',
  REELS_GENERATE:'reels_generate',
  PUBLIC_COMMENT_BATCH:'public_comment_batch',
  DIRECT_COMMENT_REPLY:'direct_comment_reply',
  DM_REPLY:'dm_reply',
  STRANGER_DM_REFRESH:'stranger_dm_refresh',
  GUESTBOOK_REFRESH:'guestbook_refresh',
  REACTION_SETTLEMENT:'reaction_settlement',
  STORY_AUDIENCE_PLAN:'story_audience_plan'
});

const INS_DEFAULT_WORLD_CULTURE_PROMPT = `【默认世界观 / 网络文化：现代现实互联网】
当前世界默认遵循现实世界的现代社会结构与互联网文化。人物来自不同国家、地区、语言与文化背景；姓名、职业、所在地、教育、生活方式、网络身份和社交习惯应与人物背景自洽。现代智能手机、社交媒体、学校、公司、旅行、娱乐与城市生活正常存在。

【陌生账号 ID / 昵称地域习惯】
- 中国大陆：IG ID 可自然使用拼音、英文名、姓名缩写、数字、重复字母或少量符号；显示昵称可用中文昵称、英文名、短网名、叠词或自然网络昵称。ID 与显示昵称可以完全不同。
- 港澳台 / 繁体中文地区：显示昵称优先繁体中文昵称、英文名或当地自然网名；ID 可用英文名、粤拼/拼音/罗马字、缩写、数字与简短符号组合，保持当地年轻用户的自然感。
- 美国及其他英语地区：ID 更常接近真实姓名、nickname、姓氏缩写或简单单词组合；重名时用轻微重复字母、点、下划线或少量数字，不要人人堆生日数字。显示昵称多用名字或自然 nickname。
- 日本：ID 偏小写罗马字、名字缩写、下划线、数字、轻微拆字；显示昵称可用日文名、ひらがな/片假名短昵称、昵称缩写、罗马字或轻量颜文字，不要求完整真实姓名。
- 韩国（当前内置偏好重点）：显示昵称不要求真实姓名，优先生成有韩国 IG 审美的短网名 / 昵称，可使用韩文短词、单字昵称、罗马字审美昵称，或参考韩系 ID 的拆字、重复字母、x/z、0、点、下划线等视觉形式；ID 同样偏韩系 INS 风，但不要把所有账号套成同一模板，也不要机械地人人使用 0 / x / z。整体可比其他地区更有设计感。
- 泰国：现实 nickname 使用非常普遍。显示昵称可直接使用英文 nickname / 泰文昵称；ID 常围绕 nickname 加重复字母、简短数字或自然变体。
- 欧洲：先尊重具体国家和语言，不把“欧洲”当成单一文化。整体 ID 更偏真实姓名、名字缩写、本国语言姓名或简洁组合，少做过度字符艺术。
- 澳洲：偏英语 nickname、名字缩写、姓氏首字母和轻量重复字母，整体自然随意，不要过度精装修。

【首页陌生推荐默认偏好】
- 默认 Home 陌生推荐提高日本、韩国、英语地区，以及繁体中文地区（香港 / 澳门 / 台湾）账号的出现权重。
- 这是概率偏好，不是固定国家配额；允许连续刷到同一地区，也允许自然混入其他国家和地区。
- 默认审美可以更偏日韩系，但人物仍必须像各自在使用真实社交平台，而不是同一个模板复制出来。

如果用户修改这段提示词，应以用户填写的新世界观 / 网络文化为准；可以把现实国家、现代互联网和以上地区规则整体替换成赛博、古风、魔法、架空势力或其他设定。`;

const INS_STRANGER_IDENTITY_LANGUAGE_CONTRACT = `【陌生账号身份 / ID / 语言通用规则】
这套规则必须用于所有 AI 生成的陌生账号与陌生作者，包括：首页陌生 Feed、陌生 Reels、公开评论区里的陌生评论者、主页留言里的陌生访客、陌生 DM 请求。
- 每个陌生账号都要形成自洽的一套 handle（IG ID）+ displayName（昵称/账号名）+ region + sourceLanguage / language；账号既可以是个人，也可以是机构、品牌、店铺、娱乐公司或其他公开账号。不要只给评论者随便起一个 public_123 占位名。
- handle 与 displayName 必须遵循当前“世界观 / 网络文化层”中该地区的账号命名习惯；显示昵称不要求等于真实姓名，尤其韩国默认允许韩系 IG 风网名 / 审美昵称。
- 陌生人说什么语言由“这个陌生人是谁”决定，不由他正在评论的帖子语言决定。公开评论尤其不能整批复制正文语种。
- 同一个临时陌生身份一旦在当前内容 / 会话中建立，后续展示应保持同一 handle、displayName、语言和地区，不要每条消息重新换身份。
- 非中文原文仍单独提供中文 translation；不要把中文翻译塞进原文字段。`;

const INS_MEDIA_LANGUAGE_CONTRACT = `【IG / Reels 字段语言硬规则】
- 图片 mediaDescription 与视频 videoDescription：一律使用中文描述画面/镜头内容。即使发帖人是韩国、日本、美国、泰国或其他地区用户，视觉描述字段也不要跟随发帖语言。
- 生成陌生人时，handle（IG ID）与 displayName（昵称）必须与该账号所属国家/地区及当地互联网命名习惯一致。Reels 的 captionOriginal 继续使用账号自然语言；仅首页 Feed 改为返回简体中文 captionZh，同时用 sourceLanguage 标记该账号原本会使用的语言，原语言正文稍后由独立 Feed 翻译 API 按需生成。
- location：只用英文，统一使用 “City·Country / Place·Country” 风格，中间使用 ·。示例：Seoul·Korea、Osaka·Japan、Taiwan·China、Hong Kong·China、Macau·China。不要输出中文地点。
- tags / #hashtags：只用英文或英文数字组合；不要生成韩文、日文、中文、泰文等非英文 tag。
- 评论者语言与正文语言必须独立决定。评论区是全球平台受众，不允许因为正文是韩语/日语/英语/中文就把整批评论都生成成同一种语言。
- 标准 5 条 initialComments / public comments：至少包含 2 种 sourceLanguage，并且至少 1 条评论的 sourceLanguage 必须与正文 sourceLanguage 不同。允许 2–4 种语言自然混合，不要机械平均分配。
- 每条评论仍使用“评论者本人”的自然语言；非中文评论把中文翻译单独放 textTranslation，中文评论 translation 留空。`;

const INS_STRANGER_ACCOUNT_ECOLOGY_PROMPT = `【INS 陌生账号生态】
陌生账号池不只包含普通个人账号。刷新陌生 Feed / Reels、公开互动、留言或陌生消息时，可以自然混入不同类型账号，但不要机械配额，也不要每批都强行出现营销号：
- 普通个人生活号仍是主体：日常、自拍、朋友、食物、通勤、风景、兴趣、随手记录。
- 兴趣/垂类账号：美食、旅行、穿搭、美妆、摄影、宠物、测评、城市生活等。
- 商业/机构账号可偶尔出现：化妆品牌新品、服装品牌、餐厅/咖啡店、店铺、旅行推荐/文旅、活动主办、娱乐公司、艺人/团体宣传、品牌合作、商单、抽奖、推广、明显社交媒体广告或搬运内容。
- 机构/品牌账号也要有自洽的 handle、displayName、region、language 和内容风格；不要把所有陌生账号都假装成一个具体性别的个人。
- 营销/广告内容是平台生态的一部分，但应偶尔出现，不要让整批陌生内容都变成广告。`;

const INS_NATURAL_INTERACTION_ECOLOGY_PROMPT = `【INS 评论 / 私信 / 留言自然互动规则】
生成公开评论、DM、陌生消息请求、主页留言或内容自带 initialComments 前，先在内部决定“这个人为什么开口、愿不愿意继续聊”，再写文字。不要默认每次互动都要夸发布者、索要信息、问地点/链接、或主动推进关系。

【互动动机要活】
可以混合：表达自己的看法、说一点自己的近况/经历、联想到别的事、注意内容里的小细节、简单附和、轻吐槽、玩笑、不同意见、纠正信息、质疑滤镜/真实性、评价产品体验、广告厌烦、跑题联想、只回一个点、低信息量反应、emoji、结束话题，以及偶尔的信息询问。
可以问“在哪里买 / 商品链接 / 店名 / 地点 / 怎么去 / 怎么做”，但提问只是其中一种动机，不是默认结尾。不要频繁套用“我看了你主页 / 很喜欢你的风格 / 加入旅行清单 / 求地址 / 求链接”。

【5 条公开互动的节奏】
标准 5 条评论/留言中，通常至少 2 条完全不提问；通常至少 1 条带评论者自己的经历或联想；通常至少 1 条关注内容细节而不是发布者本人。信息型问句通常最多 1–2 条。不要机械计数，但整批必须明显避免同一目的、同一句式和同一社交路径。

【允许少量负面与杠精】
可以低比例出现轻微负面、质疑、不同意见或杠精式评论，例如觉得滤镜太重、怀疑网图/修图、认为现场并非如此、不赞同观点、觉得产品一般、吐槽广告感。标准 5 条里通常 0–1 条即可；不要每批必出。负面主要针对内容、观点、真实性、广告感或产品体验，不做人身攻击，也不针对外貌或身体特征。
如果内容明显是营销/商单/广告，评论区可以自然出现“又是广告”“怎么又推给我”“一眼商单”“再刷到这种东西平台赔我钱/车/房”这类夸张玩笑，也可以出现“广告归广告但第二张还行”这种混合态度；不要每条广告都重复同一个梗。

【合作、邀约与真实社交来源】
陌生评论/DM 可以偶尔来自真实合作或邀约：品牌商单、商务询问、娱乐公司面试/试镜式联系、摄影约拍、活动邀请、店铺互推、旅行者询问当地建议、复刻美食失败来请教、普通交友/交换联系方式等。评论区也可以出现“接合作吗 / 已私信 / 有商务邮箱吗”这类公开留言。
合作或邀约不保证真实：可以随机为真实、模糊、群发营销或可疑联系人；一旦某个会话基本意图确定，后续根据 User 回复保持角色一致，不要无理由瞬间换身份。

【低概率骚扰 / 垃圾信息】
边界感差的搭讪、奇怪邀约、群发营销、明显可疑或违规垃圾广告可以极低概率出现，而且允许连续很多批都不出现。它们只是平台噪音，不应成为每批固定配额。
可疑/垃圾类型只表现社交信号（身份模糊、过度承诺、群发感、催促、越界等）；不要生成具体诈骗步骤、付款路径、验证码套取、外部联系链接、违法交易细节，也不要展开成人或赌博内容细节。

【性别不要模板化】
不要根据 User、角色或陌生账号的性别预设互动对象。任何性别都可能向任何性别发起合作、交友、约拍、旅行咨询、普通搭讪或其他互动；不要固定“女性只收到男性留言 / 男性只收到女性留言”。

【DM 特别规则】
陌生人发私信不代表一定想认识 User，也不一定解释“为什么看到了主页”。可以只是临时想到一句、对某个细节反应、分享相似经历、纠正信息、询问具体问题、商务联系或说完就结束。不要把陌生 DM 都写成粉丝式搭讪，也不要为了续聊强行在每条末尾加问题。
有关系的人也不要反复展示关系标签；熟人可以更省略、更跳跃，陌生人保持正常距离。

【角色主页留言板特别规则】
留言板可以有粉丝、普通访客、熟人、路人、黑粉/质疑者、商务账号或偶尔的奇怪留言。可以根据主页整体风格出现自然的“持续来看更新”“很期待下一条”式粉丝留言，也可以有“这人看着不太靠谱”“修得有点过”等低比例负面；还可以对已有留言做一句公开回应，例如质疑前一条留言的说法。不要把留言板写成连续 DM，也不要让整面墙都变成粉丝应援。

最终目标：像不同的人各自带着自己的生活、脾气和目的进入同一个平台，而不是同一个 AI 用“夸一下 + 问地址/链接 + 想认识你”的模板批量说话。`;


const INS_GLOBAL_SOCIAL_REALISM_PROMPT = `【INS · 全球社交平台活人规则】

这是一个持续运行的全球社交平台，不是围绕 User 搭建的角色互动舞台。平台上的每个账号都有自己独立的生活、朋友、兴趣、情绪、工作/学习、所在地和社交圈。人物不需要每次出现都与 User 有关，也不需要每条内容都有剧情功能。

【1. 全球化与语言】
- 人物使用符合自身背景、所在地、年龄、关系与习惯的语言，不默认所有人说中文。
- 平台可自然出现中文、韩语、日语、英语、泰语及其他语言；一次刷新不需要凑齐多个国家或刻意均衡。
- 不要为了展示“国际化”随机切换语言。跨文化表达必须有合理背景。
- 外语原文保持自然母语表达；如需要中文翻译，放入独立 translation/textTranslation 字段，不把翻译混进原文。中文原文不要生成假翻译。

【2. 自然语言优先】
- 普通但自然 > 新潮但用错 > 过气还硬用。
- 不要为了证明人物年轻、本地、有网感而堆砌热梗、缩写、emoji、颜文字、网络黑话。
- 网络表达只在人物习惯、关系、情绪与当前语境自然适合时偶尔出现；不确定是否仍流行时，直接使用普通自然口语。
- 人物设定是概率倾向，不是 checklist。活泼的人可以只回“嗯”，毒舌的人可以正常说话，年轻人可以完全不用梗。

【3. 网络表达浓度】
- 网络用语是调味品，不是主食。
- 一次批量生成多个账号时，从整个批次控制浓度；不要让每个账号各自“偶尔用一个梗”，最后变成全员玩梗。
- 一批 5 条公开评论通常允许 3–5 条完全没有明显热梗，0–2 条可自然出现网络表达；不是硬性计数，但整体应稀疏。
- 同一批尽量避免多人重复同一梗、同一 emoji、同一句式、同一口头禅。允许整批没有明显网络用语。

【4. 关系与社交距离】
- 同一个人物面对亲近朋友、普通朋友、熟人、网友、陌生人、公开评论区、较正式对象时，措辞、长度、礼貌度和情绪暴露应不同。
- 关系越近越可能省略、碎句、内部梗、缩写；关系越远越保持正常社交距离。
- 陌生人不得一上来像多年好友，也不要人人都夸 User、主动示好或过度熟悉。

【5. 真实社交允许低信息量】
- 不是每条内容都需要伏笔、剧情、情绪价值、深层含义或对 User 的暗示。
- 允许随手食物、天气抱怨、街景、通勤、简单 reaction、一个问号、已读不继续、没人回复的动态等普通内容。
- 不要为了“完整”而解释所有背景；真实社交允许省略主语、碎句、只接上文一小部分。

【6. 中文 ZH】
- 以自然现代中文口语为主，允许省略主语、拆句、短回复、单独“？”、少量重复和自然中英夹杂。
- 可自然出现“真的假的”“不是”“你等一下”“可以可以”“笑死”“好好好”等普通口语，但不要把任何示例当必用词。
- 默认避免把“绝了”“家人们谁懂啊”等已经容易产生旧网感/AI 味的万能表达当作年轻人标记。除非人物本身或当前上下文明显在引用、反串或复古玩梗。

【7. 韩语 KO】
- 以自然完整的日常韩语为主，并根据关系与场景自然区分敬语/非敬语。
- 熟人语境可偶尔出现 ㅋㅋ、ㅎㅎ、ㅠㅠ 以及 ㅇㅇ、ㄱㄱ、ㄱㅅ、ㄹㅇ、ㅇㅋ 等常见缩写；它们不是“韩国年轻人标志”，不要每句话都加。
- 正式、陌生、长辈/上级等场景降低或关闭网络缩写与玩梗。

【8. 日语 JA】
- 以自然日常日语为主。轻松网络交流可偶尔出现 笑、w、草、まじ、やば 等，但不要求出现，也不要让所有日本年轻角色说成同一种网语模板。

【9. 英语 EN】
- 以自然日常英语为主。熟人线上交流可偶尔出现 lol、lmao、omg、idk、tbh、ngl、fr、btw 等；缩写可以独立出现或夹在普通句子中，但不能像缩写合集。

【10. 泰语及其他语言】
- 优先使用自然的母语日常表达。对不确定的当地俚语宁可不用，不要编造或强塞“当地网感”。

【11. 批次多样性】
- 全球化不等于每批各国配额。一次可能连续出现同地区账号，也可能混合多个地区。
- 同一批人物应有不同活跃度、表达长度、情绪强度和互动意愿；不要所有人都热情、都长篇、都回应同一件事。

最终目标：让人感觉“这是不同地方、不同生活状态的人刚好在使用同一个平台”，而不是“一个 AI 在同时扮演不同国家的年轻人”。`;


/* ===== v5.0.4 Feed-only compact prompt =====
   Keep Roche persona + mounted chat memory in their original language.
   Only the internal platform/routing instructions are compressed into English.
*/
const INS_FEED_COMPACT_GLOBAL_PROMPT_EN = `Simulate a continuous global social network.
Each account has an independent life, routine, interests and relationships; do not center every post on the user.
Use the actor's natural language and register. Do not force multilingual quotas, slang, memes, emoji or youth-coded wording.
Respect relationship distance. Persona is a tendency, not a checklist.
Ordinary, low-information posts are valid: food, commute, weather, street scenes, hobbies, brief thoughts.
Do not invent relationships or events that are not supported by the supplied persona, memory or social context.
Preserve the actor's original language. If the original is non-Chinese, put the Chinese translation only in the separate translation field.
Visual media descriptions are always written in Chinese. Locations and tags are always English.`;

const INS_FEED_COMPACT_SURFACE_PROMPT_EN = `Instagram Feed task.
Generate one natural everyday post for each selected actor, not a plot beat or a performance for the user.
For this Home Feed task, override the general bilingual-caption rule: return the visible caption only in Simplified Chinese as captionZh. Also return sourceLanguage for the language this account would naturally use (for example ko, ja, en or zh). Do NOT return captionOriginal or captionTranslation for Feed; original-language text is created later by a separate on-demand API. Prefer a natural 2-4 sentence Feed caption with a naturally varied length; do not write to a preview or collapse threshold. Feed image count is 1-4. When request.extra.requestedMediaCounts is present, follow that plan exactly by output item index. Return mediaItems with exactly that many objects for each post; every media item must have its own distinct mediaDescription and the descriptions must describe different shots/details while still belonging to the same post. Every mediaDescription MUST be Chinese. Single-image posts are normal and should remain possible.
If a location is returned, it MUST be English in City·Country / Place·Country format with the middle dot ·. All tags MUST be English.
Return plausible positive aggregate engagement metrics: likes, comments, reposts and shares.
When includeInitialComments=true, return exactly the requested number of actual initialComments. These are visible comments and are separate from the aggregate comments metric.
Each public stranger comment must have a string handle-like author id (for example citycat_82), never an object. Keep displayName separate.
Never generate the current user/persona as a commenter.
If an initial comment genuinely refers to a supplied formal actor other than merely being on that actor's own post/reply target, return mentionedActorIds with exact actorIds and mentionStrength ('explicit' or 'contextual'). Do not invent actor references to fill this field.
If a comment is non-Chinese, provide Chinese textTranslation separately; if Chinese, leave textTranslation empty.
For a standard 5-comment batch, use at least 2 different sourceLanguage values and ensure at least one comment language differs from the post caption language. Do not mirror the post author's language across the whole comment section.
Use only supplied relationships and memory. Apply the supplied natural-interaction ecology to initialComments: questions are allowed but not the default; mix opinions, self-related remarks, detail reactions, occasional mild disagreement/advertising fatigue, and low-information reactions. Return only the requested JSON structure.`;


const INS_REELS_COMPACT_GLOBAL_PROMPT_EN = INS_FEED_COMPACT_GLOBAL_PROMPT_EN;
const INS_REELS_COMPACT_SURFACE_PROMPT_EN = `Instagram Reels task.
Generate a plausible short-video post for each selected actor. Reels are visual, brief and everyday; they do not need a plot or a performance for the user.
videoDescription must describe the actual short-video sequence or visual moment and MUST be written in Chinese. Caption can be short and must stay natural to the actor. If location is returned, use English City·Country / Place·Country format; all tags must be English.
Use only supplied persona, mounted Roche memory, direct relationships and recent INS knowledge. Do not invent relationships.
Return plausible non-negative engagement metrics: likes, comments, reposts and shares.
When includeInitialComments=true, return exactly the requested number of actual initialComments; these are separate from the aggregate comments metric. Never include the current user/persona in initialComments. If an initial comment genuinely refers to a supplied formal actor beyond the post owner/reply target, return mentionedActorIds with exact actorIds and mentionStrength ('explicit' or 'contextual'); do not invent references. For a standard 5-comment batch, use at least 2 source languages and at least one language different from the Reel caption language.
For stranger generation, create a self-consistent public account identity and keep it independent from the User. The stranger may be an ordinary person or, occasionally, a brand/shop/entertainment/travel/fashion/beauty or other public account when stranger generation is requested; do not force a commercial account every batch. Stranger handle/displayName/caption must fit that account's own region and language conventions. Apply the natural-interaction ecology to initialComments.`;


const INS_PUBLIC_COMMENT_COMPACT_PROMPT_EN = `Public comment batch.
Generate exactly the requested number of new visible comments for the supplied Feed or Reel.
Never generate the current user/persona as a commenter. The user may read or manually comment, but AI-generated public comments must come only from other formal actors or strangers.
Comments should be short and socially plausible. Mix eligible known actors and strangers only when natural; do not invent existing relationships.
Every stranger must use a stable-looking string handle in author (never an object) plus a separate displayName.
For existing formal actors, use their exact actorId.
Keep each comment in the commenter's natural language. If non-Chinese, put Chinese only in textTranslation; if Chinese, textTranslation must be empty.
Treat commenter language as independent from the post author's language. For a standard 5-comment batch, use at least 2 source languages and at least one comment language different from the post caption language. For smaller top-up batches, prefer a language that is missing or underrepresented in existingComments.
If a comment genuinely refers to a supplied formal actor who is not simply the post owner/reply target, also return mentionedActorIds with exact actorIds and mentionStrength ('explicit' or 'contextual'). Do not invent actor references just to fill these fields.
Avoid repeating existing comments. Questions are allowed, including product links/locations/how-to, but must not dominate. Mix opinions, the commenter's own experiences, detail reactions, low-information replies, occasional mild disagreement/advertising fatigue, and rare collaboration or suspicious-spam style accounts when natural. Suspicious/spam output must stay high-level and include no links, payment, verification-code, illegal transaction, adult-content or gambling details. Return JSON only.`;


const INS_GUESTBOOK_COMPACT_PROMPT_EN = `Profile guestbook batch.
Generate exactly the requested number of short public guestbook notes about the target profile.
Never generate the current user/persona as a guestbook author.
Use existing formal actors only when their supplied relationship makes a note plausible; otherwise use strangers with stable-looking string handles.
Do not turn the guestbook into a DM conversation. Keep distance, tone, attitude and familiarity varied. Guestbook notes may include fans, casual visitors, known actors, mild critics, replies to an existing note, collaboration inquiries, or rare odd/spam-like visitors; do not make every batch a fan wall or a negative wall.
For non-Chinese text, provide separate Chinese textTranslation. Return JSON only.`;

const INS_SURFACE_PROMPTS = Object.freeze({
  home_strangers:'首页陌生推荐：来源全球化但不设硬配额；默认提高日本、韩国、英语地区以及繁体中文地区（香港/澳门/台湾）账号的出现权重。账号有独立生活，不围绕 User。陌生池既可有普通个人，也可偶尔有品牌、店铺、娱乐公司、旅行/美食/穿搭/美妆等垂类账号和明显广告/商单内容；不要每批强塞营销号。内容可以平淡、低信息量；账号身份与内容应自洽。正文通常 2–4 句，长短自然。首页 Feed 只返回简体中文 captionZh，并用 sourceLanguage 标记账号原本会使用的语言；不要生成 captionOriginal 或 captionTranslation。每条陌生人 IG 动态必须同时返回 5 条可见 initialComments。图片描述只用中文；昵称和 ID 遵循账号所属地区与当地网感；地点用英文 City·Country / Place·Country；tag 只用英文；5 条评论至少 2 种语言且至少 1 条不同于正文来源语言。',
  feed_generate:'Feed：以自然生活流为主，不要求每条有剧情、金句或主题。正文通常写成自然的 2–4 句，长短自然。首页 Feed 只返回简体中文 captionZh，并用 sourceLanguage 标记账号原本会使用的语言；不要生成 captionOriginal 或 captionTranslation。图片描述固定中文；地点和 tag 固定英文。若一次生成多个账号，整体控制网感浓度；5 条评论至少 2 种语言且不能全跟正文来源语言同语种。',
  story_generate:'Story：轻量、即时、随手。一次 1–4 个 item，共用一个 caption；没有公开评论线程，进一步交流转 DM。',
  reels_generate:'Reels：更偏视觉和短视频片段，可以有城市、旅行、兴趣、日常、搞笑等内容；视频描述固定中文，文案跟随账号自然语言，地点和 tag 固定英文；评论语言独立于正文，不得整批跟随正文语种。',
  public_comment_batch:'公开评论：一批固定生成 5 条实际评论。评论应短、杂、关系、态度和社交动机不同；标准 5 条至少 2 种语言，并至少 1 条与正文主语言不同。评论者语言独立抽样，不得整批复制发帖人的语言。允许自己的经历、评价细节、不同意见、轻吐槽、低比例负面、广告厌烦、合作留言、信息询问、emoji 或不玩梗。提问可以有但不能成为默认；不要五个人都夸人、都问地点/链接、都长篇、都热情。',
  direct_comment_reply:'直接评论回复：只针对当前 thread 与目标人物自然回复，尊重关系和已有上下文；不要把一次回复写成总结全文。',
  dm_reply:'DM：承接已有上下文与关系，可拆成多条短消息、改口、停顿、省略；可以只回应一个点、说自己的事或自然结束，不要为了续聊每条都强行提问。不要像客服，也不要突然复述整段人设/记忆。可以低概率、只在话题自然相关时分享一条真实 Feed/Reel；绝不分享 Story。User 主动要求分享也不是强制命令，要按人物性格决定立即分享、暂缓/逗一下或拒绝。linked 只能从提供的候选 contentId 选；dm_only 只存在 DM；own_publish 表示角色本人先真实发布再分享。',
  stranger_dm_refresh:'陌生消息：必须使用统一陌生账号身份 / ID / 语言规则。来源可包括普通个人、旅行者、摄影师、品牌/商务、娱乐公司/活动方、店铺或其他公开账号；也允许极低概率群发营销、边界感差或可疑垃圾信息。handle、displayName、region、language 必须自洽。可以有真实合作、约拍、面试/活动、旅行咨询、美食复刻求助、普通交友或信息询问；不要人人都夸 User、都从“看了主页很喜欢你的风格”开场、或都想立刻认识。可疑垃圾内容不要提供具体链接、付款、验证码或违规操作细节。',
  guestbook_refresh:'主页留言：陌生访客同样必须使用统一陌生账号身份 / ID / 语言规则。可以有粉丝、普通访客、熟人、质疑者/黑粉、商务合作或偶尔的奇怪留言，也可以对已有留言做一句公开回应；不要整批都应援或整批都负面。像主页访客留下的短留言，不要写成连续私聊；一批角色语气、长度、语言、态度和熟悉度应有差异。',
  reaction_settlement:'Reaction：根据人物关系、内容与情绪选择最自然的 reaction；允许不 reaction，不要为了填字段强行表态。',
  story_audience_plan:'Story 24h 浏览与表态规划：仅对当前 User Story 的已绑定角色 / 社交联系人做一次整批规划，不加入陌生人。对每个候选人物独立决定会不会在 24 小时内看到；看到也可以完全不表态。若表态，只能使用允许的语义 reactionCode。浏览和表态时间应分散在整个 24 小时，不要所有人刚发布就一起出现。关系、人物性格、活跃程度、近期记忆与 Story 内容只影响概率，不强迫任何人查看或 reaction。必须返回候选列表里的真实 actorId，不得虚构人物，也绝不能替 User 决定 User 对角色 Story 的反应。'
});

function getINSSurfacePrompt(task,surface=''){
  const key=String(task||surface||'').toLowerCase();
  return INS_SURFACE_PROMPTS[key] || INS_SURFACE_PROMPTS[String(surface||'').toLowerCase()] || '';
}

function buildINSSocialPromptContext(task,surface='',extra={}){
  const key=String(task||surface||'').toLowerCase();
  const worldLayer=(typeof getINSWorldCulturePrompt==='function'?getINSWorldCulturePrompt():INS_DEFAULT_WORLD_CULTURE_PROMPT);
  const strangerIdentityTasks=new Set([
    INS_AI_TASKS.HOME_STRANGERS,
    INS_AI_TASKS.FEED_GENERATE,
    INS_AI_TASKS.REELS_GENERATE,
    INS_AI_TASKS.PUBLIC_COMMENT_BATCH,
    INS_AI_TASKS.GUESTBOOK_REFRESH,
    INS_AI_TASKS.STRANGER_DM_REFRESH,
    'home_strangers','feed_generate','reels_generate','public_comment_batch','guestbook_refresh','stranger_dm_refresh'
  ]);
  const interactionTasks=new Set([
    INS_AI_TASKS.HOME_STRANGERS,INS_AI_TASKS.FEED_GENERATE,INS_AI_TASKS.REELS_GENERATE,
    INS_AI_TASKS.PUBLIC_COMMENT_BATCH,INS_AI_TASKS.DIRECT_COMMENT_REPLY,INS_AI_TASKS.DM_REPLY,
    INS_AI_TASKS.STRANGER_DM_REFRESH,INS_AI_TASKS.GUESTBOOK_REFRESH,
    'home_strangers','feed_generate','reels_generate','public_comment_batch','direct_comment_reply','dm_reply','stranger_dm_refresh','guestbook_refresh'
  ]);
  const strangerPoolTasks=new Set([
    INS_AI_TASKS.HOME_STRANGERS,INS_AI_TASKS.PUBLIC_COMMENT_BATCH,INS_AI_TASKS.GUESTBOOK_REFRESH,INS_AI_TASKS.STRANGER_DM_REFRESH,
    'home_strangers','public_comment_batch','guestbook_refresh','stranger_dm_refresh'
  ]);
  const includeStrangerPool=strangerPoolTasks.has(key) || ((key===INS_AI_TASKS.REELS_GENERATE||key==='reels_generate')&&!!extra?.generateStrangers);
  const withWorld=(base,{media=false,strangerIdentity=false}={})=>[
    base,
    worldLayer,
    strangerIdentity?INS_STRANGER_IDENTITY_LANGUAGE_CONTRACT:'',
    includeStrangerPool?INS_STRANGER_ACCOUNT_ECOLOGY_PROMPT:'',
    interactionTasks.has(key)?INS_NATURAL_INTERACTION_ECOLOGY_PROMPT:'',
    media?INS_MEDIA_LANGUAGE_CONTRACT:''
  ].filter(Boolean).join('\n\n');
  if(key===INS_AI_TASKS.FEED_GENERATE || key==='feed_generate' || key===INS_AI_TASKS.HOME_STRANGERS || key==='home_strangers'){
    return {
      globalRules:withWorld(INS_FEED_COMPACT_GLOBAL_PROMPT_EN,{media:true,strangerIdentity:true}),
      surfaceRules:[
        INS_FEED_COMPACT_SURFACE_PROMPT_EN,
        (key===INS_AI_TASKS.HOME_STRANGERS||key==='home_strangers')?INS_SURFACE_PROMPTS.home_strangers:''
      ].filter(Boolean).join('\n\n'),
      priority:'fixed rules/facts > actor persona & memory > explicit hard constraints > current-batch temporary topic > persistent character soft preference > default surface vibe'
    };
  }
  if(key===INS_AI_TASKS.REELS_GENERATE || key==='reels_generate'){
    return {
      globalRules:withWorld(INS_REELS_COMPACT_GLOBAL_PROMPT_EN,{media:true,strangerIdentity:true}),
      surfaceRules:INS_REELS_COMPACT_SURFACE_PROMPT_EN,
      priority:'fixed rules/facts > actor persona & memory > explicit hard constraints > current-batch temporary topic > persistent character soft preference > default surface vibe'
    };
  }
  if(key===INS_AI_TASKS.PUBLIC_COMMENT_BATCH || key==='public_comment_batch'){
    return {
      globalRules:withWorld(INS_FEED_COMPACT_GLOBAL_PROMPT_EN,{media:true,strangerIdentity:true}),
      surfaceRules:INS_PUBLIC_COMMENT_COMPACT_PROMPT_EN,
      priority:'content facts > direct relationships > stable stranger identity/language > natural public-comment style'
    };
  }
  if(key===INS_AI_TASKS.GUESTBOOK_REFRESH || key==='guestbook_refresh'){
    return {
      globalRules:withWorld(INS_FEED_COMPACT_GLOBAL_PROMPT_EN,{media:false,strangerIdentity:true}),
      surfaceRules:INS_GUESTBOOK_COMPACT_PROMPT_EN,
      priority:'target profile facts > direct relationships > stable stranger identity/language > short public guestbook style'
    };
  }
  if(key===INS_AI_TASKS.STORY_AUDIENCE_PLAN || key==='story_audience_plan'){
    return {
      globalRules:withWorld(INS_GLOBAL_SOCIAL_REALISM_PROMPT,{media:false,strangerIdentity:false}),
      surfaceRules:INS_SURFACE_PROMPTS.story_audience_plan,
      priority:'candidate actor facts/persona/memory > direct relationship with User > current Story content > natural 24h viewing probability. Never invent viewers or force a reaction.'
    };
  }
  const includeMedia=[INS_AI_TASKS.HOME_STRANGERS,INS_AI_TASKS.FEED_GENERATE,INS_AI_TASKS.REELS_GENERATE,INS_AI_TASKS.PUBLIC_COMMENT_BATCH].includes(key);
  return {
    globalRules:withWorld(INS_GLOBAL_SOCIAL_REALISM_PROMPT,{media:includeMedia,strangerIdentity:strangerIdentityTasks.has(key)}),
    surfaceRules:getINSSurfacePrompt(task,surface),
    priority:'globalRules > surfaceRules > actor facts/persona/relationship/memory > current batch temporary request > persistent soft preference. Batch requests steer this call only and never rewrite identity or facts.'
  };
}
function normalizeINSBilingualText(data={}){
  const original=String(data.textOriginal??data.captionOriginal??data.caption_original??data.original??data.text??data.caption??'').trim();
  const translation=String(data.textTranslation??data.captionTranslation??data.caption_translation??data.translation??data.translationZh??data.chineseTranslation??'').trim();
  return {textOriginal:original,textTranslation:translation,sourceLanguage:String(data.sourceLanguage||data.language||'').trim()};
}
function getINSRelationshipCandidateSnapshot(ownerRef){
  const ownerId=normalizeSocialActorId(ownerRef);
  if(!ownerId||typeof getDirectRelationshipPartners!=='function')return [];
  const raw=getDirectRelationshipPartners(ownerId)||[];
  const ids=[...new Set(raw.map(item=>normalizeSocialActorId(typeof item==='string'?item:item?.id)).filter(id=>id&&id!==ownerId))];
  return ids.map(id=>{
    const actor=getRelationshipActor(id);
    if(!actor)return null;
    return {
      actorId:id,
      name:actor.name,
      handle:actor.handle||id,
      kind:actor.kind,
      ownerLooksAtActor:getDirectedRelationship(ownerId,id)?.label||'',
      actorLooksAtOwner:getDirectedRelationship(id,ownerId)?.label||'',
      recentKnowledge:typeof getActorKnowledgeContext==='function'?getActorKnowledgeContext(id,6):[]
    };
  }).filter(Boolean);
}

function getINSLeanFeedRelationshipSnapshot(ownerRef,limit=8){
  const ownerId=normalizeSocialActorId(ownerRef);
  if(!ownerId || typeof getDirectRelationshipPartners!=='function')return [];
  const raw=getDirectRelationshipPartners(ownerId)||[];
  const ids=[...new Set(raw
    .map(item=>normalizeSocialActorId(typeof item==='string'?item:item?.id))
    .filter(id=>id&&id!==ownerId))]
    .slice(0,Math.max(1,Number(limit)||8));

  return ids.map(id=>{
    const actor=getRelationshipActor(id);
    if(!actor)return null;
    return {
      actorId:id,
      name:String(actor.name||'').slice(0,80),
      handle:String(actor.handle||id).slice(0,80),
      kind:actor.kind,
      ownerLooksAtActor:getDirectedRelationship(ownerId,id)?.label||'',
      actorLooksAtOwner:getDirectedRelationship(id,ownerId)?.label||''
    };
  }).filter(Boolean);
}

function getINSLeanFeedKnowledge(ownerRef,limit=8){
  if(typeof getActorKnowledgeContext!=='function')return [];
  return (getActorKnowledgeContext(ownerRef,limit)||[])
    .map(line=>String(line||'').trim())
    .filter(Boolean)
    .slice(-Math.max(1,Number(limit)||8))
    .map(line=>line.length>220?line.slice(0,219)+'…':line);
}


function getINSLeanActorRelationshipContexts(actorIds=[],limit=4){
  return [...new Set((actorIds||[]).map(normalizeSocialActorId).filter(Boolean))].map(actorId=>({
    actorId,
    relationships:getINSLeanFeedRelationshipSnapshot(actorId,limit),
    recentKnowledge:getINSLeanFeedKnowledge(actorId,limit)
  }));
}

function buildINSAIGenerationRequest({task,actorIds=[],ownerId='',surface='',content=null,prompt='',extra={}}={}){
  const normalizedActors=[...new Set((actorIds||[]).map(normalizeSocialActorId).filter(Boolean))];
  const normalizedOwner=normalizeSocialActorId(ownerId);
  const taskKey=String(task||'').toLowerCase();
  const isHomeStrangers=taskKey===INS_AI_TASKS.HOME_STRANGERS;
  const isLeanFeed=taskKey===INS_AI_TASKS.FEED_GENERATE;
  const isLeanReels=taskKey===INS_AI_TASKS.REELS_GENERATE;
  const isLeanComment=taskKey===INS_AI_TASKS.PUBLIC_COMMENT_BATCH;
  const isLeanGuestbook=taskKey===INS_AI_TASKS.GUESTBOOK_REFRESH;
  const isStrangerDM=taskKey===INS_AI_TASKS.STRANGER_DM_REFRESH;
  const isStoryAudiencePlan=taskKey===INS_AI_TASKS.STORY_AUDIENCE_PLAN;
  const isLeanPublicContent=isLeanFeed||isLeanReels||isLeanComment||isLeanGuestbook;

  const request={
    schema:'nini-ins-ai-request-v1',
    task:String(task||''),surface:String(surface||''),ownerId:normalizedOwner,
    actorIds:normalizedActors,
    prompt:String(prompt||'').slice(0,isLeanPublicContent?8000:100000),
    content:content||null,
    promptContext:buildINSSocialPromptContext(task,surface,extra),
    relationshipCandidates:isLeanPublicContent
      ? (normalizedOwner?getINSLeanFeedRelationshipSnapshot(normalizedOwner,isLeanReels?4:8):[])
      : (normalizedOwner?getINSRelationshipCandidateSnapshot(normalizedOwner):[]),
    actorRelationshipContexts:isLeanReels
      ? getINSLeanActorRelationshipContexts(normalizedActors,4)
      : [],
    actorRecentKnowledge:isLeanPublicContent
      ? (normalizedOwner?getINSLeanFeedKnowledge(normalizedOwner,8):[])
      : [],
    albumMatcher:{
      eraInstruction:normalizedOwner&&typeof getCharAlbumEraInstruction==='function'?getCharAlbumEraInstruction(normalizedOwner):'',
      actorConfigs:normalizedActors.map(id=>typeof getCharAlbumMatcherConfig==='function'?{actorId:id,...getCharAlbumMatcherConfig(id)}:{actorId:id})
    },
    outputRules:isHomeStrangers
      ? {
          preserveOriginalLanguage:true,
          translationAsSeparateField:true,
          engagementMetricsRequired:true,
          initialCommentCount:Number(extra?.initialCommentCount)||5,
          forbidCurrentUserInComments:true,
          forbidHashtags:false,
          mediaItemsRequired:true,
          mediaItemCountRange:[1,4],
          requestedMediaCounts:Array.isArray(extra?.requestedMediaCounts)?extra.requestedMediaCounts.slice(0,8):[],freeMediaPlan:!!extra?.freeMediaPlan,
          mediaDescriptionsMustDiffer:true,
          mediaDescriptionLanguage:'zh-CN',captionLanguage:'actor_natural',locationLanguage:'en',locationFormat:'City·Country',tagsLanguage:'en',actorMentionTracking:{mentionedActorIds:'exact_formal_actor_ids_only',mentionStrength:['explicit','contextual']},commentLanguageDiversity:{minimumLanguagesForFive:2,requireOneDifferentFromCaption:true}
        }
      : isLeanFeed
      ? {
          preserveOriginalLanguage:true,
          translationAsSeparateField:true,
          engagementMetricsRequired:true,
          initialCommentCount:Number(extra?.initialCommentCount)||0,
          mediaItemsRequired:true,
          mediaItemCountRange:[1,4],
          requestedMediaCounts:Array.isArray(extra?.requestedMediaCounts)?extra.requestedMediaCounts.slice(0,8):[],freeMediaPlan:!!extra?.freeMediaPlan,
          mediaDescriptionsMustDiffer:true,
          mediaDescriptionLanguage:'zh-CN',captionLanguage:'actor_natural',locationLanguage:'en',locationFormat:'City·Country',tagsLanguage:'en',actorMentionTracking:{mentionedActorIds:'exact_formal_actor_ids_only',mentionStrength:['explicit','contextual']},commentLanguageDiversity:{minimumLanguagesForFive:2,requireOneDifferentFromCaption:true}
        }
      : isLeanReels
        ? {
            preserveOriginalLanguage:true,
            translationAsSeparateField:true,
            videoDescriptionRequired:true,
            videoDescriptionLanguage:'zh-CN',captionLanguage:'actor_natural',locationLanguage:'en',locationFormat:'City·Country',tagsLanguage:'en',
            engagementMetricsRequired:true,
            initialCommentCount:Number(extra?.initialCommentCount)||0,
            actorMentionTracking:{mentionedActorIds:'exact_formal_actor_ids_only',mentionStrength:['explicit','contextual']},
            commentLanguageDiversity:{minimumLanguagesForFive:2,requireOneDifferentFromCaption:true}
          }
        : isLeanComment
          ? {
              preserveOriginalLanguage:true,
              translationAsSeparateField:true,
              authorMustBeStringHandle:true,
              forbidCurrentUser:true,
              actualCommentCount:Number(extra?.actualCommentCount||extra?.initialCommentCount)||5,
              actorMentionTracking:{mentionedActorIds:'exact_formal_actor_ids_only',mentionStrength:['explicit','contextual']},
              commentLanguageDiversity:{minimumLanguagesForFive:2,requireOneDifferentFromCaption:true,preferMissingLanguagesOnTopUp:true}
            }
          : isLeanGuestbook
            ? {
                preserveOriginalLanguage:true,
                translationAsSeparateField:true,
                authorMustBeStringHandle:true,
                strangerIdentityRequired:true,
                followWorldCultureNaming:true,
                forbidCurrentUser:true,
                actualEntryCount:Number(extra?.actualEntryCount)||5
              }
            : isStoryAudiencePlan
              ? {
                  storyAudiencePlanRequired:true,
                  exactCandidateActorIdsOnly:true,
                  oneDecisionPerCandidate:true,
                  forbidCurrentUserAsAudience:true,
                  forbidStrangers:true,
                  viewWindowMinutes:[0,1439],
                  viewMayBeFalse:true,
                  reactionMayBeFalse:true,
                  allowedReactionCodes:['like','amused','excited','affectionate','shocked','supportive','mindblown','emotional','flustered'],
                  reactionDelayAfterViewMinutes:[0,90],
                  noAdditionalCallsForTimedReveal:true
                }
            : isStrangerDM
              ? {
                  preserveOriginalLanguage:true,
                  translationAsSeparateField:true,
                  strangerIdentityRequired:true,
                  followWorldCultureNaming:true,
                  stableHandleDisplayNameLanguageRegion:true,
                  reasonableDiscoveryPath:true
                }
              : {
            preserveOriginalLanguage:true,
            translationAsSeparateField:true,
            noTranslationFieldWhenNotNeeded:true,
            commentsUseExistingActorIdsWhenNamed:true,
            strangersMayUseTemporaryHandles:true,
            storyHasNoCommentThread:true,
            reactionUsesSemanticCode:true
          },
    imageGeneration:isLeanPublicContent
      ? {
          descriptionLanguage:typeof getINSImagePromptConfig==='function'
            ? (getINSImagePromptConfig().descriptionLanguage||'zh')
            : 'zh',
          source:'deferred_after_text_generation'
        }
      : (typeof getINSImagePromptConfig==='function'?getINSImagePromptConfig():{
          descriptionLanguage:'zh',source:'inherit_roche_global',positivePrompt:'',negativePrompt:''
        }),
    apiRouting:(()=>{
      const active=typeof getActiveINSSubApi==='function'?getActiveINSSubApi():null;
      return active?{
        mode:'ins_subapi_override',
        id:active.id||'',
        name:active.name||'',
        url:active.url||'',
        model:active.model||''
      }:{
        mode:'inherit_roche_default',
        id:'',name:'',url:'',model:''
      };
    })(),
    extra:{...(extra||{}),homeStrangers:isHomeStrangers,leanFeed:isLeanFeed,leanReels:isLeanReels,leanComments:isLeanComment,leanGuestbook:isLeanGuestbook}
  };
  if(!isStoryAudiencePlan && typeof getStoryAudiencePiggybackPayload==='function'){
    const piggyback=getStoryAudiencePiggybackPayload();
    if(piggyback){
      request.extra.storyAudiencePlanPiggyback=piggyback;
      request.promptContext.storyAudiencePlanPiggybackRules='Optional piggyback: if possible, also return storyAudiencePlan with one decision per supplied candidate. This must not change or replace the main task output.';
    }
  }
  return request;
}
async function callNiniINSAI(task,payload={}){
  const bridge=window.NiniINSAIBridge;
  const request=buildINSAIGenerationRequest({task,...(payload||{})});
  if(!bridge||typeof bridge.generate!=='function')return {ok:false,reason:'bridge_not_connected',request};
  try{
    const data=await bridge.generate(request);
    if(task!==INS_AI_TASKS.STORY_AUDIENCE_PLAN && typeof applyStoryAudiencePlanFromPiggyback==='function'){
      try{applyStoryAudiencePlanFromPiggyback(data);}catch(_storyPiggybackError){}
    }
    return {ok:true,data,request};
  }
  catch(error){return {ok:false,reason:error?.message||'generation_failed',request};}
}
function getINSCurrentUserIdentityTokens(){
  return new Set([
    'user','you',
    String(userProfileState?.id||''),
    String(userProfileState?.rochePersonaId||''),
    String(userProfileState?.displayName||'')
  ].map(v=>String(v||'').replace(/^@/,'').trim()).filter(Boolean));
}
function isINSCurrentUserIdentityRef(ref){
  if(ref && typeof ref==='object'){
    return [ref.id,ref.actorId,ref.authorId,ref.handle,ref.username,ref.name,ref.displayName]
      .some(v=>isINSCurrentUserIdentityRef(v));
  }
  const raw=String(ref||'').replace(/^@/,'').trim();
  if(!raw)return false;
  if(getINSCurrentUserIdentityTokens().has(raw))return true;
  try{return normalizeSocialActorId(raw)==='user';}catch(e){return false;}
}
function rawGeneratedEntryIsUser(raw={}){
  return [
    raw.actorId,raw.authorId,raw.author,raw.handle,raw.username,raw.displayName,raw.name,
    raw.actor,raw.author
  ].some(v=>isINSCurrentUserIdentityRef(v));
}
function normalizeGeneratedPublicComment(raw={},fallbackOwnerId='',index=0){
  if(rawGeneratedEntryIsUser(raw))return null;
  const bilingual=normalizeINSBilingualText(raw);
  const actorObj=(raw.actor&&typeof raw.actor==='object')?raw.actor:{};
  const authorObj=(raw.author&&typeof raw.author==='object')?raw.author:{};
  const authorString=typeof raw.author==='string'?raw.author:'';
  const actorIdCandidate=
    (typeof raw.actorId==='string'?raw.actorId:'')||
    (typeof raw.authorId==='string'?raw.authorId:'')||
    String(actorObj.id||authorObj.id||'');
  let actorId=normalizeSocialActorId(actorIdCandidate||authorString||'');
  if(actorId==='user')return null;
  if(actorId && !isFormalSocialActor(actorId))actorId='';

  let author=String(
    actorObj.handle||
    authorObj.handle||
    raw.handle||
    raw.username||
    authorString||
    (actorId?getIdentityMeta(actorId).handle:'')||
    `public_${Date.now().toString(36)}_${index}_${Math.random().toString(36).slice(2,5)}`
  ).replace(/^@/,'').trim();

  if(!author || author==='[object Object]'){
    author=`public_${Date.now().toString(36)}_${index}_${Math.random().toString(36).slice(2,5)}`;
  }
  if(isINSCurrentUserIdentityRef(author))return null;

  const displayName=String(
    actorObj.displayName||
    actorObj.name||
    authorObj.displayName||
    authorObj.name||
    raw.displayName||
    raw.name||
    author
  ).trim()||author;
  if(isINSCurrentUserIdentityRef(displayName))return null;

  if(actorId){
    author=actorId;
  }else{
    publicIdentityRegistry[author]={
      handle:author,
      name:displayName,
      initial:String(raw.initial||actorObj.initial||authorObj.initial||displayName||'?').slice(0,1).toUpperCase(),
      avatarUrl:STRANGER_AVATAR_URL,
      language:String(actorObj.language||authorObj.language||raw.sourceLanguage||''),
      region:String(actorObj.region||authorObj.region||''),
      bio:String(actorObj.bio||authorObj.bio||'')
    };
  }

  return {
    actorId,
    author,
    displayName,
    initial:String(raw.initial||actorObj.initial||authorObj.initial||'').slice(0,2),
    text:bilingual.textOriginal,
    translation:bilingual.textTranslation,
    sourceLanguage:bilingual.sourceLanguage,
    replyToActorId:normalizeSocialActorId(raw.replyToActorId||raw.replyTo||''),
    ownerId:normalizeSocialActorId(raw.ownerId||fallbackOwnerId),
    mentionedActorIds:[...new Set([
      ...(Array.isArray(raw.mentionedActorIds)?raw.mentionedActorIds:[]),
      ...(Array.isArray(raw.mentions)?raw.mentions:[])
    ].map(item=>normalizeSocialActorId(item?.actorId||item?.id||item)).filter(id=>id&&id!=='user'&&isFormalSocialActor(id)))],
    mentionStrength:String(raw.mentionStrength||raw.mentionType||'explicit')==='contextual'?'contextual':'explicit',
    createdAt:Number(raw.createdAt)||Date.now()
  };
}

function applyGeneratedPublicCommentBatch({surface='feed',contentId='',ownerId='',comments=[]}={}){
  const sid=String(surface||'feed').toLowerCase();
  const cid=String(contentId||'');
  const oid=normalizeSocialActorId(ownerId);
  if(!cid)return {ok:false,reason:'missing_content_id',added:[]};
  const normalized=(Array.isArray(comments)?comments:[]).map((row,index)=>normalizeGeneratedPublicComment(row,oid,index)).filter(row=>row&&row.text);
  if(sid==='feed'){
    if(typeof commentsByPost==='undefined')return {ok:false,reason:'feed_comments_not_ready',added:[]};
    if(!commentsByPost[cid])commentsByPost[cid]=[];
    normalized.forEach(row=>commentsByPost[cid].push({author:row.author||row.actorId||'public',displayName:row.displayName||'',text:row.text,translation:row.translation,sourceLanguage:row.sourceLanguage,generated:true,createdAt:row.createdAt}));
  }else if(sid==='reel'||sid==='reels'){
    if(typeof reelsV15State==='undefined')return {ok:false,reason:'reels_not_ready',added:[]};
    if(!reelsV15State.comments[cid])reelsV15State.comments[cid]=[];
    normalized.forEach(row=>reelsV15State.comments[cid].push({initial:row.initial||(socialActorName(row.actorId)||'?').slice(0,1).toUpperCase(),author:row.author||row.actorId||'public',displayName:row.displayName||'',text:row.text,translation:row.translation,sourceLanguage:row.sourceLanguage,generated:true,createdAt:row.createdAt}));
  }else return {ok:false,reason:'unsupported_surface',added:[]};
  normalized.forEach(row=>{
    if(typeof recordSocialEvent!=='function')return;
    const actorId=normalizeSocialActorId(row.actorId||row.author);
    const replyId=normalizeSocialActorId(row.replyToActorId);
    const known=[];
    if(isFormalSocialActor(actorId))known.push(actorId);
    if(isFormalSocialActor(oid))known.push(oid);
    if(replyId&&isFormalSocialActor(replyId))known.push(replyId);
    recordSocialEvent({
      type:sid==='feed'?'feed_comment':'reel_comment',actorId:actorId||row.author||'public',
      targetIds:[oid,replyId].filter(Boolean),contentId:cid,contentType:sid==='feed'?'feed':'reel',
      contentOwnerId:oid,threadId:`${sid==='feed'?'feed':'reel'}_comments:${cid}`,replyToActorId:replyId,
      relatedActorIds:row.mentionedActorIds||[],
      text:row.text,visibility:'public',
      payload:{translation:row.translation,sourceLanguage:row.sourceLanguage,generatedBatch:true,mentionedActorIds:row.mentionedActorIds||[],mentionStrength:row.mentionStrength||'explicit'},
      summary:`${socialActorName(actorId||row.author)}在 ${socialActorName(oid)} 的 ${sid==='feed'?'IG 动态':'Reel'} 下${replyId?`回复了 ${socialActorName(replyId)}`:'评论'}：“${row.text}”`
    },{knownActorIds:known,learnedVia:'generated_comment'});
  });
  if(sid==='feed'){if(typeof renderComments==='function')renderComments(cid);if(typeof renderFeed==='function')renderFeed();}
  else{if(typeof renderReelsV15Comments==='function')renderReelsV15Comments();if(typeof renderReelsV15==='function')renderReelsV15();}
  return {ok:true,added:normalized};
}

function compactPublicCommentSeed(rows,limit=10){
  return (Array.isArray(rows)?rows:[]).slice(-limit).map(row=>({
    author:getIdentityMeta(row.author).handle||String(row.author||''),
    text:String(row.text||'').slice(0,180)
  }));
}

async function generateNextPublicCommentBatch(surface,contentId,desiredCount=5){
  const sid=String(surface||'feed').toLowerCase();
  const targetCount=Math.max(1,Math.min(10,Number(desiredCount)||5));
  let ownerId='';
  let content=null;

  const buildContent=()=>{
    if(sid==='feed'){
      const post=posts.find(p=>p.id===contentId);
      if(!post)throw new Error('找不到这条 IG 动态');
      ownerId=normalizeSocialActorId(post.author);
      return {
        contentType:'feed',contentId:post.id,
        author:{actorId:ownerId,handle:getIdentityMeta(post.author).handle,name:getIdentityMeta(post.author).name},
        caption:[post.captionShort||'',post.captionLong||''].join(' ').replace(/<[^>]+>/g,' ').replace(/\s+/g,' ').trim(),
        mediaDescription:post.mediaDescription||post.mediaText||'',
        existingComments:compactPublicCommentSeed(commentsByPost[post.id]||[],18),
        forbiddenCommenters:[...getINSCurrentUserIdentityTokens()]
      };
    }
    const item=getReelsV15Item(contentId);
    if(!item)throw new Error('找不到这条 Reel');
    ownerId=normalizeSocialActorId(item.author);
    return {
      contentType:'reel',contentId:item.id,
      author:{actorId:ownerId,handle:getIdentityMeta(item.author).handle,name:getIdentityMeta(item.author).name},
      caption:item.caption||'',videoDescription:item.videoDescription||'',
      existingComments:compactPublicCommentSeed(reelsV15State.comments[item.id]||[],18),
      forbiddenCommenters:[...getINSCurrentUserIdentityTokens()]
    };
  };

  let addedTotal=0;
  for(let attempt=0;attempt<3 && addedTotal<targetCount;attempt++){
    content=buildContent();
    const missing=targetCount-addedTotal;
    const result=await callNiniINSAI(INS_AI_TASKS.PUBLIC_COMMENT_BATCH,{
      actorIds:[],ownerId,
      surface:sid==='feed'?'feed':'reels',
      content,
      prompt:`Generate exactly ${missing} new public comments. Never use the current User/persona as a commenter. Do not repeat existing comments. Stranger author must be a string handle, never an object. Commenter language is independent from the post language. Prefer languages missing from existingComments; do not let the visible comment section collapse into the same language as the caption. Questions (including product link/location/how-to) are allowed but are not the default. Mix opinions, personal experiences, detail reactions, low-information replies, occasional mild disagreement or ad fatigue, and rare collaboration/spam-like accounts when natural.`,
      extra:{actualCommentCount:missing,appendBatch:true,allowKnownActors:true,allowStrangers:true,forbidCurrentUser:true}
    });
    if(!result.ok)throw new Error(result.reason||'评论生成失败');

    const rows=Array.isArray(result.data?.comments)?result.data.comments:[];
    if(!rows.length && attempt===2)throw new Error('AI 没有返回 comments');

    const applied=applyGeneratedPublicCommentBatch({
      surface:sid,contentId,ownerId,comments:rows.slice(0,missing)
    });
    const added=Array.isArray(applied?.added)?Math.min(missing,applied.added.length):0;
    addedTotal+=added;
  }

  if(addedTotal<targetCount){
    console.warn('[INS] comment batch returned fewer safe non-user comments', {sid,contentId,targetCount,addedTotal});
  }

  if(sid==='feed'){
    const post=posts.find(p=>p.id===contentId);
    if(post)post.comments=Math.max(Number(post.comments)||0,(commentsByPost[contentId]||[]).length);
    renderFeed();renderComments(contentId);
  }else{
    const item=getReelsV15Item(contentId);
    if(item)item.comments=Math.max(Number(item.comments)||0,(reelsV15State.comments[contentId]||[]).length);
    renderReelsV15();renderReelsV15Comments();
  }
  return addedTotal;
}
async function ensurePublicCommentFloor(surface,contentId,target=5){
  const sid=String(surface||'feed').toLowerCase();
  const rows=sid==='feed'?(commentsByPost[contentId]||[]):(reelsV15State.comments[contentId]||[]);
  const missing=Math.max(0,(Number(target)||5)-rows.length);
  if(!missing)return 0;
  return await generateNextPublicCommentBatch(sid,contentId,missing);
}

function normalizeINSGeneratedContentBundle(raw={},surface='feed'){
  const caption=normalizeINSBilingualText({textOriginal:raw.captionOriginal??raw.caption,textTranslation:raw.captionTranslation??raw.translation,sourceLanguage:raw.sourceLanguage});
  return {
    surface,content:raw.content||raw.media||null,caption,
    metrics:{likes:Number(raw.metrics?.likes??raw.likes??0),comments:Number(raw.metrics?.comments??raw.comments??0),reposts:Number(raw.metrics?.reposts??raw.reposts??0),shares:Number(raw.metrics?.shares??raw.shares??0)},
    comments:Array.isArray(raw.initialComments)?raw.initialComments:Array.isArray(raw.comments)?raw.comments:[],
    reactions:Array.isArray(raw.reactions)?raw.reactions.map(normalizeINSReactionDecision):[]
  };
}
window.NiniINSAIContracts={
  tasks:INS_AI_TASKS,content:INS_CONTENT_GENERATION_CONTRACT,reactions:INS_REACTION_SCHEMA,
  globalSocialPrompt:INS_GLOBAL_SOCIAL_REALISM_PROMPT,surfacePrompts:INS_SURFACE_PROMPTS,
  buildSocialPromptContext:buildINSSocialPromptContext,
  normalizeBilingualText:normalizeINSBilingualText,getRelationshipCandidates:getINSRelationshipCandidateSnapshot,
  buildRequest:buildINSAIGenerationRequest,call:callNiniINSAI,normalizeBundle:normalizeINSGeneratedContentBundle,
  applyGeneratedPublicCommentBatch
};
window.NiniINSSettingsBridge={
  getImagePromptConfig:getINSImagePromptConfig,
  buildImageRouting:buildINSImageGenerationRouting,
  getActiveSubApi:getActiveINSSubApi
};

const STORY_DEFAULT_CAPTIONS = {user:''};

const storyBatchState = {};
Object.keys(storyViewerData).forEach(owner=>{
  storyBatchState[owner]={
    liked:false,
    caption:STORY_DEFAULT_CAPTIONS[owner]||'',
    audienceReactions:[],
    reactionPlanStatus:'idle',
    plannedReactions:[]
  };
});

function getStoryBatchState(owner){
  if(!storyBatchState[owner]){
    storyBatchState[owner]={
      liked:false,
      caption:STORY_DEFAULT_CAPTIONS[owner]||'',
      audienceReactions:[],
      reactionPlanStatus:'idle',
      plannedReactions:[]
    };
  }
  return storyBatchState[owner];
}
function getStoryBatchCompactSummary(owner){
  const items=storyViewerData[owner] || [];
  const images=items.filter(x=>x.type==='image').length;
  const videos=items.filter(x=>x.type==='video').length;
  const parts=[];
  if(images) parts.push(`${images}张图片`);
  if(videos) parts.push(`${videos}个视频`);
  return `${items.length}条（${parts.join(' + ') || '无内容'}）`;
}


/* Story Archive semantics:
   A "Story" here means one whole published batch.
   The batch may contain 1..4 internal image/video items.
   Highlight binds whole batches, never individual internal items. */
const NINI_STORY_LIFECYCLE_STORAGE_KEY='niniInstagramStoryLifecycleV507';
const NINI_STORY_LIVE_TTL_MS=24*60*60*1000;
function loadNiniStoryLifecycle(){
  try{
    const parsed=JSON.parse(localStorage.getItem(NINI_STORY_LIFECYCLE_STORAGE_KEY)||'{}');
    return parsed&&typeof parsed==='object'?parsed:{};
  }catch(e){return {};}
}
const persistedNiniStoryLifecycle=loadNiniStoryLifecycle();
const storyArchive=(persistedNiniStoryLifecycle.archive&&typeof persistedNiniStoryLifecycle.archive==='object')?persistedNiniStoryLifecycle.archive:{};
const storyLiveMeta=(persistedNiniStoryLifecycle.liveMeta&&typeof persistedNiniStoryLifecycle.liveMeta==='object')?persistedNiniStoryLifecycle.liveMeta:{};
const persistedHighlightState=(persistedNiniStoryLifecycle.highlights&&typeof persistedNiniStoryLifecycle.highlights==='object')?persistedNiniStoryLifecycle.highlights:{};
const storyReactionPlans=(persistedNiniStoryLifecycle.reactionPlans&&typeof persistedNiniStoryLifecycle.reactionPlans==='object')?persistedNiniStoryLifecycle.reactionPlans:{};
function persistNiniStoryLifecycle(){
  try{
    const charHighlights={};
    if(typeof charProfileState!=='undefined'){
      Object.entries(charProfileState.profiles||{}).forEach(([key,profile])=>{
        charHighlights[key]=Array.isArray(profile?.highlights)?profile.highlights:[];
      });
    }
    const payload={
      archive:storyArchive,
      liveMeta:storyLiveMeta,
      highlights:{
        user:(typeof userProfileState!=='undefined'&&Array.isArray(userProfileState.highlights))?userProfileState.highlights:[],
        chars:charHighlights
      },
      reactionPlans:storyReactionPlans,
      updatedAt:Date.now()
    };
    localStorage.setItem(NINI_STORY_LIFECYCLE_STORAGE_KEY,JSON.stringify(payload));
  }catch(e){console.warn('[INS] story lifecycle persist failed',e);}
}
function restorePersistedHighlights(){
  try{
    const fresh=(loadNiniStoryLifecycle().highlights||persistedHighlightState||{});
    if(Array.isArray(fresh.user) && fresh.user.length){
      userProfileState.highlights=fresh.user;
    }
    const chars=fresh.chars||{};
    Object.entries(chars).forEach(([key,rows])=>{
      if(charProfileState.profiles[key] && Array.isArray(rows))charProfileState.profiles[key].highlights=rows;
    });
  }catch(e){}
}


function cloneStoryItemForArchive(item){
  return {
    id:item.id,
    type:item.type,
    time:item.time||'',
    duration:item.duration,
    description:item.description||'',
    mediaUrl:item.mediaUrl||'',
    mediaSource:item.mediaSource||'',
    generated:!!item.generated,
    visionSummary:item.visionSummary||''
  };
}

function makeStoryArchiveRecord(owner, items, caption, options={}){
  const safeItems=(items||[]).map(cloneStoryItemForArchive);
  const imageCount=safeItems.filter(x=>x.type==='image').length;
  const videoCount=safeItems.filter(x=>x.type==='video').length;
  return {
    batchId:options.batchId || `story_batch_${owner}_${Date.now()}_${Math.random().toString(36).slice(2,7)}`,
    owner,
    createdAt:options.createdAt || Date.now(),
    caption:caption||'',
    items:safeItems,
    imageCount,
    videoCount
  };
}

function ensureStoryArchive(owner){
  if(!storyArchive[owner]) storyArchive[owner]=[];

  // Seed current sample/current Story as one archive batch only once.
  if(!storyArchive[owner].length && (storyViewerData[owner]||[]).length){
    storyArchive[owner].push(
      makeStoryArchiveRecord(
        owner,
        storyViewerData[owner],
        getStoryBatchState(owner).caption||'',
        {batchId:`story_batch_${owner}_seed`}
      )
    );
  }
  return storyArchive[owner];
}

function archivePublishedStoryBatch(owner, items, caption){
  if(!storyArchive[owner]) storyArchive[owner]=[];
  const record=makeStoryArchiveRecord(owner,items,caption);
  storyArchive[owner].push(record);
  if(storyArchive[owner].length>48)storyArchive[owner]=storyArchive[owner].slice(-48);
  persistNiniStoryLifecycle();
  return record;
}
function markStoryBatchLive(owner,record){
  if(!owner||!record)return;
  storyLiveMeta[owner]={
    batchId:record.batchId,
    publishedAt:Number(record.createdAt)||Date.now(),
    expiresAt:(Number(record.createdAt)||Date.now())+NINI_STORY_LIVE_TTL_MS
  };
  persistNiniStoryLifecycle();
}
function restoreLiveStoriesFromArchive(){
  const now=Date.now();
  Object.entries(storyLiveMeta).forEach(([owner,meta])=>{
    if(!meta || Number(meta.expiresAt)<=now){
      delete storyLiveMeta[owner];
      if(storyViewerData[owner])storyViewerData[owner]=[];
      return;
    }
    const batch=(storyArchive[owner]||[]).find(row=>row.batchId===meta.batchId);
    if(!batch)return;
    storyViewerData[owner]=(batch.items||[]).map(cloneStoryItemForArchive);
    const state=getStoryBatchState(owner);
    state.caption=batch.caption||'';
    state.publishedAt=Number(batch.createdAt)||Date.now();
    const plan=storyReactionPlans[owner];
    if(plan?.batchId===meta.batchId){
      if(plan.status==='planning')plan.status='pending';
      state.reactionPlanStatus=plan.status||'pending';
      state.plannedReactions=Array.isArray(plan.rows)?plan.rows:[];
    }
  });
  if(typeof settleStoryAudiencePlanNow==='function')settleStoryAudiencePlanNow({render:false});
  persistNiniStoryLifecycle();
}
function expireLiveStoriesNow({render=true}={}){
  const now=Date.now();
  let changed=false;
  Object.entries(storyLiveMeta).forEach(([owner,meta])=>{
    if(meta && Number(meta.expiresAt)>now)return;
    delete storyLiveMeta[owner];
    if(storyViewerData[owner]&&storyViewerData[owner].length){storyViewerData[owner]=[];changed=true;}
    storySeenState[owner]=false;
    const plan=storyReactionPlans[owner];
    if(plan && plan.status!=='cancelled'){plan.status='expired';}
    const state=getStoryBatchState(owner);
    if(state)state.reactionPlanStatus='expired';
  });
  if(changed||render){persistNiniStoryLifecycle();if(typeof syncHomeStoryStrip==='function')syncHomeStoryStrip({skipExpiry:true});}
  return changed;
}

function pruneStoryBatchFromHighlightBindings(owner,batchId){
  if(!batchId)return;
  const prune=(highlights=[])=>{
    (Array.isArray(highlights)?highlights:[]).forEach(h=>{
      const binding=h?.storyBinding;
      if(!binding)return;
      if(Array.isArray(binding.batches)){
        const kept=binding.batches.filter(batch=>batch?.batchId!==batchId);
        h.storyBinding=kept.length?{...binding,batches:kept}:null;
      }else if(binding.batchId===batchId){
        h.storyBinding=null;
      }
    });
  };
  if(owner==='user'){
    prune(userProfileState?.highlights||[]);
    return;
  }
  const key=typeof resolveCharProfileKey==='function'?resolveCharProfileKey(owner):owner;
  const profile=charProfileState?.profiles?.[key];
  if(profile)prune(profile.highlights||[]);
}
function getCurrentLiveStoryBatchId(owner){
  const meta=storyLiveMeta[owner];
  if(meta?.batchId)return meta.batchId;
  const ids=new Set((storyViewerData[owner]||[]).map(item=>item?.id).filter(Boolean));
  if(!ids.size)return '';
  const rows=storyArchive[owner]||[];
  for(let i=rows.length-1;i>=0;i--){
    const batch=rows[i];
    if((batch?.items||[]).some(item=>ids.has(item?.id)))return batch.batchId||'';
  }
  return '';
}
function deletePublishedStory(owner=storyViewerState.owner){
  if(!owner)return;
  const batchId=getCurrentLiveStoryBatchId(owner);
  if(batchId){
    const archive=storyArchive[owner]||[];
    for(let i=archive.length-1;i>=0;i--){
      if(archive[i]?.batchId===batchId)archive.splice(i,1);
    }
    pruneStoryBatchFromHighlightBindings(owner,batchId);
  }
  delete storyLiveMeta[owner];
  storyViewerData[owner]=[];
  storySeenState[owner]=false;
  const batchState=getStoryBatchState(owner);
  batchState.caption='';
  batchState.liked=false;
  batchState.audienceReactions=[];
  batchState.reactionPlanStatus='cancelled';
  batchState.plannedReactions=[];
  if(storyReactionPlans[owner]){
    storyReactionPlans[owner].status='cancelled';
    storyReactionPlans[owner].rows=[];
  }
  persistNiniStoryLifecycle();
  closeStorySheets();
  if(typeof syncHomeStoryStrip==='function')syncHomeStoryStrip({skipExpiry:true});
  if(typeof renderUserProfile==='function')renderUserProfile();
  if(typeof renderCharProfile==='function' && owner!=='user')renderCharProfile();
  hideRoute('routeStory');
}

function getStoryArchive(owner){
  return ensureStoryArchive(owner);
}

Object.keys(storyViewerData).forEach(owner=>ensureStoryArchive(owner));
setInterval(()=>expireLiveStoriesNow({render:true}),60*1000);
setInterval(()=>settleStoryAudiencePlanNow({render:true}),60*1000);
document.addEventListener('visibilitychange',()=>{
  if(!document.hidden)settleStoryAudiencePlanNow({render:true});
});


const storyViewerState = {
  owner:null,
  index:0
};

const storyEditState = {
  editIndex:0
};

/* INS-side reaction queue.
   Future Roche memory sync can summarize these items directly without
   asking the model to infer what an emoji meant. */
const storyReactionEvents = [];


function getCurrentStoryItems(){
  return storyViewerData[storyViewerState.owner] || [];
}
function getCurrentStoryItem(){
  const items=getCurrentStoryItems();
  return items[storyViewerState.index] || null;
}
function getAlbumAssetsForHandle(handle){
  if(typeof isRocheCharacterHandle==='function' && !isRocheCharacterHandle(handle)) return [];
  const key=typeof resolveCharProfileKey==='function' ? resolveCharProfileKey(handle) : handle;
  if(!key)return [];
  return typeof getCharAlbumSelectableAssets==='function'
    ? getCharAlbumSelectableAssets(key)
    : (charAlbumPreviewData[key]||[]).filter(asset=>!['generated','generated_favorite'].includes(String(asset?.kind||'')));
}
function storyAlbumDesc(handle,assetId){
  const asset=getAlbumAssetsForHandle(handle).find(a=>a.id===assetId);
  return asset ? asset.desc : '';
}
function storyDisplayDescription(item){
  if(!item) return 'Story 内容占位';
  const base=item.description || '';
  if(item.type==='image' && item.linkedAlbumId){
    const desc=storyAlbumDesc(storyViewerState.owner,item.linkedAlbumId);
    return base + (desc ? `\n\n[相册图片] ${desc}` : '');
  }
  if(item.type==='image' && item.generated){
    return base;
  }
  return base;
}
function closeStorySheets(){
  document.getElementById('storyOverlayDim')?.classList.remove('show');
  document.querySelectorAll('#routeStory .story-sheet.show').forEach(el=>el.classList.remove('show'));
}
function openStorySheet(id){
  closeStorySheets();
  document.getElementById('storyOverlayDim')?.classList.add('show');
  document.getElementById(id)?.classList.add('show');
}
function renderStoryEditSheet(){
  const owner=storyViewerState.owner;
  const items=getCurrentStoryItems();
  const item=items[storyEditState.editIndex];
  if(!item) return;

  const captionInput=document.getElementById('storyEditCaption');
  if(captionInput)captionInput.value=getStoryBatchState(owner).caption||'';
  const chipBox=document.getElementById('storyEditChipRow');
  const limit=document.getElementById('storyEditLimit');
  if(limit) limit.textContent=`当前 ${items.length} 条 · 一组最多 4 条，可以只保留 1 条。`;
  if(chipBox){
    chipBox.innerHTML=items.map((row,index)=>`
      <button class="story-edit-chip ${index===storyEditState.editIndex?'active':''}" data-story-edit-item="${index}" type="button">
        第 ${index+1} 条 · ${row.type==='video'?'视频':'图片'}
      </button>
    `).join('');
  }

  document.getElementById('storyEditTypeCopy').textContent =
    item.type==='video'
      ? '视频条目：只能保留文字描述，可调整顺序。'
      : '图片条目：可保留文字描述，也可关联角色相册或选择生图。';

  const sourceBadge = item.type==='video'
    ? '当前来源：视频描述'
    : item.linkedAlbumId
      ? `当前来源：角色相册 · ${storyAlbumDesc(owner,item.linkedAlbumId)}`
      : item.generated
        ? '当前来源：文字描述生图'
        : '当前来源：文字描述';
  document.getElementById('storyEditSourceBadge').textContent = sourceBadge;
  document.getElementById('storyEditDescription').value = item.description || '';

  const imageTools=document.getElementById('storyEditImageTools');
  const videoNote=document.getElementById('storyEditVideoNote');
  const albumSelect=document.getElementById('storyEditAlbumSelect');

  if(item.type==='video'){
    imageTools.style.display='none';
    videoNote.style.display='block';
  }else{
    imageTools.style.display='block';
    videoNote.style.display='none';

    const roche=typeof isRocheCharacterHandle==='function' && isRocheCharacterHandle(owner);
    const albumControls=document.getElementById('storyEditAlbumControls');
    const policyNote=document.getElementById('storyEditImagePolicyNote');
    if(albumControls) albumControls.style.display=roche?'block':'none';
    if(policyNote){
      policyNote.textContent=roche
        ? 'Roche Character 可优先关联私人相册；不关联则保持文字描述，之后由 user 决定是否生图。'
        : '固定 NPC / 非 Roche 账号不调用私人相册 Matcher；图片直接保留文字描述，再由 user 决定是否生图。';
    }

    if(roche){
      const assets=getAlbumAssetsForHandle(owner);
      albumSelect.innerHTML = '<option value="">不关联相册，保持文字描述</option>' +
        assets.map(a=>`<option value="${a.id}">${a.desc}</option>`).join('');
      albumSelect.value = item.linkedAlbumId || '';
    }else{
      item.linkedAlbumId='';
    }
  }
}
function moveStoryEditItem(direction){
  const items=getCurrentStoryItems();
  const from=storyEditState.editIndex;
  const to=from+direction;
  if(to<0 || to>=items.length) return;
  const temp=items[from];
  items[from]=items[to];
  items[to]=temp;
  storyEditState.editIndex=to;
  storyViewerState.index=to;
  renderStoryEditSheet();
  renderStoryViewer();
}
function addStoryEditItem(type){
  const items=getCurrentStoryItems();
  if(items.length>=4){
    alert('一组现实动态最多 4 条');
    return;
  }
  const item={
    id:'story_manual_'+Date.now(),
    type:type==='video'?'video':'image',
    time:'刚刚',
    duration:type==='video'?6000:undefined,
    description:type==='video'?'新的视频 Story 描述':'新的图片 Story 描述',
    liked:false,
    linkedAlbumId:'',
    generated:false
  };
  items.push(item);
  storyEditState.editIndex=items.length-1;
  storyViewerState.index=storyEditState.editIndex;
  renderStoryEditSheet();
  renderStoryViewer();
}
function deleteCurrentStoryEditItem(){
  const items=getCurrentStoryItems();
  if(items.length<=1){
    alert('至少保留 1 条；如果整个 Story 都不要，后面可以做“删除整组”。');
    return;
  }
  items.splice(storyEditState.editIndex,1);
  storyEditState.editIndex=Math.max(0,Math.min(storyEditState.editIndex,items.length-1));
  storyViewerState.index=storyEditState.editIndex;
  renderStoryEditSheet();
  renderStoryViewer();
}

function openStoryEditor(){
  if(!storyViewerState.owner) return;
  storyEditState.editIndex=storyViewerState.index;
  renderStoryEditSheet();
  openStorySheet('storyEditSheet');
}
function invalidateStoryReactionPlan(owner){
  const state=getStoryBatchState(owner);
  state.plannedReactions=[];
  if(owner!=='user'){
    state.reactionPlanStatus='idle';
    return;
  }
  state.reactionPlanStatus='pending';
  state.audienceReactions=[];
  const batchId=getCurrentLiveStoryBatchId(owner);
  storyReactionPlans[owner]={
    batchId,
    status:'pending',
    reason:'story_edited',
    plannedAt:0,
    lastError:'',
    rows:[]
  };
  persistNiniStoryLifecycle();
}
function syncCurrentStoryBatchToArchive(owner){
  const batchId=getCurrentLiveStoryBatchId(owner);
  if(!batchId)return;
  const rows=storyArchive[owner]||[];
  const batch=rows.find(row=>row?.batchId===batchId);
  if(!batch)return;
  batch.items=(storyViewerData[owner]||[]).map(cloneStoryItemForArchive);
  batch.caption=getStoryBatchState(owner).caption||'';
}

function getStoryReactionMemoryContext(owner){
  return storyReactionEvents
    .filter(event=>!owner || event.target===owner)
    .slice(-12)
    .map(event=>event.summary);
}

function renderStoryProgress(items,currentIndex,currentItem){
  const box=document.getElementById('storyProgress');
  if(!box)return;

  box.innerHTML=items.map((item,index)=>{
    const state =
      index<currentIndex ? 'done' :
      index===currentIndex ? `current ${currentItem.type}` :
      '';

    const style =
      index===currentIndex && currentItem.type==='video'
        ? `style="--story-duration:${Math.max(1,(currentItem.duration||6000)/1000)}s"`
        : '';

    return `<div class="story-progress-segment ${state}" ${style}><div class="story-progress-fill"></div></div>`;
  }).join('');
}

function renderStoryViewer(){
  const owner=storyViewerState.owner;
  const items=storyViewerData[owner] || [{id:'story_unknown',type:'image',time:'刚刚',description:'Story 内容占位'}];
  storyViewerState.index=Math.max(0,Math.min(storyViewerState.index,items.length-1));
  const item=items[storyViewerState.index];

  const storyOwnerProfile=owner==='user'
    ? {initial:(userProfileState?.displayName||'U').slice(0,1),name:userProfileState?.displayName||'你的快拍',avatarUrl:getUserAvatarUrl?.()||''}
    : (typeof getDMProfile==='function'?getDMProfile(owner):null);
  const profileInitial=storyOwnerProfile?.initial || String(owner||'?').slice(0,1).toUpperCase();

  document.getElementById('routeStoryOwner').textContent=owner==='user'?'你的快拍':(storyOwnerProfile?.name||owner);
  const routeStoryAvatar=document.getElementById('routeStoryAvatar');
  routeStoryAvatar.textContent=storyOwnerProfile?.avatarUrl?'':profileInitial;
  routeStoryAvatar.style.backgroundImage=storyOwnerProfile?.avatarUrl?`url("${storyOwnerProfile.avatarUrl}")`:'';
  routeStoryAvatar.style.backgroundSize='cover';
  routeStoryAvatar.style.backgroundPosition='center';
  document.getElementById('routeStoryTime').textContent=item.time||'刚刚';

  const storyMediaHTML =
    item.type==='image' && item.mediaUrl
      ? `<img draggable="false" src="${escapeDMCardHTML(item.mediaUrl)}" alt="Story 图片">`
      : '';

  const storyTextEl=document.getElementById('routeStoryText');
  storyTextEl.classList.toggle('has-media',!!storyMediaHTML);
  storyTextEl.innerHTML = storyMediaHTML
    ? storyMediaHTML
    : `<strong>${item.type==='video'?'视频 Story':'图片 Story'}</strong>` +
      `${escapeDMCardHTML(storyDisplayDescription(item) || (item.type==='image' ? '图片内容将按需识图并缓存摘要' : ''))}` +
      `<small>${storyViewerState.index+1} / ${items.length}${item.type==='video'?' · 播放进度会自动走':' · 图片按当前张数定位进度格'}</small>`;

  const own = owner==='user';
  document.getElementById('storyBottom')?.classList.toggle('own',own);
  document.getElementById('storyCaptionShell')?.classList.toggle('own',own);

  const batchState=getStoryBatchState(owner);
  const captionEl=document.getElementById('storyCaptionText');
  if(captionEl){
    captionEl.textContent=batchState.caption || '';
    captionEl.style.display=batchState.caption ? 'block' : 'none';
  }

  const heartBtn=document.getElementById('storyHeartBtn');
  const heartImg=document.getElementById('storyHeartImg');
  if(heartBtn && heartImg){
    if(own){
      heartBtn.classList.remove('liked');
      heartImg.src='https://imgbed.heliar.top/i/pbz63LCde-0-ZoaE_空心_pixian_ai.webp';
      heartBtn.setAttribute('aria-label','查看谁对我的快拍表态');
    }else{
      heartBtn.classList.toggle('liked',!!batchState.liked);
      heartImg.src=batchState.liked
        ? 'https://imgbed.heliar.top/i/kNrpXmT8iNdggBzr_红心_191553_副本_pixian_ai.webp'
        : 'https://imgbed.heliar.top/i/pbz63LCde-0-ZoaE_空心_pixian_ai.webp';
      heartBtn.setAttribute('aria-label','喜欢这组 Story');
    }
  }

  const editBtn=document.getElementById('storyEditBtn');
  if(editBtn){
    editBtn.dataset.storyMode='edit-story';
    editBtn.setAttribute('aria-label','编辑这组 Story');
  }
  const emojiLauncher=document.getElementById('storyEmojiLauncher');
  if(emojiLauncher)emojiLauncher.style.display=own?'none':'';
  const deleteBtn=document.getElementById('storyDeleteBtn');
  if(deleteBtn)deleteBtn.setAttribute('aria-label',own?'删除我的 Story':'删除这组 Story');

  renderStoryProgress(items,storyViewerState.index,item);
}

function openStory(owner){
  if(owner!=='user' && !canCurrentUserViewStory(owner))return;
  const items=storyViewerData[owner] || [];
  if(owner==='user' && !items.length){
    resetStoryCreateDraft();
    showRoute('routeStoryCreate');
    return;
  }
  if(!items.length)return;
  if(owner==='user')settleStoryAudiencePlanNow({render:false});
  markStorySeen(owner);
  storyViewerState.owner=owner;
  storyViewerState.index=0;
  renderStoryViewer();
  showRoute('routeStory');
}

function stepStory(direction){
  const owner=storyViewerState.owner;
  const items=storyViewerData[owner] || [];
  if(!items.length)return;

  const next=storyViewerState.index+direction;
  if(next<0){
    storyViewerState.index=0;
    renderStoryViewer();
    return;
  }
  if(next>=items.length){
    backRoute();
    return;
  }
  storyViewerState.index=next;
  renderStoryViewer();
}


function getOwnStoryReactionCandidates(){
  return (typeof getRelationshipActors==='function'?getRelationshipActors():[])
    .filter(actor=>actor&&actor.id!=='user'&&['roche','npc'].includes(actor.kind))
    .map(actor=>{
      const actorId=normalizeSocialActorId(actor.id);
      return {
        actorId,
        initial:actor.initial || String(actor.name||actor.handle||actorId).slice(0,1).toUpperCase(),
        name:actor.name||actor.handle||actorId,
        handle:actor.handle||actorId,
        type:actor.kind==='roche'?'角色':'社交联系人',
        actorLooksAtUser:typeof getDirectedRelationship==='function'?(getDirectedRelationship(actorId,'user')?.label||''):'',
        userLooksAtActor:typeof getDirectedRelationship==='function'?(getDirectedRelationship('user',actorId)?.label||''):'',
        recentKnowledge:typeof getActorKnowledgeContext==='function'?getActorKnowledgeContext(actorId,6):[]
      };
    });
}

function getStoryAudiencePlan(owner='user'){
  return storyReactionPlans[owner]||null;
}
function stableStoryAudienceMinute(actorId,batchId){
  const seed=String(actorId||'')+'|'+String(batchId||'');
  let h=2166136261;
  for(let i=0;i<seed.length;i++){h^=seed.charCodeAt(i);h=Math.imul(h,16777619);}
  return 5+(Math.abs(h)%(24*60-15));
}
function resetUserStoryAudiencePlan(reason='publish'){
  const owner='user';
  const batchId=getCurrentLiveStoryBatchId(owner);
  const state=getStoryBatchState(owner);
  state.reactionPlanStatus='pending';
  state.plannedReactions=[];
  state.audienceReactions=[];
  storyReactionPlans[owner]={
    batchId,
    status:'pending',
    reason,
    plannedAt:0,
    lastError:'',
    rows:[]
  };
  persistNiniStoryLifecycle();
  return storyReactionPlans[owner];
}
function buildUserStoryAudiencePlanPayload(){
  const meta=storyLiveMeta.user;
  const batchId=getCurrentLiveStoryBatchId('user');
  if(!meta||!batchId||(storyViewerData.user||[]).length===0)return null;
  const state=getStoryBatchState('user');
  const candidates=getOwnStoryReactionCandidates();
  return {
    batchId,
    story:{
      ownerId:'user',
      caption:state.caption||'',
      items:resolveStoryMediaContextForModel('user').map(row=>({
        id:row.id,
        type:row.type,
        text:row.text||'',
        source:row.source||'none'
      })),
      publishedAt:Number(meta.publishedAt)||Date.now(),
      expiresAt:Number(meta.expiresAt)||((Number(meta.publishedAt)||Date.now())+NINI_STORY_LIVE_TTL_MS)
    },
    candidates
  };
}
function getStoryAudiencePiggybackPayload(){
  const plan=getStoryAudiencePlan('user');
  const payload=buildUserStoryAudiencePlanPayload();
  if(!payload||!plan||!['pending','failed'].includes(plan.status)||plan.batchId!==payload.batchId)return null;
  return payload;
}
function normalizeStoryAudiencePlanRows(rawData,payload){
  const candidates=payload?.candidates||[];
  const byId=new Map(candidates.map(row=>[normalizeSocialActorId(row.actorId),row]));
  const rawRows=Array.isArray(rawData)
    ? rawData
    : Array.isArray(rawData?.audiencePlan)?rawData.audiencePlan
    : Array.isArray(rawData?.storyAudiencePlan)?rawData.storyAudiencePlan
    : Array.isArray(rawData?.decisions)?rawData.decisions
    : Array.isArray(rawData?.plan)?rawData.plan
    : Array.isArray(rawData?.items)?rawData.items
    : [];
  const rawByActor=new Map();
  rawRows.forEach(row=>{
    const rawId=normalizeSocialActorId(row?.actorId||row?.id||row?.handle||row?.actor);
    const actor=byId.get(rawId) || candidates.find(c=>[c.handle,c.name].includes(String(row?.handle||row?.name||row?.actor||'')));
    if(actor)rawByActor.set(normalizeSocialActorId(actor.actorId),row||{});
  });
  const publishedAt=Number(payload?.story?.publishedAt)||Date.now();
  const expiresAt=Number(payload?.story?.expiresAt)||publishedAt+NINI_STORY_LIVE_TTL_MS;
  return candidates.map(actor=>{
    const actorId=normalizeSocialActorId(actor.actorId);
    const raw=rawByActor.get(actorId)||{};
    const explicitView=raw.willView ?? raw.view ?? raw.shouldView ?? raw.watched ?? raw.seen;
    const willView=explicitView===true || explicitView===1 || String(explicitView).toLowerCase()==='true' || String(explicitView).toLowerCase()==='yes';
    const rawViewMinute=Number(raw.viewAfterMinutes ?? raw.viewDelayMinutes ?? raw.viewMinute ?? raw.minutesAfterPublish);
    const viewMinute=Number.isFinite(rawViewMinute)
      ? Math.max(0,Math.min(1439,Math.round(rawViewMinute)))
      : stableStoryAudienceMinute(actorId,payload.batchId);
    const reactionRaw=raw.reactionCode||raw.code||raw.emoji||'';
    const descriptor=reactionRaw?getINSReactionDescriptor(reactionRaw):null;
    const explicitReact=raw.react ?? raw.willReact ?? raw.shouldReact;
    const react=willView && (
      explicitReact===true || explicitReact===1 || String(explicitReact).toLowerCase()==='true' ||
      (!!descriptor && explicitReact!==false && explicitReact!==0 && String(explicitReact).toLowerCase()!=='false')
    );
    const rawReactionDelay=Number(raw.reactionAfterMinutes ?? raw.reactionDelayMinutes ?? raw.replyAfterMinutes);
    const reactionDelay=Number.isFinite(rawReactionDelay)?Math.max(0,Math.min(90,Math.round(rawReactionDelay))):0;
    const viewAt=Math.min(expiresAt-60000,publishedAt+viewMinute*60*1000);
    const reactionAt=react?Math.min(expiresAt-15000,viewAt+reactionDelay*60*1000):0;
    return {
      actorId,
      initial:actor.initial||'?',
      name:actor.name||actor.handle||actorId,
      handle:actor.handle||actorId,
      type:actor.type||'角色',
      willView,
      viewAt,
      viewRevealed:false,
      viewedAt:0,
      react,
      reactionCode:react?descriptor.code:'',
      emoji:react?descriptor.emoji:'',
      attitudeSummary:react?String(raw.attitudeSummary||raw.attitude||descriptor.promptMeaning).trim():'',
      reactionAt,
      reactionRevealed:false,
      reactedAt:0
    };
  });
}
function applyStoryAudiencePlanResponse(rawData,{source='dedicated'}={}){
  const payload=buildUserStoryAudiencePlanPayload();
  if(!payload)return false;
  const current=storyReactionPlans.user;
  if(current&&current.batchId&&current.batchId!==payload.batchId)return false;
  const rows=normalizeStoryAudiencePlanRows(rawData,payload);
  const state=getStoryBatchState('user');
  storyReactionPlans.user={
    batchId:payload.batchId,
    status:'ready',
    reason:source,
    plannedAt:Date.now(),
    lastError:'',
    rows
  };
  state.reactionPlanStatus='ready';
  state.plannedReactions=rows;
  state.audienceReactions=[];
  settleStoryAudiencePlanNow({render:false});
  persistNiniStoryLifecycle();
  renderOwnStoryReactionSheet();
  return true;
}
function applyStoryAudiencePlanFromPiggyback(data){
  const plan=getStoryAudiencePlan('user');
  if(!plan||!['pending','failed'].includes(plan.status))return false;
  const raw=data?.storyAudiencePlan ?? data?.storyAudience?.plan ?? data?.meta?.storyAudiencePlan;
  if(!raw)return false;
  return applyStoryAudiencePlanResponse(raw,{source:'piggyback'});
}
let storyAudiencePlanningPromise=null;
async function ensureStoryAudienceReactionPlan({trigger='manual',silent=false}={}){
  const payload=buildUserStoryAudiencePlanPayload();
  if(!payload)return {ok:false,reason:'no_live_user_story'};
  const candidates=payload.candidates||[];
  if(!candidates.length){
    storyReactionPlans.user={batchId:payload.batchId,status:'ready',reason:'no_candidates',plannedAt:Date.now(),lastError:'',rows:[]};
    const state=getStoryBatchState('user');
    state.reactionPlanStatus='ready';
    state.plannedReactions=[];
    state.audienceReactions=[];
    persistNiniStoryLifecycle();
    renderOwnStoryReactionSheet();
    return {ok:true,empty:true};
  }
  const existing=storyReactionPlans.user;
  if(existing?.batchId===payload.batchId&&existing.status==='ready')return {ok:true,already:true};
  if(storyAudiencePlanningPromise)return storyAudiencePlanningPromise;
  storyReactionPlans.user={
    batchId:payload.batchId,
    status:'planning',
    reason:trigger,
    plannedAt:0,
    lastError:'',
    rows:Array.isArray(existing?.rows)?existing.rows:[]
  };
  getStoryBatchState('user').reactionPlanStatus='planning';
  persistNiniStoryLifecycle();
  renderOwnStoryReactionSheet();
  storyAudiencePlanningPromise=(async()=>{
    try{
      const result=await callNiniINSAI(INS_AI_TASKS.STORY_AUDIENCE_PLAN,{
        actorIds:candidates.map(row=>row.actorId),
        ownerId:'user',
        surface:'story_audience_plan',
        content:payload,
        prompt:`Plan this User Story's entire 24-hour audience behavior in ONE call. Return one decision for every supplied candidate actorId. Each candidate may never view, may view without reacting, or may view and react later. Do not include strangers and never include User as an audience actor. Spread view times naturally across 0..1439 minutes after publish. Do not force everyone to view or react. If reacting, use only one allowed semantic reactionCode and an optional short attitudeSummary. Return JSON only.`,
        extra:{batchId:payload.batchId,candidateCount:candidates.length,oneCallForWhole24Hours:true,localTimedRevealAfterPlanning:true}
      });
      if(!result.ok)throw new Error(result.reason||'Story 反应规划失败');
      const raw=result.data?.storyAudiencePlan ?? result.data?.audiencePlan ?? result.data?.plan ?? result.data?.decisions ?? result.data;
      if(!applyStoryAudiencePlanResponse(raw,{source:trigger}))throw new Error('Story 反应规划格式无法识别');
      return {ok:true};
    }catch(error){
      const plan=storyReactionPlans.user||{};
      if(plan.batchId===payload.batchId){
        plan.status='failed';
        plan.lastError=String(error?.message||error||'unknown');
        storyReactionPlans.user=plan;
        getStoryBatchState('user').reactionPlanStatus='failed';
        persistNiniStoryLifecycle();
        renderOwnStoryReactionSheet();
      }
      if(!silent)console.warn('[INS] Story audience plan failed',error);
      return {ok:false,reason:error?.message||String(error)};
    }finally{
      storyAudiencePlanningPromise=null;
    }
  })();
  return storyAudiencePlanningPromise;
}
function recordStoryAudienceReveal(row,kind){
  if(typeof recordSocialEvent!=='function'||!row?.actorId)return;
  const contentId=currentStoryMemoryContentId('user');
  if(kind==='view'){
    recordSocialEvent({
      type:'story_view',
      actorId:row.actorId,
      targetIds:['user'],
      contentId,
      contentType:'story',
      contentOwnerId:'user',
      visibility:'private',
      dedupeKey:`story_view:${row.actorId}:user:${contentId}`,
      summary:`${socialActorName(row.actorId)}看了 ${socialActorName('user')} 的 Story。`
    },{knownActorIds:[row.actorId,'user'],learnedVia:'story_audience_plan'});
  }else if(kind==='reaction'&&row.react){
    recordSocialEvent({
      type:'story_reaction',
      actorId:row.actorId,
      targetIds:['user'],
      contentId,
      contentType:'story',
      contentOwnerId:'user',
      emoji:row.emoji,
      attitude:row.attitudeSummary||'',
      visibility:'private',
      payload:{reactionCode:row.reactionCode||''},
      dedupeKey:`story_reaction:${row.actorId}:user:${contentId}:${row.reactionCode||row.emoji}`,
      summary:`${socialActorName(row.actorId)}对 ${socialActorName('user')} 的 Story 使用 ${row.emoji}，表示：${row.attitudeSummary||getINSReactionDescriptor(row.reactionCode).promptMeaning}。`
    },{knownActorIds:[row.actorId,'user'],learnedVia:'story_audience_plan'});
  }
}
function settleStoryAudiencePlanNow({render=true}={}){
  const plan=storyReactionPlans.user;
  const meta=storyLiveMeta.user;
  if(!plan||plan.status!=='ready'||!meta||plan.batchId!==meta.batchId)return false;
  const now=Date.now();
  let changed=false;
  (plan.rows||[]).forEach(row=>{
    if(row.willView&&!row.viewRevealed&&Number(row.viewAt)<=now){
      row.viewRevealed=true;
      row.viewedAt=Number(row.viewAt)||now;
      recordStoryAudienceReveal(row,'view');
      changed=true;
    }
    if(row.react&&row.viewRevealed&&!row.reactionRevealed&&Number(row.reactionAt)<=now){
      row.reactionRevealed=true;
      row.reactedAt=Number(row.reactionAt)||now;
      recordStoryAudienceReveal(row,'reaction');
      changed=true;
    }
  });
  if(changed){
    const state=getStoryBatchState('user');
    state.plannedReactions=plan.rows||[];
    state.audienceReactions=(plan.rows||[]).filter(row=>row.viewRevealed).map(row=>({
      actorId:row.actorId,
      initial:row.initial,
      handle:row.handle,
      type:row.type,
      emoji:row.reactionRevealed?row.emoji:'👁️',
      copy:row.reactionRevealed?`已查看 · ${getINSReactionDescriptor(row.reactionCode).label}`:'已查看'
    }));
    persistNiniStoryLifecycle();
  }
  if(render)renderOwnStoryReactionSheet();
  return changed;
}
function refreshOwnStoryReactions(){
  if(!(storyViewerData.user||[]).length)return;
  settleStoryAudiencePlanNow({render:true});
}

function renderOwnStoryReactionSheet(){
  const plan=getStoryAudiencePlan('user');
  const state=getStoryBatchState('user');
  const rows=plan?.status==='ready'
    ? (plan.rows||[]).filter(row=>row.viewRevealed).map(row=>({
        ...row,
        copy:row.reactionRevealed?`已查看 · ${getINSReactionDescriptor(row.reactionCode).label}`:'已查看',
        displayEmoji:row.reactionRevealed?row.emoji:'👁️'
      }))
    : (state.audienceReactions||[]).map(row=>({...row,displayEmoji:row.emoji||'👁️'}));
  const list=document.getElementById('storyOwnReactionList');
  const empty=document.getElementById('storyOwnReactionEmpty');
  const status=document.getElementById('storyOwnReactionPlanStatus');
  const button=document.getElementById('storyOwnReactionPlanBtn');
  if(!list||!empty)return;
  list.innerHTML=rows.map(row=>`
    <div class="story-own-reaction-row">
      <div class="story-own-reaction-avatar">${escapeDMCardHTML(row.initial||'?')}</div>
      <div class="story-own-reaction-main">
        <strong>@${escapeDMCardHTML(row.handle||row.actorId||'unknown')}</strong>
        <span>${escapeDMCardHTML(row.type||'角色')} · ${escapeDMCardHTML(row.copy||'已查看')}</span>
      </div>
      <div class="story-own-reaction-emoji">${escapeDMCardHTML(row.displayEmoji||row.emoji||'👁️')}</div>
    </div>
  `).join('');
  empty.style.display=rows.length?'none':'block';
  const planStatus=plan?.status||state.reactionPlanStatus||'idle';
  if(status){
    if(planStatus==='planning')status.textContent='正在一次性规划这组 Story 的 24 小时浏览与表态…';
    else if(planStatus==='pending')status.textContent='当前 Story 等待规划。规划完成后，浏览与表态会按本地时间逐步出现，不会重复调用 AI。';
    else if(planStatus==='failed')status.textContent='上次规划失败，可手动重试。';
    else if(planStatus==='ready'){
      const planned=(plan?.rows||[]);
      const viewers=planned.filter(row=>row.willView).length;
      const reacted=planned.filter(row=>row.reactionRevealed).length;
      status.textContent=planned.length
        ? `24 小时计划已生成 · ${rows.length}/${viewers} 已浏览 · ${reacted} 已表态`
        : '当前没有已绑定角色 / 社交联系人可以参与这组 Story。';
    }else if(planStatus==='expired')status.textContent='这组 Story 已结束 24 小时展示周期。';
    else status.textContent='发布 User Story 后可规划 24 小时内的浏览与表态。';
  }
  if(button){
    button.disabled=planStatus==='planning';
    button.textContent=planStatus==='planning'?'规划中…':planStatus==='failed'?'重新规划 24 小时浏览与表态':'规划 24 小时浏览与表态';
    button.style.display=(planStatus==='ready'||planStatus==='expired')?'none':'';
  }
}

function openOwnStoryReactions(){
  settleStoryAudiencePlanNow({render:false});
  renderOwnStoryReactionSheet();
  openStorySheet('storyOwnReactionSheet');
}

const storyCreateDraft={
  items:[],
  caption:''
};

function resetStoryCreateDraft(){
  storyCreateDraft.items=[];
  storyCreateDraft.caption='';
  const caption=document.getElementById('storyCreateCaption');
  if(caption)caption.value='';
  renderStoryCreateDraft();
}

function renderStoryCreateDraft(){
  const box=document.getElementById('storyCreateItems');
  const count=document.getElementById('storyCreateCount');
  if(count)count.textContent=`${storyCreateDraft.items.length} / 4`;
  if(!box)return;

  box.innerHTML=storyCreateDraft.items.map((item,index)=>{
    const isVideo=item.type==='video';
    const preview = !isVideo && item.mediaUrl
      ? `<img src="${escapeDMCardHTML(item.mediaUrl)}" alt="Story 图片预览">`
      : !isVideo && item.generatedRequested && !item.mediaUrl
        ? `已选择「按描述生图」<br>${escapeDMCardHTML(item.description||'请继续填写图片描述')}`
        : !isVideo
          ? ''
          : '';

    return `
      <div class="story-create-item" data-story-create-index="${index}">
        <div class="story-create-item-head">
          <span class="story-create-type">${isVideo?'视频':'图片'}</span>
          <strong>第 ${index+1} 条</strong>
          <button class="story-create-remove" data-story-create-remove="${index}" type="button">×</button>
        </div>

        ${isVideo ? `` : `
          <div class="story-create-media-tools">
            <div class="story-create-media-row">
              <label class="story-create-file-btn">
                上传图片
                <input hidden type="file" accept="image/*" data-story-create-file="${index}">
              </label>
              <input class="story-create-url" type="url" data-story-create-url="${index}"
                placeholder="或粘贴图片 URL"
                value="${escapeDMCardHTML(item.mediaSource==='url' ? (item.mediaUrl||'') : '')}">
              <button class="story-create-generate-btn" data-story-create-generate="${index}" type="button">生图</button>
            </div>

            ${preview?`<div class="story-create-preview">${preview}</div>`:'' }
          </div>
        `}

        ${isVideo ? `
          <div class="story-create-desc-hint"><strong>视频必须有描述。</strong></div>
        ` : `
          <div class="story-create-desc-hint"><strong>图片描述可选。</strong>本描述可用于生图、帮助模型理解图片内容，如模型支持识图可同时识图并缓存摘要。</div>
        `}

        <textarea class="story-create-textarea"
          data-story-create-desc="${index}"
          placeholder="${isVideo?'描述这段视频内容……':'可选：你想怎么描述这张图片……'}">${escapeDMCardHTML(item.description||'')}</textarea>
      </div>
    `;
  }).join('');
}

function openStoryCreateChoice(){
  if(storyCreateDraft.items.length>=4){
    alert('一组 Story 最多 4 条');
    return;
  }
  document.getElementById('storyCreateDim')?.classList.add('show');
  document.getElementById('storyCreateChoiceSheet')?.classList.add('show');
}
function closeStoryCreateChoice(){
  document.getElementById('storyCreateDim')?.classList.remove('show');
  document.getElementById('storyCreateChoiceSheet')?.classList.remove('show');
}
function addStoryCreateItem(type){
  if(storyCreateDraft.items.length>=4)return;
  storyCreateDraft.items.push({
    id:'story_user_'+Date.now()+'_'+storyCreateDraft.items.length,
    type:type==='video'?'video':'image',
    time:'刚刚',
    duration:type==='video'?6000:undefined,
    description:'',
    mediaUrl:'',
    mediaSource:type==='video'?'description':'',
    fileName:'',
    generatedRequested:false,
    visionSummary:'',
    visionStatus:'idle'
  });
  renderStoryCreateDraft();
  closeStoryCreateChoice();
}

/* Media understanding policy:
   1) Manual description wins and skips vision.
   2) If image description is empty, use cached visionSummary.
   3) If both are empty, mark a one-time vision analysis as needed.
   Formal Roche integration should write a short summary back to visionSummary,
   then reuse it instead of re-sending the image on every model call. */
function getImageSemanticContext(item){
  if(!item || item.type!=='image') return {source:'none',text:''};

  const manual=(item.description||'').trim();
  if(manual) return {source:'manual',text:manual};

  const cached=(item.visionSummary||'').trim();
  if(cached) return {source:'vision_cache',text:cached};

  if(item.mediaUrl){
    return {
      source:'vision_needed',
      text:'',
      imageUrl:item.mediaUrl,
      instruction:'仅生成简短客观图片摘要，用于后续社交上下文缓存，不需要重复分析。'
    };
  }

  return {source:'none',text:''};
}
function cacheImageVisionSummary(item,summary){
  if(!item || item.type!=='image')return;
  item.visionSummary=(summary||'').trim();
  item.visionStatus=item.visionSummary?'ready':'idle';
}
function resolveStoryMediaContextForModel(owner){
  return (storyViewerData[owner]||[]).map(item=>{
    if(item.type==='video'){
      return {id:item.id,type:'video',text:(item.description||'').trim(),source:'manual'};
    }
    const semantic=getImageSemanticContext(item);
    return {id:item.id,type:'image',...semantic};
  });
}

function publishUserStory(){
  document.querySelectorAll('[data-story-create-desc]').forEach(el=>{
    const index=Number(el.dataset.storyCreateDesc||0);
    if(storyCreateDraft.items[index]) storyCreateDraft.items[index].description=el.value.trim();
  });

  document.querySelectorAll('[data-story-create-url]').forEach(el=>{
    const index=Number(el.dataset.storyCreateUrl||0);
    const item=storyCreateDraft.items[index];
    if(!item || item.type==='video')return;
    const url=el.value.trim();
    if(url){
      item.mediaUrl=url;
      item.mediaSource='url';
      item.generatedRequested=false;
      item.fileName='';
    }
  });

  const caption=document.getElementById('storyCreateCaption')?.value.trim()||'';

  if(!storyCreateDraft.items.length){
    alert('先添加至少 1 个图片或视频');
    return;
  }

  for(let index=0; index<storyCreateDraft.items.length; index++){
    const item=storyCreateDraft.items[index];

    if(item.type==='video' && !item.description?.trim()){
      alert(`第 ${index+1} 条视频必须填写内容描述`);
      return;
    }

    if(item.type==='image' && !item.mediaUrl && !item.generatedRequested){
      alert(`第 ${index+1} 张图片请选择：上传图片、图片 URL，或者生图`);
      return;
    }

    if(item.type==='image' && item.generatedRequested && !item.description?.trim()){
      alert(`第 ${index+1} 张图片选择了生图，请先写生图描述`);
      return;
    }
  }

  storyViewerData.user=storyCreateDraft.items.slice(0,4).map((item,index)=>({
    ...item,
    id:item.id || ('story_user_'+Date.now()+'_'+index),
    description:(item.description||'').trim(),
    liked:false,
    linkedAlbumId:'',
    generated:!!item.generatedRequested,
    visionSummary:item.visionSummary||'',
    visionStatus:item.visionSummary?'ready':'idle'
  }));

  const batchState=getStoryBatchState('user');
  batchState.caption=caption;
  batchState.liked=false;
  batchState.audienceReactions=[];

  // One publish = one whole Story batch in archive; live copy expires separately.
  const publishedStoryRecord=archivePublishedStoryBatch('user', storyViewerData.user, caption);
  markStoryBatchLive('user',publishedStoryRecord);
  resetUserStoryAudiencePlan('publish');

  storyViewerState.owner='user';
  storyViewerState.index=0;

  // Publishing is complete: return directly to Feed.
  hideRoute('routeStoryCreate');
  hideRoute('routeStory');
  routeStack.length=0;
  state.active='home';
  renderNav();
  queueMicrotask(()=>void ensureStoryAudienceReactionPlan({trigger:'publish',silent:true}));
}


function getStoryShareRecipients(){
  return getUnifiedDMShareRecipients()
    .filter(row=>(row.id||row.handle)!==storyViewerState.owner);
}
function renderStoryShareSheet(){
  const box=document.getElementById('storyShareList');
  if(!box)return;
  const rows=getStoryShareRecipients();
  box.innerHTML=rows.length ? rows.map(row=>{
    const recipient=row.id||row.handle;
    const allowed=canStoryRecipientReceiveFromOwner(storyViewerState.owner,recipient);
    return `
    <div class="story-share-row">
      <div class="story-share-avatar">${escapeDMCardHTML(row.initial)}</div>
      <div class="story-share-main">
        <strong>${escapeDMCardHTML(row.name)}</strong>
        <span>@${escapeDMCardHTML(row.handle)}${allowed?'':' · 与原作者无直接关系'}</span>
      </div>
      <button class="story-share-send" data-story-share-recipient="${escapeDMCardHTML(recipient)}" type="button" ${allowed?'':'disabled aria-disabled="true"'}>${allowed?'发送':'不可发送'}</button>
    </div>`;
  }).join('') : '<div class="story-emoji-copy">当前没有可分享的 DM 联系人。</div>';
}
function openStoryShareSheet(){
  renderStoryShareSheet();
  openStorySheet('storyShareSheet');
}

/* This is deterministic metadata only, not an AI Story-summary call.
   Full batch summarization for DM/Highlight is intentionally deferred. */
function compactStoryMediaUnderstanding(owner){
  const rows=resolveStoryMediaContextForModel(owner);
  const manual=rows.filter(x=>x.source==='manual').length;
  const cached=rows.filter(x=>x.source==='vision_cache').length;
  const pending=rows.filter(x=>x.source==='vision_needed').length;
  const parts=[];
  if(manual)parts.push(`${manual}条人工描述`);
  if(cached)parts.push(`${cached}条识图缓存`);
  if(pending)parts.push(`${pending}张待按需识图`);
  return parts.join(' · ');
}
function summarizeStoryBatchForShare(owner){
  const items=storyViewerData[owner]||[];
  const imageCount=items.filter(x=>x.type==='image').length;
  const videoCount=items.filter(x=>x.type==='video').length;
  const bits=[];
  if(imageCount)bits.push(`${imageCount}张图片`);
  if(videoCount)bits.push(`${videoCount}个视频`);
  return `${items.length}条 · ${bits.join(' + ') || 'Story'}`;
}
function sendStoryBatchToDM(recipient){
  const owner=storyViewerState.owner;
  if(!owner || !recipient || !canStoryRecipientReceiveFromOwner(owner,recipient))return;
  const state=getStoryBatchState(owner);
  if(!dmSharedCards[recipient])dmSharedCards[recipient]=[];
  dmSharedCards[recipient].push(makeDMContextCard('story_share',{
    author:owner==='user'?userProfileState.id:owner,
    caption:state.caption||'',
    mediaDescription:summarizeStoryBatchForShare(owner),
    contextSummary:`user 分享了 @${owner==='user'?userProfileState.id:owner} 的一组 Story。文案：${compactDMContextText(state.caption||'无',90)}。内容：${summarizeStoryBatchForShare(owner)}。`
  }));
  scheduleINSDMHistoryPersistence();
  updateDMListPreview(recipient,'分享了一组 Story');

  if(typeof recordSocialEvent==='function'){
    const recipientId=normalizeSocialActorId(recipient);
    const ownerId=normalizeSocialActorId(owner);
    recordSocialEvent({
      type:'dm_share_content',
      actorId:'user',
      targetIds:[recipientId],
      contentId:currentStoryMemoryContentId(owner),
      contentType:'story',
      contentOwnerId:ownerId,
      visibility:'private',
      payload:{
        sharedType:'Story',
        sharedOwnerId:ownerId,
        sharedOwnerLabel:owner==='user'?(userProfileState.displayName||userProfileState.id):owner,
        caption:state.caption||'',
        sharedSummary:summarizeStoryBatchForShare(owner)
      },
      summary:`${socialActorName('user')}在 DM 给 ${socialActorName(recipientId)} 分享了 ${socialActorName(ownerId)} 的 Story。`
    },{knownActorIds:['user',recipientId],learnedVia:'dm_share'});
    if(isFormalSocialActor(recipientId))exposePublicContentToActor(recipientId,currentStoryMemoryContentId(owner),'dm_share');
  }

  closeStorySheets();
  const toast=document.getElementById('storyReactionToast');
  if(toast){
    toast.textContent='已分享到 DM';
    toast.classList.add('show');
    clearTimeout(window.__storyShareToastTimer);
    window.__storyShareToastTimer=setTimeout(()=>toast.classList.remove('show'),1000);
  }
}

function sendStoryReaction(emoji){
  const owner=storyViewerState.owner;
  if(!owner || owner==='user')return;

  const items=storyViewerData[owner] || [];
  if(!items.length)return;

  const reaction=normalizeINSReactionDecision({emoji});
  const meaning=reaction.attitudeSummary;
  const batchSummary=getStoryBatchCompactSummary(owner);
  const event={
    type:'story_reaction',
    actor:'user',
    target:owner,
    storyBatchSize:items.length,
    reactionCode:reaction.reactionCode,
    emoji:reaction.emoji,
    attitude:meaning,
    createdAt:Date.now(),
    summary:`user 对 ${owner} 这一组 Story（${batchSummary}）使用 ${emoji}，表示：${meaning}。`
  };
  storyReactionEvents.push(event);

  if(typeof recordSocialEvent==='function'){
    const ownerId=normalizeSocialActorId(owner);
    const contentId=currentStoryMemoryContentId(owner);
    recordSocialEvent({
      type:'story_reaction',
      actorId:'user',
      targetIds:[ownerId],
      contentId,
      contentType:'story',
      contentOwnerId:ownerId,
      emoji:reaction.emoji,
      attitude:meaning,
      visibility:'private',
      payload:{reactionCode:reaction.reactionCode},
      summary:`${socialActorName('user')}对 ${socialActorName(ownerId)} 的 Story 使用 ${reaction.emoji}，表示：${meaning}。`
    },{knownActorIds:['user',ownerId],learnedVia:'story_reaction'});
  }

  if(!dmSharedCards[owner]) dmSharedCards[owner]=[];
  dmSharedCards[owner].push(makeDMContextCard('story_reaction',{
    author:owner,
    emoji:reaction.emoji,
    attitude:meaning,
    storyId:'story_batch:'+owner,
    storyDescription:batchSummary,
    contextSummary:event.summary
  }));
  scheduleINSDMHistoryPersistence();
  if(currentDMChat===owner) renderDMChatStream(owner);

  const toast=document.getElementById('storyReactionToast');
  if(toast){
    toast.textContent=`${reaction.emoji} · ${reaction.displayLabel}`;
    toast.classList.add('show');
    clearTimeout(window.__storyReactionToastTimer);
    window.__storyReactionToastTimer=setTimeout(()=>toast.classList.remove('show'),1100);
  }
}


const charProfileState = {activeTab:'posts',editingHighlightId:null,profiles:{},boardBatches:{},boardIndex:{}};


/* ===== v1.15.15 Single Actor Registry + Single Directed Relationship Graph =====
   Actor source of truth:
   - user: userProfileState
   - Roche characters / fixed NPCs: dmProfiles (DM is the import/promotion gate)
   Temporary strangers are intentionally excluded until promoted to fixed NPC.
*/
const relationshipGraphState = {centerId:'user',edges:[]};

function getRelationshipActors(){
  const actors=[{
    id:'user',
    kind:'user',
    profileType:'user',
    initial:(userProfileState.displayName||userProfileState.id||'U').slice(0,1).toUpperCase(),
    name:userProfileState.displayName||userProfileState.id||'User',
    handle:userProfileState.id||'user',
    avatarUrl:getUserAvatarUrl()
  }];

  Object.entries(dmProfiles||{}).forEach(([key,profile])=>{
    if(!profile)return;
    const type=profile.profileType==='roche'?'roche':'npc';
    const id=String(profile.rocheId||key||profile.handle||'');
    if(!id)return;
    actors.push({
      id,
      kind:type,
      profileType:type,
      initial:profile.initial || String(profile.name||profile.handle||id).slice(0,1).toUpperCase(),
      name:profile.name || profile.handle || id,
      handle:profile.handle || id,
      avatarUrl:profile.avatarUrl||''
    });
  });

  return actors;
}

function getRelationshipActor(id){
  const raw=String(id??'').replace(/^@/,'');
  return getRelationshipActors().find(actor=>
    actor.id===raw ||
    actor.handle===raw ||
    actor.name===raw
  ) || null;
}

function relationshipTypeLabel(actor){
  if(!actor)return '';
  if(actor.kind==='user')return 'User';
  if(actor.kind==='roche')return '角色';
  return '社交联系人';
}

function getDirectedRelationship(from,to){
  return relationshipGraphState.edges.find(edge=>edge.from===from && edge.to===to) || null;
}

function upsertDirectedRelationship(from,to,label){
  const clean=(label||'').trim();
  const index=relationshipGraphState.edges.findIndex(edge=>edge.from===from && edge.to===to);

  if(!clean){
    if(index>=0)relationshipGraphState.edges.splice(index,1);
    return;
  }

  const next={from,to,label:clean,source:'manual',locked:true};
  if(index>=0)relationshipGraphState.edges[index]=next;
  else relationshipGraphState.edges.push(next);
}

function deleteRelationshipPair(a,b){
  relationshipGraphState.edges=relationshipGraphState.edges.filter(edge=>{
    return !((edge.from===a && edge.to===b) || (edge.from===b && edge.to===a));
  });
}

function countActorRelations(id){
  const ids=new Set();
  relationshipGraphState.edges.forEach(edge=>{
    if(edge.from===id)ids.add(edge.to);
    if(edge.to===id)ids.add(edge.from);
  });
  return ids.size;
}

function getDirectRelationshipPartners(id){
  const ids=new Set();
  relationshipGraphState.edges.forEach(edge=>{
    if(edge.from===id)ids.add(edge.to);
    if(edge.to===id)ids.add(edge.from);
  });
  return [...ids].filter(otherId=>getRelationshipActor(otherId));
}

function prEscape(value){
  return escapeDMCardHTML(String(value??''));
}

function renderPeopleRegistry(){
  const box=document.getElementById('prActorList');
  if(!box)return;
  const actors=getRelationshipActors();

  box.innerHTML=actors.map(actor=>{
    const typeClass=actor.kind==='roche'?'roche':actor.kind==='npc'?'npc':'';
    return `
      <div class="pr-actor-card">
        <div class="pr-avatar ${typeClass}">${prEscape(actor.initial)}</div>
        <div class="pr-actor-main">
          <strong>${prEscape(actor.name)}</strong>
          <span>${actor.id==='user'
            ? `@${prEscape(actor.handle)} · 人物库中心`
            : `@${prEscape(actor.handle)} · ${countActorRelations(actor.id)} 个直接关系`}</span>
        </div>
        <span class="pr-type ${typeClass}">${prEscape(relationshipTypeLabel(actor))}</span>
        <button class="pr-view" data-pr-view="${prEscape(actor.id)}" type="button">看关系</button>
      </div>`;
  }).join('');
}

function renderRelationshipFocus(){
  const focus=document.getElementById('prFocusCard');
  const switcher=document.getElementById('prFocusSwitch');
  const actor=getRelationshipActor(relationshipGraphState.centerId) || getRelationshipActor('user');
  if(!actor || !focus || !switcher)return;

  relationshipGraphState.centerId=actor.id;
  const typeClass=actor.kind==='roche'?'roche':actor.kind==='npc'?'npc':'';
  const partners=getDirectRelationshipPartners(actor.id);

  focus.innerHTML=`
    <div class="pr-focus-head">
      <div class="pr-avatar ${typeClass}">${prEscape(actor.initial)}</div>
      <div class="pr-focus-meta">
        <strong>${prEscape(actor.name)}</strong>
        <span>${prEscape(relationshipTypeLabel(actor))} · @${prEscape(actor.handle)}</span>
      </div>
      <span class="pr-type ${typeClass}">${partners.length} 个直接关系</span>
    </div>
    <div class="pr-focus-note">
      当前只看 ${prEscape(actor.name)} 的一度关系。没有关系边的人不会因为“同在人物库”就被系统当成认识。
    </div>`;

  switcher.innerHTML=getRelationshipActors().map(item=>`
    <button
      class="pr-focus-chip ${item.id===actor.id?'active':''}"
      data-pr-center="${prEscape(item.id)}"
      type="button"
    >${prEscape(item.name)}</button>
  `).join('');
}

function renderRelationshipCards(){
  const box=document.getElementById('prRelationList');
  if(!box)return;

  const center=getRelationshipActor(relationshipGraphState.centerId);
  if(!center){
    box.innerHTML='<div class="pr-empty">当前人物不存在。</div>';
    return;
  }

  const partners=getDirectRelationshipPartners(center.id);
  if(!partners.length){
    box.innerHTML=`
      <div class="pr-empty">
        ${prEscape(center.name)} 还没有建立任何关系边。<br>
        点上面的“建立 / 修改关系”添加；系统不会自动假设他/她认识人物库里的其他人。
      </div>`;
    return;
  }

  box.innerHTML=partners.map(otherId=>{
    const other=getRelationshipActor(otherId);
    const forward=getDirectedRelationship(center.id,otherId);
    const backward=getDirectedRelationship(otherId,center.id);
    const typeClass=other.kind==='roche'?'roche':other.kind==='npc'?'npc':'';
    const locked=!!(forward?.locked || backward?.locked);

    return `
      <div class="pr-rel-card">
        <div class="pr-rel-head">
          <div class="pr-avatar ${typeClass}">${prEscape(other.initial)}</div>
          <div class="pr-rel-head-main">
            <strong>${prEscape(other.name)}</strong>
            <span>${prEscape(relationshipTypeLabel(other))} · @${prEscape(other.handle)}</span>
          </div>
          ${locked?'<span class="pr-lock">手动锁定</span>':''}
        </div>

        <div class="pr-direction">
          <div class="pr-direction-box">
            <b>${prEscape(center.name)} 看 ${prEscape(other.name)}</b>
            <span>${prEscape(forward?.label || '未设定')}</span>
          </div>
          <div class="pr-direction-mid">↔</div>
          <div class="pr-direction-box">
            <b>${prEscape(other.name)} 看 ${prEscape(center.name)}</b>
            <span>${prEscape(backward?.label || '未设定')}</span>
          </div>
        </div>

        <button class="pr-edit" data-pr-edit="${prEscape(other.id)}" type="button">编辑这组关系</button>
      </div>`;
  }).join('');
}

function renderPeopleRelations(){
  const actors=getRelationshipActors();
  if(!actors.some(actor=>actor.id===relationshipGraphState.centerId)){
    relationshipGraphState.centerId='user';
  }
  renderPeopleRegistry();
  renderRelationshipFocus();
  renderRelationshipCards();
}

function setPeopleRelationsTab(tab){
  const actorsTab=tab==='actors';
  document.querySelectorAll('[data-pr-tab]').forEach(btn=>{
    btn.classList.toggle('active',btn.dataset.prTab===tab);
  });
  document.getElementById('prActorsPane')?.classList.toggle('show',actorsTab);
  document.getElementById('prRelationsPane')?.classList.toggle('show',!actorsTab);
}

function openPeopleRelations(centerId='user',tab='relations'){
  closeProfileDrawer?.();
  closeCharDrawer?.();
  closeDMListCards?.();

  const resolvedCenter=getRelationshipActor(centerId);
  relationshipGraphState.centerId=resolvedCenter?.id||'user';
  renderPeopleRelations();
  setPeopleRelationsTab(tab);
  showRoute('routePeopleRelations');
}

function closeRelationshipEditor(){
  document.getElementById('prSheetDim')?.classList.remove('show');
  document.getElementById('prRelationSheet')?.classList.remove('show');
}

function syncRelationshipEditorLabels(){
  const center=getRelationshipActor(relationshipGraphState.centerId);
  const other=getRelationshipActor(document.getElementById('prOtherActor')?.value);
  if(!center || !other)return;

  document.getElementById('prForwardLabel').textContent=`${center.name} 看 ${other.name}`;
  document.getElementById('prBackwardLabel').textContent=`${other.name} 看 ${center.name}`;

  const forward=getDirectedRelationship(center.id,other.id);
  const backward=getDirectedRelationship(other.id,center.id);
  document.getElementById('prForwardInput').value=forward?.label||'';
  document.getElementById('prBackwardInput').value=backward?.label||'';

  const hasPair=!!(forward || backward);
  document.getElementById('prRelationDelete').style.display=hasPair?'':'none';
  document.getElementById('prSheetTitle').textContent=hasPair?'编辑关系':'建立关系';
}

function openRelationshipEditor(otherId=''){
  const center=getRelationshipActor(relationshipGraphState.centerId);
  if(!center)return;

  const select=document.getElementById('prOtherActor');
  if(!select)return;
  const options=getRelationshipActors().filter(actor=>actor.id!==center.id);

  if(!options.length){
    alert('当前没有其他人物可以建立关系');
    return;
  }

  select.innerHTML=options.map(actor=>`
    <option value="${prEscape(actor.id)}">${prEscape(actor.name)} · ${prEscape(relationshipTypeLabel(actor))}</option>
  `).join('');

  if(otherId && options.some(actor=>actor.id===otherId))select.value=otherId;
  else select.value=options[0].id;

  syncRelationshipEditorLabels();
  document.getElementById('prSheetDim')?.classList.add('show');
  document.getElementById('prRelationSheet')?.classList.add('show');
}

function saveRelationshipEditor(){
  const center=getRelationshipActor(relationshipGraphState.centerId);
  const otherId=document.getElementById('prOtherActor')?.value;
  const other=getRelationshipActor(otherId);
  if(!center || !other)return;

  const forward=document.getElementById('prForwardInput')?.value||'';
  const backward=document.getElementById('prBackwardInput')?.value||'';

  if(!forward.trim() && !backward.trim()){
    alert('至少填写一个方向的关系');
    return;
  }

  upsertDirectedRelationship(center.id,other.id,forward);
  upsertDirectedRelationship(other.id,center.id,backward);
  closeRelationshipEditor();
  renderPeopleRelations();
  setPeopleRelationsTab('relations');
}

function deleteRelationshipEditor(){
  const center=getRelationshipActor(relationshipGraphState.centerId);
  const otherId=document.getElementById('prOtherActor')?.value;
  if(!center || !otherId)return;
  deleteRelationshipPair(center.id,otherId);
  closeRelationshipEditor();
  renderPeopleRelations();
  setPeopleRelationsTab('relations');
}

function renderDMImportActorList(){
  const box=document.getElementById('dmImportActorList');
  if(!box)return;
  const available=window.__niniAvailableRocheCharacters||[];
  const imported=window.__niniImportedRocheCharacterIds||new Set();
  if(!available.length){box.innerHTML='<div class="dm-list-copy">正在读取 Roche 角色，或当前 Roche 没有可用角色。</div>';return;}
  box.innerHTML=available.map(c=>{
    const id=String(c.id||'');const name=String(c.name||c.handle||id);const handle=String(c.handle||c.name||id).replace(/^@/,'');const avatar=String(c.avatar||'');const on=imported.has(id);
    const actions=on
      ? `<button class="dm-list-contact-action" data-roche-import-toggle="${prEscape(id)}" type="button">解绑</button>`
      : `<button class="dm-list-contact-action" data-roche-import-toggle="${prEscape(id)}" type="button">绑定</button>`;
    return `<div class="dm-list-contact"><div class="dm-list-contact-avatar" style="${avatar?`background-image:url(&quot;${prEscape(avatar)}&quot;);background-size:cover;background-position:center`:''}">${avatar?'':prEscape(name.slice(0,1).toUpperCase())}</div><div class="dm-list-contact-copy"><strong>${prEscape(name)}</strong><span>@${prEscape(handle)} · ${on?'已绑定至INS':'角色 · 尚未绑定'}</span></div>${actions}</div>`;
  }).join('');
}

function renderDMUserSocialActorList(){
  const box=document.getElementById('dmUserSocialActorList');
  if(!box)return;

  const rows=getRelationshipActors()
    .filter(actor=>actor.kind==='npc')
    .filter(actor=>getDirectedRelationship('user',actor.id) || getDirectedRelationship(actor.id,'user'));

  box.innerHTML=rows.length ? rows.map(actor=>{
    const forward=getDirectedRelationship('user',actor.id)?.label;
    const backward=getDirectedRelationship(actor.id,'user')?.label;
    const rel=forward || backward || '已建立关系';

    return `
      <div class="dm-list-contact">
        <div class="dm-list-contact-avatar" data-identity-avatar="${prEscape(actor.id)}">${actor.avatarUrl?'':prEscape(actor.initial)}</div>
        <div class="dm-list-contact-copy">
          <strong>${prEscape(actor.name)}</strong>
          <span>社交联系人 · 与 User：${prEscape(rel)}</span>
        </div>
        <div class="dm-user-inline-actions">
          <button class="dm-list-contact-action" data-dm-open-npc-profile="${prEscape(actor.id)}" type="button">主页</button>
          <button class="dm-list-contact-action" data-dm-open-rel="${prEscape(actor.id)}" type="button">编辑</button>
          <button class="dm-list-contact-action" data-dm-delete-npc="${prEscape(actor.id)}" type="button">删除</button>
        </div>
      </div>`;
  }).join('') : '<div class="dm-list-copy">当前没有和 User 建立关系的社交联系人。</div>';
  hydrateIdentityAvatars(box);
}

function getDMStrangerRequestCount(){
  const badge=document.querySelector('#dmStrangerRequests .dm-v2-badge');
  const count=Number(badge?.textContent||0);
  return Number.isFinite(count) ? count : 0;
}

function renderDMQuickTempNpcList(){
  const box=document.getElementById('dmQuickTempNpcList');
  if(!box)return;

  const sessions=Object.values(strangerTempSessions||{})
    .filter(session=>session && !session.promoted);

  if(!sessions.length){
    box.innerHTML='<div class="dm-list-copy">当前没有可升级的临时陌生人。</div>';
    return;
  }

  box.innerHTML=sessions.map(session=>`
    <div class="dm-user-temp-row">
      <div class="dm-user-temp-avatar">${prEscape(session.initial||'?')}</div>
      <div class="dm-user-temp-main">
        <strong>@${prEscape(session.handle)}</strong>
        <span>${prEscape(session.messages?.at(-1)?.text || session.copy || '临时陌生会话')}</span>
      </div>
      <div class="dm-user-temp-actions">
        <button data-quick-open-temp="${prEscape(session.handle)}" type="button">打开</button>
        <button class="primary" data-quick-promote-temp="${prEscape(session.handle)}" type="button">升级</button>
      </div>
    </div>
  `).join('');
}

function renderDMUserNpcHub(){
  renderDMUserSocialActorList();
  renderDMQuickTempNpcList();

  const count=document.getElementById('dmQuickStrangerCount');
  if(count)count.textContent=String(getDMStrangerRequestCount());
}

function resetDMQuickNpcForm(){
  const form=document.getElementById('dmQuickAddNpcForm');
  form?.classList.remove('show');

  const name=document.getElementById('dmQuickNpcName');
  const handle=document.getElementById('dmQuickNpcHandle');
  const forward=document.getElementById('dmQuickNpcForwardRel');
  const backward=document.getElementById('dmQuickNpcBackwardRel');

  if(name)name.value='';
  if(handle)handle.value='';
  if(forward)forward.value='';
  if(backward)backward.value='';
}

function createUserFixedNPCFromHub(){
  const name=(document.getElementById('dmQuickNpcName')?.value||'').trim();
  const rawHandle=(document.getElementById('dmQuickNpcHandle')?.value||'').trim().replace(/^@/,'');
  const forward=(document.getElementById('dmQuickNpcForwardRel')?.value||'').trim();
  const backward=(document.getElementById('dmQuickNpcBackwardRel')?.value||'').trim();

  if(!name || !rawHandle){
    alert('请填写昵称和 IG ID');
    return;
  }

  if(!/^[A-Za-z0-9._-]+$/.test(rawHandle)){
    alert('IG ID 先只使用字母、数字、点、下划线或短横线');
    return;
  }

  if(dmProfiles[rawHandle]){
    alert('这个 IG ID 已经在人物库里了，可以直接去「人物与关系」建立关系');
    return;
  }

  if(!forward && !backward){
    alert('至少填写一个方向的关系');
    return;
  }

  const session={
    handle:rawHandle,
    initial:(name||rawHandle).slice(0,1).toUpperCase(),
    displayName:name,
    followers:0,
    messages:[],
    promoted:true,
    savedToDM:true
  };

  ensureFixedNPCProfile(session);
  upsertFixedNPCDMRow(session);
  upsertDirectedRelationship('user',rawHandle,forward);
  upsertDirectedRelationship(rawHandle,'user',backward);

  renderPeopleRelations();
  renderDMUserNpcHub();
  renderFeedShareDMRecipients();
  renderReelsV15DMRecipients();
  resetDMQuickNpcForm();
}

function bindPeopleRelationsV11515(){
  document.getElementById('drawerPeopleRelationsBtn')?.addEventListener('click',()=>{
    openPeopleRelations('user','actors');
  });

  document.getElementById('charRelationsOpenBtn')?.addEventListener('click',()=>{
    if(!currentCharProfile)return;
    const profile=getCharProfileData(currentCharProfile);
    openPeopleRelations(profile.handle,'relations');
  });

  document.querySelectorAll('[data-pr-tab]').forEach(btn=>{
    btn.addEventListener('click',()=>setPeopleRelationsTab(btn.dataset.prTab));
  });

  document.getElementById('prActorList')?.addEventListener('click',e=>{
    const btn=e.target.closest('[data-pr-view]');
    if(!btn)return;
    relationshipGraphState.centerId=btn.dataset.prView;
    renderPeopleRelations();
    setPeopleRelationsTab('relations');
  });

  document.getElementById('prFocusSwitch')?.addEventListener('click',e=>{
    const btn=e.target.closest('[data-pr-center]');
    if(!btn)return;
    relationshipGraphState.centerId=btn.dataset.prCenter;
    renderPeopleRelations();
    setPeopleRelationsTab('relations');
  });

  document.getElementById('prAddRelationBtn')?.addEventListener('click',()=>openRelationshipEditor());

  document.getElementById('prRelationList')?.addEventListener('click',e=>{
    const btn=e.target.closest('[data-pr-edit]');
    if(!btn)return;
    openRelationshipEditor(btn.dataset.prEdit);
  });

  document.getElementById('prOtherActor')?.addEventListener('change',syncRelationshipEditorLabels);
  document.getElementById('prRelationSheet')?.addEventListener('pointerup',event=>event.stopPropagation());
  document.getElementById('prRelationSheet')?.addEventListener('click',event=>event.stopPropagation());
  document.getElementById('prSheetClose')?.addEventListener('click',closeRelationshipEditor);
  document.getElementById('prSheetDim')?.addEventListener('click',closeRelationshipEditor);
  document.getElementById('prRelationCancel')?.addEventListener('click',closeRelationshipEditor);
  document.getElementById('prRelationSave')?.addEventListener('click',saveRelationshipEditor);
  document.getElementById('prRelationDelete')?.addEventListener('click',deleteRelationshipEditor);

  document.getElementById('dmOpenPeopleRelationsBtn')?.addEventListener('click',()=>{
    openPeopleRelations('user','relations');
  });

  document.getElementById('dmUserSocialActorList')?.addEventListener('click',e=>{
    const profileBtn=e.target.closest('[data-dm-open-npc-profile]');
    if(profileBtn){closeDMListCards();openCharProfile(profileBtn.dataset.dmOpenNpcProfile);return;}
    const deleteBtn=e.target.closest('[data-dm-delete-npc]');
    if(deleteBtn){deleteFixedNPCFromDMHub(deleteBtn.dataset.dmDeleteNpc);return;}
    const btn=e.target.closest('[data-dm-open-rel]');
    if(!btn)return;
    openPeopleRelations('user','relations');
    setTimeout(()=>openRelationshipEditor(btn.dataset.dmOpenRel),0);
  });

  document.getElementById('dmQuickAddNpcBtn')?.addEventListener('click',()=>{
    document.getElementById('dmQuickAddNpcForm')?.classList.toggle('show');
  });
  document.getElementById('dmQuickNpcCancel')?.addEventListener('click',resetDMQuickNpcForm);
  document.getElementById('dmQuickNpcSave')?.addEventListener('click',createUserFixedNPCFromHub);

  document.getElementById('dmOpenStrangerRequestsBtn')?.addEventListener('click',()=>{
    openDMListCard('dmStrangerCard');
  });

  document.getElementById('dmQuickTempNpcList')?.addEventListener('click',e=>{
    const openBtn=e.target.closest('[data-quick-open-temp]');
    if(openBtn){
      const handle=openBtn.dataset.quickOpenTemp;
      const session=strangerTempSessions[handle];
      if(!session)return;
      activeStrangerTempHandle=handle;
      renderStrangerTempSession();
      openDMListCard('dmStrangerTempCard');
      return;
    }

    const promoteBtn=e.target.closest('[data-quick-promote-temp]');
    if(promoteBtn){
      const handle=promoteBtn.dataset.quickPromoteTemp;
      const session=strangerTempSessions[handle];
      if(!session)return;
      promoteTempSessionToNPC(session);
    }
  });
}


/* ===== v1.15.17 INS Social Event Log + Actor Knowledge Ledger =====
   Design:
   1) World event stored exactly once.
   2) Per-actor ledger stores only {eventId, learnedAt, learnedVia}.
   3) Public visibility != actor knowledge.
   4) Sync cursor advances only after an actual Roche write succeeds.
*/
const INS_MEMORY_SYNC_PREFIX='[NiniINSSync';
const INS_SOCIAL_MEMORY_STORAGE_KEY='nini-instagram-social-memory-v5';
const INS_MEMORY_BUILD_ID='5.1.4';

function makeINSSocialMemoryBase(){
  return {
    version:3,
    buildId:INS_MEMORY_BUILD_ID,
    events:[],
    knowledge:{},
    syncCursor:{},
    mounts:{},              // legacy v1 writeback mount
    sources:{},             // actorId -> [{conversationId,...}]
    writebackTargets:{},    // actorId -> auto-detected single-chat target
    discoveryPrefs:{},      // actorId -> low | normal | high
    nextSeq:1
  };
}
function loadINSSocialMemoryState(){
  const base=makeINSSocialMemoryBase();
  try{
    const raw=localStorage.getItem(INS_SOCIAL_MEMORY_STORAGE_KEY);
    if(!raw)return base;
    const parsed=JSON.parse(raw)||{};

    // Development/update boundary: a new Nini build starts with a clean Event/Knowledge world,
    // while explicit user memory mounts and discovery preferences survive the update.
    if(String(parsed.buildId||'')!==INS_MEMORY_BUILD_ID){
      const fresh={
        ...base,
        mounts:parsed.mounts&&typeof parsed.mounts==='object'?parsed.mounts:{},
        sources:parsed.sources&&typeof parsed.sources==='object'?parsed.sources:{},
        discoveryPrefs:parsed.discoveryPrefs&&typeof parsed.discoveryPrefs==='object'?parsed.discoveryPrefs:{}
      };
      try{localStorage.setItem(INS_SOCIAL_MEMORY_STORAGE_KEY,JSON.stringify(fresh));}catch(e){}
      return fresh;
    }

    return {
      ...base,
      ...parsed,
      buildId:INS_MEMORY_BUILD_ID,
      events:Array.isArray(parsed.events)?parsed.events:[],
      knowledge:parsed.knowledge&&typeof parsed.knowledge==='object'?parsed.knowledge:{},
      syncCursor:parsed.syncCursor&&typeof parsed.syncCursor==='object'?parsed.syncCursor:{},
      mounts:parsed.mounts&&typeof parsed.mounts==='object'?parsed.mounts:{},
      sources:parsed.sources&&typeof parsed.sources==='object'?parsed.sources:{},
      writebackTargets:parsed.writebackTargets&&typeof parsed.writebackTargets==='object'?parsed.writebackTargets:{},
      discoveryPrefs:parsed.discoveryPrefs&&typeof parsed.discoveryPrefs==='object'?parsed.discoveryPrefs:{},
      nextSeq:Number(parsed.nextSeq)||1
    };
  }catch(e){
    return base;
  }
}

const insSocialMemoryState=loadINSSocialMemoryState();
// v5.0 writeback boundary: only knowledge learned after THIS plugin open belongs
// to the close-time batch. Older unsynced entries are intentionally not folded in.
const insMemorySessionStartedAt=Date.now()-1;

function persistINSSocialMemory(){
  try{
    /* Keep the prototype bounded. The real plugin can move this to Roche storage. */
    if(insSocialMemoryState.events.length>800){
      const keep=insSocialMemoryState.events.slice(-800);
      const keepIds=new Set(keep.map(event=>event.eventId));
      insSocialMemoryState.events=keep;
      Object.keys(insSocialMemoryState.knowledge).forEach(actorId=>{
        insSocialMemoryState.knowledge[actorId]=(
          insSocialMemoryState.knowledge[actorId]||[]
        ).filter(entry=>keepIds.has(entry.eventId));
      });
    }
    localStorage.setItem(
      INS_SOCIAL_MEMORY_STORAGE_KEY,
      JSON.stringify(insSocialMemoryState)
    );
  }catch(e){}
}

function normalizeSocialActorId(ref){
  const raw=String(ref||'').replace(/^@/,'').trim();
  if(!raw)return '';
  if(raw==='user' || raw==='you' || raw===String(userProfileState?.id||''))return 'user';

  const actors=typeof getRelationshipActors==='function' ? getRelationshipActors() : [];
  const found=actors.find(actor=>
    actor.id===raw ||
    actor.handle===raw ||
    actor.name===raw
  );
  return found?.id || raw;
}

function socialActorName(ref){
  const id=normalizeSocialActorId(ref);
  if(id==='user'){
    return userProfileState?.displayName || userProfileState?.id || 'user';
  }
  const actor=typeof getRelationshipActor==='function' ? getRelationshipActor(id) : null;
  return actor?.name || actor?.handle || String(ref||id||'某人');
}

function isFormalSocialActor(ref){
  const id=normalizeSocialActorId(ref);
  if(id==='user')return true;
  return !!(typeof getRelationshipActor==='function' && getRelationshipActor(id));
}

function getActorPublicDiscoveryLevel(actorId){
  const id=normalizeSocialActorId(actorId);
  if(!insSocialMemoryState.discoveryPrefs||typeof insSocialMemoryState.discoveryPrefs!=='object')insSocialMemoryState.discoveryPrefs={};
  const value=String(insSocialMemoryState.discoveryPrefs[id]||'normal');
  return ['low','normal','high'].includes(value)?value:'normal';
}
function setActorPublicDiscoveryLevel(actorId,value='normal'){
  const id=normalizeSocialActorId(actorId);
  if(!id||!isFormalSocialActor(id))return false;
  if(!insSocialMemoryState.discoveryPrefs||typeof insSocialMemoryState.discoveryPrefs!=='object')insSocialMemoryState.discoveryPrefs={};
  insSocialMemoryState.discoveryPrefs[id]=['low','normal','high'].includes(String(value))?String(value):'normal';
  persistINSSocialMemory();
  return true;
}
function detectFormalActorMentions(text='',supplied=[]){
  const body=String(text||'');
  const rows=[];
  const push=(id,strength='explicit',directAt=false)=>{
    const actor=getRelationshipActor(normalizeSocialActorId(id));
    if(!actor||actor.id==='user')return;
    const old=rows.find(x=>x.actorId===actor.id);
    const next={actorId:actor.id,strength,directAt:!!directAt};
    if(!old)rows.push(next);
    else if(directAt||strength==='explicit'){old.directAt=old.directAt||!!directAt;old.strength='explicit';}
  };
  (Array.isArray(supplied)?supplied:[]).forEach(raw=>{
    if(raw&&typeof raw==='object')push(raw.actorId||raw.id||raw.handle,raw.strength||raw.mentionStrength||'explicit',!!raw.directAt);
    else push(raw,'explicit',false);
  });
  (typeof getRelationshipActors==='function'?getRelationshipActors():[]).forEach(actor=>{
    if(!actor||actor.id==='user')return;
    const handle=String(actor.handle||'').replace(/^@/,'').trim();
    const name=String(actor.name||'').trim();
    if(handle){
      const escaped=[...handle].map(ch=>'^$.*+?()[]{}|'.includes(ch)?'\\'+ch:ch).join('');
      if(new RegExp('@'+escaped+'(?:\\b|$)','i').test(body))push(actor.id,'explicit',true);
      else if(handle.length>=3 && body.toLowerCase().includes(handle.toLowerCase()))push(actor.id,'explicit',false);
    }
    if(name.length>=2 && body.includes(name))push(actor.id,'explicit',false);
  });
  return rows;
}
function publicDiscoveryProbability(actorId,strength='explicit'){
  const level=getActorPublicDiscoveryLevel(actorId);
  const multiplier=level==='low'?0.5:level==='high'?1.8:1;
  const base=String(strength)==='contextual'?0.15:0.35;
  return Math.min(0.85,Math.max(0,base*multiplier));
}
function maybeGrantPublicDiscovery(event){
  if(!event||event.visibility!=='public')return;
  const mentionRows=Array.isArray(event.payload?.mentionMeta)?event.payload.mentionMeta:[];
  const related=[...new Set((event.relatedActorIds||[]).map(normalizeSocialActorId).filter(Boolean))];
  related.forEach(actorId=>{
    const actor=getRelationshipActor(actorId);
    if(!actor||actor.kind!=='roche')return;
    const meta=mentionRows.find(row=>normalizeSocialActorId(row?.actorId||row?.id)===actorId)||{};
    const directAt=!!meta.directAt;
    const strength=meta.strength||event.payload?.mentionStrength||'explicit';
    if(directAt){grantActorKnowledge(actorId,event.eventId,'public_mention',Date.now());return;}
    if(Math.random()<publicDiscoveryProbability(actorId,strength))grantActorKnowledge(actorId,event.eventId,'public_discovery',Date.now());
  });
}

function getSocialEvent(eventId){
  return insSocialMemoryState.events.find(event=>event.eventId===eventId) || null;
}

function getActorKnowledgeLedger(actorId){
  const id=normalizeSocialActorId(actorId);
  if(!insSocialMemoryState.knowledge[id])insSocialMemoryState.knowledge[id]=[];
  return insSocialMemoryState.knowledge[id];
}

function grantActorKnowledge(actorId,eventId,learnedVia='direct',learnedAt=Date.now()){
  const id=normalizeSocialActorId(actorId);
  if(!id || !isFormalSocialActor(id) || !getSocialEvent(eventId))return false;

  const ledger=getActorKnowledgeLedger(id);
  if(ledger.some(entry=>entry.eventId===eventId))return false;

  ledger.push({
    eventId,
    learnedAt:Number(learnedAt)||Date.now(),
    learnedVia:learnedVia||'direct'
  });
  persistINSSocialMemory();

  const actor=typeof getRelationshipActor==='function' ? getRelationshipActor(id) : null;
  if(actor?.kind==='roche'){
    queueINSMemoryRealtimeSync(id);
  }
  return true;
}

function recordSocialEvent(data={},options={}){
  const actorId=normalizeSocialActorId(data.actorId || data.actor || '');
  const targetIds=[...(data.targetIds||[])].map(normalizeSocialActorId).filter(Boolean);
  if(data.targetId || data.target){
    const one=normalizeSocialActorId(data.targetId || data.target);
    if(one && !targetIds.includes(one))targetIds.push(one);
  }
  const contentOwnerId=normalizeSocialActorId(data.contentOwnerId||'');
  const replyToActorId=normalizeSocialActorId(data.replyToActorId||'');
  const rawRelated=[...(data.relatedActorIds||data.mentionedActorIds||data.payload?.mentionedActorIds||[])];
  const suppliedRelated=rawRelated.map(item=>item&&typeof item==='object'?item:{actorId:item,strength:data.payload?.mentionStrength||'explicit'});
  const detectedMentions=detectFormalActorMentions(data.text||data.summary||'',suppliedRelated);
  const relatedActorIds=[...new Set(detectedMentions.map(row=>row.actorId).filter(Boolean))];
  const optionKnown=(options.knownActorIds||[]).map(normalizeSocialActorId).filter(Boolean);

  // Pure stranger↔stranger platform chatter stays Content only. It becomes an Event only
  // when a formal actor participates, owns/is targeted by the content, or is explicitly referenced.
  const relevanceRefs=[actorId,...targetIds,contentOwnerId,replyToActorId,...relatedActorIds,...optionKnown].filter(Boolean);
  if(!options.forceEvent && !relevanceRefs.some(isFormalSocialActor))return null;

  const dedupeKey=String(data.dedupeKey||'');
  if(dedupeKey){
    const existing=insSocialMemoryState.events.find(event=>event.dedupeKey===dedupeKey);
    if(existing){
      const known=[actorId,...targetIds,...optionKnown].filter(Boolean);
      [...new Set(known)].forEach(id=>grantActorKnowledge(id,existing.eventId,options.learnedVia||'direct'));
      return existing;
    }
  }

  const now=Number(data.createdAt)||Date.now();
  const seq=insSocialMemoryState.nextSeq++;
  const payload=data.payload&&typeof data.payload==='object'?{...data.payload}:{};
  if(detectedMentions.length)payload.mentionMeta=detectedMentions;
  const event={
    eventId:data.eventId || `evt_${now}_${seq}`,
    type:data.type || 'social_event',
    actorId,
    targetIds:[...new Set(targetIds)],
    relatedActorIds,
    contentId:String(data.contentId||''),
    contentType:String(data.contentType||''),
    contentOwnerId,
    threadId:String(data.threadId||''),
    replyToActorId,
    text:String(data.text||''),
    emoji:String(data.emoji||''),
    attitude:String(data.attitude||''),
    visibility:data.visibility || 'private',
    summary:String(data.summary||''),
    dedupeKey,
    payload,
    createdAt:now
  };

  insSocialMemoryState.events.push(event);
  persistINSSocialMemory();

  const known=[actorId,...event.targetIds,...optionKnown].filter(Boolean);
  [...new Set(known)].forEach(id=>grantActorKnowledge(id,event.eventId,options.learnedVia||'direct',now));
  maybeGrantPublicDiscovery(event);

  if(document.getElementById('routeMemoryBridge')?.classList.contains('show'))renderINSMemoryBridge();
  return event;
}

/* Explicitly grant an already-existing public event to a role that actually saw it. */
function exposeSocialEventToActor(actorId,eventId,via='public_exposure'){
  const event=getSocialEvent(eventId);
  if(!event || event.visibility!=='public')return false;
  const ok=grantActorKnowledge(actorId,eventId,via,Date.now());
  if(ok && document.getElementById('routeMemoryBridge')?.classList.contains('show')){
    renderINSMemoryBridge();
  }
  return ok;
}

/* When a role "scrolls past" a public post/reel later, the browsing engine can call this.
   It grants knowledge for all public events already attached to that content/thread. */
function exposePublicContentToActor(actorId,contentId,via='feed_seen'){
  const id=normalizeSocialActorId(actorId);
  const rows=insSocialMemoryState.events.filter(event=>
    event.visibility==='public' &&
    event.contentId===String(contentId||'')
  );

  let changed=0;
  rows.forEach(event=>{
    if(grantActorKnowledge(id,event.eventId,via,Date.now()))changed++;
  });

  if(changed && document.getElementById('routeMemoryBridge')?.classList.contains('show')){
    renderINSMemoryBridge();
  }
  return changed;
}

function getKnownActorIdsForEvent(eventId){
  const ids=[];
  Object.entries(insSocialMemoryState.knowledge).forEach(([actorId,ledger])=>{
    if((ledger||[]).some(entry=>entry.eventId===eventId))ids.push(actorId);
  });
  return ids;
}

function getActorKnowledgeEntries(actorId,{sinceLearnedAt=0,limit=0}={}){
  const id=normalizeSocialActorId(actorId);
  const ledger=(getActorKnowledgeLedger(id)||[])
    .filter(entry=>Number(entry.learnedAt||0)>Number(sinceLearnedAt||0))
    .map(entry=>({entry,event:getSocialEvent(entry.eventId)}))
    .filter(row=>row.event)
    .sort((a,b)=>a.entry.learnedAt-b.entry.learnedAt);

  return limit>0 ? ledger.slice(0,limit) : ledger;
}

function compactMemoryText(text,max=120){
  const clean=String(text||'').replace(/\s+/g,' ').trim();
  return clean.length>max ? clean.slice(0,max-1)+'…' : clean;
}

function memoryContentLabel(event){
  if(event.contentType==='story')return 'Story';
  if(event.contentType==='reel')return 'Reel';
  if(event.contentType==='feed')return 'IG 动态';
  return '内容';
}

function perspectiveLineForEvent(event,viewerId){
  const viewer=normalizeSocialActorId(viewerId);
  const actor=normalizeSocialActorId(event.actorId);
  const owner=normalizeSocialActorId(event.contentOwnerId);
  const replyTo=normalizeSocialActorId(event.replyToActorId);
  const actorName=socialActorName(actor);
  const ownerName=socialActorName(owner);
  const target=event.targetIds?.[0] || '';
  const targetName=socialActorName(target);
  const text=compactMemoryText(event.text,140);
  const contentLabel=memoryContentLabel(event);

  switch(event.type){
    case 'story_view':
      if(viewer===owner)return `${actorName}看了我的 Story。`;
      if(viewer===actor)return `我看了 ${ownerName} 的 Story。`;
      return `我知道 ${actorName} 看了 ${ownerName} 的 Story。`;

    case 'story_reaction':
      if(viewer===owner){
        return `${actorName}看了我的 Story，并用 ${event.emoji||'表情'} 表态${event.attitude?`（${event.attitude}）`:''}。`;
      }
      if(viewer===actor){
        return `我对 ${ownerName} 的 Story 使用了 ${event.emoji||'表情'}。`;
      }
      return `我看到 ${actorName} 对 ${ownerName} 的 Story 使用了 ${event.emoji||'表情'}。`;

    case 'feed_publish':
    case 'reel_publish': {
      const label=event.type==='reel_publish'?'Reel':'IG 动态';
      const caption=compactMemoryText(event.payload?.caption||event.text||'',140);
      const media=compactMemoryText(
        event.payload?.videoDescription||
        event.payload?.mediaDescription||
        '',
        170
      );
      if(viewer===actor){
        return [
          `我发布了一条 ${label}。`,
          caption?`文案：“${caption}”`:'',
          media?`${label==='Reel'?'视频':'配图'}：${media}`:''
        ].filter(Boolean).join(' ');
      }
      return [
        `${actorName}发布了一条 ${label}。`,
        caption?`文案：“${caption}”`:'',
        media?`${label==='Reel'?'视频':'配图'}：${media}`:''
      ].filter(Boolean).join(' ');
    }

    case 'feed_comment':
    case 'reel_comment': {
      const label=event.type==='reel_comment'?'Reel':'IG 动态';
      if(viewer===actor){
        if(replyTo)return `我在 ${ownerName} 的 ${label} 评论区回复了 ${socialActorName(replyTo)}：“${text}”`;
        return `我评论了 ${ownerName} 的 ${label}：“${text}”`;
      }
      if(replyTo && viewer===replyTo){
        return `${actorName}在 ${ownerName} 的 ${label} 评论区回复了我：“${text}”`;
      }
      if(viewer===owner){
        return `${actorName}在我的 ${label}下${replyTo?'回复了评论':'评论'}：“${text}”`;
      }
      return `我刷到 ${ownerName} 的 ${label}时，看见 ${actorName}${replyTo?`回复了 ${socialActorName(replyTo)}`:'评论'}：“${text}”`;
    }

    case 'dm_share_content': {
      const sharedOwner=socialActorName(event.payload?.sharedOwnerId || event.payload?.sharedOwnerLabel || '');
      const sharedType=event.payload?.sharedType || contentLabel;
      const detail=compactMemoryText(event.payload?.sharedSummary || event.payload?.caption || '',120);
      if(viewer===actor){
        return `我在 INS 私信里给 ${targetName} 分享了 ${sharedOwner} 的 ${sharedType}${detail?`：“${detail}”`:''}。`;
      }
      if(event.targetIds?.includes(viewer)){
        return `${actorName}在 INS 私信里给我分享了 ${sharedOwner} 的 ${sharedType}${detail?`：“${detail}”`:''}。`;
      }
      return `我知道 ${actorName} 给 ${targetName} 分享了 ${sharedOwner} 的 ${sharedType}。`;
    }

    case 'dm_message':
      if(viewer===actor){
        return `我在 INS 私信里对 ${targetName} 说：“${text}”`;
      }
      if(event.targetIds?.includes(viewer)){
        return `${actorName}在 INS 私信里对我说：“${text}”`;
      }
      return `我知道 ${actorName} 和 ${targetName} 在 INS 私信中说到：“${text}”`;

    case 'feed_like':
      if(viewer===owner)return `${actorName}赞了我的 IG 动态。`;
      if(viewer===actor)return `我赞了 ${ownerName} 的 IG 动态。`;
      return `我看到 ${actorName}赞了 ${ownerName} 的 IG 动态。`;

    case 'reel_like':
      if(viewer===owner)return `${actorName}赞了我的 Reel。`;
      if(viewer===actor)return `我赞了 ${ownerName} 的 Reel。`;
      return `我看到 ${actorName}赞了 ${ownerName} 的 Reel。`;

    default:
      if(event.summary)return compactMemoryText(event.summary,180);
      return `${actorName}在 INS 发生了一条 ${event.type} 事件。`;
  }
}

function getActorKnowledgeContext(actorId,limit=12){
  const id=normalizeSocialActorId(actorId);
  const rows=getActorKnowledgeEntries(id);
  const recent=rows.slice(-Math.max(1,limit));
  const seen=new Set();
  const out=[];

  recent.forEach(({event})=>{
    const line=perspectiveLineForEvent(event,id);
    if(!line || seen.has(line))return;
    seen.add(line);
    out.push(line);
  });
  return out;
}

function getActorUnsyncedKnowledge(actorId){
  const id=normalizeSocialActorId(actorId);
  const committedCursor=Number(insSocialMemoryState.syncCursor[id]||0);
  return getActorKnowledgeEntries(id,{sinceLearnedAt:committedCursor});
}

function memorySectionForType(type){
  if(type.startsWith('story_'))return 'Story';
  if(type.startsWith('feed_'))return 'Feed';
  if(type.startsWith('reel_'))return 'Reels';
  if(type.startsWith('dm_'))return 'INS 私信';
  return '其他社交';
}

function getActorMemorySources(actorId){
  const id=normalizeSocialActorId(actorId);
  if(!id)return [];
  if(!Array.isArray(insSocialMemoryState.sources[id]))insSocialMemoryState.sources[id]=[];
  return insSocialMemoryState.sources[id];
}

function normalizeRocheConversation(row={}){
  return {
    conversationId:String(row.conversationId||row.id||''),
    conversationName:String(row.conversationName||row.name||row.title||row.id||''),
    isGroup:!!(row.isGroup||row.type==='group'||row.kind==='group')
  };
}

async function bridgeListConversationsForActor(actorId){
  const bridge=window.NiniINSRocheBridge;
  if(!bridge)return null;
  if(typeof bridge.listConversationsForActor==='function')return await bridge.listConversationsForActor(actorId);
  if(typeof bridge.listConversations==='function')return await bridge.listConversations({memberId:actorId,actorId});
  if(bridge.conversation && typeof bridge.conversation.list==='function')return await bridge.conversation.list({memberId:actorId});
  return null;
}

async function bridgeGetShortTermMemory(args){
  const bridge=window.NiniINSRocheBridge;
  if(!bridge)return [];
  if(typeof bridge.getShortTerm==='function')return await bridge.getShortTerm(args);
  if(bridge.memory && typeof bridge.memory.getShortTerm==='function')return await bridge.memory.getShortTerm(args);
  return [];
}

async function bridgeGetLongTermMemory(args){
  const bridge=window.NiniINSRocheBridge;
  if(!bridge)return null;
  if(typeof bridge.getLongTerm==='function')return await bridge.getLongTerm(args);
  if(bridge.memory && typeof bridge.memory.getLongTerm==='function')return await bridge.memory.getLongTerm(args);
  return null;
}

function getActorWritebackTarget(actorId){
  const id=normalizeSocialActorId(actorId);
  const direct=getActorMemorySources(id).find(source=>source.enabled && !source.isGroup);
  if(!direct)return null;
  return {
    conversationId:String(direct.conversationId),
    conversationName:String(direct.conversationName||direct.conversationId),
    isGroup:false
  };
}

function reconcileActorConversationSettings(actorId,conversations=[]){
  const id=normalizeSocialActorId(actorId);
  if(!id)return [];

  const existing=getActorMemorySources(id);
  const byId=new Map(existing.map(row=>[row.conversationId,row]));
  const normalized=(conversations||[])
    .map(normalizeRocheConversation)
    .filter(row=>row.conversationId);

  const merged=normalized.map(conv=>{
    const prev=byId.get(conv.conversationId);
    return {
      conversationId:conv.conversationId,
      conversationName:conv.conversationName,
      isGroup:conv.isGroup,
      // v5.0.1: discovering a Roche conversation does NOT mount it.
      // The user must explicitly enable each single/group conversation, like Moments.
      enabled:prev?.enabled ?? false,
      shortLimit:Number.isFinite(Number(prev?.shortLimit)) ? Math.max(0,Number(prev.shortLimit)) : 50,
      factLimit:Number.isFinite(Number(prev?.factLimit)) ? Math.max(0,Number(prev.factLimit)) : 0,
      coreEnabled:!!prev?.coreEnabled
    };
  });

  insSocialMemoryState.sources[id]=merged;
  delete insSocialMemoryState.writebackTargets[id];
  persistINSSocialMemory();
  return merged;
}

function withINSMemoryTimeout(promise,ms=8000,label='Roche 会话读取'){
  let timer=0;
  return Promise.race([
    Promise.resolve(promise),
    new Promise((_,reject)=>{
      timer=setTimeout(()=>reject(new Error(`${label}超时（${Math.round(ms/1000)} 秒）`)),ms);
    })
  ]).finally(()=>clearTimeout(timer));
}

async function refreshActorMemoryConversations(actorId){
  const id=normalizeSocialActorId(actorId);
  if(!id)return {ok:false,reason:'actor_missing',conversations:[]};
  try{
    const rows=await withINSMemoryTimeout(
      bridgeListConversationsForActor(id),
      8000,
      'Roche 会话读取'
    );
    if(!Array.isArray(rows)){
      return {ok:false,reason:'bridge_not_connected',conversations:getActorMemorySources(id)};
    }
    const conversations=reconcileActorConversationSettings(id,rows);
    renderINSMemoryBridge();
    return {ok:true,conversations};
  }catch(error){
    return {
      ok:false,
      reason:error?.message||'conversation_list_failed',
      conversations:getActorMemorySources(id)
    };
  }
}

function updateActorMemorySource(actorId,conversationId,patch={}){
  const id=normalizeSocialActorId(actorId);
  const rows=getActorMemorySources(id);
  const row=rows.find(item=>item.conversationId===String(conversationId||''));
  if(!row)return false;

  if('enabled' in patch)row.enabled=!!patch.enabled;
  if('shortLimit' in patch)row.shortLimit=Math.max(0,Math.min(500,Number(patch.shortLimit)||0));
  if('factLimit' in patch)row.factLimit=Math.max(0,Math.min(500,Number(patch.factLimit)||0));
  if('coreEnabled' in patch)row.coreEnabled=!!patch.coreEnabled;

  persistINSSocialMemory();
  return true;
}

async function loadActorRocheMemoryContext(actorId){
  const id=normalizeSocialActorId(actorId);
  const actor=getRelationshipActor(id);
  if(!actor || actor.kind!=='roche')return {actorId:id,text:'',sources:[],reason:'not_roche_actor'};

  const enabled=getActorMemorySources(id).filter(source=>source.enabled);
  if(!enabled.length)return {actorId:id,text:'',sources:[],reason:'no_enabled_sources'};

  const sections=[];
  const errors=[];
  for(const source of enabled){
    const lines=[];
    const shortLimit=Math.max(0,Number(source.shortLimit)||0);
    const factLimit=Math.max(0,Number(source.factLimit)||0);
    let shortCount=0;
    let factCount=0;

    if(shortLimit>0){
      try{
        const msgs=await bridgeGetShortTermMemory({conversationId:source.conversationId,limit:shortLimit});
        (msgs||[]).forEach(msg=>{
          const text=String(msg?.text||'');
          if(!text || shouldIgnoreRocheMemoryText(text))return;
          const who=msg.senderHandle||msg.senderName||(msg.isMe?'user':'对方');
          lines.push(`${who}：${text}`);
          shortCount++;
        });
      }catch(error){
        errors.push({conversationId:source.conversationId,stage:'short',message:error?.message||String(error)});
      }
    }

    if(factLimit>0 || source.coreEnabled){
      try{
        const lt=await bridgeGetLongTermMemory({conversationId:source.conversationId,limit:factLimit||50});
        if(source.coreEnabled && lt?.core?.summary){
          lines.push(`【核心记忆】${lt.core.summary}`);
          factCount++;
        }
        if(factLimit>0 && Array.isArray(lt?.facts)){
          lt.facts.slice(0,factLimit).forEach(fact=>{
            const text=fact?.summaryText||fact?.action||fact?.text||'';
            if(text){lines.push(`【事实】${text}`);factCount++;}
          });
        }
      }catch(error){
        errors.push({conversationId:source.conversationId,stage:'long',message:error?.message||String(error)});
      }
    }

    if(lines.length){
      sections.push({
        conversationId:source.conversationId,
        conversationName:source.conversationName,
        isGroup:source.isGroup,
        shortCount,
        factCount,
        text:`【${source.isGroup?'群聊':'单聊'} · ${source.conversationName||source.conversationId}】\n${lines.join('\n')}`
      });
    }
  }

  return {
    actorId:id,
    actorName:actor.name,
    sources:sections,
    errors,
    reason:sections.length?'':(errors.length?'read_failed':'mounted_sources_empty'),
    text:sections.map(section=>section.text).join('\n\n')
  };
}

function buildActorSyncSummary(actorId,{limit=30}={}){
  const id=normalizeSocialActorId(actorId);
  const actor=getRelationshipActor(id);
  if(!actor || actor.kind!=='roche')return null;

  const all=getActorUnsyncedKnowledge(id);
  if(!all.length)return null;

  const chosen=all.slice(0,Math.max(1,limit));
  const groups=new Map();
  let maxLearnedAt=0;

  chosen.forEach(({entry,event})=>{
    const line=perspectiveLineForEvent(event,id);
    if(!line)return;
    const section=memorySectionForType(event.type);
    if(!groups.has(section))groups.set(section,[]);
    groups.get(section).push(`- ${line}`);
    maxLearnedAt=Math.max(maxLearnedAt,Number(entry.learnedAt||0));
  });

  if(!groups.size)return null;

  const now=new Date().toLocaleString('zh-CN',{
    month:'2-digit',day:'2-digit',hour:'2-digit',minute:'2-digit',hour12:false
  });
  const lines=[
    `${INS_MEMORY_SYNC_PREFIX} · INS 社交记忆 · ${now} · ${chosen.length}条]`,
    `角色：${actor.name}${actor.handle?`（@${actor.handle}）`:''}`,
    `user：${userProfileState.displayName||userProfileState.id}（@${userProfileState.id}）`,
    '',
    '以下只记录我实际做过、收到过或确实知道的 INS 社交，不包含我没有看到的公开内容：',
    ''
  ];

  ['Story','Feed','Reels','INS 私信','其他社交'].forEach(section=>{
    const rows=groups.get(section);
    if(!rows?.length)return;
    lines.push(`【${section}】`);
    lines.push(...rows);
    lines.push('');
  });

  lines.push('这是我的私人社交记忆。之后聊天时可以自然记得并延续，但不需要向 user 逐字复述这份记录。');

  const target=getActorWritebackTarget(id);
  return {
    actorId:id,
    actorName:actor.name,
    actorHandle:actor.handle||'',
    conversationId:target?.conversationId || '',
    conversationName:target?.conversationName || '',
    eventCount:chosen.length,
    maxLearnedAt,
    hasMore:all.length>chosen.length,
    summary:lines.join('\n')
  };
}

function buildAllRocheSyncPayloads(limitPerActor=30,actorIds=null){
  const allow=actorIds ? new Set([].concat(actorIds).map(normalizeSocialActorId)) : null;
  return getRelationshipActors()
    .filter(actor=>actor.kind==='roche' && (!allow || allow.has(actor.id)))
    .map(actor=>buildActorSyncSummary(actor.id,{limit:limitPerActor}))
    .filter(Boolean);
}

function markActorSyncCommitted(actorId,maxLearnedAt){
  const id=normalizeSocialActorId(actorId);
  const ts=Number(maxLearnedAt||0);
  if(!id || !ts)return false;
  insSocialMemoryState.syncCursor[id]=Math.max(Number(insSocialMemoryState.syncCursor[id]||0),ts);
  persistINSSocialMemory();
  renderINSMemoryBridge();
  return true;
}

function setActorMemoryMount(actorId,conversationId,conversationName=''){
  const id=normalizeSocialActorId(actorId);
  if(!id)return;
  const target={
    conversationId:String(conversationId||''),
    conversationName:String(conversationName||conversationId||''),
    isGroup:false
  };
  insSocialMemoryState.mounts[id]=target;
  insSocialMemoryState.writebackTargets[id]=target;
  persistINSSocialMemory();
}

function shouldIgnoreRocheMemoryText(text){
  return String(text||'').startsWith(INS_MEMORY_SYNC_PREFIX);
}

async function syncPendingKnowledgeToRoche(actorIds=null){
  const bridge=window.NiniINSRocheBridge;
  const payloads=buildAllRocheSyncPayloads(30,actorIds);

  if(!bridge || typeof bridge.injectShortTerm!=='function'){
    return {ok:false,reason:'bridge_not_connected',payloads};
  }

  const results=[];
  for(const payload of payloads){
    if(!payload.conversationId){
      results.push({actorId:payload.actorId,ok:false,reason:'single_chat_not_found'});
      continue;
    }
    try{
      const result=await bridge.injectShortTerm(payload);
      if(result!==false){
        markActorSyncCommitted(payload.actorId,payload.maxLearnedAt);
        results.push({actorId:payload.actorId,ok:true});
      }else{
        results.push({actorId:payload.actorId,ok:false,reason:'write_rejected'});
      }
    }catch(error){
      results.push({actorId:payload.actorId,ok:false,reason:error?.message||'write_failed'});
    }
  }

  return {ok:payloads.length===0?true:results.every(row=>row.ok),results,payloads};
}

const insMemoryRealtimeActorQueue=new Set();

/* v5.0: no timed/idle writeback.
   During the session we only mark which Roche roles gained new INS knowledge.
   Automatic writeback happens when the INS app closes/unmounts. */
function queueINSMemoryRealtimeSync(actorId){
  const id=normalizeSocialActorId(actorId);
  const actor=typeof getRelationshipActor==='function' ? getRelationshipActor(id) : null;
  if(!actor || actor.kind!=='roche')return;
  insMemoryRealtimeActorQueue.add(id);
}

/* Kept as an explicit/manual hook for future realtime experiments.
   Nothing calls this automatically in v5.0. */
async function flushINSMemoryRealtimeQueue(){
  const ids=[...insMemoryRealtimeActorQueue];
  if(!ids.length)return {ok:true,results:[],payloads:[]};
  return await syncPendingKnowledgeToRoche(ids);
}

async function flushCurrentINSSessionOnClose(){
  const result=await syncPendingKnowledgeToRoche();
  insMemoryRealtimeActorQueue.clear();
  return result;
}

async function primeINSMemoryRocheConnections(){
  const roles=getRelationshipActors().filter(actor=>actor.kind==='roche');
  for(const actor of roles){
    await refreshActorMemoryConversations(actor.id);
  }
}

function currentStoryMemoryContentId(owner){
  try{
    const archive=ensureStoryArchive(owner);
    return archive?.at(-1)?.batchId || `story_batch:${owner}`;
  }catch(e){
    return `story_batch:${owner}`;
  }
}

function getEventDisplaySummary(event){
  if(event.summary)return compactMemoryText(event.summary,160);
  return perspectiveLineForEvent(event,'user');
}

function getEventCategoryLabel(event){
  const type=String(event?.type||'');
  const actorFormal=isFormalSocialActor(event?.actorId);
  const ownerFormal=isFormalSocialActor(event?.contentOwnerId);
  const related=(event?.relatedActorIds||[]).some(isFormalSocialActor);
  if(related && !actorFormal && !ownerFormal)return '公开提及';
  if(type.includes('publish'))return '角色发布';
  if(type.startsWith('dm_'))return 'DM';
  if(type.startsWith('story_'))return 'Story 互动';
  if(type.includes('comment'))return '评论互动';
  return '角色相关';
}

let activeINSMemorySourceActorId='';

function renderINSMemoryBridge(){
  const stats=document.getElementById('insMemoryStats');
  const roleList=document.getElementById('insMemoryRoleList');
  const eventList=document.getElementById('insMemoryEventList');
  if(!stats || !roleList || !eventList)return;

  const roles=getRelationshipActors().filter(actor=>actor.kind==='roche');
  const pendingPayloads=buildAllRocheSyncPayloads();
  const knownCount=Object.values(insSocialMemoryState.knowledge)
    .reduce((sum,ledger)=>sum+(ledger?.length||0),0);

  stats.innerHTML=`
    <div class="ins-memory-stat"><strong>${insSocialMemoryState.events.length}</strong><span>角色相关事件</span></div>
    <div class="ins-memory-stat"><strong>${knownCount}</strong><span>Knowledge 索引</span></div>
    <div class="ins-memory-stat"><strong>${pendingPayloads.length}</strong><span>待回传角色</span></div>
  `;

  roleList.innerHTML=roles.length ? roles.map(actor=>{
    const pending=getActorUnsyncedKnowledge(actor.id).length;
    const sources=getActorMemorySources(actor.id);
    const enabledSources=sources.filter(source=>source.enabled).length;
    const target=getActorWritebackTarget(actor.id);

    return `
      <div class="ins-memory-role">
        <div class="ins-memory-avatar" data-identity-avatar="${prEscape(actor.id)}">${prEscape(actor.initial)}</div>
        <div class="ins-memory-role-main">
          <strong>${prEscape(actor.name)}</strong>
          <span>${enabledSources?`读取 ${enabledSources}/${sources.length} 个会话`:(sources.length?'当前未启用记忆来源':'尚未读取 Roche 会话')} · ${target?`回传：${prEscape(target.conversationName||target.conversationId)}`:'尚未挂载单聊'} · 已知 ${getActorKnowledgeLedger(actor.id).length} 条</span>
        </div>
        <span class="ins-memory-badge ${pending?'pending':''}">${pending?`待同步 ${pending}`:'无新增'}</span>
        <div class="ins-memory-role-actions">
          <button class="ins-memory-source-btn" data-ins-memory-source="${prEscape(actor.id)}" type="button">挂载</button>
          <button class="ins-memory-preview-btn" data-ins-memory-preview="${prEscape(actor.id)}" type="button">预览</button>
        </div>
      </div>`;
  }).join('') : '<div class="pr-empty">当前还没有引入 Roche 角色。</div>';

  hydrateIdentityAvatars?.(roleList);

  const recent=insSocialMemoryState.events.slice(-12).reverse();
  eventList.innerHTML=recent.length ? recent.map(event=>{
    const known=getKnownActorIdsForEvent(event.eventId).map(socialActorName).join('、') || '无人';
    return `
      <div class="ins-memory-event">
        <div class="ins-memory-event-head">
          <strong>${prEscape(getEventCategoryLabel(event))} · ${prEscape(event.type)}</strong>
          <span>${prEscape(event.visibility)}</span>
        </div>
        <div class="ins-memory-event-copy">${prEscape(getEventDisplaySummary(event))}</div>
        <div class="ins-memory-event-known">已知：${prEscape(known)}</div>
      </div>`;
  }).join('') : '<div class="pr-empty">还没有新的角色相关事件。纯路人动态和路人闲聊不会进入这里。</div>';
}

function openINSMemoryBridge(){
  closeProfileDrawer?.();
  renderINSMemoryBridge();
  showRoute('routeMemoryBridge');
}

function closeINSMemorySheets(){
  activeINSMemorySourceActorId='';
  document.getElementById('insMemorySheetDim')?.classList.remove('show');
  document.getElementById('insMemoryPreviewSheet')?.classList.remove('show');
  document.getElementById('insMemorySourceSheet')?.classList.remove('show');
}
function closeINSMemoryPreview(){closeINSMemorySheets()}

function openINSMemoryPreview(actorId){
  const actor=getRelationshipActor(actorId);
  if(!actor)return;
  const payload=buildActorSyncSummary(actorId);
  document.getElementById('insMemoryPreviewTitle').textContent=`${actor.name} · Roche 回传预览`;

  if(!payload){
    document.getElementById('insMemoryPreviewMeta').textContent='当前没有需要同步的新 Knowledge；不会向 Roche 写空记录。';
    document.getElementById('insMemoryPreviewText').textContent='（无新增）';
  }else{
    document.getElementById('insMemoryPreviewMeta').textContent=
      `${payload.eventCount} 条 Knowledge · ${payload.hasMore?'还有下一批未包含 · ':''}${payload.conversationId?`自动回传到：${payload.conversationName||payload.conversationId}`:'尚未找到角色单聊'} · 预览不会推进同步游标`;
    document.getElementById('insMemoryPreviewText').textContent=payload.summary;
  }
  document.getElementById('insMemorySheetDim')?.classList.add('show');
  document.getElementById('insMemoryPreviewSheet')?.classList.add('show');
}

function renderINSMemorySourceSheet(actorId){
  const id=normalizeSocialActorId(actorId);
  const actor=getRelationshipActor(id);
  if(!actor)return;

  activeINSMemorySourceActorId=id;
  const sources=getActorMemorySources(id);
  const target=getActorWritebackTarget(id);

  document.getElementById('insMemorySourceTitle').textContent=`${actor.name} · 挂载的会话记忆`;
  const discoverySelect=document.getElementById('insMemoryDiscoverySelect');
  if(discoverySelect)discoverySelect.value=getActorPublicDiscoveryLevel(id);
  document.getElementById('insMemorySourceMeta').textContent=
    '只显示包含该 char 的 Roche 会话。打开开关才会读取；短期默认 50。开启的单聊同时作为“关闭 INS 时”的回传目标；群聊只读不写。';

  const list=document.getElementById('insMemorySourceList');
  list.innerHTML=sources.length ? sources.map(source=>{
    const isTarget=!!(target && target.conversationId===source.conversationId);
    const sub=source.enabled
      ? (source.isGroup?'群聊 · 已挂载读取':'单聊 · 已挂载读取'+(isTarget?' · 关闭时回传':'') )
      : (source.isGroup?'群聊':'单聊');
    return `
      <div class="ins-memory-source ${source.enabled?'on':''}" data-memory-source-id="${prEscape(source.conversationId)}">
        <div class="ins-memory-source-head">
          <div class="ins-memory-source-main">
            <strong>${prEscape(source.conversationName||source.conversationId)}</strong>
            <span>${prEscape(sub)}</span>
          </div>
          <button class="ins-memory-source-toggle ${source.enabled?'on':''}" data-memory-source-toggle="${prEscape(source.conversationId)}" type="button"><i></i></button>
        </div>
        ${source.enabled?`
          <div class="ins-memory-source-opts">
            <label>短期
              <input type="number" min="0" max="500" value="${Number(source.shortLimit)||0}" data-memory-source-short="${prEscape(source.conversationId)}">
            </label>
            <label>事实
              <input type="number" min="0" max="500" value="${Number(source.factLimit)||0}" data-memory-source-fact="${prEscape(source.conversationId)}">
            </label>
            <label class="ins-memory-core-toggle">
              <input type="checkbox" ${source.coreEnabled?'checked':''} data-memory-source-core="${prEscape(source.conversationId)}">
              核心
            </label>
          </div>
        `:''}
      </div>`;
  }).join('') : `
    <div class="ins-memory-empty-source">
      该 char 还没有读取到可挂载会话。请先在 Roche 与这个 char 建立单聊或群聊，然后点下面“刷新 Roche 会话”。
    </div>`;

  document.getElementById('insMemoryWritebackInfo').innerHTML=target
    ? `<strong>双向挂载已建立</strong>Roche → INS 会读取你开启的会话；INS → Roche 在关闭插件时只回传到单聊「${prEscape(target.conversationName||target.conversationId)}」。`
    : `<strong>当前仅可单向读取</strong>如果你只开启群聊，INS 可以读取群聊上下文，但不会写回。要启用关闭时回传，请打开该 char 的单聊开关。`;

  const refreshBtn=document.getElementById('insMemorySourceRefresh');
  if(refreshBtn && refreshBtn.dataset.loading!=='1'){
    refreshBtn.disabled=!window.NiniINSRocheBridge;
    refreshBtn.textContent='刷新 Roche 会话';
  }
  document.getElementById('insMemorySheetDim')?.classList.add('show');
  document.getElementById('insMemorySourceSheet')?.classList.add('show');
}

async function openINSMemorySourceSheet(actorId){
  const id=normalizeSocialActorId(actorId);
  renderINSMemorySourceSheet(id);

  const meta=document.getElementById('insMemorySourceMeta');
  if(meta)meta.textContent='正在读取包含该 char 的 Roche 会话…';

  const result=await refreshActorMemoryConversations(id);

  if(activeINSMemorySourceActorId===id){
    renderINSMemorySourceSheet(id);
    if(result.ok){
      const count=Array.isArray(result.conversations)?result.conversations.length:0;
      document.getElementById('insMemorySourceMeta').textContent=
        `已读取 ${count} 个包含该 char 的 Roche 会话。打开开关后才会作为记忆读取。`;
    }else{
      document.getElementById('insMemorySourceMeta').textContent=
        result.reason==='bridge_not_connected'
          ? '当前没有连接到 Roche 会话接口。'
          : `读取 Roche 会话失败：${result.reason}。你仍可使用上一次成功读取并保存的挂载。`;
    }
  }
}

function bindINSSocialMemoryUI(){
  document.getElementById('drawerMemorySystemBtn')?.addEventListener('click',openINSMemoryBridge);

  document.getElementById('insMemoryRoleList')?.addEventListener('click',event=>{
    const sourceBtn=event.target.closest('[data-ins-memory-source]');
    if(sourceBtn){
      void openINSMemorySourceSheet(sourceBtn.dataset.insMemorySource);
      return;
    }
    const previewBtn=event.target.closest('[data-ins-memory-preview]');
    if(previewBtn)openINSMemoryPreview(previewBtn.dataset.insMemoryPreview);
  });

  document.getElementById('insMemorySourceList')?.addEventListener('click',event=>{
    const btn=event.target.closest('[data-memory-source-toggle]');
    if(!btn || !activeINSMemorySourceActorId)return;
    const conversationId=btn.dataset.memorySourceToggle;
    const source=getActorMemorySources(activeINSMemorySourceActorId).find(row=>row.conversationId===conversationId);
    if(!source)return;
    updateActorMemorySource(activeINSMemorySourceActorId,conversationId,{enabled:!source.enabled});
    renderINSMemorySourceSheet(activeINSMemorySourceActorId);
    renderINSMemoryBridge();
  });

  document.getElementById('insMemoryDiscoverySelect')?.addEventListener('change',event=>{
    if(!activeINSMemorySourceActorId)return;
    setActorPublicDiscoveryLevel(activeINSMemorySourceActorId,event.target.value||'normal');
    renderINSMemoryBridge();
  });

  document.getElementById('insMemorySourceList')?.addEventListener('change',event=>{
    if(!activeINSMemorySourceActorId)return;
    const target=event.target;
    if(target.matches('[data-memory-source-short]')){
      updateActorMemorySource(activeINSMemorySourceActorId,target.dataset.memorySourceShort,{shortLimit:target.value});
    }else if(target.matches('[data-memory-source-fact]')){
      updateActorMemorySource(activeINSMemorySourceActorId,target.dataset.memorySourceFact,{factLimit:target.value});
    }else if(target.matches('[data-memory-source-core]')){
      updateActorMemorySource(activeINSMemorySourceActorId,target.dataset.memorySourceCore,{coreEnabled:target.checked});
    }
    renderINSMemoryBridge();
  });

  document.getElementById('insMemorySourceRefresh')?.addEventListener('click',async()=>{
    if(!activeINSMemorySourceActorId)return;
    const btn=document.getElementById('insMemorySourceRefresh');
    if(!btn || btn.dataset.loading==='1')return;

    btn.dataset.loading='1';
    btn.disabled=true;
    btn.textContent='读取中…';

    const id=activeINSMemorySourceActorId;
    try{
      const result=await refreshActorMemoryConversations(id);

      if(activeINSMemorySourceActorId===id){
        renderINSMemorySourceSheet(id);

        if(result.ok){
          const count=Array.isArray(result.conversations)?result.conversations.length:0;
          document.getElementById('insMemorySourceMeta').textContent=
            `已读取 ${count} 个包含该 char 的 Roche 会话。打开开关后才会作为记忆读取。`;
        }else{
          document.getElementById('insMemorySourceMeta').textContent=
            result.reason==='bridge_not_connected'
              ? '当前没有连接到 Roche 会话接口。'
              : `读取 Roche 会话失败：${result.reason}。已保留上一次成功读取的挂载列表。`;
        }
      }
    }finally{
      // v5.0.2: no matter success / error / timeout, never leave the UI stuck at “读取中…”
      btn.dataset.loading='0';
      btn.disabled=!window.NiniINSRocheBridge;
      btn.textContent='刷新 Roche 会话';
    }
  });

  document.getElementById('insMemoryPreviewClose')?.addEventListener('click',closeINSMemorySheets);
  document.getElementById('insMemorySourceClose')?.addEventListener('click',closeINSMemorySheets);
  document.getElementById('insMemorySheetDim')?.addEventListener('click',closeINSMemorySheets);
}

window.NiniINSSocialMemory={
  state:insSocialMemoryState,
  recordSocialEvent,
  grantActorKnowledge,
  exposeSocialEventToActor,
  exposePublicContentToActor,
  getActorPublicDiscoveryLevel,
  setActorPublicDiscoveryLevel,
  getActorKnowledgeContext,
  buildActorSyncSummary,
  buildAllRocheSyncPayloads,
  markActorSyncCommitted,
  setActorMemoryMount,
  shouldIgnoreRocheMemoryText,
  getActorMemorySources,
  updateActorMemorySource,
  refreshActorMemoryConversations,
  loadActorRocheMemoryContext,
  getActorWritebackTarget,
  queueINSMemoryRealtimeSync,
  flushINSMemoryRealtimeQueue,
  syncPendingKnowledgeToRoche,
  flushCurrentINSSessionOnClose,
  flushBeforeUnmount:flushCurrentINSSessionOnClose
};

window.addEventListener('pagehide',()=>{
  if(window.__NiniINSResetting)return;
  persistINSSocialMemory();
  persistNiniStoryLifecycle?.();
  void flushINSDMHistoryPersistence();
  void flushCurrentINSSessionOnClose();
});


function resolveCharProfileKey(ref){
  if(charProfileState.profiles[ref]) return ref;
  const entry = Object.entries(charProfileState.profiles).find(([,profile])=>profile.handle===ref);
  return entry ? entry[0] : ref;
}

function getCharProfileData(ref){
  const key = resolveCharProfileKey(ref);
  if(charProfileState.profiles[key]) return charProfileState.profiles[key];

  const dm = typeof dmProfiles !== 'undefined' ? dmProfiles[key] : null;
  const created = {
    profileType:dm?.profileType || dm?.kind || 'npc',
    initial:dm?.initial || (ref?.[0] || '?').toUpperCase(),
    name:dm?.name || ref,
    handle:dm?.handle || ref,
    avatarUrl:dm?.avatarUrl || '',
    location:dm?.location || '',
    followers:dm?.followers || 0,
    following:0,
    bio:'Instagram profile',
    highlights:[
      {id:'daily',name:'日常',coverUrl:''},
      {id:'fav',name:'收藏',coverUrl:''}
    ]
  };
  charProfileState.profiles[key] = created;
  return created;
}


function isRocheCharacterHandle(ref){
  const key=resolveCharProfileKey(ref);
  return charProfileState.profiles[key]?.profileType === 'roche';
}
function isFixedNPCHandle(ref){
  const key=resolveCharProfileKey(ref);
  return charProfileState.profiles[key]?.profileType === 'npc';
}
function getMediaResolutionPolicy(author){
  return {
    author,
    usePrivateAlbum:isRocheCharacterHandle(author),
    allowTextDescription:true,
    allowUserApprovedImageGeneration:true,
    rule:isRocheCharacterHandle(author)
      ? 'Roche Character：先自由规划媒体；再按相册参与度逐张尝试本地匹配。部分命中允许，未命中保留文字描述或按设置标记待生图。'
      : '非 Roche Character：不调用私人相册 Matcher；直接使用文字描述，user 决定是否生图。'
  };
}

function getProfileIdentityRefs(profileOrRef){
  if(profileOrRef==='user'){
    return new Set(['user',String(userProfileState?.id||''),String(userProfileState?.rochePersonaId||''),String(userProfileState?.displayName||'')].filter(Boolean));
  }
  const profile=(profileOrRef&&typeof profileOrRef==='object')?profileOrRef:getCharProfileData(profileOrRef);
  const key=resolveCharProfileKey(profile?.rocheId||profileOrRef||profile?.handle||'');
  return new Set([
    String(key||''),String(profile?.rocheId||''),String(profile?.handle||''),String(profile?.name||'')
  ].filter(Boolean));
}
function authorBelongsToProfile(author,profileOrRef){
  const refs=getProfileIdentityRefs(profileOrRef);
  const raw=String(author||'').replace(/^@/,'');
  if(refs.has(raw))return true;
  const actor=typeof getRelationshipActor==='function'?getRelationshipActor(raw):null;
  return !!(actor && [actor.id,actor.handle,actor.name].some(v=>refs.has(String(v||''))));
}
function getPostsForProfile(profileOrRef){
  return posts.filter(p=>p.type!=='stranger' && authorBelongsToProfile(p.author,profileOrRef));
}
function getCharFollowed(ref){
  const matching=getPostsForProfile(ref);
  return matching.length ? matching.some(p=>p.followed) : true;
}
function setCharFollowed(ref,followed){
  getPostsForProfile(ref).forEach(p=>{p.followed=followed;});
  rerenderKeepScroll();
  renderCharProfile();
}
function charPostCount(ref){
  return getPostsForProfile(ref).length;
}

function renderCharHighlights(){
  const strip=document.getElementById('charHighlightStrip');
  if(!strip || !currentCharProfile) return;
  const profile=getCharProfileData(currentCharProfile);

  const items=(profile.highlights||[]).map(h=>{
    const cover=h.coverUrl
      ? `<img class="char-highlight-cover" src="${escapeDMCardHTML(h.coverUrl)}" alt="">`
      : (h.id==='fav'?'♡':'✦');
    return `
      <button
        class="char-highlight"
        data-char-highlight-id="${escapeDMCardHTML(h.id)}"
        type="button"
        aria-label="编辑 Highlight：${escapeDMCardHTML(h.name)}"
      >
        <div class="char-highlight-ring">${cover}</div>
        <div class="char-highlight-name">${escapeDMCardHTML(h.name)}</div>
      </button>`;
  }).join('');

  // Char Profile: existing highlights first, "new" always last.
  strip.innerHTML=`
    ${items}
    <button
      class="char-highlight add"
      data-char-highlight-id="__new"
      type="button"
      aria-label="新建 Highlight"
    >
      <div class="char-highlight-ring">＋</div>
      <div class="char-highlight-name">新建</div>
    </button>`;
  bindHighlightGeometryFallback();
}

function renderCharPosts(profile){
  const ownPosts=getPostsForProfile(profile);
  if(!ownPosts.length){
    return `<div class="char-empty"><strong>暂无 IG 动态</strong><span>角色之后发布的 Feed 动态会自动出现在这里。</span></div>`;
  }
  return `
    <div class="char-profile-grid">
      ${ownPosts.map((p,index)=>{
        const media=getPostCurrentMediaItem(p,posts.indexOf(p));
        return `<div class="char-profile-cell ${media.url?'has-image':''}" data-char-post-id="${escapeDMCardHTML(p.id)}">
          ${media.url?`<img src="${escapeDMCardHTML(media.url)}" alt="">`:escapeDMCardHTML(p.mediaDescription||p.mediaText||'IG 动态')}
        </div>`;
      }).join('')}
    </div>`;
}
function getReelsForProfile(profileOrHandle){
  return (reelsV15State.items||[]).filter(item=>authorBelongsToProfile(item.author,profileOrHandle));
}

function renderCharReels(profile){
  const ownReels=getReelsForProfile(profile);
  if(!ownReels.length){
    return `
      <div class="char-empty">
        <strong>暂无 Reels</strong>
        <span>${escapeDMCardHTML(profile.name)} 之后发布到主 Reels 的内容会自动出现在这里。</span>
      </div>`;
  }

  return `
    <div class="char-profile-grid">
      ${ownReels.map(item=>`
        <div class="char-profile-cell reel" data-char-reel-id="${escapeDMCardHTML(item.id)}">
          <div class="char-reel-copy">${escapeDMCardHTML(item.videoDescription || item.caption || 'Reel')}</div>
        </div>
      `).join('')}
    </div>`;
}

function openReelFromCharProfile(reelId){
  const item=getReelsV15Item(reelId);
  if(!item)return;

  reelsV15State.activeId=reelId;

  document.querySelectorAll('.route-page.show').forEach(el=>el.classList.remove('show'));
  routeStack.length=0;
  state.active='reels';
  renderNav();
  renderReelsV15();

  requestAnimationFrame(()=>{
    document.querySelector(`[data-reels-v15-id="${CSS.escape(reelId)}"]`)
      ?.scrollIntoView({block:'start',behavior:'auto'});
  });
}

function getCurrentCharBoard(){
  const key=resolveCharProfileKey(currentCharProfile);
  const batches=charProfileState.boardBatches[key]||charProfileState.boardBatches[currentCharProfile];
  if(!Array.isArray(batches) || !batches.length)return [];
  return batches.flatMap(batch=>Array.isArray(batch)?batch:[]).filter(Boolean);
}

function normalizeGuestbookEntry(raw={},ownerId='',index=0){
  const row=normalizeGeneratedPublicComment(raw,ownerId,index);
  if(!row)return null;
  const meta=String(raw.meta||raw.relationshipLabel||raw.relationship||'网友').slice(0,40);
  return {
    id:String(raw.id||`board_${ownerId}_${Date.now()}_${index}`),
    initial:row.initial||(getIdentityMeta(row.author).initial||'?'),
    name:row.author,
    displayName:row.displayName||getIdentityMeta(row.author).name,
    meta,
    text:row.text,
    translation:row.translation||'',
    sourceLanguage:row.sourceLanguage||'',
    showTranslation:false,
    generated:true,
    createdAt:row.createdAt
  };
}
async function generateCharBoardBatch(){
  if(!currentCharProfile)return;
  const key=resolveCharProfileKey(currentCharProfile);
  const profile=getCharProfileData(key);
  const ownerId=normalizeSocialActorId(key);
  const btn=document.getElementById('charBoardRefreshBtn');
  if(btn?.disabled)return;
  setNiniRefreshDotsBusy(btn,true);
  try{
    const result=await callNiniINSAI(INS_AI_TASKS.GUESTBOOK_REFRESH,{
      actorIds:isFormalSocialActor(ownerId)?[ownerId]:[],
      ownerId:isFormalSocialActor(ownerId)?ownerId:'',
      surface:'guestbook',
      content:{
        target:{actorId:ownerId,handle:profile.handle,name:profile.name,bio:profile.bio||''},
        existingEntries:(charProfileState.boardBatches[key]||[]).flat().slice(-12).map(r=>({author:r.name,text:r.text})),
        forbiddenAuthors:[...getINSCurrentUserIdentityTokens()]
      },
      prompt:'Generate exactly 5 short guestbook notes. Never write as the current User/persona. Keep motives mixed: fan/casual visitor/known actor/opinion/mild criticism/reply to an existing note/collaboration inquiry are all possible; do not turn every batch into fan praise.',
      extra:{actualEntryCount:5,forbidCurrentUser:true,allowKnownActors:true,allowStrangers:true}
    });
    if(!result.ok)throw new Error(result.reason||'留言生成失败');
    const rawRows=Array.isArray(result.data?.entries)?result.data.entries:Array.isArray(result.data?.comments)?result.data.comments:[];
    const rows=rawRows.map((raw,index)=>normalizeGuestbookEntry(raw,ownerId,index)).filter(Boolean).slice(0,5);
    if(!rows.length)throw new Error('AI 没有返回可用留言');
    if(!charProfileState.boardBatches[key])charProfileState.boardBatches[key]=[];
    charProfileState.boardBatches[key].push(rows);
    charProfileState.boardIndex[key]=charProfileState.boardBatches[key].length-1;
    renderCharProfileContent();
  }catch(error){
    alert('刷新留言失败：'+(error?.message||error));
  }finally{
    const live=document.getElementById('charBoardRefreshBtn');
    if(live===btn)setNiniRefreshDotsBusy(live,false);
    else if(live){live.disabled=false;live.classList.remove('nini-busy');}
  }
}

function renderCharBoard(){
  const rows=getCurrentCharBoard();
  return `
    <div class="char-board-wrap">
      <div class="char-board-head">
        <div class="char-board-title">关于 TA 的留言</div>
        <button id="charBoardRefreshBtn" class="char-board-refresh" type="button" aria-label="刷新留言">
          <svg viewBox="0 0 24 24"><path d="M20 6v5h-5"/><path d="M19 11a7 7 0 1 0 1 5"/></svg>
        </button>
      </div>

      ${rows.length ? rows.map(r=>{
        const identity=getIdentityMeta(r.name);
        const handle=identity.handle||String(r.name||'public');
        const canTranslate=shouldOfferTranslation(r.text,r.translation);
        return `
        <div class="char-board-row" data-char-board-row="${escapeDMCardHTML(r.id||'')}">
          <div class="char-board-avatar" data-identity-avatar="${escapeDMCardHTML(r.name)}">${escapeDMCardHTML(r.initial||identity.initial||'?')}</div>
          <div class="char-board-main">
            <div class="char-board-name">${escapeDMCardHTML(identity.name||r.displayName||handle)} <span class="char-board-meta">@${escapeDMCardHTML(handle)} · ${escapeDMCardHTML(r.meta||'网友')}</span></div>
            <div class="char-board-copy">${escapeDMCardHTML(r.text)}</div>
            ${canTranslate?`<div class="char-board-toggle" data-char-board-translate="${escapeDMCardHTML(r.id||'')}">${r.showTranslation?'收起翻译':'查看翻译'}</div>`:''}
            ${canTranslate&&r.showTranslation?`<div class="char-board-translation">${escapeDMCardHTML(r.translation)}</div>`:''}
          </div>
          <div class="char-board-actions">
            <button class="char-board-mini danger" data-char-board-delete="${escapeDMCardHTML(r.id||'')}" type="button">删除</button>
          </div>
        </div>`;
      }).join('') : `
        <div class="char-empty" style="min-height:220px">
          <strong>还没有留言</strong>
          <span>点右上角刷新按钮新增一批主页留言；旧留言会保留，可逐条删除。</span>
        </div>`}
    </div>`;
}

function toggleCharBoardEntryTranslation(entryId=''){
  if(!currentCharProfile || !entryId)return;
  const key=resolveCharProfileKey(currentCharProfile);
  const batches=charProfileState.boardBatches[key];
  if(!Array.isArray(batches) || !batches.length)return;
  const row=batches.flatMap(batch=>Array.isArray(batch)?batch:[])
    .find(item=>String(item?.id||'')===String(entryId));
  if(!row)return;
  row.showTranslation=!row.showTranslation;
  renderCharProfileContent();
}
function deleteCharBoardEntry(entryId=''){
  if(!currentCharProfile || !entryId)return;
  const key=resolveCharProfileKey(currentCharProfile);
  const batches=charProfileState.boardBatches[key];
  if(!Array.isArray(batches) || !batches.length)return;
  for(let i=0;i<batches.length;i++){
    const rows=Array.isArray(batches[i])?batches[i]:[];
    batches[i]=rows.filter(item=>String(item?.id||'')!==String(entryId));
  }
  charProfileState.boardBatches[key]=batches.filter(batch=>Array.isArray(batch)&&batch.length);
  renderCharProfileContent();
}



/* ===== v5.1 Album Matcher V3: category + tag + optional portrait era + participation ===== */
const CHAR_ALBUM_SETTINGS_STORAGE_KEY='nini-ins-char-album-settings-v1';
const CHAR_ALBUM_CATEGORY_OPTIONS=Object.freeze([
  {id:'portrait_selfie',group:'人像',label:'自拍',portrait:true},
  {id:'portrait_candid',group:'人像',label:'他拍',portrait:true},
  {id:'portrait_group',group:'人像',label:'合影',portrait:true},
  {id:'scene_landscape',group:'景物',label:'风景',portrait:false},
  {id:'scene_object',group:'景物',label:'物品',portrait:false},
  {id:'scene_food',group:'景物',label:'食物',portrait:false},
  {id:'scene_pet',group:'景物',label:'动物/宠物',portrait:false}
]);
function loadCharAlbumSettingsState(){try{const raw=localStorage.getItem(CHAR_ALBUM_SETTINGS_STORAGE_KEY);const parsed=raw?JSON.parse(raw):{};return parsed&&typeof parsed==='object'?parsed:{};}catch(e){return {};}}
const charAlbumSettingsState=loadCharAlbumSettingsState();
let charAlbumEditingEraId=null;let charAlbumSelectedCategory='';let charAlbumSelectedEraIds=new Set();
function persistCharAlbumSettingsState(){try{localStorage.setItem(CHAR_ALBUM_SETTINGS_STORAGE_KEY,JSON.stringify(charAlbumSettingsState));}catch(e){}}
function charAlbumSettingsKey(ref){const raw=ref||currentCharProfile||'';return resolveCharProfileKey(raw)||String(raw||'');}
function splitCharAlbumTags(value=''){return [...new Set(String(value||'').split(/[，,、/|·\n]+/).map(v=>v.trim()).filter(Boolean))];}
function ensureCharAlbumSettings(ref){
  const key=charAlbumSettingsKey(ref);if(!key)return {periods:[],eraMatchingEnabled:false,albumParticipation:70,autoGenerateMissing:false};
  if(!charAlbumSettingsState[key])charAlbumSettingsState[key]={periods:[],eraMatchingEnabled:false,albumParticipation:70,autoGenerateMissing:false,updatedAt:Date.now()};
  const state=charAlbumSettingsState[key];if(!Array.isArray(state.periods))state.periods=[];
  if(typeof state.eraMatchingEnabled!=='boolean')state.eraMatchingEnabled=false;
  state.albumParticipation=[0,40,70,100].includes(Number(state.albumParticipation))?Number(state.albumParticipation):70;
  state.autoGenerateMissing=!!state.autoGenerateMissing;
  state.periods=state.periods.map((period,index)=>({id:String(period.id||`era_${index}_${Date.now()}`),name:String(period.name||period.label||'未命名时期'),description:String(period.description||period.desc||''),featureTags:Array.isArray(period.featureTags)?period.featureTags.map(String).filter(Boolean):splitCharAlbumTags(period.description||period.desc||''),isCurrent:!!period.isCurrent,createdAt:Number(period.createdAt)||Date.now()}));
  if(state.periods.length&&!state.periods.some(p=>p.isCurrent))state.periods[0].isCurrent=true;
  return state;
}
function getCharAlbumPeriods(ref){return ensureCharAlbumSettings(ref).periods;}
function getCharAlbumCurrentPeriod(ref){const ps=getCharAlbumPeriods(ref);return ps.find(p=>p.isCurrent)||ps[0]||null;}
function canEnableCharAlbumEraMatching(ref){const state=ensureCharAlbumSettings(ref);const cur=state.periods.find(p=>p.isCurrent);const old=state.periods.find(p=>!p.isCurrent);return !!(state.periods.length>=2&&cur&&old&&splitCharAlbumTags(cur.description).length&&state.periods.some(p=>!p.isCurrent&&splitCharAlbumTags(p.description).length));}
function getCharAlbumEraInstruction(ref){const state=ensureCharAlbumSettings(ref);if(!state.eraMatchingEnabled||!canEnableCharAlbumEraMatching(ref))return '';const current=getCharAlbumCurrentPeriod(ref);if(!current)return '';return ['【相册时期匹配】',`当前时期：${current.name}（${current.id}）`,current.description?`当前外貌 Tag：${splitCharAlbumTags(current.description).join('、')}`:'','可在有明确记忆/语境依据时选择非当前时期；绝不能为了使用旧照编造过去事件。','时期只作用于自拍 / 他拍 / 合影；景物不看时期。'].filter(Boolean).join('\n');}
function getCharAlbumMatcherConfig(ref){const state=ensureCharAlbumSettings(ref);const current=getCharAlbumCurrentPeriod(ref);return {enabled:state.albumParticipation>0,participation:state.albumParticipation,autoGenerateMissing:state.autoGenerateMissing,eraMatchingEnabled:state.eraMatchingEnabled&&canEnableCharAlbumEraMatching(ref),currentPeriodId:current?.id||'',currentPeriodName:current?.name||'',periods:state.eraMatchingEnabled?state.periods.map(p=>({id:p.id,name:p.name,featureTags:splitCharAlbumTags(p.description),isCurrent:p.isCurrent})):[],eraInstruction:getCharAlbumEraInstruction(ref)};}
function isPortraitAlbumCategory(category=''){return String(category).startsWith('portrait_');}
function normalizeCharAlbumCategory(value='',desc=''){const raw=String(value||'').toLowerCase().trim();if(CHAR_ALBUM_CATEGORY_OPTIONS.some(x=>x.id===raw))return raw;const t=(raw+' '+String(desc||'')).toLowerCase();if(/自拍|selfie|镜前/.test(t))return 'portrait_selfie';if(/合影|合照|group|朋友.*拍/.test(t))return 'portrait_group';if(/他拍|人像|portrait|candid|街拍/.test(t))return 'portrait_candid';if(/食物|甜品|咖啡|蛋糕|food|meal|drink/.test(t))return 'scene_food';if(/宠物|猫|狗|动物|pet|cat|dog/.test(t))return 'scene_pet';if(/物品|桌面|衣服|包|相机|object|item/.test(t))return 'scene_object';if(/风景|街景|海|山|夜景|landscape|scenery|view/.test(t))return 'scene_landscape';return '';}
function getCharAlbumCategoryMeta(id=''){return CHAR_ALBUM_CATEGORY_OPTIONS.find(x=>x.id===id)||null;}
function filterCharAlbumAssetsByConfiguredPeriod(ref,assets=[]){return [...assets].filter(asset=>!['generated','generated_favorite'].includes(String(asset?.kind||'')));}
function renderCharAlbumCategoryControls(){const grid=document.getElementById('charAlbumCategoryGrid');if(!grid)return;grid.innerHTML=CHAR_ALBUM_CATEGORY_OPTIONS.map(x=>`<button class="char-album-category-chip ${charAlbumSelectedCategory===x.id?'active':''}" data-album-category="${x.id}" type="button">${x.group}·${x.label}</button>`).join('');renderCharAlbumAssetEraChoices();}
function renderCharAlbumAssetEraChoices(){const wrap=document.getElementById('charAlbumAssetEraWrap'),row=document.getElementById('charAlbumAssetEraRow');if(!wrap||!row||!currentCharProfile)return;const state=ensureCharAlbumSettings(currentCharProfile);const show=state.eraMatchingEnabled&&isPortraitAlbumCategory(charAlbumSelectedCategory);wrap.hidden=!show;if(!show){charAlbumSelectedEraIds.clear();row.innerHTML='';return;}const ps=state.periods;if(!charAlbumSelectedEraIds.size){const cur=getCharAlbumCurrentPeriod(currentCharProfile);if(cur)charAlbumSelectedEraIds.add(cur.id);}row.innerHTML=ps.map(p=>`<button class="char-album-era-select-chip ${charAlbumSelectedEraIds.has(p.id)?'active':''}" data-album-asset-era="${escapeDMCardHTML(p.id)}" type="button">${p.isCurrent?'当前 · ':''}${escapeDMCardHTML(p.name)}</button>`).join('');}
function renderCharAlbumParticipation(){if(!currentCharProfile)return;const state=ensureCharAlbumSettings(currentCharProfile);document.getElementById('charAlbumParticipationBadge').textContent=state.albumParticipation?state.albumParticipation+'%':'关闭';document.querySelectorAll('[data-album-rate]').forEach(btn=>btn.classList.toggle('active',Number(btn.dataset.albumRate)===state.albumParticipation));const ag=document.getElementById('charAlbumAutoGenerateMissing');if(ag)ag.checked=state.autoGenerateMissing;}
function setCharAlbumParticipation(value){if(!currentCharProfile)return;const state=ensureCharAlbumSettings(currentCharProfile);state.albumParticipation=[0,40,70,100].includes(Number(value))?Number(value):70;state.updatedAt=Date.now();persistCharAlbumSettingsState();renderCharAlbumParticipation();}
function setCharAlbumEraMatchingEnabled(on){if(!currentCharProfile)return;const state=ensureCharAlbumSettings(currentCharProfile);state.eraMatchingEnabled=!!on;state.updatedAt=Date.now();persistCharAlbumSettingsState();renderCharAlbumEraManager();renderCharAlbumCategoryControls();}
function renderCharAlbumEraManager(){const status=document.getElementById('charAlbumEraStatus'),badge=document.getElementById('charAlbumEraStatusBadge'),toggle=document.getElementById('charAlbumEraEnabled'),collapse=document.getElementById('charAlbumEraCollapse'),row=document.getElementById('charAlbumEraRow');if(!status||!badge||!toggle||!collapse||!row||!currentCharProfile)return;const state=ensureCharAlbumSettings(currentCharProfile),ps=state.periods,cur=getCharAlbumCurrentPeriod(currentCharProfile);toggle.checked=state.eraMatchingEnabled;collapse.hidden=!state.eraMatchingEnabled;const ready=canEnableCharAlbumEraMatching(currentCharProfile);badge.textContent=state.eraMatchingEnabled?(ready?'已开启':'待配置'):'关闭';status.textContent=state.eraMatchingEnabled?(ready?`当前：${cur?.name||'未设置'}。人像会使用时期 Tag；景物不受时期限制。`:'已打开时期匹配设置。请补齐：1 个当前时期 + 至少 1 个非当前时期，并给每个时期填写外貌特征 Tag；补齐前 Matcher 不启用时期过滤。'):'时期匹配关闭：相册仍可按分类 + 普通 Tag 使用。';row.innerHTML=ps.length?ps.map(p=>`<button class="char-album-era-chip ${p.isCurrent?'current':''}" data-char-album-era-edit="${escapeDMCardHTML(p.id)}" type="button">${p.isCurrent?'当前 · ':''}${escapeDMCardHTML(p.name)}</button>`).join(''):'<div class="char-album-era-empty">还没有时期。先添加至少两个时期，再打开上面的开关。</div>';}
function closeCharAlbumEraEditor(){charAlbumEditingEraId=null;const e=document.getElementById('charAlbumEraEditor');if(e)e.hidden=true;}
function openCharAlbumEraEditor(periodId='__new'){if(!currentCharProfile)return;const settings=ensureCharAlbumSettings(currentCharProfile),isNew=periodId==='__new',period=isNew?{id:'__new',name:'',description:'',isCurrent:settings.periods.length===0}:settings.periods.find(x=>x.id===periodId);if(!period)return;charAlbumEditingEraId=periodId;document.getElementById('charAlbumEraEditorTitle').textContent=isNew?'添加时期':'编辑时期';document.getElementById('charAlbumEraName').value=period.name||'';document.getElementById('charAlbumEraDesc').value=period.description||'';document.getElementById('charAlbumEraCurrent').checked=!!period.isCurrent;const d=document.getElementById('charAlbumEraDelete');if(d)d.style.visibility=isNew?'hidden':'visible';const e=document.getElementById('charAlbumEraEditor');if(e)e.hidden=false;}
function saveCharAlbumEra(){if(!currentCharProfile||!charAlbumEditingEraId)return;const name=(document.getElementById('charAlbumEraName')?.value||'').trim(),description=(document.getElementById('charAlbumEraDesc')?.value||'').trim(),wantsCurrent=!!document.getElementById('charAlbumEraCurrent')?.checked;if(!name){alert('先填写时期名称');return;}if(!splitCharAlbumTags(description).length){alert('请至少填写一个外貌特征 Tag');return;}const settings=ensureCharAlbumSettings(currentCharProfile),isNew=charAlbumEditingEraId==='__new';if(isNew){const id='era_'+Date.now().toString(36)+'_'+Math.random().toString(36).slice(2,6);settings.periods.push({id,name,description,featureTags:splitCharAlbumTags(description),isCurrent:wantsCurrent||settings.periods.length===0,createdAt:Date.now()});charAlbumEditingEraId=id;}else{const p=settings.periods.find(x=>x.id===charAlbumEditingEraId);if(!p)return;p.name=name;p.description=description;p.featureTags=splitCharAlbumTags(description);p.isCurrent=wantsCurrent;}if(wantsCurrent)settings.periods.forEach(p=>p.isCurrent=p.id===charAlbumEditingEraId);if(settings.periods.length&&!settings.periods.some(p=>p.isCurrent))settings.periods[0].isCurrent=true;settings.updatedAt=Date.now();persistCharAlbumSettingsState();closeCharAlbumEraEditor();renderCharAlbumEraManager();renderCharAlbumCategoryControls();}
function deleteCharAlbumEra(){if(!currentCharProfile||!charAlbumEditingEraId||charAlbumEditingEraId==='__new')return;const settings=ensureCharAlbumSettings(currentCharProfile),target=settings.periods.find(p=>p.id===charAlbumEditingEraId);if(!target)return;const was=target.isCurrent;settings.periods=settings.periods.filter(p=>p.id!==charAlbumEditingEraId);if(was&&settings.periods.length)settings.periods[0].isCurrent=true;settings.updatedAt=Date.now();persistCharAlbumSettingsState();closeCharAlbumEraEditor();renderCharAlbumEraManager();renderCharAlbumCategoryControls();}
function buildCharAlbumGenerationPrompt(actorIds=[],surface='feed'){if(surface==='reels')return '';return [...new Set((actorIds||[]).map(normalizeSocialActorId).filter(Boolean))].map(id=>{if(!isRocheCharacterHandle(id))return '';const cfg=getCharAlbumMatcherConfig(id),actor=getRelationshipActor(id);const periods=cfg.eraMatchingEnabled?cfg.periods.map(p=>`${p.id}=${p.name}[${p.featureTags.join('、')}]${p.isCurrent?'(当前)':''}`).join('；'):'';return `【${actor?.name||id} · 私人相册媒体规划】
先决定这条 ${surface==='story'?'Story':'Feed'} 想表达什么，再自由决定 1–4 个媒体槽位；不要为了迁就相册反向编剧情。每个图片槽位返回 albumCategory（portrait_selfie / portrait_candid / portrait_group / scene_landscape / scene_object / scene_food / scene_pet 之一）和 albumTags（中文短标签数组）。不要求一人一景，不要求两张：可以只有自拍、只有景物、两张食物、全是人像等。
相册参与度=${cfg.participation}%。这只是本地 Matcher 的尝试概率，不要因为概率改变内容计划。${cfg.participation===0?'本次不会读取相册，仍要正常给出图片描述和分类/Tag。':''}
${cfg.eraMatchingEnabled?`时期匹配只用于人像。可用时期：${periods}。人像槽位额外返回 albumEraId；没有明确旧时期依据时使用当前时期 ${cfg.currentPeriodId}。选择旧时期必须有角色记忆/上下文依据，严禁为了用旧照编造过去事件。景物槽位不要返回时期。`:'时期匹配关闭：不要输出时期约束。'}
相册找不到素材也完全可以发，未命中的槽位保留中文 mediaDescription。`;}).filter(Boolean).join('\n\n');}
window.NiniINSAlbumMatcher={getPeriods:getCharAlbumPeriods,getCurrentPeriod:getCharAlbumCurrentPeriod,getEraInstruction:getCharAlbumEraInstruction,getMatcherConfig:getCharAlbumMatcherConfig,filterAssetsByConfiguredPeriod:filterCharAlbumAssetsByConfiguredPeriod,categories:CHAR_ALBUM_CATEGORY_OPTIONS,buildGenerationPrompt:buildCharAlbumGenerationPrompt};


const charAlbumPreviewData = {};

const CHAR_ALBUM_USER_ASSETS_STORAGE_KEY='nini_ins_char_album_user_assets_v51';
const charAlbumUserAssetsState=(()=>{const fresh=loadJSONLocalState(CHAR_ALBUM_USER_ASSETS_STORAGE_KEY);if(Object.keys(fresh).length)return fresh;return loadJSONLocalState('nini_ins_char_album_user_assets_v5019');})();

function ensureCharAlbumUserAssetList(ref){
  const key=charSettingKey(ref);if(!key)return [];
  if(!Array.isArray(charAlbumUserAssetsState[key]))charAlbumUserAssetsState[key]=[];
  return charAlbumUserAssetsState[key];
}
function syncCharAlbumUserAssetsIntoPreview(ref){
  const key=charSettingKey(ref);if(!key)return [];
  const stored=ensureCharAlbumUserAssetList(key);
  const base=(charAlbumPreviewData[key]||[]).filter(asset=>!asset?.userAdded);
  charAlbumPreviewData[key]=[...base,...stored.map(asset=>({...asset,userAdded:true}))];
  return charAlbumPreviewData[key];
}
function persistCharAlbumUserAssets(){return saveJSONLocalState(CHAR_ALBUM_USER_ASSETS_STORAGE_KEY,charAlbumUserAssetsState);}
function getCharAlbumSelectableAssets(ref){const key=charSettingKey(ref);if(!key)return [];syncCharAlbumUserAssetsIntoPreview(key);return filterCharAlbumAssetsByConfiguredPeriod(key,charAlbumPreviewData[key]||[]);}
function getCharAlbumGeneratedFavorites(ref){const key=charSettingKey(ref);if(!key)return [];return (charAlbumPreviewData[key]||[]).filter(asset=>['generated','generated_favorite'].includes(String(asset?.kind||'')));}
function charAlbumAssetSearchText(asset){return [asset.desc,...(asset.tags||[]),...(asset.eraTags||[])].filter(Boolean).join(' ').toLowerCase();}
function charAlbumRequestTags(item){return [...new Set([...(Array.isArray(item?.albumTags)?item.albumTags:[]),...splitCharAlbumTags(item?.desc||item?.mediaDescription||'')].map(x=>String(x).trim()).filter(Boolean))];}
function scoreCharAlbumAsset(asset,request,cfg){const cat=normalizeCharAlbumCategory(asset.category||asset.type,asset.desc);if(request.category&&cat!==request.category)return -1;if(cfg.eraMatchingEnabled&&isPortraitAlbumCategory(request.category)){const eraId=request.eraId||cfg.currentPeriodId;const ids=Array.isArray(asset.eraIds)?asset.eraIds:(asset.eraId?[asset.eraId]:[]);if(!eraId||!ids.includes(eraId))return -1;}const text=charAlbumAssetSearchText(asset),tags=request.tags||[];let score=request.category?5:0;for(const tag of tags){const t=String(tag).toLowerCase();if(!t)continue;if(text.includes(t))score+=3;else{for(const part of splitCharAlbumTags(t)){if(part&&text.includes(part))score+=1;}}}return score;}
function matchCharAlbumAssetForMedia(ref,item){const cfg=getCharAlbumMatcherConfig(ref),assets=getCharAlbumSelectableAssets(ref);if(!cfg.enabled||!assets.length)return null;const category=normalizeCharAlbumCategory(item?.albumCategory||item?.category,item?.desc||item?.mediaDescription||'');const request={category,tags:charAlbumRequestTags(item),eraId:String(item?.albumEraId||item?.eraId||'')};let best=null,bestScore=-1;assets.forEach(asset=>{const score=scoreCharAlbumAsset(asset,request,cfg);if(score<0)return;const jitter=Math.random()*.15;if(score+jitter>bestScore){bestScore=score+jitter;best=asset;}});if(!best)return null;if(request.tags.length&&bestScore<5.5)return null;return best;}
function resolveGeneratedMediaItemsWithAlbum(ref,items=[]){if(!isRocheCharacterHandle(ref))return items;const cfg=getCharAlbumMatcherConfig(ref);return (items||[]).map(item=>{const base={...item,albumCategory:normalizeCharAlbumCategory(item?.albumCategory||item?.category,item?.desc||''),albumTags:charAlbumRequestTags(item),albumEraId:String(item?.albumEraId||item?.eraId||'')};if(cfg.participation<=0||Math.random()*100>=cfg.participation)return {...base,source:cfg.autoGenerateMissing?'generated_pending':'description',generatedRequested:cfg.autoGenerateMissing};const asset=matchCharAlbumAssetForMedia(ref,base);if(!asset)return {...base,source:cfg.autoGenerateMissing?'generated_pending':'description',generatedRequested:cfg.autoGenerateMissing};return {...base,id:asset.id,url:asset.url||'',source:'album_matched',linkedAlbumId:asset.id,albumAssetDesc:asset.desc||''};});}
function renderCharAlbumManagerAssets(){if(!currentCharProfile)return;const selectable=getCharAlbumSelectableAssets(currentCharProfile),summary=document.getElementById('charAlbumLibrarySummary');if(summary){const cfg=getCharAlbumMatcherConfig(currentCharProfile);summary.textContent=`当前照片库 ${selectable.length} 张 · 相册参与度 ${cfg.participation?cfg.participation+'%':'关闭'} · 时期匹配 ${cfg.eraMatchingEnabled?'开':'关'}。生成收藏不会进入 Matcher。`;}const list=document.getElementById('charAlbumLibraryList');if(list)list.innerHTML=selectable.length?selectable.slice(-16).reverse().map(asset=>{const meta=getCharAlbumCategoryMeta(normalizeCharAlbumCategory(asset.category||asset.type,asset.desc));const era=(asset.eraIds||[]).map(id=>getCharAlbumPeriods(currentCharProfile).find(p=>p.id===id)?.name).filter(Boolean).join('/');return `<div class="char-album-library-item">${asset.url?`<img src="${escapeDMCardHTML(asset.url)}" alt="">`:''}<span class="album-meta">${escapeDMCardHTML(meta?`${meta.group}·${meta.label}`:'未分类')}${era?' · '+escapeDMCardHTML(era):''}<br>${escapeDMCardHTML(asset.desc||'相册图片')}</span>${asset.userAdded?`<button class="char-album-library-delete" data-char-album-delete="${escapeDMCardHTML(asset.id)}" type="button">×</button>`:''}</div>`;}).join(''):'<div class="char-album-generated-empty">还没有加入可用于动态的私人照片。先选分类，再用 URL 或手机照片库加入。</div>';const generated=document.getElementById('charAlbumGeneratedCollection');if(generated){const favs=getCharAlbumGeneratedFavorites(currentCharProfile);generated.innerHTML=favs.length?favs.map((asset,index)=>`<div class="char-album-generated"><div class="char-album-generated-thumb">${asset.url?`<img src="${escapeDMCardHTML(asset.url)}" alt="">`:escapeDMCardHTML(asset.desc||'AI 收藏')}</div><span>AI 收藏 · ${String(index+1).padStart(2,'0')}</span></div>`).join(''):'<div class="char-album-generated-empty">还没有生成收藏。这里的内容永远不会参与自动选图。</div>';}}
function getSelectedCharAlbumImportContext(){if(!charAlbumSelectedCategory){alert('先选择一个固定分类');return null;}const state=ensureCharAlbumSettings(currentCharProfile);let eraIds=[];if(state.eraMatchingEnabled&&canEnableCharAlbumEraMatching(currentCharProfile)&&isPortraitAlbumCategory(charAlbumSelectedCategory)){eraIds=[...charAlbumSelectedEraIds];if(!eraIds.length){alert('开启时期匹配时，人像图片至少选择一个适用时期');return null;}}const tags=splitCharAlbumTags(document.getElementById('charAlbumImportDesc')?.value||'');const eraTags=eraIds.flatMap(id=>{const p=state.periods.find(x=>x.id===id);return p?[p.name,...splitCharAlbumTags(p.description)]:[];});return {category:charAlbumSelectedCategory,tags,eraIds,eraTags:[...new Set(eraTags)]};}
function makeCharAlbumUserAsset({url='',desc='',source='url',fileName='',category='',tags=[],eraIds=[],eraTags=[]}){return {id:`album_${Date.now().toString(36)}_${Math.random().toString(36).slice(2,6)}`,kind:source==='phone'?'upload':'url',source,url:String(url||''),desc:String(desc||fileName||tags.join(' · ')||'用户加入的相册图片'),type:category,category,tags:[...tags],eraIds:[...eraIds],eraTags:[...eraTags],userAdded:true,createdAt:Date.now()};}
function addCharAlbumUrlAsset(){
  if(!currentCharProfile)return;
  const input=document.getElementById('charAlbumUrlInput');
  const url=(input?.value||'').trim();
  if(!url){alert('先粘贴图片 URL');return;}
  if(!/^(https?:|data:image\/|blob:)/i.test(url)){alert('请输入可用的图片 URL');return;}
  const ctx=getSelectedCharAlbumImportContext();if(!ctx)return;
  const desc=(document.getElementById('charAlbumImportDesc')?.value||'').trim();
  ensureCharAlbumUserAssetList(currentCharProfile).push(makeCharAlbumUserAsset({url,desc,source:'url',...ctx}));
  persistCharAlbumUserAssets();
  syncCharAlbumUserAssetsIntoPreview(currentCharProfile);
  if(input)input.value='';
  renderCharAlbumManagerAssets();
  renderCharProfileContent();
}
function readAlbumFileAsDataUrl(file){return new Promise((resolve,reject)=>{const reader=new FileReader();reader.onload=()=>resolve(String(reader.result||''));reader.onerror=reject;reader.readAsDataURL(file);});}
async function addCharAlbumPhoneFiles(fileList){
  if(!currentCharProfile)return;
  const files=[...(fileList||[])].filter(file=>file&&/^image\//.test(String(file.type||''))).slice(0,12);
  if(!files.length)return;
  const ctx=getSelectedCharAlbumImportContext();if(!ctx)return;
  const sharedDesc=(document.getElementById('charAlbumImportDesc')?.value||'').trim();
  const urls=await Promise.all(files.map(readAlbumFileAsDataUrl));
  const list=ensureCharAlbumUserAssetList(currentCharProfile);
  urls.forEach((url,index)=>list.push(makeCharAlbumUserAsset({url,desc:sharedDesc||files[index]?.name||'',source:'phone',fileName:files[index]?.name||'',...ctx})));
  const persisted=persistCharAlbumUserAssets();
  if(!persisted)console.warn('[INS] 手机导入图片已在当前会话加入，但浏览器持久化空间可能不足。');
  syncCharAlbumUserAssetsIntoPreview(currentCharProfile);
  renderCharAlbumManagerAssets();
  renderCharProfileContent();
}
function deleteCharAlbumUserAsset(assetId=''){
  if(!currentCharProfile||!assetId)return;
  const list=ensureCharAlbumUserAssetList(currentCharProfile);
  const next=list.filter(asset=>String(asset?.id||'')!==String(assetId));
  const key=charSettingKey(currentCharProfile);charAlbumUserAssetsState[key]=next;
  persistCharAlbumUserAssets();
  syncCharAlbumUserAssetsIntoPreview(currentCharProfile);
  renderCharAlbumManagerAssets();
  renderCharProfileContent();
}

function getCharAlbumPreviewAssets(){
  if(!currentCharProfile || !isRocheCharacterHandle(currentCharProfile)) return [];
  return getCharAlbumSelectableAssets(currentCharProfile);
}

function stableAlbumShuffle(items,key){
  const seed=[...(String(key||''))].reduce((a,c)=>a+c.charCodeAt(0),0);
  return [...items].sort((a,b)=>{
    const av=(a.id.charCodeAt(a.id.length-1)*17+seed)%97;
    const bv=(b.id.charCodeAt(b.id.length-1)*17+seed)%97;
    return av-bv;
  });
}

function renderCharAlbum(){
  const profile=getCharProfileData(currentCharProfile);
  const assets=stableAlbumShuffle(getCharAlbumPreviewAssets(),profile.handle).slice(0,8);

  return `
    <div class="char-album-shell">
      <div class="char-album-minihead">
        <strong>私人相册</strong>
        <span>随机预览 ${assets.length} 张 · 仅你可管理</span>
      </div>

      <div class="char-album-grid">
        <div id="charAlbumOpenManager" class="char-album-tile char-album-add">
          <strong>＋</strong>
          <span>管理相册</span>
        </div>

        ${assets.slice(0,8).map(asset=>`
          <div class="char-album-tile" data-album-asset="${escapeDMCardHTML(asset.id)}">
            ${asset.url?`<img src="${escapeDMCardHTML(asset.url)}" alt="">`:`<div class="char-album-tile-copy">${escapeDMCardHTML(asset.desc)}</div>`}
            <span class="char-album-tile-badge">
              ${asset.kind==='private'?'私享':asset.era==='archive'?'旧照':asset.era==='na'?'景物':asset.kind==='url'?'URL':'照片库'}
            </span>
          </div>`).join('')}
      </div>

      <div class="char-album-footnote">
        这里只预览“可用于动态”的私人照片库；生成收藏单独保存，不进入自动选图范围。完整管理从「＋」进入。
      </div>
    </div>`;
}

function openCharAlbumManager(){
  if(!currentCharProfile || !isRocheCharacterHandle(currentCharProfile)) return;
  const profile=getCharProfileData(currentCharProfile);
  document.getElementById('charAlbumManagerTitle').textContent=`${profile.name} · 私人相册`;
  charAlbumSelectedCategory='';charAlbumSelectedEraIds.clear();
  renderCharAlbumParticipation();renderCharAlbumEraManager();renderCharAlbumCategoryControls();renderCharAlbumManagerAssets();
  closeCharAlbumEraEditor();
  document.getElementById('charAlbumDim')?.classList.add('show');
  document.getElementById('charAlbumManager')?.classList.add('show');

  const scroller=document.querySelector('#charAlbumManager .char-album-manager-scroll');
  if(scroller)scroller.scrollTop=0;
}
function closeCharAlbumManager(){
  closeCharAlbumEraEditor();
  document.getElementById('charAlbumDim')?.classList.remove('show');
  document.getElementById('charAlbumManager')?.classList.remove('show');
}


function renderCharProfileContent(){
  const box=document.getElementById('charProfileContent');
  if(!box || !currentCharProfile) return;
  const profile=getCharProfileData(currentCharProfile);

  if(charProfileState.activeTab==='posts') box.innerHTML=renderCharPosts(profile);
  else if(charProfileState.activeTab==='reels') box.innerHTML=renderCharReels(profile);
  else if(charProfileState.activeTab==='album' && profile.profileType==='roche') box.innerHTML=renderCharAlbum(profile);
  else box.innerHTML=renderCharBoard();

  const tabs=[...document.querySelectorAll('.char-tab')];
  const visibleTabs=tabs.filter(btn=>profile.profileType==='roche' || btn.dataset.charTab!=='album');
  tabs.forEach(btn=>btn.classList.toggle('active',btn.dataset.charTab===charProfileState.activeTab));
  const activeIndex=Math.max(0,visibleTabs.findIndex(btn=>btn.dataset.charTab===charProfileState.activeTab));
  const indicator=document.getElementById('charTabIndicator');
  if(indicator) indicator.style.transform=`translateX(${activeIndex*100}%)`;

  document.getElementById('charBoardRefreshBtn')?.addEventListener('click',()=>void generateCharBoardBatch());
  box.querySelectorAll('[data-char-board-delete]').forEach(btn=>btn.addEventListener('click',event=>{event.preventDefault();event.stopPropagation();deleteCharBoardEntry(btn.dataset.charBoardDelete);}));
  box.querySelectorAll('[data-char-board-translate]').forEach(btn=>btn.addEventListener('click',event=>{event.preventDefault();event.stopPropagation();toggleCharBoardEntryTranslation(btn.dataset.charBoardTranslate);}));

  document.getElementById('charAlbumOpenManager')?.addEventListener('click',openCharAlbumManager);
}

function renderCharProfile(){
  if(!currentCharProfile) return;
  const profile=getCharProfileData(currentCharProfile);
  const isRoche=profile.profileType==='roche';

  if(!isRoche && charProfileState.activeTab==='album'){
    charProfileState.activeTab='posts';
  }

  const tabsWrap=document.querySelector('#routeCharProfile .char-profile-tabs');
  tabsWrap?.classList.toggle('npc-profile',!isRoche);

  document.getElementById('charProfileTopId').textContent=profile.handle;
  document.getElementById('charProfileDisplayName').textContent=profile.name;
  document.getElementById('charProfileHandle').textContent='@'+profile.handle;
  const locationEl=document.getElementById('charProfileLocation');
  if(locationEl){const text=String(profile.location||'').trim();locationEl.classList.toggle('show',!!text);const span=locationEl.querySelector('span');if(span)span.textContent=text;}
  document.getElementById('charProfileBio').textContent=profile.bio || '';
  document.getElementById('charProfileFollowers').textContent=profile.followers;
  document.getElementById('charProfileFollowingCount').textContent=profile.following;
  document.getElementById('charProfilePostCount').textContent=charPostCount(profile);

  const avatar=document.getElementById('charProfileAvatar');
  avatar.textContent=profile.initial;
  avatar.classList.toggle('has-image',!!profile.avatarUrl);
  avatar.style.backgroundImage=profile.avatarUrl ? `url("${profile.avatarUrl}")` : '';

  const followed=getCharFollowed(profile);
  const followBtn=document.getElementById('charProfileFollowBtn');
  followBtn.textContent=followed?'已关注':'关注';
  followBtn.classList.toggle('following',followed);
  followBtn.classList.toggle('follow',!followed);

  renderCharHighlights();
  renderCharProfileContent();
}

function openCharProfile(name){
  clearTransientProfileRoutes();
  currentCharProfile=resolveCharProfileKey(name);
  charProfileState.activeTab='posts';
  renderCharProfile();
  showRoute('routeCharProfile');
}

function closeCharDrawer(){
  document.getElementById('charDrawerDim')?.classList.remove('show');
  document.getElementById('charDrawer')?.classList.remove('show');
}
function openCharDrawer(){
  document.getElementById('charDrawerDim')?.classList.add('show');
  document.getElementById('charDrawer')?.classList.add('show');
}

const CHAR_GENERATION_PREF_STORAGE_KEY='nini_ins_char_generation_pref_v5019';
const CHAR_ACTIVE_PUBLISH_STORAGE_KEY='nini_ins_char_active_publish_v5019';
const CHAR_ACTIVE_MIN_MINUTES=30;
const CHAR_ACTIVE_JITTER=.20;
const CHAR_ACTIVE_CHECK_MS=60*1000;

function loadJSONLocalState(key){
  try{const raw=localStorage.getItem(key);const parsed=raw?JSON.parse(raw):{};return parsed&&typeof parsed==='object'?parsed:{};}catch(e){return {};}
}
function saveJSONLocalState(key,value){try{localStorage.setItem(key,JSON.stringify(value));return true;}catch(e){console.warn('[INS] local save failed',key,e);return false;}}
const charGenerationPreferenceState=loadJSONLocalState(CHAR_GENERATION_PREF_STORAGE_KEY);
const charActivePublishState=loadJSONLocalState(CHAR_ACTIVE_PUBLISH_STORAGE_KEY);
const charAutoPublishBusy=new Set();

function charSettingKey(ref){return resolveCharProfileKey(ref||currentCharProfile||'')||normalizeSocialActorId(ref||currentCharProfile||'')||String(ref||currentCharProfile||'');}
function ensureCharGenerationPreference(ref){
  const key=charSettingKey(ref);if(!key)return {social:'',posting:'',visual:''};
  if(!charGenerationPreferenceState[key])charGenerationPreferenceState[key]={social:'',posting:'',visual:'',updatedAt:0};
  return charGenerationPreferenceState[key];
}
function getCharGenerationPreferencePrompt(ref,surface='feed'){
  const pref=ensureCharGenerationPreference(ref);
  const rows=[
    pref.social?`社交氛围：${pref.social}`:'',
    pref.posting?`发帖风格：${pref.posting}`:'',
    pref.visual?`图片/视频意图：${pref.visual}`:''
  ].filter(Boolean);
  if(!rows.length)return '';
  const actor=getRelationshipActor(normalizeSocialActorId(ref));
  return `【${actor?.name||ref} · 长期生成偏好（软倾向）】\n${rows.join('\n')}\n当前 surface：${surface}。这些偏好负责“通常怎么发/怎么拍”，不改写人设、记忆、关系、语言和地点事实；如果本次批次有临时主题，优先满足本次主题，再保留这些长期风格。`;
}
function buildCharGenerationPreferenceBatchPrompt(actorIds=[],surface='feed'){
  const blocks=[...new Set((actorIds||[]).map(normalizeSocialActorId).filter(Boolean))].map(id=>getCharGenerationPreferencePrompt(id,surface)).filter(Boolean);
  return blocks.join('\n\n');
}
function composeCharGenerationPrompt({actorIds=[],surface='feed',batchPrompt='',extraPrompt=''}){
  const persistent=buildCharGenerationPreferenceBatchPrompt(actorIds,surface);
  const albumPrompt=typeof buildCharAlbumGenerationPrompt==='function'?buildCharAlbumGenerationPrompt(actorIds,surface):'';
  const temporary=String(batchPrompt||'').trim();
  const tempBlock=temporary?`【本次批次临时偏好】\n${temporary}\n只影响这一次生成的主题/场景；不要把它写回角色长期设定。`:'';
  return [extraPrompt,tempBlock,persistent,albumPrompt].filter(Boolean).join('\n\n');
}

function ensureCharActivePublish(ref){
  const key=charSettingKey(ref);if(!key)return null;
  if(!charActivePublishState[key]){
    charActivePublishState[key]={
      feed:{enabled:false,intervalMin:180,nextAt:0},
      story:{enabled:false,intervalMin:180,nextAt:0},
      reels:{enabled:false,intervalMin:240,nextAt:0},
      updatedAt:0
    };
  }
  const state=charActivePublishState[key];
  ['feed','story','reels'].forEach(surface=>{
    if(!state[surface]||typeof state[surface]!=='object')state[surface]={enabled:false,intervalMin:surface==='reels'?240:180,nextAt:0};
    state[surface].enabled=!!state[surface].enabled;
    state[surface].intervalMin=Math.max(CHAR_ACTIVE_MIN_MINUTES,Number(state[surface].intervalMin)|| (surface==='reels'?240:180));
    state[surface].nextAt=Number(state[surface].nextAt)||0;
  });
  return state;
}
function randomCharActiveInterval(baseMin){
  const base=Math.max(CHAR_ACTIVE_MIN_MINUTES,Number(baseMin)||CHAR_ACTIVE_MIN_MINUTES)*60*1000;
  return Math.round(base + base*CHAR_ACTIVE_JITTER*(Math.random()*2-1));
}
function fmtNextActiveTime(ts){
  if(!ts)return '未排程';
  try{return new Date(ts).toLocaleString('zh-CN',{month:'2-digit',day:'2-digit',hour:'2-digit',minute:'2-digit',hour12:false});}catch(e){return ''+ts;}
}
function closeCharQuickCards(){
  document.getElementById('charActiveFrequencyCard')?.classList.remove('show');
  document.getElementById('charGenerationPreferenceCard')?.classList.remove('show');
  document.getElementById('charDrawerDim')?.classList.remove('show');
}
function renderCharFrequencyNextHint(ref=currentCharProfile){
  const hint=document.getElementById('charFrequencyNextHint');if(!hint)return;
  const state=ensureCharActivePublish(ref);if(!state){hint.textContent='';return;}
  hint.textContent=['feed','story','reels'].map(surface=>{
    const row=state[surface];
    const label=surface==='feed'?'Feed':surface==='story'?'Story':'Reels';
    return `${label}：${row.enabled?fmtNextActiveTime(row.nextAt):'关闭'}`;
  }).join(' · ');
}
function openCharActiveFrequency(){
  if(!currentCharProfile)return;
  closeCharDrawer();
  const state=ensureCharActivePublish(currentCharProfile);
  document.getElementById('charFreqFeedEnabled').checked=state.feed.enabled;
  document.getElementById('charFreqFeedMinutes').value=state.feed.intervalMin;
  document.getElementById('charFreqStoryEnabled').checked=state.story.enabled;
  document.getElementById('charFreqStoryMinutes').value=state.story.intervalMin;
  document.getElementById('charFreqReelsEnabled').checked=state.reels.enabled;
  document.getElementById('charFreqReelsMinutes').value=state.reels.intervalMin;
  renderCharFrequencyNextHint();
  document.getElementById('charDrawerDim')?.classList.add('show');
  document.getElementById('charActiveFrequencyCard')?.classList.add('show');
}
function saveCharActiveFrequency(){
  if(!currentCharProfile)return;
  const state=ensureCharActivePublish(currentCharProfile);
  const now=Date.now();
  const rows={
    feed:{enabled:!!document.getElementById('charFreqFeedEnabled')?.checked,intervalMin:Number(document.getElementById('charFreqFeedMinutes')?.value)||180},
    story:{enabled:!!document.getElementById('charFreqStoryEnabled')?.checked,intervalMin:Number(document.getElementById('charFreqStoryMinutes')?.value)||180},
    reels:{enabled:!!document.getElementById('charFreqReelsEnabled')?.checked,intervalMin:Number(document.getElementById('charFreqReelsMinutes')?.value)||240}
  };
  Object.entries(rows).forEach(([surface,row])=>{
    state[surface].enabled=row.enabled;
    state[surface].intervalMin=Math.max(CHAR_ACTIVE_MIN_MINUTES,row.intervalMin);
    state[surface].nextAt=row.enabled?now+randomCharActiveInterval(state[surface].intervalMin):0;
  });
  state.updatedAt=now;
  saveJSONLocalState(CHAR_ACTIVE_PUBLISH_STORAGE_KEY,charActivePublishState);
  renderCharFrequencyNextHint();
}
function openCharGenerationPreference(){
  if(!currentCharProfile)return;
  closeCharDrawer();
  const pref=ensureCharGenerationPreference(currentCharProfile);
  document.getElementById('charPrefSocial').value=pref.social||'';
  document.getElementById('charPrefPosting').value=pref.posting||'';
  document.getElementById('charPrefVisual').value=pref.visual||'';
  document.getElementById('charDrawerDim')?.classList.add('show');
  document.getElementById('charGenerationPreferenceCard')?.classList.add('show');
}
function saveCharGenerationPreference(){
  if(!currentCharProfile)return;
  const pref=ensureCharGenerationPreference(currentCharProfile);
  pref.social=(document.getElementById('charPrefSocial')?.value||'').trim();
  pref.posting=(document.getElementById('charPrefPosting')?.value||'').trim();
  pref.visual=(document.getElementById('charPrefVisual')?.value||'').trim();
  pref.updatedAt=Date.now();
  saveJSONLocalState(CHAR_GENERATION_PREF_STORAGE_KEY,charGenerationPreferenceState);
  closeCharQuickCards();
}

function closeCharHighlightEditor(){
  document.getElementById('charDrawerDim')?.classList.remove('show');
  document.getElementById('charHighlightEditor')?.classList.remove('show');
}
function openCharHighlightEditor(id){
  if(!currentCharProfile) return;
  clearProfileInteractionOverlays();
  closeHighlightViewer();
  const profile=getCharProfileData(currentCharProfile);
  const isNew=id==='__new';
  const h=isNew?{id:'__new',name:'',coverUrl:'',description:'',storyBinding:null}:(profile.highlights||[]).find(x=>x.id===id);
  if(!h) return;

  charHighlightStoryBindingDraft=new Set(getExistingBoundStoryBatchIds(h.storyBinding));
  charProfileState.editingHighlightId=id;
  closeCharDrawer();
  document.getElementById('charDrawerDim')?.classList.add('show');
  document.getElementById('charHighlightEditor')?.classList.add('show');
  document.getElementById('charHighlightEditorTitle').textContent=isNew?'新建 Highlight':'编辑 Highlight';
  document.getElementById('charHighlightNameInput').value=h.name||'';
  document.getElementById('charHighlightCoverInput').value=h.coverUrl||'';
  hardEnableHighlightTextarea('charHighlightDescriptionInput');
  document.getElementById('charHighlightDescriptionInput').value=h.description||'';
  updateHighlightDescriptionCount('charHighlightDescriptionInput','charHighlightDescriptionCount');
  document.getElementById('charHighlightDeleteBtn').style.visibility=isNew?'hidden':'visible';

  const preview=document.getElementById('charHighlightPreview');
  const url=h.coverUrl||'';
  preview.classList.toggle('has-image',!!url);
  preview.style.backgroundImage=url?`url("${url}")`:'';
  preview.textContent=url?'':(isNew?'＋':'✦');
  renderCharHighlightStoryBinding();
}
function saveCharHighlight(){
  if(!currentCharProfile) return;
  const profile=getCharProfileData(currentCharProfile);
  if(!profile.highlights) profile.highlights=[];

  const id=charProfileState.editingHighlightId;
  const name=document.getElementById('charHighlightNameInput').value.trim()||'未命名';
  const coverUrl=document.getElementById('charHighlightCoverInput').value.trim();
  const description=document.getElementById('charHighlightDescriptionInput').value.trim();
  const owner=resolveCharProfileKey(currentCharProfile);

  if(id==='__new'){
    profile.highlights.push({
      id:'chl_'+Date.now(),
      name,
      coverUrl,
      description,
      storyBinding:makeStoryBindingSnapshot(owner,charHighlightStoryBindingDraft)
    });
  }else{
    const h=profile.highlights.find(x=>x.id===id);
    if(h){
      h.name=name;
      h.coverUrl=coverUrl;
      h.description=description;
      h.storyBinding=makeStoryBindingSnapshot(owner,charHighlightStoryBindingDraft);
    }
  }
  persistNiniStoryLifecycle();
  closeCharHighlightEditor();
  renderCharProfile();
}
function deleteCharHighlight(){
  if(!currentCharProfile) return;
  const profile=getCharProfileData(currentCharProfile);
  const id=charProfileState.editingHighlightId;
  if(id && id!=='__new'){
    profile.highlights=(profile.highlights||[]).filter(x=>x.id!==id);
  }
  persistNiniStoryLifecycle();
  closeCharHighlightEditor();
  renderCharProfile();
}


function openDMChat(name){
  currentDMChat = name;
  renderDMChatStream(name);
  showRoute('routeDMChat');
}

function removeINSSocialMemoryEvents(eventIds=[]){
  const ids=new Set([].concat(eventIds||[]).map(String).filter(Boolean));
  if(!ids.size||typeof insSocialMemoryState==='undefined')return 0;
  const before=insSocialMemoryState.events.length;
  insSocialMemoryState.events=insSocialMemoryState.events.filter(event=>!ids.has(String(event.eventId||'')));
  Object.keys(insSocialMemoryState.knowledge||{}).forEach(actorId=>{insSocialMemoryState.knowledge[actorId]=(insSocialMemoryState.knowledge[actorId]||[]).filter(entry=>!ids.has(String(entry.eventId||'')));});
  if(insSocialMemoryState.events.length!==before)persistINSSocialMemory?.();
  return before-insSocialMemoryState.events.length;
}
function findINSDMMemoryEventForRow(recipient,row){
  if(!row||typeof insSocialMemoryState==='undefined')return null;
  if(row._memoryEventId){const direct=insSocialMemoryState.events.find(event=>String(event.eventId)===String(row._memoryEventId));if(direct)return direct;}
  const recipientId=normalizeSocialActorId(recipient);
  return insSocialMemoryState.events.find(event=>event.type==='dm_message'&&event.contentId===`dm:${recipientId}`&&String(event.text||'')===String(row.text||'')&&Math.abs((Number(event.createdAt)||0)-(Number(row.createdAt)||0))<5)||null;
}
function removeINSDMMemoryForConversation(recipient){
  if(!recipient||typeof insSocialMemoryState==='undefined')return 0;
  const recipientId=normalizeSocialActorId(recipient);
  const ids=insSocialMemoryState.events.filter(event=>{if(event.visibility!=='private'||!String(event.type||'').startsWith('dm_'))return false;if(event.contentId===`dm:${recipientId}`)return true;const actor=normalizeSocialActorId(event.actorId);const targets=(event.targetIds||[]).map(normalizeSocialActorId);return(actor==='user'&&targets.includes(recipientId))||(actor===recipientId&&targets.includes('user'));}).map(event=>event.eventId);
  (dmBaseMessages[recipient]||[]).forEach(row=>{const event=findINSDMMemoryEventForRow(recipient,row);if(event?.eventId)ids.push(event.eventId);});
  return removeINSSocialMemoryEvents(ids);
}
function deleteDMMessageById(recipient,messageId){
  const rows=dmBaseMessages[recipient];if(!Array.isArray(rows))return false;
  const index=rows.findIndex(row=>String(row._uiId||'')===String(messageId||''));if(index<0)return false;
  const event=findINSDMMemoryEventForRow(recipient,rows[index]);if(event?.eventId)removeINSSocialMemoryEvents([event.eventId]);
  rows.splice(index,1);scheduleINSDMHistoryPersistence?.();updateDMListPreview?.(recipient);if(currentDMChat===recipient)renderDMChatStream(recipient);return true;
}
function deleteDMMessageAt(recipient,index){const row=(dmBaseMessages[recipient]||[])[Number(index)];return row?deleteDMMessageById(recipient,row._uiId):false;}
let dmBubbleDeleteTimer=null,dmBubbleDeleteState=null;
function clearDMBubbleDeleteTimer(){if(dmBubbleDeleteTimer){clearTimeout(dmBubbleDeleteTimer);dmBubbleDeleteTimer=null;}dmBubbleDeleteState=null;}
function armDMBubbleDelete(messageId,event){clearDMBubbleDeleteTimer();dmBubbleDeleteState={id:String(messageId||''),x:Number(event?.clientX)||0,y:Number(event?.clientY)||0,pointerId:event?.pointerId};dmBubbleDeleteTimer=setTimeout(()=>{const state=dmBubbleDeleteState;clearDMBubbleDeleteTimer();if(state?.id&&currentDMChat&&confirm('删除这条消息？'))deleteDMMessageById(currentDMChat,state.id);},560);}
const dmChatMessagesEl=document.getElementById('dmChatMessages');
dmChatMessagesEl?.addEventListener('pointerdown',event=>{const row=event.target.closest?.('[data-dm-msg-id]');if(!row||event.button>0)return;armDMBubbleDelete(row.dataset.dmMsgId,event);},{passive:true});
dmChatMessagesEl?.addEventListener('pointermove',event=>{const state=dmBubbleDeleteState;if(!state)return;if(state.pointerId!==undefined&&state.pointerId!==event.pointerId)return;if(Math.hypot((Number(event.clientX)||0)-state.x,(Number(event.clientY)||0)-state.y)>10)clearDMBubbleDeleteTimer();},{passive:true});
['pointerup','pointercancel','pointerleave'].forEach(type=>dmChatMessagesEl?.addEventListener(type,clearDMBubbleDeleteTimer,{passive:true}));
dmChatMessagesEl?.addEventListener('contextmenu',event=>{const row=event.target.closest?.('[data-dm-msg-id]');if(!row)return;event.preventDefault();clearDMBubbleDeleteTimer();if(confirm('删除这条消息？'))deleteDMMessageById(currentDMChat,row.dataset.dmMsgId);});
function deleteFixedNPCFromDMHub(actorId){
  const id=String(actorId||''),actor=typeof getRelationshipActor==='function'?getRelationshipActor(id):null;if(!id||actor?.kind!=='npc')return;if(!confirm(`删除固定联系人 ${actor.name||id}？`))return;
  removeINSDMMemoryForConversation(id);delete dmBaseMessages[id];delete dmSharedCards[id];delete dmProfiles[id];
  if(typeof charProfileState!=='undefined'){delete charProfileState.profiles?.[id];delete charProfileState.boardBatches?.[id];delete charProfileState.boardIndex?.[id];}
  if(typeof relationshipGraphState!=='undefined')relationshipGraphState.edges=(relationshipGraphState.edges||[]).filter(edge=>edge.from!==id&&edge.to!==id);
  document.querySelectorAll(`#dmV2List [data-dm-chat="${CSS.escape(id)}"]`).forEach(row=>row.remove());scheduleINSDMHistoryPersistence?.();renderDMUserNpcHub?.();renderPeopleRelations?.();renderFeedShareDMRecipients?.();renderReelsV15DMRecipients?.();
}


function openRouteSheet(id){
  const el = document.getElementById(id);
  if(el) el.classList.add('show');
}
function closeRouteSheet(id){
  const el = document.getElementById(id);
  if(el) el.classList.remove('show');
}

document.querySelectorAll('[data-route-back]').forEach(btn=>{
  btn.addEventListener('click', backRoute);
});

document.getElementById('instagramLogo')?.addEventListener('click', ()=>{
  if(window.NiniINSHost && typeof window.NiniINSHost.closeApp==='function'){
    window.NiniINSHost.closeApp();
  }
});

const feedGlassDim = document.getElementById('feedGlassDim');
const feedRefreshGlass = document.getElementById('feedRefreshGlass');
const feedNoticeGlass = document.getElementById('feedNoticeGlass');
const feedNoticeDot = document.getElementById('feedNoticeDot');
const insNotificationState=[];
function formatINSNoticeTime(ts){
  const d=Math.max(0,Date.now()-Number(ts||Date.now()));
  if(d<60000)return '刚刚';
  if(d<3600000)return `${Math.max(1,Math.floor(d/60000))} 分钟前`;
  if(d<86400000)return `${Math.max(1,Math.floor(d/3600000))} 小时前`;
  return `${Math.max(1,Math.floor(d/86400000))} 天前`;
}
function pushINSNotification({actorId='',type='activity',copy='',emoji='',targetPostId=''}={}){
  const id=normalizeSocialActorId(actorId);
  if(!id||id==='user'||!isFormalSocialActor(id))return;
  insNotificationState.unshift({id:`n_${Date.now()}_${Math.random().toString(36).slice(2,5)}`,actorId:id,type,copy,emoji,targetPostId,createdAt:Date.now(),read:false});
  if(insNotificationState.length>50)insNotificationState.length=50;
  renderINSNotifications();
}
function renderINSNotifications(){
  const rows=insNotificationState.slice(0,30);
  const make=()=>rows.length?rows.map(row=>{
    const identity=getIdentityMeta(row.actorId);
    const attr=row.type==='dm'?`data-notice-dm="${escapeDMCardHTML(row.actorId)}"`:row.targetPostId?`data-notice-post="${escapeDMCardHTML(row.targetPostId)}"`:'';
    return `<button class="feed-notice-row clickable" ${attr} type="button"><div class="feed-notice-avatar" data-identity-avatar="${escapeDMCardHTML(row.actorId)}">${identity.avatarUrl?'':escapeDMCardHTML(identity.initial)}</div><div class="feed-notice-copy"><strong>${escapeDMCardHTML(identity.name)}</strong> ${escapeDMCardHTML(row.copy||'有新的互动')}<div class="feed-notice-time">${escapeDMCardHTML(formatINSNoticeTime(row.createdAt))}</div></div><div class="feed-notice-side">${row.emoji?escapeDMCardHTML(row.emoji):'<span class="feed-notice-chevron">›</span>'}</div></button>`;
  }).join(''):'<div class="route-empty">暂无新提醒</div>';
  const list=document.getElementById('feedNoticeList'); if(list){list.innerHTML=make();hydrateIdentityAvatars(list);}
  const route=document.getElementById('routeNotificationList'); if(route){route.innerHTML=make();hydrateIdentityAvatars(route);}
  if(feedNoticeDot)feedNoticeDot.style.display=rows.some(x=>!x.read)?'':'none';
}
function markINSNotificationsRead(){insNotificationState.forEach(x=>x.read=true);renderINSNotifications();}
function handleINSNotificationClick(e){
  const dm=e.target.closest('[data-notice-dm]'); if(dm){closeFeedGlass();openDMChat(dm.dataset.noticeDm);return;}
  const post=e.target.closest('[data-notice-post]'); if(post){closeFeedGlass();state.active='home';renderNav();openCommentsForPost(post.dataset.noticePost);}
}
document.getElementById('feedNoticeList')?.addEventListener('click',handleINSNotificationClick);
document.getElementById('routeNotificationList')?.addEventListener('click',handleINSNotificationClick);

function resetFeedPublishActionSelection(){
  document.getElementById('feedCharsPostBtn')?.classList.remove('selected');
  document.getElementById('feedCharsStoryBtn')?.classList.remove('selected');
}
function setFeedPublishActionSelection(type){
  resetFeedPublishActionSelection();
  if(type==='char_post')document.getElementById('feedCharsPostBtn')?.classList.add('selected');
  if(type==='char_story')document.getElementById('feedCharsStoryBtn')?.classList.add('selected');
}
function closeFeedGlass(){
  feedRefreshGlass?.classList.remove('show');
  feedNoticeGlass?.classList.remove('show');
  feedGlassDim?.classList.remove('show');
  resetFeedPublishActionSelection();
}
function openFeedGlass(panel){
  const wasOpen = panel?.classList.contains('show');
  closeFeedGlass();
  if(panel && !wasOpen){
    panel.classList.add('show');
    feedGlassDim?.classList.add('show');
  }
}

document.getElementById('homeRefreshBtn')?.addEventListener('click', ()=>{
  openFeedGlass(feedRefreshGlass);
});

document.getElementById('homeNotificationsBtn')?.addEventListener('click', ()=>{
  renderINSNotifications();
  openFeedGlass(feedNoticeGlass);
  markINSNotificationsRead();
});

feedGlassDim?.addEventListener('click', closeFeedGlass);
document.querySelectorAll('[data-feed-glass-close]').forEach(btn=>{
  btn.addEventListener('click', closeFeedGlass);
});

document.querySelectorAll('[data-story-owner]').forEach(el=>{
  el.addEventListener('click', e=>{
    if(e.target && e.target.id === 'userStoryAddBtn') return;
    openStory(el.dataset.storyOwner);
  });
});
sortStoryStrip();

document.getElementById('userStoryAddBtn')?.addEventListener('click', e=>{
  e.stopPropagation();
  resetStoryCreateDraft();
  showRoute('routeStoryCreate');
});


document.getElementById('storyCreateAddBtn')?.addEventListener('click',openStoryCreateChoice);
document.getElementById('storyCreateDim')?.addEventListener('click',closeStoryCreateChoice);
document.querySelectorAll('[data-story-create-close]').forEach(btn=>btn.addEventListener('click',closeStoryCreateChoice));
document.getElementById('storyCreateAddImage')?.addEventListener('click',()=>addStoryCreateItem('image'));
document.getElementById('storyCreateAddVideo')?.addEventListener('click',()=>addStoryCreateItem('video'));
document.getElementById('storyCreateItems')?.addEventListener('input',e=>{
  const area=e.target.closest('[data-story-create-desc]');
  if(area){
    const index=Number(area.dataset.storyCreateDesc||0);
    if(storyCreateDraft.items[index]) storyCreateDraft.items[index].description=area.value;
    return;
  }

  const urlInput=e.target.closest('[data-story-create-url]');
  if(urlInput){
    const index=Number(urlInput.dataset.storyCreateUrl||0);
    const item=storyCreateDraft.items[index];
    if(item && item.type==='image'){
      item.mediaUrl=urlInput.value.trim();
      item.mediaSource=item.mediaUrl?'url':'';
      item.generatedRequested=false;
      item.fileName='';
    }
  }
});

document.getElementById('storyCreateItems')?.addEventListener('change',e=>{
  const fileInput=e.target.closest('[data-story-create-file]');
  if(fileInput){
    const index=Number(fileInput.dataset.storyCreateFile||0);
    const item=storyCreateDraft.items[index];
    const file=fileInput.files?.[0];
    if(!item || item.type!=='image' || !file)return;

    item.mediaUrl=URL.createObjectURL(file);
    item.mediaSource='upload';
    item.fileName=file.name;
    item.generatedRequested=false;
    renderStoryCreateDraft();
    return;
  }

  const urlInput=e.target.closest('[data-story-create-url]');
  if(urlInput){
    const index=Number(urlInput.dataset.storyCreateUrl||0);
    const item=storyCreateDraft.items[index];
    if(!item || item.type!=='image')return;

    item.mediaUrl=urlInput.value.trim();
    item.mediaSource=item.mediaUrl?'url':'';
    item.generatedRequested=false;
    item.fileName='';
    renderStoryCreateDraft();
  }
});
document.getElementById('storyCreateItems')?.addEventListener('click',async e=>{
  const removeBtn=e.target.closest('[data-story-create-remove]');
  if(removeBtn){
    const index=Number(removeBtn.dataset.storyCreateRemove||0);
    storyCreateDraft.items.splice(index,1);
    renderStoryCreateDraft();
    return;
  }

  const generateBtn=e.target.closest('[data-story-create-generate]');
  if(generateBtn){
    const index=Number(generateBtn.dataset.storyCreateGenerate||0);
    const item=storyCreateDraft.items[index];
    if(!item || item.type!=='image')return;

    const desc=storyCreateDraft.items[index]?.description?.trim() || '';
    if(!desc){
      alert('先填写这张图片的描述，再生图');
      return;
    }

    try{
      const generated=await withINSImageButtonBusy(generateBtn,'生图中…',()=>requestRocheGeneratedImage({description:desc,entry:'story'}));
      item.generatedRequested=true;
      item.generated=true;
      item.mediaSource='generated';
      item.mediaUrl=generated.url;
      item.fileName='';
      renderStoryCreateDraft();
    }catch(error){
      const reason=String(error?.message||error||'未知错误');
      alert(reason==='bridge_not_connected'?'当前没有连接到 Roche 生图接口。请先在 Roche 生图设置里完成配置。':`生图失败：${reason}`);
    }
  }
});
document.getElementById('storyCreateCaption')?.addEventListener('input',e=>{
  storyCreateDraft.caption=e.target.value;
});
document.getElementById('storyCreatePublishBtn')?.addEventListener('click',publishUserStory);

document.getElementById('storyPrevBtn')?.addEventListener('click',e=>{
  e.stopPropagation();
  stepStory(-1);
});
document.getElementById('storyNextBtn')?.addEventListener('click',e=>{
  e.stopPropagation();
  stepStory(1);
});
document.getElementById('storyEmojiLauncher')?.addEventListener('click',e=>{
  e.stopPropagation();
  openStorySheet('storyEmojiSheet');
});
document.getElementById('storyShareBtn')?.addEventListener('click',e=>{
  e.stopPropagation();
  openStoryShareSheet();
});
document.getElementById('storyShareList')?.addEventListener('click',e=>{
  const btn=e.target.closest('[data-story-share-recipient]');
  if(!btn)return;
  sendStoryBatchToDM(btn.dataset.storyShareRecipient);
});

document.getElementById('storyHeartBtn')?.addEventListener('click',e=>{
  e.stopPropagation();
  if(storyViewerState.owner==='user'){
    openOwnStoryReactions();
    return;
  }
  const state=getStoryBatchState(storyViewerState.owner);
  state.liked=!state.liked;
  if(state.liked) sendStoryReaction('❤️');
  renderStoryViewer();
});
document.getElementById('storyEditBtn')?.addEventListener('click',e=>{
  e.stopPropagation();
  openStoryEditor();
});
document.getElementById('storyDeleteBtn')?.addEventListener('click',e=>{
  e.stopPropagation();
  if(!storyViewerState.owner)return;
  openStorySheet('storyDeleteSheet');
});
document.getElementById('storyDeleteCancelBtn')?.addEventListener('click',closeStorySheets);
document.getElementById('storyDeleteConfirmBtn')?.addEventListener('click',()=>{
  deletePublishedStory(storyViewerState.owner);
});
document.getElementById('storyOverlayDim')?.addEventListener('click',closeStorySheets);
document.querySelectorAll('[data-story-sheet-close]').forEach(btn=>{
  btn.addEventListener('click',closeStorySheets);
});
document.querySelectorAll('[data-story-reaction]').forEach(btn=>{
  btn.addEventListener('click',e=>{
    e.stopPropagation();
    sendStoryReaction(btn.dataset.storyReaction);
    closeStorySheets();
  });
});
document.getElementById('storyOwnReactionPlanBtn')?.addEventListener('click',()=>{
  void ensureStoryAudienceReactionPlan({trigger:'manual',silent:false});
});
document.getElementById('storyEditChipRow')?.addEventListener('click',e=>{
  const btn=e.target.closest('[data-story-edit-item]');
  if(!btn) return;
  storyEditState.editIndex=Number(btn.dataset.storyEditItem||0);
  renderStoryEditSheet();
});
document.getElementById('storyEditMoveUp')?.addEventListener('click',()=>moveStoryEditItem(-1));
document.getElementById('storyEditMoveDown')?.addEventListener('click',()=>moveStoryEditItem(1));
document.getElementById('storyEditAddImage')?.addEventListener('click',()=>addStoryEditItem('image'));
document.getElementById('storyEditAddVideo')?.addEventListener('click',()=>addStoryEditItem('video'));
document.getElementById('storyEditDeleteItem')?.addEventListener('click',deleteCurrentStoryEditItem);
document.getElementById('storyEditLinkAlbum')?.addEventListener('click',()=>{
  const item=getCurrentStoryItems()[storyEditState.editIndex];
  if(!item || item.type==='video') return;
  if(!isRocheCharacterHandle(storyViewerState.owner)) return;
  item.linkedAlbumId=document.getElementById('storyEditAlbumSelect')?.value || '';
  item.generated=false;
  renderStoryEditSheet();
  renderStoryViewer();
});
document.getElementById('storyEditGenerateImage')?.addEventListener('click',async ()=>{
  const item=getCurrentStoryItems()[storyEditState.editIndex];
  if(!item || item.type==='video') return;
  const desc=document.getElementById('storyEditDescription')?.value.trim() || item.description || '';
  if(!desc){alert('先写图片描述再生图');return;}
  const btn=document.getElementById('storyEditGenerateImage');
  try{
    const generated=await withINSImageButtonBusy(btn,'生图中…',()=>requestRocheGeneratedImage({description:desc,entry:'story'}));
    item.description=desc;
    item.generated=true;
    item.generatedRequested=true;
    item.linkedAlbumId='';
    item.mediaSource='generated';
    item.mediaUrl=generated.url;
    item.fileName='';
    renderStoryEditSheet();
    renderStoryViewer();
  }catch(error){
    const reason=String(error?.message||error||'未知错误');
    alert(reason==='bridge_not_connected'?'当前没有连接到 Roche 生图接口。请先在 Roche 生图设置里完成配置。':`生图失败：${reason}`);
  }
});
document.getElementById('storyEditSaveBtn')?.addEventListener('click',()=>{
  const owner=storyViewerState.owner;
  const item=getCurrentStoryItems()[storyEditState.editIndex];
  if(!owner || !item) return;
  item.description=document.getElementById('storyEditDescription')?.value.trim() || item.description;
  const captionInput=document.getElementById('storyEditCaption');
  if(captionInput)getStoryBatchState(owner).caption=captionInput.value.trim();
  invalidateStoryReactionPlan(owner);
  syncCurrentStoryBatchToArchive(owner);
  persistNiniStoryLifecycle();
  renderStoryViewer();
  if(typeof syncHomeStoryStrip==='function')syncHomeStoryStrip({skipExpiry:true});
  closeStorySheets();
});

document.getElementById('charProfileMessage')?.addEventListener('click', ()=>{
  if(currentCharProfile) openDMChat(currentCharProfile);
});

document.getElementById('charProfileFollow')?.addEventListener('click', function(){
  this.textContent = this.textContent === '已关注' ? '关注' : '已关注';
});

document.getElementById('socialCircleBtn')?.addEventListener('click', ()=>{
  showRoute('routeSocialCircle');
});

document.querySelectorAll('[data-dm-chat]').forEach(row=>{
  row.addEventListener('click', e=>{
    if(e.target.closest('[data-char-profile]')) return;
    openDMChat(row.dataset.dmChat);
  });
});

document.querySelectorAll('[data-char-profile]').forEach(avatar=>{
  avatar.addEventListener('click', e=>{
    e.stopPropagation();
    openCharProfile(avatar.dataset.charProfile);
  });
});

document.getElementById('strangerMessagesRow')?.addEventListener('click', ()=>{
  alert('陌生消息页：默认空；满足条件后手动刷新生成 3–5 条消息请求。');
});


/* ===== v1.03 comments: smooth sheet + translation + user posting ===== */
let activeCommentsPostId = 'p1';
let commentReplyTo = null;

const commentsByPost = {
p_rec_food:[
{author:'baozi.afterwork',displayName:'下班吃什么',text:'等一下 那个白草莓真的甜吗',translation:'',sourceLanguage:'zh',generated:true},
{author:'chaeon.zip',displayName:'채온',text:'저 빵 한입 먹으면 크림 옆으로 다 나올 것 같아 ㅋㅋ',translation:'那个面包感觉咬一口奶油就会从旁边全挤出来ㅋㅋ',sourceLanguage:'ko',generated:true},
{author:'yuu_tabememo',displayName:'ゆう',text:'白い苺はじめて見た、味ふつうの苺と違う？',translation:'第一次见白草莓，味道和普通草莓不一样吗？',sourceLanguage:'ja',generated:true},
{author:'lateplate.jpg',displayName:'Late Plate',text:'wait are those white strawberries??',translation:'等等，那些是白草莓吗？？',sourceLanguage:'en',generated:true},
{author:'nineteenfloor',displayName:'十九楼',text:'咖啡一般但面包会回购 这句太真实了',translation:'',sourceLanguage:'zh',generated:true}],
p_rec_idol:[
{author:'minseo.zip',displayName:'민서',text:'아니 일단 물부터 마셔 ㅋㅋ 오늘 진짜 수고했어',translation:'不是，先喝水啦ㅋㅋ 今天真的辛苦了',sourceLanguage:'ko',generated:true},
{author:'winterpage_',displayName:'Winter Page',text:'you posted before even changing out of the stage clothes 😭',translation:'你居然连舞台服都没换就先发了😭',sourceLanguage:'en',generated:true},
{author:'nana_082',displayName:'ナナ',text:'イヤモニそのまま置いてあるのリアルすぎる笑',translation:'耳返就那样放着也太真实了笑',sourceLanguage:'ja',generated:true},
{author:'lilacroom',displayName:'紫房间',text:'评论区明天还在 你先去睡吧',translation:'',sourceLanguage:'zh',generated:true},
{author:'studio_han',displayName:'한 스튜디오',text:'오늘 엔딩 멘트 살짝 꼬인 거 나만 들었나 ㅋㅋ',translation:'今天结尾那句稍微说卡了一下，只有我听到了吗ㅋㅋ',sourceLanguage:'ko',generated:true}],
p_rec_swiss:[
{author:'ellie.wav',displayName:'Ellie',text:'I would 100% miss breakfast just staring out that window',translation:'换我绝对会因为盯着这个窗景看而错过早餐',sourceLanguage:'en',generated:true},
{author:'noah.unterwegs',displayName:'Noah unterwegs',text:'Bin neidisch. Hier seit drei Tagen nur Regen 😭',translation:'羡慕了，我这里已经连续下三天雨😭',sourceLanguage:'de',generated:true},
{author:'haru.route',displayName:'하루',text:'나였으면 산책 안 나가고 침대에 있었을 듯 ㅋㅋ',translation:'换我大概不会去散步，直接躺床上了ㅋㅋ',sourceLanguage:'ko',generated:true},
{author:'pocketcloud',displayName:'口袋云',text:'这种窗景我会先坐着发呆半小时',translation:'',sourceLanguage:'zh',generated:true},
{author:'claire.enroute',displayName:'Claire en route',text:'Tu avais pris quelle ligne pour arriver là ?',translation:'你是坐哪条线到这里的？',sourceLanguage:'fr',generated:true}]
};
const feedCommentVisibleCountByPost=Object.create(null);

function escapeCommentHTML(value){
  return String(value ?? '')
    .replaceAll('&','&amp;')
    .replaceAll('<','&lt;')
    .replaceAll('>','&gt;')
    .replaceAll('"','&quot;')
    .replaceAll("'",'&#039;');
}

function renderComments(postId, focusAuthor=''){
  activeCommentsPostId = postId || activeCommentsPostId || 'p1';
  const list = document.getElementById('commentList');
  const allRows = commentsByPost[activeCommentsPostId] || [];
  const storedVisible=Number(feedCommentVisibleCountByPost[activeCommentsPostId]);
  const visibleCount=Number.isFinite(storedVisible)&&storedVisible>0?storedVisible:Math.min(5,allRows.length);
  const rows=allRows.slice(0,visibleCount);

  list.innerHTML = rows.map((c,index)=>{
    const focus = focusAuthor && c.author === focusAuthor ? ' focus' : '';
    const identity=getIdentityMeta(c.author);
    const handle=identity.handle||String(c.author||'public');
    const hasTranslation = shouldOfferTranslation(c.text,c.translation);
    return `
      <div class="comment-row${focus}" data-comment-index="${index}">
        <div class="comment-avatar" data-identity-avatar="${escapeCommentHTML(c.author)}">${identity.avatarUrl?'':escapeCommentHTML(identity.initial)}</div>
        <div class="comment-copy" style="flex:1;min-width:0">
          <div class="comment-author-line">
            <strong>${escapeCommentHTML(identity.name||c.displayName||handle)}</strong>
            <span class="comment-handle">@${escapeCommentHTML(handle)}</span>
          </div>
          <div>${escapeCommentHTML(c.text)}</div>
          <div class="comment-actions">
            <button class="comment-mini-btn" data-comment-reply="${index}" type="button">回复</button>
            ${hasTranslation
              ? `<button class="comment-mini-btn" data-comment-translate="${index}" type="button">查看翻译</button>`
              : ''}
            <button class="comment-mini-btn delete" data-comment-delete="${index}" type="button">删除</button>
          </div>
          ${hasTranslation
            ? `<div class="comment-translation" data-comment-translation-box="${index}" hidden>${escapeCommentHTML(c.translation)}</div>`
            : ''}
        </div>
      </div>`;
  }).join('');

  hydrateIdentityAvatars(list);
  const loadMoreButton=document.getElementById('commentLoadMoreBtn');
  if(loadMoreButton){
    loadMoreButton.hidden=!activeCommentsPostId;
    loadMoreButton.textContent='再加载 5 条评论';
  }

  if(!rows.length){
    list.innerHTML = '<div style="padding:28px 0;text-align:center;color:#8b8f96;font-size:13px">还没有评论，点右上角刷新生成 5 条。</div>';
  }

  if(focusAuthor){
    requestAnimationFrame(()=>{
      list.querySelector('.comment-row.focus')?.scrollIntoView({block:'center',behavior:'smooth'});
    });
  }
}

function openCommentsForPost(postId, focusAuthor=''){
  commentReplyTo = null;
  const input = document.getElementById('commentInput');
  if(input) input.placeholder = '添加评论…';

  renderComments(postId, focusAuthor);
  openRouteSheet('sheetComments');
}

document.getElementById('commentList')?.addEventListener('click', e=>{
  const deleteBtn=e.target.closest('[data-comment-delete]');
  if(deleteBtn){
    const index=Number(deleteBtn.dataset.commentDelete);
    const rows=commentsByPost[activeCommentsPostId]||[];
    if(Number.isInteger(index) && rows[index]){
      rows.splice(index,1);
      const p=posts.find(post=>post.id===activeCommentsPostId);
      if(p)p.comments=Math.max(0,(Number(p.comments)||0)-1);
      renderComments(activeCommentsPostId);
      const feed=document.getElementById('feed');
      const oldScroll=feed?feed.scrollTop:0;
      renderFeed();
      if(feed)feed.scrollTop=oldScroll;
    }
    return;
  }

  const translateBtn = e.target.closest('[data-comment-translate]');
  if(translateBtn){
    const index = translateBtn.dataset.commentTranslate;
    const box = document.querySelector(`[data-comment-translation-box="${index}"]`);
    if(box){
      const willShow = box.hidden;
      box.hidden = !willShow;
      translateBtn.textContent = willShow ? '收起翻译' : '查看翻译';
    }
    return;
  }

  const replyBtn = e.target.closest('[data-comment-reply]');
  if(replyBtn){
    const index = Number(replyBtn.dataset.commentReply);
    const c = (commentsByPost[activeCommentsPostId] || [])[index];
    if(c){
      commentReplyTo = c.author;
      const input = document.getElementById('commentInput');
      input.placeholder = '回复 ' + getIdentityMeta(c.author).name + '…';
      input.focus();
    }
  }
});

document.getElementById('commentSendBtn')?.addEventListener('click', ()=>{
  const input = document.getElementById('commentInput');
  const text = input.value.trim();
  if(!text) return;

  if(!commentsByPost[activeCommentsPostId]){
    commentsByPost[activeCommentsPostId] = [];
  }

  const replyTargetBeforeSend=commentReplyTo;

  commentsByPost[activeCommentsPostId].push({
    author:'user',
    text: replyTargetBeforeSend ? '@' + replyTargetBeforeSend + ' ' + text : text,
    translation:'',
    generated:false,
    createdAt:Date.now()
  });
  feedCommentVisibleCountByPost[activeCommentsPostId]=commentsByPost[activeCommentsPostId].length;

  const p = posts.find(post=>post.id === activeCommentsPostId);
  if(p) p.comments = Number(p.comments || 0) + 1;

  if(p && typeof recordSocialEvent==='function'){
    const ownerId=normalizeSocialActorId(p.author);
    const replyId=normalizeSocialActorId(replyTargetBeforeSend||'');
    const known=['user'];
    if(isFormalSocialActor(ownerId))known.push(ownerId);
    if(replyId && isFormalSocialActor(replyId))known.push(replyId);

    recordSocialEvent({
      type:'feed_comment',
      actorId:'user',
      targetIds:known.filter(id=>id!=='user'),
      contentId:p.id,
      contentType:'feed',
      contentOwnerId:ownerId,
      threadId:`feed_comments:${p.id}`,
      replyToActorId:replyId,
      text,
      visibility:'public',
      summary:`${socialActorName('user')}在 ${socialActorName(ownerId)} 的 IG 动态下${replyId?`回复了 ${socialActorName(replyId)}`:'评论'}：“${text}”`
    },{knownActorIds:known,learnedVia:'direct_comment'});
  }

  input.value = '';
  input.placeholder = '添加评论…';
  commentReplyTo = null;

  // Keep the comment sheet open while updating the count under the Feed card.
  const feed = document.getElementById('feed');
  const oldScroll = feed ? feed.scrollTop : 0;
  renderFeed();
  if(feed) feed.scrollTop = oldScroll;

  renderComments(activeCommentsPostId);

  requestAnimationFrame(()=>{
    document.getElementById('commentList')?.lastElementChild?.scrollIntoView({
      block:'nearest',
      behavior:'smooth'
    });
  });
});

document.getElementById('commentInput')?.addEventListener('keydown', e=>{
  if(e.key === 'Enter' && !e.shiftKey){
    e.preventDefault();
    document.getElementById('commentSendBtn')?.click();
  }
});

function setNiniRefreshDotsBusy(btn,busy){
  if(!btn)return;
  if(busy){
    if(!btn.dataset.niniIdleHtml)btn.dataset.niniIdleHtml=btn.innerHTML;
    btn.disabled=true;
    btn.classList.add('nini-busy');
    btn.innerHTML='<span class="nini-refresh-dots" aria-hidden="true"><i></i><i></i><i></i></span>';
  }else{
    btn.disabled=false;
    btn.classList.remove('nini-busy');
    if(btn.dataset.niniIdleHtml){
      btn.innerHTML=btn.dataset.niniIdleHtml;
      delete btn.dataset.niniIdleHtml;
    }
  }
}

document.getElementById('commentRefreshBtn')?.addEventListener('click', async ()=>{
  const btn=document.getElementById('commentRefreshBtn');
  if(!activeCommentsPostId || btn?.disabled)return;
  const postId=activeCommentsPostId;
  setNiniRefreshDotsBusy(btn,true);
  try{
    await generateNextPublicCommentBatch('feed',postId,5);
    feedCommentVisibleCountByPost[postId]=(commentsByPost[postId]||[]).length;
    renderComments(postId);
  }catch(error){
    renderComments(postId);
    alert('新增 IG 评论失败：'+(error?.message||error));
  }finally{
    setNiniRefreshDotsBusy(btn,false);
  }
});

document.getElementById('commentLoadMoreBtn')?.addEventListener('click',async ()=>{
  const btn=document.getElementById('commentLoadMoreBtn');
  if(!activeCommentsPostId || btn?.disabled)return;
  const postId=activeCommentsPostId;
  btn.disabled=true;
  btn.textContent='生成中…';
  try{
    await generateNextPublicCommentBatch('feed',postId,5);
    feedCommentVisibleCountByPost[postId]=(commentsByPost[postId]||[]).length;
    renderComments(postId);
  }catch(error){
    alert('加载更多 IG 评论失败：'+(error?.message||error));
  }finally{
    btn.disabled=false;
    btn.textContent='再加载 5 条评论';
  }
});


document.addEventListener('click', e=>{
  const profileTarget = e.target.closest('[data-post-profile]');
  if(profileTarget){
    const type = profileTarget.dataset.postType;
    const name = profileTarget.dataset.postProfile;
    if(type === 'char' || type === 'npc'){
      openCharProfile(name);
    }else{
      const postEl=profileTarget.closest('.post');
      openProfileContentDetail(name,'feed',postEl?.id||'');
    }
    return;
  }

  const commentsTarget = e.target.closest('[data-comments-open]');
  if(commentsTarget){
    openCommentsForPost(commentsTarget.dataset.commentsOpen);
    return;
  }

  const shareTarget = e.target.closest('[data-share-dm]');
  if(shareTarget){
    activeSharePostId = shareTarget.dataset.shareDm;
    renderFeedShareDMRecipients();
    openRouteSheet('sheetShareDM');
    return;
  }
});

['sheetComments','sheetShareDM'].forEach(id=>{
  const el = document.getElementById(id);
  el?.addEventListener('click', e=>{
    if(e.target === el) closeRouteSheet(id);
  });
});

document.querySelectorAll('.char-chip').forEach(btn=>{
  btn.addEventListener('click', ()=>btn.classList.toggle('on'));
});


/* ===== INS-v5.0 Feed generation: Roche WebView-safe capture delegation ===== */
function getSelectedFeedChars(){
  return [...document.querySelectorAll('.feed-char-chip.on')].map(el=>el.dataset.feedChar).filter(Boolean);
}
function setINSGenerationButtonBusy(btn,busy,busyText='',idleText=''){
  if(!btn)return;
  if(busy){
    btn.dataset.idleText=btn.textContent;
    btn.disabled=true;
    btn.classList.add('busy');
    btn.classList.remove('selected');
    if(busyText)btn.textContent=busyText;
  }else{
    btn.disabled=false;
    btn.classList.remove('busy');
    btn.textContent=idleText||btn.dataset.idleText||btn.textContent;
  }
}
function resolveGeneratedActorId(row,selected,index=0){
  const raw=String(row?.actorId||row?.authorId||row?.author||'').replace(/^@/,'');
  if(raw){
    const hit=(selected||[]).find(id=>{const meta=getIdentityMeta(id);return id===raw||meta.handle===raw||meta.name===raw;});
    if(hit)return hit;
  }
  return (selected||[])[index%(selected||[]).length]||'';
}
function registerGeneratedPublicIdentity(row,index=0){
  const actor=row?.actor||{};
  const handle=String(actor.handle||row?.author||row?.handle||`public_${Date.now()}_${index}`).replace(/^@/,'');
  const name=String(actor.displayName||actor.name||row?.displayName||handle);
  publicIdentityRegistry[handle]={handle,name,initial:String(actor.initial||name||'?').slice(0,1).toUpperCase(),avatarUrl:STRANGER_AVATAR_URL,language:String(actor.language||row?.sourceLanguage||''),region:String(actor.region||''),location:String(actor.location||row?.location||''),bio:String(actor.bio||'')};
  return handle;
}
function getGeneratedItems(data){
  if(Array.isArray(data))return data;
  if(Array.isArray(data?.items))return data.items;
  if(Array.isArray(data?.posts))return data.posts;
  if(Array.isArray(data?.results))return data.results;
  if(data?.item)return [data.item];
  if(data?.post)return [data.post];
  return [];
}

/* v1.07.9 Home Feed generation reader.
   The main Feed generation call returns a Simplified-Chinese caption plus the
   intended source language. Original-language text is created later, only on
   demand, through the separately configured Feed translation API. */
function normalizeHomeFeedGeneratedCaption(row={}){
  const captionObject=row?.caption && typeof row.caption==='object'?row.caption:{};
  const translationObject=row?.translation && typeof row.translation==='object'?row.translation:{};
  const captionValue=typeof row?.caption==='string'?row.caption:'';
  const translationValue=typeof row?.translation==='string'?row.translation:'';
  const firstText=(...values)=>{
    for(const value of values){
      if((typeof value==='string'||typeof value==='number') && String(value).trim())return String(value).trim();
    }
    return '';
  };
  const chinese=firstText(
      row?.captionZh,row?.caption_zh,row?.translationZh,row?.translation_zh,
      row?.chineseTranslation,row?.textTranslation,row?.text_translation,translationValue,
      captionObject?.translation,captionObject?.zh,translationObject?.zh,
      translationObject?.chinese,translationObject?.text,
      row?.captionOriginal,row?.caption_original,row?.originalCaption,row?.original_caption,
      row?.textOriginal,row?.text_original,captionValue,row?.text,
      captionObject?.original,captionObject?.text,captionObject?.source
    );
  return {
    rawCaptionOriginal:chinese,
    captionOriginal:stripGeneratedCaptionHashtags(chinese).trim(),
    captionTranslation:'',
    sourceLanguage:firstText(row?.sourceLanguage,row?.source_language,row?.language,row?.actor?.language,row?.actor?.sourceLanguage)
  };
}

function generatedFeedRowNeedsTranslation(){return false;}

async function callNiniINSFeedSinglePass(task,payload={}){
  return callNiniINSAI(task,payload);
}

function makeGeneratedReelItem(row,actorId,kind='char',index=0){
  const bilingual=normalizeINSBilingualText({
    textOriginal:row?.captionOriginal??row?.caption??row?.textOriginal??row?.text??'',
    textTranslation:row?.captionTranslation??row?.translation??row?.textTranslation??'',
    sourceLanguage:row?.sourceLanguage
  });
  const metrics=row?.metrics||{};
  const initialComments=Array.isArray(row?.initialComments)?row.initialComments:[];
  const tags=normalizeGeneratedEnglishTags(row?.tags||[]);
  const videoDescription=String(
    row?.videoDescription??
    row?.mediaDescription??
    row?.content?.videoDescription??
    row?.content?.mediaDescription??
    row?.content??
    '随手短视频'
  ).trim()||'随手短视频';

  const identitySnapshot=kind==='stranger'?cloneINSDMJSON(getIdentityMeta(actorId),{}):null;
  return {
    id:`reel_ai_${Date.now()}_${index}_${Math.random().toString(36).slice(2,6)}`,
    author:actorId,
    initial:getIdentityMeta(actorId).initial||String(actorId||'?').slice(0,1).toUpperCase(),
    kind,
    identitySnapshot,
    followed:kind!=='stranger',
    liked:false,
    reposted:false,
    translated:false,
    likes:Math.max(0,Number(metrics.likes??row?.likes??0)||0),
    comments:Math.max(initialComments.length,Number(metrics.comments??row?.comments??0)||0),
    reposts:Math.max(0,Number(metrics.reposts??row?.reposts??0)||0),
    shares:Math.max(0,Number(metrics.shares??row?.shares??row?.dmShares??0)||0),
    videoDescription,
    caption:bilingual.textOriginal||'',
    captionTranslation:bilingual.textTranslation||'',
    sourceLanguage:bilingual.sourceLanguage||'',
    location:normalizeGeneratedLocationLabel(row?.location||row?.actor?.location||'',bilingual.sourceLanguage,bilingual.textOriginal,row?.actor?.region||row?.region||''),
    tags,
    generated:true,
    createdAt:Date.now()
  };
}

function recordGeneratedReelPublish(actorId,item,{knownByUser=true}={}){
  if(!actorId||!item||typeof recordSocialEvent!=='function')return;
  const caption=compactMemoryText(item.caption||'',160);
  const video=compactMemoryText(item.videoDescription||'',180);
  const mentionMeta=detectFormalActorMentions(`${item.caption||''} ${item.videoDescription||''}`);
  if(!isFormalSocialActor(actorId) && !mentionMeta.length)return;
  const actorName=socialActorName(actorId);
  const summary=[
    `${actorName}发布了一条 Reel。`,
    caption?`文案：“${caption}”`:'',
    video?`视频：${video}`:''
  ].filter(Boolean).join(' ');

  const known=[];
  if(isFormalSocialActor(actorId))known.push(actorId);
  if(knownByUser && isFormalSocialActor(actorId))known.push('user');

  recordSocialEvent({
    type:'reel_publish',
    actorId,
    targetIds:[],
    contentId:item.id,
    contentType:'reel',
    contentOwnerId:actorId,
    relatedActorIds:mentionMeta.map(row=>row.actorId),
    text:item.caption||'',
    visibility:'public',
    payload:{
      mentionMeta,
      videoDescription:item.videoDescription||'',
      caption:item.caption||'',
      captionTranslation:item.captionTranslation||'',
      tags:Array.isArray(item.tags)?item.tags:[],
      location:item.location||'',
      metrics:{
        likes:Number(item.likes)||0,
        comments:Number(item.comments)||0,
        reposts:Number(item.reposts)||0,
        shares:Number(item.shares)||0
      },
      generated:true
    },
    summary
  },{knownActorIds:known,learnedVia:'current_reels'});
}

function normalizeGeneratedMetric(value,min,max){
  const n=Number(value);
  if(Number.isFinite(n) && n>0)return Math.max(Number(min)||0,Math.round(n));
  return min + Math.floor(Math.random()*(Math.max(min,max)-min+1));
}

function stripGeneratedCaptionHashtags(text=''){
  return String(text||'').replace(/(^|\s)#[^\s#]+/g,'$1').replace(/\s{2,}/g,' ').trim();
}
function normalizeGeneratedEnglishTags(rawTags=[]){
  const rows=Array.isArray(rawTags)?rawTags:[];
  return [...new Set(rows.map(tag=>String(tag||'').trim()).filter(Boolean).map(tag=>tag.startsWith('#')?tag:'#'+tag).filter(tag=>/^#[A-Za-z0-9_.-]+$/.test(tag)))];
}
function inferGeneratedEnglishLocation(sourceLanguage='',text='',region=''){
  const lang=String(sourceLanguage||'').toLowerCase();
  const sample=(String(text||'')+' '+String(region||'')).toLowerCase();
  if(/hong\s*kong|香港/.test(sample))return 'Hong Kong·China';
  if(/macau|macao|澳门|澳門/.test(sample))return 'Macau·China';
  if(/taiwan|台湾|台灣|traditional/.test(sample))return 'Taiwan·China';
  if(/korean|ko\b|kr|한국|韩/.test(lang) || /[가-힣]/.test(text))return 'Seoul·Korea';
  if(/japanese|ja\b|jp|日本|日语/.test(lang) || /[ぁ-んァ-ヶー]/.test(text))return 'Tokyo·Japan';
  if(/thai|th\b|泰/.test(lang))return 'Bangkok·Thailand';
  if(/vietnamese|vi\b|越/.test(lang))return 'Ho Chi Minh City·Vietnam';
  if(/indonesian|id\b|印尼/.test(lang))return 'Jakarta·Indonesia';
  if(/french|fr\b|法/.test(lang))return 'Paris·France';
  if(/german|de\b|德/.test(lang))return 'Berlin·Germany';
  if(/spanish|es\b|西/.test(lang))return 'Madrid·Spain';
  if(/portuguese|pt\b|葡/.test(lang))return 'Lisbon·Portugal';
  if(/australia|australian/.test(sample))return 'Sydney·Australia';
  if(/united kingdom|\buk\b|britain|british/.test(sample))return 'London·UK';
  if(/canada|canadian/.test(sample))return 'Toronto·Canada';
  if(/english|en\b|英/.test(lang))return 'New York·USA';
  if(/chinese|zh\b|中文|汉语|漢語/.test(lang) || /[\u4e00-\u9fff]/.test(text))return 'Shanghai·China';
  return '';
}
function normalizeGeneratedLocationLabel(value='',sourceLanguage='',text='',region=''){
  const regionText=String(region||'');
  if(/hong\s*kong|香港/i.test(regionText))return 'Hong Kong·China';
  if(/macau|macao|澳门|澳門/i.test(regionText))return 'Macau·China';
  if(/taiwan|台湾|台灣/i.test(regionText))return 'Taiwan·China';
  let raw=String(value||'').trim();
  if(raw){
    raw=raw.replace(/\s*[•|]\s*/g,'·').replace(/,\s*/g,'·').replace(/\s*·\s*/g,'·');
    raw=raw.replace(/·(?:South\s+Korea|Republic\s+of\s+Korea)$/i,'·Korea');
    if(/·Taiwan$/i.test(raw))return 'Taiwan·China';
    if(/^(?:Hong\s+Kong)(?:·.*)?$/i.test(raw))return 'Hong Kong·China';
    if(/^(?:Macau|Macao)(?:·.*)?$/i.test(raw))return 'Macau·China';
    if(/[\u3400-\u9fff\u3040-\u30ff\uac00-\ud7af\u0e00-\u0e7f]/.test(raw))raw='';
  }
  return raw || inferGeneratedEnglishLocation(sourceLanguage,text,region);
}
function splitGeneratedCaptionText(text=''){
  const clean=String(text||'').replace(/\s+/g,' ').trim();
  if(!clean)return {short:'',long:''};
  const previewLimit=32;
  if(clean.length<=previewLimit)return {short:clean,long:''};
  const marks=['。','！','？','…','~','.','!','?','，',',',' '];
  let cut=-1;
  for(const mark of marks){
    const pos=clean.lastIndexOf(mark,previewLimit);
    if(pos>cut)cut=pos;
  }
  if(cut<14)cut=previewLimit-1;
  const short=clean.slice(0,cut+1).trim();
  const long=clean.slice(cut+1).trim();
  return {short:short||clean,long};
}
function normalizeGeneratedMediaItems(row={}){
  const raw=Array.isArray(row?.mediaItems)
    ? row.mediaItems
    : Array.isArray(row?.mediaDescriptions)
      ? row.mediaDescriptions.map(item=>typeof item==='string'?{mediaDescription:item}:item)
      : [];

  const trustedInlineUrl=value=>{
    const url=String(value||'').trim();
    return /^(data:image\/|blob:)/i.test(url)?url:'';
  };

  const items=raw.map((item,idx)=>{
      if(typeof item==='string')return {id:`generated_media_${idx}`,url:'',desc:String(item).trim(),source:'description'};
      const desc=String(item?.mediaDescription??item?.description??item?.desc??'').trim();
      const url=trustedInlineUrl(item?.mediaUrl??item?.url??'');
      if(!desc && !url)return null;
      return {id:String(item?.id||`generated_media_${idx}`),url,desc:desc||'生活随手记录',source:url?'generated':'description',albumCategory:String(item?.albumCategory||item?.category||''),albumTags:Array.isArray(item?.albumTags)?item.albumTags:[],albumEraId:String(item?.albumEraId||item?.eraId||'')};
    })
    .filter(Boolean)
    .slice(0,4);

  if(items.length)return items;
  const fallbackDesc=String(row?.mediaDescription||row?.content?.mediaDescription||row?.content||'生活随手记录').trim() || '生活随手记录';
  return [{id:'generated_media_0',url:trustedInlineUrl(row?.mediaUrl||''),desc:fallbackDesc,source:'description'}];
}

function makeGeneratedFeedPost(row,actorId,type,index=0){
  const caption=normalizeHomeFeedGeneratedCaption(row);
  const sourceLanguage=String(caption.sourceLanguage||row?.actor?.language||publicIdentityRegistry?.[actorId]?.language||'zh').trim()||'zh';
  const metrics=row?.metrics||{};
  const explicitTags=normalizeGeneratedEnglishTags(row?.tags||[]);
  const inlineTags=normalizeGeneratedEnglishTags((String(caption.rawCaptionOriginal||'').match(/(^|\s)#[^\s#]+/g)||[]).map(tag=>String(tag||'').trim()));
  const tags=[...new Set([...explicitTags,...inlineTags])];
  if(!tags.length)tags.push('#daily');
  const cleanCaption=caption.captionOriginal;
  const parts=splitGeneratedCaptionText(cleanCaption);
  const initialComments=Array.isArray(row?.initialComments)?row.initialComments:(Array.isArray(row?.comments)?row.comments:[]);
  const id=`feed_ai_${Date.now()}_${index}_${Math.random().toString(36).slice(2,5)}`;
  const actorRegion=String(row?.actor?.region||row?.region||row?.country||'').trim();
  const locationLabel=normalizeGeneratedLocationLabel(row?.location||row?.actor?.location||'',sourceLanguage,cleanCaption,actorRegion);
  let mediaItems=normalizeGeneratedMediaItems(row);
  mediaItems=typeof resolveGeneratedMediaItemsWithAlbum==='function'?resolveGeneratedMediaItemsWithAlbum(actorId,mediaItems):mediaItems;
  const primaryMedia=mediaItems[0]||{url:'',desc:'生活随手记录',source:'description'};
  const post={
    id,type,followed:type!=='stranger',author:actorId,avatar:getIdentityMeta(actorId).initial,sub:locationLabel,
    mediaClass:`m${1+(index%4)}`,mediaType:'image',
    mediaText:String(primaryMedia.desc||'生活随手记录'),
    mediaDescription:String(primaryMedia.desc||'生活随手记录'),
    mediaUrl:String(primaryMedia.url||''),
    mediaItems,
    mediaCarouselIndex:0,
    mediaFlipped:false,
    mediaSource:mediaItems.some(x=>x.source==='album_matched')?(mediaItems.every(x=>x.source==='album_matched')?'album':'mixed'):(primaryMedia.generatedRequested?'generated_pending':primaryMedia.url?'generated':'description'),
    likes:normalizeGeneratedMetric(metrics.likes??row?.likes,28,2600),
    comments:Math.max(initialComments.length,normalizeGeneratedMetric(metrics.comments??row?.comments,8,96)),
    reposts:normalizeGeneratedMetric(metrics.reposts??row?.reposts,1,42),
    shares:normalizeGeneratedMetric(metrics.shares??row?.shares,1,78),
    liked:false,reposted:false,expanded:false,translated:false,homeTranslationOpen:false,
    feedCaptionZh:cleanCaption,
    feedOriginalText:'',
    feedSourceLanguage:sourceLanguage,
    feedShowingOriginal:false,
    captionOriginal:cleanCaption,
    captionTranslation:'',
    homeCaptionOriginal:cleanCaption,
    homeCaptionTranslation:'',
    captionShort:parts.short?` ${escapeDMCardHTML(parts.short)}`:'',
    captionLong:parts.long?` ${escapeDMCardHTML(parts.long)}`:'',
    tags,
    translation:'',sourceLanguage,location:locationLabel,generated:true
  };
  ensurePostFallbackDeck(post,posts.length+index);
  return post;
}
function recordGeneratedFeedPublish(actorId,post){
  if(!actorId||!post||typeof recordSocialEvent!=='function')return;
  const caption=ensureHomeFeedCaptionModel(post).chinese;
  const media=String(post.mediaDescription||post.mediaText||'').trim();
  const mentionMeta=detectFormalActorMentions(`${caption} ${media}`);
  if(!isFormalSocialActor(actorId) && !mentionMeta.length)return;
  const summary=[
    `${socialActorName(actorId)}发布了一条 IG 动态。`,
    caption?`文案：“${compactMemoryText(caption,160)}”`:'',
    media?`配图：${compactMemoryText(media,180)}`:''
  ].filter(Boolean).join(' ');
  recordSocialEvent({
    type:'feed_publish',
    actorId,
    targetIds:[],
    contentId:post.id,
    contentType:'feed',
    contentOwnerId:actorId,
    relatedActorIds:mentionMeta.map(row=>row.actorId),
    text:caption,
    visibility:'public',
    payload:{
      mentionMeta,
      mediaDescription:media,
      caption,
      metrics:{
        likes:Number(post.likes)||0,
        comments:Number(post.comments)||0,
        reposts:Number(post.reposts)||0,
        shares:Number(post.shares)||0
      },
      generated:true
    },
    summary
  },{knownActorIds:isFormalSocialActor(actorId)?[actorId,'user']:[],learnedVia:'current_feed'});
}
function rollINSFeedMediaCounts(count){
  const total=Math.max(1,Number(count)||1);
  const weighted=[1,1,1,2,2,3,4];
  const counts=Array.from({length:total},()=>weighted[Math.floor(Math.random()*weighted.length)]);
  if(total>=3){
    if(counts.every(n=>n===1))counts[Math.floor(Math.random()*total)]=2+Math.floor(Math.random()*2);
    if(counts.every(n=>n>1))counts[Math.floor(Math.random()*total)]=1;
  }
  return counts;
}
function buildINSFeedMediaPlanPrompt(counts=[]){
  if(!Array.isArray(counts)||!counts.length)return '';
  return '本批 IG 图片数量计划（必须严格按输出 item 顺序执行）：'+counts.map((n,i)=>`第${i+1}条=${n}张`).join('；')+'。多图时每张必须是不同画面/细节并分别返回独立 mediaDescription，禁止复制同一描述。';
}
function getINSProfileLocation(actorRef){
  const id=normalizeSocialActorId(actorRef);
  if(id==='user')return String(userProfileState?.location||'').trim();
  const key=typeof resolveCharProfileKey==='function'?resolveCharProfileKey(id):id;
  return String(charProfileState?.profiles?.[key]?.location||dmProfiles?.[key]?.location||'').trim();
}
function buildINSActorLocationPrompt(actorIds=[]){
  const rows=[...new Set((actorIds||[]).map(normalizeSocialActorId).filter(Boolean))]
    .map(id=>{const location=getINSProfileLocation(id);if(!location)return '';const meta=getIdentityMeta(id);return `@${meta.handle||id} 当前主页地点：${location}`;})
    .filter(Boolean);
  if(!rows.length)return '';
  return '生成 IG / Reels 时先读取以下账号主页的当前地点。地点是人物当前公开位置上下文；如输出 location，请转换/保持为英文 City·Country / Place·Country 格式，不要无理由改去其他城市：\n'+rows.join('\n');
}

async function runINSRefreshStrangers(){
  const btn=document.getElementById('feedRefreshStrangersBtn');
  if(btn?.disabled)return;
  const prompt=document.getElementById('feedBatchPrompt')?.value.trim()||'';
  const requestedMediaCounts=rollINSFeedMediaCounts(4);
  const mediaPlanPrompt=buildINSFeedMediaPlanPrompt(requestedMediaCounts);
  setINSGenerationButtonBusy(btn,true,'生成中…');
  try{
    const result=await callNiniINSFeedSinglePass(INS_AI_TASKS.HOME_STRANGERS,{
      ownerId:'user',surface:'home_strangers',prompt:[prompt,mediaPlanPrompt].filter(Boolean).join('\n\n'),
      extra:{
        batchSize:4,globalPool:true,requestedMediaCounts,
        preferredRegions:['Korea','Japan','English-speaking regions','Hong Kong','Macau','Taiwan'],weightedPreference:true,
        includeInitialComments:true,initialCommentCount:5,
        requireEngagementMetrics:true,forbidCurrentUserInComments:true,
        forbidHashtags:false,diagnosticBuild:'5.0.16-home-strangers'
      }
    });
    if(!result.ok)throw new Error(result.reason||'陌生人动态生成失败');
    const items=getGeneratedItems(result.data);
    if(!items.length)throw new Error('AI 没有返回陌生人动态 items');

    const jobs=items.slice(0,6).map((row,index)=>{
      const handle=registerGeneratedPublicIdentity(row,index);
      const post=makeGeneratedFeedPost(row,handle,'stranger',index);
      post.generatedStranger=true;
      post.followed=false;
      recordGeneratedFeedPublish(handle,post);
      const initialComments=Array.isArray(row?.initialComments)
        ? row.initialComments.slice(0,5)
        : (Array.isArray(row?.comments)?row.comments.slice(0,5):[]);
      return {post,ownerId:handle,comments:initialComments};
    });
    const fresh=jobs.map(job=>job.post);
    posts.unshift(...fresh);

    jobs.forEach(job=>{
      commentsByPost[job.post.id]=[];
      applyGeneratedPublicCommentBatch({surface:'feed',contentId:job.post.id,ownerId:job.ownerId,comments:job.comments});
    });

    // Do not expose a newly refreshed stranger post until its visible comment sheet has 5 real rows.
    await Promise.all(jobs.map(job=>
      ensurePublicCommentFloor('feed',job.post.id,5)
        .catch(err=>console.warn('[INS] stranger initial comment top-up failed',job.post.id,err))
    ));
    jobs.forEach(job=>{
      const visible=(commentsByPost[job.post.id]||[]).length;
      job.post.comments=Math.max(Number(job.post.comments)||0,visible);
    });

    renderFeed();
    closeFeedGlass();
  }catch(error){
    alert('刷新陌生人动态失败：'+(error?.message||error));
  }finally{
    setINSGenerationButtonBusy(btn,false,'','刷新陌生人');
  }
}

async function runINSCharFeedGeneration(){
  const selected=getSelectedFeedChars().filter(id=>isFormalSocialActor(id));
  if(!selected.length){alert('请先在“绑定角色”中绑定至少一个角色，再选择角色');return;}
  const btn=document.getElementById('feedCharsPostBtn');
  if(btn?.disabled)return;
  const prompt=document.getElementById('feedBatchPrompt')?.value.trim()||'';
  const locationPrompt=buildINSActorLocationPrompt(selected);
  setINSGenerationButtonBusy(btn,true,'生成中…');
  try{
    const feedOwnerId=selected.length===1?selected[0]:'user';
    const result=await callNiniINSFeedSinglePass(INS_AI_TASKS.FEED_GENERATE,{
      actorIds:selected,ownerId:feedOwnerId,surface:'feed',prompt:composeCharGenerationPrompt({actorIds:selected,surface:'feed',batchPrompt:prompt,extraPrompt:locationPrompt}),
      extra:{
        requestedCount:selected.length,onePerActor:true,freeMediaPlan:true,
        includeInitialComments:true,initialCommentCount:5,
        requireEngagementMetrics:true,forbidCurrentUserInComments:true,
        diagnosticBuild:'5.1-feed',
        ownerMode:selected.length===1?'selected_actor':'shared_batch'
      }
    });
    if(!result.ok)throw new Error(result.reason||'角色动态生成失败');
    const items=getGeneratedItems(result.data);
    if(!items.length)throw new Error('AI 没有返回角色动态 items');

    const made=[];
    const commentJobs=[];
    items.forEach((row,index)=>{
      const actorId=resolveGeneratedActorId(row,selected,index);
      if(!actorId||!selected.includes(actorId))return;
      const postType=(typeof isFixedNPCHandle==='function'&&isFixedNPCHandle(actorId))?'npc':'char';
      const post=makeGeneratedFeedPost(row,actorId,postType,index);
      post.followed=true;
      made.push(post);
      recordGeneratedFeedPublish(actorId,post);
      commentJobs.push({post,ownerId:actorId,comments:Array.isArray(row?.initialComments)?row.initialComments.slice(0,5):[]});
    });
    if(!made.length)throw new Error('AI 返回的人物无法匹配到已绑定角色');

    posts.unshift(...made);
    commentJobs.forEach(job=>{
      applyGeneratedPublicCommentBatch({surface:'feed',contentId:job.post.id,ownerId:job.ownerId,comments:job.comments});
      queueMicrotask(()=>void ensurePublicCommentFloor('feed',job.post.id,5).catch(err=>console.warn('[INS] Feed initial comment top-up failed',err)));
    });
    renderFeed();closeFeedGlass();
  }catch(error){
    alert('生成 IG 动态失败（v5.1）\n\n'+(error?.message||error));
  }finally{
    setINSGenerationButtonBusy(btn,false,'','发 IG 动态');
    resetFeedPublishActionSelection();
  }
}

function resolveGeneratedStoryItemsWithAlbum(actorId,rawItems=[]){const normalized=(rawItems||[]).slice(0,4).map((item,index)=>({id:`story_ai_${actorId}_${Date.now()}_${index}`,type:item?.type==='video'?'video':'image',time:'刚刚',description:String(item?.mediaDescription||item?.description||'随手 Story'),mediaUrl:String(item?.mediaUrl||''),duration:Number(item?.duration)||5200,liked:false,generated:true,albumCategory:String(item?.albumCategory||item?.category||''),albumTags:Array.isArray(item?.albumTags)?item.albumTags:[],albumEraId:String(item?.albumEraId||item?.eraId||'')}));const imgs=normalized.filter(x=>x.type==='image'),resolved=typeof resolveGeneratedMediaItemsWithAlbum==='function'?resolveGeneratedMediaItemsWithAlbum(actorId,imgs.map(x=>({id:x.id,url:x.mediaUrl,desc:x.description,source:x.mediaUrl?'generated':'description',albumCategory:x.albumCategory,albumTags:x.albumTags,albumEraId:x.albumEraId}))):[];let p=0;return normalized.map(x=>{if(x.type!=='image')return x;const r=resolved[p++]||{};return {...x,mediaUrl:r.url||'',linkedAlbumId:r.linkedAlbumId||'',mediaSource:r.source||'description',generatedRequested:!!r.generatedRequested,albumCategory:r.albumCategory||x.albumCategory,albumTags:r.albumTags||x.albumTags,albumEraId:r.albumEraId||x.albumEraId};});}
async function runINSCharStoryGeneration(){
  const selected=getSelectedFeedChars().filter(id=>isFormalSocialActor(id));
  if(!selected.length){alert('请先在“绑定角色”中绑定至少一个角色，再选择角色');return;}
  const btn=document.getElementById('feedCharsStoryBtn');
  if(btn?.disabled)return;
  const prompt=document.getElementById('feedBatchPrompt')?.value.trim()||'';
  setINSGenerationButtonBusy(btn,true,'生成中…');
  try{
    const result=await callNiniINSAI(INS_AI_TASKS.STORY_GENERATE,{actorIds:selected,ownerId:'user',surface:'story',prompt:composeCharGenerationPrompt({actorIds:selected,surface:'story',batchPrompt:prompt,extraPrompt:buildINSActorLocationPrompt(selected)}),extra:{requestedCount:selected.length,oneBatchPerActor:true}});
    if(!result.ok)throw new Error(result.reason||'Story 生成失败');
    const rows=getGeneratedItems(result.data);
    if(!rows.length)throw new Error('AI 没有返回 Story items');
    let applied=0;
    rows.forEach((row,index)=>{
      const actorId=resolveGeneratedActorId(row,selected,index);
      if(!actorId||!selected.includes(actorId))return;
      const rawItems=Array.isArray(row?.storyItems)?row.storyItems:[];
      storyViewerData[actorId]=resolveGeneratedStoryItemsWithAlbum(actorId,rawItems.length?rawItems:[{type:'image',mediaDescription:row?.mediaDescription||'随手 Story'}]);
      if(!storyBatchState[actorId])storyBatchState[actorId]={liked:false,caption:'',audienceReactions:[],reactionPlanStatus:'idle',plannedReactions:[]};
      const caption=String(row?.captionOriginal??row?.caption??'');
      storyBatchState[actorId].caption=caption;
      storyBatchState[actorId].translation=String(row?.captionTranslation??row?.translation??'');
      storyBatchState[actorId].publishedAt=Date.now();
      const archiveRecord=archivePublishedStoryBatch(actorId,storyViewerData[actorId],caption);
      markStoryBatchLive(actorId,archiveRecord);
      storySeenState[actorId]=false;
      applied++;
    });
    if(!applied)throw new Error('AI 返回的 Story 无法匹配到已引入 Roche char');
    syncHomeStoryStrip();closeFeedGlass();
  }catch(error){alert('生成 Story 失败：'+(error?.message||error));}
  finally{setINSGenerationButtonBusy(btn,false,'','发 Story');resetFeedPublishActionSelection();}
}
/* ===== v5.1 per-char proactive publishing ===== */
async function runINSAutoFeedForActor(actorId){
  const id=normalizeSocialActorId(actorId);if(!id||!isFormalSocialActor(id)||id==='user')return;
  const prompt=composeCharGenerationPrompt({actorIds:[id],surface:'feed',extraPrompt:buildINSActorLocationPrompt([id])});
  const result=await callNiniINSFeedSinglePass(INS_AI_TASKS.FEED_GENERATE,{actorIds:[id],ownerId:id,surface:'feed',prompt,extra:{requestedCount:1,onePerActor:true,freeMediaPlan:true,includeInitialComments:true,initialCommentCount:5,requireEngagementMetrics:true,forbidCurrentUserInComments:true,diagnosticBuild:'5.1-auto-feed',ownerMode:'selected_actor'}});
  if(!result.ok)throw new Error(result.reason||'主动 Feed 生成失败');
  const row=getGeneratedItems(result.data)[0];if(!row)throw new Error('主动 Feed 没有返回 item');
  const postType=(typeof isFixedNPCHandle==='function'&&isFixedNPCHandle(id))?'npc':'char';
  const post=makeGeneratedFeedPost(row,id,postType,0);post.followed=true;posts.unshift(post);recordGeneratedFeedPublish(id,post);
  applyGeneratedPublicCommentBatch({surface:'feed',contentId:post.id,ownerId:id,comments:Array.isArray(row?.initialComments)?row.initialComments.slice(0,5):[]});
  await ensurePublicCommentFloor('feed',post.id,5).catch(()=>{});
  renderFeed();
}
async function runINSAutoStoryForActor(actorId){
  const id=normalizeSocialActorId(actorId);if(!id||!isFormalSocialActor(id)||id==='user')return;
  const prompt=composeCharGenerationPrompt({actorIds:[id],surface:'story',extraPrompt:buildINSActorLocationPrompt([id])});
  const result=await callNiniINSAI(INS_AI_TASKS.STORY_GENERATE,{actorIds:[id],ownerId:id,surface:'story',prompt,extra:{requestedCount:1,oneBatchPerActor:true,diagnosticBuild:'5.1-auto-story'}});
  if(!result.ok)throw new Error(result.reason||'主动 Story 生成失败');
  const row=getGeneratedItems(result.data)[0];if(!row)throw new Error('主动 Story 没有返回 item');
  const rawItems=Array.isArray(row?.storyItems)?row.storyItems:[];
  storyViewerData[id]=resolveGeneratedStoryItemsWithAlbum(id,rawItems.length?rawItems:[{type:'image',mediaDescription:row?.mediaDescription||'随手 Story'}]);
  if(!storyBatchState[id])storyBatchState[id]={liked:false,caption:'',audienceReactions:[],reactionPlanStatus:'idle',plannedReactions:[]};
  const caption=String(row?.captionOriginal??row?.caption??'');
  storyBatchState[id].caption=caption;storyBatchState[id].translation=String(row?.captionTranslation??row?.translation??'');storyBatchState[id].publishedAt=Date.now();
  const archiveRecord=archivePublishedStoryBatch(id,storyViewerData[id],caption);markStoryBatchLive(id,archiveRecord);storySeenState[id]=false;syncHomeStoryStrip();
}
async function runINSAutoReelsForActor(actorId){
  const id=normalizeSocialActorId(actorId);if(!id||!isFormalSocialActor(id)||id==='user')return;
  const prompt=composeCharGenerationPrompt({actorIds:[id],surface:'reels',extraPrompt:buildINSActorLocationPrompt([id])});
  const result=await callNiniINSAI(INS_AI_TASKS.REELS_GENERATE,{actorIds:[id],ownerId:id,surface:'reels',prompt,extra:{requestedCount:1,onePerActor:true,includeInitialComments:true,initialCommentCount:5,diagnosticBuild:'5.1-auto-reels'}});
  if(!result.ok)throw new Error(result.reason||'主动 Reels 生成失败');
  const row=getGeneratedItems(result.data)[0];if(!row)throw new Error('主动 Reels 没有返回 item');
  const actor=getRelationshipActor(id);const kind=actor?.kind==='npc'?'npc':'char';
  const item=makeGeneratedReelItem(row,id,kind,0);reelsV15State.items.unshift(item);reelsV15State.activeId=reelsV15State.activeId||item.id;recordGeneratedReelPublish(id,item);
  applyGeneratedPublicCommentBatch({surface:'reels',contentId:item.id,ownerId:id,comments:Array.isArray(row?.initialComments)?row.initialComments.slice(0,5):[]});
  await ensurePublicCommentFloor('reels',item.id,5).catch(()=>{});scheduleINSReelsPersistence();renderReelsV15();
}
async function runINSAutoPublishSurface(actorId,surface){
  if(surface==='feed')return runINSAutoFeedForActor(actorId);
  if(surface==='story')return runINSAutoStoryForActor(actorId);
  if(surface==='reels')return runINSAutoReelsForActor(actorId);
}
function checkINSAutoPublishTasks(){
  const now=Date.now();
  (typeof getRelationshipActors==='function'?getRelationshipActors():[]).filter(actor=>actor&&actor.id&&actor.id!=='user'&&isFormalSocialActor(actor.id)).forEach(actor=>{
    const state=ensureCharActivePublish(actor.id);if(!state)return;
    ['feed','story','reels'].forEach(surface=>{
      const cfg=state[surface];if(!cfg.enabled)return;
      if(!cfg.nextAt){cfg.nextAt=now+randomCharActiveInterval(cfg.intervalMin);saveJSONLocalState(CHAR_ACTIVE_PUBLISH_STORAGE_KEY,charActivePublishState);return;}
      if(now<cfg.nextAt)return;
      const busyKey=`${actor.id}:${surface}`;if(charAutoPublishBusy.has(busyKey))return;
      cfg.nextAt=now+randomCharActiveInterval(cfg.intervalMin);saveJSONLocalState(CHAR_ACTIVE_PUBLISH_STORAGE_KEY,charActivePublishState);
      charAutoPublishBusy.add(busyKey);
      Promise.resolve(runINSAutoPublishSurface(actor.id,surface)).catch(error=>console.warn('[INS] 主动发布失败',actor.id,surface,error)).finally(()=>charAutoPublishBusy.delete(busyKey));
    });
  });
}
function startINSAutoPublishTimer(){
  if(window.__niniINSAutoPublishStarted)return;
  window.__niniINSAutoPublishStarted=true;
  setInterval(checkINSAutoPublishTasks,CHAR_ACTIVE_CHECK_MS);
  setTimeout(checkINSAutoPublishTasks,3000);
}

// Capture-phase delegation: more reliable inside Roche mobile WebView after dynamic repaint.
document.addEventListener('click',event=>{
  const chip=event.target.closest?.('.feed-char-chip[data-feed-char]');
  if(chip){event.preventDefault();event.stopPropagation();chip.classList.toggle('on');return;}
  const action=event.target.closest?.('[data-ins-feed-action]');
  if(!action)return;
  event.preventDefault();event.stopPropagation();
  const type=action.dataset.insFeedAction;
  if(type==='refresh_strangers')void runINSRefreshStrangers();
  else if(type==='char_post'){resetFeedPublishActionSelection();void runINSCharFeedGeneration();}
  else if(type==='char_story'){resetFeedPublishActionSelection();void runINSCharStoryGeneration();}
},{capture:true});


/* ===== v1.15.22 per-post menu/editor behavior ===== */
let activePostMenuIndex = null;
let postEditCarouselIndex = 0;
let postEditCarouselFlipped = false;
let postEditPointerStartX = null;
let postEditPointerStartY = null;

const postGlassDim = document.getElementById('postGlassDim');
const postActionGlass = document.getElementById('postActionGlass');
const postEditGlass = document.getElementById('postEditGlass');
const postEditCaption = document.getElementById('postEditCaption');
const postEditMediaDescription = document.getElementById('postEditMediaDescription');
const postEditMediaPreview = document.getElementById('postEditMediaPreview');
const postEditMediaTypeNote = document.getElementById('postEditMediaTypeNote');
const postEditSourceBadge = document.getElementById('postEditSourceBadge');
const postEditAlbumTools = document.getElementById('postEditAlbumTools');
const postEditCarouselCounter = document.getElementById('postEditCarouselCounter');
const postEditStrangerIdentity = document.getElementById('postEditStrangerIdentity');
const postEditDisplayName = document.getElementById('postEditDisplayName');
const postEditHandle = document.getElementById('postEditHandle');
const postEditLocation = document.getElementById('postEditLocation');

function closePostGlass(){
  postActionGlass?.classList.remove('show');
  postEditGlass?.classList.remove('show');
  document.getElementById('postDeleteConfirm')?.classList.remove('show');
  postGlassDim?.classList.remove('show');
}

function openPostMenu(index){
  activePostMenuIndex = index;
  closeFeedGlass?.();
  postEditGlass?.classList.remove('show');
  postActionGlass?.classList.add('show');
  postGlassDim?.classList.add('show');
}

function getPostAlbumAssetsForCurrentEdit(author){
  if(typeof isRocheCharacterHandle==='function' && !isRocheCharacterHandle(author)) return [];
  if(typeof getCharAlbumSelectableAssets==='function')return getCharAlbumSelectableAssets(author);
  const key=typeof resolveCharProfileKey==='function' ? resolveCharProfileKey(author) : author;
  const assets=(charAlbumPreviewData[key] || []).filter(asset=>!['generated','generated_favorite'].includes(String(asset?.kind||'')));
  return typeof filterCharAlbumAssetsByConfiguredPeriod==='function'
    ? filterCharAlbumAssetsByConfiguredPeriod(author,assets)
    : assets;
}

function getPostEditSourceMode(post){
  if(!post)return 'fallback';
  if(post.mediaSource==='album' && post.linkedAlbumId)return 'album';
  if(post.mediaSource==='generated')return 'generated';
  if(['upload','user_upload','url'].includes(String(post.mediaSource||'')))return 'upload';
  if(Array.isArray(post.mediaItems) && post.mediaItems.length){
    const hasUploaded=post.mediaItems.some(item=>['upload','user_upload','url'].includes(String(item?.source||'').toLowerCase()));
    if(hasUploaded)return 'upload';
  }
  return 'fallback';
}

function getPostEditCandidates(post){
  if(!post)return [];
  const mode=getPostEditSourceMode(post);
  if((post.mediaType||'image')==='video'){
    return [{id:'video_description',url:'',desc:post.mediaDescription||post.mediaText||'视频描述',kind:'video',source:'description'}];
  }
  if(mode==='album'){
    const assets=getPostAlbumAssetsForCurrentEdit(post.author).slice(0,4);
    return assets.map((asset,index)=>({
      id:asset.id,
      url:asset.url || INS_FEED_FALLBACK_MEDIA_POOL[index%INS_FEED_FALLBACK_MEDIA_POOL.length]?.url || '',
      desc:asset.desc || '相册素材',
      kind:'album',
      source:'album'
    }));
  }
  const items=ensurePostEditMediaItems(post);
  const deck=ensurePostFallbackDeck(post,activePostMenuIndex||0);
  return items.slice(0,4).map((item,index)=>({
    id:item.id||`media_${index}`,
    url:item.url||deck[index]?.url||'',
    desc:item.desc||'',
    draft:!!item.draft,
    kind:mode,
    source:item.source||post.mediaSource||''
  }));
}

function ensurePostEditMediaItems(post){
  if(!post || (post.mediaType||'image')==='video')return [];
  if(Array.isArray(post.mediaItems) && post.mediaItems.length){
    post.mediaItems=post.mediaItems.filter(item=>item && (item.url || item.desc || item.draft)).slice(0,4).map(item=>({
      id:item.id||`media_${Date.now()}`,
      url:item.url||'',
      desc:item.desc||post.mediaDescription||post.mediaText||'',
      source:item.source||(['upload','user_upload','url'].includes(String(post.mediaSource||''))?'upload':post.mediaSource==='generated'?'generated':'description'),
      draft:!!item.draft
    }));
    return post.mediaItems;
  }
  const seeded=getPostMediaItems(post,activePostMenuIndex||0).filter(item=>item && (item.url || item.desc)).slice(0,4).map(item=>({
    id:item.id||`media_${Date.now()}`,
    url:item.url||'',
    desc:item.desc||post.mediaDescription||post.mediaText||'',
    source:item.source||(['upload','user_upload','url'].includes(String(post.mediaSource||''))?'upload':post.mediaSource==='generated'?'generated':'description')
  }));
  post.mediaItems=seeded.length?seeded:[{id:`media_${Date.now()}`,url:'',desc:post.mediaDescription||post.mediaText||'',source:'description'}];
  return post.mediaItems;
}

function commitPostEditCurrentDescription(post){
  if(!post || !postEditMediaDescription || (post.mediaType||'image')==='video')return;
  if(getPostEditSourceMode(post)==='album')return;
  const items=ensurePostEditMediaItems(post);
  if(!items.length)return;
  const idx=Math.max(0,Math.min(postEditCarouselIndex,items.length-1));
  const value=postEditMediaDescription.value.trim();
  items[idx]={...items[idx],desc:value,draft:value?false:!!items[idx].draft,source:items[idx].source||(['upload','user_upload','url'].includes(String(post.mediaSource||''))?'upload':post.mediaSource==='generated'?'generated':'description')};
  post.mediaItems=items;
  post.mediaCarouselIndex=idx;
  post.mediaDescription=value || post.mediaDescription || post.mediaText || '';
  if(value)post.mediaText=value;
}

function syncPostEditCarouselIndex(post){
  const candidates=getPostEditCandidates(post);
  if(!candidates.length){postEditCarouselIndex=0;return;}
  const mode=getPostEditSourceMode(post);
  if(mode==='album' && post.linkedAlbumId){
    const idx=candidates.findIndex(item=>item.id===post.linkedAlbumId);
    postEditCarouselIndex=idx>=0?idx:0;
    return;
  }
  const currentId=Array.isArray(post.mediaItems) && Number.isInteger(post.mediaCarouselIndex)?post.mediaItems[post.mediaCarouselIndex]?.id:'';
  const currentUrl=post.mediaUrl || post.fallbackMediaUrl || '';
  const idx=currentId?candidates.findIndex(item=>String(item.id||'')===String(currentId||'')):candidates.findIndex(item=>item.url===currentUrl);
  postEditCarouselIndex=idx>=0?idx:Math.min(postEditCarouselIndex,candidates.length-1);
}

function applyPostEditCandidate(post,candidate){
  if(!post || !candidate)return;
  const mode=getPostEditSourceMode(post);
  if(mode==='album'){
    post.linkedAlbumId=candidate.id;
    post.mediaSource='album';
    post.mediaUrl=candidate.kind==='album' && candidate.url && getPostAlbumAssetsForCurrentEdit(post.author).some(asset=>asset.id===candidate.id && asset.url)?candidate.url:'';
    if(candidate.desc){
      post.mediaDescription=candidate.desc;
      post.mediaText=candidate.desc;
      if(postEditMediaDescription)postEditMediaDescription.value=candidate.desc;
    }
    return;
  }
  const items=ensurePostEditMediaItems(post);
  const idx=items.findIndex(item=>String(item?.id||'')===String(candidate.id||''));
  const resolvedIdx=idx>=0?idx:Math.max(0,Math.min(postEditCarouselIndex,items.length-1));
  post.mediaCarouselIndex=resolvedIdx;
  post.linkedAlbumId='';
  if(mode==='upload'){
    post.mediaSource='user_upload';
    if(candidate.url)post.mediaUrl=candidate.url;
  }else if(mode==='generated'){
    post.mediaSource='generated';
    if(candidate.url)post.mediaUrl=candidate.url;
  }else{
    post.mediaSource='description';
    post.fallbackMediaUrl=candidate.url||post.fallbackMediaUrl;
    post.fallbackMediaId=candidate.id||post.fallbackMediaId;
  }
  if(postEditMediaDescription)postEditMediaDescription.value=candidate.draft?'':(candidate.desc||post.mediaDescription||post.mediaText||'');
}

function renderPostEditCarousel(post){
  if(!postEditMediaPreview || !post)return;
  const candidates=getPostEditCandidates(post);
  if(!candidates.length){
    postEditMediaPreview.innerHTML=`<div class="post-edit-media-back"><span>这个角色的私人相册目前没有可用于 Matcher 的图片。</span></div>`;
    if(postEditCarouselCounter)postEditCarouselCounter.textContent='0 / 0';
    if(postEditMediaDescription)postEditMediaDescription.value='';
    return;
  }
  postEditCarouselIndex=(postEditCarouselIndex+candidates.length)%candidates.length;
  const candidate=candidates[postEditCarouselIndex];
  if(postEditCarouselCounter)postEditCarouselCounter.textContent=`${postEditCarouselIndex+1} / ${candidates.length}`;
  if(postEditCarouselFlipped || !candidate.url){
    const previewDesc=candidate.draft?'请填写这张图的图片描述':(candidate.desc||post.mediaDescription||'暂无图片描述');
    postEditMediaPreview.innerHTML=`<div class="post-edit-media-back"><span>${escapeDMCardHTML(previewDesc)}</span></div>`;
  }else{
    postEditMediaPreview.innerHTML=`<img draggable="false" src="${escapeDMCardHTML(candidate.url)}" alt="动态候选图片">`;
  }
  applyPostEditCandidate(post,candidate);
}

function updatePostEditSourceUI(post){
  if(!post)return;
  const mediaType=post.mediaType||'image';
  const isVideo=mediaType==='video';
  const roche=typeof isRocheCharacterHandle==='function' && isRocheCharacterHandle(post.author);
  const mode=getPostEditSourceMode(post);
  const editableCount=Math.max(0,getPostEditCandidates(post).length);
  const fallbackBtn=document.getElementById('postEditUseFallback');
  const albumBtn=document.getElementById('postEditUseAlbum');
  const generatedBtn=document.getElementById('postEditUseGenerated');
  const uploadBtn=document.getElementById('postEditUseUpload');
  const genAction=document.getElementById('postEditGenerateImage');
  const uploadAction=document.getElementById('postEditChooseUpload');
  const addAction=document.getElementById('postEditAddMedia');
  const delAction=document.getElementById('postEditDeleteMedia');
  const sourceNote=document.getElementById('postEditSourceNote');
  [fallbackBtn,albumBtn,generatedBtn,uploadBtn].forEach(btn=>btn?.classList.remove('active'));
  if(mode==='album')albumBtn?.classList.add('active');
  else if(mode==='generated')generatedBtn?.classList.add('active');
  else if(mode==='upload')uploadBtn?.classList.add('active');
  else fallbackBtn?.classList.add('active');
  if(albumBtn)albumBtn.disabled=isVideo || !roche;
  if(fallbackBtn)fallbackBtn.disabled=isVideo;
  if(generatedBtn)generatedBtn.disabled=isVideo;
  if(uploadBtn)uploadBtn.disabled=isVideo;
  if(postEditAlbumTools)postEditAlbumTools.style.display=isVideo?'none':'block';
  if(postEditMediaTypeNote){
    postEditMediaTypeNote.textContent=isVideo?'当前是视频动态：只编辑文案和视频描述，不走相册图片 / 生图 / 上传图片。':'支持默认示例图、角色相册、生图与上传图片；可做 1–4 张图，每张图都有独立图片描述。';
  }
  if(postEditSourceBadge){
    postEditSourceBadge.textContent=isVideo?'当前来源：视频描述':mode==='album'?'当前来源：角色私人相册':mode==='generated'?'当前来源：按描述生图':mode==='upload'?'当前来源：上传图片 + 图片描述':'当前来源：默认示例图 + 图片描述';
  }
  if(sourceNote){
    sourceNote.textContent=mode==='album'?'左右滑动浏览该角色相册候选；当前停留的卡片就是这条动态选择的相册素材。':mode==='generated'?'会直接调用 Roche 当前生图接口，用这张图的图片描述生成真实图片；插件内未单独配置时，默认继承 Roche 生图配置。':mode==='upload'?'上传模式支持 1–4 张图；你可以继续追加图片，也可以单独修改当前这张的描述。':'默认图片只是视觉占位；现在同一条动态支持 1–4 张图，每张图都可以写独立图片描述。';
  }
  if(genAction)genAction.classList.toggle('show',mode==='generated' && !isVideo);
  if(uploadAction)uploadAction.classList.toggle('show',mode==='upload' && !isVideo);
  if(addAction){addAction.classList.toggle('show',!isVideo && mode!=='album');addAction.disabled=editableCount>=4;addAction.textContent=mode==='upload'?'新增图':'新增图';}
  if(delAction){delAction.classList.toggle('show',!isVideo && mode!=='album');delAction.disabled=editableCount<=1;}
}

function renderPostEditAssetOptions(post){
  if(!post)return;
  syncPostEditCarouselIndex(post);
  updatePostEditSourceUI(post);
  renderPostEditCarousel(post);
}

function openPostEditor(){
  const post=posts[activePostMenuIndex];
  if(!post)return;
  const feedCaption=ensureHomeFeedCaptionModel(post).chinese;
  postEditCaption.value=[feedCaption,normalizeFeedPostTags(post.tags||[]).join(' ')].filter(Boolean).join(' ');
  const editableStranger=!!(post.generatedStranger && post.type==='stranger');
  postEditStrangerIdentity?.classList.toggle('show',editableStranger);
  if(editableStranger){const meta=getIdentityMeta(post.author);if(postEditDisplayName)postEditDisplayName.value=meta.name||post.author;if(postEditHandle)postEditHandle.value=meta.handle||post.author;if(postEditLocation)postEditLocation.value=post.location||post.sub||'';}
  ensurePostFallbackMedia(post,activePostMenuIndex||0);
  ensurePostEditMediaItems(post);
  postEditMediaDescription.value=post.mediaDescription||post.mediaText||'';
  postEditCarouselIndex=Math.max(0,Math.min(Number(post.mediaCarouselIndex)||0,Math.max(0,(post.mediaItems?.length||1)-1)));
  postEditCarouselFlipped=false;
  renderPostEditAssetOptions(post);
  postActionGlass.classList.remove('show');
  postEditGlass.classList.add('show');
  postEditGlass.scrollTop=0;
}

function deleteActivePost(){
  if(activePostMenuIndex===null || !posts[activePostMenuIndex])return;
  const post=posts[activePostMenuIndex];
  document.getElementById('postDeleteConfirmCopy').textContent='删除 @'+post.author+' 的这条动态后，它会立即从当前 Feed 中移除。';
  postActionGlass?.classList.remove('show');
  document.getElementById('postDeleteConfirm')?.classList.add('show');
  postGlassDim?.classList.add('show');
}
function cancelDeleteActivePost(){document.getElementById('postDeleteConfirm')?.classList.remove('show');postGlassDim?.classList.remove('show');}
function confirmDeleteActivePost(){
  if(activePostMenuIndex===null || !posts[activePostMenuIndex])return;
  const feed=document.getElementById('feed');
  const oldScroll=feed?feed.scrollTop:0;
  posts.splice(activePostMenuIndex,1);
  activePostMenuIndex=null;
  document.getElementById('postDeleteConfirm')?.classList.remove('show');
  postGlassDim?.classList.remove('show');
  renderFeed();
  if(feed){requestAnimationFrame(()=>{feed.scrollTop=Math.max(0,Math.min(oldScroll,feed.scrollHeight-feed.clientHeight));});}
}

function applyGeneratedStrangerIdentityEdit(post){
  if(!post || !post.generatedStranger || post.type!=='stranger')return;
  const oldHandle=String(post.author||'').replace(/^@/,'');
  const oldMeta=publicIdentityRegistry[oldHandle]||getIdentityMeta(oldHandle);
  const newHandle=String(postEditHandle?.value||oldHandle).replace(/^@/,'').trim()||oldHandle;
  const newName=String(postEditDisplayName?.value||oldMeta.name||newHandle).trim()||newHandle;
  const newLocation=normalizeGeneratedLocationLabel(postEditLocation?.value||post.location||post.sub||'',post.sourceLanguage||'',post.captionShort||'',oldMeta.region||'') || String(postEditLocation?.value||'').trim();
  const next={...oldMeta,handle:newHandle,name:newName,avatarUrl:STRANGER_AVATAR_URL,location:newLocation};
  if(oldHandle!==newHandle)delete publicIdentityRegistry[oldHandle];
  publicIdentityRegistry[newHandle]=next;
  post.author=newHandle;
  post.avatar=next.initial||String(newName||'?').slice(0,1).toUpperCase();
  post.location=newLocation;
  post.sub=newLocation;
}

function saveActivePostEdit(){
  const post=posts[activePostMenuIndex];
  if(!post)return;
  const caption=postEditCaption.value.trim();
  const mediaDesc=postEditMediaDescription.value.trim();
  applyGeneratedStrangerIdentityEdit(post);
  commitPostEditCurrentDescription(post);
  const parsedCaption=splitFeedEditorCaptionAndTags(caption);
  const canonicalCaption=stripGeneratedCaptionHashtags(caption).trim();
  const captionChanged=canonicalCaption!==String(post.feedCaptionZh||post.captionOriginal||'').trim();
  post.feedCaptionZh=canonicalCaption;
  post.captionOriginal=canonicalCaption;
  post.homeCaptionOriginal=canonicalCaption;
  post.captionShort=parsedCaption.parts.short?' '+escapeDMCardHTML(parsedCaption.parts.short):'';
  post.captionLong=parsedCaption.parts.long?' '+escapeDMCardHTML(parsedCaption.parts.long):'';
  post.tags=parsedCaption.tags;
  post.expanded=false;
  if(captionChanged){
    post.feedOriginalText='';
    post.feedShowingOriginal=false;
    delete homeFeedTranslationCache[String(post.id||'')];
    homeFeedTranslationRuntime.delete(String(post.id||''));
    persistHomeFeedTranslationCache();
  }
  if(mediaDesc)post.mediaDescription=mediaDesc;
  if((post.mediaType||'image')==='image'){
    if(post.mediaSource==='album' && post.linkedAlbumId){
      const desc=storyAlbumDesc(post.author,post.linkedAlbumId);
      if(desc && !mediaDesc)post.mediaDescription=desc;
      post.mediaItems=[];
    }else if(post.mediaSource==='generated'){
      post.linkedAlbumId='';
      ensurePostEditMediaItems(post);
      const items=post.mediaItems.filter(item=>item && (item.url || item.desc)).slice(0,4);
      post.mediaItems=items;
      post.mediaUrl=items[Math.max(0,Math.min(Number(post.mediaCarouselIndex)||0,items.length-1))]?.url || items[0]?.url || '';
      ensurePostFallbackMedia(post,activePostMenuIndex||0);
    }else if(getPostEditSourceMode(post)==='upload'){
      post.linkedAlbumId='';
      post.mediaSource='user_upload';
      const items=ensurePostEditMediaItems(post).filter(item=>item && (item.url || item.desc)).slice(0,4);
      if(items.length){post.mediaItems=items.map(item=>({...item,source:'upload',draft:false}));post.mediaUrl=items[Math.max(0,Math.min(Number(post.mediaCarouselIndex)||0,items.length-1))]?.url || items[0].url || '';}
    }else{
      post.linkedAlbumId='';
      post.mediaSource='description';
      const items=ensurePostEditMediaItems(post).filter(item=>item && (item.url || item.desc)).slice(0,4);
      post.mediaItems=items.map(item=>({...item,source:'description',draft:false}));
      post.mediaUrl='';
      ensurePostFallbackMedia(post,activePostMenuIndex||0);
    }
  }else{post.mediaSource='description';post.linkedAlbumId='';}
  post.mediaFlipped=false;
  closePostGlass();
  renderFeed();
  renderUserProfile?.();
}

function setPostEditSource(mode){
  const post=posts[activePostMenuIndex];
  if(!post || (post.mediaType||'image')==='video')return;
  commitPostEditCurrentDescription(post);
  const roche=typeof isRocheCharacterHandle==='function' && isRocheCharacterHandle(post.author);
  if(mode==='album'){
    if(!roche){alert('固定 NPC / 非 Roche 账号不调用私人相册 Matcher');return;}
    const assets=getPostAlbumAssetsForCurrentEdit(post.author);
    if(!assets.length){alert('这个角色的私人相册还没有可用素材');return;}
    post.mediaSource='album';post.mediaUrl='';post.mediaItems=[];
    if(!post.linkedAlbumId || !assets.some(asset=>asset.id===post.linkedAlbumId))post.linkedAlbumId=assets[0].id;
  }else if(mode==='generated'){
    post.mediaSource='generated';post.linkedAlbumId='';ensurePostEditMediaItems(post);if(!post.mediaUrl)post.mediaUrl=post.mediaItems[post.mediaCarouselIndex||0]?.url||'';
  }else if(mode==='upload'){
    post.mediaSource='user_upload';post.linkedAlbumId='';ensurePostEditMediaItems(post);if(!(Array.isArray(post.mediaItems) && post.mediaItems.some(item=>item && item.url)) && !post.mediaUrl)requestPostUploadPicker();
  }else{
    post.mediaSource='description';post.linkedAlbumId='';post.mediaUrl='';ensurePostEditMediaItems(post);post.mediaItems=post.mediaItems.map(item=>({...item,source:'description'}));ensurePostFallbackMedia(post,activePostMenuIndex||0);
  }
  postEditCarouselFlipped=false;
  postEditCarouselIndex=Math.max(0,Math.min(Number(post.mediaCarouselIndex)||0,Math.max(0,(getPostEditCandidates(post).length||1)-1)));
  renderPostEditAssetOptions(post);
}

function requestPostUploadPicker(){document.getElementById('postEditUploadInput')?.click();}
async function handlePostEditUploadFiles(fileList){
  const post=posts[activePostMenuIndex];
  if(!post)return;
  commitPostEditCurrentDescription(post);
  const existing=getPostEditSourceMode(post)==='upload'?ensurePostEditMediaItems(post).filter(item=>item && item.url).slice(0,4):[];
  const room=Math.max(0,4-existing.length);
  const files=[...(fileList||[])].filter(file=>file && /^image\//.test(String(file.type||''))).slice(0,room||4);
  if(!files.length)return;
  const readAsDataUrl=file=>new Promise((resolve,reject)=>{const reader=new FileReader();reader.onload=()=>resolve(String(reader.result||''));reader.onerror=reject;reader.readAsDataURL(file);});
  const urls=await Promise.all(files.map(readAsDataUrl));
  const currentDesc=(postEditMediaDescription?.value||post.mediaDescription||post.mediaText||'').trim();
  const appended=urls.map((url,index)=>({id:`upload_${Date.now()}_${index}`,url,desc:currentDesc || files[index]?.name || '上传图片',source:'upload'}));
  post.mediaItems=[...existing,...appended].slice(0,4);
  post.mediaCarouselIndex=Math.max(0,Math.min(existing.length,post.mediaItems.length-1));
  post.mediaUrl=post.mediaItems[post.mediaCarouselIndex]?.url||post.mediaItems[0]?.url||'';
  post.mediaSource='user_upload';
  post.linkedAlbumId='';
  post.uploadedFileNames=[...(post.uploadedFileNames||[]),...files.map(file=>file.name).filter(Boolean)].slice(0,4);
  if(currentDesc){post.mediaDescription=currentDesc;post.mediaText=currentDesc;}
  postEditCarouselIndex=post.mediaCarouselIndex;
  postEditCarouselFlipped=false;
  renderPostEditAssetOptions(post);
}

function addPostEditMediaSlot(){
  const post=posts[activePostMenuIndex];
  if(!post || (post.mediaType||'image')==='video')return;
  const mode=getPostEditSourceMode(post);
  if(mode==='album'){alert('角色相册模式不在这里手动增删；如需自由增删，请切到默认图片 / 生图 / 上传图片。');return;}
  if(mode==='upload'){requestPostUploadPicker();return;}
  commitPostEditCurrentDescription(post);
  const items=ensurePostEditMediaItems(post);
  if(items.length>=4){alert('一条动态最多 4 张图');return;}
  if(mode==='generated')post.mediaSource='description';
  items.push({id:`media_${Date.now()}`,url:'',desc:'',source:'description',draft:true});
  post.mediaItems=items;
  post.mediaCarouselIndex=items.length-1;
  postEditCarouselIndex=items.length-1;
  postEditCarouselFlipped=false;
  renderPostEditAssetOptions(post);
}

function deleteCurrentPostEditMedia(){
  const post=posts[activePostMenuIndex];
  if(!post || (post.mediaType||'image')==='video')return;
  const mode=getPostEditSourceMode(post);
  if(mode==='album'){alert('角色相册模式不在这里手动删图；如需自由删图，请先切换来源。');return;}
  commitPostEditCurrentDescription(post);
  const items=ensurePostEditMediaItems(post);
  if(items.length<=1){alert('至少保留 1 张图 / 1 段图像语义');return;}
  const idx=Math.max(0,Math.min(postEditCarouselIndex,items.length-1));
  items.splice(idx,1);
  post.mediaItems=items;
  post.mediaCarouselIndex=Math.max(0,Math.min(idx,items.length-1));
  postEditCarouselIndex=post.mediaCarouselIndex;
  if(mode==='upload'){post.mediaUrl=items[post.mediaCarouselIndex]?.url||items[0]?.url||'';}
  else if(mode==='generated' && !items.some(item=>item.url)){post.mediaSource='description';post.mediaUrl='';}
  ensurePostFallbackMedia(post,activePostMenuIndex||0);
  postEditCarouselFlipped=false;
  renderPostEditAssetOptions(post);
}

function shiftPostEditCarousel(delta){
  const post=posts[activePostMenuIndex];
  if(!post)return;
  commitPostEditCurrentDescription(post);
  const candidates=getPostEditCandidates(post);
  if(candidates.length<=1)return;
  postEditCarouselIndex=(postEditCarouselIndex+delta+candidates.length)%candidates.length;
  postEditCarouselFlipped=false;
  renderPostEditCarousel(post);
}

document.getElementById('postEditAction')?.addEventListener('click',openPostEditor);
document.getElementById('postDeleteAction')?.addEventListener('click',deleteActivePost);
document.getElementById('postDeleteCancel')?.addEventListener('click',cancelDeleteActivePost);
document.getElementById('postDeleteConfirmBtn')?.addEventListener('click',confirmDeleteActivePost);
document.getElementById('postEditClose')?.addEventListener('click',closePostGlass);
postGlassDim?.addEventListener('click',closePostGlass);
document.getElementById('postEditUseFallback')?.addEventListener('click',()=>setPostEditSource('fallback'));
document.getElementById('postEditUseAlbum')?.addEventListener('click',()=>setPostEditSource('album'));
document.getElementById('postEditUseGenerated')?.addEventListener('click',()=>setPostEditSource('generated'));
document.getElementById('postEditUseUpload')?.addEventListener('click',()=>setPostEditSource('upload'));
document.getElementById('postEditMediaPrev')?.addEventListener('click',event=>{event.stopPropagation();shiftPostEditCarousel(-1);});
document.getElementById('postEditMediaNext')?.addEventListener('click',event=>{event.stopPropagation();shiftPostEditCarousel(1);});
postEditMediaPreview?.addEventListener('click',()=>{postEditCarouselFlipped=!postEditCarouselFlipped;const post=posts[activePostMenuIndex];if(post)renderPostEditCarousel(post);});
postEditMediaPreview?.addEventListener('pointerdown',event=>{postEditPointerStartX=event.clientX;postEditPointerStartY=event.clientY;});
postEditMediaPreview?.addEventListener('pointerup',event=>{if(postEditPointerStartX===null || postEditPointerStartY===null)return;const dx=event.clientX-postEditPointerStartX;const dy=event.clientY-postEditPointerStartY;postEditPointerStartX=null;postEditPointerStartY=null;if(Math.abs(dx)>45 && Math.abs(dx)>Math.abs(dy)*1.25){event.preventDefault();shiftPostEditCarousel(dx<0?1:-1);}});
postEditMediaDescription?.addEventListener('input',()=>{const post=posts[activePostMenuIndex];if(post)commitPostEditCurrentDescription(post);});
document.getElementById('postEditChooseUpload')?.addEventListener('click',requestPostUploadPicker);
document.getElementById('postEditAddMedia')?.addEventListener('click',addPostEditMediaSlot);
document.getElementById('postEditDeleteMedia')?.addEventListener('click',deleteCurrentPostEditMedia);
document.getElementById('postEditUploadInput')?.addEventListener('change',async event=>{await handlePostEditUploadFiles(event.target.files);event.target.value='';});
document.getElementById('postEditGenerateImage')?.addEventListener('click',async ()=>{const post=posts[activePostMenuIndex];if(!post)return;const desc=postEditMediaDescription.value.trim();if(!desc){alert('先写图片描述再生图');return;}const btn=document.getElementById('postEditGenerateImage');try{const generated=await withINSImageButtonBusy(btn,'生图中…',()=>requestRocheGeneratedImage({description:desc,entry:'post_edit'}));commitPostEditCurrentDescription(post);post.mediaDescription=desc;post.mediaText=desc;post.mediaSource='generated';post.linkedAlbumId='';const items=ensurePostEditMediaItems(post);const idx=Math.max(0,Math.min(post.mediaCarouselIndex||0,Math.max(0,items.length-1)));items[idx]={...items[idx],url:generated.url,desc,source:'generated',draft:false};post.mediaItems=items;post.mediaCarouselIndex=idx;post.mediaUrl=generated.url;post.fallbackMediaUrl='';post.fallbackMediaId='';ensurePostFallbackMedia(post,activePostMenuIndex||0);updatePostEditSourceUI(post);renderPostEditCarousel(post);}catch(error){const reason=String(error?.message||error||'未知错误');alert(reason==='bridge_not_connected'?'当前没有连接到 Roche 生图接口。请先在 Roche 生图设置里完成配置。':`生图失败：${reason}`);}});
document.getElementById('postEditSave')?.addEventListener('click',saveActivePostEdit);


/* ===== v1.06 User Profile behavior ===== */
const userProfileState = {
  id: 'user.ins',
  displayName: 'User',
  location: '',
  bio: '记录一些喜欢的东西。',
  avatarUrl: 'https://imgbed.heliar.top/i/RLwxO3YcIyV5qmm3',
  defaultAvatarUrl: 'https://imgbed.heliar.top/i/RLwxO3YcIyV5qmm3',
  followers: 148,
  following: 86,
  activeTab: 'posts',
  editingHighlightId: null,
  highlights: [
    {id:'daily', name:'日常', coverUrl:''},
    {id:'fav', name:'收藏', coverUrl:''}
  ],
  posts: [],
  videos: [],
  reposts: []
};


function getUserAvatarUrl(){
  return userProfileState.avatarUrl || userProfileState.defaultAvatarUrl || '';
}

function applyUserAvatarVisual(){
  const avatar = document.querySelector('.user-avatar-wrap');
  const url = getUserAvatarUrl();
  if(avatar){
    avatar.classList.toggle('has-image', !!url);
    avatar.style.backgroundImage = url ? `url("${url}")` : '';
  }
  if(typeof hydrateIdentityAvatars==='function')hydrateIdentityAvatars(document);
}

function updateAvatarPreview(url){
  const preview = document.getElementById('profileAvatarPreview');
  if(!preview) return;
  preview.classList.toggle('has-image', !!url);
  preview.style.backgroundImage = url ? `url("${url}")` : '';
}

function renderUserHighlights(){
  const strip = document.getElementById('userHighlightStrip');
  if(!strip) return;

  const highlightItems = userProfileState.highlights.map(h=>{
    const cover = h.coverUrl
      ? `<img class="highlight-cover-img" src="${escapeDMCardHTML(h.coverUrl)}" alt="">`
      : (h.id === 'fav' ? '♡' : '✦');
    return `
      <button
        class="user-highlight"
        data-highlight-id="${escapeDMCardHTML(h.id)}"
        type="button"
        aria-label="编辑 Highlight：${escapeDMCardHTML(h.name)}"
      >
        <div class="user-highlight-ring">${cover}</div>
        <div class="user-highlight-name">${escapeDMCardHTML(h.name)}</div>
      </button>`;
  }).join('');

  strip.innerHTML = `
    <button
      class="user-highlight add"
      data-highlight-id="__new"
      type="button"
      aria-label="新建 Highlight"
    >
      <div class="user-highlight-ring">＋</div>
      <div class="user-highlight-name">新建</div>
    </button>
    ${highlightItems}
  `;
  bindHighlightGeometryFallback();
}


(userProfileState.highlights||[]).forEach(item=>{
  if(typeof item.description!=='string') item.description='';
});

Object.values(charProfileState.profiles||{}).forEach(profile=>{
  (profile.highlights||[]).forEach(item=>{
    if(typeof item.description!=='string') item.description='';
  });
});

function getUserOwnFeedPosts(){
  return posts.filter(p=>p.type==='user' || p.author==='user' || p.author===userProfileState.id);
}
function getUserOwnReels(){
  return (reelsV15State.items||[]).filter(item=>item.kind==='user' && (item.author==='user' || item.author===userProfileState.id));
}
function getUserRepostItems(){
  const feedRows=posts
    .filter(p=>p.reposted && !(p.type==='user'||p.author==='user'||p.author===userProfileState.id))
    .map(p=>({kind:'feed',post:p}));
  const reelRows=(reelsV15State.items||[])
    .filter(item=>item.reposted && item.kind!=='user')
    .map(item=>({kind:'reel',reel:item}));
  return [...feedRows,...reelRows];
}
function renderUserProfileGrid(active){
  const grid=document.getElementById('userProfileGrid');
  if(!grid)return;

  if(active==='posts'){
    const data=getUserOwnFeedPosts();
    if(!data.length){
      grid.innerHTML='<div class="user-empty"><strong>暂无动态</strong><span>发布 IG 动态后会直接出现在这里。</span></div>';
      return;
    }
    grid.innerHTML=`<div class="user-grid">${data.map((post,index)=>{
      const media=getPostCurrentMediaItem(post,posts.indexOf(post));
      return `<div class="user-cell ${media.url?'has-image':''}" data-user-post-id="${escapeDMCardHTML(post.id)}">${media.url?`<img src="${escapeDMCardHTML(media.url)}" alt="">`:escapeDMCardHTML(post.mediaDescription||'IG 动态')}</div>`;
    }).join('')}</div>`;
    return;
  }

  if(active==='videos'){
    const data=getUserOwnReels();
    if(!data.length){
      grid.innerHTML='<div class="user-empty"><strong>暂无 Reels</strong><span>发布 Reel 后会直接出现在这里。</span></div>';
      return;
    }
    grid.innerHTML=`<div class="user-grid">${data.map(item=>`
      <div class="user-cell reel-live" data-user-reel-id="${escapeDMCardHTML(item.id)}"><div class="repost-reel-card">${escapeDMCardHTML(item.caption||item.videoDescription||'Reel')}</div></div>
    `).join('')}</div>`;
    return;
  }

  const data=getUserRepostItems();
  if(!data.length){
    grid.innerHTML='<div class="user-empty"><strong>暂无转发</strong><span>转发 Feed / Reel 后会直接出现在这里。</span></div>';
    return;
  }
  grid.innerHTML=`<div class="user-grid">${data.map(row=>{
    if(row.kind==='feed'){
      const media=getPostCurrentMediaItem(row.post,posts.indexOf(row.post));
      return `<div class="user-cell repost repost-linked ${media.url?'has-image':''}" data-link-label="IG" data-user-repost-feed="${escapeDMCardHTML(row.post.id)}">${media.url?`<img src="${escapeDMCardHTML(media.url)}" alt="">`:`<div class="repost-reel-card">${escapeDMCardHTML(row.post.mediaDescription||row.post.captionShort||'IG 动态')}</div>`}</div>`;
    }
    return `<div class="user-cell repost reel-live repost-linked" data-link-label="REEL" data-user-repost-reel="${escapeDMCardHTML(row.reel.id)}"><div class="repost-reel-card">${escapeDMCardHTML(row.reel.caption||row.reel.videoDescription||'Reel')}</div></div>`;
  }).join('')}</div>`;
}
function ensureUserRepostPreview(){
  let dim=document.getElementById('userRepostPreviewDim');
  if(dim)return dim;
  dim=document.createElement('div');
  dim.id='userRepostPreviewDim';
  dim.className='user-repost-preview-dim';
  dim.innerHTML='<div id="userRepostPreviewCard" class="user-repost-preview-card"></div>';
  document.querySelector('.phone')?.appendChild(dim);
  dim.addEventListener('click',event=>{
    if(event.target.closest('[data-user-repost-translate]')){
      const post=posts.find(p=>p.id===event.target.closest('[data-user-repost-translate]').dataset.userRepostTranslate);
      if(post){post.translated=!post.translated;openUserRepostFeedCard(post.id);}
      return;
    }
    if(event.target===dim || event.target.closest('[data-user-repost-preview-close]')){
      dim.classList.remove('show');
    }
  });
  return dim;
}

function openUserRepostFeedCard(postId){
  const post=posts.find(p=>p.id===postId);
  if(!post)return;
  const dim=ensureUserRepostPreview();
  const card=document.getElementById('userRepostPreviewCard');
  if(!card)return;
  const media=getPostCurrentMediaItem(post,posts.indexOf(post));
  const identity=getIdentityMeta(post.author);
  const caption=[post.captionShort||'',post.captionLong||''].join('');
  const canTranslate=shouldOfferTranslation(caption,post.translation);
  card.innerHTML=`
    <div class="user-repost-preview-head">
      <div class="user-repost-preview-avatar" data-identity-avatar="${escapeDMCardHTML(post.author)}">${escapeDMCardHTML(identity.initial||'?')}</div>
      <div class="user-repost-preview-name">
        <strong>${escapeDMCardHTML(identity.name)}</strong>
        <span>@${escapeDMCardHTML(identity.handle||post.author)}</span>
        <div class="user-repost-preview-note">来自转发收藏</div>
      </div>
      <button class="user-repost-preview-close" data-user-repost-preview-close type="button">×</button>
    </div>
    <div class="user-repost-preview-media">
      ${media.url?`<img src="${escapeDMCardHTML(media.url)}" alt="">`:escapeDMCardHTML(post.mediaDescription||post.mediaText||'IG 动态')}
    </div>
    <div class="user-repost-preview-body">
      <div class="user-repost-preview-caption">${caption}</div>
      ${canTranslate?`<div class="user-repost-preview-translation-toggle" data-user-repost-translate="${escapeDMCardHTML(post.id)}">${post.translated?'收起翻译':'查看翻译'}</div><div class="user-repost-preview-translation ${post.translated?'show':''}">${escapeDMCardHTML(post.translation||'')}</div>`:''}
      <div class="user-repost-preview-metrics">
        <span>赞 ${fmtSocial(post.likes+(post.liked?1:0))}</span>
        <span>评论 ${fmtSocial(post.comments)}</span>
        <span>转发 ${fmtSocial(post.reposts+(post.reposted?1:0))}</span>
        <span>分享 ${fmtSocial(post.shares)}</span>
      </div>
    </div>`;
  hydrateIdentityAvatars(card);
  dim.classList.add('show');
}


function jumpUserRepostToReel(reelId){
  const item=getReelsV15Item(reelId);
  if(!item)return;
  reelsV15State.activeId=item.id;
  state.active='reels';
  renderNav();
  renderReelsV15();
  requestAnimationFrame(()=>{
    const node=[...document.querySelectorAll('[data-reels-v15-id]')]
      .find(el=>el.dataset.reelsV15Id===item.id);
    node?.scrollIntoView({block:'start',behavior:'smooth'});
  });
}

document.getElementById('userProfileGrid')?.addEventListener('click',event=>{
  const ownFeed=event.target.closest('[data-user-post-id]');
  if(ownFeed){openProfileContentDetail('user','feed',ownFeed.dataset.userPostId);return;}
  const ownReel=event.target.closest('[data-user-reel-id]');
  if(ownReel){openProfileContentDetail('user','reels',ownReel.dataset.userReelId);return;}
  const feed=event.target.closest('[data-user-repost-feed]');
  if(feed){
    const post=posts.find(p=>p.id===feed.dataset.userRepostFeed);
    if(post)openProfileContentDetail(post.author,'feed',post.id);
    return;
  }
  const reel=event.target.closest('[data-user-repost-reel]');
  if(reel){
    const item=getReelsV15Item(reel.dataset.userRepostReel);
    if(item)openProfileContentDetail(item.author,'reels',item.id);
  }
});

const profileContentDetailState={ownerRef:'',kind:'feed',selectedId:'',returnLabel:''};
function getProfileContentOwnerMeta(ref){
  if(ref==='user')return getIdentityMeta('user');
  return getIdentityMeta(ref);
}
function getFeedItemsForOwner(ref){
  if(ref==='user' || isINSCurrentUserIdentityRef(ref))return getUserOwnFeedPosts();
  const profileKey=resolveCharProfileKey(ref);
  if(charProfileState.profiles[profileKey])return getPostsForProfile(charProfileState.profiles[profileKey]);
  return posts.filter(p=>String(p.author||'')===String(ref||''));
}
function getReelItemsForOwner(ref){
  if(ref==='user' || isINSCurrentUserIdentityRef(ref))return getUserOwnReels();
  const profileKey=resolveCharProfileKey(ref);
  if(charProfileState.profiles[profileKey])return getReelsForProfile(charProfileState.profiles[profileKey]);
  return (reelsV15State.items||[]).filter(item=>String(item.author||'')===String(ref||''));
}
function ensureProfileContentDetailRoute(){
  let route=document.getElementById('profileContentDetailRoute');
  if(route)return route;
  route=document.createElement('section');
  route.id='profileContentDetailRoute';
  route.className='profile-content-detail-route';
  route.innerHTML=`
    <header class="pcd-head">
      <button id="pcdBackBtn" class="pcd-back" type="button" aria-label="返回">‹</button>
      <div class="pcd-title"><strong id="pcdTitle">Profile</strong><span id="pcdSubtitle">动态</span></div>
      <div></div>
    </header>
    <div id="pcdScroll" class="pcd-scroll"></div>`;
  document.querySelector('.phone')?.appendChild(route);
  route.querySelector('#pcdBackBtn')?.addEventListener('click',closeProfileContentDetail);
  route.querySelector('#pcdScroll')?.addEventListener('click',handleProfileContentDetailClick);
  return route;
}
function renderProfileContentDetail(scrollToSelected=true){
  const route=ensureProfileContentDetailRoute();
  const ownerRef=profileContentDetailState.ownerRef;
  const kind=profileContentDetailState.kind;
  const meta=getProfileContentOwnerMeta(ownerRef);
  const items=kind==='reels'?getReelItemsForOwner(ownerRef):getFeedItemsForOwner(ownerRef);
  route.querySelector('#pcdTitle').textContent='@'+(meta.handle||meta.name||ownerRef||'profile');
  route.querySelector('#pcdSubtitle').textContent=kind==='reels'?'Reels':'IG 动态';
  const scroll=route.querySelector('#pcdScroll');
  if(!items.length){
    scroll.innerHTML='<div class="route-empty"><strong>这里还没有内容</strong></div>';
    return;
  }
  scroll.innerHTML=items.map(item=>{
    const identity=getIdentityMeta(item.author);
    const selected=String(item.id)===String(profileContentDetailState.selectedId);
    const repostFromUser=ownerRef==='user' && String(item.author||'')!=='user';
    if(kind==='reels'){
      const canTranslate=shouldOfferTranslation(item.caption||'',item.translation);
      return `<article class="pcd-card ${selected?'pcd-selected':''}" data-pcd-id="${escapeDMCardHTML(item.id)}">
        <div class="pcd-card-head">
          <div class="pcd-avatar" data-identity-avatar="${escapeDMCardHTML(item.author)}">${escapeDMCardHTML(identity.initial||'?')}</div>
          <div class="pcd-author"><strong>${escapeDMCardHTML(identity.name)}</strong><span>@${escapeDMCardHTML(identity.handle||item.author)}</span>${repostFromUser?'<div class="pcd-repost-note">来自你的转发</div>':''}</div>
        </div>
        <div class="pcd-media reel">${escapeDMCardHTML(item.videoDescription||'Reel')}</div>
        <div class="pcd-body">
          <div class="pcd-actions">
            <button class="pcd-action" data-pcd-like="reels">♡ ${fmtSocial(item.likes+(item.liked?1:0))}</button>
            <button class="pcd-action" data-pcd-comments="reels">评论 ${fmtSocial(item.comments)}</button>
            <button class="pcd-action" data-pcd-repost="reels">↻ ${fmtSocial(item.reposts+(item.reposted?1:0))}</button>
            <span>分享 ${fmtSocial(Number(item.shares)||0)}</span>
          </div>
          <div class="pcd-caption"><strong>${escapeDMCardHTML(identity.name)}</strong>${escapeDMCardHTML(item.caption||'')}</div>
          ${canTranslate?`<div class="pcd-translation-toggle" data-pcd-translate="${escapeDMCardHTML(item.id)}">${item.translated?'收起翻译':'查看翻译'}</div><div class="pcd-translation ${item.translated?'show':''}">${escapeDMCardHTML(item.translation||'')}</div>`:''}
        </div>
      </article>`;
    }
    const media=getPostCurrentMediaItem(item,posts.indexOf(item));
    const caption=[item.captionShort||'',item.captionLong||''].join('').trim();
    const canTranslate=shouldOfferTranslation(caption,item.translation);
    return `<article class="pcd-card ${selected?'pcd-selected':''}" data-pcd-id="${escapeDMCardHTML(item.id)}">
      <div class="pcd-card-head">
        <div class="pcd-avatar" data-identity-avatar="${escapeDMCardHTML(item.author)}">${escapeDMCardHTML(identity.initial||'?')}</div>
        <div class="pcd-author"><strong>${escapeDMCardHTML(identity.name)}</strong><span>@${escapeDMCardHTML(identity.handle||item.author)}</span>${repostFromUser?'<div class="pcd-repost-note">来自你的转发</div>':''}</div>
      </div>
      <div class="pcd-media feed ${media.url?'has-image':'text-only'}">${media.url?`<img src="${escapeDMCardHTML(media.url)}" alt="">`:escapeDMCardHTML(item.mediaDescription||item.mediaText||'IG 动态')}</div>
      <div class="pcd-body">
        <div class="pcd-actions">
          <button class="pcd-action" data-pcd-like="feed">♡ ${fmtSocial(item.likes+(item.liked?1:0))}</button>
          <button class="pcd-action" data-pcd-comments="feed">评论 ${fmtSocial(item.comments)}</button>
          <button class="pcd-action" data-pcd-repost="feed">↻ ${fmtSocial(item.reposts+(item.reposted?1:0))}</button>
          <span>分享 ${fmtSocial(item.shares)}</span>
        </div>
        <div class="pcd-caption"><strong>${escapeDMCardHTML(identity.name)}</strong>${caption}</div>
        ${canTranslate?`<div class="pcd-translation-toggle" data-pcd-translate="${escapeDMCardHTML(item.id)}">${item.translated?'收起翻译':'查看翻译'}</div><div class="pcd-translation ${item.translated?'show':''}">${escapeDMCardHTML(item.translation||'')}</div>`:''}
      </div>
    </article>`;
  }).join('');
  hydrateIdentityAvatars(scroll);
  if(scrollToSelected){
    requestAnimationFrame(()=>{
      const target=scroll.querySelector(`[data-pcd-id="${CSS.escape(String(profileContentDetailState.selectedId||''))}"]`);
      scroll.scrollTop=target ? Math.max(0,target.offsetTop) : 0;
      document.documentElement.scrollTop=0;
      document.body.scrollTop=0;
    });
  }
}

function openProfileContentDetail(ownerRef,kind,selectedId){
  clearProfileInteractionOverlays();
  closeHighlightViewer();
  profileContentDetailState.ownerRef=ownerRef||'user';
  profileContentDetailState.kind=String(kind||'feed').toLowerCase()==='reels'?'reels':'feed';
  profileContentDetailState.selectedId=selectedId||'';
  const route=ensureProfileContentDetailRoute();
  route.classList.add('show');
  renderProfileContentDetail(true);
}
function closeProfileContentDetail(){
  const route=document.getElementById('profileContentDetailRoute');
  route?.classList.remove('show');
  const scroll=route?.querySelector('#pcdScroll');
  if(scroll)scroll.scrollTop=0;
  document.documentElement.scrollTop=0;
  document.body.scrollTop=0;
}
function handleProfileContentDetailClick(event){
  const card=event.target.closest('[data-pcd-id]');
  if(!card)return;
  const id=card.dataset.pcdId;
  const kind=profileContentDetailState.kind;
  if(event.target.closest('[data-pcd-like]')){
    if(kind==='reels'){
      const item=getReelsV15Item(id);if(item)item.liked=!item.liked;
    }else{
      const post=posts.find(p=>p.id===id);if(post)post.liked=!post.liked;
    }
    renderProfileContentDetail(false);return;
  }
  if(event.target.closest('[data-pcd-repost]')){
    if(kind==='reels'){
      const item=getReelsV15Item(id);if(item)item.reposted=!item.reposted;
    }else{
      const post=posts.find(p=>p.id===id);if(post)post.reposted=!post.reposted;
    }
    renderUserProfile();renderProfileContentDetail(false);return;
  }
  if(event.target.closest('[data-pcd-translate]')){
    if(kind==='reels'){
      const item=getReelsV15Item(id);if(item)item.translated=!item.translated;
    }else{
      const post=posts.find(p=>p.id===id);if(post)post.translated=!post.translated;
    }
    renderProfileContentDetail(false);return;
  }
  if(event.target.closest('[data-pcd-comments]')){
    if(kind==='reels'){
      reelsV15State.activeId=id;renderReelsV15Comments();openReelsV15Sheet('reelsV15CommentSheet');
    }else openCommentsForPost(id);
  }
}

function renderUserProfile(){
  const topId = document.getElementById('userProfileTopId');
  const display = document.getElementById('userDisplayName');
  const handle = document.getElementById('userHandle');
  const location = document.getElementById('userProfileLocation');
  const bio = document.getElementById('userBio');
  const followers = document.getElementById('userFollowers');
  const following = document.getElementById('userFollowing');
  const count = document.getElementById('userPostCount');

  if(!topId) return;

  topId.textContent = userProfileState.id;
  display.textContent = userProfileState.displayName;
  handle.textContent = '@' + userProfileState.id;
  if(location){const text=String(userProfileState.location||'').trim();location.classList.toggle('show',!!text);const span=location.querySelector('span');if(span)span.textContent=text;}
  bio.innerHTML = escapeDMCardHTML(userProfileState.bio).replace(/\n/g,'<br>');
  followers.textContent = userProfileState.followers;
  following.textContent = userProfileState.following;
  count.textContent = getUserOwnFeedPosts().length + getUserOwnReels().length;
  applyUserAvatarVisual();
  hydrateIdentityAvatars(document);
  renderUserHighlights();

  const userTabs = [...document.querySelectorAll('.user-tab')];
  userTabs.forEach(btn=>{
    btn.classList.toggle('active', btn.dataset.userTab === userProfileState.activeTab);
  });
  const indicator = document.getElementById('userTabIndicator');
  const activeIndex = Math.max(0, userTabs.findIndex(btn => btn.dataset.userTab === userProfileState.activeTab));
  if(indicator){
    indicator.style.transform = `translateX(${activeIndex * 100}%)`;
  }

  renderUserProfileGrid(userProfileState.activeTab);
}


const INS_WORLD_PROMPT_STORAGE_KEY='nini_ins_world_culture_prompt_v5018';
const atmospherePromptSettings = {
  mode:'editable_world_layer',
  customPrompt:''
};
try{atmospherePromptSettings.customPrompt=localStorage.getItem(INS_WORLD_PROMPT_STORAGE_KEY)||'';}catch(e){}
function getINSWorldCulturePrompt(){
  return String(atmospherePromptSettings.customPrompt||'').trim() || INS_DEFAULT_WORLD_CULTURE_PROMPT;
}
function openAtmosphereSettings(){
  closeProfileDrawer();
  document.getElementById('profileDrawerDim')?.classList.add('show');
  document.getElementById('profileAtmosphereCard')?.classList.add('show');
  const textarea=document.getElementById('profileWorldCulturePrompt');
  if(textarea)textarea.value=getINSWorldCulturePrompt();
}
function closeAtmosphereSettings(){
  document.getElementById('profileAtmosphereCard')?.classList.remove('show');
  document.getElementById('profileDrawerDim')?.classList.remove('show');
}
function getAtmospherePromptContext(target='feed'){
  const maps={
    feed:'平台默认规则：全球化 INS 世界；动态偏生活流；不是所有人都围着 user 转。',
    story:'平台默认规则：Story 轻量即时；无评论线程；进一步交流转 DM。',
    comments:'平台默认规则：评论允许熟人、关系 NPC、陌生人混合出现；可以看见但不一定回复。',
    strangers:'平台默认规则：陌生人保持正常社交距离；来源应合理；不应突然过度熟悉。'
  };
  return maps[String(target||'feed').toLowerCase()] || maps.feed;
}

const insImagePromptSettings = {
  descriptionLanguage:'zh',
  positive:'',
  negative:''
};
function installINSImageSettingsUI(){
  const drawerButton=document.getElementById('drawerImagePromptBtn');
  const drawerTitle=drawerButton?.querySelector('.drawer-copy strong');
  const drawerLabel=drawerButton?.querySelector('.drawer-copy span');
  if(drawerTitle)drawerTitle.textContent='生图设置';
  if(drawerLabel){
    drawerLabel.id='drawerImageSettingsLabel';
    if(!drawerLabel.textContent?.trim() || drawerLabel.textContent.includes('留空'))drawerLabel.textContent='自动关联 Roche 当前生图 API';
  }

  const card=document.getElementById('profileImagePromptCard');
  const cardTitle=card?.querySelector('.profile-setting-head strong');
  if(cardTitle)cardTitle.textContent='生图设置';
  const originalFirstBlock=card?.querySelector('.profile-setting-block');
  const priorityNote=originalFirstBlock?.querySelector('.profile-setting-notebox');
  if(priorityNote)priorityNote.innerHTML='INS 不读取或保存 Roche 内部的 URL、Key、渠道和模型。生图时直接调用 <b>roche.ai.generateImage</b>；实际提示词由 <b>INS 提示词（如有）+ INS 生图规则 + AI 图片描述</b> 组成。';
  if(card&&!document.getElementById('insRocheImageApiBlock')){
    originalFirstBlock?.insertAdjacentHTML('beforebegin',`
      <div class="profile-setting-block" id="insRocheImageApiBlock">
        <label class="profile-setting-label">Roche 生图功能</label>
        <div class="profile-setting-notebox" id="insRocheImageApiStatus">正在检测 roche.ai.generateImage…</div>
        <button class="profile-subapi-mini ins-image-api-refresh" id="insRocheImageApiRefresh" type="button">重新检测 Roche 生图功能</button>
      </div>
      <div class="profile-setting-block" id="insImageGenerationTestBlock">
        <label class="profile-setting-label" for="insImageGenerationTestPrompt">测试生图</label>
        <textarea class="profile-setting-textarea" id="insImageGenerationTestPrompt" placeholder="输入一段测试图片描述">窗边的一杯咖啡和一本打开的书，真实自然的手机随手拍</textarea>
        <div class="profile-setting-notebox ins-image-test-note">测试会直接调用 Roche 当前的生图功能。成功后图片会显示在下方，不会发布到 Feed 或 Story。</div>
        <button class="profile-setting-save" id="insImageGenerationTestBtn" type="button">测试生图</button>
        <div class="ins-image-test-result" id="insImageGenerationTestResult" hidden></div>
        <div class="ins-image-test-preview" id="insImageGenerationTestPreview" hidden>
          <img id="insImageGenerationTestImage" alt="生图测试结果"/>
        </div>
      </div>`);
  }
}
async function refreshINSRocheImageApiStatus(){
  installINSImageSettingsUI();
  const status=document.getElementById('insRocheImageApiStatus');
  const refresh=document.getElementById('insRocheImageApiRefresh');
  const drawerLabel=document.getElementById('drawerImageSettingsLabel');
  if(status)status.textContent='正在检测 Roche 生图功能…';
  if(refresh){refresh.disabled=true;refresh.textContent='检测中…';}
  try{
    const bridge=getRocheImageBridge();
    if(!bridge){
      if(status)status.textContent='未检测到 Roche 生图接口。请先在 Roche 完成生图配置。';
      if(drawerLabel)drawerLabel.textContent='未检测到 Roche 生图功能';
      return {available:false,route:''};
    }
    let capability=null;
    try{capability=await bridge.getImageCapability?.();}catch(error){}
    if(!capability){
      const route=typeof bridge.ai?.generateImage==='function'?'roche.ai.generateImage':typeof bridge.generateImage==='function'?'Roche 生图桥接':typeof bridge.image?.generate==='function'?'Roche image.generate':'';
      capability={available:!!route,route};
    }
    if(capability.available){
      const route=String(capability.route||'roche.ai.generateImage');
      if(status)status.textContent=`已关联 Roche 当前生图功能 · ${route}`;
      if(drawerLabel)drawerLabel.textContent='已关联 Roche 生图功能';
    }else{
      if(status)status.textContent='当前 Roche 没有提供可用的生图函数。请先在 Roche 完成生图配置。';
      if(drawerLabel)drawerLabel.textContent='Roche 生图功能不可用';
    }
    return capability;
  }catch(error){
    if(status)status.textContent='检测 Roche 生图功能失败：'+String(error?.message||error||'未知错误');
    if(drawerLabel)drawerLabel.textContent='Roche 生图功能检测失败';
    return {available:false,route:''};
  }finally{
    if(refresh){refresh.disabled=false;refresh.textContent='重新检测 Roche 生图功能';}
  }
}
async function runINSImageGenerationTest(){
  const prompt=String(document.getElementById('insImageGenerationTestPrompt')?.value||'').trim();
  const button=document.getElementById('insImageGenerationTestBtn');
  const result=document.getElementById('insImageGenerationTestResult');
  const preview=document.getElementById('insImageGenerationTestPreview');
  const image=document.getElementById('insImageGenerationTestImage');
  if(!prompt){
    if(result){result.hidden=false;result.textContent='请先填写测试图片描述。';result.dataset.state='error';}
    return;
  }
  insImagePromptSettings.descriptionLanguage=document.getElementById('insImagePromptLanguage')?.value||'zh';
  insImagePromptSettings.positive=document.getElementById('insImagePromptPositive')?.value.trim()||'';
  insImagePromptSettings.negative=document.getElementById('insImagePromptNegative')?.value.trim()||'';
  if(button){button.disabled=true;button.textContent='生成中…';}
  if(result){result.hidden=false;result.textContent='正在调用 Roche 生图功能…';result.dataset.state='loading';}
  if(preview)preview.hidden=true;
  if(image)image.removeAttribute('src');
  try{
    const generated=await requestRocheGeneratedImage({description:prompt,entry:'settings_test'});
    if(!generated?.url)throw new Error('no_image_url');
    if(image)image.src=generated.url;
    if(preview)preview.hidden=false;
    if(result){result.textContent='测试成功，已收到 Roche 返回的图片。';result.dataset.state='success';}
  }catch(error){
    const reason=String(error?.message||error||'未知错误');
    const message=reason==='bridge_not_connected'?'未连接到 Roche 生图功能。':reason==='image_generation_unavailable'?'Roche 当前没有可用的生图函数。':reason==='no_image_url'?'Roche 已响应，但没有返回可显示的图片地址。':`测试失败：${reason}`;
    if(result){result.hidden=false;result.textContent=message;result.dataset.state='error';}
  }finally{
    if(button){button.disabled=false;button.textContent='测试生图';}
  }
}
function openImagePromptSettings(){
  installINSImageSettingsUI();
  closeProfileDrawer();
  document.getElementById('profileDrawerDim')?.classList.add('show');
  const card=document.getElementById('profileImagePromptCard');
  card?.classList.add('show');
  if(card)card.scrollTop=0;
  document.getElementById('insImagePromptLanguage').value=insImagePromptSettings.descriptionLanguage||'zh';
  document.getElementById('insImagePromptPositive').value=insImagePromptSettings.positive||'';
  document.getElementById('insImagePromptNegative').value=insImagePromptSettings.negative||'';
  void refreshINSRocheImageApiStatus();
}
function closeImagePromptSettings(){
  document.getElementById('profileImagePromptCard')?.classList.remove('show');
  document.getElementById('profileDrawerDim')?.classList.remove('show');
}
function getINSImagePromptConfig(){
  const positive=(insImagePromptSettings.positive||'').trim();
  const negative=(insImagePromptSettings.negative||'').trim();
  return {
    descriptionLanguage:insImagePromptSettings.descriptionLanguage||'zh',
    source:(positive||negative)?'ins_override':'roche_direct',
    positivePrompt:positive,
    negativePrompt:negative
  };
}
function buildINSImageGenerationRouting(mediaDescription=''){
  const cfg=getINSImagePromptConfig();
  return {
    source:cfg.source,
    descriptionLanguage:cfg.descriptionLanguage,
    positivePrompt:cfg.positivePrompt,
    negativePrompt:cfg.negativePrompt,
    composedPrompt:cfg.positivePrompt?`${cfg.positivePrompt}\n${String(mediaDescription||'').trim()}`:String(mediaDescription||'').trim()
  };
}

function getINSImageInternalPositive(entry='feed'){
  const target=String(entry||'feed').toLowerCase();
  const common='真实自然的社交媒体照片，适合 INS 发布；生活化、日常感、不过度棚拍；无水印、无 logo、无拼贴字卡、无界面截图、无多余文字叠层。';
  if(target==='story')return common+' Story 画面更轻即时，像手机随手拍，但依然清晰自然。';
  if(target==='post_edit')return common+' 用于替换当前动态图片，保留原有图片描述语义，不要跑题。';
  return common+' Feed 画面可稍微更完整，像真实用户发布的单张或多图内容。';
}
function getINSImageInternalNegative(entry='feed'){
  return '不要生成水印、UI 截图、营销海报排版、过度文字、畸形肢体、严重失真、低清晰度、明显 AI 瑕疵。';
}
function getRocheImageBridge(){
  const direct=window.NiniINSRocheBridge||window.RocheBridge||window.RocheImageBridge||null;
  if(direct)return direct;
  try{
    if(window.parent && window.parent!==window){
      return window.parent.NiniINSRocheBridge||window.parent.RocheBridge||window.parent.RocheImageBridge||null;
    }
  }catch(e){}
  return null;
}
async function getRocheImageConfigSnapshot(){
  const bridge=getRocheImageBridge();
  const out={channel:'',endpoint:'',model:'',positivePrompt:'',negativePrompt:'',raw:null};
  if(!bridge)return out;
  const tries=[
    ()=>bridge.getImageConfig?.(),
    ()=>bridge.image?.getConfig?.(),
    ()=>bridge.ai?.getImageConfig?.(),
    ()=>bridge.imageConfig?.get?.(),
    ()=>bridge.getConfig?.('image'),
    ()=>bridge.invoke?.('image.getConfig',{}),
    ()=>bridge.call?.('image.getConfig',{})
  ];
  for(const fn of tries){
    if(typeof fn!=='function')continue;
    try{
      const value=await fn();
      if(!value)continue;
      const raw=value?.data||value?.config||value;
      out.raw=raw;
      out.channel=String(raw?.channel||raw?.provider||raw?.format||raw?.mode||'');
      out.endpoint=String(raw?.endpoint||raw?.baseURL||raw?.baseUrl||raw?.url||'');
      out.model=String(raw?.model||raw?.modelName||raw?.imageModel||'');
      out.positivePrompt=String(raw?.defaultPositive||raw?.positivePrompt||raw?.positive||raw?.promptPositive||'');
      out.negativePrompt=String(raw?.defaultNegative||raw?.negativePrompt||raw?.negative||raw?.promptNegative||'');
      return out;
    }catch(error){}
  }
  return out;
}
function buildINSImageGenerationRequest({description='',entry='feed',count=1}={}){
  const cleanDesc=String(description||'').trim();
  const cfg=getINSImagePromptConfig();
  const internalPositive=getINSImageInternalPositive(entry);
  const internalNegative=getINSImageInternalNegative(entry);
  const basePositive=(cfg.positivePrompt||'').trim();
  const baseNegative=(cfg.negativePrompt||'').trim();
  const finalPositive=[basePositive,internalPositive,cleanDesc].filter(Boolean).join('\n');
  const finalNegative=[baseNegative,internalNegative].filter(Boolean).join('\n');
  const payload={
    prompt:finalPositive,
    description:cleanDesc,
    positivePrompt:finalPositive,
    negativePrompt:finalNegative,
    entry,
    count:Math.max(1,Number(count)||1),
    aspectRatio:'1:1',
    meta:{
      source:'ins',
      plugin:'INS',
      routingSource:cfg.source,
      descriptionLanguage:cfg.descriptionLanguage||'zh'
    }
  };
  return {payload};
}
function normalizeGeneratedImageUrl(value){
  if(!value)return '';
  const raw=String(value||'').trim();
  if(!raw)return '';
  if(/^data:image\//i.test(raw) || /^https?:\/\//i.test(raw) || /^blob:/i.test(raw))return raw;
  if(/^[A-Za-z0-9+/=\n\r]+$/.test(raw) && raw.length>96)return `data:image/png;base64,${raw.replace(/[\r\n]+/g,'')}`;
  return '';
}
function extractGeneratedImageUrl(result){
  if(!result)return '';
  if(typeof result==='string')return normalizeGeneratedImageUrl(result);
  if(Array.isArray(result)){
    for(const item of result){
      const url=extractGeneratedImageUrl(item);
      if(url)return url;
    }
    return '';
  }
  const directKeys=['url','imageUrl','outputUrl','src','image','base64','b64_json'];
  for(const key of directKeys){
    const url=normalizeGeneratedImageUrl(result?.[key]);
    if(url)return url;
  }
  const nested=[result?.data,result?.images,result?.results,result?.output,result?.items,result?.choices];
  for(const part of nested){
    const url=extractGeneratedImageUrl(part);
    if(url)return url;
  }
  return '';
}
async function requestRocheGeneratedImage({description='',entry='feed',count=1}={}){
  const cleanDesc=String(description||'').trim();
  if(!cleanDesc)throw new Error('missing_description');
  const bridge=getRocheImageBridge();
  if(!bridge)throw new Error('bridge_not_connected');
  const {payload}=buildINSImageGenerationRequest({description:cleanDesc,entry,count});
  const calls=[
    ()=>bridge.generateImage?.(payload),
    ()=>bridge.image?.generate?.(payload),
    ()=>bridge.ai?.generateImage?.(payload),
    ()=>bridge.invoke?.('image.generate',payload),
    ()=>bridge.call?.('image.generate',payload),
    ()=>bridge.generate?.({type:'image',...payload})
  ];
  let lastError=null;
  for(const fn of calls){
    if(typeof fn!=='function')continue;
    try{
      const result=await fn();
      const url=extractGeneratedImageUrl(result);
      if(url)return {url,result,request:payload};
      lastError=new Error('no_image_url');
    }catch(error){lastError=error;}
  }
  throw lastError||new Error('image_generation_unavailable');
}
async function withINSImageButtonBusy(button,busyText,work){
  const btn=button||null;
  const oldText=btn?btn.textContent:'';
  if(btn){btn.disabled=true;btn.textContent=busyText||'生成中…';}
  try{return await work();}
  finally{if(btn){btn.disabled=false;btn.textContent=oldText||'生图';}}
}

const insSubApiSettings = {
  presets:[],
  activeId:''
};
function getActiveINSSubApi(){
  return null;
}
function updateDrawerSubApiLabel(){
  const el=document.getElementById('drawerSubApiSubLabel');
  if(!el)return;
  const active=getActiveINSSubApi();
  el.textContent=active?`${active.name} · ${active.model}`:'默认 Roche AI / 未单独覆盖';
}
function openSubApiSettings(){
  closeProfileDrawer();
  document.getElementById('profileDrawerDim')?.classList.add('show');
  document.getElementById('profileSubApiCard')?.classList.add('show');
  renderINSSubApiSettings();
}
function closeSubApiSettings(){
  document.getElementById('profileSubApiCard')?.classList.remove('show');
  document.getElementById('profileDrawerDim')?.classList.remove('show');
}
function renderINSSubApiSettings(){
  const list=document.getElementById('insSubApiPresetList');
  const route=document.getElementById('insSubApiCurrentRoute');
  const active=getActiveINSSubApi();

  if(route){
    route.innerHTML=active
      ? `当前已单独覆盖为 <b>${escapeDMCardHTML(active.name)}</b> · ${escapeDMCardHTML(active.model)}<br>${escapeDMCardHTML(active.url)}`
      : '默认使用 Roche 内置 AI。未单独覆盖 INS 的调用链。';
  }

  if(!list)return;
  if(!insSubApiSettings.presets.length){
    list.innerHTML='<div class="profile-subapi-empty">还没有保存任何副 API 预设。留空时，INS 默认继续走 Roche 全局 AI。</div>';
    updateDrawerSubApiLabel();
    return;
  }

  list.innerHTML=insSubApiSettings.presets.map(p=>`
    <div class="profile-subapi-item ${p.id===insSubApiSettings.activeId?'active':''}">
      <div class="profile-subapi-copy">
        <strong>${escapeDMCardHTML(p.name)}</strong>
        <span>${escapeDMCardHTML(p.url)} · ${escapeDMCardHTML(p.model)}</span>
      </div>
      <div class="profile-subapi-actions">
        <button class="${p.id===insSubApiSettings.activeId?'on':'off'}" data-ins-subapi-enable="${escapeDMCardHTML(p.id)}" type="button">${p.id===insSubApiSettings.activeId?'已启用':'启用'}</button>
        <button class="danger" data-ins-subapi-delete="${escapeDMCardHTML(p.id)}" type="button">删除</button>
      </div>
    </div>
  `).join('');
  updateDrawerSubApiLabel();
}
async function refreshINSSubApiModels(){
  if(!document.getElementById('profileSubApiCard')?.classList.contains('show'))return;
  const url=(document.getElementById('insSubApiUrl')?.value||'').trim().replace(/\/+$/,'');
  const apiKey=(document.getElementById('insSubApiKey')?.value||'').trim();
  const modelSel=document.getElementById('insSubApiModel');
  if(!url || !apiKey){
    alert('先填写 Base URL 和 API Key');
    return;
  }
  if(modelSel)modelSel.innerHTML='<option value="">正在获取...</option>';
  try{
    const res=await fetch(url+'/models',{headers:{'Authorization':'Bearer '+apiKey}});
    if(!res.ok) throw new Error('状态 '+res.status);
    const data=await res.json();
    const models=(Array.isArray(data?.data)?data.data:[])
      .map(item=>String(item.id||'').trim())
      .filter(Boolean);
    if(!modelSel)return;
    if(!models.length){
      modelSel.innerHTML='<option value="">未获取到模型</option>';
      return;
    }
    modelSel.innerHTML=models.map(id=>`<option value="${escapeDMCardHTML(id)}">${escapeDMCardHTML(id)}</option>`).join('');
  }catch(error){
    if(modelSel)modelSel.innerHTML='<option value="">获取失败，请手填</option>';
    alert('刷新模型失败：'+(error?.message||'未知错误'));
  }
}
function saveINSSubApiPreset(){
  if(!document.getElementById('profileSubApiCard')?.classList.contains('show'))return;
  const name=(document.getElementById('insSubApiName')?.value||'').trim();
  const url=(document.getElementById('insSubApiUrl')?.value||'').trim();
  const apiKey=(document.getElementById('insSubApiKey')?.value||'').trim();
  const model=(document.getElementById('insSubApiModel')?.value||'').trim();
  if(!name || !url || !apiKey || !model){
    alert('请完整填写名称、Base URL、API Key 和模型');
    return;
  }
  insSubApiSettings.presets.push({
    id:'insapi_'+Date.now().toString(36)+Math.random().toString(36).slice(2,6),
    name,url,apiKey,model
  });
  document.getElementById('insSubApiName').value='';
  document.getElementById('insSubApiUrl').value='';
  document.getElementById('insSubApiKey').value='';
  document.getElementById('insSubApiModel').innerHTML='<option value="">先点刷新模型获取</option>';
  renderINSSubApiSettings();
}

function isHomeFeedTranslationApiConfigured(){
  return !!String(homeFeedTranslationApiSettings.url||'').trim()
    && !!String(homeFeedTranslationApiSettings.apiKey||'').trim()
    && !!String(homeFeedTranslationApiSettings.model||'').trim();
}
function updateHomeFeedTranslationApiDrawerLabel(){
  const label=document.getElementById('drawerFeedTranslationApiLabel');
  if(label)label.textContent=isHomeFeedTranslationApiConfigured()
    ? `已配置 · ${String(homeFeedTranslationApiSettings.model||'')}`
    : '未配置 · 仅首页显示原文时使用';
}
function installHomeFeedTranslationApiUI(){
  const imagePromptButton=document.getElementById('drawerImagePromptBtn');
  if(imagePromptButton&&!document.getElementById('drawerFeedTranslationApiBtn')){
    imagePromptButton.insertAdjacentHTML('afterend',`
      <button class="drawer-item" id="drawerFeedTranslationApiBtn" type="button">
        <span class="drawer-icon">🌐</span>
        <span class="drawer-copy"><strong>Feed 正文翻译 API</strong><span id="drawerFeedTranslationApiLabel">未配置 · 仅首页显示原文时使用</span></span>
        <span class="drawer-arrow">›</span>
      </button>`);
  }
  const phone=document.querySelector('.phone');
  if(phone&&!document.getElementById('profileFeedTranslationApiCard')){
    phone.insertAdjacentHTML('beforeend',`
      <section class="profile-setting-card" id="profileFeedTranslationApiCard">
        <div class="profile-setting-head">
          <strong>Feed 正文翻译 API</strong>
          <button class="profile-setting-close" id="profileFeedTranslationApiClose" type="button">×</button>
        </div>
        <div class="profile-setting-block">
          <div class="profile-setting-notebox">只在首页 Feed 点「显示原文」且该动态尚无缓存时调用一次。要求接口兼容 OpenAI <b>/chat/completions</b>；不会影响主生成 API、Reels、Story、评论或 DM。</div>
        </div>
        <div class="profile-setting-block">
          <label class="profile-setting-label" for="feedTranslationApiUrl">Base URL 或完整接口 URL</label>
          <input class="profile-setting-input" id="feedTranslationApiUrl" inputmode="url" placeholder="例如 https://api.example.com/v1"/>
        </div>
        <div class="profile-setting-block">
          <label class="profile-setting-label" for="feedTranslationApiKey">API Key</label>
          <input class="profile-setting-input" id="feedTranslationApiKey" autocomplete="off" placeholder="填写翻译接口 Key" type="password"/>
        </div>
        <div class="profile-setting-block">
          <label class="profile-setting-label" for="feedTranslationApiModel">模型</label>
          <div class="profile-subapi-formrow">
            <select class="profile-setting-select" id="feedTranslationApiModel">
              <option value="">先填写 URL / Key，再拉取模型</option>
            </select>
            <button class="profile-subapi-mini" id="feedTranslationApiRefreshModels" type="button">拉取模型</button>
          </div>
        </div>
        <div class="profile-setting-notebox">配置只保存在当前设备本地。插件不会自动测试接口，也不会失败后自动重试；每次点「重试」才会再次请求。</div>
        <button class="profile-setting-save" id="profileFeedTranslationApiSave" type="button">保存 Feed 翻译 API</button>
      </section>`);
  }
  updateHomeFeedTranslationApiDrawerLabel();
}
function openHomeFeedTranslationApiSettings(){
  installHomeFeedTranslationApiUI();
  closeProfileDrawer();
  document.getElementById('profileDrawerDim')?.classList.add('show');
  const card=document.getElementById('profileFeedTranslationApiCard');
  card?.classList.add('show');
  if(card)card.scrollTop=0;
  const url=document.getElementById('feedTranslationApiUrl');
  const key=document.getElementById('feedTranslationApiKey');
  const model=document.getElementById('feedTranslationApiModel');
  if(url)url.value=homeFeedTranslationApiSettings.url||'';
  if(key)key.value=homeFeedTranslationApiSettings.apiKey||'';
  if(model){
    const current=String(homeFeedTranslationApiSettings.model||'').trim();
    model.innerHTML=current
      ? `<option value="${escapeDMCardHTML(current)}">${escapeDMCardHTML(current)}</option>`
      : '<option value="">先填写 URL / Key，再拉取模型</option>';
    model.value=current;
  }
}
function closeHomeFeedTranslationApiSettings(){
  document.getElementById('profileFeedTranslationApiCard')?.classList.remove('show');
  document.getElementById('profileDrawerDim')?.classList.remove('show');
}
function normalizeHomeFeedModelsEndpoint(url=''){
  const clean=String(url||'').trim().replace(/\/+$/,'');
  if(!clean)return '';
  return clean.replace(/\/chat\/completions$/i,'') + '/models';
}
async function refreshHomeFeedTranslationModels(){
  const url=String(document.getElementById('feedTranslationApiUrl')?.value||'').trim();
  const apiKey=String(document.getElementById('feedTranslationApiKey')?.value||'').trim();
  const select=document.getElementById('feedTranslationApiModel');
  if(!url||!apiKey){alert('先填写 Base URL 和 API Key');return;}
  if(select)select.innerHTML='<option value="">正在拉取模型...</option>';
  try{
    const res=await fetch(normalizeHomeFeedModelsEndpoint(url),{headers:{Authorization:'Bearer '+apiKey}});
    if(!res.ok)throw new Error('状态 '+res.status);
    const data=await res.json();
    const models=(Array.isArray(data?.data)?data.data:[])
      .map(item=>String(item?.id||'').trim())
      .filter(Boolean);
    if(!select)return;
    if(!models.length){
      select.innerHTML='<option value="">未获取到模型</option>';
      return;
    }
    const current=String(homeFeedTranslationApiSettings.model||'').trim();
    select.innerHTML=models.map(id=>`<option value="${escapeDMCardHTML(id)}">${escapeDMCardHTML(id)}</option>`).join('');
    if(current&&models.includes(current))select.value=current;
  }catch(error){
    if(select)select.innerHTML='<option value="">拉取失败，请检查 URL / Key</option>';
    alert('拉取模型失败：'+(error?.message||'未知错误'));
  }
}
function saveHomeFeedTranslationApiSettings(){
  const url=String(document.getElementById('feedTranslationApiUrl')?.value||'').trim().replace(/\/+$/,'');
  const apiKey=String(document.getElementById('feedTranslationApiKey')?.value||'').trim();
  const model=String(document.getElementById('feedTranslationApiModel')?.value||'').trim();
  if(!url||!apiKey||!model){alert('请完整填写 URL、API Key 和模型');return;}
  if(!/^https?:\/\//i.test(url)){alert('URL 需要以 http:// 或 https:// 开头');return;}
  homeFeedTranslationApiSettings={url,apiKey,model};
  persistHomeFeedTranslationApiSettings();
  updateHomeFeedTranslationApiDrawerLabel();
  closeHomeFeedTranslationApiSettings();
  alert('Feed 正文翻译 API 已保存');
}

function normalizeINSUserBindingPersona(raw={}){
  const id=String(raw?.id||raw?.personaId||raw?.handle||raw?.name||'').trim();
  const handle=String(raw?.handle||raw?.username||raw?.id||raw?.name||'').replace(/^@/,'').trim();
  const name=String(raw?.name||raw?.displayName||handle||id||'').trim();
  return {id:id||handle||name,handle:handle||id||name,name:name||handle||id,bio:String(raw?.bio||''),avatar:String(raw?.avatar||raw?.avatarUrl||''),active:!!raw?.active};
}
async function getAvailableINSUserBindingPersonas(){
  let list=[];
  try{if(window.NiniINSHost?.getAvailableUserPersonas){const raw=await window.NiniINSHost.getAvailableUserPersonas();if(Array.isArray(raw))list=raw;}}catch(error){console.warn('[INS] 获取可绑定 user persona 失败',error);}
  const current=normalizeINSUserBindingPersona({id:userProfileState.rochePersonaId||userProfileState.id,handle:userProfileState.id,name:userProfileState.displayName,bio:userProfileState.bio,avatar:userProfileState.avatarUrl||userProfileState.defaultAvatarUrl||'',active:true});
  const map=new Map();
  [current,...list.map(normalizeINSUserBindingPersona)].forEach(persona=>{if(!persona?.id && !persona?.handle)return;const key=String(persona.id||persona.handle);map.set(key,map.has(key)?{...map.get(key),...persona}:persona);});
  const items=[...map.values()];
  const activeKey=String(userProfileState.rochePersonaId||current.id||'');
  items.forEach(item=>{item.active=String(item.id||'')===activeKey || String(item.handle||'')===String(userProfileState.id||'');});
  return items;
}
function applyBoundINSUserPersona(rawPersona={}){
  const persona=normalizeINSUserBindingPersona(rawPersona);
  if(!persona.id && !persona.handle)return;
  userProfileState.rochePersonaId=persona.id||userProfileState.rochePersonaId||'';
  userProfileState.id=persona.handle||userProfileState.id;
  userProfileState.displayName=persona.name||userProfileState.displayName;
  userProfileState.bio=persona.bio || '';
  userProfileState.avatarUrl=persona.avatar || '';
  userProfileState.defaultAvatarUrl=persona.avatar || '';
  renderRocheHydratedIdentityUI?.();
  renderDMImportActorList?.();
  renderDMUserNpcHub?.();
  renderFeedShareDMRecipients?.();
  renderReelsV15DMRecipients?.();
  syncAllDMListPreviews?.();
  renderUserProfile?.();
  renderFeed?.();
  renderReelsV15?.();
  renderCreatePostCharChoices?.();
  hydrateIdentityAvatars(document);
}
async function renderINSUserIdentityBindingList(){
  const wrap=document.getElementById('identityBindList');
  if(!wrap)return;
  const personas=await getAvailableINSUserBindingPersonas();
  if(!personas.length){wrap.innerHTML='<div class="identity-bind-empty">还没有可切换的 user persona。</div>';return;}
  wrap.innerHTML=personas.map(persona=>{const avatarStyle=persona.avatar?` style="background-image:url(&quot;${escapeDMCardHTML(persona.avatar)}&quot;);"`:'';return `<div class="identity-bind-row"><div class="identity-bind-avatar"${avatarStyle}>${persona.avatar?'':escapeDMCardHTML((persona.name||persona.handle||'?').slice(0,1).toUpperCase())}</div><div class="identity-bind-copy"><div class="identity-bind-name">${escapeDMCardHTML(persona.name||persona.handle||persona.id)}</div><div class="identity-bind-handle">@${escapeDMCardHTML(persona.handle||persona.id)}</div><div class="identity-bind-meta">${persona.active?'当前绑定的 INS user 身份':'切换后，INS 里的 user 主页、头像、发帖身份都会一起切换。'}</div></div><button class="identity-bind-apply ${persona.active?'light':''}" type="button" data-bind-persona="${escapeDMCardHTML(persona.id)}">${persona.active?'已绑定':'切换'}</button></div>`;}).join('');
}
function openINSUserIdentityBindingSheet(){closeProfileDrawer();document.getElementById('profileDrawerDim')?.classList.add('show');document.getElementById('identityBindSheet')?.classList.add('show');void renderINSUserIdentityBindingList();}
function closeINSUserIdentityBindingSheet(){document.getElementById('identityBindSheet')?.classList.remove('show');}
async function switchINSBoundUserPersona(personaId){const targetId=String(personaId||'').trim();if(!targetId)return;const list=await getAvailableINSUserBindingPersonas();let persona=list.find(item=>String(item.id||'')===targetId) || list.find(item=>String(item.handle||'')===targetId);if(!persona)return;try{if(window.NiniINSHost?.setActiveUserPersona){const result=await window.NiniINSHost.setActiveUserPersona(targetId);if(result && typeof result==='object')persona=normalizeINSUserBindingPersona(result);}if(window.NiniINSHost?.getActiveUserPersona){const fresh=await window.NiniINSHost.getActiveUserPersona();if(fresh && typeof fresh==='object')persona=normalizeINSUserBindingPersona(fresh);}}catch(error){alert('切换绑定 user 身份失败：'+(error?.message||'未知错误'));return;}applyBoundINSUserPersona(persona);await renderINSUserIdentityBindingList();closeINSUserIdentityBindingSheet();}
document.getElementById('identityBindList')?.addEventListener('click',event=>{const btn=event.target.closest('[data-bind-persona]');if(!btn)return;void switchINSBoundUserPersona(btn.getAttribute('data-bind-persona'));});
const INS_INTERFACE_SIZE_STORAGE_KEY='nini-ins-interface-size-v1';
const INS_INTERFACE_SIZE_DEFAULTS=Object.freeze({topSafe:44,bottomSafe:26});
function clampINSInterfaceSize(value,min,max,fallback){
  const number=Number(value);
  return Number.isFinite(number)?Math.min(max,Math.max(min,Math.round(number))):fallback;
}
function loadINSInterfaceSize(){
  try{
    const saved=JSON.parse(localStorage.getItem(INS_INTERFACE_SIZE_STORAGE_KEY)||'{}');
    return {
      topSafe:clampINSInterfaceSize(saved.topSafe,0,80,INS_INTERFACE_SIZE_DEFAULTS.topSafe),
      bottomSafe:clampINSInterfaceSize(saved.bottomSafe,0,56,INS_INTERFACE_SIZE_DEFAULTS.bottomSafe)
    };
  }catch(e){return {...INS_INTERFACE_SIZE_DEFAULTS};}
}
let insInterfaceSizeState=loadINSInterfaceSize();
function applyINSInterfaceSize(){
  const root=document.documentElement;
  if(!root?.style)return;
  root.style.setProperty('--ins-safe-top',insInterfaceSizeState.topSafe+'px');
  root.style.setProperty('--ins-safe-bottom',insInterfaceSizeState.bottomSafe+'px');
}
function persistINSInterfaceSize(){
  try{localStorage.setItem(INS_INTERFACE_SIZE_STORAGE_KEY,JSON.stringify(insInterfaceSizeState));}catch(e){}
}
function renderINSInterfaceSizeControls(){
  const topRange=document.getElementById('insTopSafeRange');
  const bottomRange=document.getElementById('insBottomSafeRange');
  const topValue=document.getElementById('insTopSafeValue');
  const bottomValue=document.getElementById('insBottomSafeValue');
  if(topRange)topRange.value=String(insInterfaceSizeState.topSafe);
  if(bottomRange)bottomRange.value=String(insInterfaceSizeState.bottomSafe);
  if(topValue)topValue.textContent=insInterfaceSizeState.topSafe+'px';
  if(bottomValue)bottomValue.textContent=insInterfaceSizeState.bottomSafe+'px';
}
function updateINSInterfaceSize(next){
  insInterfaceSizeState={
    topSafe:clampINSInterfaceSize(next.topSafe,0,80,insInterfaceSizeState.topSafe),
    bottomSafe:clampINSInterfaceSize(next.bottomSafe,0,56,insInterfaceSizeState.bottomSafe)
  };
  applyINSInterfaceSize();
  persistINSInterfaceSize();
  renderINSInterfaceSizeControls();
}
function openINSInterfaceSizePanel(){
  closeProfileDrawer();
  document.getElementById('profileDrawerDim')?.classList.add('show');
  renderINSInterfaceSizeControls();
  const panel=document.getElementById('insInterfaceSizePanel');
  panel?.classList.add('show');
  panel?.setAttribute('aria-hidden','false');
}
function closeINSInterfaceSizePanel(){
  const panel=document.getElementById('insInterfaceSizePanel');
  panel?.classList.remove('show');
  panel?.setAttribute('aria-hidden','true');
  document.getElementById('profileDrawerDim')?.classList.remove('show');
}
applyINSInterfaceSize();
function openProfileDrawer(){
  document.getElementById('profileDrawerDim')?.classList.add('show');
  document.getElementById('profileDrawer')?.classList.add('show');
}
function closeProfileDrawer(){
  document.getElementById('profileDrawerDim')?.classList.remove('show');
  document.getElementById('profileDrawer')?.classList.remove('show');
}
function openProfileEdit(){
  closeProfileDrawer();
  document.getElementById('profileDrawerDim')?.classList.add('show');
  const editCard=document.getElementById('profileEditCard');
  editCard?.classList.add('show');
  if(editCard)editCard.scrollTop=0;
  document.getElementById('profileEditName').value = userProfileState.displayName;
  document.getElementById('profileEditId').value = userProfileState.id;
  document.getElementById('profileEditLocation').value = userProfileState.location || '';
  document.getElementById('profileEditBio').value = userProfileState.bio;
  document.getElementById('profileEditAvatar').value = userProfileState.avatarUrl;
  updateAvatarPreview(getUserAvatarUrl());
  document.getElementById('profileEditFollowers').value = userProfileState.followers;
  document.getElementById('profileEditFollowing').value = userProfileState.following;
}
function closeProfileEdit(){
  document.getElementById('profileDrawerDim')?.classList.remove('show');
  document.getElementById('profileEditCard')?.classList.remove('show');
}

function scrubINSImageSemanticCache(root){
  let count=0;
  const seen=new WeakSet();
  const walk=value=>{
    if(!value||typeof value!=='object'||seen.has(value))return;
    seen.add(value);
    if(Object.prototype.hasOwnProperty.call(value,'visionSummary') && value.visionSummary){value.visionSummary='';count++;}
    if(Object.prototype.hasOwnProperty.call(value,'visionStatus'))value.visionStatus='idle';
    if(Array.isArray(value)){value.forEach(walk);return;}
    Object.values(value).forEach(walk);
  };
  walk(root);
  return count;
}
async function clearNiniINSImageCache(){
  const count=[storyViewerData,storyArchive,storyCreateDraft].reduce((sum,root)=>sum+scrubINSImageSemanticCache(root),0);
  try{persistNiniStoryLifecycle?.();}catch(e){}
  try{
    if(window.caches&&typeof caches.keys==='function'){
      const keys=await caches.keys();
      await Promise.all(keys.filter(key=>/nini|instagram/i.test(String(key))).map(key=>caches.delete(key)));
    }
  }catch(e){}
  alert(`已清空 INS 图片缓存${count?`（${count} 条识图摘要）`:''}。私人相册、URL 照片和手机导入照片不会被删除。`);
}
function isNiniINSLocalStorageKey(key=''){
  return /^(nini-ins|nini_ins|niniInstagram|nini-instagram|niniIns)/i.test(String(key||''));
}
async function clearAllNiniINSData(){
  if(!confirm('这会清空 INS 的本地数据：私人相册、时期设置、角色生成偏好/主动频率、世界观提示词、Story 存档、Social Event / Knowledge，以及 INS DM 历史。Roche 本体的角色、聊天和记忆不会删除。\n\n继续吗？'))return;
  if(!confirm('最后确认：确定要把 INS 当作第一次安装重新开始吗？'))return;
  window.__NiniINSResetting=true;
  try{
    const keys=[];
    for(let i=0;i<localStorage.length;i++){
      const key=localStorage.key(i);if(isNiniINSLocalStorageKey(key))keys.push(key);
    }
    keys.forEach(key=>{try{localStorage.removeItem(key);}catch(e){}});
  }catch(e){}
  try{await window.NiniINSHost?.clearPluginPersistentData?.();}catch(e){console.warn('[INS] Roche storage reset failed',e);}
  alert('INS 数据已清空。现在会关闭插件；重新打开后就是干净的新开始。');
  try{await window.NiniINSHost?.closeAfterReset?.();}catch(e){}
}

installHomeFeedTranslationApiUI();
installINSImageSettingsUI();
void refreshINSRocheImageApiStatus();
document.getElementById('userProfileMenuBtn')?.addEventListener('click', openProfileDrawer);
document.getElementById('profileDrawerClose')?.addEventListener('click', closeProfileDrawer);
document.getElementById('profileDrawerDim')?.addEventListener('click', ()=>{
  closeProfileDrawer();
  closeProfileEdit();
  closeHighlightEditor();
  closeAtmosphereSettings();
  closeImagePromptSettings();
  closeSubApiSettings();
  closeHomeFeedTranslationApiSettings();
  closeINSUserIdentityBindingSheet();
  closeINSInterfaceSizePanel();
});
document.getElementById('editProfileBtn')?.addEventListener('click', openProfileEdit);
document.getElementById('drawerEditProfileBtn')?.addEventListener('click', openProfileEdit);
document.getElementById('profileInterfaceSizeBtn')?.addEventListener('click', openINSInterfaceSizePanel);
function bindINSInterfaceSizeAction(id,handler){
  const button=document.getElementById(id);
  if(!button)return;
  let pointerHandledAt=0;
  button.addEventListener('pointerup',event=>{
    if(typeof event.button==='number' && event.button!==0)return;
    pointerHandledAt=Date.now();
    event.preventDefault();
    event.stopPropagation();
    handler();
  });
  button.addEventListener('click',event=>{
    if(Date.now()-pointerHandledAt<700){event.preventDefault();event.stopPropagation();return;}
    handler();
  });
}
bindINSInterfaceSizeAction('insInterfaceSizeClose',closeINSInterfaceSizePanel);
bindINSInterfaceSizeAction('insInterfaceSizeDone',closeINSInterfaceSizePanel);
bindINSInterfaceSizeAction('insInterfaceSizeReset',()=>updateINSInterfaceSize(INS_INTERFACE_SIZE_DEFAULTS));
document.getElementById('insTopSafeRange')?.addEventListener('input',event=>updateINSInterfaceSize({topSafe:event.target.value,bottomSafe:insInterfaceSizeState.bottomSafe}));
document.getElementById('insBottomSafeRange')?.addEventListener('input',event=>updateINSInterfaceSize({topSafe:insInterfaceSizeState.topSafe,bottomSafe:event.target.value}));
document.getElementById('drawerBindUserIdentityBtn')?.addEventListener('click', openINSUserIdentityBindingSheet);
document.getElementById('drawerAtmosphereBtn')?.addEventListener('click', openAtmosphereSettings);
document.getElementById('drawerImagePromptBtn')?.addEventListener('click', openImagePromptSettings);
document.getElementById('drawerFeedTranslationApiBtn')?.addEventListener('click', openHomeFeedTranslationApiSettings);
document.getElementById('drawerClearImageCacheBtn')?.addEventListener('click',()=>{closeProfileDrawer();void clearNiniINSImageCache();});
document.getElementById('drawerClearDataBtn')?.addEventListener('click',()=>{closeProfileDrawer();void clearAllNiniINSData();});
document.getElementById('profileAtmosphereClose')?.addEventListener('click', closeAtmosphereSettings);
document.getElementById('profileAtmosphereSave')?.addEventListener('click',()=>{
  const value=document.getElementById('profileWorldCulturePrompt')?.value.trim()||'';
  atmospherePromptSettings.customPrompt=value||INS_DEFAULT_WORLD_CULTURE_PROMPT;
  try{localStorage.setItem(INS_WORLD_PROMPT_STORAGE_KEY,atmospherePromptSettings.customPrompt);}catch(e){}
  closeAtmosphereSettings();
});
document.getElementById('profileAtmosphereReset')?.addEventListener('click',()=>{
  atmospherePromptSettings.customPrompt=INS_DEFAULT_WORLD_CULTURE_PROMPT;
  const textarea=document.getElementById('profileWorldCulturePrompt');
  if(textarea)textarea.value=INS_DEFAULT_WORLD_CULTURE_PROMPT;
  try{localStorage.removeItem(INS_WORLD_PROMPT_STORAGE_KEY);}catch(e){}
});
document.getElementById('profileImagePromptClose')?.addEventListener('click', closeImagePromptSettings);
document.getElementById('profileImagePromptCard')?.addEventListener('pointerup',event=>event.stopPropagation());
document.getElementById('profileImagePromptCard')?.addEventListener('click',event=>event.stopPropagation());
document.getElementById('insRocheImageApiRefresh')?.addEventListener('click',event=>{event.preventDefault();event.stopPropagation();void refreshINSRocheImageApiStatus();});
document.getElementById('insImageGenerationTestBtn')?.addEventListener('click',event=>{event.preventDefault();event.stopPropagation();void runINSImageGenerationTest();});
document.getElementById('profileImagePromptSave')?.addEventListener('click',()=>{
  insImagePromptSettings.descriptionLanguage=document.getElementById('insImagePromptLanguage').value||'zh';
  insImagePromptSettings.positive=document.getElementById('insImagePromptPositive').value.trim();
  insImagePromptSettings.negative=document.getElementById('insImagePromptNegative').value.trim();
  closeImagePromptSettings();
});
document.getElementById('profileFeedTranslationApiCard')?.addEventListener('pointerup',event=>event.stopPropagation());
document.getElementById('profileFeedTranslationApiCard')?.addEventListener('click',event=>event.stopPropagation());
document.getElementById('profileFeedTranslationApiClose')?.addEventListener('click',event=>{event.preventDefault();event.stopPropagation();closeHomeFeedTranslationApiSettings();});
document.getElementById('profileFeedTranslationApiSave')?.addEventListener('click',event=>{event.preventDefault();event.stopPropagation();saveHomeFeedTranslationApiSettings();});
document.getElementById('feedTranslationApiRefreshModels')?.addEventListener('click',event=>{event.preventDefault();event.stopPropagation();void refreshHomeFeedTranslationModels();});
document.getElementById('insSubApiPresetList')?.addEventListener('click',(event)=>{
  const enableBtn=event.target.closest('[data-ins-subapi-enable]');
  const deleteBtn=event.target.closest('[data-ins-subapi-delete]');
  if(enableBtn){
    insSubApiSettings.activeId=enableBtn.dataset.insSubapiEnable || enableBtn.getAttribute('data-ins-subapi-enable');
    renderINSSubApiSettings();
    return;
  }
  if(deleteBtn){
    const pid=deleteBtn.dataset.insSubapiDelete || deleteBtn.getAttribute('data-ins-subapi-delete');
    insSubApiSettings.presets=insSubApiSettings.presets.filter(p=>p.id!==pid);
    if(insSubApiSettings.activeId===pid) insSubApiSettings.activeId='';
    renderINSSubApiSettings();
  }
});
document.getElementById('profileEditClose')?.addEventListener('click', closeProfileEdit);

document.getElementById('profileEditSave')?.addEventListener('click', ()=>{
  const name = document.getElementById('profileEditName').value.trim();
  const id = document.getElementById('profileEditId').value.trim();
  const bio = document.getElementById('profileEditBio').value.trim();
  const location = document.getElementById('profileEditLocation').value.trim();
  const avatarUrl = document.getElementById('profileEditAvatar').value.trim();
  const followers = Number(document.getElementById('profileEditFollowers').value.trim());
  const following = Number(document.getElementById('profileEditFollowing').value.trim());

  if(name) userProfileState.displayName = name;
  if(id) userProfileState.id = id.replace(/^@/,'');
  userProfileState.avatarUrl = avatarUrl;
  userProfileState.location = location;
  if(Number.isFinite(followers) && followers >= 0) userProfileState.followers = followers;
  if(Number.isFinite(following) && following >= 0) userProfileState.following = following;
  userProfileState.bio = bio || '';

  closeProfileEdit();
  renderUserProfile();
});



let userHighlightStoryBindingDraft=new Set();
let charHighlightStoryBindingDraft=new Set();

function getExistingBoundStoryBatchIds(binding){
  if(!binding)return [];
  if(Array.isArray(binding.batches)){
    return binding.batches.map(batch=>batch.batchId).filter(Boolean);
  }

  // Compatibility with the earlier single-batch prototype shape.
  if(binding.batchId) return [binding.batchId];

  return [];
}

function makeStoryBindingSnapshot(owner, selectedBatchIds){
  const archive=getStoryArchive(owner);
  const ids=new Set(
    selectedBatchIds instanceof Set
      ? [...selectedBatchIds]
      : Array.isArray(selectedBatchIds)
        ? selectedBatchIds
        : []
  );

  const chosen=archive.filter(batch=>ids.has(batch.batchId));
  if(!chosen.length)return null;

  return {
    owner,
    boundAt:Date.now(),
    batches:chosen.map(batch=>({
      batchId:batch.batchId,
      owner:batch.owner,
      createdAt:batch.createdAt,
      caption:batch.caption||'',
      imageCount:batch.imageCount||0,
      videoCount:batch.videoCount||0,
      items:batch.items.map(cloneStoryItemForArchive)
    }))
  };
}

function formatStoryArchiveBatchMeta(batch,index){
  const parts=[];
  if(batch.imageCount)parts.push(`${batch.imageCount}张图片`);
  if(batch.videoCount)parts.push(`${batch.videoCount}个视频`);
  return `Story ${index+1} · ${parts.join(' + ') || '无媒体'}`;
}

function renderStoryArchiveBindingList(box, owner, draftSet, dataAttr, rerender){
  if(!box)return;
  const archive=getStoryArchive(owner);

  if(!archive.length){
    box.innerHTML='<div class="highlight-story-bind-empty">当前还没有可以绑定的 Story 记录。</div>';
    return;
  }

  box.innerHTML=
    `<div class="highlight-bind-caption-note">这里选择的是整组 Story。每组内部有多少图片/视频都不会拆开。</div>` +
    archive.map((batch,index)=>{
      const selected=draftSet.has(batch.batchId);
      const caption=(batch.caption||'无文案').trim();
      return `
        <div class="highlight-story-item">
          <div class="highlight-story-index">${index+1}</div>
          <div class="highlight-story-summary">
            <strong>${escapeDMCardHTML(formatStoryArchiveBatchMeta(batch,index))}</strong>
            <span>${escapeDMCardHTML(caption)}</span>
          </div>
          <button
            class="highlight-story-select ${selected?'selected':''}"
            ${dataAttr}="${escapeDMCardHTML(batch.batchId)}"
            type="button"
          >${selected?'✓':'＋'}</button>
        </div>`;
    }).join('');

  box.querySelectorAll(`[${dataAttr}]`).forEach(btn=>{
    btn.addEventListener('click',()=>{
      const id=btn.getAttribute(dataAttr);
      if(draftSet.has(id)) draftSet.delete(id);
      else draftSet.add(id);
      rerender();
    });
  });
}

function renderUserHighlightStoryBinding(){
  renderStoryArchiveBindingList(
    document.getElementById('highlightStoryBindingList'),
    'user',
    userHighlightStoryBindingDraft,
    'data-user-highlight-story-batch',
    renderUserHighlightStoryBinding
  );
}

function renderCharHighlightStoryBinding(){
  if(!currentCharProfile)return;
  const owner=resolveCharProfileKey(currentCharProfile);
  renderStoryArchiveBindingList(
    document.getElementById('charHighlightStoryBindingList'),
    owner,
    charHighlightStoryBindingDraft,
    'data-char-highlight-story-batch',
    renderCharHighlightStoryBinding
  );
}


function updateHighlightDescriptionCount(inputId,countId){
  const input=document.getElementById(inputId);
  const count=document.getElementById(countId);
  if(!input || !count)return;
  count.textContent=`${input.value.length} / 160`;
}

const highlightViewerState={owner:'user',highlightId:'',slideIndex:0,isChar:false};
function ensureHighlightViewer(){
  let el=document.getElementById('niniHighlightViewer');
  if(el)return el;
  el=document.createElement('section');
  el.id='niniHighlightViewer';
  el.className='highlight-viewer';
  el.innerHTML=`
    <div id="niniHighlightProgress" class="highlight-viewer-progress"></div>
    <div class="highlight-viewer-top">
      <div id="niniHighlightAvatar" class="highlight-viewer-avatar"></div>
      <div class="highlight-viewer-title"><strong id="niniHighlightTitle">Highlight</strong><span id="niniHighlightMeta"></span></div>
      <button id="niniHighlightEdit" class="highlight-viewer-btn" type="button" aria-label="编辑 Highlight">⋯</button>
      <button id="niniHighlightClose" class="highlight-viewer-btn" type="button" aria-label="关闭">×</button>
    </div>
    <div id="niniHighlightStage" class="highlight-viewer-stage"></div>
    <div class="highlight-viewer-nav"><button data-highlight-nav="prev" type="button">prev</button><button data-highlight-nav="next" type="button">next</button></div>`;
  document.querySelector('.phone')?.appendChild(el);
  el.querySelector('#niniHighlightClose')?.addEventListener('click',closeHighlightViewer);
  el.querySelector('#niniHighlightEdit')?.addEventListener('click',()=>{
    const id=highlightViewerState.highlightId;
    const isChar=highlightViewerState.isChar;
    closeHighlightViewer();
    if(isChar)openCharHighlightEditor(id);else openHighlightEditor(id);
  });
  el.querySelectorAll('[data-highlight-nav]').forEach(btn=>btn.addEventListener('click',event=>{
    event.stopPropagation();
    moveHighlightViewer(btn.dataset.highlightNav==='next'?1:-1);
  }));
  return el;
}
function getHighlightViewerHighlight(){
  if(highlightViewerState.isChar){
    const profile=getCharProfileData(highlightViewerState.owner);
    return (profile.highlights||[]).find(h=>h.id===highlightViewerState.highlightId)||null;
  }
  return (userProfileState.highlights||[]).find(h=>h.id===highlightViewerState.highlightId)||null;
}
function getHighlightViewerSlides(){
  const h=getHighlightViewerHighlight();
  const batches=Array.isArray(h?.storyBinding?.batches)?h.storyBinding.batches:[];
  const slides=[];
  batches.forEach(batch=>{
    (batch.items||[]).forEach((item,index)=>slides.push({batch,item,itemIndex:index,caption:batch.caption||''}));
  });
  return slides;
}
function setHighlightViewerProgress(index){
  const el=document.getElementById('niniHighlightViewer');
  if(!el)return;
  const spans=[...el.querySelectorAll('#niniHighlightProgress span')];
  spans.forEach((span,i)=>span.classList.toggle('on',i<=index));
}
function bindHighlightViewerScroll(stage){
  if(!stage || stage.dataset.scrollBound)return;
  stage.dataset.scrollBound='1';
  let raf=0;
  stage.addEventListener('scroll',()=>{
    if(raf)cancelAnimationFrame(raf);
    raf=requestAnimationFrame(()=>{
      raf=0;
      const h=Math.max(1,stage.clientHeight);
      const index=Math.max(0,Math.min(getHighlightViewerSlides().length-1,Math.round(stage.scrollTop/h)));
      if(index!==highlightViewerState.slideIndex){
        highlightViewerState.slideIndex=index;
        setHighlightViewerProgress(index);
      }
    });
  },{passive:true});
}
function renderHighlightViewer(){
  const el=ensureHighlightViewer();
  const h=getHighlightViewerHighlight();
  const slides=getHighlightViewerSlides();
  const ownerMeta=getIdentityMeta(highlightViewerState.owner);
  const avatar=el.querySelector('#niniHighlightAvatar');
  avatar.setAttribute('data-identity-avatar',highlightViewerState.owner);
  avatar.textContent=ownerMeta.initial||'?';
  hydrateIdentityAvatars(el);
  el.querySelector('#niniHighlightTitle').textContent=h?.name||'Highlight';
  el.querySelector('#niniHighlightMeta').textContent='@'+(ownerMeta.handle||ownerMeta.name||highlightViewerState.owner);
  const progress=el.querySelector('#niniHighlightProgress');
  progress.innerHTML=slides.length?slides.map((_,i)=>`<span class="${i<=highlightViewerState.slideIndex?'on':''}"></span>`).join(''):'';
  const stage=el.querySelector('#niniHighlightStage');
  if(!h){stage.innerHTML='<div class="highlight-viewer-slide"><div class="highlight-viewer-desc">找不到这个 Highlight。</div></div>';return;}
  if(!slides.length){
    stage.innerHTML=`<div class="highlight-viewer-slide"><div class="highlight-viewer-desc">${escapeDMCardHTML(h.description||'这个 Highlight 还没有绑定 Story。')}</div></div>`;
    return;
  }
  highlightViewerState.slideIndex=Math.max(0,Math.min(highlightViewerState.slideIndex,slides.length-1));
  stage.innerHTML=slides.map((slide,index)=>{
    const item=slide.item||{};
    const visual=item.mediaUrl
      ? `<img src="${escapeDMCardHTML(item.mediaUrl)}" alt="">`
      : `<div class="highlight-viewer-desc">${item.type==='video'?'▶\n':''}${escapeDMCardHTML(item.description||item.visionSummary||'Story')}</div>`;
    return `<section class="highlight-viewer-slide" data-highlight-slide="${index}">
      ${visual}
      <div class="highlight-viewer-caption">${escapeDMCardHTML(slide.caption||'')}<div style="margin-top:6px;font-size:10px;opacity:.72">${index+1} / ${slides.length}</div></div>
    </section>`;
  }).join('');
  bindHighlightViewerScroll(stage);
  requestAnimationFrame(()=>{stage.scrollTop=highlightViewerState.slideIndex*Math.max(1,stage.clientHeight);});
}
function openHighlightViewer(owner,highlightId,isChar=false){
  highlightViewerState.owner=owner||'user';
  highlightViewerState.highlightId=highlightId;
  highlightViewerState.slideIndex=0;
  highlightViewerState.isChar=!!isChar;
  const el=ensureHighlightViewer();
  el.classList.add('show');
  renderHighlightViewer();
}
function closeHighlightViewer(){
  const viewer=document.getElementById('niniHighlightViewer');
  viewer?.classList.remove('show');
  const stage=viewer?.querySelector('#niniHighlightStage');
  if(stage)stage.scrollTop=0;
}
function moveHighlightViewer(delta){
  const stage=document.querySelector('#niniHighlightViewer #niniHighlightStage');
  const slides=getHighlightViewerSlides();
  if(!stage || !slides.length)return;
  const next=highlightViewerState.slideIndex+delta;
  if(next<0 || next>=slides.length){closeHighlightViewer();return;}
  highlightViewerState.slideIndex=next;
  stage.scrollTo({top:next*Math.max(1,stage.clientHeight),behavior:'smooth'});
  setHighlightViewerProgress(next);
}
function highlightHasBoundStories(h){
  return !!(h && Array.isArray(h.storyBinding?.batches) && h.storyBinding.batches.some(batch=>Array.isArray(batch?.items) && batch.items.length));
}
function handleUserHighlightTap(id){
  if(id==='__new'){openHighlightEditor(id);return;}
  const h=(userProfileState.highlights||[]).find(item=>item.id===id);
  if(!h)return;
  if(highlightHasBoundStories(h))openHighlightViewer('user',id,false);
  else openHighlightEditor(id);
}
function handleCharHighlightTap(id){
  if(id==='__new'){openCharHighlightEditor(id);return;}
  const owner=resolveCharProfileKey(currentCharProfile);
  const profile=getCharProfileData(owner);
  const h=(profile.highlights||[]).find(item=>item.id===id);
  if(!h)return;
  if(highlightHasBoundStories(h))openHighlightViewer(owner,id,true);
  else openCharHighlightEditor(id);
}

function openHighlightEditor(id){
  clearProfileInteractionOverlays();
  closeHighlightViewer();
  const isNew = id === '__new';
  const h = isNew ? {id:'__new', name:'', coverUrl:'', description:'', storyBinding:null} : userProfileState.highlights.find(item=>item.id === id);
  if(!h) return;

  userHighlightStoryBindingDraft=new Set(getExistingBoundStoryBatchIds(h.storyBinding));
  userProfileState.editingHighlightId = id;
  closeProfileDrawer();
  closeProfileEdit();
  document.getElementById('profileDrawerDim')?.classList.add('show');
  document.getElementById('highlightEditorCard')?.classList.add('show');

  document.getElementById('highlightEditorTitle').textContent = isNew ? '新建 Highlight' : '编辑 Highlight';
  document.getElementById('highlightNameInput').value = h.name || '';
  document.getElementById('highlightCoverInput').value = h.coverUrl || '';
  hardEnableHighlightTextarea('highlightDescriptionInput');
  document.getElementById('highlightDescriptionInput').value = h.description || '';
  updateHighlightDescriptionCount('highlightDescriptionInput','highlightDescriptionCount');
  document.getElementById('highlightDeleteBtn').style.visibility = isNew ? 'hidden' : 'visible';

  const preview = document.getElementById('highlightPreview');
  const url = h.coverUrl || '';
  preview.classList.toggle('has-image', !!url);
  preview.style.backgroundImage = url ? `url("${url}")` : '';
  preview.textContent = url ? '' : (isNew ? '＋' : (h.id === 'fav' ? '♡' : '✦'));
  renderUserHighlightStoryBinding();
}
function closeHighlightEditor(){
  document.getElementById('profileDrawerDim')?.classList.remove('show');
  document.getElementById('highlightEditorCard')?.classList.remove('show');
}
function saveHighlight(){
  const id = userProfileState.editingHighlightId;
  const name = document.getElementById('highlightNameInput').value.trim() || '未命名';
  const coverUrl = document.getElementById('highlightCoverInput').value.trim();
  const description = document.getElementById('highlightDescriptionInput').value.trim();

  if(id === '__new'){
    userProfileState.highlights.push({
      id:'hl_' + Date.now(),
      name,
      coverUrl,
      description,
      storyBinding:makeStoryBindingSnapshot('user',userHighlightStoryBindingDraft)
    });
  }else{
    const h = userProfileState.highlights.find(item=>item.id === id);
    if(h){
      h.name = name;
      h.coverUrl = coverUrl;
      h.description = description;
      h.storyBinding=makeStoryBindingSnapshot('user',userHighlightStoryBindingDraft);
    }
  }

  persistNiniStoryLifecycle();
  closeHighlightEditor();
  renderUserProfile();
}
function deleteHighlight(){
  const id = userProfileState.editingHighlightId;
  if(id && id !== '__new'){
    userProfileState.highlights = userProfileState.highlights.filter(item=>item.id !== id);
  }
  persistNiniStoryLifecycle();
  closeHighlightEditor();
  renderUserProfile();
}

document.getElementById('profileAvatarApplyBtn')?.addEventListener('click', ()=>{
  updateAvatarPreview(document.getElementById('profileEditAvatar').value.trim());
});
document.getElementById('profileAvatarResetBtn')?.addEventListener('click', ()=>{
  document.getElementById('profileEditAvatar').value = '';
  updateAvatarPreview(userProfileState.defaultAvatarUrl || '');
});

function findHighlightCellAtPoint(stripSelector,cellSelector,x,y){
  const strip=document.querySelector(stripSelector);
  if(!strip)return null;
  const stripRect=strip.getBoundingClientRect();
  if(x<stripRect.left-4 || x>stripRect.right+4 || y<stripRect.top-4 || y>stripRect.bottom+6) return null;

  const cells=[...strip.querySelectorAll(cellSelector)];
  return cells.find(cell=>{
    const r=cell.getBoundingClientRect();
    return x>=r.left-5 && x<=r.right+5 && y>=r.top-4 && y<=r.bottom+8;
  }) || null;
}

let lastHighlightPointerHandledAt=0;

function bindOneHighlightPointerFallback(scroll,stripSelector,cellSelector,idAttr,openFn,boundKey){
  if(!scroll || scroll.dataset[boundKey])return;
  scroll.dataset[boundKey]='1';

  let start=null;

  scroll.addEventListener('pointerdown',e=>{
    const cell=findHighlightCellAtPoint(stripSelector,cellSelector,e.clientX,e.clientY);
    if(!cell){
      start=null;
      return;
    }
    start={
      x:e.clientX,
      y:e.clientY,
      id:cell.getAttribute(idAttr),
      pointerId:e.pointerId
    };
  },true);

  scroll.addEventListener('pointerup',e=>{
    if(!start || start.pointerId!==e.pointerId){
      start=null;
      return;
    }

    const dx=e.clientX-start.x;
    const dy=e.clientY-start.y;
    const moved=Math.hypot(dx,dy);
    const cell=findHighlightCellAtPoint(stripSelector,cellSelector,e.clientX,e.clientY);
    const id=cell?.getAttribute(idAttr);

    if(cell && id===start.id && moved<10){
      if(Date.now()-lastHighlightPointerHandledAt<450){
        start=null;
        return;
      }
      e.preventDefault();
      e.stopImmediatePropagation();
      lastHighlightPointerHandledAt=Date.now();
      openFn(id);
    }
    start=null;
  },true);

  scroll.addEventListener('pointercancel',()=>{start=null;},true);

  /* suppress the synthetic click that follows a handled pointerup */
  scroll.addEventListener('click',e=>{
    if(Date.now()-lastHighlightPointerHandledAt>450)return;
    const cell=findHighlightCellAtPoint(stripSelector,cellSelector,e.clientX,e.clientY);
    if(!cell)return;
    e.preventDefault();
    e.stopImmediatePropagation();
  },true);
}

function bindHighlightGeometryFallback(){
  bindOneHighlightPointerFallback(
    document.querySelector('.user-profile-scroll'),
    '#userHighlightStrip',
    '[data-highlight-id]',
    'data-highlight-id',
    handleUserHighlightTap,
    'highlightPointerBound'
  );

  bindOneHighlightPointerFallback(
    document.querySelector('.char-profile-scroll'),
    '#charHighlightStrip',
    '[data-char-highlight-id]',
    'data-char-highlight-id',
    handleCharHighlightTap,
    'highlightPointerBound'
  );
}

bindHighlightGeometryFallback();

/* v5.0.8: normal tap keeps opening the archived Highlight; long-press / context menu
   is a direct secondary edit path for name, cover and purpose description. */
function bindHighlightLongPressEdit(stripSelector,itemSelector,idAttr,editFn){
  const strip=document.querySelector(stripSelector);
  if(!strip || strip.dataset.longPressEditBound)return;
  strip.dataset.longPressEditBound='1';
  let timer=null,start=null;
  const clear=()=>{if(timer)clearTimeout(timer);timer=null;start=null;};
  strip.addEventListener('pointerdown',event=>{
    const item=event.target.closest(itemSelector);
    const id=item?.getAttribute(idAttr);
    if(!item || !id || id==='__new')return;
    start={x:event.clientX,y:event.clientY,id,pointerId:event.pointerId};
    timer=setTimeout(()=>{
      if(!start)return;
      lastHighlightPointerHandledAt=Date.now();
      editFn(start.id);
      clear();
    },520);
  },true);
  strip.addEventListener('pointermove',event=>{
    if(start && Math.hypot(event.clientX-start.x,event.clientY-start.y)>10)clear();
  },true);
  strip.addEventListener('pointerup',clear,true);
  strip.addEventListener('pointercancel',clear,true);
  strip.addEventListener('contextmenu',event=>{
    const item=event.target.closest(itemSelector);
    const id=item?.getAttribute(idAttr);
    if(!item || !id || id==='__new')return;
    event.preventDefault();
    event.stopPropagation();
    editFn(id);
  });
}
/* v5.0.9: no long-press edit binding. Bound Highlights open Story viewer; edit lives on viewer ⋯. */

document.getElementById('userHighlightStrip')?.addEventListener('click', e=>{
  const item = e.target.closest('[data-highlight-id]');
  if(!item) return;
  e.preventDefault();
  e.stopPropagation();
  handleUserHighlightTap(item.dataset.highlightId);
});
document.getElementById('highlightDescriptionInput')?.addEventListener('input',()=>{
  updateHighlightDescriptionCount('highlightDescriptionInput','highlightDescriptionCount');
});
document.getElementById('highlightEditorClose')?.addEventListener('click', closeHighlightEditor);
document.getElementById('highlightCancelBtn')?.addEventListener('click', closeHighlightEditor);
document.getElementById('highlightSaveBtn')?.addEventListener('click', saveHighlight);
document.getElementById('highlightDeleteBtn')?.addEventListener('click', deleteHighlight);
document.getElementById('highlightCoverInput')?.addEventListener('input', e=>{
  const url = e.target.value.trim();
  const preview = document.getElementById('highlightPreview');
  preview.classList.toggle('has-image', !!url);
  preview.style.backgroundImage = url ? `url("${url}")` : '';
  preview.textContent = url ? '' : '✦';
});

document.querySelectorAll('.user-tab').forEach(btn=>{
  btn.addEventListener('click', ()=>{
    userProfileState.activeTab = btn.dataset.userTab;
    renderUserProfile();
  });
});




/* ===== v1.14.1 Char Profile editor / post card ===== */
let activeCharPostId = null;

function openCharProfileEditor(){
  if(!currentCharProfile) return;
  const profile = getCharProfileData(currentCharProfile);

  closeCharDrawer();
  document.getElementById('charDrawerDim')?.classList.add('show');
  document.getElementById('charProfileEditor')?.classList.add('show');

  document.getElementById('charProfileEditAvatar').value = profile.avatarUrl || '';
  document.getElementById('charProfileEditName').value = profile.name || '';
  document.getElementById('charProfileEditHandle').value = profile.handle || '';
  document.getElementById('charProfileEditLocation').value = profile.location || '';
  document.getElementById('charProfileEditFollowers').value = profile.followers ?? 0;
  document.getElementById('charProfileEditFollowing').value = profile.following ?? 0;
  document.getElementById('charProfileEditBio').value = profile.bio || '';

  updateCharProfileEditPreview(profile.avatarUrl || '', profile.initial || '?');
}

function closeCharProfileEditor(){
  document.getElementById('charProfileEditor')?.classList.remove('show');
  document.getElementById('charDrawerDim')?.classList.remove('show');
}

function updateCharProfileEditPreview(url,initial){
  const preview = document.getElementById('charProfileEditPreview');
  if(!preview) return;
  preview.classList.toggle('has-image',!!url);
  preview.style.backgroundImage = url ? `url("${url}")` : '';
  preview.textContent = url ? '' : (initial || '?');
}

function syncEditedCharProfile(key,oldHandle,profile){
  // Keep Feed posts bound to the same imported Roche char even if IG ID is edited.
  posts.forEach(p=>{
    if(p.author===oldHandle){
      p.author = profile.handle;
      p.avatar = profile.initial || p.avatar;
    }
  });

  // DM Chat reads the same public IG display data.
  if(typeof dmProfiles !== 'undefined'){
    if(!dmProfiles[key]){
      dmProfiles[key] = {
        initial:profile.initial || '?',
        name:profile.name,
        handle:profile.handle,
        followers:profile.followers
      };
    }else{
      dmProfiles[key].name = profile.name;
      dmProfiles[key].handle = profile.handle;
      dmProfiles[key].followers = profile.followers;
      dmProfiles[key].avatarUrl = profile.avatarUrl || '';
      dmProfiles[key].location = profile.location || '';
    }
  }

  // Refresh static DM-list labels without changing the internal binding key.
  document.querySelectorAll(`[data-dm-profile="${key}"] .dm-profile-name`).forEach(el=>{
    el.textContent = profile.handle;
  });
  document.querySelectorAll(`#dmV2List [data-dm-chat="${key}"] .dm-v2-name`).forEach(el=>{
    el.textContent = profile.handle;
  });

  renderFeed();
}

function saveCharProfileEditor(){
  if(!currentCharProfile) return;
  const key = resolveCharProfileKey(currentCharProfile);
  const profile = getCharProfileData(key);
  const oldHandle = profile.handle;

  const avatarUrl = document.getElementById('charProfileEditAvatar').value.trim();
  const name = document.getElementById('charProfileEditName').value.trim();
  const handle = document.getElementById('charProfileEditHandle').value.trim().replace(/^@/,'');
  const location = document.getElementById('charProfileEditLocation').value.trim();
  const followers = Number(document.getElementById('charProfileEditFollowers').value.trim());
  const following = Number(document.getElementById('charProfileEditFollowing').value.trim());
  const bio = document.getElementById('charProfileEditBio').value.trim();

  profile.avatarUrl = avatarUrl;
  if(name) profile.name = name;
  if(handle) profile.handle = handle;
  profile.location = location;
  if(Number.isFinite(followers) && followers >= 0) profile.followers = followers;
  if(Number.isFinite(following) && following >= 0) profile.following = following;
  profile.bio = bio;

  syncEditedCharProfile(key,oldHandle,profile);
  closeCharProfileEditor();
  renderCharProfile();

  if(currentDMChat===key){
    renderDMChatStream(key);
  }
}

function closeCharPostCard(){
  document.getElementById('charPostDim')?.classList.remove('show');
  document.getElementById('charPostCard')?.classList.remove('show');
  activeCharPostId = null;
}

function renderCharPostComments(postId){
  const list = document.getElementById('charPostCommentList');
  if(!list) return;

  const rows = (typeof commentsByPost !== 'undefined' && commentsByPost[postId])
    ? commentsByPost[postId]
    : [];

  if(!rows.length){
    list.innerHTML = '<div class="char-post-comment-empty">还没有留言</div>';
    return;
  }

  list.innerHTML = rows.map(c=>`
    <div class="char-post-comment-row">
      <div class="char-post-comment-avatar">${escapeDMCardHTML((c.author || '?').slice(0,1).toUpperCase())}</div>
      <div class="char-post-comment-main">
        <div class="char-post-comment-name">${escapeDMCardHTML(c.author)}</div>
        <div class="char-post-comment-copy">${escapeDMCardHTML(c.text)}</div>
        ${c.translation
          ? `<div class="char-post-comment-translation">${escapeDMCardHTML(c.translation)}</div>`
          : ''}
      </div>
    </div>`).join('');
}

function openCharPostCard(postId){
  const post = posts.find(p=>p.id===postId);
  if(!post) return;

  activeCharPostId = postId;

  document.getElementById('charPostCardAvatar').textContent = post.avatar || (post.author?.[0] || '?').toUpperCase();
  document.getElementById('charPostCardAuthor').textContent = post.author;

  const media = document.getElementById('charPostCardMedia');
  media.className = `char-post-card-media ${post.mediaClass || ''}`;
  media.innerHTML = post.mediaUrl
    ? `<img src="${escapeDMCardHTML(post.mediaUrl)}" alt="">`
    : escapeDMCardHTML(post.mediaDescription || post.mediaText || '').replace(/\n/g,'<br>');

  document.getElementById('charPostCardActions').innerHTML = `
    <div class="char-post-card-action"><img src="${post.liked?ICONS.heart1:ICONS.heart0}" alt="">${fmtSocial(post.likes+(post.liked?1:0))}</div>
    <div class="char-post-card-action"><img src="${ICONS.reply}" alt="">${fmtSocial(post.comments)}</div>
    <div class="char-post-card-action"><img src="${post.reposted?ICONS.repost1:ICONS.repost0}" alt="">${fmtSocial(post.reposts+(post.reposted?1:0))}</div>
    <div class="char-post-card-action"><img src="${ICONS.dm}" alt="">${fmtSocial(post.shares)}</div>`;

  document.getElementById('charPostCardCaption').innerHTML = `
    <strong>${escapeDMCardHTML(post.author)}</strong>
    ${post.captionShort || ''}${post.captionLong || ''}
    ${post.translation ? `<div style="margin-top:8px;color:#858a92;font-size:12px">${escapeDMCardHTML(post.translation)}</div>` : ''}`;

  renderCharPostComments(postId);

  document.getElementById('charPostDim')?.classList.add('show');
  document.getElementById('charPostCard')?.classList.add('show');

  const scroller = document.querySelector('#charPostCard .char-post-card-scroll');
  if(scroller) scroller.scrollTop = 0;
}


/* ===== v1.13 Char Profile events ===== */

document.getElementById('charAlbumManagerClose')?.addEventListener('click',closeCharAlbumManager);

document.getElementById('charAlbumParticipationRow')?.addEventListener('click',e=>{const b=e.target.closest('[data-album-rate]');if(b)setCharAlbumParticipation(Number(b.dataset.albumRate));});
document.getElementById('charAlbumAutoGenerateMissing')?.addEventListener('change',e=>{if(!currentCharProfile)return;const st=ensureCharAlbumSettings(currentCharProfile);st.autoGenerateMissing=!!e.target.checked;st.updatedAt=Date.now();persistCharAlbumSettingsState();renderCharAlbumManagerAssets();});
document.getElementById('charAlbumEraEnabled')?.addEventListener('change',e=>setCharAlbumEraMatchingEnabled(!!e.target.checked));
document.getElementById('charAlbumCategoryGrid')?.addEventListener('click',e=>{const b=e.target.closest('[data-album-category]');if(!b)return;charAlbumSelectedCategory=String(b.dataset.albumCategory||'');charAlbumSelectedEraIds.clear();renderCharAlbumCategoryControls();});
document.getElementById('charAlbumAssetEraRow')?.addEventListener('click',e=>{const b=e.target.closest('[data-album-asset-era]');if(!b)return;const id=String(b.dataset.albumAssetEra||'');if(charAlbumSelectedEraIds.has(id))charAlbumSelectedEraIds.delete(id);else charAlbumSelectedEraIds.add(id);renderCharAlbumAssetEraChoices();});

document.getElementById('charAlbumDim')?.addEventListener('click',closeCharAlbumManager);

document.getElementById('charAlbumAddEraBtn')?.addEventListener('click',()=>{
  openCharAlbumEraEditor('__new');
});

document.getElementById('charAlbumEraRow')?.addEventListener('click',event=>{
  const btn=event.target.closest('[data-char-album-era-edit]');
  if(!btn)return;
  openCharAlbumEraEditor(btn.dataset.charAlbumEraEdit);
});

document.getElementById('charAlbumEraEditorClose')?.addEventListener('click',closeCharAlbumEraEditor);
document.getElementById('charAlbumEraCancel')?.addEventListener('click',closeCharAlbumEraEditor);
document.getElementById('charAlbumEraSave')?.addEventListener('click',saveCharAlbumEra);
document.getElementById('charAlbumEraDelete')?.addEventListener('click',deleteCharAlbumEra);
document.getElementById('charAlbumAddUrlBtn')?.addEventListener('click',addCharAlbumUrlAsset);
document.getElementById('charAlbumPickPhoneBtn')?.addEventListener('click',()=>document.getElementById('charAlbumPhoneInput')?.click());
document.getElementById('charAlbumPhoneInput')?.addEventListener('change',async event=>{await addCharAlbumPhoneFiles(event.target.files);event.target.value='';});
document.getElementById('charAlbumLibraryList')?.addEventListener('click',event=>{const btn=event.target.closest('[data-char-album-delete]');if(btn)deleteCharAlbumUserAsset(btn.dataset.charAlbumDelete);});

document.getElementById('charProfileMenuBtn')?.addEventListener('click',openCharDrawer);
document.getElementById('charActiveFrequencyBtn')?.addEventListener('click',openCharActiveFrequency);
document.getElementById('charActiveFrequencyClose')?.addEventListener('click',closeCharQuickCards);
document.getElementById('charActiveFrequencySave')?.addEventListener('click',saveCharActiveFrequency);
document.getElementById('charGenerationPreferenceBtn')?.addEventListener('click',openCharGenerationPreference);
document.getElementById('charGenerationPreferenceClose')?.addEventListener('click',closeCharQuickCards);
document.getElementById('charGenerationPreferenceSave')?.addEventListener('click',saveCharGenerationPreference);
document.getElementById('charEditProfileOpenBtn')?.addEventListener('click',openCharProfileEditor);
document.getElementById('charProfileEditorClose')?.addEventListener('click',closeCharProfileEditor);
document.getElementById('charProfileEditSave')?.addEventListener('click',saveCharProfileEditor);
document.getElementById('charProfileEditAvatar')?.addEventListener('input',e=>{
  const profile = currentCharProfile ? getCharProfileData(currentCharProfile) : null;
  updateCharProfileEditPreview(e.target.value.trim(),profile?.initial || '?');
});
document.getElementById('charPostCardClose')?.addEventListener('click',closeCharPostCard);
document.getElementById('charPostDim')?.addEventListener('click',closeCharPostCard);
document.getElementById('charPostCommentRefresh')?.addEventListener('click',()=>{
  const btn = document.getElementById('charPostCommentRefresh');
  btn.style.transform='rotate(180deg)';
  setTimeout(()=>btn.style.transform='',220);
  if(activeCharPostId) renderCharPostComments(activeCharPostId);
});
document.getElementById('charProfileContent')?.addEventListener('click',e=>{
  const owner=currentCharProfile||'';
  const reelCell=e.target.closest('[data-char-reel-id]');
  if(reelCell){openProfileContentDetail(owner,'reels',reelCell.dataset.charReelId);return;}
  const cell=e.target.closest('[data-char-post-id]');
  if(cell)openProfileContentDetail(owner,'feed',cell.dataset.charPostId);
});
document.getElementById('charDrawerClose')?.addEventListener('click',closeCharDrawer);
document.getElementById('charDrawerDim')?.addEventListener('click',()=>{
  closeCharDrawer();
  closeCharHighlightEditor();
  closeCharProfileEditor();
  closeCharQuickCards();
});

document.getElementById('charProfileFollowBtn')?.addEventListener('click',()=>{
  if(!currentCharProfile) return;
  setCharFollowed(currentCharProfile,!getCharFollowed(currentCharProfile));
});

document.getElementById('charProfileMessageBtn')?.addEventListener('click',()=>{
  if(currentCharProfile) openDMChat(currentCharProfile);
});

document.querySelectorAll('.char-tab').forEach(btn=>{
  btn.addEventListener('click',()=>{
    const profile=currentCharProfile ? getCharProfileData(currentCharProfile) : null;
    if(btn.dataset.charTab==='album' && profile?.profileType!=='roche') return;
    charProfileState.activeTab=btn.dataset.charTab;
    renderCharProfileContent();
  });
});

document.querySelector('#routeCharProfile .char-profile-tabs')?.addEventListener('click',event=>{
  const btn=event.target.closest('.char-tab[data-char-tab]');
  if(!btn)return;
  const profile=currentCharProfile?getCharProfileData(currentCharProfile):null;
  if(btn.dataset.charTab==='album' && profile?.profileType!=='roche')return;
  event.preventDefault();
  event.stopPropagation();
  charProfileState.activeTab=btn.dataset.charTab;
  renderCharProfileContent();
},{capture:true});


/* v5.0.8: geometry fallback for Roche WebView.
   It resolves the tab by coordinates at the route capture layer, so a transparent sibling
   can no longer swallow the four Profile tabs. */
function activateCharProfileTab(tabName){
  if(!currentCharProfile || !tabName)return false;
  const profile=getCharProfileData(currentCharProfile);
  if(tabName==='album' && profile?.profileType!=='roche')return false;
  if(!['posts','reels','album','board'].includes(tabName))return false;
  charProfileState.activeTab=tabName;
  renderCharProfileContent();
  return true;
}
function getCharTabAtPoint(x,y){
  const wrap=document.querySelector('#routeCharProfile .char-profile-tabs');
  if(!wrap)return null;
  const r=wrap.getBoundingClientRect();
  if(x<r.left || x>r.right || y<r.top || y>r.bottom)return null;
  const profile=currentCharProfile?getCharProfileData(currentCharProfile):null;
  const names=profile?.profileType==='roche'?['posts','reels','album','board']:['posts','reels','board'];
  const unit=Math.max(1,r.width/names.length);
  const idx=Math.max(0,Math.min(names.length-1,Math.floor((x-r.left)/unit)));
  const tabName=names[idx];
  return wrap.querySelector(`.char-tab[data-char-tab="${tabName}"]`);
}
(function bindCharProfileTabGeometryFallback(){
  const route=document.getElementById('routeCharProfile');
  if(!route || route.dataset.tabGeometryBound)return;
  route.dataset.tabGeometryBound='1';
  let down=null;
  route.addEventListener('pointerdown',event=>{
    const btn=getCharTabAtPoint(event.clientX,event.clientY);
    down=btn?{pointerId:event.pointerId,x:event.clientX,y:event.clientY,tab:btn.dataset.charTab}:null;
  },true);
  route.addEventListener('pointerup',event=>{
    if(!down || down.pointerId!==event.pointerId){down=null;return;}
    const moved=Math.hypot(event.clientX-down.x,event.clientY-down.y);
    const btn=getCharTabAtPoint(event.clientX,event.clientY);
    const tab=btn?.dataset.charTab;
    if(moved<12 && tab && tab===down.tab && activateCharProfileTab(tab)){
      event.preventDefault();
      event.stopImmediatePropagation();
    }
    down=null;
  },true);
  route.addEventListener('pointercancel',()=>{down=null;},true);
})();

document.getElementById('charHighlightStrip')?.addEventListener('click',e=>{
  const item=e.target.closest('[data-char-highlight-id]');
  if(!item) return;
  e.preventDefault();
  e.stopPropagation();
  handleCharHighlightTap(item.dataset.charHighlightId);
});

document.getElementById('charHighlightDescriptionInput')?.addEventListener('input',()=>{
  updateHighlightDescriptionCount('charHighlightDescriptionInput','charHighlightDescriptionCount');
});

function hardEnableHighlightTextarea(id){
  const el=document.getElementById(id);
  if(!el)return;
  el.disabled=false;
  el.readOnly=false;
  el.removeAttribute('readonly');
  el.removeAttribute('disabled');
  if(el.dataset.highlightInputGuardBound)return;
  el.dataset.highlightInputGuardBound='1';
  ['pointerdown','mousedown','touchstart','click'].forEach(type=>{
    el.addEventListener(type,event=>event.stopPropagation(),{passive:type==='touchstart'});
  });
}
hardEnableHighlightTextarea('highlightDescriptionInput');
hardEnableHighlightTextarea('charHighlightDescriptionInput');
document.getElementById('charHighlightEditorClose')?.addEventListener('click',closeCharHighlightEditor);
document.getElementById('charHighlightCancelBtn')?.addEventListener('click',closeCharHighlightEditor);
document.getElementById('charHighlightSaveBtn')?.addEventListener('click',saveCharHighlight);
document.getElementById('charHighlightDeleteBtn')?.addEventListener('click',deleteCharHighlight);
document.getElementById('charHighlightCoverInput')?.addEventListener('input',e=>{
  const url=e.target.value.trim();
  const preview=document.getElementById('charHighlightPreview');
  preview.classList.toggle('has-image',!!url);
  preview.style.backgroundImage=url?`url("${url}")`:'';
  preview.textContent=url?'':'✦';
});




const STRANGER_TEMP_SESSION_STORAGE_KEY='nini-ins-stranger-temp-sessions-v1';
function loadStrangerTempSessions(){
  try{
    const parsed=JSON.parse(localStorage.getItem(STRANGER_TEMP_SESSION_STORAGE_KEY)||'{}');
    return parsed&&typeof parsed==='object'?parsed:{};
  }catch(e){return {};}
}
const strangerTempSessions = loadStrangerTempSessions();
function persistStrangerTempSessions(){
  try{localStorage.setItem(STRANGER_TEMP_SESSION_STORAGE_KEY,JSON.stringify(strangerTempSessions));}catch(e){}
}
let activeStrangerTempHandle = null;
const userSocialScopeSettings = {
  batchSize:4,
  triggers:['public_post','char_comment'],
  strangerPrompt:''
};

function ensureStrangerTempSession(req){
  if(!req || !req.handle) return null;
  if(!strangerTempSessions[req.handle]){
    strangerTempSessions[req.handle]={
      handle:req.handle,
      initial:req.initial||'?',
      displayName:req.displayName||req.handle,
      location:req.location||'',
      savedToDM:false,
      promoted:false,
      messages:[{
        from:'them',
        text:req.copy||'你好。',
        translation:String(req.translation||''),
        translated:false,
        createdAt:Date.now()
      }]
    };
  }else if(req.translation){
    const first=strangerTempSessions[req.handle].messages?.[0];
    if(first && first.from==='them' && first.text===(req.copy||first.text) && !first.translation){
      first.translation=String(req.translation||'');
      first.translated=false;
    }
  }
  persistStrangerTempSessions();
  return strangerTempSessions[req.handle];
}
function renderStrangerTempSession(){
  const session=strangerTempSessions[activeStrangerTempHandle];
  if(!session) return;
  const tempAvatar=document.getElementById('dmTempAvatar');
  if(tempAvatar){tempAvatar.textContent='';tempAvatar.style.backgroundImage=`url("${STRANGER_AVATAR_URL}")`;tempAvatar.style.backgroundSize='cover';tempAvatar.style.backgroundPosition='center';}
  document.getElementById('dmTempHandle').textContent='@'+session.handle;
  const box=document.getElementById('dmTempMessages');
  box.innerHTML=session.messages.map((row,index)=>{
    const canTranslate=row.from==='them' && shouldOfferTranslation(row.text,row.translation);
    return `
    <div class="dm-temp-row ${row.from==='me'?'me':'them'}">
      <div class="dm-temp-stack">
        <div class="dm-temp-bubble ${canTranslate?'dm-temp-bubble-translatable':''}" ${canTranslate?`data-dm-temp-translate="${index}"`:''}>
          <div class="dm-temp-bubble-original">${escapeDMCardHTML(row.text)}</div>
          ${canTranslate&&row.translated?`<div class="dm-temp-bubble-translation">${escapeDMCardHTML(row.translation)}</div>`:''}
        </div>
      </div>
    </div>`;
  }).join('');
  requestAnimationFrame(()=>box.scrollTop=box.scrollHeight);
}
document.getElementById('dmTempMessages')?.addEventListener('click',event=>{
  const btn=event.target.closest('.dm-temp-bubble[data-dm-temp-translate]');
  if(!btn)return;
  const session=strangerTempSessions[activeStrangerTempHandle];
  const index=Number(btn.dataset.dmTempTranslate);
  const row=session?.messages?.[index];
  if(!row || !shouldOfferTranslation(row.text,row.translation))return;
  row.translated=!row.translated;
  persistStrangerTempSessions();
  renderStrangerTempSession();
});

function openStrangerTempSession(req){
  const session=ensureStrangerTempSession(req);
  if(!session)return;
  if(session.promoted){
    openDMChat(session.handle);
    return;
  }
  activeStrangerTempHandle=session.handle;
  renderStrangerTempSession();
  openDMListCard('dmStrangerTempCard');
}
function addTempSessionToDMList(session,label='临时'){
  if(!session || session.savedToDM) return;
  const list=document.getElementById('dmV2List');
  if(!list)return;

  const row=document.createElement('div');
  row.className='dm-v2-row dm-temp-row-list';
  row.dataset.tempStranger=session.handle;
  row.dataset.search=session.handle;
  row.innerHTML=`
    <button class="dm-edit-check" type="button" aria-label="选择对话"></button>
    <div class="dm-v2-avatar" style="background-image:url(&quot;${escapeDMCardHTML(STRANGER_AVATAR_URL)}&quot;);background-size:cover;background-position:center"></div>
    <div class="dm-v2-main">
      <div class="dm-v2-name">@${escapeDMCardHTML(session.handle)} <span class="dm-temp-tag">${label}</span></div>
      <div class="dm-v2-preview">${escapeDMCardHTML(session.messages.at(-1)?.text||'临时陌生会话')}</div>
    </div>
  `;
  list.prepend(row);
  session.savedToDM=true;
  persistStrangerTempSessions();

  row.addEventListener('click',e=>{
    if(dmListEditMode){
      toggleDMRowEditSelection(row);
      return;
    }
    openStrangerTempSession({handle:session.handle,initial:session.initial,copy:''});
  });
  row.querySelector('.dm-edit-check')?.addEventListener('click',e=>{
    e.preventDefault();e.stopPropagation();toggleDMRowEditSelection(row);
  });
}

function restoreSavedTempSessionsToDMList(){
  Object.values(strangerTempSessions||{}).forEach(session=>{
    if(session && session.savedToDM && !session.promoted){
      const existing=document.querySelector(`#dmV2List [data-temp-stranger="${CSS.escape(session.handle)}"]`);
      if(!existing){
        session.savedToDM=false;
        addTempSessionToDMList(session,'临时');
      }
    }
  });
}

function ensureFixedNPCProfile(session){
  const handle=session.handle;
  dmProfiles[handle]={
    profileType:'npc',
    initial:session.initial||'?',
    name:session.displayName || handle,
    handle,
    avatarUrl:STRANGER_AVATAR_URL,
    location:session.location||'',
    followers:session.followers || 0
  };

  dmBaseMessages[handle]=(session.messages||[]).map(row=>({
    from:row.from==='me'?'me':'them',
    text:row.text,
    translation:row.translation||'',
    translated:false,
    createdAt:Number(row.createdAt)||Date.now()
  }));
  if(!dmSharedCards[handle]) dmSharedCards[handle]=[];
  scheduleINSDMHistoryPersistence();

  charProfileState.profiles[handle]={
    profileType:'npc',
    initial:session.initial||'?',
    name:session.displayName || handle,
    handle,
    avatarUrl:STRANGER_AVATAR_URL,
    location:session.location||'',
    followers:session.followers || 0,
    following:0,
    bio:'社交联系人 · 主页可编辑',
    highlights:[
      {id:'daily',name:'日常',coverUrl:''},
      {id:'social',name:'社交',coverUrl:''}
    ]
  };
  if(!charProfileState.boardBatches[handle]){
    charProfileState.boardBatches[handle]=[[
      {initial:'?',name:'public_01',meta:'网友',text:'刚刚加入固定社交范围，留言板还没有形成稳定评价。'}
    ]];
  }
}
function bindFixedNPCDMRow(row,handle){
  row.addEventListener('click',e=>{
    if(dmListEditMode){
      e.preventDefault();
      toggleDMRowEditSelection(row);
      return;
    }
    if(e.target.closest('[data-dm-profile]')) return;
    row.querySelector('.dm-v2-unread')?.remove();
    openDMChat(handle);
  });
  row.querySelector('[data-dm-profile]')?.addEventListener('click',e=>{
    e.preventDefault();
    e.stopPropagation();
    if(dmListEditMode){
      toggleDMRowEditSelection(row);
      return;
    }
    openCharProfile(handle);
  });
  row.querySelector('.dm-edit-check')?.addEventListener('click',e=>{
    e.preventDefault();
    e.stopPropagation();
    toggleDMRowEditSelection(row);
  });
}
function upsertFixedNPCDMRow(session){
  const list=document.getElementById('dmV2List');
  if(!list)return;

  const selector=`[data-temp-stranger="${CSS.escape(session.handle)}"]`;
  const oldRow=list.querySelector(selector);

  const row=document.createElement('div');
  row.className='dm-v2-row';
  row.dataset.kind='npc';
  row.dataset.dmChat=session.handle;
  row.dataset.search=`${session.handle} 固定社交 NPC`;
  row.innerHTML=`
    <button class="dm-edit-check" type="button" aria-label="选择对话"></button>
    <div class="dm-v2-avatar" data-dm-profile="${escapeDMCardHTML(session.handle)}">${escapeDMCardHTML(session.initial||'?')}</div>
    <div class="dm-v2-main">
      <div class="dm-v2-topline">
        <div class="dm-v2-name">${escapeDMCardHTML(session.handle)}</div>
        <div class="dm-v2-badge">固定社交</div>
        <div class="dm-v2-time">刚刚</div>
      </div>
      <div class="dm-v2-preview">固定 NPC · ${escapeDMCardHTML(session.messages.at(-1)?.text||'已保存为固定社交')}</div>
    </div>
  `;

  if(oldRow) oldRow.replaceWith(row);
  else list.prepend(row);

  bindFixedNPCDMRow(row,session.handle);
}

function promoteTempSessionToNPC(session){
  if(!session)return;
  session.promoted=true;
  session.savedToDM=true;
  persistStrangerTempSessions();

  ensureFixedNPCProfile(session);
  upsertFixedNPCDMRow(session);
  if(!getDirectedRelationship('user',session.handle) && !getDirectedRelationship(session.handle,'user')){
    upsertDirectedRelationship('user',session.handle,'固定社交');
    upsertDirectedRelationship(session.handle,'user','认识');
  }
  renderPeopleRelations();
  renderDMUserNpcHub();
  renderFeedShareDMRecipients();
  renderReelsV15DMRecipients();

  activeStrangerTempHandle=null;
  closeDMListCards();
  openDMChat(session.handle);
}

let dmListEditMode=false;

function ensureDMEditChecks(){
  document.querySelectorAll('#dmV2List .dm-v2-row').forEach(row=>{
    if(row.querySelector('.dm-edit-check'))return;
    const check=document.createElement('button');
    check.type='button';
    check.className='dm-edit-check';
    check.setAttribute('aria-label','选择对话');
    row.prepend(check);
  });
}
function updateDMEditCount(){
  const count=document.querySelectorAll('#dmV2List .dm-v2-row.edit-selected').length;
  const label=document.getElementById('dmEditCount');
  const del=document.getElementById('dmDeleteSelectedBtn');
  if(label)label.textContent=`已选 ${count}`;
  if(del)del.disabled=count===0;
}
function setDMListEditMode(enabled){
  dmListEditMode=!!enabled;
  const page=document.querySelector('#page-dm .dm-v2-page');
  page?.classList.toggle('editing',dmListEditMode);
  document.getElementById('dmEditListBtn').textContent=dmListEditMode?'完成':'编辑';

  if(!dmListEditMode){
    document.querySelectorAll('#dmV2List .dm-v2-row.edit-selected')
      .forEach(row=>row.classList.remove('edit-selected'));
  }
  updateDMEditCount();
}
function toggleDMRowEditSelection(row){
  if(!row)return;
  row.classList.toggle('edit-selected');
  updateDMEditCount();
}
function deleteSelectedDMRows(){
  const rows=[...document.querySelectorAll('#dmV2List .dm-v2-row.edit-selected')];
  if(!rows.length)return;

  rows.forEach(row=>{
    const key=row.dataset.dmChat;
    if(key){
      if(dmBaseMessages[key])dmBaseMessages[key]=[];
      if(dmSharedCards[key])dmSharedCards[key]=[];
    }
    row.remove();
  });

  scheduleINSDMHistoryPersistence();
  setDMListEditMode(false);
}

/* ===== v1.12 clean DM list behavior ===== */
function closeDMListCards(){
  document.getElementById('dmListDim')?.classList.remove('show');
  document.getElementById('dmNoteCard')?.classList.remove('show');
  document.getElementById('dmImportCard')?.classList.remove('show');
  document.getElementById('dmUserSocialCard')?.classList.remove('show');
  document.getElementById('dmStrangerCard')?.classList.remove('show');
  document.getElementById('dmStrangerTempCard')?.classList.remove('show');
}
function openDMListCard(id){
  closeDMListCards();
  document.getElementById('dmListDim')?.classList.add('show');
  document.getElementById(id)?.classList.add('show');
}
function bindDMListV2(){
  ensureDMEditChecks();

  document.getElementById('dmEditListBtn')?.addEventListener('click',()=>{
    setDMListEditMode(!dmListEditMode);
  });
  document.getElementById('dmDeleteSelectedBtn')?.addEventListener('click',deleteSelectedDMRows);

  document.querySelectorAll('#dmV2List .dm-edit-check').forEach(check=>{
    check.addEventListener('click',e=>{
      e.preventDefault();
      e.stopPropagation();
      toggleDMRowEditSelection(check.closest('.dm-v2-row'));
    });
  });

  document.querySelectorAll('#dmV2List [data-dm-chat]').forEach(row=>{
    row.addEventListener('click', e=>{
      if(dmListEditMode){
        e.preventDefault();
        toggleDMRowEditSelection(row);
        return;
      }
      if(e.target.closest('[data-dm-profile]')) return;
      row.querySelector('.dm-v2-unread')?.remove();
      row.dataset.read = '1';
      openDMChat(row.dataset.dmChat);
    });
  });

  document.querySelectorAll('[data-dm-profile]').forEach(el=>{
    el.addEventListener('click', e=>{
      if(dmListEditMode){
        e.preventDefault();
        e.stopPropagation();
        toggleDMRowEditSelection(el.closest('.dm-v2-row'));
        return;
      }
      e.stopPropagation();
      openCharProfile(el.dataset.dmProfile);
    });
  });

  document.getElementById('dmUserNoteItem')?.addEventListener('click', ()=>openDMListCard('dmNoteCard'));
  document.getElementById('dmImportManageBtn')?.addEventListener('click', ()=>{
    renderDMImportActorList();
    openDMListCard('dmImportCard');
  });
  document.getElementById('dmImportActorList')?.addEventListener('click',async e=>{
    const btn=e.target.closest('[data-roche-import-toggle]');
    if(!btn)return;
    btn.disabled=true;
    try{await toggleRocheCharacterImport(btn.dataset.rocheImportToggle);}finally{btn.disabled=false;}
  });
  document.getElementById('dmSocialSearchBtn')?.addEventListener('click', ()=>{
    renderDMUserNpcHub();
    openDMListCard('dmUserSocialCard');
  });
  document.getElementById('dmStrangerRequests')?.addEventListener('click', e=>{
    const row=e.currentTarget;
    if(dmListEditMode){
      e.preventDefault();
      toggleDMRowEditSelection(row);
      return;
    }
    openDMListCard('dmStrangerCard');
  });

  const dmSearchInput=document.getElementById('dmSearchInput');
  if(dmSearchInput){dmSearchInput.readOnly=true;dmSearchInput.tabIndex=-1;dmSearchInput.setAttribute('inputmode','none');['pointerdown','touchstart','mousedown','click','focus'].forEach(type=>dmSearchInput.addEventListener(type,event=>{if(type!=='focus')event.preventDefault();dmSearchInput.blur();},{passive:false}));}

  document.getElementById('dmSaveNoteBtn')?.addEventListener('click', ()=>{
    const value = document.getElementById('dmNoteInput').value.trim() || '当前状态';
    document.getElementById('dmUserNoteBubble').textContent = value;
    closeDMListCards();
  });

  document.getElementById('dmRefreshStrangersBtn')?.addEventListener('click', async ()=>{
    const row = document.getElementById('dmStrangerRequests');
    const list = document.getElementById('dmStrangerRequestList');
    const copy = document.getElementById('dmStrangerRequestCopy');
    const btn = document.getElementById('dmRefreshStrangersBtn');
    const count = Math.max(3,Math.min(5,Number(userSocialScopeSettings.batchSize||4)));
    if(btn){btn.disabled=true;btn.textContent='生成中…';}
    if(copy)copy.textContent='正在从全球陌生账号池生成这一轮消息请求…';
    try{
      const existingRequests=[...document.querySelectorAll('#dmStrangerRequestList [data-stranger-handle]')].slice(-12).map(item=>({
        handle:String(item.dataset.strangerHandle||''),
        displayName:String(item.dataset.strangerDisplayName||''),
        text:String(item.dataset.strangerCopy||'')
      }));
      const result=await callNiniINSAI(INS_AI_TASKS.STRANGER_DM_REFRESH,{
        ownerId:'user',surface:'strangers',prompt:userSocialScopeSettings.strangerPrompt||'',
        content:{existingRequests},
        extra:{batchSize:count,allowedTriggers:userSocialScopeSettings.triggers||[],globalPool:true,appendBatch:true}
      });
      if(!result.ok)throw new Error(result.reason||'生成失败');
      const raw=Array.isArray(result.data?.requests)?result.data.requests:[];
      const batch=raw.slice(0,count).map((req,index)=>{
        const text=String(req.textOriginal??req.text??req.copy??'').trim();
        const translation=String(req.textTranslation??req.translation??'').trim();
        const handle=String(req.handle||`global_${Date.now()}_${index}`).replace(/^@/,'');
        const displayName=String(req.displayName||handle).trim()||handle;
        const initial=String(req.initial||displayName||handle||'?').slice(0,1).toUpperCase();
        return {initial,handle,displayName,location:String(req.location||''),copy:text||'你好。',translation:translation&&translation!==text?translation:''};
      });
      if(!batch.length)throw new Error('AI 没有返回陌生消息请求');
      let addedBatch=batch;
      if(list){
        const existingHandles=new Set([...list.querySelectorAll('[data-stranger-handle]')].map(item=>String(item.dataset.strangerHandle||'').toLowerCase()));
        addedBatch=batch.filter(req=>!existingHandles.has(String(req.handle||'').toLowerCase()));
        list.insertAdjacentHTML('beforeend',addedBatch.map(req=>`
          <div class="dm-stranger-request" data-stranger-handle="${escapeDMCardHTML(req.handle)}" data-stranger-initial="${escapeDMCardHTML(req.initial)}" data-stranger-display-name="${escapeDMCardHTML(req.displayName||req.handle)}" data-stranger-location="${escapeDMCardHTML(req.location||'')}" data-stranger-copy="${escapeDMCardHTML(req.copy)}" data-stranger-translation="${escapeDMCardHTML(req.translation||'')}">
            <div class="dm-stranger-request-avatar" style="background-image:url(&quot;${escapeDMCardHTML(STRANGER_AVATAR_URL)}&quot;);background-size:cover;background-position:center"></div>
            <div class="dm-stranger-request-main"><strong>${escapeDMCardHTML(req.displayName||req.handle)} <span style="font-weight:600;color:#8b9098">@${escapeDMCardHTML(req.handle)}</span></strong><span>${escapeDMCardHTML(req.copy)}</span></div>
            <span class="dm-stranger-request-new"></span>
            <button class="dm-stranger-request-delete" data-stranger-request-delete="" type="button" aria-label="删除请求">×</button>
          </div>`).join(''));
        list.classList.add('show');
      }
      const totalRequests=list?list.querySelectorAll('[data-stranger-handle]').length:addedBatch.length;
      if(copy) copy.textContent = `本次新增 ${addedBatch.length} 条，当前保留 ${totalRequests} 条陌生消息请求。`;
      if(row){
        const badge=row.querySelector('.dm-v2-badge'); const preview=row.querySelector('.dm-v2-preview');
        if(badge)badge.textContent=String(totalRequests);
        const newest=addedBatch[addedBatch.length-1]||batch[batch.length-1];
        if(preview&&newest)preview.textContent=`${newest.handle}：${newest.copy}`;
      }
      const quickCount=document.getElementById('dmQuickStrangerCount');
      if(quickCount)quickCount.textContent=String(totalRequests);
    }catch(error){
      if(copy)copy.textContent='生成失败：'+(error?.message||error);
    }finally{
      if(btn){btn.disabled=false;btn.textContent='刷新请求';}
    }
  });


  document.getElementById('dmStrangerRequestList')?.addEventListener('click',e=>{
    const deleteBtn=e.target.closest('[data-stranger-request-delete]');
    const row=e.target.closest('[data-stranger-handle]');
    if(!row)return;
    if(deleteBtn){
      e.preventDefault();
      e.stopPropagation();
      row.remove();
      const list=document.getElementById('dmStrangerRequestList');
      const total=list?.querySelectorAll('[data-stranger-handle]').length||0;
      const hub=document.getElementById('dmStrangerRequests');
      const badge=hub?.querySelector('.dm-v2-badge');
      const preview=hub?.querySelector('.dm-v2-preview');
      if(badge)badge.textContent=String(total);
      if(preview)preview.textContent=total?'仍有保留的陌生消息请求':'暂无请求 · 满足条件后可手动刷新生成';
      const quick=document.getElementById('dmQuickStrangerCount');
      if(quick)quick.textContent=String(total);
      const copy=document.getElementById('dmStrangerRequestCopy');
      if(copy)copy.textContent=total?`当前保留 ${total} 条陌生消息请求。`:'暂无保留请求；可继续刷新生成。';
      return;
    }
    openStrangerTempSession({
      handle:row.dataset.strangerHandle,
      initial:row.dataset.strangerInitial||'?',
      displayName:row.dataset.strangerDisplayName||row.dataset.strangerHandle,
      location:row.dataset.strangerLocation||'',
      copy:row.dataset.strangerCopy||'',
      translation:row.dataset.strangerTranslation||''
    });
  });

  function dmTempSendInput(){
    const session=strangerTempSessions[activeStrangerTempHandle];
    const input=document.getElementById('dmTempInput');
    const text=input?.value.trim();
    if(!session || !text)return false;
    session.messages.push({from:'me',text,createdAt:Date.now()});
    persistStrangerTempSessions();
    input.value='';
    renderStrangerTempSession();
    return true;
  }

  async function dmTempSummonAI(){
    const session=strangerTempSessions[activeStrangerTempHandle];
    if(!session)return;
    dmTempSendInput();
    try{
      const result=await callNiniINSAI(INS_AI_TASKS.DM_REPLY,{
        actorIds:[],ownerId:'user',surface:'dm',
        content:{temporaryStranger:{handle:session.handle,initial:session.initial},recentMessages:(session.messages||[]).slice(-16)},
        extra:{temporaryStranger:true,keepNormalStrangerDistance:true}
      });
      if(!result.ok)throw new Error(result.reason||'AI 回复失败');
      const rows=Array.isArray(result.data?.messages)?result.data.messages:(result.data?.reply?[result.data.reply]:[]);
      const live=strangerTempSessions[activeStrangerTempHandle];
      if(!live || live.handle!==session.handle)return;
      rows.slice(0,3).forEach(row=>{
        const text=String(row.textOriginal??row.text??'').trim();
        const translation=String(row.textTranslation??row.translation??'').trim();
        if(text)live.messages.push({from:'them',text,translation:translation&&translation!==text?translation:'',translated:false,createdAt:Date.now()});
      });
      persistStrangerTempSessions();
      renderStrangerTempSession();
    }catch(error){
      alert('陌生私信 AI 调用失败：'+(error?.message||error));
    }
  }

  document.getElementById('dmTempInput')?.addEventListener('keydown',e=>{
    if(e.key==='Enter' && !e.shiftKey){
      e.preventDefault();
      dmTempSendInput();
    }
  });

  document.getElementById('dmTempSendBtn')?.addEventListener('click',dmTempSummonAI);

  document.getElementById('dmTempSaveToList')?.addEventListener('click',()=>{
    const session=strangerTempSessions[activeStrangerTempHandle];
    if(!session)return;
    addTempSessionToDMList(session,'临时');
    closeDMListCards();
  });

  document.getElementById('dmTempPromoteNpc')?.addEventListener('click',()=>{
    const session=strangerTempSessions[activeStrangerTempHandle];
    if(!session)return;
    promoteTempSessionToNPC(session);
    closeDMListCards();
  });

  document.getElementById('dmTempDelete')?.addEventListener('click',()=>{
    const handle=activeStrangerTempHandle;
    if(!handle)return;
    delete strangerTempSessions[handle];
    persistStrangerTempSessions();
    document.querySelectorAll(`#dmV2List [data-temp-stranger="${CSS.escape(handle)}"]`).forEach(row=>row.remove());
    activeStrangerTempHandle=null;
    renderDMQuickTempNpcList();
    closeDMListCards();
  });

  document.getElementById('dmStrangerScopeToggle')?.addEventListener('click',()=>{
    const box=document.getElementById('dmStrangerScopeSettings');
    const show=!box.classList.contains('show');
    box.classList.toggle('show',show);
    document.getElementById('dmStrangerScopeToggle').textContent=show?'收起':'展开';
  });

  document.querySelectorAll('[data-social-trigger]').forEach(btn=>{
    btn.addEventListener('click',()=>btn.classList.toggle('active'));
  });

  document.getElementById('dmStrangerScopeSave')?.addEventListener('click',()=>{
    userSocialScopeSettings.batchSize=Number(document.getElementById('dmStrangerBatchSize')?.value||4);
    userSocialScopeSettings.triggers=[...document.querySelectorAll('[data-social-trigger].active')].map(btn=>btn.dataset.socialTrigger);
    userSocialScopeSettings.strangerPrompt=document.getElementById('dmStrangerScopePrompt')?.value.trim()||'';
    closeDMListCards();
  });

  document.getElementById('dmListDim')?.addEventListener('click', closeDMListCards);
  document.querySelectorAll('[data-dm-list-close]').forEach(btn=>btn.addEventListener('click', closeDMListCards));
}

/* ===== INS-v5.0.7 Roche Identity Hydration =====
   Demo identities are only prototype scaffolding.
   In the real Roche plugin, active persona + character.list() are the source of truth.
*/
let rocheAvailableCharacters=[];
let rocheImportedCharacterIds=new Set();
window.__niniAvailableRocheCharacters=rocheAvailableCharacters;
window.__niniImportedRocheCharacterIds=rocheImportedCharacterIds;
function escapeRocheHydrationHTML(value){
  return escapeDMCardHTML(String(value??''));
}

function bindHydratedDMRow(row){
  if(!row)return;
  const check=row.querySelector('.dm-edit-check');
  check?.addEventListener('click',e=>{
    e.preventDefault();
    e.stopPropagation();
    toggleDMRowEditSelection(row);
  });

  row.addEventListener('click',e=>{
    if(dmListEditMode){
      e.preventDefault();
      toggleDMRowEditSelection(row);
      return;
    }
    if(e.target.closest('[data-dm-profile]'))return;
    row.querySelector('.dm-v2-unread')?.remove();
    row.dataset.read='1';
    openDMChat(row.dataset.dmChat);
  });

  row.querySelector('[data-dm-profile]')?.addEventListener('click',e=>{
    e.preventDefault();
    e.stopPropagation();
    if(dmListEditMode){
      toggleDMRowEditSelection(row);
      return;
    }
    openCharProfile(e.currentTarget.dataset.dmProfile);
  });
}

function renderRocheHydratedIdentityUI(){
  const rocheActors=getRelationshipActors().filter(actor=>actor.kind==='roche');

  const dmTopId=document.querySelector('#page-dm .dm-v2-id');
  if(dmTopId)dmTopId.textContent=userProfileState.id||userProfileState.displayName||'user';

  const userStory=document.querySelector('.stories .story.user');
  if(userStory){
    const img=userStory.querySelector('.story-inner img');
    if(img && getUserAvatarUrl())img.src=getUserAvatarUrl();
  }

  if(typeof syncHomeStoryStrip==='function'){
    syncHomeStoryStrip();
  }

  restoreSavedTempSessionsToDMList();

  const strip=document.querySelector('#page-dm .dm-avatar-strip');
  const manage=document.getElementById('dmImportManageBtn');
  if(strip && manage){
    [...strip.querySelectorAll('[data-dm-profile]')].forEach(el=>el.remove());
    rocheActors.forEach(actor=>{
      const item=document.createElement('div');
      item.className='dm-profile-item';
      item.dataset.dmProfile=actor.id;
      item.innerHTML=`
        <div class="dm-profile-ring" style="${actor.avatarUrl?`background-image:url(&quot;${escapeRocheHydrationHTML(actor.avatarUrl)}&quot;);background-size:cover;background-position:center`:''}">
          ${actor.avatarUrl?'':escapeRocheHydrationHTML(actor.initial)}
        </div>
        <div class="dm-profile-name">${escapeRocheHydrationHTML(actor.name)}</div>`;
      item.addEventListener('click',()=>openCharProfile(actor.id));
      strip.insertBefore(item,manage);
    });

    const userRing=strip.querySelector('#dmUserNoteItem .dm-profile-ring');
    if(userRing){
      const url=getUserAvatarUrl();
      userRing.style.backgroundImage=url?`url("${url}")`:'';
      userRing.style.backgroundSize=url?'cover':'';
      userRing.style.backgroundPosition=url?'center':'';
    }
  }

  const dmList=document.getElementById('dmV2List');
  if(dmList){
    dmList.querySelectorAll('[data-dm-chat]').forEach(row=>row.remove());
    rocheActors.forEach(actor=>{
      const profile=getDMProfile(actor.id);
      const row=document.createElement('div');
      row.className='dm-v2-row';
      row.dataset.kind='char';
      row.dataset.dmChat=actor.id;
      row.dataset.search=`${actor.id} ${actor.handle} ${actor.name} Roche`;
      row.innerHTML=`
        <button class="dm-edit-check" type="button" aria-label="选择对话"></button>
        <div class="dm-v2-avatar" data-dm-profile="${escapeRocheHydrationHTML(actor.id)}"
          style="${profile.avatarUrl?`background-image:url(&quot;${escapeRocheHydrationHTML(profile.avatarUrl)}&quot;);background-size:cover;background-position:center`:''}">
          ${profile.avatarUrl?'':escapeRocheHydrationHTML(profile.initial)}
        </div>
        <div class="dm-v2-main">
          <div class="dm-v2-topline">
            <div class="dm-v2-name">${escapeRocheHydrationHTML(profile.name)}</div>
            <div class="dm-v2-time"></div>
          </div>
          <div class="dm-v2-preview">Roche 角色 · @${escapeRocheHydrationHTML(profile.handle)}</div>
        </div>`;
      dmList.appendChild(row);
      bindHydratedDMRow(row);
    });
  }

  const feedChips=document.getElementById('feedCharChips');
  if(feedChips){
    feedChips.innerHTML=rocheActors.map((actor,index)=>`
      <button class="feed-char-chip ${index===0?'on':''}" data-feed-char="${escapeRocheHydrationHTML(actor.id)}" type="button">
        ${escapeRocheHydrationHTML(actor.name)}
      </button>`).join('');
    feedChips.querySelectorAll('.feed-char-chip').forEach(chip=>{
      chip.addEventListener('click',()=>chip.classList.toggle('on'));
    });
  }

  if(typeof renderReelsV15GenerationSources==='function'){
    renderReelsV15GenerationSources();
  }

  renderDMImportActorList();
  renderDMUserNpcHub();
  renderPeopleRelations();
  renderFeedShareDMRecipients();
  renderReelsV15DMRecipients();
  renderUserProfile();
  renderFeed();
  renderReelsV15();
  renderCreatePostCharChoices?.();
  hydrateIdentityAvatars(document);
}

function hydrateRocheIdentityData(payload={}){
  const persona=payload.persona||null;
  const characters=Array.isArray(payload.characters)?payload.characters.filter(c=>c&&c.id):[];
  if(payload.dmState)applyPersistedINSDMHistory(payload.dmState);
  rocheAvailableCharacters=characters;
  rocheImportedCharacterIds=new Set((Array.isArray(payload.importedIds)?payload.importedIds:[]).map(String));
  window.__niniAvailableRocheCharacters=rocheAvailableCharacters;
  window.__niniImportedRocheCharacterIds=rocheImportedCharacterIds;

  if(persona){
    userProfileState.rochePersonaId=persona.id||'';
    userProfileState.id=String(persona.handle||persona.name||persona.id||userProfileState.id).replace(/^@/,'');
    userProfileState.displayName=persona.name||persona.handle||persona.id||userProfileState.displayName;
    userProfileState.bio=persona.bio||userProfileState.bio||'';
    userProfileState.avatarUrl=persona.avatar||'';
    userProfileState.defaultAvatarUrl=persona.avatar||'';
  }

  // Remove prototype-only Roche identities. Fixed NPCs created by the user later still use profileType:'npc'.
  Object.keys(dmProfiles).forEach(key=>delete dmProfiles[key]);
  Object.keys(charProfileState.profiles).forEach(key=>delete charProfileState.profiles[key]);
  Object.keys(charProfileState.boardBatches).forEach(key=>delete charProfileState.boardBatches[key]);
  Object.keys(charProfileState.boardIndex).forEach(key=>delete charProfileState.boardIndex[key]);

  Object.keys(storyViewerData).forEach(key=>{
    if(key!=='user')delete storyViewerData[key];
  });
  Object.keys(dmBaseMessages).forEach(key=>delete dmBaseMessages[key]);
  Object.keys(dmSharedCards).forEach(key=>delete dmSharedCards[key]);
  Object.keys(charAlbumPreviewData).forEach(key=>delete charAlbumPreviewData[key]);
  Object.keys(storyBatchState).forEach(key=>{
    if(key!=='user')delete storyBatchState[key];
  });

  // Remove demo-char content while keeping public strangers/user content.
  const demoRefs=new Set(['FufuA1','yy_1008','wuujo11','芙芙','Yuan_','Wujo_']);
  for(let i=posts.length-1;i>=0;i--){
    const p=posts[i];
    if(demoRefs.has(p.author) || p.type==='char' || p.type==='npc')posts.splice(i,1);
  }
  if(typeof commentsByPost!=='undefined'){
    Object.keys(commentsByPost).forEach(pid=>{
      commentsByPost[pid]=(commentsByPost[pid]||[]).filter(c=>!demoRefs.has(c.author));
    });
  }
  if(typeof reelsV15State!=='undefined'){
    reelsV15State.items=(reelsV15State.items||[]).filter(item=>
      item.kind==='stranger' || item.kind==='user'
    );
    const keptReelIds=new Set((reelsV15State.items||[]).map(item=>String(item.id||'')));
    Object.keys(reelsV15State.comments||{}).forEach(id=>{
      if(!keptReelIds.has(String(id)))delete reelsV15State.comments[id];
    });
    reelsV15State.activeId=reelsV15State.items[0]?.id||null;
  }

  characters.filter(c=>rocheImportedCharacterIds.has(String(c.id))).forEach(c=>{
    const actorId=String(c.id);
    const name=String(c.name||c.handle||actorId);
    const igHandle=String(c.handle||c.name||actorId).replace(/^@/,'');
    const avatarUrl=String(c.avatar||'');
    const bio=String(c.bio||'');
    const personaText=String(c.persona||c.bio||'');
    const initial=String(name||igHandle||'?').slice(0,1).toUpperCase();

    dmProfiles[actorId]={
      profileType:'roche',
      rocheId:actorId,
      initial,
      name,
      handle:igHandle,
      avatarUrl,
      location:'',
      followers:0,
      countryLanguage:'',
      ageVibe:'',
      personality:personaText,
      postingStyle:'',
      speechStyle:'',
      relationToUser:'',
      personaSummary:personaText
    };

    charProfileState.profiles[actorId]={
      profileType:'roche',
      rocheId:actorId,
      initial,
      name,
      handle:igHandle,
      avatarUrl,
      location:'',
      followers:0,
      following:0,
      bio,
      countryLanguage:'',
      ageVibe:'',
      personality:personaText,
      postingStyle:'',
      speechStyle:'',
      personaSummary:personaText,
      highlights:[]
    };

    charProfileState.boardBatches[actorId]=[];
    charProfileState.boardIndex[actorId]=0;
    storyViewerData[actorId]=[];
    // v5.0: restore the INS-native DM timeline from Roche storage.
    dmBaseMessages[actorId]=getStoredINSDMMessages(actorId);
    dmSharedCards[actorId]=getStoredINSDMCards(actorId);
    charAlbumPreviewData[actorId]=[];
    syncCharAlbumUserAssetsIntoPreview(actorId);
    storyBatchState[actorId]={liked:false,caption:'',audienceReactions:[],reactionPlanStatus:'idle',plannedReactions:[]};
  });

  if(payload.reelsState)applyPersistedINSReelsState(payload.reelsState);

  // Prototype seed relationships must not make real Roche characters magically know each other.
  relationshipGraphState.edges=relationshipGraphState.edges.filter(edge=>
    !demoRefs.has(edge.from) && !demoRefs.has(edge.to)
  );
  relationshipGraphState.centerId='user';

  // Drop only old demo memory-source keys, then discover the real Roche conversations.
  if(typeof insSocialMemoryState!=='undefined'){
    ['FufuA1','yy_1008','wuujo11'].forEach(id=>{
      delete insSocialMemoryState.mounts?.[id];
      delete insSocialMemoryState.sources?.[id];
      delete insSocialMemoryState.writebackTargets?.[id];
      delete insSocialMemoryState.knowledge?.[id];
      delete insSocialMemoryState.syncCursor?.[id];
    });
    persistINSSocialMemory?.();
  }

  restorePersistedHighlights();
  restoreLiveStoriesFromArchive();
  expireLiveStoriesNow({render:false});
  renderRocheHydratedIdentityUI();
  renderDMImportActorList();

  return {
    ok:true,
    user:{
      rochePersonaId:userProfileState.rochePersonaId||'',
      handle:userProfileState.id,
      name:userProfileState.displayName
    },
    characters:getRelationshipActors()
      .filter(actor=>actor.kind==='roche')
      .map(actor=>({id:actor.id,handle:actor.handle,name:actor.name}))
  };
}

async function toggleRocheCharacterImport(actorId){
  const id=String(actorId||'');if(!id)return;
  const adding=!rocheImportedCharacterIds.has(id);
  if(adding)rocheImportedCharacterIds.add(id);else rocheImportedCharacterIds.delete(id);
  window.__niniImportedRocheCharacterIds=rocheImportedCharacterIds;
  try{if(window.NiniINSHost?.setImportedCharacterIds)await window.NiniINSHost.setImportedCharacterIds([...rocheImportedCharacterIds]);}catch(e){console.warn('[INS] 保存引入角色失败',e);}
  hydrateRocheIdentityData({
    persona:{id:userProfileState.rochePersonaId||'',handle:userProfileState.id,name:userProfileState.displayName,bio:userProfileState.bio,avatar:userProfileState.avatarUrl},
    characters:rocheAvailableCharacters,
    importedIds:[...rocheImportedCharacterIds],
    dmState:niniPersistedDMState,
    reelsState:niniPersistedReelsState
  });
  // v5.0.1: importing a Roche char does not discover or mount conversations.
  // Memory is configured explicitly from “记忆挂载”, matching Roche Moments.
}

window.NiniINSRocheIdentity={
  hydrate:hydrateRocheIdentityData,
  getSnapshot:()=>({
    user:{
      rochePersonaId:userProfileState.rochePersonaId||'',
      handle:userProfileState.id,
      name:userProfileState.displayName
    },
    importedIds:[...rocheImportedCharacterIds],
    available:rocheAvailableCharacters,
    actors:getRelationshipActors()
  })
};


/* ===== v1.06.1 DM list avatar routing =====
   In the message list, tapping the avatar opens the same chat as tapping the row.
   The top horizontal profile rail is intentionally untouched and still opens Char Profile. */
function bindDMListAvatarToChatOnly(){
  const list=document.getElementById('dmV2List');
  if(!list || list.dataset.avatarToChatOnlyBound)return;
  list.dataset.avatarToChatOnlyBound='1';

  list.addEventListener('click',event=>{
    const avatar=event.target.closest?.('.dm-v2-avatar');
    const row=event.target.closest?.('.dm-v2-row');
    if(!avatar || !row || !list.contains(row))return;

    const chatId=String(row.dataset.dmChat||'').trim();
    if(!chatId)return;

    event.preventDefault();
    event.stopImmediatePropagation();

    if(dmListEditMode){
      toggleDMRowEditSelection(row);
      return;
    }

    row.querySelector('.dm-v2-unread')?.remove();
    row.dataset.read='1';
    openDMChat(chatId);
  },true);
}

bindDMListV2();
bindDMListAvatarToChatOnly();
bindPeopleRelationsV11515();
bindINSSocialMemoryUI();
renderDMImportActorList();
renderDMUserNpcHub();
renderPeopleRelations();
syncAllDMListPreviews();


renderUserProfile();
renderFeed();
renderReelsV15();
renderNav();
hydrateIdentityAvatars(document);
renderINSNotifications();

updateDrawerSubApiLabel();
renderINSSubApiSettings();
startINSAutoPublishTimer();

  }

  function normalizeTextResponse(r) {
    if (r == null) return '';
    if (typeof r === 'string') return r;
    return r.text || r.content || r.message || r.output || '';
  }

  function extractJson(text) {
    if (text && typeof text === 'object') return text;
    let raw = String(text || '').trim();
    if (!raw) return {};
    raw = raw.replace(/^```(?:json)?\s*/i, '').replace(/\s*```$/, '').trim();
    try { return JSON.parse(raw); } catch (e) {}
    const a = raw.indexOf('{');
    const b = raw.lastIndexOf('}');
    if (a >= 0 && b > a) {
      try { return JSON.parse(raw.slice(a, b + 1)); } catch (e) {}
    }
    throw new Error('AI 返回内容不是可解析 JSON');
  }

  function taskOutputInstruction(task) {
    if (task === 'home_strangers') {
      return 'Return JSON only. No Markdown or explanation. Return exactly {"items":[{"actor":{"handle":"...","displayName":"...","language":"...","region":"...","bio":"..."},"mediaItems":[{"mediaDescription":"中文图片描述","mediaUrl":""}],"captionZh":"简体中文正文","sourceLanguage":"ko","location":"Tokyo·Japan","tags":["#daily"],"metrics":{"likes":123,"comments":18,"reposts":4,"shares":7},"initialComments":[{"actorId":"","author":"string_handle","displayName":"Display Name","textOriginal":"...","textTranslation":"","sourceLanguage":"...","replyToActorId":""}]}]}. For Home Feed, captionZh MUST be natural Simplified Chinese and sourceLanguage marks the language the account would naturally post in. Do not return captionOriginal or captionTranslation; original-language text is handled by a separate on-demand API. mediaDescription is always Chinese; location and tags are English. Follow request.extra.requestedMediaCounts exactly and return exactly the requested five initialComments per post.';
    }
    if (task === 'reels_generate') {
      return 'Return JSON only. No Markdown or explanation. Schema: {"items":[{"actorId":"...","actor":{"handle":"...","displayName":"...","language":"...","region":"...","bio":"..."},"author":"...","videoDescription":"中文视频画面描述","captionOriginal":"...","captionTranslation":"","sourceLanguage":"...","location":"Seoul·Korea","tags":["#daily"],"metrics":{"likes":0,"comments":0,"reposts":0,"shares":0},"initialComments":[{"actorId":"","author":"string_handle","displayName":"Display Name","textOriginal":"...","textTranslation":"","sourceLanguage":"...","replyToActorId":""}]}]}.' +
        ' For imported actors, use the provided actorId exactly and omit actor if unnecessary. For stranger generation, actorId may be empty but actor.handle/displayName must be present and must fit the current world/network-culture naming rules for that account\'s own region/language/IG style. Stranger commenters follow the same identity/naming rule independently of the Reel author.' +
        ' captionOriginal stays in the actor\'s natural language. If non-Chinese, put Chinese only in captionTranslation; if Chinese, captionTranslation must be empty.' +
        ' videoDescription MUST always be Chinese. location MUST be English in City·Country / Place·Country format. tags MUST be English-only hashtags.' +
        ' If extra.includeInitialComments=true, return exactly extra.initialCommentCount actual initialComments and make metrics.comments at least that count. Never include the current user/persona in initialComments. For the standard 5-comment batch use at least 2 sourceLanguage values and at least one comment language different from the Reel caption language; do not mirror the post language across the whole comment section.';
    }
    if (task === 'feed_generate') {
      return 'Return JSON only. No Markdown or explanation. Schema: {"items":[{"actorId":"...","author":"...","mediaType":"image","mediaItems":[{"mediaDescription":"中文图片描述","mediaUrl":"","albumCategory":"portrait_selfie","albumTags":["夏季","甜品店"],"albumEraId":""}],"captionZh":"简体中文正文","sourceLanguage":"ko","location":"Seoul·Korea","tags":["#daily"],"metrics":{"likes":123,"comments":18,"reposts":4,"shares":7},"initialComments":[{"actorId":"","author":"citycat_82","displayName":"City Cat","textOriginal":"...","textTranslation":"","sourceLanguage":"en","replyToActorId":""}]}]}.' +
        ' Every mediaDescription MUST be Chinese. For image media, when the supplied preference/albumMatcher asks for album planning, also return albumCategory and Chinese albumTags for every slot; albumEraId is only for portrait slots when era matching is active. If extra.freeMediaPlan=true, freely choose 1-4 media items based on the post itself; do not force a human/scene mix or a fixed count. For Home Feed return only natural Simplified-Chinese captionZh plus sourceLanguage for the language the actor would naturally post in. Do not return captionOriginal or captionTranslation. Prefer 2-4 natural sentences with a naturally varied length; do not write to a preview or collapse threshold.' +
        ' location MUST be English in City·Country / Place·Country format. tags MUST be English-only hashtags.' +
        ' Metrics must be plausible positive integers. If extra.includeInitialComments=true, return exactly extra.initialCommentCount visible comments and make metrics.comments at least that count.' +
        ' Stranger comment author must always be a string handle, never an object. Keep displayName separate. Every stranger handle/displayName must follow the current world/network-culture naming rules for the commenter account region/language; commenter language is independent from the post author language. Never generate the current user/persona as a commenter. For the standard 5-comment batch use at least 2 sourceLanguage values and at least one language different from the post caption language.';
    }
    const common =
      '只返回 JSON，不要 Markdown，不要解释。' +
      '人物原文放 textOriginal/captionOriginal；' +
      '非中文时如需中文翻译放 textTranslation/captionTranslation；' +
      '中文原文翻译字段留空。';

    const map = {
      home_strangers:
        '返回 {items:[{actor:{handle,displayName,language,region,bio},mediaItems:[{mediaDescription,mediaUrl?}],captionZh,sourceLanguage,location,tags,metrics:{likes,comments,reposts,shares},initialComments:[{actorId,author,displayName,textOriginal,textTranslation,sourceLanguage,replyToActorId}]}]}。首页 Feed 的 captionZh 固定为自然简体中文，sourceLanguage 标记账号原本会使用的语言；不要返回 captionOriginal/captionTranslation。必须严格遵守 request.extra.requestedMediaCounts；每条动态 1-4 个 mediaItems，多图描述必须不同且 mediaDescription 一律中文。陌生人的昵称+ID 遵循其国家/地区与当地 IG 命名习惯。location 只用英文 City·Country / Place·Country，tags 只用英文。',
      feed_generate:
        '返回 {items:[{actorId,author,mediaType,mediaItems:[{mediaDescription,mediaUrl?,albumCategory?,albumTags?,albumEraId?}],captionZh,sourceLanguage,location,tags,metrics:{likes,comments,reposts,shares},initialComments:[]}]}。首页 Feed 的 captionZh 固定为自然简体中文，sourceLanguage 标记账号原本会使用的语言；不要返回 captionOriginal/captionTranslation。如果 extra.freeMediaPlan=true，则根据动态本身自然决定 1-4 个 mediaItems；多图描述必须彼此不同且全部中文。location 只用英文 City·Country / Place·Country；tags 只用英文。',
      story_generate:
        '返回 {items:[{actorId,captionOriginal,captionTranslation,sourceLanguage,storyItems:[{type,mediaDescription,mediaUrl?,albumCategory?,albumTags?,albumEraId?}]}]}；每个 Story batch 自然决定 1-4 个 item，无公开评论线程。图片描述一律中文。如 supplied preference 要求私人相册规划，图片 item 返回固定 albumCategory + 中文 albumTags；时期匹配开启时只有人像返回 albumEraId。不要固定人像/景物比例。',
      reels_generate:
        '返回 {items:[{actorId,actor?,author,videoDescription,captionOriginal,captionTranslation,sourceLanguage,location,tags,metrics:{likes,comments,reposts,shares},initialComments:[]}]}。videoDescription 一律中文；陌生人的昵称+ID+文案按其国家/地区自然语言与当地 IG 风格；location 只用英文 City·Country / Place·Country；tags 只用英文；标准 5 条评论至少 2 种语言且不能全跟正文同语种。',
      public_comment_batch:
        'Return JSON only: {"comments":[{"actorId":"","author":"string_handle","displayName":"Display Name","textOriginal":"...","textTranslation":"","sourceLanguage":"...","replyToActorId":""}]}. Return exactly extra.actualCommentCount actual visible comments. Never generate the current user/persona as a commenter. author MUST be a string handle, never an object. For existing formal actors use exact actorId. For strangers keep actorId empty and generate a stable-looking handle + separate displayName that obey the current world/network-culture naming convention for the stranger account region/language. Commenter language is independent from the post author language: for a standard 5-comment batch use at least 2 sourceLanguage values and at least one language different from the caption language; for top-ups prefer languages missing from existing comments. Non-Chinese comments need separate Chinese textTranslation; Chinese comments leave it empty.',
      direct_comment_reply:
        '返回 {reply:{actorId,author,textOriginal,textTranslation,sourceLanguage,replyToActorId}}。',
      dm_reply:
        '返回 {messages:[{textOriginal,textTranslation,sourceLanguage}],share?:{decision:"share|defer|none",source:"linked|dm_only|own_publish",contentType:"feed|reel",contentId?:"候选ID",relevance?:0-1,content?:{author,displayName,caption,captionOriginal,captionZh,captionTranslation,sourceLanguage,mediaDescription,mediaUrl,videoDescription,location,tags,metrics,initialComments}}}。通常 1-4 条短消息。绝不返回 Story 分享。linked 必须使用 dmShare.candidates 中真实 contentId；dm_only 不进入主页/Feed/Reels；own_publish 表示当前角色本人真实发布后再分享。User 主动请求时仍根据人物性格决定是否立即执行；若暂时不分享可 decision=defer。若决定立即满足 User 的主动请求，优先返回 dm_only（全新临时内容），不要复用 linked 内置/已有候选；只有角色明确表示自己要公开发布时才用 own_publish。若回复文字表达“已经发/给你看这个/喏这个”等立即分享语义，则必须同时返回 share.decision=share，禁止只说发送却不返回 share。',
      stranger_dm_refresh:
        '返回 {requests:[{handle,displayName,initial,language,region,source,textOriginal,textTranslation,sourceLanguage}]}，数量遵循 batchSize。每个陌生请求都必须按当前 world/network-culture 规则生成自洽的 handle + displayName + region + language/sourceLanguage；账号命名遵循其地区真实网络习惯，语言由该陌生人身份决定。全球来源、合理发现路径、正常陌生距离。',
      guestbook_refresh:
        'Return JSON only: {"entries":[{"actorId":"","author":"string_handle","displayName":"Display Name","textOriginal":"...","textTranslation":"","sourceLanguage":"...","meta":"网友"}]}. Return exactly extra.actualEntryCount entries. Never generate the current user/persona as an author. Existing formal actors use exact actorId. Stranger visitors must use a stable string handle plus separate displayName following the current world/network-culture naming rules for their own region/language; do not copy the profile owner language just because they are leaving a note.',
      reaction_settlement:
        '返回 {reactions:[{actorId,code}]}；code 仅可为 like/amused/excited/moved/surprised/supportive/intense，也允许 reactions 为空。'
    };

    return common + '\n' + (map[task] || '根据 request.task 返回结构化 JSON。');
  }

  async function safeCharacters(roche) {
    try {
      return roche && roche.character && roche.character.list
        ? (await roche.character.list()) || []
        : [];
    } catch (e) {
      return [];
    }
  }

  async function safeActivePersona(roche) {
    try {
      if (roche && roche.persona) {
        if (typeof roche.persona.getActiveUserPersona === 'function') return await roche.persona.getActiveUserPersona();
        if (typeof roche.persona.getActive === 'function') return await roche.persona.getActive();
      }
      return null;
    } catch (e) {
      return null;
    }
  }

  async function safeUserPersonas(roche) {
    try {
      if (roche && roche.persona) {
        // Roche Moments uses this API: it returns every user persona on the small phone.
        if (typeof roche.persona.getUserPersonas === 'function') {
          const rows = await roche.persona.getUserPersonas();
          return Array.isArray(rows) ? rows : [];
        }
        // Compatibility fallbacks for other Roche builds.
        if (typeof roche.persona.listUserPersonas === 'function') {
          const rows = await roche.persona.listUserPersonas();
          return Array.isArray(rows) ? rows : [];
        }
        if (typeof roche.persona.getAllUserPersonas === 'function') {
          const rows = await roche.persona.getAllUserPersonas();
          return Array.isArray(rows) ? rows : [];
        }
      }
      return [];
    } catch (e) {
      return [];
    }
  }

  const INS_BOUND_USER_PERSONA_KEY='niniInsBoundUserPersonaId';

  async function safeINSBoundUserPersona(roche) {
    const list=await safeUserPersonas(roche);
    let boundId='';
    try{
      boundId=String(await roche?.storage?.get?.(INS_BOUND_USER_PERSONA_KEY)||'');
    }catch(e){}

    if(boundId){
      const bound=list.find(per=>String(per?.id||'')===boundId);
      if(bound)return bound;
    }

    const active=await safeActivePersona(roche);
    if(active){
      const activeId=String(active?.id||'');
      const matched=list.find(per=>String(per?.id||'')===activeId);
      if(matched)return matched;
      return active;
    }

    return list[0]||null;
  }

  async function safeSetINSBoundUserPersona(roche, personaId) {
    const id=String(personaId||'').trim();
    if(!id)return await safeINSBoundUserPersona(roche);
    const list=await safeUserPersonas(roche);
    const target=list.find(per=>String(per?.id||'')===id);
    if(!target)throw new Error('找不到这个 user persona');
    try{
      await roche?.storage?.set?.(INS_BOUND_USER_PERSONA_KEY,id);
    }catch(e){
      throw new Error('保存 INS user 绑定失败');
    }
    // Important: INS binding is plugin-local. Do not change Roche's globally active user persona.
    return target;
  }

  async function buildActorContext(roche, appWindow, actorIds) {
    const charsStarted=Date.now();
    const chars = await safeCharacters(roche);
    const charsElapsedMs=Date.now()-charsStarted;
    const charMap = {};
    chars.forEach(c => { if (c && c.id) charMap[c.id] = c; });

    const rows = [];
    for (const id of (actorIds || [])) {
      const c = charMap[id] || null;
      let mem = '';
      let memoryMeta={elapsedMs:0,totalShortCount:0,totalFactCount:0,sources:[],errors:[]};
      const memStarted=Date.now();
      try {
        if (appWindow && appWindow.NiniINSSocialMemory && typeof appWindow.NiniINSSocialMemory.loadActorRocheMemoryContext === 'function') {
          const ctx = await appWindow.NiniINSSocialMemory.loadActorRocheMemoryContext(id);
          mem = (ctx && ctx.text) || '';
          const sources=Array.isArray(ctx?.sources)?ctx.sources:[];
          memoryMeta={
            elapsedMs:Date.now()-memStarted,
            totalShortCount:sources.reduce((n,row)=>n+(Number(row?.shortCount)||0),0),
            totalFactCount:sources.reduce((n,row)=>n+(Number(row?.factCount)||0),0),
            sources:sources.map(row=>({conversationId:row?.conversationId||'',conversationName:row?.conversationName||'',isGroup:!!row?.isGroup,shortCount:Number(row?.shortCount)||0,factCount:Number(row?.factCount)||0})),
            errors:Array.isArray(ctx?.errors)?ctx.errors:[],
            reason:ctx?.reason||''
          };
        }
      } catch (e) {
        memoryMeta.elapsedMs=Date.now()-memStarted;
        memoryMeta.errors=[{stage:'memory_context',message:String(e?.message||e)}];
        console.warn('[INS] Roche memory context load failed', id, e);
      }

      rows.push({
        actorId: id,
        rocheCharacter: c ? {id:c.id,name:c.name||'',handle:c.handle||'',bio:c.bio||'',persona:c.persona||'',avatar:c.avatar||''} : null,
        rocheMemoryContext: mem,
        rocheMemoryMeta: memoryMeta,
        characterListElapsedMs: charsElapsedMs
      });
    }
    return rows;
  }

  async function callSubApi(preset, messages, temperature) {
    const base = String(preset.url || '').replace(/\/+$/, '');
    const res = await fetch(base + '/chat/completions', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'Authorization': 'Bearer ' + preset.apiKey
      },
      body: JSON.stringify({
        model: preset.model,
        messages,
        temperature,
        stream: false
      })
    });
    if (!res.ok) throw new Error('INS 副 API 状态 ' + res.status);
    const data = await res.json();
    return data &&
      data.choices &&
      data.choices[0] &&
      data.choices[0].message
        ? data.choices[0].message.content || ''
        : '';
  }

  function sleep(ms){return new Promise(resolve=>setTimeout(resolve,ms));}
  function isTransientModelError(error){const msg=String(error&&(error.message||error)||'').toLowerCase();return /temporarily unavailable|try again later|overloaded|rate limit|status 429|status 502|status 503|status 504/.test(msg);}
  async function callRocheAIWithRetry(roche,options){let last=null;for(let i=0;i<3;i++){try{return normalizeTextResponse(await roche.ai.chat(options));}catch(e){last=e;if(!isTransientModelError(e)||i===2)break;await sleep(700*(i+1));}}if(isTransientModelError(last))throw new Error('当前 Roche 默认模型暂时不可用，INS 已自动重试 2 次。请稍后再试，或切换可用的副 API。原始错误：'+String(last?.message||last||'unknown'));throw last||new Error('Roche AI 调用失败');}


  function compactFeedMiddle(value,max=8000){
    const text=String(value||'');
    if(text.length<=max)return text;
    const head=Math.ceil(max*0.62);
    const tail=max-head;
    return text.slice(0,head)+'\n...[middle omitted for Feed request]...\n'+text.slice(-tail);
  }

  function compactFeedTail(value,max=10000){
    const text=String(value||'');
    if(text.length<=max)return text;
    return '...[older mounted context omitted for Feed request]...\n'+text.slice(-max);
  }

  function buildLeanFeedActorContext(actorContext){
    return (actorContext||[]).map(row=>{
      const c=row?.rocheCharacter||null;
      return {
        actorId:row?.actorId||'',
        rocheCharacter:c?{
          id:c.id||'',
          name:String(c.name||'').slice(0,120),
          handle:String(c.handle||'').slice(0,120),
          bio:compactFeedMiddle(c.bio||'',1400),
          persona:compactFeedMiddle(c.persona||'',8000)
        }:null,
        rocheMemoryContext:compactFeedTail(row?.rocheMemoryContext||'',10000)
      };
    });
  }

  function buildLeanFeedRelationships(rows){
    return (Array.isArray(rows)?rows:[]).slice(0,8).map(row=>({
      actorId:row?.actorId||'',
      name:String(row?.name||'').slice(0,80),
      handle:String(row?.handle||'').slice(0,80),
      kind:row?.kind||'',
      ownerLooksAtActor:String(row?.ownerLooksAtActor||'').slice(0,120),
      actorLooksAtOwner:String(row?.actorLooksAtOwner||'').slice(0,120)
    }));
  }

  function formatNiniAIDiagnosticError(error,diag){
    const memory=(diag.memory||[]).map(row=>{
      const groups=(row.sources||[]).length;
      const errCount=(row.errors||[]).length;
      return `${row.actorId}: ${row.shortCount||0}条短期 / ${row.factCount||0}条长期 / ${groups}个挂载 / ${row.elapsedMs||0}ms${errCount?` / ${errCount}个读取错误`:''}`;
    }).join('；') || '无角色挂载记忆';
    const lines=[
      `[${diag.stage||'UNKNOWN'}] ${String(error?.message||error||'unknown')}`,
      `路由：${diag.route||'未知'}`,
      `角色：${(diag.actorIds||[]).join(', ')||'无'}`,
      `记忆：${memory}`,
      `角色准备：${diag.timings?.actorContextMs||0}ms`,
      `Persona：${diag.timings?.personaMs||0}ms`,
      `Prompt：${diag.promptChars||0} 字符`,
      ...(diag.breakdown?[
        `构成：system ${diag.breakdown.systemChars||0} / actor ${diag.breakdown.actorChars||0} / memory ${diag.breakdown.memoryChars||0} / relation ${diag.breakdown.relationshipChars||0} / user ${diag.breakdown.userPersonaChars||0}`
      ]:[]),
      `AI 请求：${diag.timings?.modelMs||0}ms`,
      `解析：${diag.timings?.parseMs||0}ms`,
      `总耗时：${Date.now()-(diag.startedAt||Date.now())}ms`
    ];
    return lines.join('\n');
  }

  function createAIBridge(roche, getAppWindow) {
    return {
      async generate(request) {
        const diag={task:request?.task||'',actorIds:Array.isArray(request?.actorIds)?request.actorIds.slice():[],stage:'ACTOR_CONTEXT',route:'未决定',startedAt:Date.now(),timings:{actorContextMs:0,personaMs:0,modelMs:0,parseMs:0},promptChars:0,rawChars:0,memory:[]};
        try{
          const appWindow = getAppWindow();
          let t=Date.now();
          const actorContext = await buildActorContext(roche, appWindow, request.actorIds || []);
          diag.timings.actorContextMs=Date.now()-t;
          diag.memory=actorContext.map(row=>({actorId:row.actorId,elapsedMs:Number(row.rocheMemoryMeta?.elapsedMs)||0,shortCount:Number(row.rocheMemoryMeta?.totalShortCount)||0,factCount:Number(row.rocheMemoryMeta?.totalFactCount)||0,sources:Array.isArray(row.rocheMemoryMeta?.sources)?row.rocheMemoryMeta.sources:[],errors:Array.isArray(row.rocheMemoryMeta?.errors)?row.rocheMemoryMeta.errors:[]}));

          diag.stage='PERSONA';
          t=Date.now();
          const persona = await safeActivePersona(roche);
          diag.timings.personaMs=Date.now()-t;
          const pc = request.promptContext || {};

          diag.stage='BUILD_PROMPT';
          const isHomeStrangers=request?.task==='home_strangers';
          const isLeanFeed=request?.task==='feed_generate'||isHomeStrangers;
          const isLeanReels=request?.task==='reels_generate';
          const isLeanComment=request?.task==='public_comment_batch';
          const isLeanGuestbook=request?.task==='guestbook_refresh';
          const isLeanPublicContent=isLeanFeed||isLeanReels||isLeanComment||isLeanGuestbook;
          const system = [
            pc.globalRules||'',
            pc.surfaceRules||'',
            isLeanPublicContent?'OUTPUT':'【输出契约】',
            taskOutputInstruction(request.task)
          ].filter(Boolean).join('\n\n');

          let payload;
          if(isLeanPublicContent){
            const leanActorContext=buildLeanFeedActorContext(actorContext);
            const leanRelationships=buildLeanFeedRelationships(request.relationshipCandidates||[]);
            const recentKnowledge=(Array.isArray(request.actorRecentKnowledge)?request.actorRecentKnowledge:[])
              .slice(-8)
              .map(v=>String(v||'').slice(0,220));
            const perActorRelationships=(Array.isArray(request.actorRelationshipContexts)?request.actorRelationshipContexts:[])
              .slice(0,8)
              .map(row=>({
                actorId:String(row?.actorId||''),
                relationships:buildLeanFeedRelationships(row?.relationships||[]).slice(0,4),
                recentKnowledge:(Array.isArray(row?.recentKnowledge)?row.recentKnowledge:[])
                  .slice(-4)
                  .map(v=>String(v||'').slice(0,220))
              }));
            const isStrangerBatch=isHomeStrangers||!!request?.extra?.generateStrangers;

            payload={
              task:request.task,
              ownerId:request.ownerId,
              actorIds:request.actorIds,
              preference:String(request.prompt||'').slice(0,8000),
              content:(isLeanComment||isLeanGuestbook)?(request.content||null):null,
              actorContext:leanActorContext,
              directRelationships:isStrangerBatch?[]:leanRelationships,
              actorRelationshipContexts:isLeanReels?perActorRelationships:[],
              recentINSKnowledge:isStrangerBatch?[]:recentKnowledge,
              albumMatcher:request.albumMatcher||{},
              activeUser:isStrangerBatch?null:(persona?{
                id:persona.id||'',
                name:String(persona.name||'').slice(0,100),
                handle:String(persona.handle||'').slice(0,100)
              }:null),
              extra:{
                requestedCount:Number(request?.extra?.requestedCount)||1,
                onePerActor:request?.extra?.onePerActor!==false,
                generateStrangers:isStrangerBatch,
                strangerCount:Number(request?.extra?.strangerCount)||0,
                includeInitialComments:!!request?.extra?.includeInitialComments,
                initialCommentCount:Number(request?.extra?.initialCommentCount)||0,
                requestedMediaCounts:Array.isArray(request?.extra?.requestedMediaCounts)?request.extra.requestedMediaCounts.slice(0,12):[],
                freeMediaPlan:!!request?.extra?.freeMediaPlan,
                actualCommentCount:Number(request?.extra?.actualCommentCount)||0,
                actualEntryCount:Number(request?.extra?.actualEntryCount)||0,
                forbidCurrentUser:request?.extra?.forbidCurrentUser!==false,
                allowKnownActors:request?.extra?.allowKnownActors!==false,
                allowStrangers:request?.extra?.allowStrangers!==false
              }
            };

            const actorChars=leanActorContext.reduce((n,row)=>{
              const c=row.rocheCharacter||{};
              return n+String(c.bio||'').length+String(c.persona||'').length;
            },0);
            const memoryChars=leanActorContext.reduce((n,row)=>n+String(row.rocheMemoryContext||'').length,0);
            const relationshipChars=
              JSON.stringify(isStrangerBatch?[]:leanRelationships).length+
              JSON.stringify(isLeanReels?perActorRelationships:recentKnowledge).length;
            const userPersonaChars=JSON.stringify(payload.activeUser||{}).length;
            diag.breakdown={
              systemChars:system.length,
              actorChars,
              memoryChars,
              relationshipChars,
              userPersonaChars
            };
          }else{
            payload = {
              schema:request.schema,task:request.task,surface:request.surface,ownerId:request.ownerId,actorIds:request.actorIds,
              userBatchPreference:request.prompt||'',content:request.content||null,
              relationshipCandidates:request.relationshipCandidates||[],
              actorRelationshipContexts:request.actorRelationshipContexts||[],
              actorContext,
              activeUserPersona:persona?{id:persona.id,name:persona.name||'',handle:persona.handle||'',bio:persona.bio||'',persona:persona.persona||''}:null,
              albumMatcher:request.albumMatcher||{},imageGeneration:request.imageGeneration||{},outputRules:request.outputRules||{},extra:request.extra||{}
            };
          }

          const userMessage=isLeanPublicContent
            ? `Generate this Instagram ${isLeanReels?'Reels':isLeanComment?'public comment batch':isLeanGuestbook?'profile guestbook batch':'Feed'} task using only the supplied content facts, actor persona, mounted Roche memory, direct relationships and recent INS knowledge. Apply the supplied world/network-culture stranger identity and language rules to every generated stranger. Do not invent relationships. Never generate the current user/persona as a public commenter or guestbook author.\n`+JSON.stringify(payload)
            : '请完成下面这个 INS 任务。只依据提供的关系、人物、记忆与当前内容，不要擅自假设人物认识。\n'+JSON.stringify(payload);
          const messages=[{role:'system',content:system},{role:'user',content:userMessage}];
          diag.promptChars=system.length+userMessage.length;
          let preset=null;
          try{
            if(appWindow&&appWindow.NiniINSSettingsBridge&&typeof appWindow.NiniINSSettingsBridge.getActiveSubApi==='function')preset=appWindow.NiniINSSettingsBridge.getActiveSubApi();
          }catch(e){}

          diag.stage='MODEL_REQUEST';
          let raw='';
          t=Date.now();
          const requestTemperature=(request?.task==='feed_generate'||request?.task==='home_strangers'||request?.task==='reels_generate')?0.82:((request?.task==='public_comment_batch'||request?.task==='guestbook_refresh')?0.88:0.92);
          if(preset&&preset.url&&preset.apiKey&&preset.model){
            diag.route=`INS 副 API · ${preset.model||'unknown'}`;
            raw=await callSubApi(preset,messages,requestTemperature);
          }else{
            diag.route='Roche 主 API · roche.ai.chat';
            if(!roche||!roche.ai||!roche.ai.chat)throw new Error('roche.ai.chat 不可用');
            raw=await callRocheAIWithRetry(roche,{messages,temperature:requestTemperature});
          }
          diag.timings.modelMs=Date.now()-t;
          diag.rawChars=String(raw||'').length;

          diag.stage='PARSE_RESPONSE';
          t=Date.now();
          const parsed=extractJson(raw);
          diag.timings.parseMs=Date.now()-t;
          diag.stage='SUCCESS';
          diag.totalMs=Date.now()-diag.startedAt;
          if(parsed&&typeof parsed==='object')parsed.__niniDiagnostic=diag;
          return parsed;
        }catch(error){
          if(diag.stage==='MODEL_REQUEST'&&!diag.timings.modelMs){
            diag.timings.modelMs=Math.max(0,Date.now()-diag.startedAt-(diag.timings.actorContextMs||0)-(diag.timings.personaMs||0));
          }
          diag.totalMs=Date.now()-diag.startedAt;
          const wrapped=new Error(formatNiniAIDiagnosticError(error,diag));
          wrapped.niniDiagnostic=diag;
          throw wrapped;
        }
      }
    };
  }

  function filterConversationsForActor(list, actorId) {
    return (list || []).filter(c => {
      if (!c) return false;
      if (c.contactId && c.contactId === actorId) return true;
      if (Array.isArray(c.members) && c.members.includes(actorId)) return true;
      if (
        Array.isArray(c.memberProfiles) &&
        c.memberProfiles.some(m => m && m.id === actorId)
      ) return true;
      // 如果 Roche 已经通过 memberId 过滤，则信任返回结果。
      return true;
    });
  }

  function openRocheDb() {
    return new Promise((resolve, reject) => {
      const req = indexedDB.open('Roche_db');
      req.onsuccess = () => resolve(req.result);
      req.onerror = () => reject(req.error);
    });
  }

  function addMessageRecord(db, msg) {
    return new Promise((resolve, reject) => {
      const req = db
        .transaction('messages', 'readwrite')
        .objectStore('messages')
        .add(msg);
      req.onsuccess = () => resolve(req.result);
      req.onerror = () => reject(req.error);
    });
  }

  function createRocheBridge(roche) {
    const toRocheImageArgs = payload => {
      const prompt=String(payload?.prompt||payload?.positivePrompt||'').trim();
      const negativePrompt=String(payload?.negativePrompt||'').trim();
      return negativePrompt?{prompt,negativePrompt}:{prompt};
    };
    return {
      async listConversationsForActor(actorId) {
        if (!roche || !roche.conversation || !roche.conversation.list) return [];
        let list;
        try {
          list = await roche.conversation.list({ memberId: actorId });
        } catch (e) {
          list = await roche.conversation.list();
        }
        return filterConversationsForActor(list, actorId).map(c => ({
          id: c.id || c.conversationId,
          conversationId: c.id || c.conversationId,
          name: c.name || c.title || c.handle || (c.id || c.conversationId),
          isGroup: !!(c.isGroup || c.type === 'group' || c.kind === 'group'),
          contactId: c.contactId || '',
          members: c.members || [],
          memberProfiles: c.memberProfiles || [],
          type: c.type || '',
          kind: c.kind || ''
        }));
      },

      async getShortTerm(args) {
        return roche && roche.memory && roche.memory.getShortTerm
          ? await roche.memory.getShortTerm(args)
          : [];
      },

      async getLongTerm(args) {
        return roche && roche.memory && roche.memory.getLongTerm
          ? await roche.memory.getLongTerm(args)
          : null;
      },

      memory: {
        getShortTerm: async args =>
          roche && roche.memory && roche.memory.getShortTerm
            ? await roche.memory.getShortTerm(args)
            : [],
        getLongTerm: async args =>
          roche && roche.memory && roche.memory.getLongTerm
            ? await roche.memory.getLongTerm(args)
            : null
      },

      conversation: {
        list: async args =>
          roche && roche.conversation && roche.conversation.list
            ? await roche.conversation.list(args || {})
            : []
      },

      getImageCapability() {
        if (roche && roche.ai && typeof roche.ai.generateImage === 'function') return { available:true, route:'roche.ai.generateImage' };
        if (roche && roche.image && typeof roche.image.generate === 'function') return { available:true, route:'roche.image.generate' };
        if (roche && roche.images && typeof roche.images.generate === 'function') return { available:true, route:'roche.images.generate' };
        if (roche && typeof roche.generateImage === 'function') return { available:true, route:'roche.generateImage' };
        if (roche && typeof roche.invoke === 'function') return { available:true, route:'roche.invoke(image.generate)' };
        return { available:false, route:'' };
      },

      async getImageConfig() {
        if (roche && roche.ai && typeof roche.ai.getImageConfig === 'function') return await roche.ai.getImageConfig();
        if (roche && roche.image && typeof roche.image.getConfig === 'function') return await roche.image.getConfig();
        if (roche && roche.image && roche.image.config && typeof roche.image.config.get === 'function') return await roche.image.config.get();
        if (roche && typeof roche.getImageConfig === 'function') return await roche.getImageConfig();
        if (roche && typeof roche.invoke === 'function') return await roche.invoke('image.getConfig', {});
        return null;
      },

      async generateImage(payload) {
        if (roche && roche.ai && typeof roche.ai.generateImage === 'function') return await roche.ai.generateImage(toRocheImageArgs(payload));
        if (roche && roche.image && typeof roche.image.generate === 'function') return await roche.image.generate(payload);
        if (roche && roche.images && typeof roche.images.generate === 'function') return await roche.images.generate(payload);
        if (roche && typeof roche.generateImage === 'function') return await roche.generateImage(payload);
        if (roche && typeof roche.invoke === 'function') return await roche.invoke('image.generate', payload);
        throw new Error('image_generation_unavailable');
      },

      image: {
        getConfig: async () => {
          if (roche && roche.ai && typeof roche.ai.getImageConfig === 'function') return await roche.ai.getImageConfig();
          if (roche && roche.image && typeof roche.image.getConfig === 'function') return await roche.image.getConfig();
          if (roche && roche.image && roche.image.config && typeof roche.image.config.get === 'function') return await roche.image.config.get();
          if (roche && typeof roche.getImageConfig === 'function') return await roche.getImageConfig();
          if (roche && typeof roche.invoke === 'function') return await roche.invoke('image.getConfig', {});
          return null;
        },
        generate: async payload => {
          if (roche && roche.ai && typeof roche.ai.generateImage === 'function') return await roche.ai.generateImage(toRocheImageArgs(payload));
          if (roche && roche.image && typeof roche.image.generate === 'function') return await roche.image.generate(payload);
          if (roche && roche.images && typeof roche.images.generate === 'function') return await roche.images.generate(payload);
          if (roche && typeof roche.generateImage === 'function') return await roche.generateImage(payload);
          if (roche && typeof roche.invoke === 'function') return await roche.invoke('image.generate', payload);
          throw new Error('image_generation_unavailable');
        }
      },

      async injectShortTerm(payload) {
        if (!payload || !payload.conversationId || !payload.summary) return false;

        const db = await openRocheDb();
        try {
          const now = Date.now();
          const msg = {
            id: now + Math.floor(Math.random() * 1000),
            isMe: false,
            text: payload.summary,
            senderId: payload.actorId,
            timestamp: now,
            senderName: payload.actorName,
            conversationId: payload.conversationId
          };
          if (String(payload.conversationId).slice(-8) === '_offline') {
            msg.isStreaming = false;
          }
          await addMessageRecord(db, msg);
          return true;
        } finally {
          try { db.close(); } catch (e) {}
        }
      }
    };
  }

  // v5.1.6 Roche-native runtime: no iframe, no ShadowRoot and no Proxy.
  // Important mobile fix: the INS app must NOT query/bind against Roche's whole document.
  // A tiny plain-object document facade scopes selectors and click handlers to the INS root
  // without the cost/complexity of the v5.1.4 Proxy runtime.
  function createNativeRuntime(rootEl) {
    const realWindow = window;
    const realDocument = document;
    const cleanups = [];
    const timeouts = new Set();
    const intervals = new Set();
    const rafs = new Set();

    const cssEscape = value => {
      const raw = String(value == null ? '' : value);
      if (realWindow.CSS && typeof realWindow.CSS.escape === 'function') return realWindow.CSS.escape(raw);
      return raw.replace(/[^a-zA-Z0-9_-]/g, ch => '\\' + ch);
    };
    const scopedDocument = {
      getElementById(id) { return rootEl.querySelector('#' + cssEscape(id)); },
      querySelector(selector) { return rootEl.querySelector(selector); },
      querySelectorAll(selector) { return rootEl.querySelectorAll(selector); },
      createElement(tag, options) { return realDocument.createElement(tag, options); },
      createDocumentFragment() { return realDocument.createDocumentFragment(); },
      createTextNode(text) { return realDocument.createTextNode(text); },
      get body() { return rootEl; },
      get documentElement() { return rootEl; },
      get hidden() { return realDocument.hidden; },
      addEventListener(type, fn, options) {
        const target = type === 'visibilitychange' ? realDocument : rootEl;
        target.addEventListener(type, fn, options);
        cleanups.push(() => target.removeEventListener(type, fn, options));
      },
      removeEventListener(type, fn, options) {
        const target = type === 'visibilitychange' ? realDocument : rootEl;
        target.removeEventListener(type, fn, options);
      }
    };

    const trackedSetTimeout = (fn, ms, ...args) => {
      const id = realWindow.setTimeout((...cbArgs) => { timeouts.delete(id); fn(...cbArgs); }, ms, ...args);
      timeouts.add(id); return id;
    };
    const trackedClearTimeout = id => { timeouts.delete(id); return realWindow.clearTimeout(id); };
    const trackedSetInterval = (fn, ms, ...args) => {
      const id = realWindow.setInterval(fn, ms, ...args); intervals.add(id); return id;
    };
    const trackedClearInterval = id => { intervals.delete(id); return realWindow.clearInterval(id); };
    const trackedRAF = fn => {
      const id = realWindow.requestAnimationFrame(ts => { rafs.delete(id); fn(ts); }); rafs.add(id); return id;
    };
    const trackedCancelRAF = id => { rafs.delete(id); return realWindow.cancelAnimationFrame(id); };

    // Window is only wrapped to track global listeners/timers. DOM lookup is NOT proxied/scoped.
    const local = Object.create(null);
    let appWindow = null;
    appWindow = new Proxy(local, {
      get(target, prop) {
        if (prop === 'window' || prop === 'self' || prop === 'globalThis') return appWindow;
        if (prop === 'document') return scopedDocument;
        if (prop === 'addEventListener') return (type, fn, options) => {
          realWindow.addEventListener(type, fn, options);
          cleanups.push(() => realWindow.removeEventListener(type, fn, options));
        };
        if (prop === 'removeEventListener') return realWindow.removeEventListener.bind(realWindow);
        if (prop === 'setTimeout') return trackedSetTimeout;
        if (prop === 'clearTimeout') return trackedClearTimeout;
        if (prop === 'setInterval') return trackedSetInterval;
        if (prop === 'clearInterval') return trackedClearInterval;
        if (prop === 'requestAnimationFrame') return trackedRAF;
        if (prop === 'cancelAnimationFrame') return trackedCancelRAF;
        if (Object.prototype.hasOwnProperty.call(target, prop)) return target[prop];
        const value = realWindow[prop];
        return typeof value === 'function' ? value.bind(realWindow) : value;
      },
      set(target, prop, value) { target[prop] = value; return true; },
      has(target, prop) { return Object.prototype.hasOwnProperty.call(target, prop) || prop in realWindow; }
    });

    return {
      document: scopedDocument,
      window: appWindow,
      setTimeout: trackedSetTimeout,
      clearTimeout: trackedClearTimeout,
      setInterval: trackedSetInterval,
      clearInterval: trackedClearInterval,
      requestAnimationFrame: trackedRAF,
      cancelAnimationFrame: trackedCancelRAF,
      cleanup() {
        for (const id of [...timeouts]) realWindow.clearTimeout(id);
        for (const id of [...intervals]) realWindow.clearInterval(id);
        for (const id of [...rafs]) realWindow.cancelAnimationFrame(id);
        timeouts.clear(); intervals.clear(); rafs.clear();
        while (cleanups.length) { try { cleanups.pop()(); } catch (e) {} }
      }
    };
  }

  // Parse the former Shadow DOM stylesheet in a detached document and prefix ordinary
  // selectors with .nini-ins-root. This keeps Roche styles isolated without Shadow DOM.
  function buildNativeScopedCSS(cssText) {
    try {
      const tempDoc = document.implementation.createHTMLDocument('nini-ins-style-scope');
      const style = tempDoc.createElement('style');
      style.textContent = String(cssText || '').replace(/:host\s*\{[^}]*\}/g, '');
      tempDoc.head.appendChild(style);

      const splitSelectors = text => {
        const out=[]; let buf=''; let depth=0;
        for (const ch of String(text||'')) {
          if (ch==='(' || ch==='[') depth++;
          else if (ch===')' || ch===']') depth=Math.max(0,depth-1);
          if (ch===',' && depth===0) { out.push(buf.trim()); buf=''; }
          else buf+=ch;
        }
        if (buf.trim()) out.push(buf.trim());
        return out;
      };
      const prefixSelector = sel => {
        if (!sel) return sel;
        if (sel.startsWith('.nini-ins-root')) return sel;
        if (sel === 'html' || sel === 'body' || sel === ':root') return '.nini-ins-root';
        if (sel.startsWith('html ') || sel.startsWith('body ')) return '.nini-ins-root ' + sel.replace(/^(html|body)\s+/, '');
        return '.nini-ins-root ' + sel;
      };
      const renderRules = rules => {
        let out='';
        for (const rule of Array.from(rules || [])) {
          if (rule.selectorText != null) {
            const scoped = splitSelectors(rule.selectorText).map(prefixSelector).join(',');
            out += scoped + '{' + rule.style.cssText + '}\n';
          } else if (rule.cssRules) {
            const head = String(rule.cssText || '').split('{')[0].trim();
            out += head + '{\n' + renderRules(rule.cssRules) + '}\n';
          } else {
            out += rule.cssText + '\n';
          }
        }
        return out;
      };
      return '.nini-ins-native-host{position:absolute;inset:0;display:block;overflow:hidden;min-width:0;min-height:0;background:#fff;}\n' +
             '.nini-ins-root{position:absolute;inset:0;width:100%;height:100%;min-width:0;min-height:0;overflow:hidden;background:#fff;color:#111;}\n' +
             renderRules(style.sheet && style.sheet.cssRules);
    } catch (e) {
      console.error('[INS] CSS scoping failed', e);
      return '.nini-ins-native-host,.nini-ins-root{position:absolute;inset:0;display:block;overflow:hidden;background:#fff;}';
    }
  }

  function installDirectBridges(appWindow, roche) {
    appWindow.NiniINSHost = {
      async closeApp() {
        try {
          if(appWindow && appWindow.NiniINSDMHistory && typeof appWindow.NiniINSDMHistory.flush==='function'){
            await appWindow.NiniINSDMHistory.flush();
          }
          if(appWindow && appWindow.NiniINSReelsPersistence && typeof appWindow.NiniINSReelsPersistence.flush==='function'){
            await appWindow.NiniINSReelsPersistence.flush();
          }
          if(appWindow && appWindow.NiniINSSocialMemory && typeof appWindow.NiniINSSocialMemory.flushBeforeUnmount==='function'){
            await appWindow.NiniINSSocialMemory.flushBeforeUnmount();
          }
        } catch(e) { console.warn('[INS] close flush failed',e); }
        try { if (roche && roche.ui && roche.ui.closeApp) return roche.ui.closeApp(); } catch (e) {}
      },
      async getImportedCharacterIds(){try{const x=await roche.storage.get('niniInsImportedCharacterIds');return Array.isArray(x)?x.map(String):[];}catch(e){return []; }},
      async setImportedCharacterIds(ids){const safe=Array.isArray(ids)?[...new Set(ids.map(String))]:[];await roche.storage.set('niniInsImportedCharacterIds',safe);return safe;},
      async getActiveUserPersona(){return await safeINSBoundUserPersona(roche);},
      async getAvailableUserPersonas(){return await safeUserPersonas(roche);},
      async setActiveUserPersona(personaId){return await safeSetINSBoundUserPersona(roche,personaId);},
      async getDMHistoryState(){try{const x=await roche.storage.get('niniInstagramDMHistoryV5');return x&&typeof x==='object'?x:{version:1,messages:{},sharedCards:{},updatedAt:0};}catch(e){return {version:1,messages:{},sharedCards:{},updatedAt:0};}},
      async setDMHistoryState(state){const safe=state&&typeof state==='object'?state:{version:1,messages:{},sharedCards:{},updatedAt:Date.now()};await roche.storage.set('niniInstagramDMHistoryV5',safe);return true;},
      async getReelsState(){try{const x=await roche.storage.get('niniInstagramReelsV1');return x&&typeof x==='object'?x:{version:1,items:[],comments:{},updatedAt:0};}catch(e){return {version:1,items:[],comments:{},updatedAt:0};}},
      async setReelsState(state){const safe=state&&typeof state==='object'?state:{version:1,items:[],comments:{},updatedAt:Date.now()};await roche.storage.set('niniInstagramReelsV1',safe);return true;},
      async clearPluginPersistentData(){SKIP_NEXT_UNMOUNT_FLUSH=true;try{await roche.storage.set('niniInsImportedCharacterIds',[]);}catch(e){}try{await roche.storage.set('niniInstagramDMHistoryV5',{version:1,messages:{},sharedCards:{},updatedAt:Date.now()});}catch(e){}try{await roche.storage.set('niniInstagramReelsV1',{version:1,items:[],comments:{},updatedAt:Date.now()});}catch(e){}try{await roche.storage.set(INS_BOUND_USER_PERSONA_KEY,'');}catch(e){}return true;},
      async closeAfterReset(){SKIP_NEXT_UNMOUNT_FLUSH=true;try{if(roche&&roche.ui&&roche.ui.closeApp)return roche.ui.closeApp();}catch(e){}}
    };
    appWindow.NiniINSRocheBridge = createRocheBridge(roche);
    appWindow.NiniINSAIBridge = createAIBridge(roche, () => appWindow);
  }


  function realThisHasTouch() {
    try {
      return (navigator.maxTouchPoints || 0) > 0 ||
        (window.matchMedia && window.matchMedia('(pointer: coarse)').matches);
    } catch (e) { return false; }
  }

  // ============================================================
  // 🎨 插件 App
  // ============================================================
  const pluginApp = {
    id: APP_ID,
    name: 'INS',
    iconImage: ICON_URL,

    async mount(container, roche) {
      ROCHE = roche;

      // Tear down any stale instance before mounting a new one.
      if (ACTIVE_DIRECT_RUNTIME) { try { ACTIVE_DIRECT_RUNTIME.cleanup(); } catch (e) {} ACTIVE_DIRECT_RUNTIME = null; }
      if (ACTIVE_DIRECT_STYLE && ACTIVE_DIRECT_STYLE.parentNode) ACTIVE_DIRECT_STYLE.parentNode.removeChild(ACTIVE_DIRECT_STYLE);
      ACTIVE_DIRECT_STYLE = null;
      if (ACTIVE_DIRECT_HOST && ACTIVE_DIRECT_HOST.parentNode) ACTIVE_DIRECT_HOST.parentNode.removeChild(ACTIVE_DIRECT_HOST);
      ACTIVE_DIRECT_HOST = null; ACTIVE_APP_WINDOW = null;

      // Roche owns the viewport. Do not force width/height/flex or run geometry observers.
      container.replaceChildren();
      container.style.padding = '0';
      container.style.margin = '0';
      container.style.overflow = 'hidden';
      container.style.minWidth = '0';
      container.style.minHeight = '0';
      if (!container.style.position || container.style.position === 'static') container.style.position = 'relative';

      // Same principle as the stable diary plugin: mount a visible root FIRST.
      const host = document.createElement('div');
      host.className = 'nini-ins-native-host';
      host.setAttribute('data-nini-ins-native-host','5.1.11');
      const rootEl = document.createElement('div');
      rootEl.className = 'nini-ins-root';
      host.appendChild(rootEl);
      container.appendChild(host);
      ACTIVE_DIRECT_HOST = host;

      // Direct-mount apps share Roche's real document. Let INS target/root handlers run,
      // then fence the complete gesture at the host boundary before Roche ancestors see it.
      // Roche mobile builds may listen to pointer/touch/mouse phases instead of click only.
      const stopRocheGestureLeak = event => {
        if (event && host.contains(event.target)) event.stopPropagation();
      };
      [
        'pointerdown','pointerup','pointercancel',
        'touchstart','touchend','touchcancel',
        'mousedown','mouseup','click','auxclick','dblclick','contextmenu'
      ].forEach(type=>host.addEventListener(type,stopRocheGestureLeak,false));

      // Some Android WebViews still synthesize a click after the finger has scrolled.
      // Cancel only that moved gesture in capture phase; normal taps continue unchanged.
      let mobilePointerGesture = null;
      host.addEventListener('pointerdown',event=>{
        if(event.isPrimary===false)return;
        mobilePointerGesture={
          pointerId:event.pointerId,
          x:event.clientX,
          y:event.clientY,
          moved:false,
          endedAt:0
        };
      },{capture:true,passive:true});
      host.addEventListener('pointermove',event=>{
        const gesture=mobilePointerGesture;
        if(!gesture || gesture.pointerId!==event.pointerId)return;
        if(Math.hypot(event.clientX-gesture.x,event.clientY-gesture.y)>12)gesture.moved=true;
      },{capture:true,passive:true});
      host.addEventListener('pointerup',event=>{
        const gesture=mobilePointerGesture;
        if(gesture && gesture.pointerId===event.pointerId)gesture.endedAt=Date.now();
      },{capture:true,passive:true});
      host.addEventListener('pointercancel',event=>{
        if(mobilePointerGesture && mobilePointerGesture.pointerId===event.pointerId)mobilePointerGesture=null;
      },{capture:true,passive:true});
      host.addEventListener('click',event=>{
        const gesture=mobilePointerGesture;
        if(!gesture)return;
        const recent=!gesture.endedAt || Date.now()-gesture.endedAt<800;
        mobilePointerGesture=null;
        if(!gesture.moved || !recent)return;
        event.preventDefault();
        event.stopImmediatePropagation();
      },true);

      // Install scoped CSS without Shadow DOM.
      const style = document.createElement('style');
      style.id = 'nini-ins-v5111-style';
      style.textContent = buildNativeScopedCSS(DIRECT_STYLE_TEXT);
      style.textContent += buildNativeScopedCSS(DM_SHARE_V120_STYLE);
      style.textContent += buildNativeScopedCSS(`
.feed-original-toggle{
  display:block;
  width:calc(100% - 28px);
  min-height:28px;
  margin:0 14px 8px;
  padding:3px 0;
  border:0;
  background:transparent;
  color:#70757d;
  text-align:right;
  font:inherit;
  font-size:11px;
  line-height:20px;
  cursor:pointer;
  touch-action:manipulation;
  -webkit-tap-highlight-color:transparent;
}
.feed-original-toggle:disabled{opacity:.58;cursor:default}
#profileFeedTranslationApiCard .profile-setting-notebox{word-break:break-word}
#profileFeedTranslationApiCard .profile-setting-save{margin-top:14px}
#profileImagePromptCard .profile-setting-notebox{word-break:break-word}
#profileImagePromptCard .ins-image-api-refresh{width:100%;height:38px;margin-top:10px}
#profileImagePromptCard .profile-setting-save{margin-top:14px}
#profileImagePromptCard #insImageGenerationTestBtn{width:100%}
#profileImagePromptCard .ins-image-test-note{margin-top:9px}
#profileImagePromptCard .ins-image-test-result{margin-top:10px;padding:10px 12px;border-radius:13px;background:#eef1f5;color:#59616c;font-size:12px;line-height:1.5}
#profileImagePromptCard .ins-image-test-result[data-state="success"]{background:#e9f7ee;color:#207342}
#profileImagePromptCard .ins-image-test-result[data-state="error"]{background:#fff0f0;color:#b44242}
#profileImagePromptCard .ins-image-test-result[hidden],#profileImagePromptCard .ins-image-test-preview[hidden]{display:none!important}
#profileImagePromptCard .ins-image-test-preview{margin-top:10px;overflow:hidden;border-radius:16px;background:#e8eaee;aspect-ratio:1/1}
#profileImagePromptCard .ins-image-test-preview img{display:block;width:100%;height:100%;object-fit:cover}
`);
      style.textContent += `
.nini-ins-native-host,.nini-ins-root{
  pointer-events:auto!important;
  touch-action:manipulation;
  -webkit-tap-highlight-color:transparent;
}
.nini-ins-root button,
.nini-ins-root input,
.nini-ins-root textarea,
.nini-ins-root select,
.nini-ins-root [role="button"],
.nini-ins-root .tab,
.nini-ins-root .act,
.nini-ins-root .story-wrap{
  touch-action:manipulation;
}

/* Ins v1.02: compact Home Story rail.
   Keep the visible avatar lighter while preserving a comfortable tap target. */
.nini-ins-root .stories{
  gap:12px;
  padding:9px 12px 10px;
  min-height:88px;
  align-items:flex-start;
}
.nini-ins-root .story{
  width:62px;
  flex:0 0 62px;
}
.nini-ins-root .story-wrap{
  width:56px;
  height:56px;
}
.nini-ins-root .story-wrap::after{
  content:"";
  position:absolute;
  inset:-4px;
  border-radius:50%;
}
.nini-ins-root .story-ring{
  padding:2.5px;
}
.nini-ins-root .story-inner{
  border-width:2px;
  font-size:10px;
}
.nini-ins-root .story-add{
  right:-3px;
  bottom:-3px;
  width:24px;
  height:24px;
}
.nini-ins-root .story-name{
  margin-top:4px;
  font-size:10px;
  line-height:14px;
}

/* Ins v1.02: compact DM avatar rail and speech-bubble user note. */
.nini-ins-root #page-dm .dm-avatar-strip{
  gap:10px;
  padding:27px 12px 9px 18px;
  min-height:96px;
  overflow-y:hidden;
}
.nini-ins-root #page-dm .dm-profile-item{
  width:60px;
  flex:0 0 60px;
  position:relative;
}
.nini-ins-root #page-dm .dm-profile-ring,
.nini-ins-root #page-dm .dm-profile-plus{
  width:48px;
  height:48px;
}
.nini-ins-root #page-dm .dm-profile-ring{
  font-size:11px;
}
.nini-ins-root #page-dm .dm-profile-plus{
  font-size:25px;
}
.nini-ins-root #page-dm .dm-profile-name{
  margin-top:4px;
  font-size:10px;
  line-height:13px;
}
.nini-ins-root #page-dm .dm-note-bubble{
  top:-24px;
  min-width:72px;
  max-width:96px;
  padding:7px 10px;
  border-radius:16px;
  border:1px solid rgba(220,222,228,.92);
  box-shadow:0 6px 18px rgba(0,0,0,.10);
  font-size:10px;
  line-height:13px;
  z-index:3;
}
.nini-ins-root #page-dm .dm-note-bubble::after{
  content:"";
  position:absolute;
  left:28px;
  bottom:-9px;
  width:7px;
  height:7px;
  border-radius:50%;
  background:#fff;
  border:1px solid rgba(220,222,228,.92);
  box-shadow:0 2px 5px rgba(0,0,0,.06);
}

/* Ins v1.02: persistent interface safe-area controls. Geometry is changed with
   inset values instead of transform scaling, so visible and tappable areas stay aligned. */
.nini-ins-root{
  --ins-safe-top:44px;
  --ins-safe-bottom:26px;
}
.nini-ins-root .phone > .screen{
  top:var(--ins-safe-top)!important;
  bottom:calc(68px + var(--ins-safe-bottom))!important;
}
.nini-ins-root .phone > .bottom-nav{
  bottom:var(--ins-safe-bottom)!important;
}
.nini-ins-root .phone > .bottom-nav::after{
  content:"";
  position:absolute;
  left:0;
  right:0;
  top:100%;
  height:var(--ins-safe-bottom);
  background:inherit;
  pointer-events:none;
}
.nini-ins-root .profile-drawer-dim{
  top:var(--ins-safe-top)!important;
  bottom:calc(68px + var(--ins-safe-bottom))!important;
}
.nini-ins-root .profile-drawer{
  top:var(--ins-safe-top)!important;
  bottom:calc(68px + var(--ins-safe-bottom))!important;
}
.nini-ins-root .phone > .route-page,
.nini-ins-root .phone > .create-post-overlay,
.nini-ins-root .phone > .highlight-viewer{
  top:var(--ins-safe-top)!important;
  bottom:var(--ins-safe-bottom)!important;
  height:auto!important;
}
.nini-ins-root .profile-setting-card{
  bottom:calc(14px + var(--ins-safe-bottom))!important;
  max-height:calc(100% - 28px - var(--ins-safe-top) - var(--ins-safe-bottom))!important;
}

/* v1.1.1: Feed translation API and Image settings use centered cards. */
.nini-ins-root #profileFeedTranslationApiCard,
.nini-ins-root #profileImagePromptCard{
  left:50%!important;
  right:auto!important;
  top:50%!important;
  bottom:auto!important;
  width:min(390px,calc(100% - 28px))!important;
  max-height:min(620px,calc(100% - var(--ins-safe-top) - var(--ins-safe-bottom) - 48px))!important;
  margin:0!important;
  transform:translate(-50%,-50%) scale(.985)!important;
  z-index:320!important;
  padding:14px!important;
  overscroll-behavior:contain;
  -webkit-overflow-scrolling:touch;
}
.nini-ins-root #profileFeedTranslationApiCard.show,
.nini-ins-root #profileImagePromptCard.show{
  transform:translate(-50%,-50%) scale(1)!important;
}

/* v1.08: only the INS 副 API card follows the centered Feed-translation card layout. */
.nini-ins-root #profileSubApiCard{
  left:50%!important;
  right:auto!important;
  top:50%!important;
  bottom:auto!important;
  width:min(390px,calc(100% - 28px))!important;
  max-height:min(620px,calc(100% - var(--ins-safe-top) - var(--ins-safe-bottom) - 48px))!important;
  margin:0!important;
  transform:translate(-50%,-50%) scale(.985)!important;
  z-index:320!important;
  padding:14px!important;
  overflow-y:auto!important;
  overscroll-behavior:contain;
  -webkit-overflow-scrolling:touch;
}
.nini-ins-root #profileSubApiCard.show{
  transform:translate(-50%,-50%) scale(1)!important;
}
.nini-ins-root #profileSubApiCard .profile-setting-head{
  position:sticky;
  top:-14px;
  z-index:4;
  margin:-14px -4px 10px;
  padding:14px 4px 8px;
  background:linear-gradient(180deg,rgba(247,248,250,.99) 0%,rgba(247,248,250,.95) 82%,rgba(247,248,250,0) 100%);
  -webkit-backdrop-filter:blur(22px);
  backdrop-filter:blur(22px);
}
.nini-ins-root #profileFeedTranslationApiCard .profile-setting-head,
.nini-ins-root #profileImagePromptCard .profile-setting-head{
  position:sticky;
  top:-14px;
  z-index:4;
  margin:-14px -4px 10px;
  padding:14px 4px 8px;
  background:linear-gradient(180deg,rgba(247,248,250,.99) 0%,rgba(247,248,250,.95) 82%,rgba(247,248,250,0) 100%);
  -webkit-backdrop-filter:blur(22px);
  backdrop-filter:blur(22px);
}
.nini-ins-root .ins-interface-size-panel{
  position:absolute;
  left:12px;
  right:12px;
  top:calc(var(--ins-safe-top) + 14px);
  bottom:auto;
  width:auto;
  max-height:calc(100% - var(--ins-safe-top) - var(--ins-safe-bottom) - 28px);
  z-index:260;
  display:none;
  flex-direction:column;
  overflow:hidden;
  border:1px solid rgba(255,255,255,.82);
  border-radius:26px;
  background:rgba(247,248,250,.94);
  color:#15171a;
  box-shadow:0 22px 58px rgba(15,20,30,.24);
  -webkit-backdrop-filter:blur(28px) saturate(1.35);
  backdrop-filter:blur(28px) saturate(1.35);
  pointer-events:auto;
  isolation:isolate;
}
.nini-ins-root .ins-interface-size-panel.show{display:flex}
.nini-ins-root .ins-interface-size-head{
  flex:0 0 58px;
  padding:0 14px 0 18px;
  display:flex;
  align-items:center;
  justify-content:space-between;
  border-bottom:1px solid rgba(0,0,0,.055);
  background:rgba(255,255,255,.38);
}
.nini-ins-root .ins-interface-size-head strong{
  font-size:18px;
  font-weight:850;
  letter-spacing:-.2px;
}
.nini-ins-root .ins-interface-size-head button{
  position:relative;
  width:40px;
  height:40px;
  border:0;
  border-radius:50%;
  background:rgba(255,255,255,.76);
  color:#111;
  font-size:25px;
  line-height:1;
  cursor:pointer;
  pointer-events:auto;
  touch-action:manipulation;
}
.nini-ins-root .ins-interface-size-head button::after{
  content:"";
  position:absolute;
  inset:-5px;
  border-radius:50%;
}
.nini-ins-root .ins-interface-size-body{
  flex:1 1 auto;
  min-height:0;
  overflow-y:auto;
  padding:14px 14px 4px;
  overscroll-behavior:contain;
  -webkit-overflow-scrolling:touch;
}
.nini-ins-root .ins-interface-size-help{
  margin:0 2px 12px;
  color:#858a92;
  font-size:12px;
  line-height:1.55;
}
.nini-ins-root .ins-interface-size-row{
  min-height:96px;
  display:grid;
  grid-template-columns:88px minmax(0,1fr);
  column-gap:12px;
  align-items:center;
  margin-bottom:10px;
  padding:14px;
  border:1px solid rgba(255,255,255,.86);
  border-radius:19px;
  background:rgba(255,255,255,.72);
  box-shadow:0 8px 24px rgba(31,37,47,.035);
}
.nini-ins-root .ins-interface-size-row-top,
.nini-ins-root .ins-interface-size-row-bottom{
  min-height:122px;
}
.nini-ins-root .ins-interface-size-copy{
  grid-column:1;
  align-self:center;
}
.nini-ins-root .ins-interface-size-copy strong{
  display:block;
  font-size:14px;
  line-height:1.45;
}
.nini-ins-root .ins-interface-size-copy span{
  display:block;
  margin-top:3px;
  color:#111;
  font-size:13px;
  font-weight:800;
}
.nini-ins-root .ins-interface-size-row p{
  grid-column:2;
  margin:0 0 7px;
  color:#8b9097;
  font-size:11px;
  line-height:1.5;
}
.nini-ins-root .ins-interface-size-row input[type="range"]{
  grid-column:2;
  width:100%;
  min-width:0;
  height:42px;
  margin:0;
  accent-color:#111;
  cursor:pointer;
  touch-action:none;
}
.nini-ins-root .ins-interface-size-row-top p,
.nini-ins-root .ins-interface-size-row-bottom p{
  grid-row:1;
}
.nini-ins-root .ins-interface-size-row-top input[type="range"],
.nini-ins-root .ins-interface-size-row-bottom input[type="range"]{
  grid-row:2;
}
.nini-ins-root .ins-interface-size-actions{
  position:relative;
  z-index:3;
  flex:0 0 auto;
  display:grid;
  grid-template-columns:1fr 1fr;
  gap:10px;
  padding:10px 14px 14px;
  background:transparent;
  pointer-events:auto;
}
.nini-ins-root .ins-interface-size-actions button{
  position:relative;
  z-index:4;
  min-height:50px;
  border:0;
  border-radius:15px;
  background:#111;
  color:#fff;
  font:inherit;
  font-size:14px;
  font-weight:850;
  cursor:pointer;
  pointer-events:auto!important;
  touch-action:manipulation;
  -webkit-tap-highlight-color:transparent;
}
.nini-ins-root .ins-interface-size-actions .secondary{
  background:#26272a;
  color:#fff;
}
.nini-ins-root .ins-interface-size-actions .primary{
  background:#111;
  color:#fff;
}
.nini-ins-root .ins-interface-size-actions button:active{
  transform:scale(.985);
  opacity:.88;
}

/* v1.03 global mobile hit adaptation. Invisible expansion follows the visual
   control, so it improves taps without changing layout or overlapping content. */
.nini-ins-root button,
.nini-ins-root [role="button"],
.nini-ins-root .act,
.nini-ins-root .tab{
  -webkit-tap-highlight-color:transparent;
  touch-action:manipulation;
}
.nini-ins-root button[class*="close"],
.nini-ins-root button[class*="back"],
.nini-ins-root .drawer-close,
.nini-ins-root .menu-btn,
.nini-ins-root .user-profile-menu,
.nini-ins-root .reels-v15-topbtn{
  position:relative;
  overflow:visible;
}
.nini-ins-root button[class*="close"]::after,
.nini-ins-root button[class*="back"]::after,
.nini-ins-root .drawer-close::after,
.nini-ins-root .menu-btn::after,
.nini-ins-root .user-profile-menu::after,
.nini-ins-root .reels-v15-topbtn::after{
  content:"";
  position:absolute;
  inset:-6px;
  border-radius:inherit;
}
.nini-ins-root .phone > .bottom-nav .tab{
  min-height:54px;
}

/* v1.04.2 Feed hit routing: keep the actual controls above media/overlay layers.
   Children do not steal the hit; the semantic parent receives the tap consistently. */
.nini-ins-root .post-head,
.nini-ins-root .post-right,
.nini-ins-root .actions,
.nini-ins-root .caption{position:relative;z-index:3;pointer-events:auto!important}
.nini-ins-root .menu-btn,
.nini-ins-root .actions .act,
.nini-ins-root .caption .more{
  position:relative;
  z-index:4;
  pointer-events:auto!important;
  touch-action:manipulation!important;
  -webkit-tap-highlight-color:transparent;
}
.nini-ins-root .menu-btn svg,
.nini-ins-root .menu-btn path,
.nini-ins-root .actions .act > *,
.nini-ins-root .caption .more > *{pointer-events:none!important}
.nini-ins-root .menu-btn{min-width:34px;min-height:34px}
.nini-ins-root .actions .act{min-height:38px}
.nini-ins-root .caption .more{display:inline-block;min-height:28px;line-height:28px;margin-left:2px}

/* v1.06.1: keep the visible DM composer above covered root navigation. */
.nini-ins-root #routeDMChat.show .dm-composer-wrap,
.nini-ins-root #routeDMChat.show .dm-composer,
.nini-ins-root #routeDMChat.show #dmAIPlaneBtn{
  pointer-events:auto!important;
}
.nini-ins-root #routeDMChat.show .dm-composer-wrap{z-index:150!important}

/* v1.06 DM-only interaction fixes */
.nini-ins-root #dmSearchInput{pointer-events:none!important;caret-color:transparent!important;user-select:none!important;-webkit-user-select:none!important}
.nini-ins-root #dmAIPlaneBtn{width:46px!important;height:32px!important;min-width:46px!important;min-height:32px!important;touch-action:manipulation!important}
.nini-ins-root #dmAIPlaneBtn:disabled{opacity:.46!important;cursor:default!important}
.nini-ins-root #dmAIPlaneBtn .plane-img{pointer-events:none!important}
.nini-ins-root .dm-menu-action#dmClearOpenBtn{justify-content:flex-start!important}
.nini-ins-root #dmChatMessages [data-dm-msg-id]{touch-action:pan-y!important;-webkit-user-select:none;user-select:none}

/* v1.07.1 targeted DM UI only */
.nini-ins-root #dmAIPlaneBtn.dm-sent-btn{
  border:0!important;border-radius:16px!important;background:#111!important;color:#fff!important;
  font-size:12px!important;font-weight:750!important;line-height:32px!important;padding:0!important;
  letter-spacing:.1px!important;display:grid!important;place-items:center!important;
}
.nini-ins-root .dm-bubble-translatable{cursor:pointer}
.nini-ins-root .dm-bubble-translation{
  margin-top:8px;padding-top:8px;border-top:1px solid rgba(127,127,127,.22);
  font-size:12px;line-height:1.55;opacity:.82;
}

/* v1.04.1: only the successful comment batching UI is carried over from v1.04. */
.nini-ins-root .feed-comment-load-more{
  width:calc(100% - 28px);
  min-height:38px;
  margin:3px 14px 10px;
  border:0;
  border-radius:12px;
  background:#f0f1f4;
  color:#777c84;
  font:inherit;
  font-size:12px;
  font-weight:750;
  cursor:pointer;
  touch-action:manipulation;
}
.nini-ins-root .feed-comment-load-more:disabled{opacity:.58;cursor:default}
.nini-ins-root .feed-comment-load-more[hidden]{display:none}

`;

      // Android/mobile WebView compatibility: remove expensive compositor effects.
      // Desktop keeps the full visual treatment. This is intentionally aggressive for
      // diagnosis: if mobile becomes stable, we can selectively restore effects later.
      const isMobileLite = /Android|Mobile/i.test(navigator.userAgent || '') ||
        (realThisHasTouch() && Math.min(window.innerWidth || 9999, window.innerHeight || 9999) < 900);
      if (isMobileLite) {
        rootEl.classList.add('nini-ins-mobile-lite');
        style.textContent += `
.nini-ins-root.nini-ins-mobile-lite *,
.nini-ins-root.nini-ins-mobile-lite *::before,
.nini-ins-root.nini-ins-mobile-lite *::after{
  -webkit-backdrop-filter:none!important;
  backdrop-filter:none!important;
  filter:none!important;
  transition-duration:0.001ms!important;
  transition-delay:0ms!important;
  animation-duration:0.001ms!important;
  animation-delay:0ms!important;
  animation-iteration-count:1!important;
  scroll-behavior:auto!important;
  will-change:auto!important;
}
.nini-ins-root.nini-ins-mobile-lite .page:not(.active),
.nini-ins-root.nini-ins-mobile-lite .route-page:not(.show),
.nini-ins-root.nini-ins-mobile-lite .create-post-overlay:not(.show),
.nini-ins-root.nini-ins-mobile-lite .sheet-backdrop:not(.show),
.nini-ins-root.nini-ins-mobile-lite .cp-modal:not(.show),
.nini-ins-root.nini-ins-mobile-lite .profile-content-detail-route:not(.show),
.nini-ins-root.nini-ins-mobile-lite .highlight-viewer:not(.show),
.nini-ins-root.nini-ins-mobile-lite .user-repost-preview-dim:not(.show),
.nini-ins-root.nini-ins-mobile-lite .route-sheet-backdrop:not(.show),
.nini-ins-root.nini-ins-mobile-lite .feed-glass-dim:not(.show),
.nini-ins-root.nini-ins-mobile-lite .feed-glass-panel:not(.show),
.nini-ins-root.nini-ins-mobile-lite .post-glass-dim:not(.show),
.nini-ins-root.nini-ins-mobile-lite .post-action-glass:not(.show),
.nini-ins-root.nini-ins-mobile-lite .post-edit-glass:not(.show),
.nini-ins-root.nini-ins-mobile-lite .post-delete-confirm:not(.show),
.nini-ins-root.nini-ins-mobile-lite #sheetComments:not(.show),
.nini-ins-root.nini-ins-mobile-lite .dm-chat-glass-dim:not(.show),
.nini-ins-root.nini-ins-mobile-lite .dm-chat-glass-card:not(.show),
.nini-ins-root.nini-ins-mobile-lite .profile-drawer-dim:not(.show),
.nini-ins-root.nini-ins-mobile-lite .profile-drawer:not(.show),
.nini-ins-root.nini-ins-mobile-lite .profile-edit-card:not(.show),
.nini-ins-root.nini-ins-mobile-lite .highlight-editor-card:not(.show),
.nini-ins-root.nini-ins-mobile-lite .profile-setting-card:not(.show),
.nini-ins-root.nini-ins-mobile-lite .dm-list-glass-dim:not(.show),
.nini-ins-root.nini-ins-mobile-lite .dm-list-glass-card:not(.show),
.nini-ins-root.nini-ins-mobile-lite .char-drawer-dim:not(.show),
.nini-ins-root.nini-ins-mobile-lite .char-drawer:not(.show),
.nini-ins-root.nini-ins-mobile-lite .char-quick-card:not(.show),
.nini-ins-root.nini-ins-mobile-lite .char-highlight-editor:not(.show),
.nini-ins-root.nini-ins-mobile-lite .char-profile-editor:not(.show),
.nini-ins-root.nini-ins-mobile-lite .char-post-dim:not(.show),
.nini-ins-root.nini-ins-mobile-lite .char-post-card:not(.show),
.nini-ins-root.nini-ins-mobile-lite .char-album-dim:not(.show),
.nini-ins-root.nini-ins-mobile-lite .char-album-manager:not(.show),
.nini-ins-root.nini-ins-mobile-lite .reels-v15-dim:not(.show),
.nini-ins-root.nini-ins-mobile-lite .reels-v15-sheet:not(.show),
.nini-ins-root.nini-ins-mobile-lite .story-overlay-dim:not(.show),
.nini-ins-root.nini-ins-mobile-lite .story-sheet:not(.show),
.nini-ins-root.nini-ins-mobile-lite .pr-sheet-dim:not(.show),
.nini-ins-root.nini-ins-mobile-lite .pr-sheet:not(.show),
.nini-ins-root.nini-ins-mobile-lite .ins-memory-sheet-dim:not(.show),
.nini-ins-root.nini-ins-mobile-lite .ins-memory-sheet:not(.show){
  display:none!important;
  pointer-events:none!important;
  visibility:hidden!important;
}
.nini-ins-root.nini-ins-mobile-lite .nav-inner{height:100%;}
.nini-ins-root.nini-ins-mobile-lite .bottom-nav .tab{
  width:20%;
  flex:1 1 20%;
}
.nini-ins-root.nini-ins-mobile-lite .bottom-nav .tab::after{
  content:"";
  position:absolute;
  inset:-8px 0;
}
.nini-ins-root.nini-ins-mobile-lite button[class*="close"],
.nini-ins-root.nini-ins-mobile-lite button[class*="back"],
.nini-ins-root.nini-ins-mobile-lite .drawer-close{
  position:relative;
  overflow:visible;
}
.nini-ins-root.nini-ins-mobile-lite button[class*="close"]::after,
.nini-ins-root.nini-ins-mobile-lite button[class*="back"]::after,
.nini-ins-root.nini-ins-mobile-lite .drawer-close::after{
  content:"";
  position:absolute;
  inset:-7px;
}
`;
      }
      document.head.appendChild(style);
      ACTIVE_DIRECT_STYLE = style;

      // Only after the root exists do we inject the app DOM and bootstrap JS.
      rootEl.innerHTML = DIRECT_BODY_HTML;
      const runtime = createNativeRuntime(rootEl);
      ACTIVE_DIRECT_RUNTIME = runtime;
      ACTIVE_APP_WINDOW = runtime.window;
      runtime.window.__NiniINSMobileLite = isMobileLite;
      installDirectBridges(runtime.window, roche);

      try {
        runDirectApp(
          runtime.document,
          runtime.window,
          runtime.setTimeout,
          runtime.clearTimeout,
          runtime.setInterval,
          runtime.clearInterval,
          runtime.requestAnimationFrame,
          runtime.cancelAnimationFrame
        );
      } catch (e) {
        console.error('[INS] native app bootstrap failed', e);
        rootEl.innerHTML = '<div style="padding:24px;font:14px/1.6 system-ui;color:#b42318;background:#fff;white-space:pre-wrap">INS 启动失败：' + String(e && (e.stack||e.message||e) || 'unknown') + '</div>';
        return;
      }

      try {
        const [persona, characters, importedIds, dmState, reelsState] = await Promise.all([
          safeINSBoundUserPersona(roche),
          safeCharacters(roche),
          (async()=>{try{const x=await roche.storage.get('niniInsImportedCharacterIds');return Array.isArray(x)?x.map(String):[];}catch(e){return [];}})(),
          (async()=>{try{const x=await roche.storage.get('niniInstagramDMHistoryV5');return x&&typeof x==='object'?x:{version:1,messages:{},sharedCards:{},updatedAt:0};}catch(e){return {version:1,messages:{},sharedCards:{},updatedAt:0};}})(),
          (async()=>{try{const x=await roche.storage.get('niniInstagramReelsV1');return x&&typeof x==='object'?x:{version:1,items:[],comments:{},updatedAt:0};}catch(e){return {version:1,items:[],comments:{},updatedAt:0};}})()
        ]);
        if (runtime.window && runtime.window.NiniINSRocheIdentity && typeof runtime.window.NiniINSRocheIdentity.hydrate === 'function') {
          runtime.window.NiniINSRocheIdentity.hydrate({ persona, characters, importedIds, dmState, reelsState });
        }
      } catch (e) {
        console.error('[INS] Roche hydration failed', e);
        const err = document.createElement('div');
        err.style.cssText='position:absolute;left:12px;right:12px;bottom:12px;z-index:9999;padding:10px 12px;border-radius:12px;background:#fff1f0;color:#b42318;font:12px/1.5 system-ui;box-shadow:0 4px 18px rgba(0,0,0,.12)';
        err.textContent='INS 数据加载失败：'+String(e && (e.message||e) || 'unknown');
        rootEl.appendChild(err);
      }
    },
    async unmount(container) {
      const appWindow = ACTIVE_APP_WINDOW;
      try {
        const skipFlush = SKIP_NEXT_UNMOUNT_FLUSH || !!(appWindow && appWindow.__NiniINSResetting);
        if (!skipFlush && appWindow && appWindow.NiniINSDMHistory && typeof appWindow.NiniINSDMHistory.flush === 'function') await appWindow.NiniINSDMHistory.flush();
        if (!skipFlush && appWindow && appWindow.NiniINSReelsPersistence && typeof appWindow.NiniINSReelsPersistence.flush === 'function') await appWindow.NiniINSReelsPersistence.flush();
        if (!skipFlush && appWindow && appWindow.NiniINSSocialMemory && typeof appWindow.NiniINSSocialMemory.flushBeforeUnmount === 'function') await appWindow.NiniINSSocialMemory.flushBeforeUnmount();
      } catch (e) {
        console.warn('[INS] close-time flush failed', e);
      } finally {
        SKIP_NEXT_UNMOUNT_FLUSH = false;
      }
      if (ACTIVE_DIRECT_RUNTIME) { try { ACTIVE_DIRECT_RUNTIME.cleanup(); } catch (e) {} }
      ACTIVE_DIRECT_RUNTIME = null;
      ACTIVE_APP_WINDOW = null;
      if (ACTIVE_DIRECT_STYLE && ACTIVE_DIRECT_STYLE.parentNode) ACTIVE_DIRECT_STYLE.parentNode.removeChild(ACTIVE_DIRECT_STYLE);
      ACTIVE_DIRECT_STYLE = null;
      if (container) container.replaceChildren();
      ACTIVE_DIRECT_HOST = null;
    }
  };

  // ============================================================
  // 📦 注册插件
  // ============================================================
  window.RochePlugin.register({
    id: PLUGIN_ID,
    name: 'INS',
    version: VERSION,
    description: 'INS',
    author: 'Raccoon001',
    apps: [pluginApp]
  });

  // 可选调试接口
  window.__NiniINS = {
    version: VERSION,
    pluginId: PLUGIN_ID,
    appId: APP_ID,
    get frame() { return null; },
    get host() { return ACTIVE_DIRECT_HOST; },
    get shadowRoot() { return null; },
    get styleElement() { return ACTIVE_DIRECT_STYLE; },
    get appWindow() { return ACTIVE_APP_WINDOW; },
    get roche() { return ROCHE; }
  };
})();